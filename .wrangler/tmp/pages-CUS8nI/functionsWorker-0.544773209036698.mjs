var __defProp = Object.defineProperty;
var __name = (target, value) => __defProp(target, "name", { value, configurable: true });

// ../node_modules/unenv/dist/runtime/_internal/utils.mjs
// @__NO_SIDE_EFFECTS__
function createNotImplementedError(name) {
  return new Error(`[unenv] ${name} is not implemented yet!`);
}
__name(createNotImplementedError, "createNotImplementedError");
// @__NO_SIDE_EFFECTS__
function notImplemented(name) {
  const fn = /* @__PURE__ */ __name(() => {
    throw /* @__PURE__ */ createNotImplementedError(name);
  }, "fn");
  return Object.assign(fn, { __unenv__: true });
}
__name(notImplemented, "notImplemented");
// @__NO_SIDE_EFFECTS__
function notImplementedClass(name) {
  return class {
    __unenv__ = true;
    constructor() {
      throw new Error(`[unenv] ${name} is not implemented yet!`);
    }
  };
}
__name(notImplementedClass, "notImplementedClass");

// ../node_modules/unenv/dist/runtime/node/internal/perf_hooks/performance.mjs
var _timeOrigin = globalThis.performance?.timeOrigin ?? Date.now();
var _performanceNow = globalThis.performance?.now ? globalThis.performance.now.bind(globalThis.performance) : () => Date.now() - _timeOrigin;
var nodeTiming = {
  name: "node",
  entryType: "node",
  startTime: 0,
  duration: 0,
  nodeStart: 0,
  v8Start: 0,
  bootstrapComplete: 0,
  environment: 0,
  loopStart: 0,
  loopExit: 0,
  idleTime: 0,
  uvMetricsInfo: {
    loopCount: 0,
    events: 0,
    eventsWaiting: 0
  },
  detail: void 0,
  toJSON() {
    return this;
  }
};
var PerformanceEntry = class {
  static {
    __name(this, "PerformanceEntry");
  }
  __unenv__ = true;
  detail;
  entryType = "event";
  name;
  startTime;
  constructor(name, options) {
    this.name = name;
    this.startTime = options?.startTime || _performanceNow();
    this.detail = options?.detail;
  }
  get duration() {
    return _performanceNow() - this.startTime;
  }
  toJSON() {
    return {
      name: this.name,
      entryType: this.entryType,
      startTime: this.startTime,
      duration: this.duration,
      detail: this.detail
    };
  }
};
var PerformanceMark = class PerformanceMark2 extends PerformanceEntry {
  static {
    __name(this, "PerformanceMark");
  }
  entryType = "mark";
  constructor() {
    super(...arguments);
  }
  get duration() {
    return 0;
  }
};
var PerformanceMeasure = class extends PerformanceEntry {
  static {
    __name(this, "PerformanceMeasure");
  }
  entryType = "measure";
};
var PerformanceResourceTiming = class extends PerformanceEntry {
  static {
    __name(this, "PerformanceResourceTiming");
  }
  entryType = "resource";
  serverTiming = [];
  connectEnd = 0;
  connectStart = 0;
  decodedBodySize = 0;
  domainLookupEnd = 0;
  domainLookupStart = 0;
  encodedBodySize = 0;
  fetchStart = 0;
  initiatorType = "";
  name = "";
  nextHopProtocol = "";
  redirectEnd = 0;
  redirectStart = 0;
  requestStart = 0;
  responseEnd = 0;
  responseStart = 0;
  secureConnectionStart = 0;
  startTime = 0;
  transferSize = 0;
  workerStart = 0;
  responseStatus = 0;
};
var PerformanceObserverEntryList = class {
  static {
    __name(this, "PerformanceObserverEntryList");
  }
  __unenv__ = true;
  getEntries() {
    return [];
  }
  getEntriesByName(_name, _type) {
    return [];
  }
  getEntriesByType(type) {
    return [];
  }
};
var Performance = class {
  static {
    __name(this, "Performance");
  }
  __unenv__ = true;
  timeOrigin = _timeOrigin;
  eventCounts = /* @__PURE__ */ new Map();
  _entries = [];
  _resourceTimingBufferSize = 0;
  navigation = void 0;
  timing = void 0;
  timerify(_fn, _options) {
    throw createNotImplementedError("Performance.timerify");
  }
  get nodeTiming() {
    return nodeTiming;
  }
  eventLoopUtilization() {
    return {};
  }
  markResourceTiming() {
    return new PerformanceResourceTiming("");
  }
  onresourcetimingbufferfull = null;
  now() {
    if (this.timeOrigin === _timeOrigin) {
      return _performanceNow();
    }
    return Date.now() - this.timeOrigin;
  }
  clearMarks(markName) {
    this._entries = markName ? this._entries.filter((e) => e.name !== markName) : this._entries.filter((e) => e.entryType !== "mark");
  }
  clearMeasures(measureName) {
    this._entries = measureName ? this._entries.filter((e) => e.name !== measureName) : this._entries.filter((e) => e.entryType !== "measure");
  }
  clearResourceTimings() {
    this._entries = this._entries.filter((e) => e.entryType !== "resource" || e.entryType !== "navigation");
  }
  getEntries() {
    return this._entries;
  }
  getEntriesByName(name, type) {
    return this._entries.filter((e) => e.name === name && (!type || e.entryType === type));
  }
  getEntriesByType(type) {
    return this._entries.filter((e) => e.entryType === type);
  }
  mark(name, options) {
    const entry = new PerformanceMark(name, options);
    this._entries.push(entry);
    return entry;
  }
  measure(measureName, startOrMeasureOptions, endMark) {
    let start;
    let end;
    if (typeof startOrMeasureOptions === "string") {
      start = this.getEntriesByName(startOrMeasureOptions, "mark")[0]?.startTime;
      end = this.getEntriesByName(endMark, "mark")[0]?.startTime;
    } else {
      start = Number.parseFloat(startOrMeasureOptions?.start) || this.now();
      end = Number.parseFloat(startOrMeasureOptions?.end) || this.now();
    }
    const entry = new PerformanceMeasure(measureName, {
      startTime: start,
      detail: {
        start,
        end
      }
    });
    this._entries.push(entry);
    return entry;
  }
  setResourceTimingBufferSize(maxSize) {
    this._resourceTimingBufferSize = maxSize;
  }
  addEventListener(type, listener, options) {
    throw createNotImplementedError("Performance.addEventListener");
  }
  removeEventListener(type, listener, options) {
    throw createNotImplementedError("Performance.removeEventListener");
  }
  dispatchEvent(event) {
    throw createNotImplementedError("Performance.dispatchEvent");
  }
  toJSON() {
    return this;
  }
};
var PerformanceObserver = class {
  static {
    __name(this, "PerformanceObserver");
  }
  __unenv__ = true;
  static supportedEntryTypes = [];
  _callback = null;
  constructor(callback) {
    this._callback = callback;
  }
  takeRecords() {
    return [];
  }
  disconnect() {
    throw createNotImplementedError("PerformanceObserver.disconnect");
  }
  observe(options) {
    throw createNotImplementedError("PerformanceObserver.observe");
  }
  bind(fn) {
    return fn;
  }
  runInAsyncScope(fn, thisArg, ...args) {
    return fn.call(thisArg, ...args);
  }
  asyncId() {
    return 0;
  }
  triggerAsyncId() {
    return 0;
  }
  emitDestroy() {
    return this;
  }
};
var performance = globalThis.performance && "addEventListener" in globalThis.performance ? globalThis.performance : new Performance();

// ../node_modules/@cloudflare/unenv-preset/dist/runtime/polyfill/performance.mjs
if (!("__unenv__" in performance)) {
  const proto = Performance.prototype;
  for (const key of Object.getOwnPropertyNames(proto)) {
    if (key !== "constructor" && !(key in performance)) {
      const desc3 = Object.getOwnPropertyDescriptor(proto, key);
      if (desc3) {
        Object.defineProperty(performance, key, desc3);
      }
    }
  }
}
globalThis.performance = performance;
globalThis.Performance = Performance;
globalThis.PerformanceEntry = PerformanceEntry;
globalThis.PerformanceMark = PerformanceMark;
globalThis.PerformanceMeasure = PerformanceMeasure;
globalThis.PerformanceObserver = PerformanceObserver;
globalThis.PerformanceObserverEntryList = PerformanceObserverEntryList;
globalThis.PerformanceResourceTiming = PerformanceResourceTiming;

// ../node_modules/unenv/dist/runtime/node/console.mjs
import { Writable } from "node:stream";

// ../node_modules/unenv/dist/runtime/mock/noop.mjs
var noop_default = Object.assign(() => {
}, { __unenv__: true });

// ../node_modules/unenv/dist/runtime/node/console.mjs
var _console = globalThis.console;
var _ignoreErrors = true;
var _stderr = new Writable();
var _stdout = new Writable();
var log = _console?.log ?? noop_default;
var info = _console?.info ?? log;
var trace = _console?.trace ?? info;
var debug = _console?.debug ?? log;
var table = _console?.table ?? log;
var error = _console?.error ?? log;
var warn = _console?.warn ?? error;
var createTask = _console?.createTask ?? /* @__PURE__ */ notImplemented("console.createTask");
var clear = _console?.clear ?? noop_default;
var count = _console?.count ?? noop_default;
var countReset = _console?.countReset ?? noop_default;
var dir = _console?.dir ?? noop_default;
var dirxml = _console?.dirxml ?? noop_default;
var group = _console?.group ?? noop_default;
var groupEnd = _console?.groupEnd ?? noop_default;
var groupCollapsed = _console?.groupCollapsed ?? noop_default;
var profile = _console?.profile ?? noop_default;
var profileEnd = _console?.profileEnd ?? noop_default;
var time = _console?.time ?? noop_default;
var timeEnd = _console?.timeEnd ?? noop_default;
var timeLog = _console?.timeLog ?? noop_default;
var timeStamp = _console?.timeStamp ?? noop_default;
var Console = _console?.Console ?? /* @__PURE__ */ notImplementedClass("console.Console");
var _times = /* @__PURE__ */ new Map();
var _stdoutErrorHandler = noop_default;
var _stderrErrorHandler = noop_default;

// ../node_modules/@cloudflare/unenv-preset/dist/runtime/node/console.mjs
var workerdConsole = globalThis["console"];
var {
  assert,
  clear: clear2,
  // @ts-expect-error undocumented public API
  context,
  count: count2,
  countReset: countReset2,
  // @ts-expect-error undocumented public API
  createTask: createTask2,
  debug: debug2,
  dir: dir2,
  dirxml: dirxml2,
  error: error2,
  group: group2,
  groupCollapsed: groupCollapsed2,
  groupEnd: groupEnd2,
  info: info2,
  log: log2,
  profile: profile2,
  profileEnd: profileEnd2,
  table: table2,
  time: time2,
  timeEnd: timeEnd2,
  timeLog: timeLog2,
  timeStamp: timeStamp2,
  trace: trace2,
  warn: warn2
} = workerdConsole;
Object.assign(workerdConsole, {
  Console,
  _ignoreErrors,
  _stderr,
  _stderrErrorHandler,
  _stdout,
  _stdoutErrorHandler,
  _times
});
var console_default = workerdConsole;

// ../node_modules/wrangler/_virtual_unenv_global_polyfill-@cloudflare-unenv-preset-node-console
globalThis.console = console_default;

// ../node_modules/unenv/dist/runtime/node/internal/process/hrtime.mjs
var hrtime = /* @__PURE__ */ Object.assign(/* @__PURE__ */ __name(function hrtime2(startTime) {
  const now = Date.now();
  const seconds = Math.trunc(now / 1e3);
  const nanos = now % 1e3 * 1e6;
  if (startTime) {
    let diffSeconds = seconds - startTime[0];
    let diffNanos = nanos - startTime[0];
    if (diffNanos < 0) {
      diffSeconds = diffSeconds - 1;
      diffNanos = 1e9 + diffNanos;
    }
    return [diffSeconds, diffNanos];
  }
  return [seconds, nanos];
}, "hrtime"), { bigint: /* @__PURE__ */ __name(function bigint() {
  return BigInt(Date.now() * 1e6);
}, "bigint") });

// ../node_modules/unenv/dist/runtime/node/internal/process/process.mjs
import { EventEmitter } from "node:events";

// ../node_modules/unenv/dist/runtime/node/internal/tty/read-stream.mjs
var ReadStream = class {
  static {
    __name(this, "ReadStream");
  }
  fd;
  isRaw = false;
  isTTY = false;
  constructor(fd) {
    this.fd = fd;
  }
  setRawMode(mode) {
    this.isRaw = mode;
    return this;
  }
};

// ../node_modules/unenv/dist/runtime/node/internal/tty/write-stream.mjs
var WriteStream = class {
  static {
    __name(this, "WriteStream");
  }
  fd;
  columns = 80;
  rows = 24;
  isTTY = false;
  constructor(fd) {
    this.fd = fd;
  }
  clearLine(dir3, callback) {
    callback && callback();
    return false;
  }
  clearScreenDown(callback) {
    callback && callback();
    return false;
  }
  cursorTo(x, y, callback) {
    callback && typeof callback === "function" && callback();
    return false;
  }
  moveCursor(dx, dy, callback) {
    callback && callback();
    return false;
  }
  getColorDepth(env2) {
    return 1;
  }
  hasColors(count3, env2) {
    return false;
  }
  getWindowSize() {
    return [this.columns, this.rows];
  }
  write(str, encoding, cb) {
    if (str instanceof Uint8Array) {
      str = new TextDecoder().decode(str);
    }
    try {
      console.log(str);
    } catch {
    }
    cb && typeof cb === "function" && cb();
    return false;
  }
};

// ../node_modules/unenv/dist/runtime/node/internal/process/node-version.mjs
var NODE_VERSION = "22.14.0";

// ../node_modules/unenv/dist/runtime/node/internal/process/process.mjs
var Process = class _Process extends EventEmitter {
  static {
    __name(this, "Process");
  }
  env;
  hrtime;
  nextTick;
  constructor(impl) {
    super();
    this.env = impl.env;
    this.hrtime = impl.hrtime;
    this.nextTick = impl.nextTick;
    for (const prop of [...Object.getOwnPropertyNames(_Process.prototype), ...Object.getOwnPropertyNames(EventEmitter.prototype)]) {
      const value = this[prop];
      if (typeof value === "function") {
        this[prop] = value.bind(this);
      }
    }
  }
  // --- event emitter ---
  emitWarning(warning, type, code) {
    console.warn(`${code ? `[${code}] ` : ""}${type ? `${type}: ` : ""}${warning}`);
  }
  emit(...args) {
    return super.emit(...args);
  }
  listeners(eventName) {
    return super.listeners(eventName);
  }
  // --- stdio (lazy initializers) ---
  #stdin;
  #stdout;
  #stderr;
  get stdin() {
    return this.#stdin ??= new ReadStream(0);
  }
  get stdout() {
    return this.#stdout ??= new WriteStream(1);
  }
  get stderr() {
    return this.#stderr ??= new WriteStream(2);
  }
  // --- cwd ---
  #cwd = "/";
  chdir(cwd2) {
    this.#cwd = cwd2;
  }
  cwd() {
    return this.#cwd;
  }
  // --- dummy props and getters ---
  arch = "";
  platform = "";
  argv = [];
  argv0 = "";
  execArgv = [];
  execPath = "";
  title = "";
  pid = 200;
  ppid = 100;
  get version() {
    return `v${NODE_VERSION}`;
  }
  get versions() {
    return { node: NODE_VERSION };
  }
  get allowedNodeEnvironmentFlags() {
    return /* @__PURE__ */ new Set();
  }
  get sourceMapsEnabled() {
    return false;
  }
  get debugPort() {
    return 0;
  }
  get throwDeprecation() {
    return false;
  }
  get traceDeprecation() {
    return false;
  }
  get features() {
    return {};
  }
  get release() {
    return {};
  }
  get connected() {
    return false;
  }
  get config() {
    return {};
  }
  get moduleLoadList() {
    return [];
  }
  constrainedMemory() {
    return 0;
  }
  availableMemory() {
    return 0;
  }
  uptime() {
    return 0;
  }
  resourceUsage() {
    return {};
  }
  // --- noop methods ---
  ref() {
  }
  unref() {
  }
  // --- unimplemented methods ---
  umask() {
    throw createNotImplementedError("process.umask");
  }
  getBuiltinModule() {
    return void 0;
  }
  getActiveResourcesInfo() {
    throw createNotImplementedError("process.getActiveResourcesInfo");
  }
  exit() {
    throw createNotImplementedError("process.exit");
  }
  reallyExit() {
    throw createNotImplementedError("process.reallyExit");
  }
  kill() {
    throw createNotImplementedError("process.kill");
  }
  abort() {
    throw createNotImplementedError("process.abort");
  }
  dlopen() {
    throw createNotImplementedError("process.dlopen");
  }
  setSourceMapsEnabled() {
    throw createNotImplementedError("process.setSourceMapsEnabled");
  }
  loadEnvFile() {
    throw createNotImplementedError("process.loadEnvFile");
  }
  disconnect() {
    throw createNotImplementedError("process.disconnect");
  }
  cpuUsage() {
    throw createNotImplementedError("process.cpuUsage");
  }
  setUncaughtExceptionCaptureCallback() {
    throw createNotImplementedError("process.setUncaughtExceptionCaptureCallback");
  }
  hasUncaughtExceptionCaptureCallback() {
    throw createNotImplementedError("process.hasUncaughtExceptionCaptureCallback");
  }
  initgroups() {
    throw createNotImplementedError("process.initgroups");
  }
  openStdin() {
    throw createNotImplementedError("process.openStdin");
  }
  assert() {
    throw createNotImplementedError("process.assert");
  }
  binding() {
    throw createNotImplementedError("process.binding");
  }
  // --- attached interfaces ---
  permission = { has: /* @__PURE__ */ notImplemented("process.permission.has") };
  report = {
    directory: "",
    filename: "",
    signal: "SIGUSR2",
    compact: false,
    reportOnFatalError: false,
    reportOnSignal: false,
    reportOnUncaughtException: false,
    getReport: /* @__PURE__ */ notImplemented("process.report.getReport"),
    writeReport: /* @__PURE__ */ notImplemented("process.report.writeReport")
  };
  finalization = {
    register: /* @__PURE__ */ notImplemented("process.finalization.register"),
    unregister: /* @__PURE__ */ notImplemented("process.finalization.unregister"),
    registerBeforeExit: /* @__PURE__ */ notImplemented("process.finalization.registerBeforeExit")
  };
  memoryUsage = Object.assign(() => ({
    arrayBuffers: 0,
    rss: 0,
    external: 0,
    heapTotal: 0,
    heapUsed: 0
  }), { rss: /* @__PURE__ */ __name(() => 0, "rss") });
  // --- undefined props ---
  mainModule = void 0;
  domain = void 0;
  // optional
  send = void 0;
  exitCode = void 0;
  channel = void 0;
  getegid = void 0;
  geteuid = void 0;
  getgid = void 0;
  getgroups = void 0;
  getuid = void 0;
  setegid = void 0;
  seteuid = void 0;
  setgid = void 0;
  setgroups = void 0;
  setuid = void 0;
  // internals
  _events = void 0;
  _eventsCount = void 0;
  _exiting = void 0;
  _maxListeners = void 0;
  _debugEnd = void 0;
  _debugProcess = void 0;
  _fatalException = void 0;
  _getActiveHandles = void 0;
  _getActiveRequests = void 0;
  _kill = void 0;
  _preload_modules = void 0;
  _rawDebug = void 0;
  _startProfilerIdleNotifier = void 0;
  _stopProfilerIdleNotifier = void 0;
  _tickCallback = void 0;
  _disconnect = void 0;
  _handleQueue = void 0;
  _pendingMessage = void 0;
  _channel = void 0;
  _send = void 0;
  _linkedBinding = void 0;
};

// ../node_modules/@cloudflare/unenv-preset/dist/runtime/node/process.mjs
var globalProcess = globalThis["process"];
var getBuiltinModule = globalProcess.getBuiltinModule;
var workerdProcess = getBuiltinModule("node:process");
var unenvProcess = new Process({
  env: globalProcess.env,
  hrtime,
  // `nextTick` is available from workerd process v1
  nextTick: workerdProcess.nextTick
});
var { exit, features, platform } = workerdProcess;
var {
  _channel,
  _debugEnd,
  _debugProcess,
  _disconnect,
  _events,
  _eventsCount,
  _exiting,
  _fatalException,
  _getActiveHandles,
  _getActiveRequests,
  _handleQueue,
  _kill,
  _linkedBinding,
  _maxListeners,
  _pendingMessage,
  _preload_modules,
  _rawDebug,
  _send,
  _startProfilerIdleNotifier,
  _stopProfilerIdleNotifier,
  _tickCallback,
  abort,
  addListener,
  allowedNodeEnvironmentFlags,
  arch,
  argv,
  argv0,
  assert: assert2,
  availableMemory,
  binding,
  channel,
  chdir,
  config,
  connected,
  constrainedMemory,
  cpuUsage,
  cwd,
  debugPort,
  disconnect,
  dlopen,
  domain,
  emit,
  emitWarning,
  env,
  eventNames,
  execArgv,
  execPath,
  exitCode,
  finalization,
  getActiveResourcesInfo,
  getegid,
  geteuid,
  getgid,
  getgroups,
  getMaxListeners,
  getuid,
  hasUncaughtExceptionCaptureCallback,
  hrtime: hrtime3,
  initgroups,
  kill,
  listenerCount,
  listeners,
  loadEnvFile,
  mainModule,
  memoryUsage,
  moduleLoadList,
  nextTick,
  off,
  on,
  once,
  openStdin,
  permission,
  pid,
  ppid,
  prependListener,
  prependOnceListener,
  rawListeners,
  reallyExit,
  ref,
  release,
  removeAllListeners,
  removeListener,
  report,
  resourceUsage,
  send,
  setegid,
  seteuid,
  setgid,
  setgroups,
  setMaxListeners,
  setSourceMapsEnabled,
  setuid,
  setUncaughtExceptionCaptureCallback,
  sourceMapsEnabled,
  stderr,
  stdin,
  stdout,
  throwDeprecation,
  title,
  traceDeprecation,
  umask,
  unref,
  uptime,
  version,
  versions
} = unenvProcess;
var _process = {
  abort,
  addListener,
  allowedNodeEnvironmentFlags,
  hasUncaughtExceptionCaptureCallback,
  setUncaughtExceptionCaptureCallback,
  loadEnvFile,
  sourceMapsEnabled,
  arch,
  argv,
  argv0,
  chdir,
  config,
  connected,
  constrainedMemory,
  availableMemory,
  cpuUsage,
  cwd,
  debugPort,
  dlopen,
  disconnect,
  emit,
  emitWarning,
  env,
  eventNames,
  execArgv,
  execPath,
  exit,
  finalization,
  features,
  getBuiltinModule,
  getActiveResourcesInfo,
  getMaxListeners,
  hrtime: hrtime3,
  kill,
  listeners,
  listenerCount,
  memoryUsage,
  nextTick,
  on,
  off,
  once,
  pid,
  platform,
  ppid,
  prependListener,
  prependOnceListener,
  rawListeners,
  release,
  removeAllListeners,
  removeListener,
  report,
  resourceUsage,
  setMaxListeners,
  setSourceMapsEnabled,
  stderr,
  stdin,
  stdout,
  title,
  throwDeprecation,
  traceDeprecation,
  umask,
  uptime,
  version,
  versions,
  // @ts-expect-error old API
  domain,
  initgroups,
  moduleLoadList,
  reallyExit,
  openStdin,
  assert: assert2,
  binding,
  send,
  exitCode,
  channel,
  getegid,
  geteuid,
  getgid,
  getgroups,
  getuid,
  setegid,
  seteuid,
  setgid,
  setgroups,
  setuid,
  permission,
  mainModule,
  _events,
  _eventsCount,
  _exiting,
  _maxListeners,
  _debugEnd,
  _debugProcess,
  _fatalException,
  _getActiveHandles,
  _getActiveRequests,
  _kill,
  _preload_modules,
  _rawDebug,
  _startProfilerIdleNotifier,
  _stopProfilerIdleNotifier,
  _tickCallback,
  _disconnect,
  _handleQueue,
  _pendingMessage,
  _channel,
  _send,
  _linkedBinding
};
var process_default = _process;

// ../node_modules/wrangler/_virtual_unenv_global_polyfill-@cloudflare-unenv-preset-node-process
globalThis.process = process_default;

// chunk-01zmyzc8.js
function getContext(args) {
  const len = args.length;
  return args[len - 1];
}
__name(getContext, "getContext");

// chunk-ar91h9ff.js
var entityKind = /* @__PURE__ */ Symbol.for("drizzle:entityKind");
function is(value, type) {
  if (!value || typeof value !== "object") {
    return false;
  }
  if (value instanceof type) {
    return true;
  }
  if (!Object.prototype.hasOwnProperty.call(type, entityKind)) {
    throw new Error(`Class "${type.name ?? "<unknown>"}" doesn't look like a Drizzle entity. If this is incorrect and the class is provided by Drizzle, please report this as a bug.`);
  }
  let cls = Object.getPrototypeOf(value).constructor;
  if (cls) {
    while (cls) {
      if (entityKind in cls && cls[entityKind] === type[entityKind]) {
        return true;
      }
      cls = Object.getPrototypeOf(cls);
    }
  }
  return false;
}
__name(is, "is");
var ColumnBuilder = class {
  static {
    __name(this, "ColumnBuilder");
  }
  static [entityKind] = "ColumnBuilder";
  config;
  constructor(name, dataType, columnType) {
    this.config = {
      name,
      keyAsName: name === "",
      notNull: false,
      default: void 0,
      hasDefault: false,
      primaryKey: false,
      isUnique: false,
      uniqueName: void 0,
      uniqueType: void 0,
      dataType,
      columnType,
      generated: void 0
    };
  }
  $type() {
    return this;
  }
  notNull() {
    this.config.notNull = true;
    return this;
  }
  default(value) {
    this.config.default = value;
    this.config.hasDefault = true;
    return this;
  }
  $defaultFn(fn) {
    this.config.defaultFn = fn;
    this.config.hasDefault = true;
    return this;
  }
  $default = this.$defaultFn;
  $onUpdateFn(fn) {
    this.config.onUpdateFn = fn;
    this.config.hasDefault = true;
    return this;
  }
  $onUpdate = this.$onUpdateFn;
  primaryKey() {
    this.config.primaryKey = true;
    this.config.notNull = true;
    return this;
  }
  setName(name) {
    if (this.config.name !== "")
      return;
    this.config.name = name;
  }
};
var Column = class {
  static {
    __name(this, "Column");
  }
  constructor(table3, config2) {
    this.table = table3;
    this.config = config2;
    this.name = config2.name;
    this.keyAsName = config2.keyAsName;
    this.notNull = config2.notNull;
    this.default = config2.default;
    this.defaultFn = config2.defaultFn;
    this.onUpdateFn = config2.onUpdateFn;
    this.hasDefault = config2.hasDefault;
    this.primary = config2.primaryKey;
    this.isUnique = config2.isUnique;
    this.uniqueName = config2.uniqueName;
    this.uniqueType = config2.uniqueType;
    this.dataType = config2.dataType;
    this.columnType = config2.columnType;
    this.generated = config2.generated;
    this.generatedIdentity = config2.generatedIdentity;
  }
  static [entityKind] = "Column";
  name;
  keyAsName;
  primary;
  notNull;
  default;
  defaultFn;
  onUpdateFn;
  hasDefault;
  isUnique;
  uniqueName;
  uniqueType;
  dataType;
  columnType;
  enumValues = void 0;
  generated = void 0;
  generatedIdentity = void 0;
  config;
  mapFromDriverValue(value) {
    return value;
  }
  mapToDriverValue(value) {
    return value;
  }
  shouldDisableInsert() {
    return this.config.generated !== void 0 && this.config.generated.type !== "byDefault";
  }
};
var TableName = /* @__PURE__ */ Symbol.for("drizzle:Name");
function iife(fn, ...args) {
  return fn(...args);
}
__name(iife, "iife");
function uniqueKeyName(table3, columns) {
  return `${table3[TableName]}_${columns.join("_")}_unique`;
}
__name(uniqueKeyName, "uniqueKeyName");
var PgColumn = class extends Column {
  static {
    __name(this, "PgColumn");
  }
  constructor(table3, config2) {
    if (!config2.uniqueName) {
      config2.uniqueName = uniqueKeyName(table3, [config2.name]);
    }
    super(table3, config2);
    this.table = table3;
  }
  static [entityKind] = "PgColumn";
};
var ExtraConfigColumn = class extends PgColumn {
  static {
    __name(this, "ExtraConfigColumn");
  }
  static [entityKind] = "ExtraConfigColumn";
  getSQLType() {
    return this.getSQLType();
  }
  indexConfig = {
    order: this.config.order ?? "asc",
    nulls: this.config.nulls ?? "last",
    opClass: this.config.opClass
  };
  defaultConfig = {
    order: "asc",
    nulls: "last",
    opClass: void 0
  };
  asc() {
    this.indexConfig.order = "asc";
    return this;
  }
  desc() {
    this.indexConfig.order = "desc";
    return this;
  }
  nullsFirst() {
    this.indexConfig.nulls = "first";
    return this;
  }
  nullsLast() {
    this.indexConfig.nulls = "last";
    return this;
  }
  op(opClass) {
    this.indexConfig.opClass = opClass;
    return this;
  }
};
var PgEnumObjectColumn = class extends PgColumn {
  static {
    __name(this, "PgEnumObjectColumn");
  }
  static [entityKind] = "PgEnumObjectColumn";
  enum;
  enumValues = this.config.enum.enumValues;
  constructor(table3, config2) {
    super(table3, config2);
    this.enum = config2.enum;
  }
  getSQLType() {
    return this.enum.enumName;
  }
};
var isPgEnumSym = /* @__PURE__ */ Symbol.for("drizzle:isPgEnum");
function isPgEnum(obj) {
  return !!obj && typeof obj === "function" && isPgEnumSym in obj && obj[isPgEnumSym] === true;
}
__name(isPgEnum, "isPgEnum");
var PgEnumColumn = class extends PgColumn {
  static {
    __name(this, "PgEnumColumn");
  }
  static [entityKind] = "PgEnumColumn";
  enum = this.config.enum;
  enumValues = this.config.enum.enumValues;
  constructor(table3, config2) {
    super(table3, config2);
    this.enum = config2.enum;
  }
  getSQLType() {
    return this.enum.enumName;
  }
};
var Subquery = class {
  static {
    __name(this, "Subquery");
  }
  static [entityKind] = "Subquery";
  constructor(sql3, fields, alias, isWith = false, usedTables = []) {
    this._ = {
      brand: "Subquery",
      sql: sql3,
      selectedFields: fields,
      alias,
      isWith,
      usedTables
    };
  }
};
var WithSubquery = class extends Subquery {
  static {
    __name(this, "WithSubquery");
  }
  static [entityKind] = "WithSubquery";
};
var version2 = "0.45.1";
var otel;
var rawTracer;
var tracer = {
  startActiveSpan(name, fn) {
    if (!otel) {
      return fn();
    }
    if (!rawTracer) {
      rawTracer = otel.trace.getTracer("drizzle-orm", version2);
    }
    return iife((otel22, rawTracer22) => rawTracer22.startActiveSpan(name, (span) => {
      try {
        return fn(span);
      } catch (e) {
        span.setStatus({
          code: otel22.SpanStatusCode.ERROR,
          message: e instanceof Error ? e.message : "Unknown error"
        });
        throw e;
      } finally {
        span.end();
      }
    }), otel, rawTracer);
  }
};
var ViewBaseConfig = /* @__PURE__ */ Symbol.for("drizzle:ViewBaseConfig");
var Schema = /* @__PURE__ */ Symbol.for("drizzle:Schema");
var Columns = /* @__PURE__ */ Symbol.for("drizzle:Columns");
var ExtraConfigColumns = /* @__PURE__ */ Symbol.for("drizzle:ExtraConfigColumns");
var OriginalName = /* @__PURE__ */ Symbol.for("drizzle:OriginalName");
var BaseName = /* @__PURE__ */ Symbol.for("drizzle:BaseName");
var IsAlias = /* @__PURE__ */ Symbol.for("drizzle:IsAlias");
var ExtraConfigBuilder = /* @__PURE__ */ Symbol.for("drizzle:ExtraConfigBuilder");
var IsDrizzleTable = /* @__PURE__ */ Symbol.for("drizzle:IsDrizzleTable");
var Table = class {
  static {
    __name(this, "Table");
  }
  static [entityKind] = "Table";
  static Symbol = {
    Name: TableName,
    Schema,
    OriginalName,
    Columns,
    ExtraConfigColumns,
    BaseName,
    IsAlias,
    ExtraConfigBuilder
  };
  [TableName];
  [OriginalName];
  [Schema];
  [Columns];
  [ExtraConfigColumns];
  [BaseName];
  [IsAlias] = false;
  [IsDrizzleTable] = true;
  [ExtraConfigBuilder] = void 0;
  constructor(name, schema, baseName) {
    this[TableName] = this[OriginalName] = name;
    this[Schema] = schema;
    this[BaseName] = baseName;
  }
};
function getTableName(table3) {
  return table3[TableName];
}
__name(getTableName, "getTableName");
function getTableUniqueName(table3) {
  return `${table3[Schema] ?? "public"}.${table3[TableName]}`;
}
__name(getTableUniqueName, "getTableUniqueName");
function isSQLWrapper(value) {
  return value !== null && value !== void 0 && typeof value.getSQL === "function";
}
__name(isSQLWrapper, "isSQLWrapper");
function mergeQueries(queries) {
  const result = { sql: "", params: [] };
  for (const query of queries) {
    result.sql += query.sql;
    result.params.push(...query.params);
    if (query.typings?.length) {
      if (!result.typings) {
        result.typings = [];
      }
      result.typings.push(...query.typings);
    }
  }
  return result;
}
__name(mergeQueries, "mergeQueries");
var StringChunk = class {
  static {
    __name(this, "StringChunk");
  }
  static [entityKind] = "StringChunk";
  value;
  constructor(value) {
    this.value = Array.isArray(value) ? value : [value];
  }
  getSQL() {
    return new SQL([this]);
  }
};
var SQL = class _SQL {
  static {
    __name(this, "SQL");
  }
  constructor(queryChunks) {
    this.queryChunks = queryChunks;
    for (const chunk of queryChunks) {
      if (is(chunk, Table)) {
        const schemaName = chunk[Table.Symbol.Schema];
        this.usedTables.push(schemaName === void 0 ? chunk[Table.Symbol.Name] : schemaName + "." + chunk[Table.Symbol.Name]);
      }
    }
  }
  static [entityKind] = "SQL";
  decoder = noopDecoder;
  shouldInlineParams = false;
  usedTables = [];
  append(query) {
    this.queryChunks.push(...query.queryChunks);
    return this;
  }
  toQuery(config2) {
    return tracer.startActiveSpan("drizzle.buildSQL", (span) => {
      const query = this.buildQueryFromSourceParams(this.queryChunks, config2);
      span?.setAttributes({
        "drizzle.query.text": query.sql,
        "drizzle.query.params": JSON.stringify(query.params)
      });
      return query;
    });
  }
  buildQueryFromSourceParams(chunks, _config) {
    const config2 = Object.assign({}, _config, {
      inlineParams: _config.inlineParams || this.shouldInlineParams,
      paramStartIndex: _config.paramStartIndex || { value: 0 }
    });
    const {
      casing,
      escapeName,
      escapeParam,
      prepareTyping,
      inlineParams,
      paramStartIndex
    } = config2;
    return mergeQueries(chunks.map((chunk) => {
      if (is(chunk, StringChunk)) {
        return { sql: chunk.value.join(""), params: [] };
      }
      if (is(chunk, Name)) {
        return { sql: escapeName(chunk.value), params: [] };
      }
      if (chunk === void 0) {
        return { sql: "", params: [] };
      }
      if (Array.isArray(chunk)) {
        const result = [new StringChunk("(")];
        for (const [i, p] of chunk.entries()) {
          result.push(p);
          if (i < chunk.length - 1) {
            result.push(new StringChunk(", "));
          }
        }
        result.push(new StringChunk(")"));
        return this.buildQueryFromSourceParams(result, config2);
      }
      if (is(chunk, _SQL)) {
        return this.buildQueryFromSourceParams(chunk.queryChunks, {
          ...config2,
          inlineParams: inlineParams || chunk.shouldInlineParams
        });
      }
      if (is(chunk, Table)) {
        const schemaName = chunk[Table.Symbol.Schema];
        const tableName = chunk[Table.Symbol.Name];
        return {
          sql: schemaName === void 0 || chunk[IsAlias] ? escapeName(tableName) : escapeName(schemaName) + "." + escapeName(tableName),
          params: []
        };
      }
      if (is(chunk, Column)) {
        const columnName = casing.getColumnCasing(chunk);
        if (_config.invokeSource === "indexes") {
          return { sql: escapeName(columnName), params: [] };
        }
        const schemaName = chunk.table[Table.Symbol.Schema];
        return {
          sql: chunk.table[IsAlias] || schemaName === void 0 ? escapeName(chunk.table[Table.Symbol.Name]) + "." + escapeName(columnName) : escapeName(schemaName) + "." + escapeName(chunk.table[Table.Symbol.Name]) + "." + escapeName(columnName),
          params: []
        };
      }
      if (is(chunk, View)) {
        const schemaName = chunk[ViewBaseConfig].schema;
        const viewName = chunk[ViewBaseConfig].name;
        return {
          sql: schemaName === void 0 || chunk[ViewBaseConfig].isAlias ? escapeName(viewName) : escapeName(schemaName) + "." + escapeName(viewName),
          params: []
        };
      }
      if (is(chunk, Param)) {
        if (is(chunk.value, Placeholder)) {
          return { sql: escapeParam(paramStartIndex.value++, chunk), params: [chunk], typings: ["none"] };
        }
        const mappedValue = chunk.value === null ? null : chunk.encoder.mapToDriverValue(chunk.value);
        if (is(mappedValue, _SQL)) {
          return this.buildQueryFromSourceParams([mappedValue], config2);
        }
        if (inlineParams) {
          return { sql: this.mapInlineParam(mappedValue, config2), params: [] };
        }
        let typings = ["none"];
        if (prepareTyping) {
          typings = [prepareTyping(chunk.encoder)];
        }
        return { sql: escapeParam(paramStartIndex.value++, mappedValue), params: [mappedValue], typings };
      }
      if (is(chunk, Placeholder)) {
        return { sql: escapeParam(paramStartIndex.value++, chunk), params: [chunk], typings: ["none"] };
      }
      if (is(chunk, _SQL.Aliased) && chunk.fieldAlias !== void 0) {
        return { sql: escapeName(chunk.fieldAlias), params: [] };
      }
      if (is(chunk, Subquery)) {
        if (chunk._.isWith) {
          return { sql: escapeName(chunk._.alias), params: [] };
        }
        return this.buildQueryFromSourceParams([
          new StringChunk("("),
          chunk._.sql,
          new StringChunk(") "),
          new Name(chunk._.alias)
        ], config2);
      }
      if (isPgEnum(chunk)) {
        if (chunk.schema) {
          return { sql: escapeName(chunk.schema) + "." + escapeName(chunk.enumName), params: [] };
        }
        return { sql: escapeName(chunk.enumName), params: [] };
      }
      if (isSQLWrapper(chunk)) {
        if (chunk.shouldOmitSQLParens?.()) {
          return this.buildQueryFromSourceParams([chunk.getSQL()], config2);
        }
        return this.buildQueryFromSourceParams([
          new StringChunk("("),
          chunk.getSQL(),
          new StringChunk(")")
        ], config2);
      }
      if (inlineParams) {
        return { sql: this.mapInlineParam(chunk, config2), params: [] };
      }
      return { sql: escapeParam(paramStartIndex.value++, chunk), params: [chunk], typings: ["none"] };
    }));
  }
  mapInlineParam(chunk, { escapeString: escapeString2 }) {
    if (chunk === null) {
      return "null";
    }
    if (typeof chunk === "number" || typeof chunk === "boolean") {
      return chunk.toString();
    }
    if (typeof chunk === "string") {
      return escapeString2(chunk);
    }
    if (typeof chunk === "object") {
      const mappedValueAsString = chunk.toString();
      if (mappedValueAsString === "[object Object]") {
        return escapeString2(JSON.stringify(chunk));
      }
      return escapeString2(mappedValueAsString);
    }
    throw new Error("Unexpected param value: " + chunk);
  }
  getSQL() {
    return this;
  }
  as(alias) {
    if (alias === void 0) {
      return this;
    }
    return new _SQL.Aliased(this, alias);
  }
  mapWith(decoder2) {
    this.decoder = typeof decoder2 === "function" ? { mapFromDriverValue: decoder2 } : decoder2;
    return this;
  }
  inlineParams() {
    this.shouldInlineParams = true;
    return this;
  }
  if(condition) {
    return condition ? this : void 0;
  }
};
var Name = class {
  static {
    __name(this, "Name");
  }
  constructor(value) {
    this.value = value;
  }
  static [entityKind] = "Name";
  brand;
  getSQL() {
    return new SQL([this]);
  }
};
function isDriverValueEncoder(value) {
  return typeof value === "object" && value !== null && "mapToDriverValue" in value && typeof value.mapToDriverValue === "function";
}
__name(isDriverValueEncoder, "isDriverValueEncoder");
var noopDecoder = {
  mapFromDriverValue: /* @__PURE__ */ __name((value) => value, "mapFromDriverValue")
};
var noopEncoder = {
  mapToDriverValue: /* @__PURE__ */ __name((value) => value, "mapToDriverValue")
};
var noopMapper = {
  ...noopDecoder,
  ...noopEncoder
};
var Param = class {
  static {
    __name(this, "Param");
  }
  constructor(value, encoder2 = noopEncoder) {
    this.value = value;
    this.encoder = encoder2;
  }
  static [entityKind] = "Param";
  brand;
  getSQL() {
    return new SQL([this]);
  }
};
function sql(strings, ...params) {
  const queryChunks = [];
  if (params.length > 0 || strings.length > 0 && strings[0] !== "") {
    queryChunks.push(new StringChunk(strings[0]));
  }
  for (const [paramIndex, param2] of params.entries()) {
    queryChunks.push(param2, new StringChunk(strings[paramIndex + 1]));
  }
  return new SQL(queryChunks);
}
__name(sql, "sql");
((sql22) => {
  function empty() {
    return new SQL([]);
  }
  __name(empty, "empty");
  sql22.empty = empty;
  function fromList(list) {
    return new SQL(list);
  }
  __name(fromList, "fromList");
  sql22.fromList = fromList;
  function raw(str) {
    return new SQL([new StringChunk(str)]);
  }
  __name(raw, "raw");
  sql22.raw = raw;
  function join(chunks, separator) {
    const result = [];
    for (const [i, chunk] of chunks.entries()) {
      if (i > 0 && separator !== void 0) {
        result.push(separator);
      }
      result.push(chunk);
    }
    return new SQL(result);
  }
  __name(join, "join");
  sql22.join = join;
  function identifier(value) {
    return new Name(value);
  }
  __name(identifier, "identifier");
  sql22.identifier = identifier;
  function placeholder2(name2) {
    return new Placeholder(name2);
  }
  __name(placeholder2, "placeholder2");
  sql22.placeholder = placeholder2;
  function param2(value, encoder2) {
    return new Param(value, encoder2);
  }
  __name(param2, "param2");
  sql22.param = param2;
})(sql || (sql = {}));
((SQL22) => {
  class Aliased {
    static {
      __name(this, "Aliased");
    }
    constructor(sql22, fieldAlias) {
      this.sql = sql22;
      this.fieldAlias = fieldAlias;
    }
    static [entityKind] = "SQL.Aliased";
    isSelectionField = false;
    getSQL() {
      return this.sql;
    }
    clone() {
      return new Aliased(this.sql, this.fieldAlias);
    }
  }
  SQL22.Aliased = Aliased;
})(SQL || (SQL = {}));
var Placeholder = class {
  static {
    __name(this, "Placeholder");
  }
  constructor(name2) {
    this.name = name2;
  }
  static [entityKind] = "Placeholder";
  getSQL() {
    return new SQL([this]);
  }
};
function fillPlaceholders(params, values) {
  return params.map((p) => {
    if (is(p, Placeholder)) {
      if (!(p.name in values)) {
        throw new Error(`No value for placeholder "${p.name}" was provided`);
      }
      return values[p.name];
    }
    if (is(p, Param) && is(p.value, Placeholder)) {
      if (!(p.value.name in values)) {
        throw new Error(`No value for placeholder "${p.value.name}" was provided`);
      }
      return p.encoder.mapToDriverValue(values[p.value.name]);
    }
    return p;
  });
}
__name(fillPlaceholders, "fillPlaceholders");
var IsDrizzleView = /* @__PURE__ */ Symbol.for("drizzle:IsDrizzleView");
var View = class {
  static {
    __name(this, "View");
  }
  static [entityKind] = "View";
  [ViewBaseConfig];
  [IsDrizzleView] = true;
  constructor({ name: name2, schema, selectedFields, query }) {
    this[ViewBaseConfig] = {
      name: name2,
      originalName: name2,
      schema,
      selectedFields,
      query,
      isExisting: !query,
      isAlias: false
    };
  }
  getSQL() {
    return new SQL([this]);
  }
};
Column.prototype.getSQL = function() {
  return new SQL([this]);
};
Table.prototype.getSQL = function() {
  return new SQL([this]);
};
Subquery.prototype.getSQL = function() {
  return new SQL([this]);
};
function bindIfParam(value, column) {
  if (isDriverValueEncoder(column) && !isSQLWrapper(value) && !is(value, Param) && !is(value, Placeholder) && !is(value, Column) && !is(value, Table) && !is(value, View)) {
    return new Param(value, column);
  }
  return value;
}
__name(bindIfParam, "bindIfParam");
var eq = /* @__PURE__ */ __name((left, right) => {
  return sql`${left} = ${bindIfParam(right, left)}`;
}, "eq");
var ne = /* @__PURE__ */ __name((left, right) => {
  return sql`${left} <> ${bindIfParam(right, left)}`;
}, "ne");
function and(...unfilteredConditions) {
  const conditions = unfilteredConditions.filter((c) => c !== void 0);
  if (conditions.length === 0) {
    return;
  }
  if (conditions.length === 1) {
    return new SQL(conditions);
  }
  return new SQL([
    new StringChunk("("),
    sql.join(conditions, new StringChunk(" and ")),
    new StringChunk(")")
  ]);
}
__name(and, "and");
function or(...unfilteredConditions) {
  const conditions = unfilteredConditions.filter((c) => c !== void 0);
  if (conditions.length === 0) {
    return;
  }
  if (conditions.length === 1) {
    return new SQL(conditions);
  }
  return new SQL([
    new StringChunk("("),
    sql.join(conditions, new StringChunk(" or ")),
    new StringChunk(")")
  ]);
}
__name(or, "or");
function not(condition) {
  return sql`not ${condition}`;
}
__name(not, "not");
var gt = /* @__PURE__ */ __name((left, right) => {
  return sql`${left} > ${bindIfParam(right, left)}`;
}, "gt");
var gte = /* @__PURE__ */ __name((left, right) => {
  return sql`${left} >= ${bindIfParam(right, left)}`;
}, "gte");
var lt = /* @__PURE__ */ __name((left, right) => {
  return sql`${left} < ${bindIfParam(right, left)}`;
}, "lt");
var lte = /* @__PURE__ */ __name((left, right) => {
  return sql`${left} <= ${bindIfParam(right, left)}`;
}, "lte");
function inArray(column, values) {
  if (Array.isArray(values)) {
    if (values.length === 0) {
      return sql`false`;
    }
    return sql`${column} in ${values.map((v) => bindIfParam(v, column))}`;
  }
  return sql`${column} in ${bindIfParam(values, column)}`;
}
__name(inArray, "inArray");
function notInArray(column, values) {
  if (Array.isArray(values)) {
    if (values.length === 0) {
      return sql`true`;
    }
    return sql`${column} not in ${values.map((v) => bindIfParam(v, column))}`;
  }
  return sql`${column} not in ${bindIfParam(values, column)}`;
}
__name(notInArray, "notInArray");
function isNull(value) {
  return sql`${value} is null`;
}
__name(isNull, "isNull");
function isNotNull(value) {
  return sql`${value} is not null`;
}
__name(isNotNull, "isNotNull");
function exists(subquery) {
  return sql`exists ${subquery}`;
}
__name(exists, "exists");
function notExists(subquery) {
  return sql`not exists ${subquery}`;
}
__name(notExists, "notExists");
function between(column, min, max) {
  return sql`${column} between ${bindIfParam(min, column)} and ${bindIfParam(max, column)}`;
}
__name(between, "between");
function notBetween(column, min, max) {
  return sql`${column} not between ${bindIfParam(min, column)} and ${bindIfParam(max, column)}`;
}
__name(notBetween, "notBetween");
function like(column, value) {
  return sql`${column} like ${value}`;
}
__name(like, "like");
function notLike(column, value) {
  return sql`${column} not like ${value}`;
}
__name(notLike, "notLike");
function ilike(column, value) {
  return sql`${column} ilike ${value}`;
}
__name(ilike, "ilike");
function notIlike(column, value) {
  return sql`${column} not ilike ${value}`;
}
__name(notIlike, "notIlike");
function asc(column) {
  return sql`${column} asc`;
}
__name(asc, "asc");
function desc(column) {
  return sql`${column} desc`;
}
__name(desc, "desc");
var ConsoleLogWriter = class {
  static {
    __name(this, "ConsoleLogWriter");
  }
  static [entityKind] = "ConsoleLogWriter";
  write(message2) {
    console.log(message2);
  }
};
var DefaultLogger = class {
  static {
    __name(this, "DefaultLogger");
  }
  static [entityKind] = "DefaultLogger";
  writer;
  constructor(config2) {
    this.writer = config2?.writer ?? new ConsoleLogWriter();
  }
  logQuery(query, params) {
    const stringifiedParams = params.map((p) => {
      try {
        return JSON.stringify(p);
      } catch {
        return String(p);
      }
    });
    const paramsStr = stringifiedParams.length ? ` -- params: [${stringifiedParams.join(", ")}]` : "";
    this.writer.write(`Query: ${query}${paramsStr}`);
  }
};
var NoopLogger = class {
  static {
    __name(this, "NoopLogger");
  }
  static [entityKind] = "NoopLogger";
  logQuery() {
  }
};
function mapResultRow(columns, row, joinsNotNullableMap) {
  const nullifyMap = {};
  const result = columns.reduce((result2, { path, field }, columnIndex) => {
    let decoder2;
    if (is(field, Column)) {
      decoder2 = field;
    } else if (is(field, SQL)) {
      decoder2 = field.decoder;
    } else if (is(field, Subquery)) {
      decoder2 = field._.sql.decoder;
    } else {
      decoder2 = field.sql.decoder;
    }
    let node = result2;
    for (const [pathChunkIndex, pathChunk] of path.entries()) {
      if (pathChunkIndex < path.length - 1) {
        if (!(pathChunk in node)) {
          node[pathChunk] = {};
        }
        node = node[pathChunk];
      } else {
        const rawValue = row[columnIndex];
        const value = node[pathChunk] = rawValue === null ? null : decoder2.mapFromDriverValue(rawValue);
        if (joinsNotNullableMap && is(field, Column) && path.length === 2) {
          const objectName = path[0];
          if (!(objectName in nullifyMap)) {
            nullifyMap[objectName] = value === null ? getTableName(field.table) : false;
          } else if (typeof nullifyMap[objectName] === "string" && nullifyMap[objectName] !== getTableName(field.table)) {
            nullifyMap[objectName] = false;
          }
        }
      }
    }
    return result2;
  }, {});
  if (joinsNotNullableMap && Object.keys(nullifyMap).length > 0) {
    for (const [objectName, tableName] of Object.entries(nullifyMap)) {
      if (typeof tableName === "string" && !joinsNotNullableMap[tableName]) {
        result[objectName] = null;
      }
    }
  }
  return result;
}
__name(mapResultRow, "mapResultRow");
function orderSelectedFields(fields, pathPrefix) {
  return Object.entries(fields).reduce((result, [name, field]) => {
    if (typeof name !== "string") {
      return result;
    }
    const newPath = pathPrefix ? [...pathPrefix, name] : [name];
    if (is(field, Column) || is(field, SQL) || is(field, SQL.Aliased) || is(field, Subquery)) {
      result.push({ path: newPath, field });
    } else if (is(field, Table)) {
      result.push(...orderSelectedFields(field[Table.Symbol.Columns], newPath));
    } else {
      result.push(...orderSelectedFields(field, newPath));
    }
    return result;
  }, []);
}
__name(orderSelectedFields, "orderSelectedFields");
function haveSameKeys(left, right) {
  const leftKeys = Object.keys(left);
  const rightKeys = Object.keys(right);
  if (leftKeys.length !== rightKeys.length) {
    return false;
  }
  for (const [index, key] of leftKeys.entries()) {
    if (key !== rightKeys[index]) {
      return false;
    }
  }
  return true;
}
__name(haveSameKeys, "haveSameKeys");
function mapUpdateSet(table3, values) {
  const entries = Object.entries(values).filter(([, value]) => value !== void 0).map(([key, value]) => {
    if (is(value, SQL) || is(value, Column)) {
      return [key, value];
    } else {
      return [key, new Param(value, table3[Table.Symbol.Columns][key])];
    }
  });
  if (entries.length === 0) {
    throw new Error("No values to set");
  }
  return Object.fromEntries(entries);
}
__name(mapUpdateSet, "mapUpdateSet");
function applyMixins(baseClass, extendedClasses) {
  for (const extendedClass of extendedClasses) {
    for (const name of Object.getOwnPropertyNames(extendedClass.prototype)) {
      if (name === "constructor")
        continue;
      Object.defineProperty(baseClass.prototype, name, Object.getOwnPropertyDescriptor(extendedClass.prototype, name) || /* @__PURE__ */ Object.create(null));
    }
  }
}
__name(applyMixins, "applyMixins");
function getTableColumns(table3) {
  return table3[Table.Symbol.Columns];
}
__name(getTableColumns, "getTableColumns");
function getTableLikeName(table3) {
  return is(table3, Subquery) ? table3._.alias : is(table3, View) ? table3[ViewBaseConfig].name : is(table3, SQL) ? void 0 : table3[Table.Symbol.IsAlias] ? table3[Table.Symbol.Name] : table3[Table.Symbol.BaseName];
}
__name(getTableLikeName, "getTableLikeName");
function getColumnNameAndConfig(a, b) {
  return {
    name: typeof a === "string" && a.length > 0 ? a : "",
    config: typeof a === "object" ? a : b
  };
}
__name(getColumnNameAndConfig, "getColumnNameAndConfig");
var textDecoder = typeof TextDecoder === "undefined" ? null : new TextDecoder();
var InlineForeignKeys = /* @__PURE__ */ Symbol.for("drizzle:PgInlineForeignKeys");
var EnableRLS = /* @__PURE__ */ Symbol.for("drizzle:EnableRLS");
var PgTable = class extends Table {
  static {
    __name(this, "PgTable");
  }
  static [entityKind] = "PgTable";
  static Symbol = Object.assign({}, Table.Symbol, {
    InlineForeignKeys,
    EnableRLS
  });
  [InlineForeignKeys] = [];
  [EnableRLS] = false;
  [Table.Symbol.ExtraConfigBuilder] = void 0;
  [Table.Symbol.ExtraConfigColumns] = {};
};
var PrimaryKeyBuilder = class {
  static {
    __name(this, "PrimaryKeyBuilder");
  }
  static [entityKind] = "PgPrimaryKeyBuilder";
  columns;
  name;
  constructor(columns, name) {
    this.columns = columns;
    this.name = name;
  }
  build(table3) {
    return new PrimaryKey(table3, this.columns, this.name);
  }
};
var PrimaryKey = class {
  static {
    __name(this, "PrimaryKey");
  }
  constructor(table3, columns, name) {
    this.table = table3;
    this.columns = columns;
    this.name = name;
  }
  static [entityKind] = "PgPrimaryKey";
  columns;
  name;
  getName() {
    return this.name ?? `${this.table[PgTable.Symbol.Name]}_${this.columns.map((column) => column.name).join("_")}_pk`;
  }
};
var Relation = class {
  static {
    __name(this, "Relation");
  }
  constructor(sourceTable, referencedTable, relationName) {
    this.sourceTable = sourceTable;
    this.referencedTable = referencedTable;
    this.relationName = relationName;
    this.referencedTableName = referencedTable[Table.Symbol.Name];
  }
  static [entityKind] = "Relation";
  referencedTableName;
  fieldName;
};
var Relations = class {
  static {
    __name(this, "Relations");
  }
  constructor(table3, config2) {
    this.table = table3;
    this.config = config2;
  }
  static [entityKind] = "Relations";
};
var One = class _One extends Relation {
  static {
    __name(this, "One");
  }
  constructor(sourceTable, referencedTable, config2, isNullable) {
    super(sourceTable, referencedTable, config2?.relationName);
    this.config = config2;
    this.isNullable = isNullable;
  }
  static [entityKind] = "One";
  withFieldName(fieldName) {
    const relation = new _One(this.sourceTable, this.referencedTable, this.config, this.isNullable);
    relation.fieldName = fieldName;
    return relation;
  }
};
var Many = class _Many extends Relation {
  static {
    __name(this, "Many");
  }
  constructor(sourceTable, referencedTable, config2) {
    super(sourceTable, referencedTable, config2?.relationName);
    this.config = config2;
  }
  static [entityKind] = "Many";
  withFieldName(fieldName) {
    const relation = new _Many(this.sourceTable, this.referencedTable, this.config);
    relation.fieldName = fieldName;
    return relation;
  }
};
function getOperators() {
  return {
    and,
    between,
    eq,
    exists,
    gt,
    gte,
    ilike,
    inArray,
    isNull,
    isNotNull,
    like,
    lt,
    lte,
    ne,
    not,
    notBetween,
    notExists,
    notLike,
    notIlike,
    notInArray,
    or,
    sql
  };
}
__name(getOperators, "getOperators");
function getOrderByOperators() {
  return {
    sql,
    asc,
    desc
  };
}
__name(getOrderByOperators, "getOrderByOperators");
function extractTablesRelationalConfig(schema, configHelpers) {
  if (Object.keys(schema).length === 1 && "default" in schema && !is(schema["default"], Table)) {
    schema = schema["default"];
  }
  const tableNamesMap = {};
  const relationsBuffer = {};
  const tablesConfig = {};
  for (const [key, value] of Object.entries(schema)) {
    if (is(value, Table)) {
      const dbName = getTableUniqueName(value);
      const bufferedRelations = relationsBuffer[dbName];
      tableNamesMap[dbName] = key;
      tablesConfig[key] = {
        tsName: key,
        dbName: value[Table.Symbol.Name],
        schema: value[Table.Symbol.Schema],
        columns: value[Table.Symbol.Columns],
        relations: bufferedRelations?.relations ?? {},
        primaryKey: bufferedRelations?.primaryKey ?? []
      };
      for (const column of Object.values(value[Table.Symbol.Columns])) {
        if (column.primary) {
          tablesConfig[key].primaryKey.push(column);
        }
      }
      const extraConfig = value[Table.Symbol.ExtraConfigBuilder]?.(value[Table.Symbol.ExtraConfigColumns]);
      if (extraConfig) {
        for (const configEntry of Object.values(extraConfig)) {
          if (is(configEntry, PrimaryKeyBuilder)) {
            tablesConfig[key].primaryKey.push(...configEntry.columns);
          }
        }
      }
    } else if (is(value, Relations)) {
      const dbName = getTableUniqueName(value.table);
      const tableName = tableNamesMap[dbName];
      const relations2 = value.config(configHelpers(value.table));
      let primaryKey;
      for (const [relationName, relation] of Object.entries(relations2)) {
        if (tableName) {
          const tableConfig = tablesConfig[tableName];
          tableConfig.relations[relationName] = relation;
          if (primaryKey) {
            tableConfig.primaryKey.push(...primaryKey);
          }
        } else {
          if (!(dbName in relationsBuffer)) {
            relationsBuffer[dbName] = {
              relations: {},
              primaryKey
            };
          }
          relationsBuffer[dbName].relations[relationName] = relation;
        }
      }
    }
  }
  return { tables: tablesConfig, tableNamesMap };
}
__name(extractTablesRelationalConfig, "extractTablesRelationalConfig");
function createOne(sourceTable) {
  return /* @__PURE__ */ __name(function one(table3, config2) {
    return new One(sourceTable, table3, config2, config2?.fields.reduce((res, f) => res && f.notNull, true) ?? false);
  }, "one");
}
__name(createOne, "createOne");
function createMany(sourceTable) {
  return /* @__PURE__ */ __name(function many(referencedTable, config2) {
    return new Many(sourceTable, referencedTable, config2);
  }, "many");
}
__name(createMany, "createMany");
function normalizeRelation(schema, tableNamesMap, relation) {
  if (is(relation, One) && relation.config) {
    return {
      fields: relation.config.fields,
      references: relation.config.references
    };
  }
  const referencedTableTsName = tableNamesMap[getTableUniqueName(relation.referencedTable)];
  if (!referencedTableTsName) {
    throw new Error(`Table "${relation.referencedTable[Table.Symbol.Name]}" not found in schema`);
  }
  const referencedTableConfig = schema[referencedTableTsName];
  if (!referencedTableConfig) {
    throw new Error(`Table "${referencedTableTsName}" not found in schema`);
  }
  const sourceTable = relation.sourceTable;
  const sourceTableTsName = tableNamesMap[getTableUniqueName(sourceTable)];
  if (!sourceTableTsName) {
    throw new Error(`Table "${sourceTable[Table.Symbol.Name]}" not found in schema`);
  }
  const reverseRelations = [];
  for (const referencedTableRelation of Object.values(referencedTableConfig.relations)) {
    if (relation.relationName && relation !== referencedTableRelation && referencedTableRelation.relationName === relation.relationName || !relation.relationName && referencedTableRelation.referencedTable === relation.sourceTable) {
      reverseRelations.push(referencedTableRelation);
    }
  }
  if (reverseRelations.length > 1) {
    throw relation.relationName ? new Error(`There are multiple relations with name "${relation.relationName}" in table "${referencedTableTsName}"`) : new Error(`There are multiple relations between "${referencedTableTsName}" and "${relation.sourceTable[Table.Symbol.Name]}". Please specify relation name`);
  }
  if (reverseRelations[0] && is(reverseRelations[0], One) && reverseRelations[0].config) {
    return {
      fields: reverseRelations[0].config.references,
      references: reverseRelations[0].config.fields
    };
  }
  throw new Error(`There is not enough information to infer relation "${sourceTableTsName}.${relation.fieldName}"`);
}
__name(normalizeRelation, "normalizeRelation");
function createTableRelationsHelpers(sourceTable) {
  return {
    one: createOne(sourceTable),
    many: createMany(sourceTable)
  };
}
__name(createTableRelationsHelpers, "createTableRelationsHelpers");
function mapRelationalRow(tablesConfig, tableConfig, row, buildQueryResultSelection, mapColumnValue = (value) => value) {
  const result = {};
  for (const [
    selectionItemIndex,
    selectionItem
  ] of buildQueryResultSelection.entries()) {
    if (selectionItem.isJson) {
      const relation = tableConfig.relations[selectionItem.tsKey];
      const rawSubRows = row[selectionItemIndex];
      const subRows = typeof rawSubRows === "string" ? JSON.parse(rawSubRows) : rawSubRows;
      result[selectionItem.tsKey] = is(relation, One) ? subRows && mapRelationalRow(tablesConfig, tablesConfig[selectionItem.relationTableTsKey], subRows, selectionItem.selection, mapColumnValue) : subRows.map((subRow) => mapRelationalRow(tablesConfig, tablesConfig[selectionItem.relationTableTsKey], subRow, selectionItem.selection, mapColumnValue));
    } else {
      const value = mapColumnValue(row[selectionItemIndex]);
      const field = selectionItem.field;
      let decoder2;
      if (is(field, Column)) {
        decoder2 = field;
      } else if (is(field, SQL)) {
        decoder2 = field.decoder;
      } else {
        decoder2 = field.sql.decoder;
      }
      result[selectionItem.tsKey] = value === null ? null : decoder2.mapFromDriverValue(value);
    }
  }
  return result;
}
__name(mapRelationalRow, "mapRelationalRow");
var ColumnAliasProxyHandler = class {
  static {
    __name(this, "ColumnAliasProxyHandler");
  }
  constructor(table3) {
    this.table = table3;
  }
  static [entityKind] = "ColumnAliasProxyHandler";
  get(columnObj, prop) {
    if (prop === "table") {
      return this.table;
    }
    return columnObj[prop];
  }
};
var TableAliasProxyHandler = class {
  static {
    __name(this, "TableAliasProxyHandler");
  }
  constructor(alias, replaceOriginalName) {
    this.alias = alias;
    this.replaceOriginalName = replaceOriginalName;
  }
  static [entityKind] = "TableAliasProxyHandler";
  get(target, prop) {
    if (prop === Table.Symbol.IsAlias) {
      return true;
    }
    if (prop === Table.Symbol.Name) {
      return this.alias;
    }
    if (this.replaceOriginalName && prop === Table.Symbol.OriginalName) {
      return this.alias;
    }
    if (prop === ViewBaseConfig) {
      return {
        ...target[ViewBaseConfig],
        name: this.alias,
        isAlias: true
      };
    }
    if (prop === Table.Symbol.Columns) {
      const columns = target[Table.Symbol.Columns];
      if (!columns) {
        return columns;
      }
      const proxiedColumns = {};
      Object.keys(columns).map((key) => {
        proxiedColumns[key] = new Proxy(columns[key], new ColumnAliasProxyHandler(new Proxy(target, this)));
      });
      return proxiedColumns;
    }
    const value = target[prop];
    if (is(value, Column)) {
      return new Proxy(value, new ColumnAliasProxyHandler(new Proxy(target, this)));
    }
    return value;
  }
};
function aliasedTable(table3, tableAlias) {
  return new Proxy(table3, new TableAliasProxyHandler(tableAlias, false));
}
__name(aliasedTable, "aliasedTable");
function aliasedTableColumn(column, tableAlias) {
  return new Proxy(column, new ColumnAliasProxyHandler(new Proxy(column.table, new TableAliasProxyHandler(tableAlias, false))));
}
__name(aliasedTableColumn, "aliasedTableColumn");
function mapColumnsInAliasedSQLToAlias(query, alias) {
  return new SQL.Aliased(mapColumnsInSQLToAlias(query.sql, alias), query.fieldAlias);
}
__name(mapColumnsInAliasedSQLToAlias, "mapColumnsInAliasedSQLToAlias");
function mapColumnsInSQLToAlias(query, alias) {
  return sql.join(query.queryChunks.map((c) => {
    if (is(c, Column)) {
      return aliasedTableColumn(c, alias);
    }
    if (is(c, SQL)) {
      return mapColumnsInSQLToAlias(c, alias);
    }
    if (is(c, SQL.Aliased)) {
      return mapColumnsInAliasedSQLToAlias(c, alias);
    }
    return c;
  }));
}
__name(mapColumnsInSQLToAlias, "mapColumnsInSQLToAlias");
var SelectionProxyHandler = class _SelectionProxyHandler {
  static {
    __name(this, "SelectionProxyHandler");
  }
  static [entityKind] = "SelectionProxyHandler";
  config;
  constructor(config2) {
    this.config = { ...config2 };
  }
  get(subquery, prop) {
    if (prop === "_") {
      return {
        ...subquery["_"],
        selectedFields: new Proxy(subquery._.selectedFields, this)
      };
    }
    if (prop === ViewBaseConfig) {
      return {
        ...subquery[ViewBaseConfig],
        selectedFields: new Proxy(subquery[ViewBaseConfig].selectedFields, this)
      };
    }
    if (typeof prop === "symbol") {
      return subquery[prop];
    }
    const columns = is(subquery, Subquery) ? subquery._.selectedFields : is(subquery, View) ? subquery[ViewBaseConfig].selectedFields : subquery;
    const value = columns[prop];
    if (is(value, SQL.Aliased)) {
      if (this.config.sqlAliasedBehavior === "sql" && !value.isSelectionField) {
        return value.sql;
      }
      const newValue = value.clone();
      newValue.isSelectionField = true;
      return newValue;
    }
    if (is(value, SQL)) {
      if (this.config.sqlBehavior === "sql") {
        return value;
      }
      throw new Error(`You tried to reference "${prop}" field from a subquery, which is a raw SQL field, but it doesn't have an alias declared. Please add an alias to the field using ".as('alias')" method.`);
    }
    if (is(value, Column)) {
      if (this.config.alias) {
        return new Proxy(value, new ColumnAliasProxyHandler(new Proxy(value.table, new TableAliasProxyHandler(this.config.alias, this.config.replaceOriginalName ?? false))));
      }
      return value;
    }
    if (typeof value !== "object" || value === null) {
      return value;
    }
    return new Proxy(value, new _SelectionProxyHandler(this.config));
  }
};
var QueryPromise = class {
  static {
    __name(this, "QueryPromise");
  }
  static [entityKind] = "QueryPromise";
  [Symbol.toStringTag] = "QueryPromise";
  catch(onRejected) {
    return this.then(void 0, onRejected);
  }
  finally(onFinally) {
    return this.then((value) => {
      onFinally?.();
      return value;
    }, (reason) => {
      onFinally?.();
      throw reason;
    });
  }
  then(onFulfilled, onRejected) {
    return this.execute().then(onFulfilled, onRejected);
  }
};
var ForeignKeyBuilder = class {
  static {
    __name(this, "ForeignKeyBuilder");
  }
  static [entityKind] = "SQLiteForeignKeyBuilder";
  reference;
  _onUpdate;
  _onDelete;
  constructor(config2, actions) {
    this.reference = () => {
      const { name, columns, foreignColumns } = config2();
      return { name, columns, foreignTable: foreignColumns[0].table, foreignColumns };
    };
    if (actions) {
      this._onUpdate = actions.onUpdate;
      this._onDelete = actions.onDelete;
    }
  }
  onUpdate(action) {
    this._onUpdate = action;
    return this;
  }
  onDelete(action) {
    this._onDelete = action;
    return this;
  }
  build(table3) {
    return new ForeignKey(table3, this);
  }
};
var ForeignKey = class {
  static {
    __name(this, "ForeignKey");
  }
  constructor(table3, builder) {
    this.table = table3;
    this.reference = builder.reference;
    this.onUpdate = builder._onUpdate;
    this.onDelete = builder._onDelete;
  }
  static [entityKind] = "SQLiteForeignKey";
  reference;
  onUpdate;
  onDelete;
  getName() {
    const { name, columns, foreignColumns } = this.reference();
    const columnNames = columns.map((column) => column.name);
    const foreignColumnNames = foreignColumns.map((column) => column.name);
    const chunks = [
      this.table[TableName],
      ...columnNames,
      foreignColumns[0].table[TableName],
      ...foreignColumnNames
    ];
    return name ?? `${chunks.join("_")}_fk`;
  }
};
function uniqueKeyName2(table3, columns) {
  return `${table3[TableName]}_${columns.join("_")}_unique`;
}
__name(uniqueKeyName2, "uniqueKeyName2");
var SQLiteColumnBuilder = class extends ColumnBuilder {
  static {
    __name(this, "SQLiteColumnBuilder");
  }
  static [entityKind] = "SQLiteColumnBuilder";
  foreignKeyConfigs = [];
  references(ref2, actions = {}) {
    this.foreignKeyConfigs.push({ ref: ref2, actions });
    return this;
  }
  unique(name) {
    this.config.isUnique = true;
    this.config.uniqueName = name;
    return this;
  }
  generatedAlwaysAs(as, config2) {
    this.config.generated = {
      as,
      type: "always",
      mode: config2?.mode ?? "virtual"
    };
    return this;
  }
  buildForeignKeys(column, table3) {
    return this.foreignKeyConfigs.map(({ ref: ref2, actions }) => {
      return ((ref22, actions2) => {
        const builder = new ForeignKeyBuilder(() => {
          const foreignColumn = ref22();
          return { columns: [column], foreignColumns: [foreignColumn] };
        });
        if (actions2.onUpdate) {
          builder.onUpdate(actions2.onUpdate);
        }
        if (actions2.onDelete) {
          builder.onDelete(actions2.onDelete);
        }
        return builder.build(table3);
      })(ref2, actions);
    });
  }
};
var SQLiteColumn = class extends Column {
  static {
    __name(this, "SQLiteColumn");
  }
  constructor(table3, config2) {
    if (!config2.uniqueName) {
      config2.uniqueName = uniqueKeyName2(table3, [config2.name]);
    }
    super(table3, config2);
    this.table = table3;
  }
  static [entityKind] = "SQLiteColumn";
};
var SQLiteBigIntBuilder = class extends SQLiteColumnBuilder {
  static {
    __name(this, "SQLiteBigIntBuilder");
  }
  static [entityKind] = "SQLiteBigIntBuilder";
  constructor(name) {
    super(name, "bigint", "SQLiteBigInt");
  }
  build(table3) {
    return new SQLiteBigInt(table3, this.config);
  }
};
var SQLiteBigInt = class extends SQLiteColumn {
  static {
    __name(this, "SQLiteBigInt");
  }
  static [entityKind] = "SQLiteBigInt";
  getSQLType() {
    return "blob";
  }
  mapFromDriverValue(value) {
    if (typeof Buffer !== "undefined" && Buffer.from) {
      const buf = Buffer.isBuffer(value) ? value : value instanceof ArrayBuffer ? Buffer.from(value) : value.buffer ? Buffer.from(value.buffer, value.byteOffset, value.byteLength) : Buffer.from(value);
      return BigInt(buf.toString("utf8"));
    }
    return BigInt(textDecoder.decode(value));
  }
  mapToDriverValue(value) {
    return Buffer.from(value.toString());
  }
};
var SQLiteBlobJsonBuilder = class extends SQLiteColumnBuilder {
  static {
    __name(this, "SQLiteBlobJsonBuilder");
  }
  static [entityKind] = "SQLiteBlobJsonBuilder";
  constructor(name) {
    super(name, "json", "SQLiteBlobJson");
  }
  build(table3) {
    return new SQLiteBlobJson(table3, this.config);
  }
};
var SQLiteBlobJson = class extends SQLiteColumn {
  static {
    __name(this, "SQLiteBlobJson");
  }
  static [entityKind] = "SQLiteBlobJson";
  getSQLType() {
    return "blob";
  }
  mapFromDriverValue(value) {
    if (typeof Buffer !== "undefined" && Buffer.from) {
      const buf = Buffer.isBuffer(value) ? value : value instanceof ArrayBuffer ? Buffer.from(value) : value.buffer ? Buffer.from(value.buffer, value.byteOffset, value.byteLength) : Buffer.from(value);
      return JSON.parse(buf.toString("utf8"));
    }
    return JSON.parse(textDecoder.decode(value));
  }
  mapToDriverValue(value) {
    return Buffer.from(JSON.stringify(value));
  }
};
var SQLiteBlobBufferBuilder = class extends SQLiteColumnBuilder {
  static {
    __name(this, "SQLiteBlobBufferBuilder");
  }
  static [entityKind] = "SQLiteBlobBufferBuilder";
  constructor(name) {
    super(name, "buffer", "SQLiteBlobBuffer");
  }
  build(table3) {
    return new SQLiteBlobBuffer(table3, this.config);
  }
};
var SQLiteBlobBuffer = class extends SQLiteColumn {
  static {
    __name(this, "SQLiteBlobBuffer");
  }
  static [entityKind] = "SQLiteBlobBuffer";
  mapFromDriverValue(value) {
    if (Buffer.isBuffer(value)) {
      return value;
    }
    return Buffer.from(value);
  }
  getSQLType() {
    return "blob";
  }
};
function blob(a, b) {
  const { name, config: config2 } = getColumnNameAndConfig(a, b);
  if (config2?.mode === "json") {
    return new SQLiteBlobJsonBuilder(name);
  }
  if (config2?.mode === "bigint") {
    return new SQLiteBigIntBuilder(name);
  }
  return new SQLiteBlobBufferBuilder(name);
}
__name(blob, "blob");
var SQLiteCustomColumnBuilder = class extends SQLiteColumnBuilder {
  static {
    __name(this, "SQLiteCustomColumnBuilder");
  }
  static [entityKind] = "SQLiteCustomColumnBuilder";
  constructor(name, fieldConfig, customTypeParams) {
    super(name, "custom", "SQLiteCustomColumn");
    this.config.fieldConfig = fieldConfig;
    this.config.customTypeParams = customTypeParams;
  }
  build(table3) {
    return new SQLiteCustomColumn(table3, this.config);
  }
};
var SQLiteCustomColumn = class extends SQLiteColumn {
  static {
    __name(this, "SQLiteCustomColumn");
  }
  static [entityKind] = "SQLiteCustomColumn";
  sqlName;
  mapTo;
  mapFrom;
  constructor(table3, config2) {
    super(table3, config2);
    this.sqlName = config2.customTypeParams.dataType(config2.fieldConfig);
    this.mapTo = config2.customTypeParams.toDriver;
    this.mapFrom = config2.customTypeParams.fromDriver;
  }
  getSQLType() {
    return this.sqlName;
  }
  mapFromDriverValue(value) {
    return typeof this.mapFrom === "function" ? this.mapFrom(value) : value;
  }
  mapToDriverValue(value) {
    return typeof this.mapTo === "function" ? this.mapTo(value) : value;
  }
};
function customType(customTypeParams) {
  return (a, b) => {
    const { name, config: config2 } = getColumnNameAndConfig(a, b);
    return new SQLiteCustomColumnBuilder(name, config2, customTypeParams);
  };
}
__name(customType, "customType");
var SQLiteBaseIntegerBuilder = class extends SQLiteColumnBuilder {
  static {
    __name(this, "SQLiteBaseIntegerBuilder");
  }
  static [entityKind] = "SQLiteBaseIntegerBuilder";
  constructor(name, dataType, columnType) {
    super(name, dataType, columnType);
    this.config.autoIncrement = false;
  }
  primaryKey(config2) {
    if (config2?.autoIncrement) {
      this.config.autoIncrement = true;
    }
    this.config.hasDefault = true;
    return super.primaryKey();
  }
};
var SQLiteBaseInteger = class extends SQLiteColumn {
  static {
    __name(this, "SQLiteBaseInteger");
  }
  static [entityKind] = "SQLiteBaseInteger";
  autoIncrement = this.config.autoIncrement;
  getSQLType() {
    return "integer";
  }
};
var SQLiteIntegerBuilder = class extends SQLiteBaseIntegerBuilder {
  static {
    __name(this, "SQLiteIntegerBuilder");
  }
  static [entityKind] = "SQLiteIntegerBuilder";
  constructor(name) {
    super(name, "number", "SQLiteInteger");
  }
  build(table3) {
    return new SQLiteInteger(table3, this.config);
  }
};
var SQLiteInteger = class extends SQLiteBaseInteger {
  static {
    __name(this, "SQLiteInteger");
  }
  static [entityKind] = "SQLiteInteger";
};
var SQLiteTimestampBuilder = class extends SQLiteBaseIntegerBuilder {
  static {
    __name(this, "SQLiteTimestampBuilder");
  }
  static [entityKind] = "SQLiteTimestampBuilder";
  constructor(name, mode) {
    super(name, "date", "SQLiteTimestamp");
    this.config.mode = mode;
  }
  defaultNow() {
    return this.default(sql`(cast((julianday('now') - 2440587.5)*86400000 as integer))`);
  }
  build(table3) {
    return new SQLiteTimestamp(table3, this.config);
  }
};
var SQLiteTimestamp = class extends SQLiteBaseInteger {
  static {
    __name(this, "SQLiteTimestamp");
  }
  static [entityKind] = "SQLiteTimestamp";
  mode = this.config.mode;
  mapFromDriverValue(value) {
    if (this.config.mode === "timestamp") {
      return new Date(value * 1e3);
    }
    return new Date(value);
  }
  mapToDriverValue(value) {
    const unix = value.getTime();
    if (this.config.mode === "timestamp") {
      return Math.floor(unix / 1e3);
    }
    return unix;
  }
};
var SQLiteBooleanBuilder = class extends SQLiteBaseIntegerBuilder {
  static {
    __name(this, "SQLiteBooleanBuilder");
  }
  static [entityKind] = "SQLiteBooleanBuilder";
  constructor(name, mode) {
    super(name, "boolean", "SQLiteBoolean");
    this.config.mode = mode;
  }
  build(table3) {
    return new SQLiteBoolean(table3, this.config);
  }
};
var SQLiteBoolean = class extends SQLiteBaseInteger {
  static {
    __name(this, "SQLiteBoolean");
  }
  static [entityKind] = "SQLiteBoolean";
  mode = this.config.mode;
  mapFromDriverValue(value) {
    return Number(value) === 1;
  }
  mapToDriverValue(value) {
    return value ? 1 : 0;
  }
};
function integer(a, b) {
  const { name, config: config2 } = getColumnNameAndConfig(a, b);
  if (config2?.mode === "timestamp" || config2?.mode === "timestamp_ms") {
    return new SQLiteTimestampBuilder(name, config2.mode);
  }
  if (config2?.mode === "boolean") {
    return new SQLiteBooleanBuilder(name, config2.mode);
  }
  return new SQLiteIntegerBuilder(name);
}
__name(integer, "integer");
var SQLiteNumericBuilder = class extends SQLiteColumnBuilder {
  static {
    __name(this, "SQLiteNumericBuilder");
  }
  static [entityKind] = "SQLiteNumericBuilder";
  constructor(name) {
    super(name, "string", "SQLiteNumeric");
  }
  build(table3) {
    return new SQLiteNumeric(table3, this.config);
  }
};
var SQLiteNumeric = class extends SQLiteColumn {
  static {
    __name(this, "SQLiteNumeric");
  }
  static [entityKind] = "SQLiteNumeric";
  mapFromDriverValue(value) {
    if (typeof value === "string")
      return value;
    return String(value);
  }
  getSQLType() {
    return "numeric";
  }
};
var SQLiteNumericNumberBuilder = class extends SQLiteColumnBuilder {
  static {
    __name(this, "SQLiteNumericNumberBuilder");
  }
  static [entityKind] = "SQLiteNumericNumberBuilder";
  constructor(name) {
    super(name, "number", "SQLiteNumericNumber");
  }
  build(table3) {
    return new SQLiteNumericNumber(table3, this.config);
  }
};
var SQLiteNumericNumber = class extends SQLiteColumn {
  static {
    __name(this, "SQLiteNumericNumber");
  }
  static [entityKind] = "SQLiteNumericNumber";
  mapFromDriverValue(value) {
    if (typeof value === "number")
      return value;
    return Number(value);
  }
  mapToDriverValue = String;
  getSQLType() {
    return "numeric";
  }
};
var SQLiteNumericBigIntBuilder = class extends SQLiteColumnBuilder {
  static {
    __name(this, "SQLiteNumericBigIntBuilder");
  }
  static [entityKind] = "SQLiteNumericBigIntBuilder";
  constructor(name) {
    super(name, "bigint", "SQLiteNumericBigInt");
  }
  build(table3) {
    return new SQLiteNumericBigInt(table3, this.config);
  }
};
var SQLiteNumericBigInt = class extends SQLiteColumn {
  static {
    __name(this, "SQLiteNumericBigInt");
  }
  static [entityKind] = "SQLiteNumericBigInt";
  mapFromDriverValue = BigInt;
  mapToDriverValue = String;
  getSQLType() {
    return "numeric";
  }
};
function numeric(a, b) {
  const { name, config: config2 } = getColumnNameAndConfig(a, b);
  const mode = config2?.mode;
  return mode === "number" ? new SQLiteNumericNumberBuilder(name) : mode === "bigint" ? new SQLiteNumericBigIntBuilder(name) : new SQLiteNumericBuilder(name);
}
__name(numeric, "numeric");
var SQLiteRealBuilder = class extends SQLiteColumnBuilder {
  static {
    __name(this, "SQLiteRealBuilder");
  }
  static [entityKind] = "SQLiteRealBuilder";
  constructor(name) {
    super(name, "number", "SQLiteReal");
  }
  build(table3) {
    return new SQLiteReal(table3, this.config);
  }
};
var SQLiteReal = class extends SQLiteColumn {
  static {
    __name(this, "SQLiteReal");
  }
  static [entityKind] = "SQLiteReal";
  getSQLType() {
    return "real";
  }
};
function real(name) {
  return new SQLiteRealBuilder(name ?? "");
}
__name(real, "real");
var SQLiteTextBuilder = class extends SQLiteColumnBuilder {
  static {
    __name(this, "SQLiteTextBuilder");
  }
  static [entityKind] = "SQLiteTextBuilder";
  constructor(name, config2) {
    super(name, "string", "SQLiteText");
    this.config.enumValues = config2.enum;
    this.config.length = config2.length;
  }
  build(table3) {
    return new SQLiteText(table3, this.config);
  }
};
var SQLiteText = class extends SQLiteColumn {
  static {
    __name(this, "SQLiteText");
  }
  static [entityKind] = "SQLiteText";
  enumValues = this.config.enumValues;
  length = this.config.length;
  constructor(table3, config2) {
    super(table3, config2);
  }
  getSQLType() {
    return `text${this.config.length ? `(${this.config.length})` : ""}`;
  }
};
var SQLiteTextJsonBuilder = class extends SQLiteColumnBuilder {
  static {
    __name(this, "SQLiteTextJsonBuilder");
  }
  static [entityKind] = "SQLiteTextJsonBuilder";
  constructor(name) {
    super(name, "json", "SQLiteTextJson");
  }
  build(table3) {
    return new SQLiteTextJson(table3, this.config);
  }
};
var SQLiteTextJson = class extends SQLiteColumn {
  static {
    __name(this, "SQLiteTextJson");
  }
  static [entityKind] = "SQLiteTextJson";
  getSQLType() {
    return "text";
  }
  mapFromDriverValue(value) {
    return JSON.parse(value);
  }
  mapToDriverValue(value) {
    return JSON.stringify(value);
  }
};
function text(a, b = {}) {
  const { name, config: config2 } = getColumnNameAndConfig(a, b);
  if (config2.mode === "json") {
    return new SQLiteTextJsonBuilder(name);
  }
  return new SQLiteTextBuilder(name, config2);
}
__name(text, "text");
function getSQLiteColumnBuilders() {
  return {
    blob,
    customType,
    integer,
    numeric,
    real,
    text
  };
}
__name(getSQLiteColumnBuilders, "getSQLiteColumnBuilders");
var InlineForeignKeys2 = /* @__PURE__ */ Symbol.for("drizzle:SQLiteInlineForeignKeys");
var SQLiteTable = class extends Table {
  static {
    __name(this, "SQLiteTable");
  }
  static [entityKind] = "SQLiteTable";
  static Symbol = Object.assign({}, Table.Symbol, {
    InlineForeignKeys: InlineForeignKeys2
  });
  [Table.Symbol.Columns];
  [InlineForeignKeys2] = [];
  [Table.Symbol.ExtraConfigBuilder] = void 0;
};
function sqliteTableBase(name, columns, extraConfig, schema, baseName = name) {
  const rawTable = new SQLiteTable(name, schema, baseName);
  const parsedColumns = typeof columns === "function" ? columns(getSQLiteColumnBuilders()) : columns;
  const builtColumns = Object.fromEntries(Object.entries(parsedColumns).map(([name2, colBuilderBase]) => {
    const colBuilder = colBuilderBase;
    colBuilder.setName(name2);
    const column = colBuilder.build(rawTable);
    rawTable[InlineForeignKeys2].push(...colBuilder.buildForeignKeys(column, rawTable));
    return [name2, column];
  }));
  const table3 = Object.assign(rawTable, builtColumns);
  table3[Table.Symbol.Columns] = builtColumns;
  table3[Table.Symbol.ExtraConfigColumns] = builtColumns;
  if (extraConfig) {
    table3[SQLiteTable.Symbol.ExtraConfigBuilder] = extraConfig;
  }
  return table3;
}
__name(sqliteTableBase, "sqliteTableBase");
var sqliteTable = /* @__PURE__ */ __name((name, columns, extraConfig) => {
  return sqliteTableBase(name, columns, extraConfig);
}, "sqliteTable");
function extractUsedTable(table3) {
  if (is(table3, SQLiteTable)) {
    return [`${table3[Table.Symbol.BaseName]}`];
  }
  if (is(table3, Subquery)) {
    return table3._.usedTables ?? [];
  }
  if (is(table3, SQL)) {
    return table3.usedTables ?? [];
  }
  return [];
}
__name(extractUsedTable, "extractUsedTable");
var SQLiteDeleteBase = class extends QueryPromise {
  static {
    __name(this, "SQLiteDeleteBase");
  }
  constructor(table3, session, dialect, withList) {
    super();
    this.table = table3;
    this.session = session;
    this.dialect = dialect;
    this.config = { table: table3, withList };
  }
  static [entityKind] = "SQLiteDelete";
  config;
  where(where) {
    this.config.where = where;
    return this;
  }
  orderBy(...columns) {
    if (typeof columns[0] === "function") {
      const orderBy = columns[0](new Proxy(this.config.table[Table.Symbol.Columns], new SelectionProxyHandler({ sqlAliasedBehavior: "alias", sqlBehavior: "sql" })));
      const orderByArray = Array.isArray(orderBy) ? orderBy : [orderBy];
      this.config.orderBy = orderByArray;
    } else {
      const orderByArray = columns;
      this.config.orderBy = orderByArray;
    }
    return this;
  }
  limit(limit2) {
    this.config.limit = limit2;
    return this;
  }
  returning(fields = this.table[SQLiteTable.Symbol.Columns]) {
    this.config.returning = orderSelectedFields(fields);
    return this;
  }
  getSQL() {
    return this.dialect.buildDeleteQuery(this.config);
  }
  toSQL() {
    const { typings: _typings, ...rest } = this.dialect.sqlToQuery(this.getSQL());
    return rest;
  }
  _prepare(isOneTimeQuery = true) {
    return this.session[isOneTimeQuery ? "prepareOneTimeQuery" : "prepareQuery"](this.dialect.sqlToQuery(this.getSQL()), this.config.returning, this.config.returning ? "all" : "run", true, void 0, {
      type: "delete",
      tables: extractUsedTable(this.config.table)
    });
  }
  prepare() {
    return this._prepare(false);
  }
  run = /* @__PURE__ */ __name((placeholderValues) => {
    return this._prepare().run(placeholderValues);
  }, "run");
  all = /* @__PURE__ */ __name((placeholderValues) => {
    return this._prepare().all(placeholderValues);
  }, "all");
  get = /* @__PURE__ */ __name((placeholderValues) => {
    return this._prepare().get(placeholderValues);
  }, "get");
  values = /* @__PURE__ */ __name((placeholderValues) => {
    return this._prepare().values(placeholderValues);
  }, "values");
  async execute(placeholderValues) {
    return this._prepare().execute(placeholderValues);
  }
  $dynamic() {
    return this;
  }
};
function toSnakeCase(input) {
  const words = input.replace(/['\u2019]/g, "").match(/[\da-z]+|[A-Z]+(?![a-z])|[A-Z][\da-z]+/g) ?? [];
  return words.map((word) => word.toLowerCase()).join("_");
}
__name(toSnakeCase, "toSnakeCase");
function toCamelCase(input) {
  const words = input.replace(/['\u2019]/g, "").match(/[\da-z]+|[A-Z]+(?![a-z])|[A-Z][\da-z]+/g) ?? [];
  return words.reduce((acc, word, i) => {
    const formattedWord = i === 0 ? word.toLowerCase() : `${word[0].toUpperCase()}${word.slice(1)}`;
    return acc + formattedWord;
  }, "");
}
__name(toCamelCase, "toCamelCase");
function noopCase(input) {
  return input;
}
__name(noopCase, "noopCase");
var CasingCache = class {
  static {
    __name(this, "CasingCache");
  }
  static [entityKind] = "CasingCache";
  cache = {};
  cachedTables = {};
  convert;
  constructor(casing) {
    this.convert = casing === "snake_case" ? toSnakeCase : casing === "camelCase" ? toCamelCase : noopCase;
  }
  getColumnCasing(column) {
    if (!column.keyAsName)
      return column.name;
    const schema = column.table[Table.Symbol.Schema] ?? "public";
    const tableName = column.table[Table.Symbol.OriginalName];
    const key = `${schema}.${tableName}.${column.name}`;
    if (!this.cache[key]) {
      this.cacheTable(column.table);
    }
    return this.cache[key];
  }
  cacheTable(table3) {
    const schema = table3[Table.Symbol.Schema] ?? "public";
    const tableName = table3[Table.Symbol.OriginalName];
    const tableKey = `${schema}.${tableName}`;
    if (!this.cachedTables[tableKey]) {
      for (const column of Object.values(table3[Table.Symbol.Columns])) {
        const columnKey = `${tableKey}.${column.name}`;
        this.cache[columnKey] = this.convert(column.name);
      }
      this.cachedTables[tableKey] = true;
    }
  }
  clearCache() {
    this.cache = {};
    this.cachedTables = {};
  }
};
var DrizzleError = class extends Error {
  static {
    __name(this, "DrizzleError");
  }
  static [entityKind] = "DrizzleError";
  constructor({ message: message2, cause }) {
    super(message2);
    this.name = "DrizzleError";
    this.cause = cause;
  }
};
var DrizzleQueryError = class _DrizzleQueryError extends Error {
  static {
    __name(this, "DrizzleQueryError");
  }
  constructor(query, params, cause) {
    super(`Failed query: ${query}
params: ${params}`);
    this.query = query;
    this.params = params;
    this.cause = cause;
    Error.captureStackTrace(this, _DrizzleQueryError);
    if (cause)
      this.cause = cause;
  }
};
var TransactionRollbackError = class extends DrizzleError {
  static {
    __name(this, "TransactionRollbackError");
  }
  static [entityKind] = "TransactionRollbackError";
  constructor() {
    super({ message: "Rollback" });
  }
};
var SQLiteViewBase = class extends View {
  static {
    __name(this, "SQLiteViewBase");
  }
  static [entityKind] = "SQLiteViewBase";
};
var SQLiteDialect = class {
  static {
    __name(this, "SQLiteDialect");
  }
  static [entityKind] = "SQLiteDialect";
  casing;
  constructor(config2) {
    this.casing = new CasingCache(config2?.casing);
  }
  escapeName(name) {
    return `"${name}"`;
  }
  escapeParam(_num) {
    return "?";
  }
  escapeString(str) {
    return `'${str.replace(/'/g, "''")}'`;
  }
  buildWithCTE(queries) {
    if (!queries?.length)
      return;
    const withSqlChunks = [sql`with `];
    for (const [i, w] of queries.entries()) {
      withSqlChunks.push(sql`${sql.identifier(w._.alias)} as (${w._.sql})`);
      if (i < queries.length - 1) {
        withSqlChunks.push(sql`, `);
      }
    }
    withSqlChunks.push(sql` `);
    return sql.join(withSqlChunks);
  }
  buildDeleteQuery({ table: table3, where, returning, withList, limit: limit2, orderBy }) {
    const withSql = this.buildWithCTE(withList);
    const returningSql = returning ? sql` returning ${this.buildSelection(returning, { isSingleTable: true })}` : void 0;
    const whereSql = where ? sql` where ${where}` : void 0;
    const orderBySql = this.buildOrderBy(orderBy);
    const limitSql = this.buildLimit(limit2);
    return sql`${withSql}delete from ${table3}${whereSql}${returningSql}${orderBySql}${limitSql}`;
  }
  buildUpdateSet(table3, set) {
    const tableColumns = table3[Table.Symbol.Columns];
    const columnNames = Object.keys(tableColumns).filter((colName) => set[colName] !== void 0 || tableColumns[colName]?.onUpdateFn !== void 0);
    const setSize = columnNames.length;
    return sql.join(columnNames.flatMap((colName, i) => {
      const col = tableColumns[colName];
      const onUpdateFnResult = col.onUpdateFn?.();
      const value = set[colName] ?? (is(onUpdateFnResult, SQL) ? onUpdateFnResult : sql.param(onUpdateFnResult, col));
      const res = sql`${sql.identifier(this.casing.getColumnCasing(col))} = ${value}`;
      if (i < setSize - 1) {
        return [res, sql.raw(", ")];
      }
      return [res];
    }));
  }
  buildUpdateQuery({ table: table3, set, where, returning, withList, joins, from, limit: limit2, orderBy }) {
    const withSql = this.buildWithCTE(withList);
    const setSql = this.buildUpdateSet(table3, set);
    const fromSql = from && sql.join([sql.raw(" from "), this.buildFromTable(from)]);
    const joinsSql = this.buildJoins(joins);
    const returningSql = returning ? sql` returning ${this.buildSelection(returning, { isSingleTable: true })}` : void 0;
    const whereSql = where ? sql` where ${where}` : void 0;
    const orderBySql = this.buildOrderBy(orderBy);
    const limitSql = this.buildLimit(limit2);
    return sql`${withSql}update ${table3} set ${setSql}${fromSql}${joinsSql}${whereSql}${returningSql}${orderBySql}${limitSql}`;
  }
  buildSelection(fields, { isSingleTable = false } = {}) {
    const columnsLen = fields.length;
    const chunks = fields.flatMap(({ field }, i) => {
      const chunk = [];
      if (is(field, SQL.Aliased) && field.isSelectionField) {
        chunk.push(sql.identifier(field.fieldAlias));
      } else if (is(field, SQL.Aliased) || is(field, SQL)) {
        const query = is(field, SQL.Aliased) ? field.sql : field;
        if (isSingleTable) {
          chunk.push(new SQL(query.queryChunks.map((c) => {
            if (is(c, Column)) {
              return sql.identifier(this.casing.getColumnCasing(c));
            }
            return c;
          })));
        } else {
          chunk.push(query);
        }
        if (is(field, SQL.Aliased)) {
          chunk.push(sql` as ${sql.identifier(field.fieldAlias)}`);
        }
      } else if (is(field, Column)) {
        const tableName = field.table[Table.Symbol.Name];
        if (field.columnType === "SQLiteNumericBigInt") {
          if (isSingleTable) {
            chunk.push(sql`cast(${sql.identifier(this.casing.getColumnCasing(field))} as text)`);
          } else {
            chunk.push(sql`cast(${sql.identifier(tableName)}.${sql.identifier(this.casing.getColumnCasing(field))} as text)`);
          }
        } else {
          if (isSingleTable) {
            chunk.push(sql.identifier(this.casing.getColumnCasing(field)));
          } else {
            chunk.push(sql`${sql.identifier(tableName)}.${sql.identifier(this.casing.getColumnCasing(field))}`);
          }
        }
      } else if (is(field, Subquery)) {
        const entries = Object.entries(field._.selectedFields);
        if (entries.length === 1) {
          const entry = entries[0][1];
          const fieldDecoder = is(entry, SQL) ? entry.decoder : is(entry, Column) ? { mapFromDriverValue: /* @__PURE__ */ __name((v) => entry.mapFromDriverValue(v), "mapFromDriverValue") } : entry.sql.decoder;
          if (fieldDecoder)
            field._.sql.decoder = fieldDecoder;
        }
        chunk.push(field);
      }
      if (i < columnsLen - 1) {
        chunk.push(sql`, `);
      }
      return chunk;
    });
    return sql.join(chunks);
  }
  buildJoins(joins) {
    if (!joins || joins.length === 0) {
      return;
    }
    const joinsArray = [];
    if (joins) {
      for (const [index, joinMeta] of joins.entries()) {
        if (index === 0) {
          joinsArray.push(sql` `);
        }
        const table3 = joinMeta.table;
        const onSql = joinMeta.on ? sql` on ${joinMeta.on}` : void 0;
        if (is(table3, SQLiteTable)) {
          const tableName = table3[SQLiteTable.Symbol.Name];
          const tableSchema = table3[SQLiteTable.Symbol.Schema];
          const origTableName = table3[SQLiteTable.Symbol.OriginalName];
          const alias = tableName === origTableName ? void 0 : joinMeta.alias;
          joinsArray.push(sql`${sql.raw(joinMeta.joinType)} join ${tableSchema ? sql`${sql.identifier(tableSchema)}.` : void 0}${sql.identifier(origTableName)}${alias && sql` ${sql.identifier(alias)}`}${onSql}`);
        } else {
          joinsArray.push(sql`${sql.raw(joinMeta.joinType)} join ${table3}${onSql}`);
        }
        if (index < joins.length - 1) {
          joinsArray.push(sql` `);
        }
      }
    }
    return sql.join(joinsArray);
  }
  buildLimit(limit2) {
    return typeof limit2 === "object" || typeof limit2 === "number" && limit2 >= 0 ? sql` limit ${limit2}` : void 0;
  }
  buildOrderBy(orderBy) {
    const orderByList = [];
    if (orderBy) {
      for (const [index, orderByValue] of orderBy.entries()) {
        orderByList.push(orderByValue);
        if (index < orderBy.length - 1) {
          orderByList.push(sql`, `);
        }
      }
    }
    return orderByList.length > 0 ? sql` order by ${sql.join(orderByList)}` : void 0;
  }
  buildFromTable(table3) {
    if (is(table3, Table) && table3[Table.Symbol.IsAlias]) {
      return sql`${sql`${sql.identifier(table3[Table.Symbol.Schema] ?? "")}.`.if(table3[Table.Symbol.Schema])}${sql.identifier(table3[Table.Symbol.OriginalName])} ${sql.identifier(table3[Table.Symbol.Name])}`;
    }
    return table3;
  }
  buildSelectQuery({
    withList,
    fields,
    fieldsFlat,
    where,
    having,
    table: table3,
    joins,
    orderBy,
    groupBy,
    limit: limit2,
    offset,
    distinct,
    setOperators
  }) {
    const fieldsList = fieldsFlat ?? orderSelectedFields(fields);
    for (const f of fieldsList) {
      if (is(f.field, Column) && getTableName(f.field.table) !== (is(table3, Subquery) ? table3._.alias : is(table3, SQLiteViewBase) ? table3[ViewBaseConfig].name : is(table3, SQL) ? void 0 : getTableName(table3)) && !((table22) => joins?.some(({ alias }) => alias === (table22[Table.Symbol.IsAlias] ? getTableName(table22) : table22[Table.Symbol.BaseName])))(f.field.table)) {
        const tableName = getTableName(f.field.table);
        throw new Error(`Your "${f.path.join("->")}" field references a column "${tableName}"."${f.field.name}", but the table "${tableName}" is not part of the query! Did you forget to join it?`);
      }
    }
    const isSingleTable = !joins || joins.length === 0;
    const withSql = this.buildWithCTE(withList);
    const distinctSql = distinct ? sql` distinct` : void 0;
    const selection = this.buildSelection(fieldsList, { isSingleTable });
    const tableSql = this.buildFromTable(table3);
    const joinsSql = this.buildJoins(joins);
    const whereSql = where ? sql` where ${where}` : void 0;
    const havingSql = having ? sql` having ${having}` : void 0;
    const groupByList = [];
    if (groupBy) {
      for (const [index, groupByValue] of groupBy.entries()) {
        groupByList.push(groupByValue);
        if (index < groupBy.length - 1) {
          groupByList.push(sql`, `);
        }
      }
    }
    const groupBySql = groupByList.length > 0 ? sql` group by ${sql.join(groupByList)}` : void 0;
    const orderBySql = this.buildOrderBy(orderBy);
    const limitSql = this.buildLimit(limit2);
    const offsetSql = offset ? sql` offset ${offset}` : void 0;
    const finalQuery = sql`${withSql}select${distinctSql} ${selection} from ${tableSql}${joinsSql}${whereSql}${groupBySql}${havingSql}${orderBySql}${limitSql}${offsetSql}`;
    if (setOperators.length > 0) {
      return this.buildSetOperations(finalQuery, setOperators);
    }
    return finalQuery;
  }
  buildSetOperations(leftSelect, setOperators) {
    const [setOperator, ...rest] = setOperators;
    if (!setOperator) {
      throw new Error("Cannot pass undefined values to any set operator");
    }
    if (rest.length === 0) {
      return this.buildSetOperationQuery({ leftSelect, setOperator });
    }
    return this.buildSetOperations(this.buildSetOperationQuery({ leftSelect, setOperator }), rest);
  }
  buildSetOperationQuery({
    leftSelect,
    setOperator: { type, isAll, rightSelect, limit: limit2, orderBy, offset }
  }) {
    const leftChunk = sql`${leftSelect.getSQL()} `;
    const rightChunk = sql`${rightSelect.getSQL()}`;
    let orderBySql;
    if (orderBy && orderBy.length > 0) {
      const orderByValues = [];
      for (const singleOrderBy of orderBy) {
        if (is(singleOrderBy, SQLiteColumn)) {
          orderByValues.push(sql.identifier(singleOrderBy.name));
        } else if (is(singleOrderBy, SQL)) {
          for (let i = 0; i < singleOrderBy.queryChunks.length; i++) {
            const chunk = singleOrderBy.queryChunks[i];
            if (is(chunk, SQLiteColumn)) {
              singleOrderBy.queryChunks[i] = sql.identifier(this.casing.getColumnCasing(chunk));
            }
          }
          orderByValues.push(sql`${singleOrderBy}`);
        } else {
          orderByValues.push(sql`${singleOrderBy}`);
        }
      }
      orderBySql = sql` order by ${sql.join(orderByValues, sql`, `)}`;
    }
    const limitSql = typeof limit2 === "object" || typeof limit2 === "number" && limit2 >= 0 ? sql` limit ${limit2}` : void 0;
    const operatorChunk = sql.raw(`${type} ${isAll ? "all " : ""}`);
    const offsetSql = offset ? sql` offset ${offset}` : void 0;
    return sql`${leftChunk}${operatorChunk}${rightChunk}${orderBySql}${limitSql}${offsetSql}`;
  }
  buildInsertQuery({ table: table3, values: valuesOrSelect, onConflict, returning, withList, select }) {
    const valuesSqlList = [];
    const columns = table3[Table.Symbol.Columns];
    const colEntries = Object.entries(columns).filter(([_, col]) => !col.shouldDisableInsert());
    const insertOrder = colEntries.map(([, column]) => sql.identifier(this.casing.getColumnCasing(column)));
    if (select) {
      const select2 = valuesOrSelect;
      if (is(select2, SQL)) {
        valuesSqlList.push(select2);
      } else {
        valuesSqlList.push(select2.getSQL());
      }
    } else {
      const values = valuesOrSelect;
      valuesSqlList.push(sql.raw("values "));
      for (const [valueIndex, value] of values.entries()) {
        const valueList = [];
        for (const [fieldName, col] of colEntries) {
          const colValue = value[fieldName];
          if (colValue === void 0 || is(colValue, Param) && colValue.value === void 0) {
            let defaultValue;
            if (col.default !== null && col.default !== void 0) {
              defaultValue = is(col.default, SQL) ? col.default : sql.param(col.default, col);
            } else if (col.defaultFn !== void 0) {
              const defaultFnResult = col.defaultFn();
              defaultValue = is(defaultFnResult, SQL) ? defaultFnResult : sql.param(defaultFnResult, col);
            } else if (!col.default && col.onUpdateFn !== void 0) {
              const onUpdateFnResult = col.onUpdateFn();
              defaultValue = is(onUpdateFnResult, SQL) ? onUpdateFnResult : sql.param(onUpdateFnResult, col);
            } else {
              defaultValue = sql`null`;
            }
            valueList.push(defaultValue);
          } else {
            valueList.push(colValue);
          }
        }
        valuesSqlList.push(valueList);
        if (valueIndex < values.length - 1) {
          valuesSqlList.push(sql`, `);
        }
      }
    }
    const withSql = this.buildWithCTE(withList);
    const valuesSql = sql.join(valuesSqlList);
    const returningSql = returning ? sql` returning ${this.buildSelection(returning, { isSingleTable: true })}` : void 0;
    const onConflictSql = onConflict?.length ? sql.join(onConflict) : void 0;
    return sql`${withSql}insert into ${table3} ${insertOrder} ${valuesSql}${onConflictSql}${returningSql}`;
  }
  sqlToQuery(sql22, invokeSource) {
    return sql22.toQuery({
      casing: this.casing,
      escapeName: this.escapeName,
      escapeParam: this.escapeParam,
      escapeString: this.escapeString,
      invokeSource
    });
  }
  buildRelationalQuery({
    fullSchema,
    schema,
    tableNamesMap,
    table: table3,
    tableConfig,
    queryConfig: config2,
    tableAlias,
    nestedQueryRelation,
    joinOn
  }) {
    let selection = [];
    let limit2, offset, orderBy = [], where;
    const joins = [];
    if (config2 === true) {
      const selectionEntries = Object.entries(tableConfig.columns);
      selection = selectionEntries.map(([key, value]) => ({
        dbKey: value.name,
        tsKey: key,
        field: aliasedTableColumn(value, tableAlias),
        relationTableTsKey: void 0,
        isJson: false,
        selection: []
      }));
    } else {
      const aliasedColumns = Object.fromEntries(Object.entries(tableConfig.columns).map(([key, value]) => [key, aliasedTableColumn(value, tableAlias)]));
      if (config2.where) {
        const whereSql = typeof config2.where === "function" ? config2.where(aliasedColumns, getOperators()) : config2.where;
        where = whereSql && mapColumnsInSQLToAlias(whereSql, tableAlias);
      }
      const fieldsSelection = [];
      let selectedColumns = [];
      if (config2.columns) {
        let isIncludeMode = false;
        for (const [field, value] of Object.entries(config2.columns)) {
          if (value === void 0) {
            continue;
          }
          if (field in tableConfig.columns) {
            if (!isIncludeMode && value === true) {
              isIncludeMode = true;
            }
            selectedColumns.push(field);
          }
        }
        if (selectedColumns.length > 0) {
          selectedColumns = isIncludeMode ? selectedColumns.filter((c) => config2.columns?.[c] === true) : Object.keys(tableConfig.columns).filter((key) => !selectedColumns.includes(key));
        }
      } else {
        selectedColumns = Object.keys(tableConfig.columns);
      }
      for (const field of selectedColumns) {
        const column = tableConfig.columns[field];
        fieldsSelection.push({ tsKey: field, value: column });
      }
      let selectedRelations = [];
      if (config2.with) {
        selectedRelations = Object.entries(config2.with).filter((entry) => !!entry[1]).map(([tsKey, queryConfig]) => ({ tsKey, queryConfig, relation: tableConfig.relations[tsKey] }));
      }
      let extras;
      if (config2.extras) {
        extras = typeof config2.extras === "function" ? config2.extras(aliasedColumns, { sql }) : config2.extras;
        for (const [tsKey, value] of Object.entries(extras)) {
          fieldsSelection.push({
            tsKey,
            value: mapColumnsInAliasedSQLToAlias(value, tableAlias)
          });
        }
      }
      for (const { tsKey, value } of fieldsSelection) {
        selection.push({
          dbKey: is(value, SQL.Aliased) ? value.fieldAlias : tableConfig.columns[tsKey].name,
          tsKey,
          field: is(value, Column) ? aliasedTableColumn(value, tableAlias) : value,
          relationTableTsKey: void 0,
          isJson: false,
          selection: []
        });
      }
      let orderByOrig = typeof config2.orderBy === "function" ? config2.orderBy(aliasedColumns, getOrderByOperators()) : config2.orderBy ?? [];
      if (!Array.isArray(orderByOrig)) {
        orderByOrig = [orderByOrig];
      }
      orderBy = orderByOrig.map((orderByValue) => {
        if (is(orderByValue, Column)) {
          return aliasedTableColumn(orderByValue, tableAlias);
        }
        return mapColumnsInSQLToAlias(orderByValue, tableAlias);
      });
      limit2 = config2.limit;
      offset = config2.offset;
      for (const {
        tsKey: selectedRelationTsKey,
        queryConfig: selectedRelationConfigValue,
        relation
      } of selectedRelations) {
        const normalizedRelation = normalizeRelation(schema, tableNamesMap, relation);
        const relationTableName = getTableUniqueName(relation.referencedTable);
        const relationTableTsName = tableNamesMap[relationTableName];
        const relationTableAlias = `${tableAlias}_${selectedRelationTsKey}`;
        const joinOn2 = and(...normalizedRelation.fields.map((field2, i) => eq(aliasedTableColumn(normalizedRelation.references[i], relationTableAlias), aliasedTableColumn(field2, tableAlias))));
        const builtRelation = this.buildRelationalQuery({
          fullSchema,
          schema,
          tableNamesMap,
          table: fullSchema[relationTableTsName],
          tableConfig: schema[relationTableTsName],
          queryConfig: is(relation, One) ? selectedRelationConfigValue === true ? { limit: 1 } : { ...selectedRelationConfigValue, limit: 1 } : selectedRelationConfigValue,
          tableAlias: relationTableAlias,
          joinOn: joinOn2,
          nestedQueryRelation: relation
        });
        const field = sql`(${builtRelation.sql})`.as(selectedRelationTsKey);
        selection.push({
          dbKey: selectedRelationTsKey,
          tsKey: selectedRelationTsKey,
          field,
          relationTableTsKey: relationTableTsName,
          isJson: true,
          selection: builtRelation.selection
        });
      }
    }
    if (selection.length === 0) {
      throw new DrizzleError({
        message: `No fields selected for table "${tableConfig.tsName}" ("${tableAlias}"). You need to have at least one item in "columns", "with" or "extras". If you need to select all columns, omit the "columns" key or set it to undefined.`
      });
    }
    let result;
    where = and(joinOn, where);
    if (nestedQueryRelation) {
      let field = sql`json_array(${sql.join(selection.map(({ field: field2 }) => is(field2, SQLiteColumn) ? sql.identifier(this.casing.getColumnCasing(field2)) : is(field2, SQL.Aliased) ? field2.sql : field2), sql`, `)})`;
      if (is(nestedQueryRelation, Many)) {
        field = sql`coalesce(json_group_array(${field}), json_array())`;
      }
      const nestedSelection = [{
        dbKey: "data",
        tsKey: "data",
        field: field.as("data"),
        isJson: true,
        relationTableTsKey: tableConfig.tsName,
        selection
      }];
      const needsSubquery = limit2 !== void 0 || offset !== void 0 || orderBy.length > 0;
      if (needsSubquery) {
        result = this.buildSelectQuery({
          table: aliasedTable(table3, tableAlias),
          fields: {},
          fieldsFlat: [
            {
              path: [],
              field: sql.raw("*")
            }
          ],
          where,
          limit: limit2,
          offset,
          orderBy,
          setOperators: []
        });
        where = void 0;
        limit2 = void 0;
        offset = void 0;
        orderBy = void 0;
      } else {
        result = aliasedTable(table3, tableAlias);
      }
      result = this.buildSelectQuery({
        table: is(result, SQLiteTable) ? result : new Subquery(result, {}, tableAlias),
        fields: {},
        fieldsFlat: nestedSelection.map(({ field: field2 }) => ({
          path: [],
          field: is(field2, Column) ? aliasedTableColumn(field2, tableAlias) : field2
        })),
        joins,
        where,
        limit: limit2,
        offset,
        orderBy,
        setOperators: []
      });
    } else {
      result = this.buildSelectQuery({
        table: aliasedTable(table3, tableAlias),
        fields: {},
        fieldsFlat: selection.map(({ field }) => ({
          path: [],
          field: is(field, Column) ? aliasedTableColumn(field, tableAlias) : field
        })),
        joins,
        where,
        limit: limit2,
        offset,
        orderBy,
        setOperators: []
      });
    }
    return {
      tableTsKey: tableConfig.tsName,
      sql: result,
      selection
    };
  }
};
var SQLiteSyncDialect = class extends SQLiteDialect {
  static {
    __name(this, "SQLiteSyncDialect");
  }
  static [entityKind] = "SQLiteSyncDialect";
  migrate(migrations, session, config2) {
    const migrationsTable = config2 === void 0 ? "__drizzle_migrations" : typeof config2 === "string" ? "__drizzle_migrations" : config2.migrationsTable ?? "__drizzle_migrations";
    const migrationTableCreate = sql`
			CREATE TABLE IF NOT EXISTS ${sql.identifier(migrationsTable)} (
				id SERIAL PRIMARY KEY,
				hash text NOT NULL,
				created_at numeric
			)
		`;
    session.run(migrationTableCreate);
    const dbMigrations = session.values(sql`SELECT id, hash, created_at FROM ${sql.identifier(migrationsTable)} ORDER BY created_at DESC LIMIT 1`);
    const lastDbMigration = dbMigrations[0] ?? void 0;
    session.run(sql`BEGIN`);
    try {
      for (const migration of migrations) {
        if (!lastDbMigration || Number(lastDbMigration[2]) < migration.folderMillis) {
          for (const stmt of migration.sql) {
            session.run(sql.raw(stmt));
          }
          session.run(sql`INSERT INTO ${sql.identifier(migrationsTable)} ("hash", "created_at") VALUES(${migration.hash}, ${migration.folderMillis})`);
        }
      }
      session.run(sql`COMMIT`);
    } catch (e) {
      session.run(sql`ROLLBACK`);
      throw e;
    }
  }
};
var SQLiteAsyncDialect = class extends SQLiteDialect {
  static {
    __name(this, "SQLiteAsyncDialect");
  }
  static [entityKind] = "SQLiteAsyncDialect";
  async migrate(migrations, session, config2) {
    const migrationsTable = config2 === void 0 ? "__drizzle_migrations" : typeof config2 === "string" ? "__drizzle_migrations" : config2.migrationsTable ?? "__drizzle_migrations";
    const migrationTableCreate = sql`
			CREATE TABLE IF NOT EXISTS ${sql.identifier(migrationsTable)} (
				id SERIAL PRIMARY KEY,
				hash text NOT NULL,
				created_at numeric
			)
		`;
    await session.run(migrationTableCreate);
    const dbMigrations = await session.values(sql`SELECT id, hash, created_at FROM ${sql.identifier(migrationsTable)} ORDER BY created_at DESC LIMIT 1`);
    const lastDbMigration = dbMigrations[0] ?? void 0;
    await session.transaction(async (tx) => {
      for (const migration of migrations) {
        if (!lastDbMigration || Number(lastDbMigration[2]) < migration.folderMillis) {
          for (const stmt of migration.sql) {
            await tx.run(sql.raw(stmt));
          }
          await tx.run(sql`INSERT INTO ${sql.identifier(migrationsTable)} ("hash", "created_at") VALUES(${migration.hash}, ${migration.folderMillis})`);
        }
      }
    });
  }
};
var TypedQueryBuilder = class {
  static {
    __name(this, "TypedQueryBuilder");
  }
  static [entityKind] = "TypedQueryBuilder";
  getSelectedFields() {
    return this._.selectedFields;
  }
};
var SQLiteSelectBuilder = class {
  static {
    __name(this, "SQLiteSelectBuilder");
  }
  static [entityKind] = "SQLiteSelectBuilder";
  fields;
  session;
  dialect;
  withList;
  distinct;
  constructor(config2) {
    this.fields = config2.fields;
    this.session = config2.session;
    this.dialect = config2.dialect;
    this.withList = config2.withList;
    this.distinct = config2.distinct;
  }
  from(source) {
    const isPartialSelect = !!this.fields;
    let fields;
    if (this.fields) {
      fields = this.fields;
    } else if (is(source, Subquery)) {
      fields = Object.fromEntries(Object.keys(source._.selectedFields).map((key) => [key, source[key]]));
    } else if (is(source, SQLiteViewBase)) {
      fields = source[ViewBaseConfig].selectedFields;
    } else if (is(source, SQL)) {
      fields = {};
    } else {
      fields = getTableColumns(source);
    }
    return new SQLiteSelectBase({
      table: source,
      fields,
      isPartialSelect,
      session: this.session,
      dialect: this.dialect,
      withList: this.withList,
      distinct: this.distinct
    });
  }
};
var SQLiteSelectQueryBuilderBase = class extends TypedQueryBuilder {
  static {
    __name(this, "SQLiteSelectQueryBuilderBase");
  }
  static [entityKind] = "SQLiteSelectQueryBuilder";
  _;
  config;
  joinsNotNullableMap;
  tableName;
  isPartialSelect;
  session;
  dialect;
  cacheConfig = void 0;
  usedTables = /* @__PURE__ */ new Set();
  constructor({ table: table3, fields, isPartialSelect, session, dialect, withList, distinct }) {
    super();
    this.config = {
      withList,
      table: table3,
      fields: { ...fields },
      distinct,
      setOperators: []
    };
    this.isPartialSelect = isPartialSelect;
    this.session = session;
    this.dialect = dialect;
    this._ = {
      selectedFields: fields,
      config: this.config
    };
    this.tableName = getTableLikeName(table3);
    this.joinsNotNullableMap = typeof this.tableName === "string" ? { [this.tableName]: true } : {};
    for (const item of extractUsedTable(table3))
      this.usedTables.add(item);
  }
  getUsedTables() {
    return [...this.usedTables];
  }
  createJoin(joinType) {
    return (table3, on2) => {
      const baseTableName = this.tableName;
      const tableName = getTableLikeName(table3);
      for (const item of extractUsedTable(table3))
        this.usedTables.add(item);
      if (typeof tableName === "string" && this.config.joins?.some((join) => join.alias === tableName)) {
        throw new Error(`Alias "${tableName}" is already used in this query`);
      }
      if (!this.isPartialSelect) {
        if (Object.keys(this.joinsNotNullableMap).length === 1 && typeof baseTableName === "string") {
          this.config.fields = {
            [baseTableName]: this.config.fields
          };
        }
        if (typeof tableName === "string" && !is(table3, SQL)) {
          const selection = is(table3, Subquery) ? table3._.selectedFields : is(table3, View) ? table3[ViewBaseConfig].selectedFields : table3[Table.Symbol.Columns];
          this.config.fields[tableName] = selection;
        }
      }
      if (typeof on2 === "function") {
        on2 = on2(new Proxy(this.config.fields, new SelectionProxyHandler({ sqlAliasedBehavior: "sql", sqlBehavior: "sql" })));
      }
      if (!this.config.joins) {
        this.config.joins = [];
      }
      this.config.joins.push({ on: on2, table: table3, joinType, alias: tableName });
      if (typeof tableName === "string") {
        switch (joinType) {
          case "left": {
            this.joinsNotNullableMap[tableName] = false;
            break;
          }
          case "right": {
            this.joinsNotNullableMap = Object.fromEntries(Object.entries(this.joinsNotNullableMap).map(([key]) => [key, false]));
            this.joinsNotNullableMap[tableName] = true;
            break;
          }
          case "cross":
          case "inner": {
            this.joinsNotNullableMap[tableName] = true;
            break;
          }
          case "full": {
            this.joinsNotNullableMap = Object.fromEntries(Object.entries(this.joinsNotNullableMap).map(([key]) => [key, false]));
            this.joinsNotNullableMap[tableName] = false;
            break;
          }
        }
      }
      return this;
    };
  }
  leftJoin = this.createJoin("left");
  rightJoin = this.createJoin("right");
  innerJoin = this.createJoin("inner");
  fullJoin = this.createJoin("full");
  crossJoin = this.createJoin("cross");
  createSetOperator(type, isAll) {
    return (rightSelection) => {
      const rightSelect = typeof rightSelection === "function" ? rightSelection(getSQLiteSetOperators()) : rightSelection;
      if (!haveSameKeys(this.getSelectedFields(), rightSelect.getSelectedFields())) {
        throw new Error("Set operator error (union / intersect / except): selected fields are not the same or are in a different order");
      }
      this.config.setOperators.push({ type, isAll, rightSelect });
      return this;
    };
  }
  union = this.createSetOperator("union", false);
  unionAll = this.createSetOperator("union", true);
  intersect = this.createSetOperator("intersect", false);
  except = this.createSetOperator("except", false);
  addSetOperators(setOperators) {
    this.config.setOperators.push(...setOperators);
    return this;
  }
  where(where) {
    if (typeof where === "function") {
      where = where(new Proxy(this.config.fields, new SelectionProxyHandler({ sqlAliasedBehavior: "sql", sqlBehavior: "sql" })));
    }
    this.config.where = where;
    return this;
  }
  having(having) {
    if (typeof having === "function") {
      having = having(new Proxy(this.config.fields, new SelectionProxyHandler({ sqlAliasedBehavior: "sql", sqlBehavior: "sql" })));
    }
    this.config.having = having;
    return this;
  }
  groupBy(...columns) {
    if (typeof columns[0] === "function") {
      const groupBy = columns[0](new Proxy(this.config.fields, new SelectionProxyHandler({ sqlAliasedBehavior: "alias", sqlBehavior: "sql" })));
      this.config.groupBy = Array.isArray(groupBy) ? groupBy : [groupBy];
    } else {
      this.config.groupBy = columns;
    }
    return this;
  }
  orderBy(...columns) {
    if (typeof columns[0] === "function") {
      const orderBy = columns[0](new Proxy(this.config.fields, new SelectionProxyHandler({ sqlAliasedBehavior: "alias", sqlBehavior: "sql" })));
      const orderByArray = Array.isArray(orderBy) ? orderBy : [orderBy];
      if (this.config.setOperators.length > 0) {
        this.config.setOperators.at(-1).orderBy = orderByArray;
      } else {
        this.config.orderBy = orderByArray;
      }
    } else {
      const orderByArray = columns;
      if (this.config.setOperators.length > 0) {
        this.config.setOperators.at(-1).orderBy = orderByArray;
      } else {
        this.config.orderBy = orderByArray;
      }
    }
    return this;
  }
  limit(limit2) {
    if (this.config.setOperators.length > 0) {
      this.config.setOperators.at(-1).limit = limit2;
    } else {
      this.config.limit = limit2;
    }
    return this;
  }
  offset(offset) {
    if (this.config.setOperators.length > 0) {
      this.config.setOperators.at(-1).offset = offset;
    } else {
      this.config.offset = offset;
    }
    return this;
  }
  getSQL() {
    return this.dialect.buildSelectQuery(this.config);
  }
  toSQL() {
    const { typings: _typings, ...rest } = this.dialect.sqlToQuery(this.getSQL());
    return rest;
  }
  as(alias) {
    const usedTables = [];
    usedTables.push(...extractUsedTable(this.config.table));
    if (this.config.joins) {
      for (const it of this.config.joins)
        usedTables.push(...extractUsedTable(it.table));
    }
    return new Proxy(new Subquery(this.getSQL(), this.config.fields, alias, false, [...new Set(usedTables)]), new SelectionProxyHandler({ alias, sqlAliasedBehavior: "alias", sqlBehavior: "error" }));
  }
  getSelectedFields() {
    return new Proxy(this.config.fields, new SelectionProxyHandler({ alias: this.tableName, sqlAliasedBehavior: "alias", sqlBehavior: "error" }));
  }
  $dynamic() {
    return this;
  }
};
var SQLiteSelectBase = class extends SQLiteSelectQueryBuilderBase {
  static {
    __name(this, "SQLiteSelectBase");
  }
  static [entityKind] = "SQLiteSelect";
  _prepare(isOneTimeQuery = true) {
    if (!this.session) {
      throw new Error("Cannot execute a query on a query builder. Please use a database instance instead.");
    }
    const fieldsList = orderSelectedFields(this.config.fields);
    const query = this.session[isOneTimeQuery ? "prepareOneTimeQuery" : "prepareQuery"](this.dialect.sqlToQuery(this.getSQL()), fieldsList, "all", true, void 0, {
      type: "select",
      tables: [...this.usedTables]
    }, this.cacheConfig);
    query.joinsNotNullableMap = this.joinsNotNullableMap;
    return query;
  }
  $withCache(config2) {
    this.cacheConfig = config2 === void 0 ? { config: {}, enable: true, autoInvalidate: true } : config2 === false ? { enable: false } : { enable: true, autoInvalidate: true, ...config2 };
    return this;
  }
  prepare() {
    return this._prepare(false);
  }
  run = /* @__PURE__ */ __name((placeholderValues) => {
    return this._prepare().run(placeholderValues);
  }, "run");
  all = /* @__PURE__ */ __name((placeholderValues) => {
    return this._prepare().all(placeholderValues);
  }, "all");
  get = /* @__PURE__ */ __name((placeholderValues) => {
    return this._prepare().get(placeholderValues);
  }, "get");
  values = /* @__PURE__ */ __name((placeholderValues) => {
    return this._prepare().values(placeholderValues);
  }, "values");
  async execute() {
    return this.all();
  }
};
applyMixins(SQLiteSelectBase, [QueryPromise]);
function createSetOperator(type, isAll) {
  return (leftSelect, rightSelect, ...restSelects) => {
    const setOperators = [rightSelect, ...restSelects].map((select) => ({
      type,
      isAll,
      rightSelect: select
    }));
    for (const setOperator of setOperators) {
      if (!haveSameKeys(leftSelect.getSelectedFields(), setOperator.rightSelect.getSelectedFields())) {
        throw new Error("Set operator error (union / intersect / except): selected fields are not the same or are in a different order");
      }
    }
    return leftSelect.addSetOperators(setOperators);
  };
}
__name(createSetOperator, "createSetOperator");
var getSQLiteSetOperators = /* @__PURE__ */ __name(() => ({
  union,
  unionAll,
  intersect,
  except
}), "getSQLiteSetOperators");
var union = createSetOperator("union", false);
var unionAll = createSetOperator("union", true);
var intersect = createSetOperator("intersect", false);
var except = createSetOperator("except", false);
var QueryBuilder = class {
  static {
    __name(this, "QueryBuilder");
  }
  static [entityKind] = "SQLiteQueryBuilder";
  dialect;
  dialectConfig;
  constructor(dialect) {
    this.dialect = is(dialect, SQLiteDialect) ? dialect : void 0;
    this.dialectConfig = is(dialect, SQLiteDialect) ? void 0 : dialect;
  }
  $with = /* @__PURE__ */ __name((alias, selection) => {
    const queryBuilder = this;
    const as = /* @__PURE__ */ __name((qb) => {
      if (typeof qb === "function") {
        qb = qb(queryBuilder);
      }
      return new Proxy(new WithSubquery(qb.getSQL(), selection ?? ("getSelectedFields" in qb ? qb.getSelectedFields() ?? {} : {}), alias, true), new SelectionProxyHandler({ alias, sqlAliasedBehavior: "alias", sqlBehavior: "error" }));
    }, "as");
    return { as };
  }, "$with");
  with(...queries) {
    const self = this;
    function select(fields) {
      return new SQLiteSelectBuilder({
        fields: fields ?? void 0,
        session: void 0,
        dialect: self.getDialect(),
        withList: queries
      });
    }
    __name(select, "select");
    function selectDistinct(fields) {
      return new SQLiteSelectBuilder({
        fields: fields ?? void 0,
        session: void 0,
        dialect: self.getDialect(),
        withList: queries,
        distinct: true
      });
    }
    __name(selectDistinct, "selectDistinct");
    return { select, selectDistinct };
  }
  select(fields) {
    return new SQLiteSelectBuilder({ fields: fields ?? void 0, session: void 0, dialect: this.getDialect() });
  }
  selectDistinct(fields) {
    return new SQLiteSelectBuilder({
      fields: fields ?? void 0,
      session: void 0,
      dialect: this.getDialect(),
      distinct: true
    });
  }
  getDialect() {
    if (!this.dialect) {
      this.dialect = new SQLiteSyncDialect(this.dialectConfig);
    }
    return this.dialect;
  }
};
var SQLiteInsertBuilder = class {
  static {
    __name(this, "SQLiteInsertBuilder");
  }
  constructor(table3, session, dialect, withList) {
    this.table = table3;
    this.session = session;
    this.dialect = dialect;
    this.withList = withList;
  }
  static [entityKind] = "SQLiteInsertBuilder";
  values(values) {
    values = Array.isArray(values) ? values : [values];
    if (values.length === 0) {
      throw new Error("values() must be called with at least one value");
    }
    const mappedValues = values.map((entry) => {
      const result = {};
      const cols = this.table[Table.Symbol.Columns];
      for (const colKey of Object.keys(entry)) {
        const colValue = entry[colKey];
        result[colKey] = is(colValue, SQL) ? colValue : new Param(colValue, cols[colKey]);
      }
      return result;
    });
    return new SQLiteInsertBase(this.table, mappedValues, this.session, this.dialect, this.withList);
  }
  select(selectQuery) {
    const select = typeof selectQuery === "function" ? selectQuery(new QueryBuilder()) : selectQuery;
    if (!is(select, SQL) && !haveSameKeys(this.table[Columns], select._.selectedFields)) {
      throw new Error("Insert select error: selected fields are not the same or are in a different order compared to the table definition");
    }
    return new SQLiteInsertBase(this.table, select, this.session, this.dialect, this.withList, true);
  }
};
var SQLiteInsertBase = class extends QueryPromise {
  static {
    __name(this, "SQLiteInsertBase");
  }
  constructor(table3, values, session, dialect, withList, select) {
    super();
    this.session = session;
    this.dialect = dialect;
    this.config = { table: table3, values, withList, select };
  }
  static [entityKind] = "SQLiteInsert";
  config;
  returning(fields = this.config.table[SQLiteTable.Symbol.Columns]) {
    this.config.returning = orderSelectedFields(fields);
    return this;
  }
  onConflictDoNothing(config2 = {}) {
    if (!this.config.onConflict)
      this.config.onConflict = [];
    if (config2.target === void 0) {
      this.config.onConflict.push(sql` on conflict do nothing`);
    } else {
      const targetSql = Array.isArray(config2.target) ? sql`${config2.target}` : sql`${[config2.target]}`;
      const whereSql = config2.where ? sql` where ${config2.where}` : sql``;
      this.config.onConflict.push(sql` on conflict ${targetSql} do nothing${whereSql}`);
    }
    return this;
  }
  onConflictDoUpdate(config2) {
    if (config2.where && (config2.targetWhere || config2.setWhere)) {
      throw new Error('You cannot use both "where" and "targetWhere"/"setWhere" at the same time - "where" is deprecated, use "targetWhere" or "setWhere" instead.');
    }
    if (!this.config.onConflict)
      this.config.onConflict = [];
    const whereSql = config2.where ? sql` where ${config2.where}` : void 0;
    const targetWhereSql = config2.targetWhere ? sql` where ${config2.targetWhere}` : void 0;
    const setWhereSql = config2.setWhere ? sql` where ${config2.setWhere}` : void 0;
    const targetSql = Array.isArray(config2.target) ? sql`${config2.target}` : sql`${[config2.target]}`;
    const setSql = this.dialect.buildUpdateSet(this.config.table, mapUpdateSet(this.config.table, config2.set));
    this.config.onConflict.push(sql` on conflict ${targetSql}${targetWhereSql} do update set ${setSql}${whereSql}${setWhereSql}`);
    return this;
  }
  getSQL() {
    return this.dialect.buildInsertQuery(this.config);
  }
  toSQL() {
    const { typings: _typings, ...rest } = this.dialect.sqlToQuery(this.getSQL());
    return rest;
  }
  _prepare(isOneTimeQuery = true) {
    return this.session[isOneTimeQuery ? "prepareOneTimeQuery" : "prepareQuery"](this.dialect.sqlToQuery(this.getSQL()), this.config.returning, this.config.returning ? "all" : "run", true, void 0, {
      type: "insert",
      tables: extractUsedTable(this.config.table)
    });
  }
  prepare() {
    return this._prepare(false);
  }
  run = /* @__PURE__ */ __name((placeholderValues) => {
    return this._prepare().run(placeholderValues);
  }, "run");
  all = /* @__PURE__ */ __name((placeholderValues) => {
    return this._prepare().all(placeholderValues);
  }, "all");
  get = /* @__PURE__ */ __name((placeholderValues) => {
    return this._prepare().get(placeholderValues);
  }, "get");
  values = /* @__PURE__ */ __name((placeholderValues) => {
    return this._prepare().values(placeholderValues);
  }, "values");
  async execute() {
    return this.config.returning ? this.all() : this.run();
  }
  $dynamic() {
    return this;
  }
};
var SQLiteUpdateBuilder = class {
  static {
    __name(this, "SQLiteUpdateBuilder");
  }
  constructor(table3, session, dialect, withList) {
    this.table = table3;
    this.session = session;
    this.dialect = dialect;
    this.withList = withList;
  }
  static [entityKind] = "SQLiteUpdateBuilder";
  set(values) {
    return new SQLiteUpdateBase(this.table, mapUpdateSet(this.table, values), this.session, this.dialect, this.withList);
  }
};
var SQLiteUpdateBase = class extends QueryPromise {
  static {
    __name(this, "SQLiteUpdateBase");
  }
  constructor(table3, set, session, dialect, withList) {
    super();
    this.session = session;
    this.dialect = dialect;
    this.config = { set, table: table3, withList, joins: [] };
  }
  static [entityKind] = "SQLiteUpdate";
  config;
  from(source) {
    this.config.from = source;
    return this;
  }
  createJoin(joinType) {
    return (table3, on2) => {
      const tableName = getTableLikeName(table3);
      if (typeof tableName === "string" && this.config.joins.some((join) => join.alias === tableName)) {
        throw new Error(`Alias "${tableName}" is already used in this query`);
      }
      if (typeof on2 === "function") {
        const from = this.config.from ? is(table3, SQLiteTable) ? table3[Table.Symbol.Columns] : is(table3, Subquery) ? table3._.selectedFields : is(table3, SQLiteViewBase) ? table3[ViewBaseConfig].selectedFields : void 0 : void 0;
        on2 = on2(new Proxy(this.config.table[Table.Symbol.Columns], new SelectionProxyHandler({ sqlAliasedBehavior: "sql", sqlBehavior: "sql" })), from && new Proxy(from, new SelectionProxyHandler({ sqlAliasedBehavior: "sql", sqlBehavior: "sql" })));
      }
      this.config.joins.push({ on: on2, table: table3, joinType, alias: tableName });
      return this;
    };
  }
  leftJoin = this.createJoin("left");
  rightJoin = this.createJoin("right");
  innerJoin = this.createJoin("inner");
  fullJoin = this.createJoin("full");
  where(where) {
    this.config.where = where;
    return this;
  }
  orderBy(...columns) {
    if (typeof columns[0] === "function") {
      const orderBy = columns[0](new Proxy(this.config.table[Table.Symbol.Columns], new SelectionProxyHandler({ sqlAliasedBehavior: "alias", sqlBehavior: "sql" })));
      const orderByArray = Array.isArray(orderBy) ? orderBy : [orderBy];
      this.config.orderBy = orderByArray;
    } else {
      const orderByArray = columns;
      this.config.orderBy = orderByArray;
    }
    return this;
  }
  limit(limit2) {
    this.config.limit = limit2;
    return this;
  }
  returning(fields = this.config.table[SQLiteTable.Symbol.Columns]) {
    this.config.returning = orderSelectedFields(fields);
    return this;
  }
  getSQL() {
    return this.dialect.buildUpdateQuery(this.config);
  }
  toSQL() {
    const { typings: _typings, ...rest } = this.dialect.sqlToQuery(this.getSQL());
    return rest;
  }
  _prepare(isOneTimeQuery = true) {
    return this.session[isOneTimeQuery ? "prepareOneTimeQuery" : "prepareQuery"](this.dialect.sqlToQuery(this.getSQL()), this.config.returning, this.config.returning ? "all" : "run", true, void 0, {
      type: "insert",
      tables: extractUsedTable(this.config.table)
    });
  }
  prepare() {
    return this._prepare(false);
  }
  run = /* @__PURE__ */ __name((placeholderValues) => {
    return this._prepare().run(placeholderValues);
  }, "run");
  all = /* @__PURE__ */ __name((placeholderValues) => {
    return this._prepare().all(placeholderValues);
  }, "all");
  get = /* @__PURE__ */ __name((placeholderValues) => {
    return this._prepare().get(placeholderValues);
  }, "get");
  values = /* @__PURE__ */ __name((placeholderValues) => {
    return this._prepare().values(placeholderValues);
  }, "values");
  async execute() {
    return this.config.returning ? this.all() : this.run();
  }
  $dynamic() {
    return this;
  }
};
var SQLiteCountBuilder = class _SQLiteCountBuilder extends SQL {
  static {
    __name(this, "SQLiteCountBuilder");
  }
  constructor(params) {
    super(_SQLiteCountBuilder.buildEmbeddedCount(params.source, params.filters).queryChunks);
    this.params = params;
    this.session = params.session;
    this.sql = _SQLiteCountBuilder.buildCount(params.source, params.filters);
  }
  sql;
  static [entityKind] = "SQLiteCountBuilderAsync";
  [Symbol.toStringTag] = "SQLiteCountBuilderAsync";
  session;
  static buildEmbeddedCount(source, filters) {
    return sql`(select count(*) from ${source}${sql.raw(" where ").if(filters)}${filters})`;
  }
  static buildCount(source, filters) {
    return sql`select count(*) from ${source}${sql.raw(" where ").if(filters)}${filters}`;
  }
  then(onfulfilled, onrejected) {
    return Promise.resolve(this.session.count(this.sql)).then(onfulfilled, onrejected);
  }
  catch(onRejected) {
    return this.then(void 0, onRejected);
  }
  finally(onFinally) {
    return this.then((value) => {
      onFinally?.();
      return value;
    }, (reason) => {
      onFinally?.();
      throw reason;
    });
  }
};
var RelationalQueryBuilder = class {
  static {
    __name(this, "RelationalQueryBuilder");
  }
  constructor(mode, fullSchema, schema, tableNamesMap, table3, tableConfig, dialect, session) {
    this.mode = mode;
    this.fullSchema = fullSchema;
    this.schema = schema;
    this.tableNamesMap = tableNamesMap;
    this.table = table3;
    this.tableConfig = tableConfig;
    this.dialect = dialect;
    this.session = session;
  }
  static [entityKind] = "SQLiteAsyncRelationalQueryBuilder";
  findMany(config2) {
    return this.mode === "sync" ? new SQLiteSyncRelationalQuery(this.fullSchema, this.schema, this.tableNamesMap, this.table, this.tableConfig, this.dialect, this.session, config2 ? config2 : {}, "many") : new SQLiteRelationalQuery(this.fullSchema, this.schema, this.tableNamesMap, this.table, this.tableConfig, this.dialect, this.session, config2 ? config2 : {}, "many");
  }
  findFirst(config2) {
    return this.mode === "sync" ? new SQLiteSyncRelationalQuery(this.fullSchema, this.schema, this.tableNamesMap, this.table, this.tableConfig, this.dialect, this.session, config2 ? { ...config2, limit: 1 } : { limit: 1 }, "first") : new SQLiteRelationalQuery(this.fullSchema, this.schema, this.tableNamesMap, this.table, this.tableConfig, this.dialect, this.session, config2 ? { ...config2, limit: 1 } : { limit: 1 }, "first");
  }
};
var SQLiteRelationalQuery = class extends QueryPromise {
  static {
    __name(this, "SQLiteRelationalQuery");
  }
  constructor(fullSchema, schema, tableNamesMap, table3, tableConfig, dialect, session, config2, mode) {
    super();
    this.fullSchema = fullSchema;
    this.schema = schema;
    this.tableNamesMap = tableNamesMap;
    this.table = table3;
    this.tableConfig = tableConfig;
    this.dialect = dialect;
    this.session = session;
    this.config = config2;
    this.mode = mode;
  }
  static [entityKind] = "SQLiteAsyncRelationalQuery";
  mode;
  getSQL() {
    return this.dialect.buildRelationalQuery({
      fullSchema: this.fullSchema,
      schema: this.schema,
      tableNamesMap: this.tableNamesMap,
      table: this.table,
      tableConfig: this.tableConfig,
      queryConfig: this.config,
      tableAlias: this.tableConfig.tsName
    }).sql;
  }
  _prepare(isOneTimeQuery = false) {
    const { query, builtQuery } = this._toSQL();
    return this.session[isOneTimeQuery ? "prepareOneTimeQuery" : "prepareQuery"](builtQuery, void 0, this.mode === "first" ? "get" : "all", true, (rawRows, mapColumnValue) => {
      const rows = rawRows.map((row) => mapRelationalRow(this.schema, this.tableConfig, row, query.selection, mapColumnValue));
      if (this.mode === "first") {
        return rows[0];
      }
      return rows;
    });
  }
  prepare() {
    return this._prepare(false);
  }
  _toSQL() {
    const query = this.dialect.buildRelationalQuery({
      fullSchema: this.fullSchema,
      schema: this.schema,
      tableNamesMap: this.tableNamesMap,
      table: this.table,
      tableConfig: this.tableConfig,
      queryConfig: this.config,
      tableAlias: this.tableConfig.tsName
    });
    const builtQuery = this.dialect.sqlToQuery(query.sql);
    return { query, builtQuery };
  }
  toSQL() {
    return this._toSQL().builtQuery;
  }
  executeRaw() {
    if (this.mode === "first") {
      return this._prepare(false).get();
    }
    return this._prepare(false).all();
  }
  async execute() {
    return this.executeRaw();
  }
};
var SQLiteSyncRelationalQuery = class extends SQLiteRelationalQuery {
  static {
    __name(this, "SQLiteSyncRelationalQuery");
  }
  static [entityKind] = "SQLiteSyncRelationalQuery";
  sync() {
    return this.executeRaw();
  }
};
var SQLiteRaw = class extends QueryPromise {
  static {
    __name(this, "SQLiteRaw");
  }
  constructor(execute, getSQL, action, dialect, mapBatchResult) {
    super();
    this.execute = execute;
    this.getSQL = getSQL;
    this.dialect = dialect;
    this.mapBatchResult = mapBatchResult;
    this.config = { action };
  }
  static [entityKind] = "SQLiteRaw";
  config;
  getQuery() {
    return { ...this.dialect.sqlToQuery(this.getSQL()), method: this.config.action };
  }
  mapResult(result, isFromBatch) {
    return isFromBatch ? this.mapBatchResult(result) : result;
  }
  _prepare() {
    return this;
  }
  isResponseInArrayMode() {
    return false;
  }
};
var BaseSQLiteDatabase = class {
  static {
    __name(this, "BaseSQLiteDatabase");
  }
  constructor(resultKind, dialect, session, schema) {
    this.resultKind = resultKind;
    this.dialect = dialect;
    this.session = session;
    this._ = schema ? {
      schema: schema.schema,
      fullSchema: schema.fullSchema,
      tableNamesMap: schema.tableNamesMap
    } : {
      schema: void 0,
      fullSchema: {},
      tableNamesMap: {}
    };
    this.query = {};
    const query = this.query;
    if (this._.schema) {
      for (const [tableName, columns] of Object.entries(this._.schema)) {
        query[tableName] = new RelationalQueryBuilder(resultKind, schema.fullSchema, this._.schema, this._.tableNamesMap, schema.fullSchema[tableName], columns, dialect, session);
      }
    }
    this.$cache = { invalidate: /* @__PURE__ */ __name(async (_params) => {
    }, "invalidate") };
  }
  static [entityKind] = "BaseSQLiteDatabase";
  query;
  $with = /* @__PURE__ */ __name((alias, selection) => {
    const self = this;
    const as = /* @__PURE__ */ __name((qb) => {
      if (typeof qb === "function") {
        qb = qb(new QueryBuilder(self.dialect));
      }
      return new Proxy(new WithSubquery(qb.getSQL(), selection ?? ("getSelectedFields" in qb ? qb.getSelectedFields() ?? {} : {}), alias, true), new SelectionProxyHandler({ alias, sqlAliasedBehavior: "alias", sqlBehavior: "error" }));
    }, "as");
    return { as };
  }, "$with");
  $count(source, filters) {
    return new SQLiteCountBuilder({ source, filters, session: this.session });
  }
  with(...queries) {
    const self = this;
    function select(fields) {
      return new SQLiteSelectBuilder({
        fields: fields ?? void 0,
        session: self.session,
        dialect: self.dialect,
        withList: queries
      });
    }
    __name(select, "select");
    function selectDistinct(fields) {
      return new SQLiteSelectBuilder({
        fields: fields ?? void 0,
        session: self.session,
        dialect: self.dialect,
        withList: queries,
        distinct: true
      });
    }
    __name(selectDistinct, "selectDistinct");
    function update(table3) {
      return new SQLiteUpdateBuilder(table3, self.session, self.dialect, queries);
    }
    __name(update, "update");
    function insert(into) {
      return new SQLiteInsertBuilder(into, self.session, self.dialect, queries);
    }
    __name(insert, "insert");
    function delete_(from) {
      return new SQLiteDeleteBase(from, self.session, self.dialect, queries);
    }
    __name(delete_, "delete_");
    return { select, selectDistinct, update, insert, delete: delete_ };
  }
  select(fields) {
    return new SQLiteSelectBuilder({ fields: fields ?? void 0, session: this.session, dialect: this.dialect });
  }
  selectDistinct(fields) {
    return new SQLiteSelectBuilder({
      fields: fields ?? void 0,
      session: this.session,
      dialect: this.dialect,
      distinct: true
    });
  }
  update(table3) {
    return new SQLiteUpdateBuilder(table3, this.session, this.dialect);
  }
  $cache;
  insert(into) {
    return new SQLiteInsertBuilder(into, this.session, this.dialect);
  }
  delete(from) {
    return new SQLiteDeleteBase(from, this.session, this.dialect);
  }
  run(query) {
    const sequel = typeof query === "string" ? sql.raw(query) : query.getSQL();
    if (this.resultKind === "async") {
      return new SQLiteRaw(async () => this.session.run(sequel), () => sequel, "run", this.dialect, this.session.extractRawRunValueFromBatchResult.bind(this.session));
    }
    return this.session.run(sequel);
  }
  all(query) {
    const sequel = typeof query === "string" ? sql.raw(query) : query.getSQL();
    if (this.resultKind === "async") {
      return new SQLiteRaw(async () => this.session.all(sequel), () => sequel, "all", this.dialect, this.session.extractRawAllValueFromBatchResult.bind(this.session));
    }
    return this.session.all(sequel);
  }
  get(query) {
    const sequel = typeof query === "string" ? sql.raw(query) : query.getSQL();
    if (this.resultKind === "async") {
      return new SQLiteRaw(async () => this.session.get(sequel), () => sequel, "get", this.dialect, this.session.extractRawGetValueFromBatchResult.bind(this.session));
    }
    return this.session.get(sequel);
  }
  values(query) {
    const sequel = typeof query === "string" ? sql.raw(query) : query.getSQL();
    if (this.resultKind === "async") {
      return new SQLiteRaw(async () => this.session.values(sequel), () => sequel, "values", this.dialect, this.session.extractRawValuesValueFromBatchResult.bind(this.session));
    }
    return this.session.values(sequel);
  }
  transaction(transaction, config2) {
    return this.session.transaction(transaction, config2);
  }
};
var Cache = class {
  static {
    __name(this, "Cache");
  }
  static [entityKind] = "Cache";
};
var NoopCache = class extends Cache {
  static {
    __name(this, "NoopCache");
  }
  strategy() {
    return "all";
  }
  static [entityKind] = "NoopCache";
  async get(_key) {
    return;
  }
  async put(_hashedQuery, _response, _tables, _config) {
  }
  async onMutate(_params) {
  }
};
async function hashQuery(sql22, params) {
  const dataToHash = `${sql22}-${JSON.stringify(params)}`;
  const encoder2 = new TextEncoder();
  const data = encoder2.encode(dataToHash);
  const hashBuffer = await crypto.subtle.digest("SHA-256", data);
  const hashArray = [...new Uint8Array(hashBuffer)];
  const hashHex = hashArray.map((b) => b.toString(16).padStart(2, "0")).join("");
  return hashHex;
}
__name(hashQuery, "hashQuery");
var ExecuteResultSync = class extends QueryPromise {
  static {
    __name(this, "ExecuteResultSync");
  }
  constructor(resultCb) {
    super();
    this.resultCb = resultCb;
  }
  static [entityKind] = "ExecuteResultSync";
  async execute() {
    return this.resultCb();
  }
  sync() {
    return this.resultCb();
  }
};
var SQLitePreparedQuery = class {
  static {
    __name(this, "SQLitePreparedQuery");
  }
  constructor(mode, executeMethod, query, cache2, queryMetadata, cacheConfig) {
    this.mode = mode;
    this.executeMethod = executeMethod;
    this.query = query;
    this.cache = cache2;
    this.queryMetadata = queryMetadata;
    this.cacheConfig = cacheConfig;
    if (cache2 && cache2.strategy() === "all" && cacheConfig === void 0) {
      this.cacheConfig = { enable: true, autoInvalidate: true };
    }
    if (!this.cacheConfig?.enable) {
      this.cacheConfig = void 0;
    }
  }
  static [entityKind] = "PreparedQuery";
  joinsNotNullableMap;
  async queryWithCache(queryString, params, query) {
    if (this.cache === void 0 || is(this.cache, NoopCache) || this.queryMetadata === void 0) {
      try {
        return await query();
      } catch (e) {
        throw new DrizzleQueryError(queryString, params, e);
      }
    }
    if (this.cacheConfig && !this.cacheConfig.enable) {
      try {
        return await query();
      } catch (e) {
        throw new DrizzleQueryError(queryString, params, e);
      }
    }
    if ((this.queryMetadata.type === "insert" || this.queryMetadata.type === "update" || this.queryMetadata.type === "delete") && this.queryMetadata.tables.length > 0) {
      try {
        const [res] = await Promise.all([
          query(),
          this.cache.onMutate({ tables: this.queryMetadata.tables })
        ]);
        return res;
      } catch (e) {
        throw new DrizzleQueryError(queryString, params, e);
      }
    }
    if (!this.cacheConfig) {
      try {
        return await query();
      } catch (e) {
        throw new DrizzleQueryError(queryString, params, e);
      }
    }
    if (this.queryMetadata.type === "select") {
      const fromCache = await this.cache.get(this.cacheConfig.tag ?? await hashQuery(queryString, params), this.queryMetadata.tables, this.cacheConfig.tag !== void 0, this.cacheConfig.autoInvalidate);
      if (fromCache === void 0) {
        let result;
        try {
          result = await query();
        } catch (e) {
          throw new DrizzleQueryError(queryString, params, e);
        }
        await this.cache.put(this.cacheConfig.tag ?? await hashQuery(queryString, params), result, this.cacheConfig.autoInvalidate ? this.queryMetadata.tables : [], this.cacheConfig.tag !== void 0, this.cacheConfig.config);
        return result;
      }
      return fromCache;
    }
    try {
      return await query();
    } catch (e) {
      throw new DrizzleQueryError(queryString, params, e);
    }
  }
  getQuery() {
    return this.query;
  }
  mapRunResult(result, _isFromBatch) {
    return result;
  }
  mapAllResult(_result, _isFromBatch) {
    throw new Error("Not implemented");
  }
  mapGetResult(_result, _isFromBatch) {
    throw new Error("Not implemented");
  }
  execute(placeholderValues) {
    if (this.mode === "async") {
      return this[this.executeMethod](placeholderValues);
    }
    return new ExecuteResultSync(() => this[this.executeMethod](placeholderValues));
  }
  mapResult(response, isFromBatch) {
    switch (this.executeMethod) {
      case "run": {
        return this.mapRunResult(response, isFromBatch);
      }
      case "all": {
        return this.mapAllResult(response, isFromBatch);
      }
      case "get": {
        return this.mapGetResult(response, isFromBatch);
      }
    }
  }
};
var SQLiteSession = class {
  static {
    __name(this, "SQLiteSession");
  }
  constructor(dialect) {
    this.dialect = dialect;
  }
  static [entityKind] = "SQLiteSession";
  prepareOneTimeQuery(query, fields, executeMethod, isResponseInArrayMode, customResultMapper, queryMetadata, cacheConfig) {
    return this.prepareQuery(query, fields, executeMethod, isResponseInArrayMode, customResultMapper, queryMetadata, cacheConfig);
  }
  run(query) {
    const staticQuery = this.dialect.sqlToQuery(query);
    try {
      return this.prepareOneTimeQuery(staticQuery, void 0, "run", false).run();
    } catch (err) {
      throw new DrizzleError({ cause: err, message: `Failed to run the query '${staticQuery.sql}'` });
    }
  }
  extractRawRunValueFromBatchResult(result) {
    return result;
  }
  all(query) {
    return this.prepareOneTimeQuery(this.dialect.sqlToQuery(query), void 0, "run", false).all();
  }
  extractRawAllValueFromBatchResult(_result) {
    throw new Error("Not implemented");
  }
  get(query) {
    return this.prepareOneTimeQuery(this.dialect.sqlToQuery(query), void 0, "run", false).get();
  }
  extractRawGetValueFromBatchResult(_result) {
    throw new Error("Not implemented");
  }
  values(query) {
    return this.prepareOneTimeQuery(this.dialect.sqlToQuery(query), void 0, "run", false).values();
  }
  async count(sql22) {
    const result = await this.values(sql22);
    return result[0][0];
  }
  extractRawValuesValueFromBatchResult(_result) {
    throw new Error("Not implemented");
  }
};
var SQLiteTransaction = class extends BaseSQLiteDatabase {
  static {
    __name(this, "SQLiteTransaction");
  }
  constructor(resultType, dialect, session, schema, nestedIndex = 0) {
    super(resultType, dialect, session, schema);
    this.schema = schema;
    this.nestedIndex = nestedIndex;
  }
  static [entityKind] = "SQLiteTransaction";
  rollback() {
    throw new TransactionRollbackError();
  }
};
var SQLiteD1Session = class extends SQLiteSession {
  static {
    __name(this, "SQLiteD1Session");
  }
  constructor(client, dialect, schema, options = {}) {
    super(dialect);
    this.client = client;
    this.schema = schema;
    this.options = options;
    this.logger = options.logger ?? new NoopLogger();
    this.cache = options.cache ?? new NoopCache();
  }
  static [entityKind] = "SQLiteD1Session";
  logger;
  cache;
  prepareQuery(query, fields, executeMethod, isResponseInArrayMode, customResultMapper, queryMetadata, cacheConfig) {
    const stmt = this.client.prepare(query.sql);
    return new D1PreparedQuery(stmt, query, this.logger, this.cache, queryMetadata, cacheConfig, fields, executeMethod, isResponseInArrayMode, customResultMapper);
  }
  async batch(queries) {
    const preparedQueries = [];
    const builtQueries = [];
    for (const query of queries) {
      const preparedQuery = query._prepare();
      const builtQuery = preparedQuery.getQuery();
      preparedQueries.push(preparedQuery);
      if (builtQuery.params.length > 0) {
        builtQueries.push(preparedQuery.stmt.bind(...builtQuery.params));
      } else {
        const builtQuery2 = preparedQuery.getQuery();
        builtQueries.push(this.client.prepare(builtQuery2.sql).bind(...builtQuery2.params));
      }
    }
    const batchResults = await this.client.batch(builtQueries);
    return batchResults.map((result, i) => preparedQueries[i].mapResult(result, true));
  }
  extractRawAllValueFromBatchResult(result) {
    return result.results;
  }
  extractRawGetValueFromBatchResult(result) {
    return result.results[0];
  }
  extractRawValuesValueFromBatchResult(result) {
    return d1ToRawMapping(result.results);
  }
  async transaction(transaction, config2) {
    const tx = new D1Transaction("async", this.dialect, this, this.schema);
    await this.run(sql.raw(`begin${config2?.behavior ? " " + config2.behavior : ""}`));
    try {
      const result = await transaction(tx);
      await this.run(sql`commit`);
      return result;
    } catch (err) {
      await this.run(sql`rollback`);
      throw err;
    }
  }
};
var D1Transaction = class _D1Transaction extends SQLiteTransaction {
  static {
    __name(this, "D1Transaction");
  }
  static [entityKind] = "D1Transaction";
  async transaction(transaction) {
    const savepointName = `sp${this.nestedIndex}`;
    const tx = new _D1Transaction("async", this.dialect, this.session, this.schema, this.nestedIndex + 1);
    await this.session.run(sql.raw(`savepoint ${savepointName}`));
    try {
      const result = await transaction(tx);
      await this.session.run(sql.raw(`release savepoint ${savepointName}`));
      return result;
    } catch (err) {
      await this.session.run(sql.raw(`rollback to savepoint ${savepointName}`));
      throw err;
    }
  }
};
function d1ToRawMapping(results) {
  const rows = [];
  for (const row of results) {
    const entry = Object.keys(row).map((k) => row[k]);
    rows.push(entry);
  }
  return rows;
}
__name(d1ToRawMapping, "d1ToRawMapping");
var D1PreparedQuery = class extends SQLitePreparedQuery {
  static {
    __name(this, "D1PreparedQuery");
  }
  constructor(stmt, query, logger, cache2, queryMetadata, cacheConfig, fields, executeMethod, _isResponseInArrayMode, customResultMapper) {
    super("async", executeMethod, query, cache2, queryMetadata, cacheConfig);
    this.logger = logger;
    this._isResponseInArrayMode = _isResponseInArrayMode;
    this.customResultMapper = customResultMapper;
    this.fields = fields;
    this.stmt = stmt;
  }
  static [entityKind] = "D1PreparedQuery";
  customResultMapper;
  fields;
  stmt;
  async run(placeholderValues) {
    const params = fillPlaceholders(this.query.params, placeholderValues ?? {});
    this.logger.logQuery(this.query.sql, params);
    return await this.queryWithCache(this.query.sql, params, async () => {
      return this.stmt.bind(...params).run();
    });
  }
  async all(placeholderValues) {
    const { fields, query, logger, stmt, customResultMapper } = this;
    if (!fields && !customResultMapper) {
      const params = fillPlaceholders(query.params, placeholderValues ?? {});
      logger.logQuery(query.sql, params);
      return await this.queryWithCache(query.sql, params, async () => {
        return stmt.bind(...params).all().then(({ results }) => this.mapAllResult(results));
      });
    }
    const rows = await this.values(placeholderValues);
    return this.mapAllResult(rows);
  }
  mapAllResult(rows, isFromBatch) {
    if (isFromBatch) {
      rows = d1ToRawMapping(rows.results);
    }
    if (!this.fields && !this.customResultMapper) {
      return rows;
    }
    if (this.customResultMapper) {
      return this.customResultMapper(rows);
    }
    return rows.map((row) => mapResultRow(this.fields, row, this.joinsNotNullableMap));
  }
  async get(placeholderValues) {
    const { fields, joinsNotNullableMap, query, logger, stmt, customResultMapper } = this;
    if (!fields && !customResultMapper) {
      const params = fillPlaceholders(query.params, placeholderValues ?? {});
      logger.logQuery(query.sql, params);
      return await this.queryWithCache(query.sql, params, async () => {
        return stmt.bind(...params).all().then(({ results }) => results[0]);
      });
    }
    const rows = await this.values(placeholderValues);
    if (!rows[0]) {
      return;
    }
    if (customResultMapper) {
      return customResultMapper(rows);
    }
    return mapResultRow(fields, rows[0], joinsNotNullableMap);
  }
  mapGetResult(result, isFromBatch) {
    if (isFromBatch) {
      result = d1ToRawMapping(result.results)[0];
    }
    if (!this.fields && !this.customResultMapper) {
      return result;
    }
    if (this.customResultMapper) {
      return this.customResultMapper([result]);
    }
    return mapResultRow(this.fields, result, this.joinsNotNullableMap);
  }
  async values(placeholderValues) {
    const params = fillPlaceholders(this.query.params, placeholderValues ?? {});
    this.logger.logQuery(this.query.sql, params);
    return await this.queryWithCache(this.query.sql, params, async () => {
      return this.stmt.bind(...params).raw();
    });
  }
  isResponseInArrayMode() {
    return this._isResponseInArrayMode;
  }
};
var DrizzleD1Database = class extends BaseSQLiteDatabase {
  static {
    __name(this, "DrizzleD1Database");
  }
  static [entityKind] = "D1Database";
  async batch(batch) {
    return this.session.batch(batch);
  }
};
function drizzle(client, config2 = {}) {
  const dialect = new SQLiteAsyncDialect({ casing: config2.casing });
  let logger;
  if (config2.logger === true) {
    logger = new DefaultLogger();
  } else if (config2.logger !== false) {
    logger = config2.logger;
  }
  let schema;
  if (config2.schema) {
    const tablesConfig = extractTablesRelationalConfig(config2.schema, createTableRelationsHelpers);
    schema = {
      fullSchema: config2.schema,
      schema: tablesConfig.tables,
      tableNamesMap: tablesConfig.tableNamesMap
    };
  }
  const session = new SQLiteD1Session(client, dialect, schema, { logger, cache: config2.cache });
  const db = new DrizzleD1Database("async", dialect, session, schema);
  db.$client = client;
  db.$cache = config2.cache;
  if (db.$cache) {
    db.$cache["invalidate"] = config2.cache?.onMutate;
  }
  return db;
}
__name(drizzle, "drizzle");
var reservedTableNames = [
  "openauth_webui_projects",
  "openauth_webui_email_templates",
  "openauth_webui",
  "openauth_webui_ui_styles",
  "openauth_webui_copy_templates",
  "openauth_webui_logs"
];
var ensureTableisValid = /* @__PURE__ */ __name((name) => {
  if (reservedTableNames.includes(name))
    throw new Error("Table name is reserved");
  return name;
}, "ensureTableisValid");
function isClientIdValid(name) {
  const regex = /^[a-zA-Z_][a-zA-Z0-9_]{2,250}$/;
  return regex.test(name);
}
__name(isClientIdValid, "isClientIdValid");
async function createUserTable(clientID, database) {
  if (!isClientIdValid(clientID))
    throw new Error(`Invalid clientID: "${clientID}"`);
  const validName = ensureTableisValid(clientID);
  const createTableSQL = `
    CREATE TABLE IF NOT EXISTS ${validName}_users (
      id TEXT PRIMARY KEY,
      identifier TEXT UNIQUE NOT NULL,
      data TEXT NOT NULL,
      session_private TEXT,
      session_public TEXT,
	  role TEXT,
      created_at TEXT NOT NULL
    );
  `;
  await database.prepare(createTableSQL).run();
}
__name(createUserTable, "createUserTable");
async function DeleteOTFusersTable(clientID, database) {
  if (!isClientIdValid(clientID))
    throw new Error(`Invalid clientID: "${clientID}"`);
  const res = await database.prepare(`DROP TABLE IF EXISTS ${clientID}_users;`).run();
  if (!res.success) {
    throw new Error("Failed to delete user table", { cause: res.error });
  }
  const dropOpenAuthTable = await database.prepare(`DROP TABLE IF EXISTS ${clientID};`).run();
  if (!dropOpenAuthTable.success) {
    throw new Error("Failed to delete openAuth table", {
      cause: dropOpenAuthTable.error
    });
  }
}
__name(DeleteOTFusersTable, "DeleteOTFusersTable");
var OTFusersTable = /* @__PURE__ */ __name((clientID) => {
  const name = `${clientID}_users`;
  if (!isClientIdValid(name))
    throw new Error(`Invalid clientID: "${clientID}"`);
  return sqliteTable(name, {
    id: text().primaryKey(),
    identifier: text().unique().notNull(),
    data: text({
      mode: "json"
    }).notNull(),
    session_private: text(),
    session_public: text(),
    role: text(),
    created_at: text().notNull()
  });
}, "OTFusersTable");
function parseDBProject(data) {
  return {
    ...data,
    clientID: String(data.clientID),
    created_at: String(data.created_at),
    active: Boolean(data.active),
    providers_data: typeof data.providers_data === "string" ? JSON.parse(data.providers_data) : data.providers_data,
    theme_id: data.theme_id || null,
    projectData: typeof data.projectData === "string" ? JSON.parse(data.projectData) : data.projectData || {},
    originURL: data.originURL || null,
    authEndpointURL: String(data.authEndpointURL),
    cloudflareDomaineID: String(data.cloudflareDomaineID),
    registerOnInvite: Boolean(data.registerOnInvite),
    secret: String(data.secret)
  };
}
__name(parseDBProject, "parseDBProject");
var uiStyleTable = sqliteTable("openauth_webui_ui_styles", {
  id: integer().primaryKey({ autoIncrement: true }),
  name: text().notNull(),
  owner_id: text().notNull(),
  owner_group_id: text().notNull(),
  themeData: text({
    mode: "json"
  }).notNull()
});
var projectTable = sqliteTable("openauth_webui_projects", {
  clientID: text().primaryKey(),
  name: text().notNull().default("Project Name"),
  owner_id: text().notNull(),
  owner_group_id: text().notNull(),
  active: integer({
    mode: "boolean"
  }).default(true),
  providers_data: text({
    mode: "json"
  }).default("[]"),
  theme_id: integer().references(() => uiStyleTable.id),
  projectData: text({
    mode: "json"
  }).default("{}"),
  registerOnInvite: integer({
    mode: "boolean"
  }).default(false),
  originURL: text(),
  secret: text().notNull(),
  authEndpointURL: text().notNull(),
  cloudflareDomaineID: text().notNull(),
  created_at: text().notNull()
});
var WebHookTable = sqliteTable("openauth_webui_webhooks", {
  id: text().primaryKey(),
  clientID: text().notNull().references(() => projectTable.clientID),
  event: text().notNull(),
  url: text().notNull(),
  method: text().notNull(),
  headers: text({
    mode: "json"
  }),
  created_at: text().notNull()
});
var emailTemplatesTable = sqliteTable("openauth_webui_email_templates", {
  id: integer().primaryKey({ autoIncrement: true }),
  name: text().notNull(),
  body: text().notNull(),
  subject: text().notNull(),
  owner_id: text().notNull(),
  owner_group_id: text().notNull(),
  created_at: text().notNull(),
  updated_at: text().notNull()
});
var webuiProjectTable = sqliteTable("openauth_webui", {
  key: text().primaryKey(),
  value: text().notNull(),
  expiry: integer()
});
var totpTable = sqliteTable("openauth_totp", {
  user_id: text().primaryKey(),
  clientID: text().notNull().references(() => projectTable.clientID),
  secret: text().notNull(),
  is_verified: integer({
    mode: "boolean"
  }).default(false),
  backup_codes: text({
    mode: "json"
  }).notNull(),
  created_at: text().notNull()
});
var totpTokenTable = sqliteTable("openauth_totp_tokens", {
  token: text().primaryKey(),
  user_id: text().notNull(),
  clientID: text().notNull().references(() => projectTable.clientID),
  token_expires_at: text().notNull(),
  created_at: text().notNull()
});
var WebUiProjectUserTable = OTFusersTable("openauth_webui");
function parseDBCopyTemplate(data) {
  return {
    ...data,
    copyData: typeof data.copyData === "string" ? JSON.parse(data.copyData) : data.copyData
  };
}
__name(parseDBCopyTemplate, "parseDBCopyTemplate");
var WebUiCopyTemplateTable = sqliteTable("openauth_webui_copy_templates", {
  id: integer().primaryKey({ autoIncrement: true }),
  name: text().notNull(),
  copyData: text({
    mode: "json"
  }).notNull(),
  owner_id: text().notNull(),
  owner_group_id: text().notNull(),
  created_at: text().notNull(),
  updated_at: text().notNull()
});
var WebUiInviteLinkTable = sqliteTable("openauth_webui_invite_links", {
  id: text().primaryKey(),
  clientID: text().notNull().references(() => projectTable.clientID),
  link: text().notNull(),
  expiresAt: text().notNull(),
  created_at: text().notNull()
});
var LogsTable = sqliteTable("openauth_webui_logs", {
  id: text().primaryKey(),
  clientID: text().notNull().references(() => projectTable.clientID),
  type: text().notNull(),
  message: text().notNull(),
  context: text({
    mode: "json"
  }),
  timestamp: text().notNull()
});
function insertLog({
  type,
  clientID,
  endpoint,
  message: message2,
  database,
  context: context2
}) {
  const logEntry = {
    id: crypto.randomUUID(),
    clientID,
    type,
    message: endpoint ? `[${endpoint}] ${message2}` : message2,
    timestamp: (/* @__PURE__ */ new Date()).toISOString(),
    context: context2 ? JSON.stringify(context2) : void 0
  };
  return drizzle(database).insert(LogsTable).values(logEntry).run().then((res) => {
    if (!res.success) {
      console.error("Failed to insert log entry:", res.error);
    }
  }).catch((err) => {
    console.error("Error inserting log entry:", err);
    throw err;
  });
}
__name(insertLog, "insertLog");
var webauthnChallengesTable = sqliteTable("webauthn_challenges", {
  id: text().primaryKey(),
  clientID: text().notNull(),
  challenge: text().notNull(),
  expires_at: text().notNull(),
  created_at: text().notNull()
});
var webauthnCredentialsTable = sqliteTable("webauthn_credentials", {
  credential_id: text().primaryKey(),
  user_id: text().notNull(),
  clientID: text().notNull(),
  public_key: text().notNull(),
  counter: integer().notNull(),
  device_type: text(),
  backed_up: integer({
    mode: "boolean"
  }).default(false),
  transports: text({
    mode: "json"
  }),
  created_at: text().notNull()
});
var webAuthnTokenAccessTable = sqliteTable("webauthn_token_access", {
  token: text().primaryKey(),
  user_id: text().notNull(),
  clientID: text().notNull(),
  expires_at: text().notNull(),
  created_at: text().notNull()
});
var inviteTable = sqliteTable("openauth_webui_ui_invites", {
  id: integer().primaryKey({ autoIncrement: true }),
  label: text().notNull(),
  from_user_id: text().notNull(),
  from_user_name: text().notNull(),
  user_id: text().notNull(),
  code: text().notNull(),
  type: text().notNull(),
  owner_group_id: text().notNull(),
  expires_at: text().notNull(),
  created_at: text().notNull(),
  status: text().default("pending")
});

// chunk-dzc5tjq7.js
var invites = /* @__PURE__ */ __name((_db) => {
  const db = drizzle(_db);
  async function create({
    type,
    owner_group_id,
    label,
    from,
    invite_user_id
  }) {
    const id = crypto.randomUUID();
    const res = await db.insert(inviteTable).values({
      type,
      owner_group_id,
      label,
      from_user_name: from.name,
      from_user_id: from.id,
      user_id: invite_user_id,
      code: id,
      expires_at: new Date(Date.now() + 7 * 24 * 60 * 60 * 1e3).toISOString(),
      created_at: (/* @__PURE__ */ new Date()).toISOString()
    }).returning().get();
    if (!res) {
      return { success: false, error: "Failed to create invite" };
    } else {
      return { success: true, data: res };
    }
  }
  __name(create, "create");
  function deleteById(id) {
    return db.delete(inviteTable).where(eq(inviteTable.id, id)).run().then((res) => res.success);
  }
  __name(deleteById, "deleteById");
  function updateStatus(id, status) {
    return db.update(inviteTable).set({ status }).where(eq(inviteTable.id, id)).returning({ status: inviteTable.status }).get().then((res) => res.status === status);
  }
  __name(updateStatus, "updateStatus");
  return {
    create,
    deleteById,
    confirmInvite: /* @__PURE__ */ __name(async (code, user_id, type, authClient) => {
      const res = await db.select().from(inviteTable).where(and(eq(inviteTable.code, code), eq(inviteTable.user_id, user_id), eq(inviteTable.type, type))).get();
      if (!res)
        return { success: false, error: "Invite not found" };
      if (new Date(res.expires_at) < /* @__PURE__ */ new Date()) {
        await deleteById(res.id);
        return { success: false, error: "Invite has expired" };
      }
      const currentUserSession = await authClient.getUserSession("private");
      if (currentUserSession instanceof Error) {
        console.error("Failed to get user session:", currentUserSession);
        return { success: false, error: currentUserSession.message };
      }
      await authClient.updateUserSession("private", {
        group_ids: [
          ...currentUserSession.private?.group_ids || [],
          res.owner_group_id
        ]
      });
      return { success: await updateStatus(res.id, "accepted") };
    }, "confirmInvite"),
    declineInvite: /* @__PURE__ */ __name(async (code, user_id) => {
      const res = await db.select().from(inviteTable).where(and(eq(inviteTable.code, code), eq(inviteTable.user_id, user_id))).get();
      if (!res)
        return { success: false, error: "Invite not found" };
      return { success: await deleteById(res.id) };
    }, "declineInvite"),
    revoke: /* @__PURE__ */ __name(async (user_id, owner_group_id, client) => {
      const userInfo = await client.getUserById(user_id);
      if (userInfo instanceof Error) {
        console.error("Failed to get user:", userInfo);
        return { success: false, error: userInfo.message };
      }
      const user = userInfo.data.users.at(0);
      const currentUser = await client.getMetaData();
      if (!user) {
        return { success: false, error: "User not found" };
      } else if (!currentUser) {
        return { success: false, error: "Current user not found" };
      }
      const res = await db.select({ owner_id: inviteTable.owner_group_id, id: inviteTable.id }).from(inviteTable).where(and(eq(inviteTable.user_id, user_id), eq(inviteTable.owner_group_id, owner_group_id), eq(inviteTable.from_user_id, currentUser.id))).get();
      if (!res)
        return { success: false, error: "Invite not found" };
      if (!user?.session_private.group_ids?.includes(res.owner_id)) {
        return { success: false, error: "Unauthorized" };
      }
      await client.updateUserById(user_id, {
        session_private: {
          group_ids: user.session_private.group_ids.filter((id) => id !== res.owner_id)
        }
      });
      return { success: await deleteById(res.id) };
    }, "revoke"),
    exists: /* @__PURE__ */ __name(({
      type,
      user_id,
      from_id,
      owner_group_id
    }) => {
      return db.select().from(inviteTable).where(and(eq(inviteTable.owner_group_id, owner_group_id), eq(inviteTable.user_id, user_id), eq(inviteTable.type, type), eq(inviteTable.from_user_id, from_id))).get();
    }, "exists")
  };
}, "invites");

// chunk-hag46gmg.js
var PROVIDER_REGISTRY = [
  {
    type: "google",
    name: "Google",
    category: "social",
    icon: "\u{1F535}",
    description: "Sign in with Google OAuth2"
  },
  {
    type: "github",
    name: "GitHub",
    category: "social",
    icon: "\u26AB",
    description: "Sign in with GitHub OAuth2"
  },
  {
    type: "discord",
    name: "Discord",
    category: "social",
    icon: "\u{1F49C}",
    description: "Sign in with Discord OAuth2"
  },
  {
    type: "x",
    name: "X (Twitter)",
    category: "social",
    icon: "\u2716\uFE0F",
    description: "Sign in with X OAuth2"
  },
  {
    type: "facebook",
    name: "Facebook",
    category: "social",
    icon: "\u{1F4D8}",
    description: "Sign in with Facebook OAuth2"
  },
  {
    type: "appleoauth",
    name: "Apple OAuth2",
    category: "social",
    icon: "\u{1F34E}",
    description: "Sign in with Apple OAuth2"
  },
  {
    type: "appleoidc",
    name: "Apple OIDC",
    category: "social",
    icon: "\u{1F34F}",
    description: "Sign in with Apple OIDC"
  },
  {
    type: "slack",
    name: "Slack",
    category: "social",
    icon: "\u{1F4AC}",
    description: "Sign in with Slack OAuth2"
  },
  {
    type: "spotify",
    name: "Spotify",
    category: "social",
    icon: "\u{1F3B5}",
    description: "Sign in with Spotify OAuth2"
  },
  {
    type: "twitch",
    name: "Twitch",
    category: "social",
    icon: "\u{1F3AE}",
    description: "Sign in with Twitch OAuth2"
  },
  {
    type: "yahoo",
    name: "Yahoo",
    category: "social",
    icon: "\u{1F7E3}",
    description: "Sign in with Yahoo OAuth2"
  },
  {
    type: "microsoft",
    name: "Microsoft",
    category: "enterprise",
    icon: "\u{1FA9F}",
    description: "Sign in with Microsoft Azure AD"
  },
  {
    type: "cognito",
    name: "AWS Cognito",
    category: "enterprise",
    icon: "\u2601\uFE0F",
    description: "Sign in with AWS Cognito"
  },
  {
    type: "keycloak",
    name: "Keycloak",
    category: "enterprise",
    icon: "\u{1F510}",
    description: "Sign in with Keycloak"
  },
  {
    type: "jumpcloud",
    name: "JumpCloud",
    category: "enterprise",
    icon: "\u2601\uFE0F",
    description: "Sign in with JumpCloud"
  },
  {
    type: "oidc",
    name: "Custom OIDC",
    category: "custom",
    icon: "\u{1F517}",
    description: "Connect to any OIDC provider"
  },
  {
    type: "oauth",
    name: "Custom OAuth2",
    category: "custom",
    icon: "\u{1F511}",
    description: "Connect to any OAuth2 provider"
  },
  {
    type: "code",
    name: "Pin Code",
    category: "form",
    icon: "\u{1F4E7}",
    description: "Email or SMS verification code"
  },
  {
    type: "password",
    name: "Password",
    category: "form",
    icon: "\u{1F512}",
    description: "Traditional email and password"
  },
  {
    type: "qr",
    name: "QR Code",
    category: "custom",
    icon: "\u{1F4F1}",
    description: "Authenticate by scanning a QR code with your authenticated mobile device"
  },
  {
    type: "passkey",
    name: "WebAuthn Passkey",
    category: "custom",
    icon: "\u{1F511}",
    description: "Sign in with WebAuthn Passkey"
  }
];
var COOKIE_NAME = "oauth_client_id";
var PUBLIC_CLIENT_ID = "openauth_webui";

// chunk-9v01vpwy.js
var __defProp2 = Object.defineProperty;
var __returnValue = /* @__PURE__ */ __name((v) => v, "__returnValue");
function __exportSetter(name, newValue) {
  this[name] = __returnValue.bind(null, newValue);
}
__name(__exportSetter, "__exportSetter");
var __export = /* @__PURE__ */ __name((target, all) => {
  for (var name in all)
    __defProp2(target, name, {
      get: all[name],
      enumerable: true,
      configurable: true,
      set: __exportSetter.bind(all, name)
    });
}, "__export");

// api/invites/template/copy/accept.js
async function POST2({ code }) {
  const ctx = getContext(arguments);
  const userId = (await ctx.data.client.getMetaData())?.id;
  if (!userId) {
    return { success: false, error: "Unauthorized" };
  }
  const manager = invites(ctx.env.PROJECT_DB);
  await manager.confirmInvite(code, userId, "copy_template", ctx.data.client);
}
__name(POST2, "POST");
var DoubleIndexedKV = class {
  static {
    __name(this, "DoubleIndexedKV");
  }
  constructor() {
    this.keyToValue = /* @__PURE__ */ new Map();
    this.valueToKey = /* @__PURE__ */ new Map();
  }
  set(key, value) {
    this.keyToValue.set(key, value);
    this.valueToKey.set(value, key);
  }
  getByKey(key) {
    return this.keyToValue.get(key);
  }
  getByValue(value) {
    return this.valueToKey.get(value);
  }
  clear() {
    this.keyToValue.clear();
    this.valueToKey.clear();
  }
};
var Registry = class {
  static {
    __name(this, "Registry");
  }
  constructor(generateIdentifier) {
    this.generateIdentifier = generateIdentifier;
    this.kv = new DoubleIndexedKV();
  }
  register(value, identifier) {
    if (this.kv.getByValue(value)) {
      return;
    }
    if (!identifier) {
      identifier = this.generateIdentifier(value);
    }
    this.kv.set(identifier, value);
  }
  clear() {
    this.kv.clear();
  }
  getIdentifier(value) {
    return this.kv.getByValue(value);
  }
  getValue(identifier) {
    return this.kv.getByKey(identifier);
  }
};
var ClassRegistry = class extends Registry {
  static {
    __name(this, "ClassRegistry");
  }
  constructor() {
    super((c) => c.name);
    this.classToAllowedProps = /* @__PURE__ */ new Map();
  }
  register(value, options) {
    if (typeof options === "object") {
      if (options.allowProps) {
        this.classToAllowedProps.set(value, options.allowProps);
      }
      super.register(value, options.identifier);
    } else {
      super.register(value, options);
    }
  }
  getAllowedProps(value) {
    return this.classToAllowedProps.get(value);
  }
};
function valuesOfObj(record) {
  if ("values" in Object) {
    return Object.values(record);
  }
  const values = [];
  for (const key in record) {
    if (record.hasOwnProperty(key)) {
      values.push(record[key]);
    }
  }
  return values;
}
__name(valuesOfObj, "valuesOfObj");
function find(record, predicate) {
  const values = valuesOfObj(record);
  if ("find" in values) {
    return values.find(predicate);
  }
  const valuesNotNever = values;
  for (let i = 0; i < valuesNotNever.length; i++) {
    const value = valuesNotNever[i];
    if (predicate(value)) {
      return value;
    }
  }
  return;
}
__name(find, "find");
function forEach(record, run) {
  Object.entries(record).forEach(([key, value]) => run(value, key));
}
__name(forEach, "forEach");
function includes(arr, value) {
  return arr.indexOf(value) !== -1;
}
__name(includes, "includes");
function findArr(record, predicate) {
  for (let i = 0; i < record.length; i++) {
    const value = record[i];
    if (predicate(value)) {
      return value;
    }
  }
  return;
}
__name(findArr, "findArr");
var CustomTransformerRegistry = class {
  static {
    __name(this, "CustomTransformerRegistry");
  }
  constructor() {
    this.transfomers = {};
  }
  register(transformer) {
    this.transfomers[transformer.name] = transformer;
  }
  findApplicable(v) {
    return find(this.transfomers, (transformer) => transformer.isApplicable(v));
  }
  findByName(name) {
    return this.transfomers[name];
  }
};
var getType = /* @__PURE__ */ __name((payload) => Object.prototype.toString.call(payload).slice(8, -1), "getType");
var isUndefined = /* @__PURE__ */ __name((payload) => typeof payload === "undefined", "isUndefined");
var isNull2 = /* @__PURE__ */ __name((payload) => payload === null, "isNull");
var isPlainObject = /* @__PURE__ */ __name((payload) => {
  if (typeof payload !== "object" || payload === null)
    return false;
  if (payload === Object.prototype)
    return false;
  if (Object.getPrototypeOf(payload) === null)
    return true;
  return Object.getPrototypeOf(payload) === Object.prototype;
}, "isPlainObject");
var isEmptyObject = /* @__PURE__ */ __name((payload) => isPlainObject(payload) && Object.keys(payload).length === 0, "isEmptyObject");
var isArray = /* @__PURE__ */ __name((payload) => Array.isArray(payload), "isArray");
var isString = /* @__PURE__ */ __name((payload) => typeof payload === "string", "isString");
var isNumber = /* @__PURE__ */ __name((payload) => typeof payload === "number" && !isNaN(payload), "isNumber");
var isBoolean = /* @__PURE__ */ __name((payload) => typeof payload === "boolean", "isBoolean");
var isRegExp = /* @__PURE__ */ __name((payload) => payload instanceof RegExp, "isRegExp");
var isMap = /* @__PURE__ */ __name((payload) => payload instanceof Map, "isMap");
var isSet = /* @__PURE__ */ __name((payload) => payload instanceof Set, "isSet");
var isSymbol = /* @__PURE__ */ __name((payload) => getType(payload) === "Symbol", "isSymbol");
var isDate = /* @__PURE__ */ __name((payload) => payload instanceof Date && !isNaN(payload.valueOf()), "isDate");
var isError = /* @__PURE__ */ __name((payload) => payload instanceof Error, "isError");
var isNaNValue = /* @__PURE__ */ __name((payload) => typeof payload === "number" && isNaN(payload), "isNaNValue");
var isPrimitive = /* @__PURE__ */ __name((payload) => isBoolean(payload) || isNull2(payload) || isUndefined(payload) || isNumber(payload) || isString(payload) || isSymbol(payload), "isPrimitive");
var isBigint = /* @__PURE__ */ __name((payload) => typeof payload === "bigint", "isBigint");
var isInfinite = /* @__PURE__ */ __name((payload) => payload === Infinity || payload === -Infinity, "isInfinite");
var isTypedArray = /* @__PURE__ */ __name((payload) => ArrayBuffer.isView(payload) && !(payload instanceof DataView), "isTypedArray");
var isURL = /* @__PURE__ */ __name((payload) => payload instanceof URL, "isURL");
var escapeKey = /* @__PURE__ */ __name((key) => key.replace(/\\/g, "\\\\").replace(/\./g, "\\."), "escapeKey");
var stringifyPath = /* @__PURE__ */ __name((path) => path.map(String).map(escapeKey).join("."), "stringifyPath");
var parsePath = /* @__PURE__ */ __name((string2, legacyPaths) => {
  const result = [];
  let segment = "";
  for (let i = 0; i < string2.length; i++) {
    let char = string2.charAt(i);
    if (!legacyPaths && char === "\\") {
      const escaped = string2.charAt(i + 1);
      if (escaped === "\\") {
        segment += "\\";
        i++;
        continue;
      } else if (escaped !== ".") {
        throw Error("invalid path");
      }
    }
    const isEscapedDot = char === "\\" && string2.charAt(i + 1) === ".";
    if (isEscapedDot) {
      segment += ".";
      i++;
      continue;
    }
    const isEndOfSegment = char === ".";
    if (isEndOfSegment) {
      result.push(segment);
      segment = "";
      continue;
    }
    segment += char;
  }
  const lastSegment = segment;
  result.push(lastSegment);
  return result;
}, "parsePath");
function simpleTransformation(isApplicable, annotation, transform, untransform) {
  return {
    isApplicable,
    annotation,
    transform,
    untransform
  };
}
__name(simpleTransformation, "simpleTransformation");
var simpleRules = [
  simpleTransformation(isUndefined, "undefined", () => null, () => {
    return;
  }),
  simpleTransformation(isBigint, "bigint", (v) => v.toString(), (v) => {
    if (typeof BigInt !== "undefined") {
      return BigInt(v);
    }
    console.error("Please add a BigInt polyfill.");
    return v;
  }),
  simpleTransformation(isDate, "Date", (v) => v.toISOString(), (v) => new Date(v)),
  simpleTransformation(isError, "Error", (v, superJson) => {
    const baseError = {
      name: v.name,
      message: v.message
    };
    if ("cause" in v) {
      baseError.cause = v.cause;
    }
    superJson.allowedErrorProps.forEach((prop) => {
      baseError[prop] = v[prop];
    });
    return baseError;
  }, (v, superJson) => {
    const e = new Error(v.message, { cause: v.cause });
    e.name = v.name;
    e.stack = v.stack;
    superJson.allowedErrorProps.forEach((prop) => {
      e[prop] = v[prop];
    });
    return e;
  }),
  simpleTransformation(isRegExp, "regexp", (v) => "" + v, (regex) => {
    const body = regex.slice(1, regex.lastIndexOf("/"));
    const flags2 = regex.slice(regex.lastIndexOf("/") + 1);
    return new RegExp(body, flags2);
  }),
  simpleTransformation(isSet, "set", (v) => [...v.values()], (v) => new Set(v)),
  simpleTransformation(isMap, "map", (v) => [...v.entries()], (v) => new Map(v)),
  simpleTransformation((v) => isNaNValue(v) || isInfinite(v), "number", (v) => {
    if (isNaNValue(v)) {
      return "NaN";
    }
    if (v > 0) {
      return "Infinity";
    } else {
      return "-Infinity";
    }
  }, Number),
  simpleTransformation((v) => v === 0 && 1 / v === -Infinity, "number", () => {
    return "-0";
  }, Number),
  simpleTransformation(isURL, "URL", (v) => v.toString(), (v) => new URL(v))
];
function compositeTransformation(isApplicable, annotation, transform, untransform) {
  return {
    isApplicable,
    annotation,
    transform,
    untransform
  };
}
__name(compositeTransformation, "compositeTransformation");
var symbolRule = compositeTransformation((s, superJson) => {
  if (isSymbol(s)) {
    const isRegistered = !!superJson.symbolRegistry.getIdentifier(s);
    return isRegistered;
  }
  return false;
}, (s, superJson) => {
  const identifier = superJson.symbolRegistry.getIdentifier(s);
  return ["symbol", identifier];
}, (v) => v.description, (_, a, superJson) => {
  const value = superJson.symbolRegistry.getValue(a[1]);
  if (!value) {
    throw new Error("Trying to deserialize unknown symbol");
  }
  return value;
});
var constructorToName = [
  Int8Array,
  Uint8Array,
  Int16Array,
  Uint16Array,
  Int32Array,
  Uint32Array,
  Float32Array,
  Float64Array,
  Uint8ClampedArray
].reduce((obj, ctor) => {
  obj[ctor.name] = ctor;
  return obj;
}, {});
var typedArrayRule = compositeTransformation(isTypedArray, (v) => ["typed-array", v.constructor.name], (v) => [...v], (v, a) => {
  const ctor = constructorToName[a[1]];
  if (!ctor) {
    throw new Error("Trying to deserialize unknown typed array");
  }
  return new ctor(v);
});
function isInstanceOfRegisteredClass(potentialClass, superJson) {
  if (potentialClass?.constructor) {
    const isRegistered = !!superJson.classRegistry.getIdentifier(potentialClass.constructor);
    return isRegistered;
  }
  return false;
}
__name(isInstanceOfRegisteredClass, "isInstanceOfRegisteredClass");
var classRule = compositeTransformation(isInstanceOfRegisteredClass, (clazz, superJson) => {
  const identifier = superJson.classRegistry.getIdentifier(clazz.constructor);
  return ["class", identifier];
}, (clazz, superJson) => {
  const allowedProps = superJson.classRegistry.getAllowedProps(clazz.constructor);
  if (!allowedProps) {
    return { ...clazz };
  }
  const result = {};
  allowedProps.forEach((prop) => {
    result[prop] = clazz[prop];
  });
  return result;
}, (v, a, superJson) => {
  const clazz = superJson.classRegistry.getValue(a[1]);
  if (!clazz) {
    throw new Error(`Trying to deserialize unknown class '${a[1]}' - check https://github.com/blitz-js/superjson/issues/116#issuecomment-773996564`);
  }
  return Object.assign(Object.create(clazz.prototype), v);
});
var customRule = compositeTransformation((value, superJson) => {
  return !!superJson.customTransformerRegistry.findApplicable(value);
}, (value, superJson) => {
  const transformer = superJson.customTransformerRegistry.findApplicable(value);
  return ["custom", transformer.name];
}, (value, superJson) => {
  const transformer = superJson.customTransformerRegistry.findApplicable(value);
  return transformer.serialize(value);
}, (v, a, superJson) => {
  const transformer = superJson.customTransformerRegistry.findByName(a[1]);
  if (!transformer) {
    throw new Error("Trying to deserialize unknown custom value");
  }
  return transformer.deserialize(v);
});
var compositeRules = [classRule, symbolRule, customRule, typedArrayRule];
var transformValue = /* @__PURE__ */ __name((value, superJson) => {
  const applicableCompositeRule = findArr(compositeRules, (rule) => rule.isApplicable(value, superJson));
  if (applicableCompositeRule) {
    return {
      value: applicableCompositeRule.transform(value, superJson),
      type: applicableCompositeRule.annotation(value, superJson)
    };
  }
  const applicableSimpleRule = findArr(simpleRules, (rule) => rule.isApplicable(value, superJson));
  if (applicableSimpleRule) {
    return {
      value: applicableSimpleRule.transform(value, superJson),
      type: applicableSimpleRule.annotation
    };
  }
  return;
}, "transformValue");
var simpleRulesByAnnotation = {};
simpleRules.forEach((rule) => {
  simpleRulesByAnnotation[rule.annotation] = rule;
});
var untransformValue = /* @__PURE__ */ __name((json, type, superJson) => {
  if (isArray(type)) {
    switch (type[0]) {
      case "symbol":
        return symbolRule.untransform(json, type, superJson);
      case "class":
        return classRule.untransform(json, type, superJson);
      case "custom":
        return customRule.untransform(json, type, superJson);
      case "typed-array":
        return typedArrayRule.untransform(json, type, superJson);
      default:
        throw new Error("Unknown transformation: " + type);
    }
  } else {
    const transformation = simpleRulesByAnnotation[type];
    if (!transformation) {
      throw new Error("Unknown transformation: " + type);
    }
    return transformation.untransform(json, superJson);
  }
}, "untransformValue");
var getNthKey = /* @__PURE__ */ __name((value, n) => {
  if (n > value.size)
    throw new Error("index out of bounds");
  const keys = value.keys();
  while (n > 0) {
    keys.next();
    n--;
  }
  return keys.next().value;
}, "getNthKey");
function validatePath(path) {
  if (includes(path, "__proto__")) {
    throw new Error("__proto__ is not allowed as a property");
  }
  if (includes(path, "prototype")) {
    throw new Error("prototype is not allowed as a property");
  }
  if (includes(path, "constructor")) {
    throw new Error("constructor is not allowed as a property");
  }
}
__name(validatePath, "validatePath");
var getDeep = /* @__PURE__ */ __name((object2, path) => {
  validatePath(path);
  for (let i = 0; i < path.length; i++) {
    const key = path[i];
    if (isSet(object2)) {
      object2 = getNthKey(object2, +key);
    } else if (isMap(object2)) {
      const row = +key;
      const type = +path[++i] === 0 ? "key" : "value";
      const keyOfRow = getNthKey(object2, row);
      switch (type) {
        case "key":
          object2 = keyOfRow;
          break;
        case "value":
          object2 = object2.get(keyOfRow);
          break;
      }
    } else {
      object2 = object2[key];
    }
  }
  return object2;
}, "getDeep");
var setDeep = /* @__PURE__ */ __name((object2, path, mapper) => {
  validatePath(path);
  if (path.length === 0) {
    return mapper(object2);
  }
  let parent = object2;
  for (let i = 0; i < path.length - 1; i++) {
    const key = path[i];
    if (isArray(parent)) {
      const index = +key;
      parent = parent[index];
    } else if (isPlainObject(parent)) {
      parent = parent[key];
    } else if (isSet(parent)) {
      const row = +key;
      parent = getNthKey(parent, row);
    } else if (isMap(parent)) {
      const isEnd = i === path.length - 2;
      if (isEnd) {
        break;
      }
      const row = +key;
      const type = +path[++i] === 0 ? "key" : "value";
      const keyOfRow = getNthKey(parent, row);
      switch (type) {
        case "key":
          parent = keyOfRow;
          break;
        case "value":
          parent = parent.get(keyOfRow);
          break;
      }
    }
  }
  const lastKey = path[path.length - 1];
  if (isArray(parent)) {
    parent[+lastKey] = mapper(parent[+lastKey]);
  } else if (isPlainObject(parent)) {
    parent[lastKey] = mapper(parent[lastKey]);
  }
  if (isSet(parent)) {
    const oldValue = getNthKey(parent, +lastKey);
    const newValue = mapper(oldValue);
    if (oldValue !== newValue) {
      parent.delete(oldValue);
      parent.add(newValue);
    }
  }
  if (isMap(parent)) {
    const row = +path[path.length - 2];
    const keyToRow = getNthKey(parent, row);
    const type = +lastKey === 0 ? "key" : "value";
    switch (type) {
      case "key": {
        const newKey = mapper(keyToRow);
        parent.set(newKey, parent.get(keyToRow));
        if (newKey !== keyToRow) {
          parent.delete(keyToRow);
        }
        break;
      }
      case "value": {
        parent.set(keyToRow, mapper(parent.get(keyToRow)));
        break;
      }
    }
  }
  return object2;
}, "setDeep");
var enableLegacyPaths = /* @__PURE__ */ __name((version4) => version4 < 1, "enableLegacyPaths");
function traverse(tree, walker26, version4, origin = []) {
  if (!tree) {
    return;
  }
  const legacyPaths = enableLegacyPaths(version4);
  if (!isArray(tree)) {
    forEach(tree, (subtree, key) => traverse(subtree, walker26, version4, [
      ...origin,
      ...parsePath(key, legacyPaths)
    ]));
    return;
  }
  const [nodeValue, children] = tree;
  if (children) {
    forEach(children, (child, key) => {
      traverse(child, walker26, version4, [
        ...origin,
        ...parsePath(key, legacyPaths)
      ]);
    });
  }
  walker26(nodeValue, origin);
}
__name(traverse, "traverse");
function applyValueAnnotations(plain, annotations, version4, superJson) {
  traverse(annotations, (type, path) => {
    plain = setDeep(plain, path, (v) => untransformValue(v, type, superJson));
  }, version4);
  return plain;
}
__name(applyValueAnnotations, "applyValueAnnotations");
function applyReferentialEqualityAnnotations(plain, annotations, version4) {
  const legacyPaths = enableLegacyPaths(version4);
  function apply(identicalPaths, path) {
    const object2 = getDeep(plain, parsePath(path, legacyPaths));
    identicalPaths.map((path2) => parsePath(path2, legacyPaths)).forEach((identicalObjectPath) => {
      plain = setDeep(plain, identicalObjectPath, () => object2);
    });
  }
  __name(apply, "apply");
  if (isArray(annotations)) {
    const [root, other] = annotations;
    root.forEach((identicalPath) => {
      plain = setDeep(plain, parsePath(identicalPath, legacyPaths), () => plain);
    });
    if (other) {
      forEach(other, apply);
    }
  } else {
    forEach(annotations, apply);
  }
  return plain;
}
__name(applyReferentialEqualityAnnotations, "applyReferentialEqualityAnnotations");
var isDeep = /* @__PURE__ */ __name((object2, superJson) => isPlainObject(object2) || isArray(object2) || isMap(object2) || isSet(object2) || isError(object2) || isInstanceOfRegisteredClass(object2, superJson), "isDeep");
function addIdentity(object2, path, identities) {
  const existingSet = identities.get(object2);
  if (existingSet) {
    existingSet.push(path);
  } else {
    identities.set(object2, [path]);
  }
}
__name(addIdentity, "addIdentity");
function generateReferentialEqualityAnnotations(identitites, dedupe) {
  const result = {};
  let rootEqualityPaths = void 0;
  identitites.forEach((paths) => {
    if (paths.length <= 1) {
      return;
    }
    if (!dedupe) {
      paths = paths.map((path) => path.map(String)).sort((a, b) => a.length - b.length);
    }
    const [representativePath, ...identicalPaths] = paths;
    if (representativePath.length === 0) {
      rootEqualityPaths = identicalPaths.map(stringifyPath);
    } else {
      result[stringifyPath(representativePath)] = identicalPaths.map(stringifyPath);
    }
  });
  if (rootEqualityPaths) {
    if (isEmptyObject(result)) {
      return [rootEqualityPaths];
    } else {
      return [rootEqualityPaths, result];
    }
  } else {
    return isEmptyObject(result) ? void 0 : result;
  }
}
__name(generateReferentialEqualityAnnotations, "generateReferentialEqualityAnnotations");
var walker = /* @__PURE__ */ __name((object2, identities, superJson, dedupe, path = [], objectsInThisPath = [], seenObjects = /* @__PURE__ */ new Map()) => {
  const primitive = isPrimitive(object2);
  if (!primitive) {
    addIdentity(object2, path, identities);
    const seen = seenObjects.get(object2);
    if (seen) {
      return dedupe ? {
        transformedValue: null
      } : seen;
    }
  }
  if (!isDeep(object2, superJson)) {
    const transformed2 = transformValue(object2, superJson);
    const result2 = transformed2 ? {
      transformedValue: transformed2.value,
      annotations: [transformed2.type]
    } : {
      transformedValue: object2
    };
    if (!primitive) {
      seenObjects.set(object2, result2);
    }
    return result2;
  }
  if (includes(objectsInThisPath, object2)) {
    return {
      transformedValue: null
    };
  }
  const transformationResult = transformValue(object2, superJson);
  const transformed = transformationResult?.value ?? object2;
  const transformedValue = isArray(transformed) ? [] : {};
  const innerAnnotations = {};
  forEach(transformed, (value, index) => {
    if (index === "__proto__" || index === "constructor" || index === "prototype") {
      throw new Error(`Detected property ${index}. This is a prototype pollution risk, please remove it from your object.`);
    }
    const recursiveResult = walker(value, identities, superJson, dedupe, [...path, index], [...objectsInThisPath, object2], seenObjects);
    transformedValue[index] = recursiveResult.transformedValue;
    if (isArray(recursiveResult.annotations)) {
      innerAnnotations[escapeKey(index)] = recursiveResult.annotations;
    } else if (isPlainObject(recursiveResult.annotations)) {
      forEach(recursiveResult.annotations, (tree, key) => {
        innerAnnotations[escapeKey(index) + "." + key] = tree;
      });
    }
  });
  const result = isEmptyObject(innerAnnotations) ? {
    transformedValue,
    annotations: transformationResult ? [transformationResult.type] : void 0
  } : {
    transformedValue,
    annotations: transformationResult ? [transformationResult.type, innerAnnotations] : innerAnnotations
  };
  if (!primitive) {
    seenObjects.set(object2, result);
  }
  return result;
}, "walker");
function getType2(payload) {
  return Object.prototype.toString.call(payload).slice(8, -1);
}
__name(getType2, "getType2");
function isArray2(payload) {
  return getType2(payload) === "Array";
}
__name(isArray2, "isArray2");
function isPlainObject2(payload) {
  if (getType2(payload) !== "Object")
    return false;
  const prototype = Object.getPrototypeOf(payload);
  return !!prototype && prototype.constructor === Object && prototype === Object.prototype;
}
__name(isPlainObject2, "isPlainObject2");
function assignProp(carry, key, newVal, originalObject, includeNonenumerable) {
  const propType = {}.propertyIsEnumerable.call(originalObject, key) ? "enumerable" : "nonenumerable";
  if (propType === "enumerable")
    carry[key] = newVal;
  if (includeNonenumerable && propType === "nonenumerable") {
    Object.defineProperty(carry, key, {
      value: newVal,
      enumerable: false,
      writable: true,
      configurable: true
    });
  }
}
__name(assignProp, "assignProp");
function copy(target, options = {}) {
  if (isArray2(target)) {
    return target.map((item) => copy(item, options));
  }
  if (!isPlainObject2(target)) {
    return target;
  }
  const props = Object.getOwnPropertyNames(target);
  const symbols = Object.getOwnPropertySymbols(target);
  return [...props, ...symbols].reduce((carry, key) => {
    if (key === "__proto__")
      return carry;
    if (isArray2(options.props) && !options.props.includes(key)) {
      return carry;
    }
    const val = target[key];
    const newVal = copy(val, options);
    assignProp(carry, key, newVal, target, options.nonenumerable);
    return carry;
  }, {});
}
__name(copy, "copy");
var SuperJSON = class {
  static {
    __name(this, "SuperJSON");
  }
  constructor({ dedupe = false } = {}) {
    this.classRegistry = new ClassRegistry();
    this.symbolRegistry = new Registry((s) => s.description ?? "");
    this.customTransformerRegistry = new CustomTransformerRegistry();
    this.allowedErrorProps = [];
    this.dedupe = dedupe;
  }
  serialize(object2) {
    const identities = /* @__PURE__ */ new Map();
    const output = walker(object2, identities, this, this.dedupe);
    const res = {
      json: output.transformedValue
    };
    if (output.annotations) {
      res.meta = {
        ...res.meta,
        values: output.annotations
      };
    }
    const equalityAnnotations = generateReferentialEqualityAnnotations(identities, this.dedupe);
    if (equalityAnnotations) {
      res.meta = {
        ...res.meta,
        referentialEqualities: equalityAnnotations
      };
    }
    if (res.meta)
      res.meta.v = 1;
    return res;
  }
  deserialize(payload, options) {
    const { json, meta } = payload;
    let result = options?.inPlace ? json : copy(json);
    if (meta?.values) {
      result = applyValueAnnotations(result, meta.values, meta.v ?? 0, this);
    }
    if (meta?.referentialEqualities) {
      result = applyReferentialEqualityAnnotations(result, meta.referentialEqualities, meta.v ?? 0);
    }
    return result;
  }
  stringify(object2) {
    return JSON.stringify(this.serialize(object2));
  }
  parse(string2) {
    return this.deserialize(JSON.parse(string2), { inPlace: true });
  }
  registerClass(v, options) {
    this.classRegistry.register(v, options);
  }
  registerSymbol(v, identifier) {
    this.symbolRegistry.register(v, identifier);
  }
  registerCustom(transformer, name) {
    this.customTransformerRegistry.register({
      name,
      ...transformer
    });
  }
  allowErrorProps(...props) {
    this.allowedErrorProps.push(...props);
  }
};
SuperJSON.defaultInstance = new SuperJSON();
SuperJSON.serialize = SuperJSON.defaultInstance.serialize.bind(SuperJSON.defaultInstance);
SuperJSON.deserialize = SuperJSON.defaultInstance.deserialize.bind(SuperJSON.defaultInstance);
SuperJSON.stringify = SuperJSON.defaultInstance.stringify.bind(SuperJSON.defaultInstance);
SuperJSON.parse = SuperJSON.defaultInstance.parse.bind(SuperJSON.defaultInstance);
SuperJSON.registerClass = SuperJSON.defaultInstance.registerClass.bind(SuperJSON.defaultInstance);
SuperJSON.registerSymbol = SuperJSON.defaultInstance.registerSymbol.bind(SuperJSON.defaultInstance);
SuperJSON.registerCustom = SuperJSON.defaultInstance.registerCustom.bind(SuperJSON.defaultInstance);
SuperJSON.allowErrorProps = SuperJSON.defaultInstance.allowErrorProps.bind(SuperJSON.defaultInstance);
var dist_default = SuperJSON;
var serialize = SuperJSON.serialize;
var deserialize = SuperJSON.deserialize;
var stringify = SuperJSON.stringify;
var parse = SuperJSON.parse;
var registerClass = SuperJSON.registerClass;
var registerCustom = SuperJSON.registerCustom;
var registerSymbol = SuperJSON.registerSymbol;
var allowErrorProps = SuperJSON.allowErrorProps;
function parseData(formData) {
  const propsArray = [];
  if (!formData)
    return propsArray;
  const batchsIDs = [];
  for (const [key, value] of Array.from(formData.entries())) {
    if (key.startsWith("FILE_"))
      propsArray.push(value);
    else if (key.startsWith("FILES_")) {
      if (batchsIDs.includes(key))
        continue;
      batchsIDs.push(key);
      propsArray.push(formData.getAll(key));
    } else {
      propsArray.push(JSON.parse(decodeURI(value)));
    }
  }
  return propsArray;
}
__name(parseData, "parseData");
function paramsFromURL(url) {
  const params = url.searchParams.entries().toArray().map(([_, v]) => v);
  return params.map((param) => JSON.parse(decodeURIComponent(param)));
}
__name(paramsFromURL, "paramsFromURL");
async function WrapRequestHandler(context2, endpoint) {
  const isServerAction = context2.request.headers.get("x-server-action") === "true";
  if (!isServerAction) {
    return new Response("Not Found", { status: 404 });
  }
  const parsedData = context2.request.method === "GET" || context2.request.method === "HEAD" ? paramsFromURL(new URL(context2.request.url)) : parseData(context2.request.headers.get("content-type") ? await context2.request.formData() : void 0);
  const missingProps = endpoint.length - parsedData.length;
  for (let i = 0; i < missingProps; i++) {
    parsedData.push(void 0);
  }
  parsedData.push(context2);
  const result = await endpoint(...parsedData);
  switch (typeof result) {
    case "string":
    case "number":
    case "boolean":
    case "bigint":
      return new Response(dist_default.stringify(result), {
        headers: {
          "Content-Type": "application/json",
          dataType: "json"
        }
      });
    case "undefined":
      return new Response(null, { status: 204 });
    case "object":
      if (result instanceof Response) {
        result.headers.set("dataType", "response");
        return result;
      } else if (result instanceof Blob) {
        const res = new Response(await result.arrayBuffer());
        res.headers.set("dataType", "blob");
        res.headers.set("Content-Type", result.type);
        return res;
      } else if (result instanceof File) {
        return new Response(await result.arrayBuffer(), {
          headers: {
            "Content-Type": result.type,
            dataType: "file",
            fileData: JSON.stringify({
              name: result.name,
              lastModified: result.lastModified
            })
          }
        });
      } else {
        return new Response(dist_default.stringify(result), {
          headers: { "Content-Type": "application/json", dataType: "json" }
        });
      }
    default:
      throw new Error(`Unsupported return type from action: ${typeof result}`);
  }
}
__name(WrapRequestHandler, "WrapRequestHandler");
var onRequest = /* @__PURE__ */ __name(async (context2) => {
  const method = context2.request.method;
  const options = {
    GET: typeof GET === "function" ? GET : void 0,
    POST: typeof POST2 === "function" ? POST2 : void 0,
    PUT: typeof PUT === "function" ? PUT : void 0,
    DELETE: typeof DELETE === "function" ? DELETE : void 0,
    PATCH: typeof PATCH === "function" ? PATCH : void 0,
    HEAD: typeof HEAD === "function" ? HEAD : void 0,
    OPTIONS: typeof OPTIONS === "function" ? OPTIONS : void 0
  };
  if (!options[method]) {
    return new Response(`Method "${method}" Not Allowed`, {
      status: 405
    });
  }
  return await WrapRequestHandler(context2, options[method]);
}, "onRequest");

// api/invites/template/copy/manage.js
async function POST3({
  user_id,
  template_id,
  from_name
}) {
  const ctx = getContext(arguments);
  const current_user_id = (await ctx.data.client.getMetaData())?.id;
  if (!current_user_id) {
    return { success: false, error: "Unauthorized" };
  } else if (current_user_id === user_id) {
    return { success: false, error: "You cannot invite yourself" };
  }
  const db = drizzle(ctx.env.PROJECT_DB);
  const template = await db.select().from(WebUiCopyTemplateTable).where(and(eq(WebUiCopyTemplateTable.id, template_id), eq(WebUiCopyTemplateTable.owner_id, current_user_id))).get();
  if (!template) {
    return {
      success: false,
      error: "Template not found or you don't have permission to invite users"
    };
  }
  const inviteManager = invites(ctx.env.PROJECT_DB);
  const existingInvite = await inviteManager.exists({
    type: "copy_template",
    user_id,
    owner_group_id: template.owner_group_id,
    from_id: current_user_id
  });
  const create = /* @__PURE__ */ __name(() => inviteManager.create({
    type: "copy_template",
    owner_group_id: template.owner_group_id,
    label: `Invite to Copy Template ${template.name}`,
    from: {
      id: current_user_id,
      name: from_name
    },
    invite_user_id: user_id
  }), "create");
  if (existingInvite) {
    await inviteManager.deleteById(existingInvite.id);
    const newInvite = await create();
    return { success: newInvite.success, error: newInvite.error };
  }
  const res = await create();
  return { success: res.success, error: res.error };
}
__name(POST3, "POST");
async function DELETE2({
  user_id,
  owner_group_id
}) {
  const ctx = getContext(arguments);
  const current_user_id = (await ctx.data.client.getMetaData())?.id;
  if (!current_user_id) {
    return { success: false, error: "Unauthorized" };
  } else if (current_user_id === user_id) {
    return { success: false, error: "You cannot revoke your own invite" };
  }
  const inviteManager = invites(ctx.env.PROJECT_DB);
  const existingInvite = await inviteManager.exists({
    type: "copy_template",
    user_id,
    owner_group_id,
    from_id: current_user_id
  });
  if (existingInvite) {
    await inviteManager.deleteById(existingInvite.id);
  } else {
    return { success: false, error: "Invite not found" };
  }
  return { success: true };
}
__name(DELETE2, "DELETE");
var DoubleIndexedKV2 = class {
  static {
    __name(this, "DoubleIndexedKV");
  }
  constructor() {
    this.keyToValue = /* @__PURE__ */ new Map();
    this.valueToKey = /* @__PURE__ */ new Map();
  }
  set(key, value) {
    this.keyToValue.set(key, value);
    this.valueToKey.set(value, key);
  }
  getByKey(key) {
    return this.keyToValue.get(key);
  }
  getByValue(value) {
    return this.valueToKey.get(value);
  }
  clear() {
    this.keyToValue.clear();
    this.valueToKey.clear();
  }
};
var Registry2 = class {
  static {
    __name(this, "Registry");
  }
  constructor(generateIdentifier) {
    this.generateIdentifier = generateIdentifier;
    this.kv = new DoubleIndexedKV2();
  }
  register(value, identifier) {
    if (this.kv.getByValue(value)) {
      return;
    }
    if (!identifier) {
      identifier = this.generateIdentifier(value);
    }
    this.kv.set(identifier, value);
  }
  clear() {
    this.kv.clear();
  }
  getIdentifier(value) {
    return this.kv.getByValue(value);
  }
  getValue(identifier) {
    return this.kv.getByKey(identifier);
  }
};
var ClassRegistry2 = class extends Registry2 {
  static {
    __name(this, "ClassRegistry");
  }
  constructor() {
    super((c) => c.name);
    this.classToAllowedProps = /* @__PURE__ */ new Map();
  }
  register(value, options) {
    if (typeof options === "object") {
      if (options.allowProps) {
        this.classToAllowedProps.set(value, options.allowProps);
      }
      super.register(value, options.identifier);
    } else {
      super.register(value, options);
    }
  }
  getAllowedProps(value) {
    return this.classToAllowedProps.get(value);
  }
};
function valuesOfObj2(record) {
  if ("values" in Object) {
    return Object.values(record);
  }
  const values = [];
  for (const key in record) {
    if (record.hasOwnProperty(key)) {
      values.push(record[key]);
    }
  }
  return values;
}
__name(valuesOfObj2, "valuesOfObj");
function find2(record, predicate) {
  const values = valuesOfObj2(record);
  if ("find" in values) {
    return values.find(predicate);
  }
  const valuesNotNever = values;
  for (let i = 0; i < valuesNotNever.length; i++) {
    const value = valuesNotNever[i];
    if (predicate(value)) {
      return value;
    }
  }
  return;
}
__name(find2, "find");
function forEach2(record, run) {
  Object.entries(record).forEach(([key, value]) => run(value, key));
}
__name(forEach2, "forEach");
function includes2(arr, value) {
  return arr.indexOf(value) !== -1;
}
__name(includes2, "includes");
function findArr2(record, predicate) {
  for (let i = 0; i < record.length; i++) {
    const value = record[i];
    if (predicate(value)) {
      return value;
    }
  }
  return;
}
__name(findArr2, "findArr");
var CustomTransformerRegistry2 = class {
  static {
    __name(this, "CustomTransformerRegistry");
  }
  constructor() {
    this.transfomers = {};
  }
  register(transformer) {
    this.transfomers[transformer.name] = transformer;
  }
  findApplicable(v) {
    return find2(this.transfomers, (transformer) => transformer.isApplicable(v));
  }
  findByName(name) {
    return this.transfomers[name];
  }
};
var getType3 = /* @__PURE__ */ __name((payload) => Object.prototype.toString.call(payload).slice(8, -1), "getType");
var isUndefined2 = /* @__PURE__ */ __name((payload) => typeof payload === "undefined", "isUndefined");
var isNull3 = /* @__PURE__ */ __name((payload) => payload === null, "isNull");
var isPlainObject3 = /* @__PURE__ */ __name((payload) => {
  if (typeof payload !== "object" || payload === null)
    return false;
  if (payload === Object.prototype)
    return false;
  if (Object.getPrototypeOf(payload) === null)
    return true;
  return Object.getPrototypeOf(payload) === Object.prototype;
}, "isPlainObject");
var isEmptyObject2 = /* @__PURE__ */ __name((payload) => isPlainObject3(payload) && Object.keys(payload).length === 0, "isEmptyObject");
var isArray3 = /* @__PURE__ */ __name((payload) => Array.isArray(payload), "isArray");
var isString2 = /* @__PURE__ */ __name((payload) => typeof payload === "string", "isString");
var isNumber2 = /* @__PURE__ */ __name((payload) => typeof payload === "number" && !isNaN(payload), "isNumber");
var isBoolean2 = /* @__PURE__ */ __name((payload) => typeof payload === "boolean", "isBoolean");
var isRegExp2 = /* @__PURE__ */ __name((payload) => payload instanceof RegExp, "isRegExp");
var isMap2 = /* @__PURE__ */ __name((payload) => payload instanceof Map, "isMap");
var isSet2 = /* @__PURE__ */ __name((payload) => payload instanceof Set, "isSet");
var isSymbol2 = /* @__PURE__ */ __name((payload) => getType3(payload) === "Symbol", "isSymbol");
var isDate2 = /* @__PURE__ */ __name((payload) => payload instanceof Date && !isNaN(payload.valueOf()), "isDate");
var isError2 = /* @__PURE__ */ __name((payload) => payload instanceof Error, "isError");
var isNaNValue2 = /* @__PURE__ */ __name((payload) => typeof payload === "number" && isNaN(payload), "isNaNValue");
var isPrimitive2 = /* @__PURE__ */ __name((payload) => isBoolean2(payload) || isNull3(payload) || isUndefined2(payload) || isNumber2(payload) || isString2(payload) || isSymbol2(payload), "isPrimitive");
var isBigint2 = /* @__PURE__ */ __name((payload) => typeof payload === "bigint", "isBigint");
var isInfinite2 = /* @__PURE__ */ __name((payload) => payload === Infinity || payload === -Infinity, "isInfinite");
var isTypedArray2 = /* @__PURE__ */ __name((payload) => ArrayBuffer.isView(payload) && !(payload instanceof DataView), "isTypedArray");
var isURL2 = /* @__PURE__ */ __name((payload) => payload instanceof URL, "isURL");
var escapeKey2 = /* @__PURE__ */ __name((key) => key.replace(/\\/g, "\\\\").replace(/\./g, "\\."), "escapeKey");
var stringifyPath2 = /* @__PURE__ */ __name((path) => path.map(String).map(escapeKey2).join("."), "stringifyPath");
var parsePath2 = /* @__PURE__ */ __name((string2, legacyPaths) => {
  const result = [];
  let segment = "";
  for (let i = 0; i < string2.length; i++) {
    let char = string2.charAt(i);
    if (!legacyPaths && char === "\\") {
      const escaped = string2.charAt(i + 1);
      if (escaped === "\\") {
        segment += "\\";
        i++;
        continue;
      } else if (escaped !== ".") {
        throw Error("invalid path");
      }
    }
    const isEscapedDot = char === "\\" && string2.charAt(i + 1) === ".";
    if (isEscapedDot) {
      segment += ".";
      i++;
      continue;
    }
    const isEndOfSegment = char === ".";
    if (isEndOfSegment) {
      result.push(segment);
      segment = "";
      continue;
    }
    segment += char;
  }
  const lastSegment = segment;
  result.push(lastSegment);
  return result;
}, "parsePath");
function simpleTransformation2(isApplicable, annotation, transform, untransform) {
  return {
    isApplicable,
    annotation,
    transform,
    untransform
  };
}
__name(simpleTransformation2, "simpleTransformation");
var simpleRules2 = [
  simpleTransformation2(isUndefined2, "undefined", () => null, () => {
    return;
  }),
  simpleTransformation2(isBigint2, "bigint", (v) => v.toString(), (v) => {
    if (typeof BigInt !== "undefined") {
      return BigInt(v);
    }
    console.error("Please add a BigInt polyfill.");
    return v;
  }),
  simpleTransformation2(isDate2, "Date", (v) => v.toISOString(), (v) => new Date(v)),
  simpleTransformation2(isError2, "Error", (v, superJson) => {
    const baseError = {
      name: v.name,
      message: v.message
    };
    if ("cause" in v) {
      baseError.cause = v.cause;
    }
    superJson.allowedErrorProps.forEach((prop) => {
      baseError[prop] = v[prop];
    });
    return baseError;
  }, (v, superJson) => {
    const e = new Error(v.message, { cause: v.cause });
    e.name = v.name;
    e.stack = v.stack;
    superJson.allowedErrorProps.forEach((prop) => {
      e[prop] = v[prop];
    });
    return e;
  }),
  simpleTransformation2(isRegExp2, "regexp", (v) => "" + v, (regex) => {
    const body = regex.slice(1, regex.lastIndexOf("/"));
    const flags2 = regex.slice(regex.lastIndexOf("/") + 1);
    return new RegExp(body, flags2);
  }),
  simpleTransformation2(isSet2, "set", (v) => [...v.values()], (v) => new Set(v)),
  simpleTransformation2(isMap2, "map", (v) => [...v.entries()], (v) => new Map(v)),
  simpleTransformation2((v) => isNaNValue2(v) || isInfinite2(v), "number", (v) => {
    if (isNaNValue2(v)) {
      return "NaN";
    }
    if (v > 0) {
      return "Infinity";
    } else {
      return "-Infinity";
    }
  }, Number),
  simpleTransformation2((v) => v === 0 && 1 / v === -Infinity, "number", () => {
    return "-0";
  }, Number),
  simpleTransformation2(isURL2, "URL", (v) => v.toString(), (v) => new URL(v))
];
function compositeTransformation2(isApplicable, annotation, transform, untransform) {
  return {
    isApplicable,
    annotation,
    transform,
    untransform
  };
}
__name(compositeTransformation2, "compositeTransformation");
var symbolRule2 = compositeTransformation2((s, superJson) => {
  if (isSymbol2(s)) {
    const isRegistered = !!superJson.symbolRegistry.getIdentifier(s);
    return isRegistered;
  }
  return false;
}, (s, superJson) => {
  const identifier = superJson.symbolRegistry.getIdentifier(s);
  return ["symbol", identifier];
}, (v) => v.description, (_, a, superJson) => {
  const value = superJson.symbolRegistry.getValue(a[1]);
  if (!value) {
    throw new Error("Trying to deserialize unknown symbol");
  }
  return value;
});
var constructorToName2 = [
  Int8Array,
  Uint8Array,
  Int16Array,
  Uint16Array,
  Int32Array,
  Uint32Array,
  Float32Array,
  Float64Array,
  Uint8ClampedArray
].reduce((obj, ctor) => {
  obj[ctor.name] = ctor;
  return obj;
}, {});
var typedArrayRule2 = compositeTransformation2(isTypedArray2, (v) => ["typed-array", v.constructor.name], (v) => [...v], (v, a) => {
  const ctor = constructorToName2[a[1]];
  if (!ctor) {
    throw new Error("Trying to deserialize unknown typed array");
  }
  return new ctor(v);
});
function isInstanceOfRegisteredClass2(potentialClass, superJson) {
  if (potentialClass?.constructor) {
    const isRegistered = !!superJson.classRegistry.getIdentifier(potentialClass.constructor);
    return isRegistered;
  }
  return false;
}
__name(isInstanceOfRegisteredClass2, "isInstanceOfRegisteredClass");
var classRule2 = compositeTransformation2(isInstanceOfRegisteredClass2, (clazz, superJson) => {
  const identifier = superJson.classRegistry.getIdentifier(clazz.constructor);
  return ["class", identifier];
}, (clazz, superJson) => {
  const allowedProps = superJson.classRegistry.getAllowedProps(clazz.constructor);
  if (!allowedProps) {
    return { ...clazz };
  }
  const result = {};
  allowedProps.forEach((prop) => {
    result[prop] = clazz[prop];
  });
  return result;
}, (v, a, superJson) => {
  const clazz = superJson.classRegistry.getValue(a[1]);
  if (!clazz) {
    throw new Error(`Trying to deserialize unknown class '${a[1]}' - check https://github.com/blitz-js/superjson/issues/116#issuecomment-773996564`);
  }
  return Object.assign(Object.create(clazz.prototype), v);
});
var customRule2 = compositeTransformation2((value, superJson) => {
  return !!superJson.customTransformerRegistry.findApplicable(value);
}, (value, superJson) => {
  const transformer = superJson.customTransformerRegistry.findApplicable(value);
  return ["custom", transformer.name];
}, (value, superJson) => {
  const transformer = superJson.customTransformerRegistry.findApplicable(value);
  return transformer.serialize(value);
}, (v, a, superJson) => {
  const transformer = superJson.customTransformerRegistry.findByName(a[1]);
  if (!transformer) {
    throw new Error("Trying to deserialize unknown custom value");
  }
  return transformer.deserialize(v);
});
var compositeRules2 = [classRule2, symbolRule2, customRule2, typedArrayRule2];
var transformValue2 = /* @__PURE__ */ __name((value, superJson) => {
  const applicableCompositeRule = findArr2(compositeRules2, (rule) => rule.isApplicable(value, superJson));
  if (applicableCompositeRule) {
    return {
      value: applicableCompositeRule.transform(value, superJson),
      type: applicableCompositeRule.annotation(value, superJson)
    };
  }
  const applicableSimpleRule = findArr2(simpleRules2, (rule) => rule.isApplicable(value, superJson));
  if (applicableSimpleRule) {
    return {
      value: applicableSimpleRule.transform(value, superJson),
      type: applicableSimpleRule.annotation
    };
  }
  return;
}, "transformValue");
var simpleRulesByAnnotation2 = {};
simpleRules2.forEach((rule) => {
  simpleRulesByAnnotation2[rule.annotation] = rule;
});
var untransformValue2 = /* @__PURE__ */ __name((json, type, superJson) => {
  if (isArray3(type)) {
    switch (type[0]) {
      case "symbol":
        return symbolRule2.untransform(json, type, superJson);
      case "class":
        return classRule2.untransform(json, type, superJson);
      case "custom":
        return customRule2.untransform(json, type, superJson);
      case "typed-array":
        return typedArrayRule2.untransform(json, type, superJson);
      default:
        throw new Error("Unknown transformation: " + type);
    }
  } else {
    const transformation = simpleRulesByAnnotation2[type];
    if (!transformation) {
      throw new Error("Unknown transformation: " + type);
    }
    return transformation.untransform(json, superJson);
  }
}, "untransformValue");
var getNthKey2 = /* @__PURE__ */ __name((value, n) => {
  if (n > value.size)
    throw new Error("index out of bounds");
  const keys = value.keys();
  while (n > 0) {
    keys.next();
    n--;
  }
  return keys.next().value;
}, "getNthKey");
function validatePath2(path) {
  if (includes2(path, "__proto__")) {
    throw new Error("__proto__ is not allowed as a property");
  }
  if (includes2(path, "prototype")) {
    throw new Error("prototype is not allowed as a property");
  }
  if (includes2(path, "constructor")) {
    throw new Error("constructor is not allowed as a property");
  }
}
__name(validatePath2, "validatePath");
var getDeep2 = /* @__PURE__ */ __name((object2, path) => {
  validatePath2(path);
  for (let i = 0; i < path.length; i++) {
    const key = path[i];
    if (isSet2(object2)) {
      object2 = getNthKey2(object2, +key);
    } else if (isMap2(object2)) {
      const row = +key;
      const type = +path[++i] === 0 ? "key" : "value";
      const keyOfRow = getNthKey2(object2, row);
      switch (type) {
        case "key":
          object2 = keyOfRow;
          break;
        case "value":
          object2 = object2.get(keyOfRow);
          break;
      }
    } else {
      object2 = object2[key];
    }
  }
  return object2;
}, "getDeep");
var setDeep2 = /* @__PURE__ */ __name((object2, path, mapper) => {
  validatePath2(path);
  if (path.length === 0) {
    return mapper(object2);
  }
  let parent = object2;
  for (let i = 0; i < path.length - 1; i++) {
    const key = path[i];
    if (isArray3(parent)) {
      const index = +key;
      parent = parent[index];
    } else if (isPlainObject3(parent)) {
      parent = parent[key];
    } else if (isSet2(parent)) {
      const row = +key;
      parent = getNthKey2(parent, row);
    } else if (isMap2(parent)) {
      const isEnd = i === path.length - 2;
      if (isEnd) {
        break;
      }
      const row = +key;
      const type = +path[++i] === 0 ? "key" : "value";
      const keyOfRow = getNthKey2(parent, row);
      switch (type) {
        case "key":
          parent = keyOfRow;
          break;
        case "value":
          parent = parent.get(keyOfRow);
          break;
      }
    }
  }
  const lastKey = path[path.length - 1];
  if (isArray3(parent)) {
    parent[+lastKey] = mapper(parent[+lastKey]);
  } else if (isPlainObject3(parent)) {
    parent[lastKey] = mapper(parent[lastKey]);
  }
  if (isSet2(parent)) {
    const oldValue = getNthKey2(parent, +lastKey);
    const newValue = mapper(oldValue);
    if (oldValue !== newValue) {
      parent.delete(oldValue);
      parent.add(newValue);
    }
  }
  if (isMap2(parent)) {
    const row = +path[path.length - 2];
    const keyToRow = getNthKey2(parent, row);
    const type = +lastKey === 0 ? "key" : "value";
    switch (type) {
      case "key": {
        const newKey = mapper(keyToRow);
        parent.set(newKey, parent.get(keyToRow));
        if (newKey !== keyToRow) {
          parent.delete(keyToRow);
        }
        break;
      }
      case "value": {
        parent.set(keyToRow, mapper(parent.get(keyToRow)));
        break;
      }
    }
  }
  return object2;
}, "setDeep");
var enableLegacyPaths2 = /* @__PURE__ */ __name((version4) => version4 < 1, "enableLegacyPaths");
function traverse2(tree, walker26, version4, origin = []) {
  if (!tree) {
    return;
  }
  const legacyPaths = enableLegacyPaths2(version4);
  if (!isArray3(tree)) {
    forEach2(tree, (subtree, key) => traverse2(subtree, walker26, version4, [
      ...origin,
      ...parsePath2(key, legacyPaths)
    ]));
    return;
  }
  const [nodeValue, children] = tree;
  if (children) {
    forEach2(children, (child, key) => {
      traverse2(child, walker26, version4, [
        ...origin,
        ...parsePath2(key, legacyPaths)
      ]);
    });
  }
  walker26(nodeValue, origin);
}
__name(traverse2, "traverse");
function applyValueAnnotations2(plain, annotations, version4, superJson) {
  traverse2(annotations, (type, path) => {
    plain = setDeep2(plain, path, (v) => untransformValue2(v, type, superJson));
  }, version4);
  return plain;
}
__name(applyValueAnnotations2, "applyValueAnnotations");
function applyReferentialEqualityAnnotations2(plain, annotations, version4) {
  const legacyPaths = enableLegacyPaths2(version4);
  function apply(identicalPaths, path) {
    const object2 = getDeep2(plain, parsePath2(path, legacyPaths));
    identicalPaths.map((path2) => parsePath2(path2, legacyPaths)).forEach((identicalObjectPath) => {
      plain = setDeep2(plain, identicalObjectPath, () => object2);
    });
  }
  __name(apply, "apply");
  if (isArray3(annotations)) {
    const [root, other] = annotations;
    root.forEach((identicalPath) => {
      plain = setDeep2(plain, parsePath2(identicalPath, legacyPaths), () => plain);
    });
    if (other) {
      forEach2(other, apply);
    }
  } else {
    forEach2(annotations, apply);
  }
  return plain;
}
__name(applyReferentialEqualityAnnotations2, "applyReferentialEqualityAnnotations");
var isDeep2 = /* @__PURE__ */ __name((object2, superJson) => isPlainObject3(object2) || isArray3(object2) || isMap2(object2) || isSet2(object2) || isError2(object2) || isInstanceOfRegisteredClass2(object2, superJson), "isDeep");
function addIdentity2(object2, path, identities) {
  const existingSet = identities.get(object2);
  if (existingSet) {
    existingSet.push(path);
  } else {
    identities.set(object2, [path]);
  }
}
__name(addIdentity2, "addIdentity");
function generateReferentialEqualityAnnotations2(identitites, dedupe) {
  const result = {};
  let rootEqualityPaths = void 0;
  identitites.forEach((paths) => {
    if (paths.length <= 1) {
      return;
    }
    if (!dedupe) {
      paths = paths.map((path) => path.map(String)).sort((a, b) => a.length - b.length);
    }
    const [representativePath, ...identicalPaths] = paths;
    if (representativePath.length === 0) {
      rootEqualityPaths = identicalPaths.map(stringifyPath2);
    } else {
      result[stringifyPath2(representativePath)] = identicalPaths.map(stringifyPath2);
    }
  });
  if (rootEqualityPaths) {
    if (isEmptyObject2(result)) {
      return [rootEqualityPaths];
    } else {
      return [rootEqualityPaths, result];
    }
  } else {
    return isEmptyObject2(result) ? void 0 : result;
  }
}
__name(generateReferentialEqualityAnnotations2, "generateReferentialEqualityAnnotations");
var walker2 = /* @__PURE__ */ __name((object2, identities, superJson, dedupe, path = [], objectsInThisPath = [], seenObjects = /* @__PURE__ */ new Map()) => {
  const primitive = isPrimitive2(object2);
  if (!primitive) {
    addIdentity2(object2, path, identities);
    const seen = seenObjects.get(object2);
    if (seen) {
      return dedupe ? {
        transformedValue: null
      } : seen;
    }
  }
  if (!isDeep2(object2, superJson)) {
    const transformed2 = transformValue2(object2, superJson);
    const result2 = transformed2 ? {
      transformedValue: transformed2.value,
      annotations: [transformed2.type]
    } : {
      transformedValue: object2
    };
    if (!primitive) {
      seenObjects.set(object2, result2);
    }
    return result2;
  }
  if (includes2(objectsInThisPath, object2)) {
    return {
      transformedValue: null
    };
  }
  const transformationResult = transformValue2(object2, superJson);
  const transformed = transformationResult?.value ?? object2;
  const transformedValue = isArray3(transformed) ? [] : {};
  const innerAnnotations = {};
  forEach2(transformed, (value, index) => {
    if (index === "__proto__" || index === "constructor" || index === "prototype") {
      throw new Error(`Detected property ${index}. This is a prototype pollution risk, please remove it from your object.`);
    }
    const recursiveResult = walker2(value, identities, superJson, dedupe, [...path, index], [...objectsInThisPath, object2], seenObjects);
    transformedValue[index] = recursiveResult.transformedValue;
    if (isArray3(recursiveResult.annotations)) {
      innerAnnotations[escapeKey2(index)] = recursiveResult.annotations;
    } else if (isPlainObject3(recursiveResult.annotations)) {
      forEach2(recursiveResult.annotations, (tree, key) => {
        innerAnnotations[escapeKey2(index) + "." + key] = tree;
      });
    }
  });
  const result = isEmptyObject2(innerAnnotations) ? {
    transformedValue,
    annotations: transformationResult ? [transformationResult.type] : void 0
  } : {
    transformedValue,
    annotations: transformationResult ? [transformationResult.type, innerAnnotations] : innerAnnotations
  };
  if (!primitive) {
    seenObjects.set(object2, result);
  }
  return result;
}, "walker");
function getType22(payload) {
  return Object.prototype.toString.call(payload).slice(8, -1);
}
__name(getType22, "getType2");
function isArray22(payload) {
  return getType22(payload) === "Array";
}
__name(isArray22, "isArray2");
function isPlainObject22(payload) {
  if (getType22(payload) !== "Object")
    return false;
  const prototype = Object.getPrototypeOf(payload);
  return !!prototype && prototype.constructor === Object && prototype === Object.prototype;
}
__name(isPlainObject22, "isPlainObject2");
function assignProp2(carry, key, newVal, originalObject, includeNonenumerable) {
  const propType = {}.propertyIsEnumerable.call(originalObject, key) ? "enumerable" : "nonenumerable";
  if (propType === "enumerable")
    carry[key] = newVal;
  if (includeNonenumerable && propType === "nonenumerable") {
    Object.defineProperty(carry, key, {
      value: newVal,
      enumerable: false,
      writable: true,
      configurable: true
    });
  }
}
__name(assignProp2, "assignProp");
function copy2(target, options = {}) {
  if (isArray22(target)) {
    return target.map((item) => copy2(item, options));
  }
  if (!isPlainObject22(target)) {
    return target;
  }
  const props = Object.getOwnPropertyNames(target);
  const symbols = Object.getOwnPropertySymbols(target);
  return [...props, ...symbols].reduce((carry, key) => {
    if (key === "__proto__")
      return carry;
    if (isArray22(options.props) && !options.props.includes(key)) {
      return carry;
    }
    const val = target[key];
    const newVal = copy2(val, options);
    assignProp2(carry, key, newVal, target, options.nonenumerable);
    return carry;
  }, {});
}
__name(copy2, "copy");
var SuperJSON2 = class {
  static {
    __name(this, "SuperJSON");
  }
  constructor({ dedupe = false } = {}) {
    this.classRegistry = new ClassRegistry2();
    this.symbolRegistry = new Registry2((s) => s.description ?? "");
    this.customTransformerRegistry = new CustomTransformerRegistry2();
    this.allowedErrorProps = [];
    this.dedupe = dedupe;
  }
  serialize(object2) {
    const identities = /* @__PURE__ */ new Map();
    const output = walker2(object2, identities, this, this.dedupe);
    const res = {
      json: output.transformedValue
    };
    if (output.annotations) {
      res.meta = {
        ...res.meta,
        values: output.annotations
      };
    }
    const equalityAnnotations = generateReferentialEqualityAnnotations2(identities, this.dedupe);
    if (equalityAnnotations) {
      res.meta = {
        ...res.meta,
        referentialEqualities: equalityAnnotations
      };
    }
    if (res.meta)
      res.meta.v = 1;
    return res;
  }
  deserialize(payload, options) {
    const { json, meta } = payload;
    let result = options?.inPlace ? json : copy2(json);
    if (meta?.values) {
      result = applyValueAnnotations2(result, meta.values, meta.v ?? 0, this);
    }
    if (meta?.referentialEqualities) {
      result = applyReferentialEqualityAnnotations2(result, meta.referentialEqualities, meta.v ?? 0);
    }
    return result;
  }
  stringify(object2) {
    return JSON.stringify(this.serialize(object2));
  }
  parse(string2) {
    return this.deserialize(JSON.parse(string2), { inPlace: true });
  }
  registerClass(v, options) {
    this.classRegistry.register(v, options);
  }
  registerSymbol(v, identifier) {
    this.symbolRegistry.register(v, identifier);
  }
  registerCustom(transformer, name) {
    this.customTransformerRegistry.register({
      name,
      ...transformer
    });
  }
  allowErrorProps(...props) {
    this.allowedErrorProps.push(...props);
  }
};
SuperJSON2.defaultInstance = new SuperJSON2();
SuperJSON2.serialize = SuperJSON2.defaultInstance.serialize.bind(SuperJSON2.defaultInstance);
SuperJSON2.deserialize = SuperJSON2.defaultInstance.deserialize.bind(SuperJSON2.defaultInstance);
SuperJSON2.stringify = SuperJSON2.defaultInstance.stringify.bind(SuperJSON2.defaultInstance);
SuperJSON2.parse = SuperJSON2.defaultInstance.parse.bind(SuperJSON2.defaultInstance);
SuperJSON2.registerClass = SuperJSON2.defaultInstance.registerClass.bind(SuperJSON2.defaultInstance);
SuperJSON2.registerSymbol = SuperJSON2.defaultInstance.registerSymbol.bind(SuperJSON2.defaultInstance);
SuperJSON2.registerCustom = SuperJSON2.defaultInstance.registerCustom.bind(SuperJSON2.defaultInstance);
SuperJSON2.allowErrorProps = SuperJSON2.defaultInstance.allowErrorProps.bind(SuperJSON2.defaultInstance);
var dist_default2 = SuperJSON2;
var serialize2 = SuperJSON2.serialize;
var deserialize2 = SuperJSON2.deserialize;
var stringify2 = SuperJSON2.stringify;
var parse2 = SuperJSON2.parse;
var registerClass2 = SuperJSON2.registerClass;
var registerCustom2 = SuperJSON2.registerCustom;
var registerSymbol2 = SuperJSON2.registerSymbol;
var allowErrorProps2 = SuperJSON2.allowErrorProps;
function parseData2(formData) {
  const propsArray = [];
  if (!formData)
    return propsArray;
  const batchsIDs = [];
  for (const [key, value] of Array.from(formData.entries())) {
    if (key.startsWith("FILE_"))
      propsArray.push(value);
    else if (key.startsWith("FILES_")) {
      if (batchsIDs.includes(key))
        continue;
      batchsIDs.push(key);
      propsArray.push(formData.getAll(key));
    } else {
      propsArray.push(JSON.parse(decodeURI(value)));
    }
  }
  return propsArray;
}
__name(parseData2, "parseData");
function paramsFromURL2(url) {
  const params = url.searchParams.entries().toArray().map(([_, v]) => v);
  return params.map((param) => JSON.parse(decodeURIComponent(param)));
}
__name(paramsFromURL2, "paramsFromURL");
async function WrapRequestHandler2(context2, endpoint) {
  const isServerAction = context2.request.headers.get("x-server-action") === "true";
  if (!isServerAction) {
    return new Response("Not Found", { status: 404 });
  }
  const parsedData = context2.request.method === "GET" || context2.request.method === "HEAD" ? paramsFromURL2(new URL(context2.request.url)) : parseData2(context2.request.headers.get("content-type") ? await context2.request.formData() : void 0);
  const missingProps = endpoint.length - parsedData.length;
  for (let i = 0; i < missingProps; i++) {
    parsedData.push(void 0);
  }
  parsedData.push(context2);
  const result = await endpoint(...parsedData);
  switch (typeof result) {
    case "string":
    case "number":
    case "boolean":
    case "bigint":
      return new Response(dist_default2.stringify(result), {
        headers: {
          "Content-Type": "application/json",
          dataType: "json"
        }
      });
    case "undefined":
      return new Response(null, { status: 204 });
    case "object":
      if (result instanceof Response) {
        result.headers.set("dataType", "response");
        return result;
      } else if (result instanceof Blob) {
        const res = new Response(await result.arrayBuffer());
        res.headers.set("dataType", "blob");
        res.headers.set("Content-Type", result.type);
        return res;
      } else if (result instanceof File) {
        return new Response(await result.arrayBuffer(), {
          headers: {
            "Content-Type": result.type,
            dataType: "file",
            fileData: JSON.stringify({
              name: result.name,
              lastModified: result.lastModified
            })
          }
        });
      } else {
        return new Response(dist_default2.stringify(result), {
          headers: { "Content-Type": "application/json", dataType: "json" }
        });
      }
    default:
      throw new Error(`Unsupported return type from action: ${typeof result}`);
  }
}
__name(WrapRequestHandler2, "WrapRequestHandler");
var onRequest2 = /* @__PURE__ */ __name(async (context2) => {
  const method = context2.request.method;
  const options = {
    GET: typeof GET === "function" ? GET : void 0,
    POST: typeof POST3 === "function" ? POST3 : void 0,
    PUT: typeof PUT === "function" ? PUT : void 0,
    DELETE: typeof DELETE2 === "function" ? DELETE2 : void 0,
    PATCH: typeof PATCH === "function" ? PATCH : void 0,
    HEAD: typeof HEAD === "function" ? HEAD : void 0,
    OPTIONS: typeof OPTIONS === "function" ? OPTIONS : void 0
  };
  if (!options[method]) {
    return new Response(`Method "${method}" Not Allowed`, {
      status: 405
    });
  }
  return await WrapRequestHandler2(context2, options[method]);
}, "onRequest");

// api/invites/template/email/accept.js
async function POST4({ code }) {
  const ctx = getContext(arguments);
  const userId = (await ctx.data.client.getMetaData())?.id;
  if (!userId) {
    return { success: false, error: "Unauthorized" };
  }
  const manager = invites(ctx.env.PROJECT_DB);
  await manager.confirmInvite(code, userId, "email_template", ctx.data.client);
}
__name(POST4, "POST");
var DoubleIndexedKV3 = class {
  static {
    __name(this, "DoubleIndexedKV");
  }
  constructor() {
    this.keyToValue = /* @__PURE__ */ new Map();
    this.valueToKey = /* @__PURE__ */ new Map();
  }
  set(key, value) {
    this.keyToValue.set(key, value);
    this.valueToKey.set(value, key);
  }
  getByKey(key) {
    return this.keyToValue.get(key);
  }
  getByValue(value) {
    return this.valueToKey.get(value);
  }
  clear() {
    this.keyToValue.clear();
    this.valueToKey.clear();
  }
};
var Registry3 = class {
  static {
    __name(this, "Registry");
  }
  constructor(generateIdentifier) {
    this.generateIdentifier = generateIdentifier;
    this.kv = new DoubleIndexedKV3();
  }
  register(value, identifier) {
    if (this.kv.getByValue(value)) {
      return;
    }
    if (!identifier) {
      identifier = this.generateIdentifier(value);
    }
    this.kv.set(identifier, value);
  }
  clear() {
    this.kv.clear();
  }
  getIdentifier(value) {
    return this.kv.getByValue(value);
  }
  getValue(identifier) {
    return this.kv.getByKey(identifier);
  }
};
var ClassRegistry3 = class extends Registry3 {
  static {
    __name(this, "ClassRegistry");
  }
  constructor() {
    super((c) => c.name);
    this.classToAllowedProps = /* @__PURE__ */ new Map();
  }
  register(value, options) {
    if (typeof options === "object") {
      if (options.allowProps) {
        this.classToAllowedProps.set(value, options.allowProps);
      }
      super.register(value, options.identifier);
    } else {
      super.register(value, options);
    }
  }
  getAllowedProps(value) {
    return this.classToAllowedProps.get(value);
  }
};
function valuesOfObj3(record) {
  if ("values" in Object) {
    return Object.values(record);
  }
  const values = [];
  for (const key in record) {
    if (record.hasOwnProperty(key)) {
      values.push(record[key]);
    }
  }
  return values;
}
__name(valuesOfObj3, "valuesOfObj");
function find3(record, predicate) {
  const values = valuesOfObj3(record);
  if ("find" in values) {
    return values.find(predicate);
  }
  const valuesNotNever = values;
  for (let i = 0; i < valuesNotNever.length; i++) {
    const value = valuesNotNever[i];
    if (predicate(value)) {
      return value;
    }
  }
  return;
}
__name(find3, "find");
function forEach3(record, run) {
  Object.entries(record).forEach(([key, value]) => run(value, key));
}
__name(forEach3, "forEach");
function includes3(arr, value) {
  return arr.indexOf(value) !== -1;
}
__name(includes3, "includes");
function findArr3(record, predicate) {
  for (let i = 0; i < record.length; i++) {
    const value = record[i];
    if (predicate(value)) {
      return value;
    }
  }
  return;
}
__name(findArr3, "findArr");
var CustomTransformerRegistry3 = class {
  static {
    __name(this, "CustomTransformerRegistry");
  }
  constructor() {
    this.transfomers = {};
  }
  register(transformer) {
    this.transfomers[transformer.name] = transformer;
  }
  findApplicable(v) {
    return find3(this.transfomers, (transformer) => transformer.isApplicable(v));
  }
  findByName(name) {
    return this.transfomers[name];
  }
};
var getType4 = /* @__PURE__ */ __name((payload) => Object.prototype.toString.call(payload).slice(8, -1), "getType");
var isUndefined3 = /* @__PURE__ */ __name((payload) => typeof payload === "undefined", "isUndefined");
var isNull4 = /* @__PURE__ */ __name((payload) => payload === null, "isNull");
var isPlainObject4 = /* @__PURE__ */ __name((payload) => {
  if (typeof payload !== "object" || payload === null)
    return false;
  if (payload === Object.prototype)
    return false;
  if (Object.getPrototypeOf(payload) === null)
    return true;
  return Object.getPrototypeOf(payload) === Object.prototype;
}, "isPlainObject");
var isEmptyObject3 = /* @__PURE__ */ __name((payload) => isPlainObject4(payload) && Object.keys(payload).length === 0, "isEmptyObject");
var isArray4 = /* @__PURE__ */ __name((payload) => Array.isArray(payload), "isArray");
var isString3 = /* @__PURE__ */ __name((payload) => typeof payload === "string", "isString");
var isNumber3 = /* @__PURE__ */ __name((payload) => typeof payload === "number" && !isNaN(payload), "isNumber");
var isBoolean3 = /* @__PURE__ */ __name((payload) => typeof payload === "boolean", "isBoolean");
var isRegExp3 = /* @__PURE__ */ __name((payload) => payload instanceof RegExp, "isRegExp");
var isMap3 = /* @__PURE__ */ __name((payload) => payload instanceof Map, "isMap");
var isSet3 = /* @__PURE__ */ __name((payload) => payload instanceof Set, "isSet");
var isSymbol3 = /* @__PURE__ */ __name((payload) => getType4(payload) === "Symbol", "isSymbol");
var isDate3 = /* @__PURE__ */ __name((payload) => payload instanceof Date && !isNaN(payload.valueOf()), "isDate");
var isError3 = /* @__PURE__ */ __name((payload) => payload instanceof Error, "isError");
var isNaNValue3 = /* @__PURE__ */ __name((payload) => typeof payload === "number" && isNaN(payload), "isNaNValue");
var isPrimitive3 = /* @__PURE__ */ __name((payload) => isBoolean3(payload) || isNull4(payload) || isUndefined3(payload) || isNumber3(payload) || isString3(payload) || isSymbol3(payload), "isPrimitive");
var isBigint3 = /* @__PURE__ */ __name((payload) => typeof payload === "bigint", "isBigint");
var isInfinite3 = /* @__PURE__ */ __name((payload) => payload === Infinity || payload === -Infinity, "isInfinite");
var isTypedArray3 = /* @__PURE__ */ __name((payload) => ArrayBuffer.isView(payload) && !(payload instanceof DataView), "isTypedArray");
var isURL3 = /* @__PURE__ */ __name((payload) => payload instanceof URL, "isURL");
var escapeKey3 = /* @__PURE__ */ __name((key) => key.replace(/\\/g, "\\\\").replace(/\./g, "\\."), "escapeKey");
var stringifyPath3 = /* @__PURE__ */ __name((path) => path.map(String).map(escapeKey3).join("."), "stringifyPath");
var parsePath3 = /* @__PURE__ */ __name((string2, legacyPaths) => {
  const result = [];
  let segment = "";
  for (let i = 0; i < string2.length; i++) {
    let char = string2.charAt(i);
    if (!legacyPaths && char === "\\") {
      const escaped = string2.charAt(i + 1);
      if (escaped === "\\") {
        segment += "\\";
        i++;
        continue;
      } else if (escaped !== ".") {
        throw Error("invalid path");
      }
    }
    const isEscapedDot = char === "\\" && string2.charAt(i + 1) === ".";
    if (isEscapedDot) {
      segment += ".";
      i++;
      continue;
    }
    const isEndOfSegment = char === ".";
    if (isEndOfSegment) {
      result.push(segment);
      segment = "";
      continue;
    }
    segment += char;
  }
  const lastSegment = segment;
  result.push(lastSegment);
  return result;
}, "parsePath");
function simpleTransformation3(isApplicable, annotation, transform, untransform) {
  return {
    isApplicable,
    annotation,
    transform,
    untransform
  };
}
__name(simpleTransformation3, "simpleTransformation");
var simpleRules3 = [
  simpleTransformation3(isUndefined3, "undefined", () => null, () => {
    return;
  }),
  simpleTransformation3(isBigint3, "bigint", (v) => v.toString(), (v) => {
    if (typeof BigInt !== "undefined") {
      return BigInt(v);
    }
    console.error("Please add a BigInt polyfill.");
    return v;
  }),
  simpleTransformation3(isDate3, "Date", (v) => v.toISOString(), (v) => new Date(v)),
  simpleTransformation3(isError3, "Error", (v, superJson) => {
    const baseError = {
      name: v.name,
      message: v.message
    };
    if ("cause" in v) {
      baseError.cause = v.cause;
    }
    superJson.allowedErrorProps.forEach((prop) => {
      baseError[prop] = v[prop];
    });
    return baseError;
  }, (v, superJson) => {
    const e = new Error(v.message, { cause: v.cause });
    e.name = v.name;
    e.stack = v.stack;
    superJson.allowedErrorProps.forEach((prop) => {
      e[prop] = v[prop];
    });
    return e;
  }),
  simpleTransformation3(isRegExp3, "regexp", (v) => "" + v, (regex) => {
    const body = regex.slice(1, regex.lastIndexOf("/"));
    const flags2 = regex.slice(regex.lastIndexOf("/") + 1);
    return new RegExp(body, flags2);
  }),
  simpleTransformation3(isSet3, "set", (v) => [...v.values()], (v) => new Set(v)),
  simpleTransformation3(isMap3, "map", (v) => [...v.entries()], (v) => new Map(v)),
  simpleTransformation3((v) => isNaNValue3(v) || isInfinite3(v), "number", (v) => {
    if (isNaNValue3(v)) {
      return "NaN";
    }
    if (v > 0) {
      return "Infinity";
    } else {
      return "-Infinity";
    }
  }, Number),
  simpleTransformation3((v) => v === 0 && 1 / v === -Infinity, "number", () => {
    return "-0";
  }, Number),
  simpleTransformation3(isURL3, "URL", (v) => v.toString(), (v) => new URL(v))
];
function compositeTransformation3(isApplicable, annotation, transform, untransform) {
  return {
    isApplicable,
    annotation,
    transform,
    untransform
  };
}
__name(compositeTransformation3, "compositeTransformation");
var symbolRule3 = compositeTransformation3((s, superJson) => {
  if (isSymbol3(s)) {
    const isRegistered = !!superJson.symbolRegistry.getIdentifier(s);
    return isRegistered;
  }
  return false;
}, (s, superJson) => {
  const identifier = superJson.symbolRegistry.getIdentifier(s);
  return ["symbol", identifier];
}, (v) => v.description, (_, a, superJson) => {
  const value = superJson.symbolRegistry.getValue(a[1]);
  if (!value) {
    throw new Error("Trying to deserialize unknown symbol");
  }
  return value;
});
var constructorToName3 = [
  Int8Array,
  Uint8Array,
  Int16Array,
  Uint16Array,
  Int32Array,
  Uint32Array,
  Float32Array,
  Float64Array,
  Uint8ClampedArray
].reduce((obj, ctor) => {
  obj[ctor.name] = ctor;
  return obj;
}, {});
var typedArrayRule3 = compositeTransformation3(isTypedArray3, (v) => ["typed-array", v.constructor.name], (v) => [...v], (v, a) => {
  const ctor = constructorToName3[a[1]];
  if (!ctor) {
    throw new Error("Trying to deserialize unknown typed array");
  }
  return new ctor(v);
});
function isInstanceOfRegisteredClass3(potentialClass, superJson) {
  if (potentialClass?.constructor) {
    const isRegistered = !!superJson.classRegistry.getIdentifier(potentialClass.constructor);
    return isRegistered;
  }
  return false;
}
__name(isInstanceOfRegisteredClass3, "isInstanceOfRegisteredClass");
var classRule3 = compositeTransformation3(isInstanceOfRegisteredClass3, (clazz, superJson) => {
  const identifier = superJson.classRegistry.getIdentifier(clazz.constructor);
  return ["class", identifier];
}, (clazz, superJson) => {
  const allowedProps = superJson.classRegistry.getAllowedProps(clazz.constructor);
  if (!allowedProps) {
    return { ...clazz };
  }
  const result = {};
  allowedProps.forEach((prop) => {
    result[prop] = clazz[prop];
  });
  return result;
}, (v, a, superJson) => {
  const clazz = superJson.classRegistry.getValue(a[1]);
  if (!clazz) {
    throw new Error(`Trying to deserialize unknown class '${a[1]}' - check https://github.com/blitz-js/superjson/issues/116#issuecomment-773996564`);
  }
  return Object.assign(Object.create(clazz.prototype), v);
});
var customRule3 = compositeTransformation3((value, superJson) => {
  return !!superJson.customTransformerRegistry.findApplicable(value);
}, (value, superJson) => {
  const transformer = superJson.customTransformerRegistry.findApplicable(value);
  return ["custom", transformer.name];
}, (value, superJson) => {
  const transformer = superJson.customTransformerRegistry.findApplicable(value);
  return transformer.serialize(value);
}, (v, a, superJson) => {
  const transformer = superJson.customTransformerRegistry.findByName(a[1]);
  if (!transformer) {
    throw new Error("Trying to deserialize unknown custom value");
  }
  return transformer.deserialize(v);
});
var compositeRules3 = [classRule3, symbolRule3, customRule3, typedArrayRule3];
var transformValue3 = /* @__PURE__ */ __name((value, superJson) => {
  const applicableCompositeRule = findArr3(compositeRules3, (rule) => rule.isApplicable(value, superJson));
  if (applicableCompositeRule) {
    return {
      value: applicableCompositeRule.transform(value, superJson),
      type: applicableCompositeRule.annotation(value, superJson)
    };
  }
  const applicableSimpleRule = findArr3(simpleRules3, (rule) => rule.isApplicable(value, superJson));
  if (applicableSimpleRule) {
    return {
      value: applicableSimpleRule.transform(value, superJson),
      type: applicableSimpleRule.annotation
    };
  }
  return;
}, "transformValue");
var simpleRulesByAnnotation3 = {};
simpleRules3.forEach((rule) => {
  simpleRulesByAnnotation3[rule.annotation] = rule;
});
var untransformValue3 = /* @__PURE__ */ __name((json, type, superJson) => {
  if (isArray4(type)) {
    switch (type[0]) {
      case "symbol":
        return symbolRule3.untransform(json, type, superJson);
      case "class":
        return classRule3.untransform(json, type, superJson);
      case "custom":
        return customRule3.untransform(json, type, superJson);
      case "typed-array":
        return typedArrayRule3.untransform(json, type, superJson);
      default:
        throw new Error("Unknown transformation: " + type);
    }
  } else {
    const transformation = simpleRulesByAnnotation3[type];
    if (!transformation) {
      throw new Error("Unknown transformation: " + type);
    }
    return transformation.untransform(json, superJson);
  }
}, "untransformValue");
var getNthKey3 = /* @__PURE__ */ __name((value, n) => {
  if (n > value.size)
    throw new Error("index out of bounds");
  const keys = value.keys();
  while (n > 0) {
    keys.next();
    n--;
  }
  return keys.next().value;
}, "getNthKey");
function validatePath3(path) {
  if (includes3(path, "__proto__")) {
    throw new Error("__proto__ is not allowed as a property");
  }
  if (includes3(path, "prototype")) {
    throw new Error("prototype is not allowed as a property");
  }
  if (includes3(path, "constructor")) {
    throw new Error("constructor is not allowed as a property");
  }
}
__name(validatePath3, "validatePath");
var getDeep3 = /* @__PURE__ */ __name((object2, path) => {
  validatePath3(path);
  for (let i = 0; i < path.length; i++) {
    const key = path[i];
    if (isSet3(object2)) {
      object2 = getNthKey3(object2, +key);
    } else if (isMap3(object2)) {
      const row = +key;
      const type = +path[++i] === 0 ? "key" : "value";
      const keyOfRow = getNthKey3(object2, row);
      switch (type) {
        case "key":
          object2 = keyOfRow;
          break;
        case "value":
          object2 = object2.get(keyOfRow);
          break;
      }
    } else {
      object2 = object2[key];
    }
  }
  return object2;
}, "getDeep");
var setDeep3 = /* @__PURE__ */ __name((object2, path, mapper) => {
  validatePath3(path);
  if (path.length === 0) {
    return mapper(object2);
  }
  let parent = object2;
  for (let i = 0; i < path.length - 1; i++) {
    const key = path[i];
    if (isArray4(parent)) {
      const index = +key;
      parent = parent[index];
    } else if (isPlainObject4(parent)) {
      parent = parent[key];
    } else if (isSet3(parent)) {
      const row = +key;
      parent = getNthKey3(parent, row);
    } else if (isMap3(parent)) {
      const isEnd = i === path.length - 2;
      if (isEnd) {
        break;
      }
      const row = +key;
      const type = +path[++i] === 0 ? "key" : "value";
      const keyOfRow = getNthKey3(parent, row);
      switch (type) {
        case "key":
          parent = keyOfRow;
          break;
        case "value":
          parent = parent.get(keyOfRow);
          break;
      }
    }
  }
  const lastKey = path[path.length - 1];
  if (isArray4(parent)) {
    parent[+lastKey] = mapper(parent[+lastKey]);
  } else if (isPlainObject4(parent)) {
    parent[lastKey] = mapper(parent[lastKey]);
  }
  if (isSet3(parent)) {
    const oldValue = getNthKey3(parent, +lastKey);
    const newValue = mapper(oldValue);
    if (oldValue !== newValue) {
      parent.delete(oldValue);
      parent.add(newValue);
    }
  }
  if (isMap3(parent)) {
    const row = +path[path.length - 2];
    const keyToRow = getNthKey3(parent, row);
    const type = +lastKey === 0 ? "key" : "value";
    switch (type) {
      case "key": {
        const newKey = mapper(keyToRow);
        parent.set(newKey, parent.get(keyToRow));
        if (newKey !== keyToRow) {
          parent.delete(keyToRow);
        }
        break;
      }
      case "value": {
        parent.set(keyToRow, mapper(parent.get(keyToRow)));
        break;
      }
    }
  }
  return object2;
}, "setDeep");
var enableLegacyPaths3 = /* @__PURE__ */ __name((version4) => version4 < 1, "enableLegacyPaths");
function traverse3(tree, walker26, version4, origin = []) {
  if (!tree) {
    return;
  }
  const legacyPaths = enableLegacyPaths3(version4);
  if (!isArray4(tree)) {
    forEach3(tree, (subtree, key) => traverse3(subtree, walker26, version4, [
      ...origin,
      ...parsePath3(key, legacyPaths)
    ]));
    return;
  }
  const [nodeValue, children] = tree;
  if (children) {
    forEach3(children, (child, key) => {
      traverse3(child, walker26, version4, [
        ...origin,
        ...parsePath3(key, legacyPaths)
      ]);
    });
  }
  walker26(nodeValue, origin);
}
__name(traverse3, "traverse");
function applyValueAnnotations3(plain, annotations, version4, superJson) {
  traverse3(annotations, (type, path) => {
    plain = setDeep3(plain, path, (v) => untransformValue3(v, type, superJson));
  }, version4);
  return plain;
}
__name(applyValueAnnotations3, "applyValueAnnotations");
function applyReferentialEqualityAnnotations3(plain, annotations, version4) {
  const legacyPaths = enableLegacyPaths3(version4);
  function apply(identicalPaths, path) {
    const object2 = getDeep3(plain, parsePath3(path, legacyPaths));
    identicalPaths.map((path2) => parsePath3(path2, legacyPaths)).forEach((identicalObjectPath) => {
      plain = setDeep3(plain, identicalObjectPath, () => object2);
    });
  }
  __name(apply, "apply");
  if (isArray4(annotations)) {
    const [root, other] = annotations;
    root.forEach((identicalPath) => {
      plain = setDeep3(plain, parsePath3(identicalPath, legacyPaths), () => plain);
    });
    if (other) {
      forEach3(other, apply);
    }
  } else {
    forEach3(annotations, apply);
  }
  return plain;
}
__name(applyReferentialEqualityAnnotations3, "applyReferentialEqualityAnnotations");
var isDeep3 = /* @__PURE__ */ __name((object2, superJson) => isPlainObject4(object2) || isArray4(object2) || isMap3(object2) || isSet3(object2) || isError3(object2) || isInstanceOfRegisteredClass3(object2, superJson), "isDeep");
function addIdentity3(object2, path, identities) {
  const existingSet = identities.get(object2);
  if (existingSet) {
    existingSet.push(path);
  } else {
    identities.set(object2, [path]);
  }
}
__name(addIdentity3, "addIdentity");
function generateReferentialEqualityAnnotations3(identitites, dedupe) {
  const result = {};
  let rootEqualityPaths = void 0;
  identitites.forEach((paths) => {
    if (paths.length <= 1) {
      return;
    }
    if (!dedupe) {
      paths = paths.map((path) => path.map(String)).sort((a, b) => a.length - b.length);
    }
    const [representativePath, ...identicalPaths] = paths;
    if (representativePath.length === 0) {
      rootEqualityPaths = identicalPaths.map(stringifyPath3);
    } else {
      result[stringifyPath3(representativePath)] = identicalPaths.map(stringifyPath3);
    }
  });
  if (rootEqualityPaths) {
    if (isEmptyObject3(result)) {
      return [rootEqualityPaths];
    } else {
      return [rootEqualityPaths, result];
    }
  } else {
    return isEmptyObject3(result) ? void 0 : result;
  }
}
__name(generateReferentialEqualityAnnotations3, "generateReferentialEqualityAnnotations");
var walker3 = /* @__PURE__ */ __name((object2, identities, superJson, dedupe, path = [], objectsInThisPath = [], seenObjects = /* @__PURE__ */ new Map()) => {
  const primitive = isPrimitive3(object2);
  if (!primitive) {
    addIdentity3(object2, path, identities);
    const seen = seenObjects.get(object2);
    if (seen) {
      return dedupe ? {
        transformedValue: null
      } : seen;
    }
  }
  if (!isDeep3(object2, superJson)) {
    const transformed2 = transformValue3(object2, superJson);
    const result2 = transformed2 ? {
      transformedValue: transformed2.value,
      annotations: [transformed2.type]
    } : {
      transformedValue: object2
    };
    if (!primitive) {
      seenObjects.set(object2, result2);
    }
    return result2;
  }
  if (includes3(objectsInThisPath, object2)) {
    return {
      transformedValue: null
    };
  }
  const transformationResult = transformValue3(object2, superJson);
  const transformed = transformationResult?.value ?? object2;
  const transformedValue = isArray4(transformed) ? [] : {};
  const innerAnnotations = {};
  forEach3(transformed, (value, index) => {
    if (index === "__proto__" || index === "constructor" || index === "prototype") {
      throw new Error(`Detected property ${index}. This is a prototype pollution risk, please remove it from your object.`);
    }
    const recursiveResult = walker3(value, identities, superJson, dedupe, [...path, index], [...objectsInThisPath, object2], seenObjects);
    transformedValue[index] = recursiveResult.transformedValue;
    if (isArray4(recursiveResult.annotations)) {
      innerAnnotations[escapeKey3(index)] = recursiveResult.annotations;
    } else if (isPlainObject4(recursiveResult.annotations)) {
      forEach3(recursiveResult.annotations, (tree, key) => {
        innerAnnotations[escapeKey3(index) + "." + key] = tree;
      });
    }
  });
  const result = isEmptyObject3(innerAnnotations) ? {
    transformedValue,
    annotations: transformationResult ? [transformationResult.type] : void 0
  } : {
    transformedValue,
    annotations: transformationResult ? [transformationResult.type, innerAnnotations] : innerAnnotations
  };
  if (!primitive) {
    seenObjects.set(object2, result);
  }
  return result;
}, "walker");
function getType23(payload) {
  return Object.prototype.toString.call(payload).slice(8, -1);
}
__name(getType23, "getType2");
function isArray23(payload) {
  return getType23(payload) === "Array";
}
__name(isArray23, "isArray2");
function isPlainObject23(payload) {
  if (getType23(payload) !== "Object")
    return false;
  const prototype = Object.getPrototypeOf(payload);
  return !!prototype && prototype.constructor === Object && prototype === Object.prototype;
}
__name(isPlainObject23, "isPlainObject2");
function assignProp3(carry, key, newVal, originalObject, includeNonenumerable) {
  const propType = {}.propertyIsEnumerable.call(originalObject, key) ? "enumerable" : "nonenumerable";
  if (propType === "enumerable")
    carry[key] = newVal;
  if (includeNonenumerable && propType === "nonenumerable") {
    Object.defineProperty(carry, key, {
      value: newVal,
      enumerable: false,
      writable: true,
      configurable: true
    });
  }
}
__name(assignProp3, "assignProp");
function copy3(target, options = {}) {
  if (isArray23(target)) {
    return target.map((item) => copy3(item, options));
  }
  if (!isPlainObject23(target)) {
    return target;
  }
  const props = Object.getOwnPropertyNames(target);
  const symbols = Object.getOwnPropertySymbols(target);
  return [...props, ...symbols].reduce((carry, key) => {
    if (key === "__proto__")
      return carry;
    if (isArray23(options.props) && !options.props.includes(key)) {
      return carry;
    }
    const val = target[key];
    const newVal = copy3(val, options);
    assignProp3(carry, key, newVal, target, options.nonenumerable);
    return carry;
  }, {});
}
__name(copy3, "copy");
var SuperJSON3 = class {
  static {
    __name(this, "SuperJSON");
  }
  constructor({ dedupe = false } = {}) {
    this.classRegistry = new ClassRegistry3();
    this.symbolRegistry = new Registry3((s) => s.description ?? "");
    this.customTransformerRegistry = new CustomTransformerRegistry3();
    this.allowedErrorProps = [];
    this.dedupe = dedupe;
  }
  serialize(object2) {
    const identities = /* @__PURE__ */ new Map();
    const output = walker3(object2, identities, this, this.dedupe);
    const res = {
      json: output.transformedValue
    };
    if (output.annotations) {
      res.meta = {
        ...res.meta,
        values: output.annotations
      };
    }
    const equalityAnnotations = generateReferentialEqualityAnnotations3(identities, this.dedupe);
    if (equalityAnnotations) {
      res.meta = {
        ...res.meta,
        referentialEqualities: equalityAnnotations
      };
    }
    if (res.meta)
      res.meta.v = 1;
    return res;
  }
  deserialize(payload, options) {
    const { json, meta } = payload;
    let result = options?.inPlace ? json : copy3(json);
    if (meta?.values) {
      result = applyValueAnnotations3(result, meta.values, meta.v ?? 0, this);
    }
    if (meta?.referentialEqualities) {
      result = applyReferentialEqualityAnnotations3(result, meta.referentialEqualities, meta.v ?? 0);
    }
    return result;
  }
  stringify(object2) {
    return JSON.stringify(this.serialize(object2));
  }
  parse(string2) {
    return this.deserialize(JSON.parse(string2), { inPlace: true });
  }
  registerClass(v, options) {
    this.classRegistry.register(v, options);
  }
  registerSymbol(v, identifier) {
    this.symbolRegistry.register(v, identifier);
  }
  registerCustom(transformer, name) {
    this.customTransformerRegistry.register({
      name,
      ...transformer
    });
  }
  allowErrorProps(...props) {
    this.allowedErrorProps.push(...props);
  }
};
SuperJSON3.defaultInstance = new SuperJSON3();
SuperJSON3.serialize = SuperJSON3.defaultInstance.serialize.bind(SuperJSON3.defaultInstance);
SuperJSON3.deserialize = SuperJSON3.defaultInstance.deserialize.bind(SuperJSON3.defaultInstance);
SuperJSON3.stringify = SuperJSON3.defaultInstance.stringify.bind(SuperJSON3.defaultInstance);
SuperJSON3.parse = SuperJSON3.defaultInstance.parse.bind(SuperJSON3.defaultInstance);
SuperJSON3.registerClass = SuperJSON3.defaultInstance.registerClass.bind(SuperJSON3.defaultInstance);
SuperJSON3.registerSymbol = SuperJSON3.defaultInstance.registerSymbol.bind(SuperJSON3.defaultInstance);
SuperJSON3.registerCustom = SuperJSON3.defaultInstance.registerCustom.bind(SuperJSON3.defaultInstance);
SuperJSON3.allowErrorProps = SuperJSON3.defaultInstance.allowErrorProps.bind(SuperJSON3.defaultInstance);
var dist_default3 = SuperJSON3;
var serialize3 = SuperJSON3.serialize;
var deserialize3 = SuperJSON3.deserialize;
var stringify3 = SuperJSON3.stringify;
var parse3 = SuperJSON3.parse;
var registerClass3 = SuperJSON3.registerClass;
var registerCustom3 = SuperJSON3.registerCustom;
var registerSymbol3 = SuperJSON3.registerSymbol;
var allowErrorProps3 = SuperJSON3.allowErrorProps;
function parseData3(formData) {
  const propsArray = [];
  if (!formData)
    return propsArray;
  const batchsIDs = [];
  for (const [key, value] of Array.from(formData.entries())) {
    if (key.startsWith("FILE_"))
      propsArray.push(value);
    else if (key.startsWith("FILES_")) {
      if (batchsIDs.includes(key))
        continue;
      batchsIDs.push(key);
      propsArray.push(formData.getAll(key));
    } else {
      propsArray.push(JSON.parse(decodeURI(value)));
    }
  }
  return propsArray;
}
__name(parseData3, "parseData");
function paramsFromURL3(url) {
  const params = url.searchParams.entries().toArray().map(([_, v]) => v);
  return params.map((param) => JSON.parse(decodeURIComponent(param)));
}
__name(paramsFromURL3, "paramsFromURL");
async function WrapRequestHandler3(context2, endpoint) {
  const isServerAction = context2.request.headers.get("x-server-action") === "true";
  if (!isServerAction) {
    return new Response("Not Found", { status: 404 });
  }
  const parsedData = context2.request.method === "GET" || context2.request.method === "HEAD" ? paramsFromURL3(new URL(context2.request.url)) : parseData3(context2.request.headers.get("content-type") ? await context2.request.formData() : void 0);
  const missingProps = endpoint.length - parsedData.length;
  for (let i = 0; i < missingProps; i++) {
    parsedData.push(void 0);
  }
  parsedData.push(context2);
  const result = await endpoint(...parsedData);
  switch (typeof result) {
    case "string":
    case "number":
    case "boolean":
    case "bigint":
      return new Response(dist_default3.stringify(result), {
        headers: {
          "Content-Type": "application/json",
          dataType: "json"
        }
      });
    case "undefined":
      return new Response(null, { status: 204 });
    case "object":
      if (result instanceof Response) {
        result.headers.set("dataType", "response");
        return result;
      } else if (result instanceof Blob) {
        const res = new Response(await result.arrayBuffer());
        res.headers.set("dataType", "blob");
        res.headers.set("Content-Type", result.type);
        return res;
      } else if (result instanceof File) {
        return new Response(await result.arrayBuffer(), {
          headers: {
            "Content-Type": result.type,
            dataType: "file",
            fileData: JSON.stringify({
              name: result.name,
              lastModified: result.lastModified
            })
          }
        });
      } else {
        return new Response(dist_default3.stringify(result), {
          headers: { "Content-Type": "application/json", dataType: "json" }
        });
      }
    default:
      throw new Error(`Unsupported return type from action: ${typeof result}`);
  }
}
__name(WrapRequestHandler3, "WrapRequestHandler");
var onRequest3 = /* @__PURE__ */ __name(async (context2) => {
  const method = context2.request.method;
  const options = {
    GET: typeof GET === "function" ? GET : void 0,
    POST: typeof POST4 === "function" ? POST4 : void 0,
    PUT: typeof PUT === "function" ? PUT : void 0,
    DELETE: typeof DELETE === "function" ? DELETE : void 0,
    PATCH: typeof PATCH === "function" ? PATCH : void 0,
    HEAD: typeof HEAD === "function" ? HEAD : void 0,
    OPTIONS: typeof OPTIONS === "function" ? OPTIONS : void 0
  };
  if (!options[method]) {
    return new Response(`Method "${method}" Not Allowed`, {
      status: 405
    });
  }
  return await WrapRequestHandler3(context2, options[method]);
}, "onRequest");

// api/invites/template/email/manage.js
async function POST5({
  user_id,
  client_id,
  from_name
}) {
  const ctx = getContext(arguments);
  const current_user_id = (await ctx.data.client.getMetaData())?.id;
  if (!current_user_id) {
    return {
      success: false,
      error: "Unauthorized"
    };
  } else if (current_user_id === user_id) {
    return {
      success: false,
      error: "You cannot invite yourself"
    };
  }
  const db = drizzle(ctx.env.PROJECT_DB);
  const project = await db.select().from(projectTable).where(and(eq(projectTable.clientID, client_id), eq(projectTable.owner_id, current_user_id))).get();
  if (!project) {
    return {
      success: false,
      error: "Project not found or you don't have permission to invite users"
    };
  }
  const inviteManager = invites(ctx.env.PROJECT_DB);
  const existingInvite = await inviteManager.exists({
    type: "email_template",
    user_id,
    owner_group_id: project.owner_group_id,
    from_id: current_user_id
  });
  const create = /* @__PURE__ */ __name(() => {
    return inviteManager.create({
      type: "email_template",
      owner_group_id: project.owner_group_id,
      label: `Invite to Project ${project.name}`,
      from: {
        id: current_user_id,
        name: from_name
      },
      invite_user_id: user_id
    });
  }, "create");
  if (existingInvite) {
    await inviteManager.deleteById(existingInvite.id);
    const newInvite = await create();
    return {
      success: newInvite.success,
      error: newInvite.error
    };
  }
  const res = await create();
  return {
    success: res.success,
    error: res.error
  };
}
__name(POST5, "POST");
async function DELETE3({
  user_id,
  owner_group_id
}) {
  const ctx = getContext(arguments);
  const current_user_id = (await ctx.data.client.getMetaData())?.id;
  if (!current_user_id) {
    return {
      success: false,
      error: "Unauthorized"
    };
  } else if (current_user_id === user_id) {
    return {
      success: false,
      error: "You cannot revoke your own invite"
    };
  }
  const inviteManager = invites(ctx.env.PROJECT_DB);
  const existingInvite = await inviteManager.exists({
    type: "email_template",
    user_id,
    owner_group_id,
    from_id: current_user_id
  });
  if (existingInvite) {
    await inviteManager.deleteById(existingInvite.id);
  } else {
    return {
      success: false,
      error: "Invite not found"
    };
  }
  return {
    success: true
  };
}
__name(DELETE3, "DELETE");
var DoubleIndexedKV4 = class {
  static {
    __name(this, "DoubleIndexedKV");
  }
  constructor() {
    this.keyToValue = /* @__PURE__ */ new Map();
    this.valueToKey = /* @__PURE__ */ new Map();
  }
  set(key, value) {
    this.keyToValue.set(key, value);
    this.valueToKey.set(value, key);
  }
  getByKey(key) {
    return this.keyToValue.get(key);
  }
  getByValue(value) {
    return this.valueToKey.get(value);
  }
  clear() {
    this.keyToValue.clear();
    this.valueToKey.clear();
  }
};
var Registry4 = class {
  static {
    __name(this, "Registry");
  }
  constructor(generateIdentifier) {
    this.generateIdentifier = generateIdentifier;
    this.kv = new DoubleIndexedKV4();
  }
  register(value, identifier) {
    if (this.kv.getByValue(value)) {
      return;
    }
    if (!identifier) {
      identifier = this.generateIdentifier(value);
    }
    this.kv.set(identifier, value);
  }
  clear() {
    this.kv.clear();
  }
  getIdentifier(value) {
    return this.kv.getByValue(value);
  }
  getValue(identifier) {
    return this.kv.getByKey(identifier);
  }
};
var ClassRegistry4 = class extends Registry4 {
  static {
    __name(this, "ClassRegistry");
  }
  constructor() {
    super((c) => c.name);
    this.classToAllowedProps = /* @__PURE__ */ new Map();
  }
  register(value, options) {
    if (typeof options === "object") {
      if (options.allowProps) {
        this.classToAllowedProps.set(value, options.allowProps);
      }
      super.register(value, options.identifier);
    } else {
      super.register(value, options);
    }
  }
  getAllowedProps(value) {
    return this.classToAllowedProps.get(value);
  }
};
function valuesOfObj4(record) {
  if ("values" in Object) {
    return Object.values(record);
  }
  const values = [];
  for (const key in record) {
    if (record.hasOwnProperty(key)) {
      values.push(record[key]);
    }
  }
  return values;
}
__name(valuesOfObj4, "valuesOfObj");
function find4(record, predicate) {
  const values = valuesOfObj4(record);
  if ("find" in values) {
    return values.find(predicate);
  }
  const valuesNotNever = values;
  for (let i = 0; i < valuesNotNever.length; i++) {
    const value = valuesNotNever[i];
    if (predicate(value)) {
      return value;
    }
  }
  return;
}
__name(find4, "find");
function forEach4(record, run) {
  Object.entries(record).forEach(([key, value]) => run(value, key));
}
__name(forEach4, "forEach");
function includes4(arr, value) {
  return arr.indexOf(value) !== -1;
}
__name(includes4, "includes");
function findArr4(record, predicate) {
  for (let i = 0; i < record.length; i++) {
    const value = record[i];
    if (predicate(value)) {
      return value;
    }
  }
  return;
}
__name(findArr4, "findArr");
var CustomTransformerRegistry4 = class {
  static {
    __name(this, "CustomTransformerRegistry");
  }
  constructor() {
    this.transfomers = {};
  }
  register(transformer) {
    this.transfomers[transformer.name] = transformer;
  }
  findApplicable(v) {
    return find4(this.transfomers, (transformer) => transformer.isApplicable(v));
  }
  findByName(name) {
    return this.transfomers[name];
  }
};
var getType5 = /* @__PURE__ */ __name((payload) => Object.prototype.toString.call(payload).slice(8, -1), "getType");
var isUndefined4 = /* @__PURE__ */ __name((payload) => typeof payload === "undefined", "isUndefined");
var isNull5 = /* @__PURE__ */ __name((payload) => payload === null, "isNull");
var isPlainObject5 = /* @__PURE__ */ __name((payload) => {
  if (typeof payload !== "object" || payload === null)
    return false;
  if (payload === Object.prototype)
    return false;
  if (Object.getPrototypeOf(payload) === null)
    return true;
  return Object.getPrototypeOf(payload) === Object.prototype;
}, "isPlainObject");
var isEmptyObject4 = /* @__PURE__ */ __name((payload) => isPlainObject5(payload) && Object.keys(payload).length === 0, "isEmptyObject");
var isArray5 = /* @__PURE__ */ __name((payload) => Array.isArray(payload), "isArray");
var isString4 = /* @__PURE__ */ __name((payload) => typeof payload === "string", "isString");
var isNumber4 = /* @__PURE__ */ __name((payload) => typeof payload === "number" && !isNaN(payload), "isNumber");
var isBoolean4 = /* @__PURE__ */ __name((payload) => typeof payload === "boolean", "isBoolean");
var isRegExp4 = /* @__PURE__ */ __name((payload) => payload instanceof RegExp, "isRegExp");
var isMap4 = /* @__PURE__ */ __name((payload) => payload instanceof Map, "isMap");
var isSet4 = /* @__PURE__ */ __name((payload) => payload instanceof Set, "isSet");
var isSymbol4 = /* @__PURE__ */ __name((payload) => getType5(payload) === "Symbol", "isSymbol");
var isDate4 = /* @__PURE__ */ __name((payload) => payload instanceof Date && !isNaN(payload.valueOf()), "isDate");
var isError4 = /* @__PURE__ */ __name((payload) => payload instanceof Error, "isError");
var isNaNValue4 = /* @__PURE__ */ __name((payload) => typeof payload === "number" && isNaN(payload), "isNaNValue");
var isPrimitive4 = /* @__PURE__ */ __name((payload) => isBoolean4(payload) || isNull5(payload) || isUndefined4(payload) || isNumber4(payload) || isString4(payload) || isSymbol4(payload), "isPrimitive");
var isBigint4 = /* @__PURE__ */ __name((payload) => typeof payload === "bigint", "isBigint");
var isInfinite4 = /* @__PURE__ */ __name((payload) => payload === Infinity || payload === -Infinity, "isInfinite");
var isTypedArray4 = /* @__PURE__ */ __name((payload) => ArrayBuffer.isView(payload) && !(payload instanceof DataView), "isTypedArray");
var isURL4 = /* @__PURE__ */ __name((payload) => payload instanceof URL, "isURL");
var escapeKey4 = /* @__PURE__ */ __name((key) => key.replace(/\\/g, "\\\\").replace(/\./g, "\\."), "escapeKey");
var stringifyPath4 = /* @__PURE__ */ __name((path) => path.map(String).map(escapeKey4).join("."), "stringifyPath");
var parsePath4 = /* @__PURE__ */ __name((string2, legacyPaths) => {
  const result = [];
  let segment = "";
  for (let i = 0; i < string2.length; i++) {
    let char = string2.charAt(i);
    if (!legacyPaths && char === "\\") {
      const escaped = string2.charAt(i + 1);
      if (escaped === "\\") {
        segment += "\\";
        i++;
        continue;
      } else if (escaped !== ".") {
        throw Error("invalid path");
      }
    }
    const isEscapedDot = char === "\\" && string2.charAt(i + 1) === ".";
    if (isEscapedDot) {
      segment += ".";
      i++;
      continue;
    }
    const isEndOfSegment = char === ".";
    if (isEndOfSegment) {
      result.push(segment);
      segment = "";
      continue;
    }
    segment += char;
  }
  const lastSegment = segment;
  result.push(lastSegment);
  return result;
}, "parsePath");
function simpleTransformation4(isApplicable, annotation, transform, untransform) {
  return {
    isApplicable,
    annotation,
    transform,
    untransform
  };
}
__name(simpleTransformation4, "simpleTransformation");
var simpleRules4 = [
  simpleTransformation4(isUndefined4, "undefined", () => null, () => {
    return;
  }),
  simpleTransformation4(isBigint4, "bigint", (v) => v.toString(), (v) => {
    if (typeof BigInt !== "undefined") {
      return BigInt(v);
    }
    console.error("Please add a BigInt polyfill.");
    return v;
  }),
  simpleTransformation4(isDate4, "Date", (v) => v.toISOString(), (v) => new Date(v)),
  simpleTransformation4(isError4, "Error", (v, superJson) => {
    const baseError = {
      name: v.name,
      message: v.message
    };
    if ("cause" in v) {
      baseError.cause = v.cause;
    }
    superJson.allowedErrorProps.forEach((prop) => {
      baseError[prop] = v[prop];
    });
    return baseError;
  }, (v, superJson) => {
    const e = new Error(v.message, { cause: v.cause });
    e.name = v.name;
    e.stack = v.stack;
    superJson.allowedErrorProps.forEach((prop) => {
      e[prop] = v[prop];
    });
    return e;
  }),
  simpleTransformation4(isRegExp4, "regexp", (v) => "" + v, (regex) => {
    const body = regex.slice(1, regex.lastIndexOf("/"));
    const flags2 = regex.slice(regex.lastIndexOf("/") + 1);
    return new RegExp(body, flags2);
  }),
  simpleTransformation4(isSet4, "set", (v) => [...v.values()], (v) => new Set(v)),
  simpleTransformation4(isMap4, "map", (v) => [...v.entries()], (v) => new Map(v)),
  simpleTransformation4((v) => isNaNValue4(v) || isInfinite4(v), "number", (v) => {
    if (isNaNValue4(v)) {
      return "NaN";
    }
    if (v > 0) {
      return "Infinity";
    } else {
      return "-Infinity";
    }
  }, Number),
  simpleTransformation4((v) => v === 0 && 1 / v === -Infinity, "number", () => {
    return "-0";
  }, Number),
  simpleTransformation4(isURL4, "URL", (v) => v.toString(), (v) => new URL(v))
];
function compositeTransformation4(isApplicable, annotation, transform, untransform) {
  return {
    isApplicable,
    annotation,
    transform,
    untransform
  };
}
__name(compositeTransformation4, "compositeTransformation");
var symbolRule4 = compositeTransformation4((s, superJson) => {
  if (isSymbol4(s)) {
    const isRegistered = !!superJson.symbolRegistry.getIdentifier(s);
    return isRegistered;
  }
  return false;
}, (s, superJson) => {
  const identifier = superJson.symbolRegistry.getIdentifier(s);
  return ["symbol", identifier];
}, (v) => v.description, (_, a, superJson) => {
  const value = superJson.symbolRegistry.getValue(a[1]);
  if (!value) {
    throw new Error("Trying to deserialize unknown symbol");
  }
  return value;
});
var constructorToName4 = [
  Int8Array,
  Uint8Array,
  Int16Array,
  Uint16Array,
  Int32Array,
  Uint32Array,
  Float32Array,
  Float64Array,
  Uint8ClampedArray
].reduce((obj, ctor) => {
  obj[ctor.name] = ctor;
  return obj;
}, {});
var typedArrayRule4 = compositeTransformation4(isTypedArray4, (v) => ["typed-array", v.constructor.name], (v) => [...v], (v, a) => {
  const ctor = constructorToName4[a[1]];
  if (!ctor) {
    throw new Error("Trying to deserialize unknown typed array");
  }
  return new ctor(v);
});
function isInstanceOfRegisteredClass4(potentialClass, superJson) {
  if (potentialClass?.constructor) {
    const isRegistered = !!superJson.classRegistry.getIdentifier(potentialClass.constructor);
    return isRegistered;
  }
  return false;
}
__name(isInstanceOfRegisteredClass4, "isInstanceOfRegisteredClass");
var classRule4 = compositeTransformation4(isInstanceOfRegisteredClass4, (clazz, superJson) => {
  const identifier = superJson.classRegistry.getIdentifier(clazz.constructor);
  return ["class", identifier];
}, (clazz, superJson) => {
  const allowedProps = superJson.classRegistry.getAllowedProps(clazz.constructor);
  if (!allowedProps) {
    return { ...clazz };
  }
  const result = {};
  allowedProps.forEach((prop) => {
    result[prop] = clazz[prop];
  });
  return result;
}, (v, a, superJson) => {
  const clazz = superJson.classRegistry.getValue(a[1]);
  if (!clazz) {
    throw new Error(`Trying to deserialize unknown class '${a[1]}' - check https://github.com/blitz-js/superjson/issues/116#issuecomment-773996564`);
  }
  return Object.assign(Object.create(clazz.prototype), v);
});
var customRule4 = compositeTransformation4((value, superJson) => {
  return !!superJson.customTransformerRegistry.findApplicable(value);
}, (value, superJson) => {
  const transformer = superJson.customTransformerRegistry.findApplicable(value);
  return ["custom", transformer.name];
}, (value, superJson) => {
  const transformer = superJson.customTransformerRegistry.findApplicable(value);
  return transformer.serialize(value);
}, (v, a, superJson) => {
  const transformer = superJson.customTransformerRegistry.findByName(a[1]);
  if (!transformer) {
    throw new Error("Trying to deserialize unknown custom value");
  }
  return transformer.deserialize(v);
});
var compositeRules4 = [classRule4, symbolRule4, customRule4, typedArrayRule4];
var transformValue4 = /* @__PURE__ */ __name((value, superJson) => {
  const applicableCompositeRule = findArr4(compositeRules4, (rule) => rule.isApplicable(value, superJson));
  if (applicableCompositeRule) {
    return {
      value: applicableCompositeRule.transform(value, superJson),
      type: applicableCompositeRule.annotation(value, superJson)
    };
  }
  const applicableSimpleRule = findArr4(simpleRules4, (rule) => rule.isApplicable(value, superJson));
  if (applicableSimpleRule) {
    return {
      value: applicableSimpleRule.transform(value, superJson),
      type: applicableSimpleRule.annotation
    };
  }
  return;
}, "transformValue");
var simpleRulesByAnnotation4 = {};
simpleRules4.forEach((rule) => {
  simpleRulesByAnnotation4[rule.annotation] = rule;
});
var untransformValue4 = /* @__PURE__ */ __name((json, type, superJson) => {
  if (isArray5(type)) {
    switch (type[0]) {
      case "symbol":
        return symbolRule4.untransform(json, type, superJson);
      case "class":
        return classRule4.untransform(json, type, superJson);
      case "custom":
        return customRule4.untransform(json, type, superJson);
      case "typed-array":
        return typedArrayRule4.untransform(json, type, superJson);
      default:
        throw new Error("Unknown transformation: " + type);
    }
  } else {
    const transformation = simpleRulesByAnnotation4[type];
    if (!transformation) {
      throw new Error("Unknown transformation: " + type);
    }
    return transformation.untransform(json, superJson);
  }
}, "untransformValue");
var getNthKey4 = /* @__PURE__ */ __name((value, n) => {
  if (n > value.size)
    throw new Error("index out of bounds");
  const keys = value.keys();
  while (n > 0) {
    keys.next();
    n--;
  }
  return keys.next().value;
}, "getNthKey");
function validatePath4(path) {
  if (includes4(path, "__proto__")) {
    throw new Error("__proto__ is not allowed as a property");
  }
  if (includes4(path, "prototype")) {
    throw new Error("prototype is not allowed as a property");
  }
  if (includes4(path, "constructor")) {
    throw new Error("constructor is not allowed as a property");
  }
}
__name(validatePath4, "validatePath");
var getDeep4 = /* @__PURE__ */ __name((object2, path) => {
  validatePath4(path);
  for (let i = 0; i < path.length; i++) {
    const key = path[i];
    if (isSet4(object2)) {
      object2 = getNthKey4(object2, +key);
    } else if (isMap4(object2)) {
      const row = +key;
      const type = +path[++i] === 0 ? "key" : "value";
      const keyOfRow = getNthKey4(object2, row);
      switch (type) {
        case "key":
          object2 = keyOfRow;
          break;
        case "value":
          object2 = object2.get(keyOfRow);
          break;
      }
    } else {
      object2 = object2[key];
    }
  }
  return object2;
}, "getDeep");
var setDeep4 = /* @__PURE__ */ __name((object2, path, mapper) => {
  validatePath4(path);
  if (path.length === 0) {
    return mapper(object2);
  }
  let parent = object2;
  for (let i = 0; i < path.length - 1; i++) {
    const key = path[i];
    if (isArray5(parent)) {
      const index = +key;
      parent = parent[index];
    } else if (isPlainObject5(parent)) {
      parent = parent[key];
    } else if (isSet4(parent)) {
      const row = +key;
      parent = getNthKey4(parent, row);
    } else if (isMap4(parent)) {
      const isEnd = i === path.length - 2;
      if (isEnd) {
        break;
      }
      const row = +key;
      const type = +path[++i] === 0 ? "key" : "value";
      const keyOfRow = getNthKey4(parent, row);
      switch (type) {
        case "key":
          parent = keyOfRow;
          break;
        case "value":
          parent = parent.get(keyOfRow);
          break;
      }
    }
  }
  const lastKey = path[path.length - 1];
  if (isArray5(parent)) {
    parent[+lastKey] = mapper(parent[+lastKey]);
  } else if (isPlainObject5(parent)) {
    parent[lastKey] = mapper(parent[lastKey]);
  }
  if (isSet4(parent)) {
    const oldValue = getNthKey4(parent, +lastKey);
    const newValue = mapper(oldValue);
    if (oldValue !== newValue) {
      parent.delete(oldValue);
      parent.add(newValue);
    }
  }
  if (isMap4(parent)) {
    const row = +path[path.length - 2];
    const keyToRow = getNthKey4(parent, row);
    const type = +lastKey === 0 ? "key" : "value";
    switch (type) {
      case "key": {
        const newKey = mapper(keyToRow);
        parent.set(newKey, parent.get(keyToRow));
        if (newKey !== keyToRow) {
          parent.delete(keyToRow);
        }
        break;
      }
      case "value": {
        parent.set(keyToRow, mapper(parent.get(keyToRow)));
        break;
      }
    }
  }
  return object2;
}, "setDeep");
var enableLegacyPaths4 = /* @__PURE__ */ __name((version4) => version4 < 1, "enableLegacyPaths");
function traverse4(tree, walker26, version4, origin = []) {
  if (!tree) {
    return;
  }
  const legacyPaths = enableLegacyPaths4(version4);
  if (!isArray5(tree)) {
    forEach4(tree, (subtree, key) => traverse4(subtree, walker26, version4, [
      ...origin,
      ...parsePath4(key, legacyPaths)
    ]));
    return;
  }
  const [nodeValue, children] = tree;
  if (children) {
    forEach4(children, (child, key) => {
      traverse4(child, walker26, version4, [
        ...origin,
        ...parsePath4(key, legacyPaths)
      ]);
    });
  }
  walker26(nodeValue, origin);
}
__name(traverse4, "traverse");
function applyValueAnnotations4(plain, annotations, version4, superJson) {
  traverse4(annotations, (type, path) => {
    plain = setDeep4(plain, path, (v) => untransformValue4(v, type, superJson));
  }, version4);
  return plain;
}
__name(applyValueAnnotations4, "applyValueAnnotations");
function applyReferentialEqualityAnnotations4(plain, annotations, version4) {
  const legacyPaths = enableLegacyPaths4(version4);
  function apply(identicalPaths, path) {
    const object2 = getDeep4(plain, parsePath4(path, legacyPaths));
    identicalPaths.map((path2) => parsePath4(path2, legacyPaths)).forEach((identicalObjectPath) => {
      plain = setDeep4(plain, identicalObjectPath, () => object2);
    });
  }
  __name(apply, "apply");
  if (isArray5(annotations)) {
    const [root, other] = annotations;
    root.forEach((identicalPath) => {
      plain = setDeep4(plain, parsePath4(identicalPath, legacyPaths), () => plain);
    });
    if (other) {
      forEach4(other, apply);
    }
  } else {
    forEach4(annotations, apply);
  }
  return plain;
}
__name(applyReferentialEqualityAnnotations4, "applyReferentialEqualityAnnotations");
var isDeep4 = /* @__PURE__ */ __name((object2, superJson) => isPlainObject5(object2) || isArray5(object2) || isMap4(object2) || isSet4(object2) || isError4(object2) || isInstanceOfRegisteredClass4(object2, superJson), "isDeep");
function addIdentity4(object2, path, identities) {
  const existingSet = identities.get(object2);
  if (existingSet) {
    existingSet.push(path);
  } else {
    identities.set(object2, [path]);
  }
}
__name(addIdentity4, "addIdentity");
function generateReferentialEqualityAnnotations4(identitites, dedupe) {
  const result = {};
  let rootEqualityPaths = void 0;
  identitites.forEach((paths) => {
    if (paths.length <= 1) {
      return;
    }
    if (!dedupe) {
      paths = paths.map((path) => path.map(String)).sort((a, b) => a.length - b.length);
    }
    const [representativePath, ...identicalPaths] = paths;
    if (representativePath.length === 0) {
      rootEqualityPaths = identicalPaths.map(stringifyPath4);
    } else {
      result[stringifyPath4(representativePath)] = identicalPaths.map(stringifyPath4);
    }
  });
  if (rootEqualityPaths) {
    if (isEmptyObject4(result)) {
      return [rootEqualityPaths];
    } else {
      return [rootEqualityPaths, result];
    }
  } else {
    return isEmptyObject4(result) ? void 0 : result;
  }
}
__name(generateReferentialEqualityAnnotations4, "generateReferentialEqualityAnnotations");
var walker4 = /* @__PURE__ */ __name((object2, identities, superJson, dedupe, path = [], objectsInThisPath = [], seenObjects = /* @__PURE__ */ new Map()) => {
  const primitive = isPrimitive4(object2);
  if (!primitive) {
    addIdentity4(object2, path, identities);
    const seen = seenObjects.get(object2);
    if (seen) {
      return dedupe ? {
        transformedValue: null
      } : seen;
    }
  }
  if (!isDeep4(object2, superJson)) {
    const transformed2 = transformValue4(object2, superJson);
    const result2 = transformed2 ? {
      transformedValue: transformed2.value,
      annotations: [transformed2.type]
    } : {
      transformedValue: object2
    };
    if (!primitive) {
      seenObjects.set(object2, result2);
    }
    return result2;
  }
  if (includes4(objectsInThisPath, object2)) {
    return {
      transformedValue: null
    };
  }
  const transformationResult = transformValue4(object2, superJson);
  const transformed = transformationResult?.value ?? object2;
  const transformedValue = isArray5(transformed) ? [] : {};
  const innerAnnotations = {};
  forEach4(transformed, (value, index) => {
    if (index === "__proto__" || index === "constructor" || index === "prototype") {
      throw new Error(`Detected property ${index}. This is a prototype pollution risk, please remove it from your object.`);
    }
    const recursiveResult = walker4(value, identities, superJson, dedupe, [...path, index], [...objectsInThisPath, object2], seenObjects);
    transformedValue[index] = recursiveResult.transformedValue;
    if (isArray5(recursiveResult.annotations)) {
      innerAnnotations[escapeKey4(index)] = recursiveResult.annotations;
    } else if (isPlainObject5(recursiveResult.annotations)) {
      forEach4(recursiveResult.annotations, (tree, key) => {
        innerAnnotations[escapeKey4(index) + "." + key] = tree;
      });
    }
  });
  const result = isEmptyObject4(innerAnnotations) ? {
    transformedValue,
    annotations: transformationResult ? [transformationResult.type] : void 0
  } : {
    transformedValue,
    annotations: transformationResult ? [transformationResult.type, innerAnnotations] : innerAnnotations
  };
  if (!primitive) {
    seenObjects.set(object2, result);
  }
  return result;
}, "walker");
function getType24(payload) {
  return Object.prototype.toString.call(payload).slice(8, -1);
}
__name(getType24, "getType2");
function isArray24(payload) {
  return getType24(payload) === "Array";
}
__name(isArray24, "isArray2");
function isPlainObject24(payload) {
  if (getType24(payload) !== "Object")
    return false;
  const prototype = Object.getPrototypeOf(payload);
  return !!prototype && prototype.constructor === Object && prototype === Object.prototype;
}
__name(isPlainObject24, "isPlainObject2");
function assignProp4(carry, key, newVal, originalObject, includeNonenumerable) {
  const propType = {}.propertyIsEnumerable.call(originalObject, key) ? "enumerable" : "nonenumerable";
  if (propType === "enumerable")
    carry[key] = newVal;
  if (includeNonenumerable && propType === "nonenumerable") {
    Object.defineProperty(carry, key, {
      value: newVal,
      enumerable: false,
      writable: true,
      configurable: true
    });
  }
}
__name(assignProp4, "assignProp");
function copy4(target, options = {}) {
  if (isArray24(target)) {
    return target.map((item) => copy4(item, options));
  }
  if (!isPlainObject24(target)) {
    return target;
  }
  const props = Object.getOwnPropertyNames(target);
  const symbols = Object.getOwnPropertySymbols(target);
  return [...props, ...symbols].reduce((carry, key) => {
    if (key === "__proto__")
      return carry;
    if (isArray24(options.props) && !options.props.includes(key)) {
      return carry;
    }
    const val = target[key];
    const newVal = copy4(val, options);
    assignProp4(carry, key, newVal, target, options.nonenumerable);
    return carry;
  }, {});
}
__name(copy4, "copy");
var SuperJSON4 = class {
  static {
    __name(this, "SuperJSON");
  }
  constructor({ dedupe = false } = {}) {
    this.classRegistry = new ClassRegistry4();
    this.symbolRegistry = new Registry4((s) => s.description ?? "");
    this.customTransformerRegistry = new CustomTransformerRegistry4();
    this.allowedErrorProps = [];
    this.dedupe = dedupe;
  }
  serialize(object2) {
    const identities = /* @__PURE__ */ new Map();
    const output = walker4(object2, identities, this, this.dedupe);
    const res = {
      json: output.transformedValue
    };
    if (output.annotations) {
      res.meta = {
        ...res.meta,
        values: output.annotations
      };
    }
    const equalityAnnotations = generateReferentialEqualityAnnotations4(identities, this.dedupe);
    if (equalityAnnotations) {
      res.meta = {
        ...res.meta,
        referentialEqualities: equalityAnnotations
      };
    }
    if (res.meta)
      res.meta.v = 1;
    return res;
  }
  deserialize(payload, options) {
    const { json, meta } = payload;
    let result = options?.inPlace ? json : copy4(json);
    if (meta?.values) {
      result = applyValueAnnotations4(result, meta.values, meta.v ?? 0, this);
    }
    if (meta?.referentialEqualities) {
      result = applyReferentialEqualityAnnotations4(result, meta.referentialEqualities, meta.v ?? 0);
    }
    return result;
  }
  stringify(object2) {
    return JSON.stringify(this.serialize(object2));
  }
  parse(string2) {
    return this.deserialize(JSON.parse(string2), { inPlace: true });
  }
  registerClass(v, options) {
    this.classRegistry.register(v, options);
  }
  registerSymbol(v, identifier) {
    this.symbolRegistry.register(v, identifier);
  }
  registerCustom(transformer, name) {
    this.customTransformerRegistry.register({
      name,
      ...transformer
    });
  }
  allowErrorProps(...props) {
    this.allowedErrorProps.push(...props);
  }
};
SuperJSON4.defaultInstance = new SuperJSON4();
SuperJSON4.serialize = SuperJSON4.defaultInstance.serialize.bind(SuperJSON4.defaultInstance);
SuperJSON4.deserialize = SuperJSON4.defaultInstance.deserialize.bind(SuperJSON4.defaultInstance);
SuperJSON4.stringify = SuperJSON4.defaultInstance.stringify.bind(SuperJSON4.defaultInstance);
SuperJSON4.parse = SuperJSON4.defaultInstance.parse.bind(SuperJSON4.defaultInstance);
SuperJSON4.registerClass = SuperJSON4.defaultInstance.registerClass.bind(SuperJSON4.defaultInstance);
SuperJSON4.registerSymbol = SuperJSON4.defaultInstance.registerSymbol.bind(SuperJSON4.defaultInstance);
SuperJSON4.registerCustom = SuperJSON4.defaultInstance.registerCustom.bind(SuperJSON4.defaultInstance);
SuperJSON4.allowErrorProps = SuperJSON4.defaultInstance.allowErrorProps.bind(SuperJSON4.defaultInstance);
var dist_default4 = SuperJSON4;
var serialize4 = SuperJSON4.serialize;
var deserialize4 = SuperJSON4.deserialize;
var stringify4 = SuperJSON4.stringify;
var parse4 = SuperJSON4.parse;
var registerClass4 = SuperJSON4.registerClass;
var registerCustom4 = SuperJSON4.registerCustom;
var registerSymbol4 = SuperJSON4.registerSymbol;
var allowErrorProps4 = SuperJSON4.allowErrorProps;
function parseData4(formData) {
  const propsArray = [];
  if (!formData)
    return propsArray;
  const batchsIDs = [];
  for (const [key, value] of Array.from(formData.entries())) {
    if (key.startsWith("FILE_"))
      propsArray.push(value);
    else if (key.startsWith("FILES_")) {
      if (batchsIDs.includes(key))
        continue;
      batchsIDs.push(key);
      propsArray.push(formData.getAll(key));
    } else {
      propsArray.push(JSON.parse(decodeURI(value)));
    }
  }
  return propsArray;
}
__name(parseData4, "parseData");
function paramsFromURL4(url) {
  const params = url.searchParams.entries().toArray().map(([_, v]) => v);
  return params.map((param) => JSON.parse(decodeURIComponent(param)));
}
__name(paramsFromURL4, "paramsFromURL");
async function WrapRequestHandler4(context2, endpoint) {
  const isServerAction = context2.request.headers.get("x-server-action") === "true";
  if (!isServerAction) {
    return new Response("Not Found", { status: 404 });
  }
  const parsedData = context2.request.method === "GET" || context2.request.method === "HEAD" ? paramsFromURL4(new URL(context2.request.url)) : parseData4(context2.request.headers.get("content-type") ? await context2.request.formData() : void 0);
  const missingProps = endpoint.length - parsedData.length;
  for (let i = 0; i < missingProps; i++) {
    parsedData.push(void 0);
  }
  parsedData.push(context2);
  const result = await endpoint(...parsedData);
  switch (typeof result) {
    case "string":
    case "number":
    case "boolean":
    case "bigint":
      return new Response(dist_default4.stringify(result), {
        headers: {
          "Content-Type": "application/json",
          dataType: "json"
        }
      });
    case "undefined":
      return new Response(null, { status: 204 });
    case "object":
      if (result instanceof Response) {
        result.headers.set("dataType", "response");
        return result;
      } else if (result instanceof Blob) {
        const res = new Response(await result.arrayBuffer());
        res.headers.set("dataType", "blob");
        res.headers.set("Content-Type", result.type);
        return res;
      } else if (result instanceof File) {
        return new Response(await result.arrayBuffer(), {
          headers: {
            "Content-Type": result.type,
            dataType: "file",
            fileData: JSON.stringify({
              name: result.name,
              lastModified: result.lastModified
            })
          }
        });
      } else {
        return new Response(dist_default4.stringify(result), {
          headers: { "Content-Type": "application/json", dataType: "json" }
        });
      }
    default:
      throw new Error(`Unsupported return type from action: ${typeof result}`);
  }
}
__name(WrapRequestHandler4, "WrapRequestHandler");
var onRequest4 = /* @__PURE__ */ __name(async (context2) => {
  const method = context2.request.method;
  const options = {
    GET: typeof GET === "function" ? GET : void 0,
    POST: typeof POST5 === "function" ? POST5 : void 0,
    PUT: typeof PUT === "function" ? PUT : void 0,
    DELETE: typeof DELETE3 === "function" ? DELETE3 : void 0,
    PATCH: typeof PATCH === "function" ? PATCH : void 0,
    HEAD: typeof HEAD === "function" ? HEAD : void 0,
    OPTIONS: typeof OPTIONS === "function" ? OPTIONS : void 0
  };
  if (!options[method]) {
    return new Response(`Method "${method}" Not Allowed`, {
      status: 405
    });
  }
  return await WrapRequestHandler4(context2, options[method]);
}, "onRequest");

// api/invites/template/ui/accept.js
async function POST6({ code }) {
  const ctx = getContext(arguments);
  const userId = (await ctx.data.client.getMetaData())?.id;
  if (!userId) {
    return { success: false, error: "Unauthorized" };
  }
  const manager = invites(ctx.env.PROJECT_DB);
  return await manager.confirmInvite(code, userId, "ui_template", ctx.data.client);
}
__name(POST6, "POST");
var DoubleIndexedKV5 = class {
  static {
    __name(this, "DoubleIndexedKV");
  }
  constructor() {
    this.keyToValue = /* @__PURE__ */ new Map();
    this.valueToKey = /* @__PURE__ */ new Map();
  }
  set(key, value) {
    this.keyToValue.set(key, value);
    this.valueToKey.set(value, key);
  }
  getByKey(key) {
    return this.keyToValue.get(key);
  }
  getByValue(value) {
    return this.valueToKey.get(value);
  }
  clear() {
    this.keyToValue.clear();
    this.valueToKey.clear();
  }
};
var Registry5 = class {
  static {
    __name(this, "Registry");
  }
  constructor(generateIdentifier) {
    this.generateIdentifier = generateIdentifier;
    this.kv = new DoubleIndexedKV5();
  }
  register(value, identifier) {
    if (this.kv.getByValue(value)) {
      return;
    }
    if (!identifier) {
      identifier = this.generateIdentifier(value);
    }
    this.kv.set(identifier, value);
  }
  clear() {
    this.kv.clear();
  }
  getIdentifier(value) {
    return this.kv.getByValue(value);
  }
  getValue(identifier) {
    return this.kv.getByKey(identifier);
  }
};
var ClassRegistry5 = class extends Registry5 {
  static {
    __name(this, "ClassRegistry");
  }
  constructor() {
    super((c) => c.name);
    this.classToAllowedProps = /* @__PURE__ */ new Map();
  }
  register(value, options) {
    if (typeof options === "object") {
      if (options.allowProps) {
        this.classToAllowedProps.set(value, options.allowProps);
      }
      super.register(value, options.identifier);
    } else {
      super.register(value, options);
    }
  }
  getAllowedProps(value) {
    return this.classToAllowedProps.get(value);
  }
};
function valuesOfObj5(record) {
  if ("values" in Object) {
    return Object.values(record);
  }
  const values = [];
  for (const key in record) {
    if (record.hasOwnProperty(key)) {
      values.push(record[key]);
    }
  }
  return values;
}
__name(valuesOfObj5, "valuesOfObj");
function find5(record, predicate) {
  const values = valuesOfObj5(record);
  if ("find" in values) {
    return values.find(predicate);
  }
  const valuesNotNever = values;
  for (let i = 0; i < valuesNotNever.length; i++) {
    const value = valuesNotNever[i];
    if (predicate(value)) {
      return value;
    }
  }
  return;
}
__name(find5, "find");
function forEach5(record, run) {
  Object.entries(record).forEach(([key, value]) => run(value, key));
}
__name(forEach5, "forEach");
function includes5(arr, value) {
  return arr.indexOf(value) !== -1;
}
__name(includes5, "includes");
function findArr5(record, predicate) {
  for (let i = 0; i < record.length; i++) {
    const value = record[i];
    if (predicate(value)) {
      return value;
    }
  }
  return;
}
__name(findArr5, "findArr");
var CustomTransformerRegistry5 = class {
  static {
    __name(this, "CustomTransformerRegistry");
  }
  constructor() {
    this.transfomers = {};
  }
  register(transformer) {
    this.transfomers[transformer.name] = transformer;
  }
  findApplicable(v) {
    return find5(this.transfomers, (transformer) => transformer.isApplicable(v));
  }
  findByName(name) {
    return this.transfomers[name];
  }
};
var getType6 = /* @__PURE__ */ __name((payload) => Object.prototype.toString.call(payload).slice(8, -1), "getType");
var isUndefined5 = /* @__PURE__ */ __name((payload) => typeof payload === "undefined", "isUndefined");
var isNull6 = /* @__PURE__ */ __name((payload) => payload === null, "isNull");
var isPlainObject6 = /* @__PURE__ */ __name((payload) => {
  if (typeof payload !== "object" || payload === null)
    return false;
  if (payload === Object.prototype)
    return false;
  if (Object.getPrototypeOf(payload) === null)
    return true;
  return Object.getPrototypeOf(payload) === Object.prototype;
}, "isPlainObject");
var isEmptyObject5 = /* @__PURE__ */ __name((payload) => isPlainObject6(payload) && Object.keys(payload).length === 0, "isEmptyObject");
var isArray6 = /* @__PURE__ */ __name((payload) => Array.isArray(payload), "isArray");
var isString5 = /* @__PURE__ */ __name((payload) => typeof payload === "string", "isString");
var isNumber5 = /* @__PURE__ */ __name((payload) => typeof payload === "number" && !isNaN(payload), "isNumber");
var isBoolean5 = /* @__PURE__ */ __name((payload) => typeof payload === "boolean", "isBoolean");
var isRegExp5 = /* @__PURE__ */ __name((payload) => payload instanceof RegExp, "isRegExp");
var isMap5 = /* @__PURE__ */ __name((payload) => payload instanceof Map, "isMap");
var isSet5 = /* @__PURE__ */ __name((payload) => payload instanceof Set, "isSet");
var isSymbol5 = /* @__PURE__ */ __name((payload) => getType6(payload) === "Symbol", "isSymbol");
var isDate5 = /* @__PURE__ */ __name((payload) => payload instanceof Date && !isNaN(payload.valueOf()), "isDate");
var isError5 = /* @__PURE__ */ __name((payload) => payload instanceof Error, "isError");
var isNaNValue5 = /* @__PURE__ */ __name((payload) => typeof payload === "number" && isNaN(payload), "isNaNValue");
var isPrimitive5 = /* @__PURE__ */ __name((payload) => isBoolean5(payload) || isNull6(payload) || isUndefined5(payload) || isNumber5(payload) || isString5(payload) || isSymbol5(payload), "isPrimitive");
var isBigint5 = /* @__PURE__ */ __name((payload) => typeof payload === "bigint", "isBigint");
var isInfinite5 = /* @__PURE__ */ __name((payload) => payload === Infinity || payload === -Infinity, "isInfinite");
var isTypedArray5 = /* @__PURE__ */ __name((payload) => ArrayBuffer.isView(payload) && !(payload instanceof DataView), "isTypedArray");
var isURL5 = /* @__PURE__ */ __name((payload) => payload instanceof URL, "isURL");
var escapeKey5 = /* @__PURE__ */ __name((key) => key.replace(/\\/g, "\\\\").replace(/\./g, "\\."), "escapeKey");
var stringifyPath5 = /* @__PURE__ */ __name((path) => path.map(String).map(escapeKey5).join("."), "stringifyPath");
var parsePath5 = /* @__PURE__ */ __name((string2, legacyPaths) => {
  const result = [];
  let segment = "";
  for (let i = 0; i < string2.length; i++) {
    let char = string2.charAt(i);
    if (!legacyPaths && char === "\\") {
      const escaped = string2.charAt(i + 1);
      if (escaped === "\\") {
        segment += "\\";
        i++;
        continue;
      } else if (escaped !== ".") {
        throw Error("invalid path");
      }
    }
    const isEscapedDot = char === "\\" && string2.charAt(i + 1) === ".";
    if (isEscapedDot) {
      segment += ".";
      i++;
      continue;
    }
    const isEndOfSegment = char === ".";
    if (isEndOfSegment) {
      result.push(segment);
      segment = "";
      continue;
    }
    segment += char;
  }
  const lastSegment = segment;
  result.push(lastSegment);
  return result;
}, "parsePath");
function simpleTransformation5(isApplicable, annotation, transform, untransform) {
  return {
    isApplicable,
    annotation,
    transform,
    untransform
  };
}
__name(simpleTransformation5, "simpleTransformation");
var simpleRules5 = [
  simpleTransformation5(isUndefined5, "undefined", () => null, () => {
    return;
  }),
  simpleTransformation5(isBigint5, "bigint", (v) => v.toString(), (v) => {
    if (typeof BigInt !== "undefined") {
      return BigInt(v);
    }
    console.error("Please add a BigInt polyfill.");
    return v;
  }),
  simpleTransformation5(isDate5, "Date", (v) => v.toISOString(), (v) => new Date(v)),
  simpleTransformation5(isError5, "Error", (v, superJson) => {
    const baseError = {
      name: v.name,
      message: v.message
    };
    if ("cause" in v) {
      baseError.cause = v.cause;
    }
    superJson.allowedErrorProps.forEach((prop) => {
      baseError[prop] = v[prop];
    });
    return baseError;
  }, (v, superJson) => {
    const e = new Error(v.message, { cause: v.cause });
    e.name = v.name;
    e.stack = v.stack;
    superJson.allowedErrorProps.forEach((prop) => {
      e[prop] = v[prop];
    });
    return e;
  }),
  simpleTransformation5(isRegExp5, "regexp", (v) => "" + v, (regex) => {
    const body = regex.slice(1, regex.lastIndexOf("/"));
    const flags2 = regex.slice(regex.lastIndexOf("/") + 1);
    return new RegExp(body, flags2);
  }),
  simpleTransformation5(isSet5, "set", (v) => [...v.values()], (v) => new Set(v)),
  simpleTransformation5(isMap5, "map", (v) => [...v.entries()], (v) => new Map(v)),
  simpleTransformation5((v) => isNaNValue5(v) || isInfinite5(v), "number", (v) => {
    if (isNaNValue5(v)) {
      return "NaN";
    }
    if (v > 0) {
      return "Infinity";
    } else {
      return "-Infinity";
    }
  }, Number),
  simpleTransformation5((v) => v === 0 && 1 / v === -Infinity, "number", () => {
    return "-0";
  }, Number),
  simpleTransformation5(isURL5, "URL", (v) => v.toString(), (v) => new URL(v))
];
function compositeTransformation5(isApplicable, annotation, transform, untransform) {
  return {
    isApplicable,
    annotation,
    transform,
    untransform
  };
}
__name(compositeTransformation5, "compositeTransformation");
var symbolRule5 = compositeTransformation5((s, superJson) => {
  if (isSymbol5(s)) {
    const isRegistered = !!superJson.symbolRegistry.getIdentifier(s);
    return isRegistered;
  }
  return false;
}, (s, superJson) => {
  const identifier = superJson.symbolRegistry.getIdentifier(s);
  return ["symbol", identifier];
}, (v) => v.description, (_, a, superJson) => {
  const value = superJson.symbolRegistry.getValue(a[1]);
  if (!value) {
    throw new Error("Trying to deserialize unknown symbol");
  }
  return value;
});
var constructorToName5 = [
  Int8Array,
  Uint8Array,
  Int16Array,
  Uint16Array,
  Int32Array,
  Uint32Array,
  Float32Array,
  Float64Array,
  Uint8ClampedArray
].reduce((obj, ctor) => {
  obj[ctor.name] = ctor;
  return obj;
}, {});
var typedArrayRule5 = compositeTransformation5(isTypedArray5, (v) => ["typed-array", v.constructor.name], (v) => [...v], (v, a) => {
  const ctor = constructorToName5[a[1]];
  if (!ctor) {
    throw new Error("Trying to deserialize unknown typed array");
  }
  return new ctor(v);
});
function isInstanceOfRegisteredClass5(potentialClass, superJson) {
  if (potentialClass?.constructor) {
    const isRegistered = !!superJson.classRegistry.getIdentifier(potentialClass.constructor);
    return isRegistered;
  }
  return false;
}
__name(isInstanceOfRegisteredClass5, "isInstanceOfRegisteredClass");
var classRule5 = compositeTransformation5(isInstanceOfRegisteredClass5, (clazz, superJson) => {
  const identifier = superJson.classRegistry.getIdentifier(clazz.constructor);
  return ["class", identifier];
}, (clazz, superJson) => {
  const allowedProps = superJson.classRegistry.getAllowedProps(clazz.constructor);
  if (!allowedProps) {
    return { ...clazz };
  }
  const result = {};
  allowedProps.forEach((prop) => {
    result[prop] = clazz[prop];
  });
  return result;
}, (v, a, superJson) => {
  const clazz = superJson.classRegistry.getValue(a[1]);
  if (!clazz) {
    throw new Error(`Trying to deserialize unknown class '${a[1]}' - check https://github.com/blitz-js/superjson/issues/116#issuecomment-773996564`);
  }
  return Object.assign(Object.create(clazz.prototype), v);
});
var customRule5 = compositeTransformation5((value, superJson) => {
  return !!superJson.customTransformerRegistry.findApplicable(value);
}, (value, superJson) => {
  const transformer = superJson.customTransformerRegistry.findApplicable(value);
  return ["custom", transformer.name];
}, (value, superJson) => {
  const transformer = superJson.customTransformerRegistry.findApplicable(value);
  return transformer.serialize(value);
}, (v, a, superJson) => {
  const transformer = superJson.customTransformerRegistry.findByName(a[1]);
  if (!transformer) {
    throw new Error("Trying to deserialize unknown custom value");
  }
  return transformer.deserialize(v);
});
var compositeRules5 = [classRule5, symbolRule5, customRule5, typedArrayRule5];
var transformValue5 = /* @__PURE__ */ __name((value, superJson) => {
  const applicableCompositeRule = findArr5(compositeRules5, (rule) => rule.isApplicable(value, superJson));
  if (applicableCompositeRule) {
    return {
      value: applicableCompositeRule.transform(value, superJson),
      type: applicableCompositeRule.annotation(value, superJson)
    };
  }
  const applicableSimpleRule = findArr5(simpleRules5, (rule) => rule.isApplicable(value, superJson));
  if (applicableSimpleRule) {
    return {
      value: applicableSimpleRule.transform(value, superJson),
      type: applicableSimpleRule.annotation
    };
  }
  return;
}, "transformValue");
var simpleRulesByAnnotation5 = {};
simpleRules5.forEach((rule) => {
  simpleRulesByAnnotation5[rule.annotation] = rule;
});
var untransformValue5 = /* @__PURE__ */ __name((json, type, superJson) => {
  if (isArray6(type)) {
    switch (type[0]) {
      case "symbol":
        return symbolRule5.untransform(json, type, superJson);
      case "class":
        return classRule5.untransform(json, type, superJson);
      case "custom":
        return customRule5.untransform(json, type, superJson);
      case "typed-array":
        return typedArrayRule5.untransform(json, type, superJson);
      default:
        throw new Error("Unknown transformation: " + type);
    }
  } else {
    const transformation = simpleRulesByAnnotation5[type];
    if (!transformation) {
      throw new Error("Unknown transformation: " + type);
    }
    return transformation.untransform(json, superJson);
  }
}, "untransformValue");
var getNthKey5 = /* @__PURE__ */ __name((value, n) => {
  if (n > value.size)
    throw new Error("index out of bounds");
  const keys = value.keys();
  while (n > 0) {
    keys.next();
    n--;
  }
  return keys.next().value;
}, "getNthKey");
function validatePath5(path) {
  if (includes5(path, "__proto__")) {
    throw new Error("__proto__ is not allowed as a property");
  }
  if (includes5(path, "prototype")) {
    throw new Error("prototype is not allowed as a property");
  }
  if (includes5(path, "constructor")) {
    throw new Error("constructor is not allowed as a property");
  }
}
__name(validatePath5, "validatePath");
var getDeep5 = /* @__PURE__ */ __name((object2, path) => {
  validatePath5(path);
  for (let i = 0; i < path.length; i++) {
    const key = path[i];
    if (isSet5(object2)) {
      object2 = getNthKey5(object2, +key);
    } else if (isMap5(object2)) {
      const row = +key;
      const type = +path[++i] === 0 ? "key" : "value";
      const keyOfRow = getNthKey5(object2, row);
      switch (type) {
        case "key":
          object2 = keyOfRow;
          break;
        case "value":
          object2 = object2.get(keyOfRow);
          break;
      }
    } else {
      object2 = object2[key];
    }
  }
  return object2;
}, "getDeep");
var setDeep5 = /* @__PURE__ */ __name((object2, path, mapper) => {
  validatePath5(path);
  if (path.length === 0) {
    return mapper(object2);
  }
  let parent = object2;
  for (let i = 0; i < path.length - 1; i++) {
    const key = path[i];
    if (isArray6(parent)) {
      const index = +key;
      parent = parent[index];
    } else if (isPlainObject6(parent)) {
      parent = parent[key];
    } else if (isSet5(parent)) {
      const row = +key;
      parent = getNthKey5(parent, row);
    } else if (isMap5(parent)) {
      const isEnd = i === path.length - 2;
      if (isEnd) {
        break;
      }
      const row = +key;
      const type = +path[++i] === 0 ? "key" : "value";
      const keyOfRow = getNthKey5(parent, row);
      switch (type) {
        case "key":
          parent = keyOfRow;
          break;
        case "value":
          parent = parent.get(keyOfRow);
          break;
      }
    }
  }
  const lastKey = path[path.length - 1];
  if (isArray6(parent)) {
    parent[+lastKey] = mapper(parent[+lastKey]);
  } else if (isPlainObject6(parent)) {
    parent[lastKey] = mapper(parent[lastKey]);
  }
  if (isSet5(parent)) {
    const oldValue = getNthKey5(parent, +lastKey);
    const newValue = mapper(oldValue);
    if (oldValue !== newValue) {
      parent.delete(oldValue);
      parent.add(newValue);
    }
  }
  if (isMap5(parent)) {
    const row = +path[path.length - 2];
    const keyToRow = getNthKey5(parent, row);
    const type = +lastKey === 0 ? "key" : "value";
    switch (type) {
      case "key": {
        const newKey = mapper(keyToRow);
        parent.set(newKey, parent.get(keyToRow));
        if (newKey !== keyToRow) {
          parent.delete(keyToRow);
        }
        break;
      }
      case "value": {
        parent.set(keyToRow, mapper(parent.get(keyToRow)));
        break;
      }
    }
  }
  return object2;
}, "setDeep");
var enableLegacyPaths5 = /* @__PURE__ */ __name((version4) => version4 < 1, "enableLegacyPaths");
function traverse5(tree, walker26, version4, origin = []) {
  if (!tree) {
    return;
  }
  const legacyPaths = enableLegacyPaths5(version4);
  if (!isArray6(tree)) {
    forEach5(tree, (subtree, key) => traverse5(subtree, walker26, version4, [
      ...origin,
      ...parsePath5(key, legacyPaths)
    ]));
    return;
  }
  const [nodeValue, children] = tree;
  if (children) {
    forEach5(children, (child, key) => {
      traverse5(child, walker26, version4, [
        ...origin,
        ...parsePath5(key, legacyPaths)
      ]);
    });
  }
  walker26(nodeValue, origin);
}
__name(traverse5, "traverse");
function applyValueAnnotations5(plain, annotations, version4, superJson) {
  traverse5(annotations, (type, path) => {
    plain = setDeep5(plain, path, (v) => untransformValue5(v, type, superJson));
  }, version4);
  return plain;
}
__name(applyValueAnnotations5, "applyValueAnnotations");
function applyReferentialEqualityAnnotations5(plain, annotations, version4) {
  const legacyPaths = enableLegacyPaths5(version4);
  function apply(identicalPaths, path) {
    const object2 = getDeep5(plain, parsePath5(path, legacyPaths));
    identicalPaths.map((path2) => parsePath5(path2, legacyPaths)).forEach((identicalObjectPath) => {
      plain = setDeep5(plain, identicalObjectPath, () => object2);
    });
  }
  __name(apply, "apply");
  if (isArray6(annotations)) {
    const [root, other] = annotations;
    root.forEach((identicalPath) => {
      plain = setDeep5(plain, parsePath5(identicalPath, legacyPaths), () => plain);
    });
    if (other) {
      forEach5(other, apply);
    }
  } else {
    forEach5(annotations, apply);
  }
  return plain;
}
__name(applyReferentialEqualityAnnotations5, "applyReferentialEqualityAnnotations");
var isDeep5 = /* @__PURE__ */ __name((object2, superJson) => isPlainObject6(object2) || isArray6(object2) || isMap5(object2) || isSet5(object2) || isError5(object2) || isInstanceOfRegisteredClass5(object2, superJson), "isDeep");
function addIdentity5(object2, path, identities) {
  const existingSet = identities.get(object2);
  if (existingSet) {
    existingSet.push(path);
  } else {
    identities.set(object2, [path]);
  }
}
__name(addIdentity5, "addIdentity");
function generateReferentialEqualityAnnotations5(identitites, dedupe) {
  const result = {};
  let rootEqualityPaths = void 0;
  identitites.forEach((paths) => {
    if (paths.length <= 1) {
      return;
    }
    if (!dedupe) {
      paths = paths.map((path) => path.map(String)).sort((a, b) => a.length - b.length);
    }
    const [representativePath, ...identicalPaths] = paths;
    if (representativePath.length === 0) {
      rootEqualityPaths = identicalPaths.map(stringifyPath5);
    } else {
      result[stringifyPath5(representativePath)] = identicalPaths.map(stringifyPath5);
    }
  });
  if (rootEqualityPaths) {
    if (isEmptyObject5(result)) {
      return [rootEqualityPaths];
    } else {
      return [rootEqualityPaths, result];
    }
  } else {
    return isEmptyObject5(result) ? void 0 : result;
  }
}
__name(generateReferentialEqualityAnnotations5, "generateReferentialEqualityAnnotations");
var walker5 = /* @__PURE__ */ __name((object2, identities, superJson, dedupe, path = [], objectsInThisPath = [], seenObjects = /* @__PURE__ */ new Map()) => {
  const primitive = isPrimitive5(object2);
  if (!primitive) {
    addIdentity5(object2, path, identities);
    const seen = seenObjects.get(object2);
    if (seen) {
      return dedupe ? {
        transformedValue: null
      } : seen;
    }
  }
  if (!isDeep5(object2, superJson)) {
    const transformed2 = transformValue5(object2, superJson);
    const result2 = transformed2 ? {
      transformedValue: transformed2.value,
      annotations: [transformed2.type]
    } : {
      transformedValue: object2
    };
    if (!primitive) {
      seenObjects.set(object2, result2);
    }
    return result2;
  }
  if (includes5(objectsInThisPath, object2)) {
    return {
      transformedValue: null
    };
  }
  const transformationResult = transformValue5(object2, superJson);
  const transformed = transformationResult?.value ?? object2;
  const transformedValue = isArray6(transformed) ? [] : {};
  const innerAnnotations = {};
  forEach5(transformed, (value, index) => {
    if (index === "__proto__" || index === "constructor" || index === "prototype") {
      throw new Error(`Detected property ${index}. This is a prototype pollution risk, please remove it from your object.`);
    }
    const recursiveResult = walker5(value, identities, superJson, dedupe, [...path, index], [...objectsInThisPath, object2], seenObjects);
    transformedValue[index] = recursiveResult.transformedValue;
    if (isArray6(recursiveResult.annotations)) {
      innerAnnotations[escapeKey5(index)] = recursiveResult.annotations;
    } else if (isPlainObject6(recursiveResult.annotations)) {
      forEach5(recursiveResult.annotations, (tree, key) => {
        innerAnnotations[escapeKey5(index) + "." + key] = tree;
      });
    }
  });
  const result = isEmptyObject5(innerAnnotations) ? {
    transformedValue,
    annotations: transformationResult ? [transformationResult.type] : void 0
  } : {
    transformedValue,
    annotations: transformationResult ? [transformationResult.type, innerAnnotations] : innerAnnotations
  };
  if (!primitive) {
    seenObjects.set(object2, result);
  }
  return result;
}, "walker");
function getType25(payload) {
  return Object.prototype.toString.call(payload).slice(8, -1);
}
__name(getType25, "getType2");
function isArray25(payload) {
  return getType25(payload) === "Array";
}
__name(isArray25, "isArray2");
function isPlainObject25(payload) {
  if (getType25(payload) !== "Object")
    return false;
  const prototype = Object.getPrototypeOf(payload);
  return !!prototype && prototype.constructor === Object && prototype === Object.prototype;
}
__name(isPlainObject25, "isPlainObject2");
function assignProp5(carry, key, newVal, originalObject, includeNonenumerable) {
  const propType = {}.propertyIsEnumerable.call(originalObject, key) ? "enumerable" : "nonenumerable";
  if (propType === "enumerable")
    carry[key] = newVal;
  if (includeNonenumerable && propType === "nonenumerable") {
    Object.defineProperty(carry, key, {
      value: newVal,
      enumerable: false,
      writable: true,
      configurable: true
    });
  }
}
__name(assignProp5, "assignProp");
function copy5(target, options = {}) {
  if (isArray25(target)) {
    return target.map((item) => copy5(item, options));
  }
  if (!isPlainObject25(target)) {
    return target;
  }
  const props = Object.getOwnPropertyNames(target);
  const symbols = Object.getOwnPropertySymbols(target);
  return [...props, ...symbols].reduce((carry, key) => {
    if (key === "__proto__")
      return carry;
    if (isArray25(options.props) && !options.props.includes(key)) {
      return carry;
    }
    const val = target[key];
    const newVal = copy5(val, options);
    assignProp5(carry, key, newVal, target, options.nonenumerable);
    return carry;
  }, {});
}
__name(copy5, "copy");
var SuperJSON5 = class {
  static {
    __name(this, "SuperJSON");
  }
  constructor({ dedupe = false } = {}) {
    this.classRegistry = new ClassRegistry5();
    this.symbolRegistry = new Registry5((s) => s.description ?? "");
    this.customTransformerRegistry = new CustomTransformerRegistry5();
    this.allowedErrorProps = [];
    this.dedupe = dedupe;
  }
  serialize(object2) {
    const identities = /* @__PURE__ */ new Map();
    const output = walker5(object2, identities, this, this.dedupe);
    const res = {
      json: output.transformedValue
    };
    if (output.annotations) {
      res.meta = {
        ...res.meta,
        values: output.annotations
      };
    }
    const equalityAnnotations = generateReferentialEqualityAnnotations5(identities, this.dedupe);
    if (equalityAnnotations) {
      res.meta = {
        ...res.meta,
        referentialEqualities: equalityAnnotations
      };
    }
    if (res.meta)
      res.meta.v = 1;
    return res;
  }
  deserialize(payload, options) {
    const { json, meta } = payload;
    let result = options?.inPlace ? json : copy5(json);
    if (meta?.values) {
      result = applyValueAnnotations5(result, meta.values, meta.v ?? 0, this);
    }
    if (meta?.referentialEqualities) {
      result = applyReferentialEqualityAnnotations5(result, meta.referentialEqualities, meta.v ?? 0);
    }
    return result;
  }
  stringify(object2) {
    return JSON.stringify(this.serialize(object2));
  }
  parse(string2) {
    return this.deserialize(JSON.parse(string2), { inPlace: true });
  }
  registerClass(v, options) {
    this.classRegistry.register(v, options);
  }
  registerSymbol(v, identifier) {
    this.symbolRegistry.register(v, identifier);
  }
  registerCustom(transformer, name) {
    this.customTransformerRegistry.register({
      name,
      ...transformer
    });
  }
  allowErrorProps(...props) {
    this.allowedErrorProps.push(...props);
  }
};
SuperJSON5.defaultInstance = new SuperJSON5();
SuperJSON5.serialize = SuperJSON5.defaultInstance.serialize.bind(SuperJSON5.defaultInstance);
SuperJSON5.deserialize = SuperJSON5.defaultInstance.deserialize.bind(SuperJSON5.defaultInstance);
SuperJSON5.stringify = SuperJSON5.defaultInstance.stringify.bind(SuperJSON5.defaultInstance);
SuperJSON5.parse = SuperJSON5.defaultInstance.parse.bind(SuperJSON5.defaultInstance);
SuperJSON5.registerClass = SuperJSON5.defaultInstance.registerClass.bind(SuperJSON5.defaultInstance);
SuperJSON5.registerSymbol = SuperJSON5.defaultInstance.registerSymbol.bind(SuperJSON5.defaultInstance);
SuperJSON5.registerCustom = SuperJSON5.defaultInstance.registerCustom.bind(SuperJSON5.defaultInstance);
SuperJSON5.allowErrorProps = SuperJSON5.defaultInstance.allowErrorProps.bind(SuperJSON5.defaultInstance);
var dist_default5 = SuperJSON5;
var serialize5 = SuperJSON5.serialize;
var deserialize5 = SuperJSON5.deserialize;
var stringify5 = SuperJSON5.stringify;
var parse5 = SuperJSON5.parse;
var registerClass5 = SuperJSON5.registerClass;
var registerCustom5 = SuperJSON5.registerCustom;
var registerSymbol5 = SuperJSON5.registerSymbol;
var allowErrorProps5 = SuperJSON5.allowErrorProps;
function parseData5(formData) {
  const propsArray = [];
  if (!formData)
    return propsArray;
  const batchsIDs = [];
  for (const [key, value] of Array.from(formData.entries())) {
    if (key.startsWith("FILE_"))
      propsArray.push(value);
    else if (key.startsWith("FILES_")) {
      if (batchsIDs.includes(key))
        continue;
      batchsIDs.push(key);
      propsArray.push(formData.getAll(key));
    } else {
      propsArray.push(JSON.parse(decodeURI(value)));
    }
  }
  return propsArray;
}
__name(parseData5, "parseData");
function paramsFromURL5(url) {
  const params = url.searchParams.entries().toArray().map(([_, v]) => v);
  return params.map((param) => JSON.parse(decodeURIComponent(param)));
}
__name(paramsFromURL5, "paramsFromURL");
async function WrapRequestHandler5(context2, endpoint) {
  const isServerAction = context2.request.headers.get("x-server-action") === "true";
  if (!isServerAction) {
    return new Response("Not Found", { status: 404 });
  }
  const parsedData = context2.request.method === "GET" || context2.request.method === "HEAD" ? paramsFromURL5(new URL(context2.request.url)) : parseData5(context2.request.headers.get("content-type") ? await context2.request.formData() : void 0);
  const missingProps = endpoint.length - parsedData.length;
  for (let i = 0; i < missingProps; i++) {
    parsedData.push(void 0);
  }
  parsedData.push(context2);
  const result = await endpoint(...parsedData);
  switch (typeof result) {
    case "string":
    case "number":
    case "boolean":
    case "bigint":
      return new Response(dist_default5.stringify(result), {
        headers: {
          "Content-Type": "application/json",
          dataType: "json"
        }
      });
    case "undefined":
      return new Response(null, { status: 204 });
    case "object":
      if (result instanceof Response) {
        result.headers.set("dataType", "response");
        return result;
      } else if (result instanceof Blob) {
        const res = new Response(await result.arrayBuffer());
        res.headers.set("dataType", "blob");
        res.headers.set("Content-Type", result.type);
        return res;
      } else if (result instanceof File) {
        return new Response(await result.arrayBuffer(), {
          headers: {
            "Content-Type": result.type,
            dataType: "file",
            fileData: JSON.stringify({
              name: result.name,
              lastModified: result.lastModified
            })
          }
        });
      } else {
        return new Response(dist_default5.stringify(result), {
          headers: { "Content-Type": "application/json", dataType: "json" }
        });
      }
    default:
      throw new Error(`Unsupported return type from action: ${typeof result}`);
  }
}
__name(WrapRequestHandler5, "WrapRequestHandler");
var onRequest5 = /* @__PURE__ */ __name(async (context2) => {
  const method = context2.request.method;
  const options = {
    GET: typeof GET === "function" ? GET : void 0,
    POST: typeof POST6 === "function" ? POST6 : void 0,
    PUT: typeof PUT === "function" ? PUT : void 0,
    DELETE: typeof DELETE === "function" ? DELETE : void 0,
    PATCH: typeof PATCH === "function" ? PATCH : void 0,
    HEAD: typeof HEAD === "function" ? HEAD : void 0,
    OPTIONS: typeof OPTIONS === "function" ? OPTIONS : void 0
  };
  if (!options[method]) {
    return new Response(`Method "${method}" Not Allowed`, {
      status: 405
    });
  }
  return await WrapRequestHandler5(context2, options[method]);
}, "onRequest");

// api/invites/template/ui/manage.js
async function POST7({
  user_id,
  theme_id,
  from_name
}) {
  const ctx = getContext(arguments);
  const current_user_id = (await ctx.data.client.getMetaData())?.id;
  if (!current_user_id) {
    return {
      success: false,
      error: "Unauthorized"
    };
  } else if (current_user_id === user_id) {
    return {
      success: false,
      error: "You cannot invite yourself"
    };
  }
  const db = drizzle(ctx.env.PROJECT_DB);
  const theme = await db.select().from(uiStyleTable).where(and(eq(uiStyleTable.id, theme_id), eq(uiStyleTable.owner_id, current_user_id))).get();
  if (!theme) {
    return {
      success: false,
      error: "Theme not found or you don't have permission to invite users"
    };
  }
  const inviteManager = invites(ctx.env.PROJECT_DB);
  const existingInvite = await inviteManager.exists({
    type: "ui_template",
    user_id,
    owner_group_id: theme.owner_group_id,
    from_id: current_user_id
  });
  const create = /* @__PURE__ */ __name(() => {
    return inviteManager.create({
      type: "ui_template",
      owner_group_id: theme.owner_group_id,
      label: `Invite to UI Theme ${theme.name}`,
      from: {
        id: current_user_id,
        name: from_name
      },
      invite_user_id: user_id
    });
  }, "create");
  if (existingInvite) {
    await inviteManager.deleteById(existingInvite.id);
    const newInvite = await create();
    return {
      success: newInvite.success,
      error: newInvite.error
    };
  }
  const res = await create();
  return {
    success: res.success,
    error: res.error
  };
}
__name(POST7, "POST");
async function DELETE4({
  user_id,
  owner_group_id
}) {
  const ctx = getContext(arguments);
  const current_user_id = (await ctx.data.client.getMetaData())?.id;
  if (!current_user_id) {
    return {
      success: false,
      error: "Unauthorized"
    };
  } else if (current_user_id === user_id) {
    return {
      success: false,
      error: "You cannot revoke your own invite"
    };
  }
  const inviteManager = invites(ctx.env.PROJECT_DB);
  const existingInvite = await inviteManager.exists({
    type: "ui_template",
    user_id,
    owner_group_id,
    from_id: current_user_id
  });
  if (existingInvite) {
    await inviteManager.deleteById(existingInvite.id);
  } else {
    return {
      success: false,
      error: "Invite not found"
    };
  }
  return {
    success: true
  };
}
__name(DELETE4, "DELETE");
var DoubleIndexedKV6 = class {
  static {
    __name(this, "DoubleIndexedKV");
  }
  constructor() {
    this.keyToValue = /* @__PURE__ */ new Map();
    this.valueToKey = /* @__PURE__ */ new Map();
  }
  set(key, value) {
    this.keyToValue.set(key, value);
    this.valueToKey.set(value, key);
  }
  getByKey(key) {
    return this.keyToValue.get(key);
  }
  getByValue(value) {
    return this.valueToKey.get(value);
  }
  clear() {
    this.keyToValue.clear();
    this.valueToKey.clear();
  }
};
var Registry6 = class {
  static {
    __name(this, "Registry");
  }
  constructor(generateIdentifier) {
    this.generateIdentifier = generateIdentifier;
    this.kv = new DoubleIndexedKV6();
  }
  register(value, identifier) {
    if (this.kv.getByValue(value)) {
      return;
    }
    if (!identifier) {
      identifier = this.generateIdentifier(value);
    }
    this.kv.set(identifier, value);
  }
  clear() {
    this.kv.clear();
  }
  getIdentifier(value) {
    return this.kv.getByValue(value);
  }
  getValue(identifier) {
    return this.kv.getByKey(identifier);
  }
};
var ClassRegistry6 = class extends Registry6 {
  static {
    __name(this, "ClassRegistry");
  }
  constructor() {
    super((c) => c.name);
    this.classToAllowedProps = /* @__PURE__ */ new Map();
  }
  register(value, options) {
    if (typeof options === "object") {
      if (options.allowProps) {
        this.classToAllowedProps.set(value, options.allowProps);
      }
      super.register(value, options.identifier);
    } else {
      super.register(value, options);
    }
  }
  getAllowedProps(value) {
    return this.classToAllowedProps.get(value);
  }
};
function valuesOfObj6(record) {
  if ("values" in Object) {
    return Object.values(record);
  }
  const values = [];
  for (const key in record) {
    if (record.hasOwnProperty(key)) {
      values.push(record[key]);
    }
  }
  return values;
}
__name(valuesOfObj6, "valuesOfObj");
function find6(record, predicate) {
  const values = valuesOfObj6(record);
  if ("find" in values) {
    return values.find(predicate);
  }
  const valuesNotNever = values;
  for (let i = 0; i < valuesNotNever.length; i++) {
    const value = valuesNotNever[i];
    if (predicate(value)) {
      return value;
    }
  }
  return;
}
__name(find6, "find");
function forEach6(record, run) {
  Object.entries(record).forEach(([key, value]) => run(value, key));
}
__name(forEach6, "forEach");
function includes6(arr, value) {
  return arr.indexOf(value) !== -1;
}
__name(includes6, "includes");
function findArr6(record, predicate) {
  for (let i = 0; i < record.length; i++) {
    const value = record[i];
    if (predicate(value)) {
      return value;
    }
  }
  return;
}
__name(findArr6, "findArr");
var CustomTransformerRegistry6 = class {
  static {
    __name(this, "CustomTransformerRegistry");
  }
  constructor() {
    this.transfomers = {};
  }
  register(transformer) {
    this.transfomers[transformer.name] = transformer;
  }
  findApplicable(v) {
    return find6(this.transfomers, (transformer) => transformer.isApplicable(v));
  }
  findByName(name) {
    return this.transfomers[name];
  }
};
var getType7 = /* @__PURE__ */ __name((payload) => Object.prototype.toString.call(payload).slice(8, -1), "getType");
var isUndefined6 = /* @__PURE__ */ __name((payload) => typeof payload === "undefined", "isUndefined");
var isNull7 = /* @__PURE__ */ __name((payload) => payload === null, "isNull");
var isPlainObject7 = /* @__PURE__ */ __name((payload) => {
  if (typeof payload !== "object" || payload === null)
    return false;
  if (payload === Object.prototype)
    return false;
  if (Object.getPrototypeOf(payload) === null)
    return true;
  return Object.getPrototypeOf(payload) === Object.prototype;
}, "isPlainObject");
var isEmptyObject6 = /* @__PURE__ */ __name((payload) => isPlainObject7(payload) && Object.keys(payload).length === 0, "isEmptyObject");
var isArray7 = /* @__PURE__ */ __name((payload) => Array.isArray(payload), "isArray");
var isString6 = /* @__PURE__ */ __name((payload) => typeof payload === "string", "isString");
var isNumber6 = /* @__PURE__ */ __name((payload) => typeof payload === "number" && !isNaN(payload), "isNumber");
var isBoolean6 = /* @__PURE__ */ __name((payload) => typeof payload === "boolean", "isBoolean");
var isRegExp6 = /* @__PURE__ */ __name((payload) => payload instanceof RegExp, "isRegExp");
var isMap6 = /* @__PURE__ */ __name((payload) => payload instanceof Map, "isMap");
var isSet6 = /* @__PURE__ */ __name((payload) => payload instanceof Set, "isSet");
var isSymbol6 = /* @__PURE__ */ __name((payload) => getType7(payload) === "Symbol", "isSymbol");
var isDate6 = /* @__PURE__ */ __name((payload) => payload instanceof Date && !isNaN(payload.valueOf()), "isDate");
var isError6 = /* @__PURE__ */ __name((payload) => payload instanceof Error, "isError");
var isNaNValue6 = /* @__PURE__ */ __name((payload) => typeof payload === "number" && isNaN(payload), "isNaNValue");
var isPrimitive6 = /* @__PURE__ */ __name((payload) => isBoolean6(payload) || isNull7(payload) || isUndefined6(payload) || isNumber6(payload) || isString6(payload) || isSymbol6(payload), "isPrimitive");
var isBigint6 = /* @__PURE__ */ __name((payload) => typeof payload === "bigint", "isBigint");
var isInfinite6 = /* @__PURE__ */ __name((payload) => payload === Infinity || payload === -Infinity, "isInfinite");
var isTypedArray6 = /* @__PURE__ */ __name((payload) => ArrayBuffer.isView(payload) && !(payload instanceof DataView), "isTypedArray");
var isURL6 = /* @__PURE__ */ __name((payload) => payload instanceof URL, "isURL");
var escapeKey6 = /* @__PURE__ */ __name((key) => key.replace(/\\/g, "\\\\").replace(/\./g, "\\."), "escapeKey");
var stringifyPath6 = /* @__PURE__ */ __name((path) => path.map(String).map(escapeKey6).join("."), "stringifyPath");
var parsePath6 = /* @__PURE__ */ __name((string2, legacyPaths) => {
  const result = [];
  let segment = "";
  for (let i = 0; i < string2.length; i++) {
    let char = string2.charAt(i);
    if (!legacyPaths && char === "\\") {
      const escaped = string2.charAt(i + 1);
      if (escaped === "\\") {
        segment += "\\";
        i++;
        continue;
      } else if (escaped !== ".") {
        throw Error("invalid path");
      }
    }
    const isEscapedDot = char === "\\" && string2.charAt(i + 1) === ".";
    if (isEscapedDot) {
      segment += ".";
      i++;
      continue;
    }
    const isEndOfSegment = char === ".";
    if (isEndOfSegment) {
      result.push(segment);
      segment = "";
      continue;
    }
    segment += char;
  }
  const lastSegment = segment;
  result.push(lastSegment);
  return result;
}, "parsePath");
function simpleTransformation6(isApplicable, annotation, transform, untransform) {
  return {
    isApplicable,
    annotation,
    transform,
    untransform
  };
}
__name(simpleTransformation6, "simpleTransformation");
var simpleRules6 = [
  simpleTransformation6(isUndefined6, "undefined", () => null, () => {
    return;
  }),
  simpleTransformation6(isBigint6, "bigint", (v) => v.toString(), (v) => {
    if (typeof BigInt !== "undefined") {
      return BigInt(v);
    }
    console.error("Please add a BigInt polyfill.");
    return v;
  }),
  simpleTransformation6(isDate6, "Date", (v) => v.toISOString(), (v) => new Date(v)),
  simpleTransformation6(isError6, "Error", (v, superJson) => {
    const baseError = {
      name: v.name,
      message: v.message
    };
    if ("cause" in v) {
      baseError.cause = v.cause;
    }
    superJson.allowedErrorProps.forEach((prop) => {
      baseError[prop] = v[prop];
    });
    return baseError;
  }, (v, superJson) => {
    const e = new Error(v.message, { cause: v.cause });
    e.name = v.name;
    e.stack = v.stack;
    superJson.allowedErrorProps.forEach((prop) => {
      e[prop] = v[prop];
    });
    return e;
  }),
  simpleTransformation6(isRegExp6, "regexp", (v) => "" + v, (regex) => {
    const body = regex.slice(1, regex.lastIndexOf("/"));
    const flags2 = regex.slice(regex.lastIndexOf("/") + 1);
    return new RegExp(body, flags2);
  }),
  simpleTransformation6(isSet6, "set", (v) => [...v.values()], (v) => new Set(v)),
  simpleTransformation6(isMap6, "map", (v) => [...v.entries()], (v) => new Map(v)),
  simpleTransformation6((v) => isNaNValue6(v) || isInfinite6(v), "number", (v) => {
    if (isNaNValue6(v)) {
      return "NaN";
    }
    if (v > 0) {
      return "Infinity";
    } else {
      return "-Infinity";
    }
  }, Number),
  simpleTransformation6((v) => v === 0 && 1 / v === -Infinity, "number", () => {
    return "-0";
  }, Number),
  simpleTransformation6(isURL6, "URL", (v) => v.toString(), (v) => new URL(v))
];
function compositeTransformation6(isApplicable, annotation, transform, untransform) {
  return {
    isApplicable,
    annotation,
    transform,
    untransform
  };
}
__name(compositeTransformation6, "compositeTransformation");
var symbolRule6 = compositeTransformation6((s, superJson) => {
  if (isSymbol6(s)) {
    const isRegistered = !!superJson.symbolRegistry.getIdentifier(s);
    return isRegistered;
  }
  return false;
}, (s, superJson) => {
  const identifier = superJson.symbolRegistry.getIdentifier(s);
  return ["symbol", identifier];
}, (v) => v.description, (_, a, superJson) => {
  const value = superJson.symbolRegistry.getValue(a[1]);
  if (!value) {
    throw new Error("Trying to deserialize unknown symbol");
  }
  return value;
});
var constructorToName6 = [
  Int8Array,
  Uint8Array,
  Int16Array,
  Uint16Array,
  Int32Array,
  Uint32Array,
  Float32Array,
  Float64Array,
  Uint8ClampedArray
].reduce((obj, ctor) => {
  obj[ctor.name] = ctor;
  return obj;
}, {});
var typedArrayRule6 = compositeTransformation6(isTypedArray6, (v) => ["typed-array", v.constructor.name], (v) => [...v], (v, a) => {
  const ctor = constructorToName6[a[1]];
  if (!ctor) {
    throw new Error("Trying to deserialize unknown typed array");
  }
  return new ctor(v);
});
function isInstanceOfRegisteredClass6(potentialClass, superJson) {
  if (potentialClass?.constructor) {
    const isRegistered = !!superJson.classRegistry.getIdentifier(potentialClass.constructor);
    return isRegistered;
  }
  return false;
}
__name(isInstanceOfRegisteredClass6, "isInstanceOfRegisteredClass");
var classRule6 = compositeTransformation6(isInstanceOfRegisteredClass6, (clazz, superJson) => {
  const identifier = superJson.classRegistry.getIdentifier(clazz.constructor);
  return ["class", identifier];
}, (clazz, superJson) => {
  const allowedProps = superJson.classRegistry.getAllowedProps(clazz.constructor);
  if (!allowedProps) {
    return { ...clazz };
  }
  const result = {};
  allowedProps.forEach((prop) => {
    result[prop] = clazz[prop];
  });
  return result;
}, (v, a, superJson) => {
  const clazz = superJson.classRegistry.getValue(a[1]);
  if (!clazz) {
    throw new Error(`Trying to deserialize unknown class '${a[1]}' - check https://github.com/blitz-js/superjson/issues/116#issuecomment-773996564`);
  }
  return Object.assign(Object.create(clazz.prototype), v);
});
var customRule6 = compositeTransformation6((value, superJson) => {
  return !!superJson.customTransformerRegistry.findApplicable(value);
}, (value, superJson) => {
  const transformer = superJson.customTransformerRegistry.findApplicable(value);
  return ["custom", transformer.name];
}, (value, superJson) => {
  const transformer = superJson.customTransformerRegistry.findApplicable(value);
  return transformer.serialize(value);
}, (v, a, superJson) => {
  const transformer = superJson.customTransformerRegistry.findByName(a[1]);
  if (!transformer) {
    throw new Error("Trying to deserialize unknown custom value");
  }
  return transformer.deserialize(v);
});
var compositeRules6 = [classRule6, symbolRule6, customRule6, typedArrayRule6];
var transformValue6 = /* @__PURE__ */ __name((value, superJson) => {
  const applicableCompositeRule = findArr6(compositeRules6, (rule) => rule.isApplicable(value, superJson));
  if (applicableCompositeRule) {
    return {
      value: applicableCompositeRule.transform(value, superJson),
      type: applicableCompositeRule.annotation(value, superJson)
    };
  }
  const applicableSimpleRule = findArr6(simpleRules6, (rule) => rule.isApplicable(value, superJson));
  if (applicableSimpleRule) {
    return {
      value: applicableSimpleRule.transform(value, superJson),
      type: applicableSimpleRule.annotation
    };
  }
  return;
}, "transformValue");
var simpleRulesByAnnotation6 = {};
simpleRules6.forEach((rule) => {
  simpleRulesByAnnotation6[rule.annotation] = rule;
});
var untransformValue6 = /* @__PURE__ */ __name((json, type, superJson) => {
  if (isArray7(type)) {
    switch (type[0]) {
      case "symbol":
        return symbolRule6.untransform(json, type, superJson);
      case "class":
        return classRule6.untransform(json, type, superJson);
      case "custom":
        return customRule6.untransform(json, type, superJson);
      case "typed-array":
        return typedArrayRule6.untransform(json, type, superJson);
      default:
        throw new Error("Unknown transformation: " + type);
    }
  } else {
    const transformation = simpleRulesByAnnotation6[type];
    if (!transformation) {
      throw new Error("Unknown transformation: " + type);
    }
    return transformation.untransform(json, superJson);
  }
}, "untransformValue");
var getNthKey6 = /* @__PURE__ */ __name((value, n) => {
  if (n > value.size)
    throw new Error("index out of bounds");
  const keys = value.keys();
  while (n > 0) {
    keys.next();
    n--;
  }
  return keys.next().value;
}, "getNthKey");
function validatePath6(path) {
  if (includes6(path, "__proto__")) {
    throw new Error("__proto__ is not allowed as a property");
  }
  if (includes6(path, "prototype")) {
    throw new Error("prototype is not allowed as a property");
  }
  if (includes6(path, "constructor")) {
    throw new Error("constructor is not allowed as a property");
  }
}
__name(validatePath6, "validatePath");
var getDeep6 = /* @__PURE__ */ __name((object2, path) => {
  validatePath6(path);
  for (let i = 0; i < path.length; i++) {
    const key = path[i];
    if (isSet6(object2)) {
      object2 = getNthKey6(object2, +key);
    } else if (isMap6(object2)) {
      const row = +key;
      const type = +path[++i] === 0 ? "key" : "value";
      const keyOfRow = getNthKey6(object2, row);
      switch (type) {
        case "key":
          object2 = keyOfRow;
          break;
        case "value":
          object2 = object2.get(keyOfRow);
          break;
      }
    } else {
      object2 = object2[key];
    }
  }
  return object2;
}, "getDeep");
var setDeep6 = /* @__PURE__ */ __name((object2, path, mapper) => {
  validatePath6(path);
  if (path.length === 0) {
    return mapper(object2);
  }
  let parent = object2;
  for (let i = 0; i < path.length - 1; i++) {
    const key = path[i];
    if (isArray7(parent)) {
      const index = +key;
      parent = parent[index];
    } else if (isPlainObject7(parent)) {
      parent = parent[key];
    } else if (isSet6(parent)) {
      const row = +key;
      parent = getNthKey6(parent, row);
    } else if (isMap6(parent)) {
      const isEnd = i === path.length - 2;
      if (isEnd) {
        break;
      }
      const row = +key;
      const type = +path[++i] === 0 ? "key" : "value";
      const keyOfRow = getNthKey6(parent, row);
      switch (type) {
        case "key":
          parent = keyOfRow;
          break;
        case "value":
          parent = parent.get(keyOfRow);
          break;
      }
    }
  }
  const lastKey = path[path.length - 1];
  if (isArray7(parent)) {
    parent[+lastKey] = mapper(parent[+lastKey]);
  } else if (isPlainObject7(parent)) {
    parent[lastKey] = mapper(parent[lastKey]);
  }
  if (isSet6(parent)) {
    const oldValue = getNthKey6(parent, +lastKey);
    const newValue = mapper(oldValue);
    if (oldValue !== newValue) {
      parent.delete(oldValue);
      parent.add(newValue);
    }
  }
  if (isMap6(parent)) {
    const row = +path[path.length - 2];
    const keyToRow = getNthKey6(parent, row);
    const type = +lastKey === 0 ? "key" : "value";
    switch (type) {
      case "key": {
        const newKey = mapper(keyToRow);
        parent.set(newKey, parent.get(keyToRow));
        if (newKey !== keyToRow) {
          parent.delete(keyToRow);
        }
        break;
      }
      case "value": {
        parent.set(keyToRow, mapper(parent.get(keyToRow)));
        break;
      }
    }
  }
  return object2;
}, "setDeep");
var enableLegacyPaths6 = /* @__PURE__ */ __name((version4) => version4 < 1, "enableLegacyPaths");
function traverse6(tree, walker26, version4, origin = []) {
  if (!tree) {
    return;
  }
  const legacyPaths = enableLegacyPaths6(version4);
  if (!isArray7(tree)) {
    forEach6(tree, (subtree, key) => traverse6(subtree, walker26, version4, [
      ...origin,
      ...parsePath6(key, legacyPaths)
    ]));
    return;
  }
  const [nodeValue, children] = tree;
  if (children) {
    forEach6(children, (child, key) => {
      traverse6(child, walker26, version4, [
        ...origin,
        ...parsePath6(key, legacyPaths)
      ]);
    });
  }
  walker26(nodeValue, origin);
}
__name(traverse6, "traverse");
function applyValueAnnotations6(plain, annotations, version4, superJson) {
  traverse6(annotations, (type, path) => {
    plain = setDeep6(plain, path, (v) => untransformValue6(v, type, superJson));
  }, version4);
  return plain;
}
__name(applyValueAnnotations6, "applyValueAnnotations");
function applyReferentialEqualityAnnotations6(plain, annotations, version4) {
  const legacyPaths = enableLegacyPaths6(version4);
  function apply(identicalPaths, path) {
    const object2 = getDeep6(plain, parsePath6(path, legacyPaths));
    identicalPaths.map((path2) => parsePath6(path2, legacyPaths)).forEach((identicalObjectPath) => {
      plain = setDeep6(plain, identicalObjectPath, () => object2);
    });
  }
  __name(apply, "apply");
  if (isArray7(annotations)) {
    const [root, other] = annotations;
    root.forEach((identicalPath) => {
      plain = setDeep6(plain, parsePath6(identicalPath, legacyPaths), () => plain);
    });
    if (other) {
      forEach6(other, apply);
    }
  } else {
    forEach6(annotations, apply);
  }
  return plain;
}
__name(applyReferentialEqualityAnnotations6, "applyReferentialEqualityAnnotations");
var isDeep6 = /* @__PURE__ */ __name((object2, superJson) => isPlainObject7(object2) || isArray7(object2) || isMap6(object2) || isSet6(object2) || isError6(object2) || isInstanceOfRegisteredClass6(object2, superJson), "isDeep");
function addIdentity6(object2, path, identities) {
  const existingSet = identities.get(object2);
  if (existingSet) {
    existingSet.push(path);
  } else {
    identities.set(object2, [path]);
  }
}
__name(addIdentity6, "addIdentity");
function generateReferentialEqualityAnnotations6(identitites, dedupe) {
  const result = {};
  let rootEqualityPaths = void 0;
  identitites.forEach((paths) => {
    if (paths.length <= 1) {
      return;
    }
    if (!dedupe) {
      paths = paths.map((path) => path.map(String)).sort((a, b) => a.length - b.length);
    }
    const [representativePath, ...identicalPaths] = paths;
    if (representativePath.length === 0) {
      rootEqualityPaths = identicalPaths.map(stringifyPath6);
    } else {
      result[stringifyPath6(representativePath)] = identicalPaths.map(stringifyPath6);
    }
  });
  if (rootEqualityPaths) {
    if (isEmptyObject6(result)) {
      return [rootEqualityPaths];
    } else {
      return [rootEqualityPaths, result];
    }
  } else {
    return isEmptyObject6(result) ? void 0 : result;
  }
}
__name(generateReferentialEqualityAnnotations6, "generateReferentialEqualityAnnotations");
var walker6 = /* @__PURE__ */ __name((object2, identities, superJson, dedupe, path = [], objectsInThisPath = [], seenObjects = /* @__PURE__ */ new Map()) => {
  const primitive = isPrimitive6(object2);
  if (!primitive) {
    addIdentity6(object2, path, identities);
    const seen = seenObjects.get(object2);
    if (seen) {
      return dedupe ? {
        transformedValue: null
      } : seen;
    }
  }
  if (!isDeep6(object2, superJson)) {
    const transformed2 = transformValue6(object2, superJson);
    const result2 = transformed2 ? {
      transformedValue: transformed2.value,
      annotations: [transformed2.type]
    } : {
      transformedValue: object2
    };
    if (!primitive) {
      seenObjects.set(object2, result2);
    }
    return result2;
  }
  if (includes6(objectsInThisPath, object2)) {
    return {
      transformedValue: null
    };
  }
  const transformationResult = transformValue6(object2, superJson);
  const transformed = transformationResult?.value ?? object2;
  const transformedValue = isArray7(transformed) ? [] : {};
  const innerAnnotations = {};
  forEach6(transformed, (value, index) => {
    if (index === "__proto__" || index === "constructor" || index === "prototype") {
      throw new Error(`Detected property ${index}. This is a prototype pollution risk, please remove it from your object.`);
    }
    const recursiveResult = walker6(value, identities, superJson, dedupe, [...path, index], [...objectsInThisPath, object2], seenObjects);
    transformedValue[index] = recursiveResult.transformedValue;
    if (isArray7(recursiveResult.annotations)) {
      innerAnnotations[escapeKey6(index)] = recursiveResult.annotations;
    } else if (isPlainObject7(recursiveResult.annotations)) {
      forEach6(recursiveResult.annotations, (tree, key) => {
        innerAnnotations[escapeKey6(index) + "." + key] = tree;
      });
    }
  });
  const result = isEmptyObject6(innerAnnotations) ? {
    transformedValue,
    annotations: transformationResult ? [transformationResult.type] : void 0
  } : {
    transformedValue,
    annotations: transformationResult ? [transformationResult.type, innerAnnotations] : innerAnnotations
  };
  if (!primitive) {
    seenObjects.set(object2, result);
  }
  return result;
}, "walker");
function getType26(payload) {
  return Object.prototype.toString.call(payload).slice(8, -1);
}
__name(getType26, "getType2");
function isArray26(payload) {
  return getType26(payload) === "Array";
}
__name(isArray26, "isArray2");
function isPlainObject26(payload) {
  if (getType26(payload) !== "Object")
    return false;
  const prototype = Object.getPrototypeOf(payload);
  return !!prototype && prototype.constructor === Object && prototype === Object.prototype;
}
__name(isPlainObject26, "isPlainObject2");
function assignProp6(carry, key, newVal, originalObject, includeNonenumerable) {
  const propType = {}.propertyIsEnumerable.call(originalObject, key) ? "enumerable" : "nonenumerable";
  if (propType === "enumerable")
    carry[key] = newVal;
  if (includeNonenumerable && propType === "nonenumerable") {
    Object.defineProperty(carry, key, {
      value: newVal,
      enumerable: false,
      writable: true,
      configurable: true
    });
  }
}
__name(assignProp6, "assignProp");
function copy6(target, options = {}) {
  if (isArray26(target)) {
    return target.map((item) => copy6(item, options));
  }
  if (!isPlainObject26(target)) {
    return target;
  }
  const props = Object.getOwnPropertyNames(target);
  const symbols = Object.getOwnPropertySymbols(target);
  return [...props, ...symbols].reduce((carry, key) => {
    if (key === "__proto__")
      return carry;
    if (isArray26(options.props) && !options.props.includes(key)) {
      return carry;
    }
    const val = target[key];
    const newVal = copy6(val, options);
    assignProp6(carry, key, newVal, target, options.nonenumerable);
    return carry;
  }, {});
}
__name(copy6, "copy");
var SuperJSON6 = class {
  static {
    __name(this, "SuperJSON");
  }
  constructor({ dedupe = false } = {}) {
    this.classRegistry = new ClassRegistry6();
    this.symbolRegistry = new Registry6((s) => s.description ?? "");
    this.customTransformerRegistry = new CustomTransformerRegistry6();
    this.allowedErrorProps = [];
    this.dedupe = dedupe;
  }
  serialize(object2) {
    const identities = /* @__PURE__ */ new Map();
    const output = walker6(object2, identities, this, this.dedupe);
    const res = {
      json: output.transformedValue
    };
    if (output.annotations) {
      res.meta = {
        ...res.meta,
        values: output.annotations
      };
    }
    const equalityAnnotations = generateReferentialEqualityAnnotations6(identities, this.dedupe);
    if (equalityAnnotations) {
      res.meta = {
        ...res.meta,
        referentialEqualities: equalityAnnotations
      };
    }
    if (res.meta)
      res.meta.v = 1;
    return res;
  }
  deserialize(payload, options) {
    const { json, meta } = payload;
    let result = options?.inPlace ? json : copy6(json);
    if (meta?.values) {
      result = applyValueAnnotations6(result, meta.values, meta.v ?? 0, this);
    }
    if (meta?.referentialEqualities) {
      result = applyReferentialEqualityAnnotations6(result, meta.referentialEqualities, meta.v ?? 0);
    }
    return result;
  }
  stringify(object2) {
    return JSON.stringify(this.serialize(object2));
  }
  parse(string2) {
    return this.deserialize(JSON.parse(string2), { inPlace: true });
  }
  registerClass(v, options) {
    this.classRegistry.register(v, options);
  }
  registerSymbol(v, identifier) {
    this.symbolRegistry.register(v, identifier);
  }
  registerCustom(transformer, name) {
    this.customTransformerRegistry.register({
      name,
      ...transformer
    });
  }
  allowErrorProps(...props) {
    this.allowedErrorProps.push(...props);
  }
};
SuperJSON6.defaultInstance = new SuperJSON6();
SuperJSON6.serialize = SuperJSON6.defaultInstance.serialize.bind(SuperJSON6.defaultInstance);
SuperJSON6.deserialize = SuperJSON6.defaultInstance.deserialize.bind(SuperJSON6.defaultInstance);
SuperJSON6.stringify = SuperJSON6.defaultInstance.stringify.bind(SuperJSON6.defaultInstance);
SuperJSON6.parse = SuperJSON6.defaultInstance.parse.bind(SuperJSON6.defaultInstance);
SuperJSON6.registerClass = SuperJSON6.defaultInstance.registerClass.bind(SuperJSON6.defaultInstance);
SuperJSON6.registerSymbol = SuperJSON6.defaultInstance.registerSymbol.bind(SuperJSON6.defaultInstance);
SuperJSON6.registerCustom = SuperJSON6.defaultInstance.registerCustom.bind(SuperJSON6.defaultInstance);
SuperJSON6.allowErrorProps = SuperJSON6.defaultInstance.allowErrorProps.bind(SuperJSON6.defaultInstance);
var dist_default6 = SuperJSON6;
var serialize6 = SuperJSON6.serialize;
var deserialize6 = SuperJSON6.deserialize;
var stringify6 = SuperJSON6.stringify;
var parse6 = SuperJSON6.parse;
var registerClass6 = SuperJSON6.registerClass;
var registerCustom6 = SuperJSON6.registerCustom;
var registerSymbol6 = SuperJSON6.registerSymbol;
var allowErrorProps6 = SuperJSON6.allowErrorProps;
function parseData6(formData) {
  const propsArray = [];
  if (!formData)
    return propsArray;
  const batchsIDs = [];
  for (const [key, value] of Array.from(formData.entries())) {
    if (key.startsWith("FILE_"))
      propsArray.push(value);
    else if (key.startsWith("FILES_")) {
      if (batchsIDs.includes(key))
        continue;
      batchsIDs.push(key);
      propsArray.push(formData.getAll(key));
    } else {
      propsArray.push(JSON.parse(decodeURI(value)));
    }
  }
  return propsArray;
}
__name(parseData6, "parseData");
function paramsFromURL6(url) {
  const params = url.searchParams.entries().toArray().map(([_, v]) => v);
  return params.map((param) => JSON.parse(decodeURIComponent(param)));
}
__name(paramsFromURL6, "paramsFromURL");
async function WrapRequestHandler6(context2, endpoint) {
  const isServerAction = context2.request.headers.get("x-server-action") === "true";
  if (!isServerAction) {
    return new Response("Not Found", { status: 404 });
  }
  const parsedData = context2.request.method === "GET" || context2.request.method === "HEAD" ? paramsFromURL6(new URL(context2.request.url)) : parseData6(context2.request.headers.get("content-type") ? await context2.request.formData() : void 0);
  const missingProps = endpoint.length - parsedData.length;
  for (let i = 0; i < missingProps; i++) {
    parsedData.push(void 0);
  }
  parsedData.push(context2);
  const result = await endpoint(...parsedData);
  switch (typeof result) {
    case "string":
    case "number":
    case "boolean":
    case "bigint":
      return new Response(dist_default6.stringify(result), {
        headers: {
          "Content-Type": "application/json",
          dataType: "json"
        }
      });
    case "undefined":
      return new Response(null, { status: 204 });
    case "object":
      if (result instanceof Response) {
        result.headers.set("dataType", "response");
        return result;
      } else if (result instanceof Blob) {
        const res = new Response(await result.arrayBuffer());
        res.headers.set("dataType", "blob");
        res.headers.set("Content-Type", result.type);
        return res;
      } else if (result instanceof File) {
        return new Response(await result.arrayBuffer(), {
          headers: {
            "Content-Type": result.type,
            dataType: "file",
            fileData: JSON.stringify({
              name: result.name,
              lastModified: result.lastModified
            })
          }
        });
      } else {
        return new Response(dist_default6.stringify(result), {
          headers: { "Content-Type": "application/json", dataType: "json" }
        });
      }
    default:
      throw new Error(`Unsupported return type from action: ${typeof result}`);
  }
}
__name(WrapRequestHandler6, "WrapRequestHandler");
var onRequest6 = /* @__PURE__ */ __name(async (context2) => {
  const method = context2.request.method;
  const options = {
    GET: typeof GET === "function" ? GET : void 0,
    POST: typeof POST7 === "function" ? POST7 : void 0,
    PUT: typeof PUT === "function" ? PUT : void 0,
    DELETE: typeof DELETE4 === "function" ? DELETE4 : void 0,
    PATCH: typeof PATCH === "function" ? PATCH : void 0,
    HEAD: typeof HEAD === "function" ? HEAD : void 0,
    OPTIONS: typeof OPTIONS === "function" ? OPTIONS : void 0
  };
  if (!options[method]) {
    return new Response(`Method "${method}" Not Allowed`, {
      status: 405
    });
  }
  return await WrapRequestHandler6(context2, options[method]);
}, "onRequest");

// api/invites/project/accept.js
async function POST8({ code }) {
  const ctx = getContext(arguments);
  const userId = (await ctx.data.client.getMetaData())?.id;
  if (!userId) {
    return { success: false, error: "Unauthorized" };
  }
  return await invites(ctx.env.PROJECT_DB).confirmInvite(code, userId, "project", ctx.data.client);
}
__name(POST8, "POST");
var DoubleIndexedKV7 = class {
  static {
    __name(this, "DoubleIndexedKV");
  }
  constructor() {
    this.keyToValue = /* @__PURE__ */ new Map();
    this.valueToKey = /* @__PURE__ */ new Map();
  }
  set(key, value) {
    this.keyToValue.set(key, value);
    this.valueToKey.set(value, key);
  }
  getByKey(key) {
    return this.keyToValue.get(key);
  }
  getByValue(value) {
    return this.valueToKey.get(value);
  }
  clear() {
    this.keyToValue.clear();
    this.valueToKey.clear();
  }
};
var Registry7 = class {
  static {
    __name(this, "Registry");
  }
  constructor(generateIdentifier) {
    this.generateIdentifier = generateIdentifier;
    this.kv = new DoubleIndexedKV7();
  }
  register(value, identifier) {
    if (this.kv.getByValue(value)) {
      return;
    }
    if (!identifier) {
      identifier = this.generateIdentifier(value);
    }
    this.kv.set(identifier, value);
  }
  clear() {
    this.kv.clear();
  }
  getIdentifier(value) {
    return this.kv.getByValue(value);
  }
  getValue(identifier) {
    return this.kv.getByKey(identifier);
  }
};
var ClassRegistry7 = class extends Registry7 {
  static {
    __name(this, "ClassRegistry");
  }
  constructor() {
    super((c) => c.name);
    this.classToAllowedProps = /* @__PURE__ */ new Map();
  }
  register(value, options) {
    if (typeof options === "object") {
      if (options.allowProps) {
        this.classToAllowedProps.set(value, options.allowProps);
      }
      super.register(value, options.identifier);
    } else {
      super.register(value, options);
    }
  }
  getAllowedProps(value) {
    return this.classToAllowedProps.get(value);
  }
};
function valuesOfObj7(record) {
  if ("values" in Object) {
    return Object.values(record);
  }
  const values = [];
  for (const key in record) {
    if (record.hasOwnProperty(key)) {
      values.push(record[key]);
    }
  }
  return values;
}
__name(valuesOfObj7, "valuesOfObj");
function find7(record, predicate) {
  const values = valuesOfObj7(record);
  if ("find" in values) {
    return values.find(predicate);
  }
  const valuesNotNever = values;
  for (let i = 0; i < valuesNotNever.length; i++) {
    const value = valuesNotNever[i];
    if (predicate(value)) {
      return value;
    }
  }
  return;
}
__name(find7, "find");
function forEach7(record, run) {
  Object.entries(record).forEach(([key, value]) => run(value, key));
}
__name(forEach7, "forEach");
function includes7(arr, value) {
  return arr.indexOf(value) !== -1;
}
__name(includes7, "includes");
function findArr7(record, predicate) {
  for (let i = 0; i < record.length; i++) {
    const value = record[i];
    if (predicate(value)) {
      return value;
    }
  }
  return;
}
__name(findArr7, "findArr");
var CustomTransformerRegistry7 = class {
  static {
    __name(this, "CustomTransformerRegistry");
  }
  constructor() {
    this.transfomers = {};
  }
  register(transformer) {
    this.transfomers[transformer.name] = transformer;
  }
  findApplicable(v) {
    return find7(this.transfomers, (transformer) => transformer.isApplicable(v));
  }
  findByName(name) {
    return this.transfomers[name];
  }
};
var getType8 = /* @__PURE__ */ __name((payload) => Object.prototype.toString.call(payload).slice(8, -1), "getType");
var isUndefined7 = /* @__PURE__ */ __name((payload) => typeof payload === "undefined", "isUndefined");
var isNull8 = /* @__PURE__ */ __name((payload) => payload === null, "isNull");
var isPlainObject8 = /* @__PURE__ */ __name((payload) => {
  if (typeof payload !== "object" || payload === null)
    return false;
  if (payload === Object.prototype)
    return false;
  if (Object.getPrototypeOf(payload) === null)
    return true;
  return Object.getPrototypeOf(payload) === Object.prototype;
}, "isPlainObject");
var isEmptyObject7 = /* @__PURE__ */ __name((payload) => isPlainObject8(payload) && Object.keys(payload).length === 0, "isEmptyObject");
var isArray8 = /* @__PURE__ */ __name((payload) => Array.isArray(payload), "isArray");
var isString7 = /* @__PURE__ */ __name((payload) => typeof payload === "string", "isString");
var isNumber7 = /* @__PURE__ */ __name((payload) => typeof payload === "number" && !isNaN(payload), "isNumber");
var isBoolean7 = /* @__PURE__ */ __name((payload) => typeof payload === "boolean", "isBoolean");
var isRegExp7 = /* @__PURE__ */ __name((payload) => payload instanceof RegExp, "isRegExp");
var isMap7 = /* @__PURE__ */ __name((payload) => payload instanceof Map, "isMap");
var isSet7 = /* @__PURE__ */ __name((payload) => payload instanceof Set, "isSet");
var isSymbol7 = /* @__PURE__ */ __name((payload) => getType8(payload) === "Symbol", "isSymbol");
var isDate7 = /* @__PURE__ */ __name((payload) => payload instanceof Date && !isNaN(payload.valueOf()), "isDate");
var isError7 = /* @__PURE__ */ __name((payload) => payload instanceof Error, "isError");
var isNaNValue7 = /* @__PURE__ */ __name((payload) => typeof payload === "number" && isNaN(payload), "isNaNValue");
var isPrimitive7 = /* @__PURE__ */ __name((payload) => isBoolean7(payload) || isNull8(payload) || isUndefined7(payload) || isNumber7(payload) || isString7(payload) || isSymbol7(payload), "isPrimitive");
var isBigint7 = /* @__PURE__ */ __name((payload) => typeof payload === "bigint", "isBigint");
var isInfinite7 = /* @__PURE__ */ __name((payload) => payload === Infinity || payload === -Infinity, "isInfinite");
var isTypedArray7 = /* @__PURE__ */ __name((payload) => ArrayBuffer.isView(payload) && !(payload instanceof DataView), "isTypedArray");
var isURL7 = /* @__PURE__ */ __name((payload) => payload instanceof URL, "isURL");
var escapeKey7 = /* @__PURE__ */ __name((key) => key.replace(/\\/g, "\\\\").replace(/\./g, "\\."), "escapeKey");
var stringifyPath7 = /* @__PURE__ */ __name((path) => path.map(String).map(escapeKey7).join("."), "stringifyPath");
var parsePath7 = /* @__PURE__ */ __name((string2, legacyPaths) => {
  const result = [];
  let segment = "";
  for (let i = 0; i < string2.length; i++) {
    let char = string2.charAt(i);
    if (!legacyPaths && char === "\\") {
      const escaped = string2.charAt(i + 1);
      if (escaped === "\\") {
        segment += "\\";
        i++;
        continue;
      } else if (escaped !== ".") {
        throw Error("invalid path");
      }
    }
    const isEscapedDot = char === "\\" && string2.charAt(i + 1) === ".";
    if (isEscapedDot) {
      segment += ".";
      i++;
      continue;
    }
    const isEndOfSegment = char === ".";
    if (isEndOfSegment) {
      result.push(segment);
      segment = "";
      continue;
    }
    segment += char;
  }
  const lastSegment = segment;
  result.push(lastSegment);
  return result;
}, "parsePath");
function simpleTransformation7(isApplicable, annotation, transform, untransform) {
  return {
    isApplicable,
    annotation,
    transform,
    untransform
  };
}
__name(simpleTransformation7, "simpleTransformation");
var simpleRules7 = [
  simpleTransformation7(isUndefined7, "undefined", () => null, () => {
    return;
  }),
  simpleTransformation7(isBigint7, "bigint", (v) => v.toString(), (v) => {
    if (typeof BigInt !== "undefined") {
      return BigInt(v);
    }
    console.error("Please add a BigInt polyfill.");
    return v;
  }),
  simpleTransformation7(isDate7, "Date", (v) => v.toISOString(), (v) => new Date(v)),
  simpleTransformation7(isError7, "Error", (v, superJson) => {
    const baseError = {
      name: v.name,
      message: v.message
    };
    if ("cause" in v) {
      baseError.cause = v.cause;
    }
    superJson.allowedErrorProps.forEach((prop) => {
      baseError[prop] = v[prop];
    });
    return baseError;
  }, (v, superJson) => {
    const e = new Error(v.message, { cause: v.cause });
    e.name = v.name;
    e.stack = v.stack;
    superJson.allowedErrorProps.forEach((prop) => {
      e[prop] = v[prop];
    });
    return e;
  }),
  simpleTransformation7(isRegExp7, "regexp", (v) => "" + v, (regex) => {
    const body = regex.slice(1, regex.lastIndexOf("/"));
    const flags2 = regex.slice(regex.lastIndexOf("/") + 1);
    return new RegExp(body, flags2);
  }),
  simpleTransformation7(isSet7, "set", (v) => [...v.values()], (v) => new Set(v)),
  simpleTransformation7(isMap7, "map", (v) => [...v.entries()], (v) => new Map(v)),
  simpleTransformation7((v) => isNaNValue7(v) || isInfinite7(v), "number", (v) => {
    if (isNaNValue7(v)) {
      return "NaN";
    }
    if (v > 0) {
      return "Infinity";
    } else {
      return "-Infinity";
    }
  }, Number),
  simpleTransformation7((v) => v === 0 && 1 / v === -Infinity, "number", () => {
    return "-0";
  }, Number),
  simpleTransformation7(isURL7, "URL", (v) => v.toString(), (v) => new URL(v))
];
function compositeTransformation7(isApplicable, annotation, transform, untransform) {
  return {
    isApplicable,
    annotation,
    transform,
    untransform
  };
}
__name(compositeTransformation7, "compositeTransformation");
var symbolRule7 = compositeTransformation7((s, superJson) => {
  if (isSymbol7(s)) {
    const isRegistered = !!superJson.symbolRegistry.getIdentifier(s);
    return isRegistered;
  }
  return false;
}, (s, superJson) => {
  const identifier = superJson.symbolRegistry.getIdentifier(s);
  return ["symbol", identifier];
}, (v) => v.description, (_, a, superJson) => {
  const value = superJson.symbolRegistry.getValue(a[1]);
  if (!value) {
    throw new Error("Trying to deserialize unknown symbol");
  }
  return value;
});
var constructorToName7 = [
  Int8Array,
  Uint8Array,
  Int16Array,
  Uint16Array,
  Int32Array,
  Uint32Array,
  Float32Array,
  Float64Array,
  Uint8ClampedArray
].reduce((obj, ctor) => {
  obj[ctor.name] = ctor;
  return obj;
}, {});
var typedArrayRule7 = compositeTransformation7(isTypedArray7, (v) => ["typed-array", v.constructor.name], (v) => [...v], (v, a) => {
  const ctor = constructorToName7[a[1]];
  if (!ctor) {
    throw new Error("Trying to deserialize unknown typed array");
  }
  return new ctor(v);
});
function isInstanceOfRegisteredClass7(potentialClass, superJson) {
  if (potentialClass?.constructor) {
    const isRegistered = !!superJson.classRegistry.getIdentifier(potentialClass.constructor);
    return isRegistered;
  }
  return false;
}
__name(isInstanceOfRegisteredClass7, "isInstanceOfRegisteredClass");
var classRule7 = compositeTransformation7(isInstanceOfRegisteredClass7, (clazz, superJson) => {
  const identifier = superJson.classRegistry.getIdentifier(clazz.constructor);
  return ["class", identifier];
}, (clazz, superJson) => {
  const allowedProps = superJson.classRegistry.getAllowedProps(clazz.constructor);
  if (!allowedProps) {
    return { ...clazz };
  }
  const result = {};
  allowedProps.forEach((prop) => {
    result[prop] = clazz[prop];
  });
  return result;
}, (v, a, superJson) => {
  const clazz = superJson.classRegistry.getValue(a[1]);
  if (!clazz) {
    throw new Error(`Trying to deserialize unknown class '${a[1]}' - check https://github.com/blitz-js/superjson/issues/116#issuecomment-773996564`);
  }
  return Object.assign(Object.create(clazz.prototype), v);
});
var customRule7 = compositeTransformation7((value, superJson) => {
  return !!superJson.customTransformerRegistry.findApplicable(value);
}, (value, superJson) => {
  const transformer = superJson.customTransformerRegistry.findApplicable(value);
  return ["custom", transformer.name];
}, (value, superJson) => {
  const transformer = superJson.customTransformerRegistry.findApplicable(value);
  return transformer.serialize(value);
}, (v, a, superJson) => {
  const transformer = superJson.customTransformerRegistry.findByName(a[1]);
  if (!transformer) {
    throw new Error("Trying to deserialize unknown custom value");
  }
  return transformer.deserialize(v);
});
var compositeRules7 = [classRule7, symbolRule7, customRule7, typedArrayRule7];
var transformValue7 = /* @__PURE__ */ __name((value, superJson) => {
  const applicableCompositeRule = findArr7(compositeRules7, (rule) => rule.isApplicable(value, superJson));
  if (applicableCompositeRule) {
    return {
      value: applicableCompositeRule.transform(value, superJson),
      type: applicableCompositeRule.annotation(value, superJson)
    };
  }
  const applicableSimpleRule = findArr7(simpleRules7, (rule) => rule.isApplicable(value, superJson));
  if (applicableSimpleRule) {
    return {
      value: applicableSimpleRule.transform(value, superJson),
      type: applicableSimpleRule.annotation
    };
  }
  return;
}, "transformValue");
var simpleRulesByAnnotation7 = {};
simpleRules7.forEach((rule) => {
  simpleRulesByAnnotation7[rule.annotation] = rule;
});
var untransformValue7 = /* @__PURE__ */ __name((json, type, superJson) => {
  if (isArray8(type)) {
    switch (type[0]) {
      case "symbol":
        return symbolRule7.untransform(json, type, superJson);
      case "class":
        return classRule7.untransform(json, type, superJson);
      case "custom":
        return customRule7.untransform(json, type, superJson);
      case "typed-array":
        return typedArrayRule7.untransform(json, type, superJson);
      default:
        throw new Error("Unknown transformation: " + type);
    }
  } else {
    const transformation = simpleRulesByAnnotation7[type];
    if (!transformation) {
      throw new Error("Unknown transformation: " + type);
    }
    return transformation.untransform(json, superJson);
  }
}, "untransformValue");
var getNthKey7 = /* @__PURE__ */ __name((value, n) => {
  if (n > value.size)
    throw new Error("index out of bounds");
  const keys = value.keys();
  while (n > 0) {
    keys.next();
    n--;
  }
  return keys.next().value;
}, "getNthKey");
function validatePath7(path) {
  if (includes7(path, "__proto__")) {
    throw new Error("__proto__ is not allowed as a property");
  }
  if (includes7(path, "prototype")) {
    throw new Error("prototype is not allowed as a property");
  }
  if (includes7(path, "constructor")) {
    throw new Error("constructor is not allowed as a property");
  }
}
__name(validatePath7, "validatePath");
var getDeep7 = /* @__PURE__ */ __name((object2, path) => {
  validatePath7(path);
  for (let i = 0; i < path.length; i++) {
    const key = path[i];
    if (isSet7(object2)) {
      object2 = getNthKey7(object2, +key);
    } else if (isMap7(object2)) {
      const row = +key;
      const type = +path[++i] === 0 ? "key" : "value";
      const keyOfRow = getNthKey7(object2, row);
      switch (type) {
        case "key":
          object2 = keyOfRow;
          break;
        case "value":
          object2 = object2.get(keyOfRow);
          break;
      }
    } else {
      object2 = object2[key];
    }
  }
  return object2;
}, "getDeep");
var setDeep7 = /* @__PURE__ */ __name((object2, path, mapper) => {
  validatePath7(path);
  if (path.length === 0) {
    return mapper(object2);
  }
  let parent = object2;
  for (let i = 0; i < path.length - 1; i++) {
    const key = path[i];
    if (isArray8(parent)) {
      const index = +key;
      parent = parent[index];
    } else if (isPlainObject8(parent)) {
      parent = parent[key];
    } else if (isSet7(parent)) {
      const row = +key;
      parent = getNthKey7(parent, row);
    } else if (isMap7(parent)) {
      const isEnd = i === path.length - 2;
      if (isEnd) {
        break;
      }
      const row = +key;
      const type = +path[++i] === 0 ? "key" : "value";
      const keyOfRow = getNthKey7(parent, row);
      switch (type) {
        case "key":
          parent = keyOfRow;
          break;
        case "value":
          parent = parent.get(keyOfRow);
          break;
      }
    }
  }
  const lastKey = path[path.length - 1];
  if (isArray8(parent)) {
    parent[+lastKey] = mapper(parent[+lastKey]);
  } else if (isPlainObject8(parent)) {
    parent[lastKey] = mapper(parent[lastKey]);
  }
  if (isSet7(parent)) {
    const oldValue = getNthKey7(parent, +lastKey);
    const newValue = mapper(oldValue);
    if (oldValue !== newValue) {
      parent.delete(oldValue);
      parent.add(newValue);
    }
  }
  if (isMap7(parent)) {
    const row = +path[path.length - 2];
    const keyToRow = getNthKey7(parent, row);
    const type = +lastKey === 0 ? "key" : "value";
    switch (type) {
      case "key": {
        const newKey = mapper(keyToRow);
        parent.set(newKey, parent.get(keyToRow));
        if (newKey !== keyToRow) {
          parent.delete(keyToRow);
        }
        break;
      }
      case "value": {
        parent.set(keyToRow, mapper(parent.get(keyToRow)));
        break;
      }
    }
  }
  return object2;
}, "setDeep");
var enableLegacyPaths7 = /* @__PURE__ */ __name((version4) => version4 < 1, "enableLegacyPaths");
function traverse7(tree, walker26, version4, origin = []) {
  if (!tree) {
    return;
  }
  const legacyPaths = enableLegacyPaths7(version4);
  if (!isArray8(tree)) {
    forEach7(tree, (subtree, key) => traverse7(subtree, walker26, version4, [
      ...origin,
      ...parsePath7(key, legacyPaths)
    ]));
    return;
  }
  const [nodeValue, children] = tree;
  if (children) {
    forEach7(children, (child, key) => {
      traverse7(child, walker26, version4, [
        ...origin,
        ...parsePath7(key, legacyPaths)
      ]);
    });
  }
  walker26(nodeValue, origin);
}
__name(traverse7, "traverse");
function applyValueAnnotations7(plain, annotations, version4, superJson) {
  traverse7(annotations, (type, path) => {
    plain = setDeep7(plain, path, (v) => untransformValue7(v, type, superJson));
  }, version4);
  return plain;
}
__name(applyValueAnnotations7, "applyValueAnnotations");
function applyReferentialEqualityAnnotations7(plain, annotations, version4) {
  const legacyPaths = enableLegacyPaths7(version4);
  function apply(identicalPaths, path) {
    const object2 = getDeep7(plain, parsePath7(path, legacyPaths));
    identicalPaths.map((path2) => parsePath7(path2, legacyPaths)).forEach((identicalObjectPath) => {
      plain = setDeep7(plain, identicalObjectPath, () => object2);
    });
  }
  __name(apply, "apply");
  if (isArray8(annotations)) {
    const [root, other] = annotations;
    root.forEach((identicalPath) => {
      plain = setDeep7(plain, parsePath7(identicalPath, legacyPaths), () => plain);
    });
    if (other) {
      forEach7(other, apply);
    }
  } else {
    forEach7(annotations, apply);
  }
  return plain;
}
__name(applyReferentialEqualityAnnotations7, "applyReferentialEqualityAnnotations");
var isDeep7 = /* @__PURE__ */ __name((object2, superJson) => isPlainObject8(object2) || isArray8(object2) || isMap7(object2) || isSet7(object2) || isError7(object2) || isInstanceOfRegisteredClass7(object2, superJson), "isDeep");
function addIdentity7(object2, path, identities) {
  const existingSet = identities.get(object2);
  if (existingSet) {
    existingSet.push(path);
  } else {
    identities.set(object2, [path]);
  }
}
__name(addIdentity7, "addIdentity");
function generateReferentialEqualityAnnotations7(identitites, dedupe) {
  const result = {};
  let rootEqualityPaths = void 0;
  identitites.forEach((paths) => {
    if (paths.length <= 1) {
      return;
    }
    if (!dedupe) {
      paths = paths.map((path) => path.map(String)).sort((a, b) => a.length - b.length);
    }
    const [representativePath, ...identicalPaths] = paths;
    if (representativePath.length === 0) {
      rootEqualityPaths = identicalPaths.map(stringifyPath7);
    } else {
      result[stringifyPath7(representativePath)] = identicalPaths.map(stringifyPath7);
    }
  });
  if (rootEqualityPaths) {
    if (isEmptyObject7(result)) {
      return [rootEqualityPaths];
    } else {
      return [rootEqualityPaths, result];
    }
  } else {
    return isEmptyObject7(result) ? void 0 : result;
  }
}
__name(generateReferentialEqualityAnnotations7, "generateReferentialEqualityAnnotations");
var walker7 = /* @__PURE__ */ __name((object2, identities, superJson, dedupe, path = [], objectsInThisPath = [], seenObjects = /* @__PURE__ */ new Map()) => {
  const primitive = isPrimitive7(object2);
  if (!primitive) {
    addIdentity7(object2, path, identities);
    const seen = seenObjects.get(object2);
    if (seen) {
      return dedupe ? {
        transformedValue: null
      } : seen;
    }
  }
  if (!isDeep7(object2, superJson)) {
    const transformed2 = transformValue7(object2, superJson);
    const result2 = transformed2 ? {
      transformedValue: transformed2.value,
      annotations: [transformed2.type]
    } : {
      transformedValue: object2
    };
    if (!primitive) {
      seenObjects.set(object2, result2);
    }
    return result2;
  }
  if (includes7(objectsInThisPath, object2)) {
    return {
      transformedValue: null
    };
  }
  const transformationResult = transformValue7(object2, superJson);
  const transformed = transformationResult?.value ?? object2;
  const transformedValue = isArray8(transformed) ? [] : {};
  const innerAnnotations = {};
  forEach7(transformed, (value, index) => {
    if (index === "__proto__" || index === "constructor" || index === "prototype") {
      throw new Error(`Detected property ${index}. This is a prototype pollution risk, please remove it from your object.`);
    }
    const recursiveResult = walker7(value, identities, superJson, dedupe, [...path, index], [...objectsInThisPath, object2], seenObjects);
    transformedValue[index] = recursiveResult.transformedValue;
    if (isArray8(recursiveResult.annotations)) {
      innerAnnotations[escapeKey7(index)] = recursiveResult.annotations;
    } else if (isPlainObject8(recursiveResult.annotations)) {
      forEach7(recursiveResult.annotations, (tree, key) => {
        innerAnnotations[escapeKey7(index) + "." + key] = tree;
      });
    }
  });
  const result = isEmptyObject7(innerAnnotations) ? {
    transformedValue,
    annotations: transformationResult ? [transformationResult.type] : void 0
  } : {
    transformedValue,
    annotations: transformationResult ? [transformationResult.type, innerAnnotations] : innerAnnotations
  };
  if (!primitive) {
    seenObjects.set(object2, result);
  }
  return result;
}, "walker");
function getType27(payload) {
  return Object.prototype.toString.call(payload).slice(8, -1);
}
__name(getType27, "getType2");
function isArray27(payload) {
  return getType27(payload) === "Array";
}
__name(isArray27, "isArray2");
function isPlainObject27(payload) {
  if (getType27(payload) !== "Object")
    return false;
  const prototype = Object.getPrototypeOf(payload);
  return !!prototype && prototype.constructor === Object && prototype === Object.prototype;
}
__name(isPlainObject27, "isPlainObject2");
function assignProp7(carry, key, newVal, originalObject, includeNonenumerable) {
  const propType = {}.propertyIsEnumerable.call(originalObject, key) ? "enumerable" : "nonenumerable";
  if (propType === "enumerable")
    carry[key] = newVal;
  if (includeNonenumerable && propType === "nonenumerable") {
    Object.defineProperty(carry, key, {
      value: newVal,
      enumerable: false,
      writable: true,
      configurable: true
    });
  }
}
__name(assignProp7, "assignProp");
function copy7(target, options = {}) {
  if (isArray27(target)) {
    return target.map((item) => copy7(item, options));
  }
  if (!isPlainObject27(target)) {
    return target;
  }
  const props = Object.getOwnPropertyNames(target);
  const symbols = Object.getOwnPropertySymbols(target);
  return [...props, ...symbols].reduce((carry, key) => {
    if (key === "__proto__")
      return carry;
    if (isArray27(options.props) && !options.props.includes(key)) {
      return carry;
    }
    const val = target[key];
    const newVal = copy7(val, options);
    assignProp7(carry, key, newVal, target, options.nonenumerable);
    return carry;
  }, {});
}
__name(copy7, "copy");
var SuperJSON7 = class {
  static {
    __name(this, "SuperJSON");
  }
  constructor({ dedupe = false } = {}) {
    this.classRegistry = new ClassRegistry7();
    this.symbolRegistry = new Registry7((s) => s.description ?? "");
    this.customTransformerRegistry = new CustomTransformerRegistry7();
    this.allowedErrorProps = [];
    this.dedupe = dedupe;
  }
  serialize(object2) {
    const identities = /* @__PURE__ */ new Map();
    const output = walker7(object2, identities, this, this.dedupe);
    const res = {
      json: output.transformedValue
    };
    if (output.annotations) {
      res.meta = {
        ...res.meta,
        values: output.annotations
      };
    }
    const equalityAnnotations = generateReferentialEqualityAnnotations7(identities, this.dedupe);
    if (equalityAnnotations) {
      res.meta = {
        ...res.meta,
        referentialEqualities: equalityAnnotations
      };
    }
    if (res.meta)
      res.meta.v = 1;
    return res;
  }
  deserialize(payload, options) {
    const { json, meta } = payload;
    let result = options?.inPlace ? json : copy7(json);
    if (meta?.values) {
      result = applyValueAnnotations7(result, meta.values, meta.v ?? 0, this);
    }
    if (meta?.referentialEqualities) {
      result = applyReferentialEqualityAnnotations7(result, meta.referentialEqualities, meta.v ?? 0);
    }
    return result;
  }
  stringify(object2) {
    return JSON.stringify(this.serialize(object2));
  }
  parse(string2) {
    return this.deserialize(JSON.parse(string2), { inPlace: true });
  }
  registerClass(v, options) {
    this.classRegistry.register(v, options);
  }
  registerSymbol(v, identifier) {
    this.symbolRegistry.register(v, identifier);
  }
  registerCustom(transformer, name) {
    this.customTransformerRegistry.register({
      name,
      ...transformer
    });
  }
  allowErrorProps(...props) {
    this.allowedErrorProps.push(...props);
  }
};
SuperJSON7.defaultInstance = new SuperJSON7();
SuperJSON7.serialize = SuperJSON7.defaultInstance.serialize.bind(SuperJSON7.defaultInstance);
SuperJSON7.deserialize = SuperJSON7.defaultInstance.deserialize.bind(SuperJSON7.defaultInstance);
SuperJSON7.stringify = SuperJSON7.defaultInstance.stringify.bind(SuperJSON7.defaultInstance);
SuperJSON7.parse = SuperJSON7.defaultInstance.parse.bind(SuperJSON7.defaultInstance);
SuperJSON7.registerClass = SuperJSON7.defaultInstance.registerClass.bind(SuperJSON7.defaultInstance);
SuperJSON7.registerSymbol = SuperJSON7.defaultInstance.registerSymbol.bind(SuperJSON7.defaultInstance);
SuperJSON7.registerCustom = SuperJSON7.defaultInstance.registerCustom.bind(SuperJSON7.defaultInstance);
SuperJSON7.allowErrorProps = SuperJSON7.defaultInstance.allowErrorProps.bind(SuperJSON7.defaultInstance);
var dist_default7 = SuperJSON7;
var serialize7 = SuperJSON7.serialize;
var deserialize7 = SuperJSON7.deserialize;
var stringify7 = SuperJSON7.stringify;
var parse7 = SuperJSON7.parse;
var registerClass7 = SuperJSON7.registerClass;
var registerCustom7 = SuperJSON7.registerCustom;
var registerSymbol7 = SuperJSON7.registerSymbol;
var allowErrorProps7 = SuperJSON7.allowErrorProps;
function parseData7(formData) {
  const propsArray = [];
  if (!formData)
    return propsArray;
  const batchsIDs = [];
  for (const [key, value] of Array.from(formData.entries())) {
    if (key.startsWith("FILE_"))
      propsArray.push(value);
    else if (key.startsWith("FILES_")) {
      if (batchsIDs.includes(key))
        continue;
      batchsIDs.push(key);
      propsArray.push(formData.getAll(key));
    } else {
      propsArray.push(JSON.parse(decodeURI(value)));
    }
  }
  return propsArray;
}
__name(parseData7, "parseData");
function paramsFromURL7(url) {
  const params = url.searchParams.entries().toArray().map(([_, v]) => v);
  return params.map((param) => JSON.parse(decodeURIComponent(param)));
}
__name(paramsFromURL7, "paramsFromURL");
async function WrapRequestHandler7(context2, endpoint) {
  const isServerAction = context2.request.headers.get("x-server-action") === "true";
  if (!isServerAction) {
    return new Response("Not Found", { status: 404 });
  }
  const parsedData = context2.request.method === "GET" || context2.request.method === "HEAD" ? paramsFromURL7(new URL(context2.request.url)) : parseData7(context2.request.headers.get("content-type") ? await context2.request.formData() : void 0);
  const missingProps = endpoint.length - parsedData.length;
  for (let i = 0; i < missingProps; i++) {
    parsedData.push(void 0);
  }
  parsedData.push(context2);
  const result = await endpoint(...parsedData);
  switch (typeof result) {
    case "string":
    case "number":
    case "boolean":
    case "bigint":
      return new Response(dist_default7.stringify(result), {
        headers: {
          "Content-Type": "application/json",
          dataType: "json"
        }
      });
    case "undefined":
      return new Response(null, { status: 204 });
    case "object":
      if (result instanceof Response) {
        result.headers.set("dataType", "response");
        return result;
      } else if (result instanceof Blob) {
        const res = new Response(await result.arrayBuffer());
        res.headers.set("dataType", "blob");
        res.headers.set("Content-Type", result.type);
        return res;
      } else if (result instanceof File) {
        return new Response(await result.arrayBuffer(), {
          headers: {
            "Content-Type": result.type,
            dataType: "file",
            fileData: JSON.stringify({
              name: result.name,
              lastModified: result.lastModified
            })
          }
        });
      } else {
        return new Response(dist_default7.stringify(result), {
          headers: { "Content-Type": "application/json", dataType: "json" }
        });
      }
    default:
      throw new Error(`Unsupported return type from action: ${typeof result}`);
  }
}
__name(WrapRequestHandler7, "WrapRequestHandler");
var onRequest7 = /* @__PURE__ */ __name(async (context2) => {
  const method = context2.request.method;
  const options = {
    GET: typeof GET === "function" ? GET : void 0,
    POST: typeof POST8 === "function" ? POST8 : void 0,
    PUT: typeof PUT === "function" ? PUT : void 0,
    DELETE: typeof DELETE === "function" ? DELETE : void 0,
    PATCH: typeof PATCH === "function" ? PATCH : void 0,
    HEAD: typeof HEAD === "function" ? HEAD : void 0,
    OPTIONS: typeof OPTIONS === "function" ? OPTIONS : void 0
  };
  if (!options[method]) {
    return new Response(`Method "${method}" Not Allowed`, {
      status: 405
    });
  }
  return await WrapRequestHandler7(context2, options[method]);
}, "onRequest");

// api/invites/project/manage.js
async function POST9({
  user_id,
  client_id,
  from_name
}) {
  const ctx = getContext(arguments);
  const current_user_id = (await ctx.data.client.getMetaData())?.id;
  if (!current_user_id) {
    return {
      success: false,
      error: "Unauthorized"
    };
  } else if (current_user_id === user_id) {
    return {
      success: false,
      error: "You cannot invite yourself"
    };
  }
  const db = drizzle(ctx.env.PROJECT_DB);
  const project = await db.select().from(projectTable).where(and(eq(projectTable.clientID, client_id), eq(projectTable.owner_id, current_user_id))).get();
  if (!project) {
    return {
      success: false,
      error: "Project not found or you don't have permission to invite users"
    };
  }
  const inviteManager = invites(ctx.env.PROJECT_DB);
  const existingInvite = await inviteManager.exists({
    type: "project",
    user_id,
    owner_group_id: project.owner_group_id,
    from_id: current_user_id
  });
  const create = /* @__PURE__ */ __name(() => {
    return inviteManager.create({
      type: "project",
      owner_group_id: project.owner_group_id,
      label: `Invite to Project ${project.name}`,
      from: {
        id: current_user_id,
        name: from_name
      },
      invite_user_id: user_id
    });
  }, "create");
  if (existingInvite) {
    await inviteManager.deleteById(existingInvite.id);
    const newInvite = await create();
    return {
      success: newInvite.success,
      error: newInvite.error
    };
  }
  const res = await create();
  return {
    success: res.success,
    error: res.error
  };
}
__name(POST9, "POST");
async function DELETE5({
  user_id,
  owner_group_id
}) {
  const ctx = getContext(arguments);
  const current_user_id = (await ctx.data.client.getMetaData())?.id;
  if (!current_user_id) {
    return {
      success: false,
      error: "Unauthorized"
    };
  } else if (current_user_id === user_id) {
    return {
      success: false,
      error: "You cannot revoke your own invite"
    };
  }
  const inviteManager = invites(ctx.env.PROJECT_DB);
  const existingInvite = await inviteManager.exists({
    type: "project",
    user_id,
    from_id: current_user_id,
    owner_group_id
  });
  if (existingInvite) {
    await inviteManager.deleteById(existingInvite.id);
  } else {
    return {
      success: false,
      error: "Invite not found"
    };
  }
  return {
    success: true
  };
}
__name(DELETE5, "DELETE");
var DoubleIndexedKV8 = class {
  static {
    __name(this, "DoubleIndexedKV");
  }
  constructor() {
    this.keyToValue = /* @__PURE__ */ new Map();
    this.valueToKey = /* @__PURE__ */ new Map();
  }
  set(key, value) {
    this.keyToValue.set(key, value);
    this.valueToKey.set(value, key);
  }
  getByKey(key) {
    return this.keyToValue.get(key);
  }
  getByValue(value) {
    return this.valueToKey.get(value);
  }
  clear() {
    this.keyToValue.clear();
    this.valueToKey.clear();
  }
};
var Registry8 = class {
  static {
    __name(this, "Registry");
  }
  constructor(generateIdentifier) {
    this.generateIdentifier = generateIdentifier;
    this.kv = new DoubleIndexedKV8();
  }
  register(value, identifier) {
    if (this.kv.getByValue(value)) {
      return;
    }
    if (!identifier) {
      identifier = this.generateIdentifier(value);
    }
    this.kv.set(identifier, value);
  }
  clear() {
    this.kv.clear();
  }
  getIdentifier(value) {
    return this.kv.getByValue(value);
  }
  getValue(identifier) {
    return this.kv.getByKey(identifier);
  }
};
var ClassRegistry8 = class extends Registry8 {
  static {
    __name(this, "ClassRegistry");
  }
  constructor() {
    super((c) => c.name);
    this.classToAllowedProps = /* @__PURE__ */ new Map();
  }
  register(value, options) {
    if (typeof options === "object") {
      if (options.allowProps) {
        this.classToAllowedProps.set(value, options.allowProps);
      }
      super.register(value, options.identifier);
    } else {
      super.register(value, options);
    }
  }
  getAllowedProps(value) {
    return this.classToAllowedProps.get(value);
  }
};
function valuesOfObj8(record) {
  if ("values" in Object) {
    return Object.values(record);
  }
  const values = [];
  for (const key in record) {
    if (record.hasOwnProperty(key)) {
      values.push(record[key]);
    }
  }
  return values;
}
__name(valuesOfObj8, "valuesOfObj");
function find8(record, predicate) {
  const values = valuesOfObj8(record);
  if ("find" in values) {
    return values.find(predicate);
  }
  const valuesNotNever = values;
  for (let i = 0; i < valuesNotNever.length; i++) {
    const value = valuesNotNever[i];
    if (predicate(value)) {
      return value;
    }
  }
  return;
}
__name(find8, "find");
function forEach8(record, run) {
  Object.entries(record).forEach(([key, value]) => run(value, key));
}
__name(forEach8, "forEach");
function includes8(arr, value) {
  return arr.indexOf(value) !== -1;
}
__name(includes8, "includes");
function findArr8(record, predicate) {
  for (let i = 0; i < record.length; i++) {
    const value = record[i];
    if (predicate(value)) {
      return value;
    }
  }
  return;
}
__name(findArr8, "findArr");
var CustomTransformerRegistry8 = class {
  static {
    __name(this, "CustomTransformerRegistry");
  }
  constructor() {
    this.transfomers = {};
  }
  register(transformer) {
    this.transfomers[transformer.name] = transformer;
  }
  findApplicable(v) {
    return find8(this.transfomers, (transformer) => transformer.isApplicable(v));
  }
  findByName(name) {
    return this.transfomers[name];
  }
};
var getType9 = /* @__PURE__ */ __name((payload) => Object.prototype.toString.call(payload).slice(8, -1), "getType");
var isUndefined8 = /* @__PURE__ */ __name((payload) => typeof payload === "undefined", "isUndefined");
var isNull9 = /* @__PURE__ */ __name((payload) => payload === null, "isNull");
var isPlainObject9 = /* @__PURE__ */ __name((payload) => {
  if (typeof payload !== "object" || payload === null)
    return false;
  if (payload === Object.prototype)
    return false;
  if (Object.getPrototypeOf(payload) === null)
    return true;
  return Object.getPrototypeOf(payload) === Object.prototype;
}, "isPlainObject");
var isEmptyObject8 = /* @__PURE__ */ __name((payload) => isPlainObject9(payload) && Object.keys(payload).length === 0, "isEmptyObject");
var isArray9 = /* @__PURE__ */ __name((payload) => Array.isArray(payload), "isArray");
var isString8 = /* @__PURE__ */ __name((payload) => typeof payload === "string", "isString");
var isNumber8 = /* @__PURE__ */ __name((payload) => typeof payload === "number" && !isNaN(payload), "isNumber");
var isBoolean8 = /* @__PURE__ */ __name((payload) => typeof payload === "boolean", "isBoolean");
var isRegExp8 = /* @__PURE__ */ __name((payload) => payload instanceof RegExp, "isRegExp");
var isMap8 = /* @__PURE__ */ __name((payload) => payload instanceof Map, "isMap");
var isSet8 = /* @__PURE__ */ __name((payload) => payload instanceof Set, "isSet");
var isSymbol8 = /* @__PURE__ */ __name((payload) => getType9(payload) === "Symbol", "isSymbol");
var isDate8 = /* @__PURE__ */ __name((payload) => payload instanceof Date && !isNaN(payload.valueOf()), "isDate");
var isError8 = /* @__PURE__ */ __name((payload) => payload instanceof Error, "isError");
var isNaNValue8 = /* @__PURE__ */ __name((payload) => typeof payload === "number" && isNaN(payload), "isNaNValue");
var isPrimitive8 = /* @__PURE__ */ __name((payload) => isBoolean8(payload) || isNull9(payload) || isUndefined8(payload) || isNumber8(payload) || isString8(payload) || isSymbol8(payload), "isPrimitive");
var isBigint8 = /* @__PURE__ */ __name((payload) => typeof payload === "bigint", "isBigint");
var isInfinite8 = /* @__PURE__ */ __name((payload) => payload === Infinity || payload === -Infinity, "isInfinite");
var isTypedArray8 = /* @__PURE__ */ __name((payload) => ArrayBuffer.isView(payload) && !(payload instanceof DataView), "isTypedArray");
var isURL8 = /* @__PURE__ */ __name((payload) => payload instanceof URL, "isURL");
var escapeKey8 = /* @__PURE__ */ __name((key) => key.replace(/\\/g, "\\\\").replace(/\./g, "\\."), "escapeKey");
var stringifyPath8 = /* @__PURE__ */ __name((path) => path.map(String).map(escapeKey8).join("."), "stringifyPath");
var parsePath8 = /* @__PURE__ */ __name((string2, legacyPaths) => {
  const result = [];
  let segment = "";
  for (let i = 0; i < string2.length; i++) {
    let char = string2.charAt(i);
    if (!legacyPaths && char === "\\") {
      const escaped = string2.charAt(i + 1);
      if (escaped === "\\") {
        segment += "\\";
        i++;
        continue;
      } else if (escaped !== ".") {
        throw Error("invalid path");
      }
    }
    const isEscapedDot = char === "\\" && string2.charAt(i + 1) === ".";
    if (isEscapedDot) {
      segment += ".";
      i++;
      continue;
    }
    const isEndOfSegment = char === ".";
    if (isEndOfSegment) {
      result.push(segment);
      segment = "";
      continue;
    }
    segment += char;
  }
  const lastSegment = segment;
  result.push(lastSegment);
  return result;
}, "parsePath");
function simpleTransformation8(isApplicable, annotation, transform, untransform) {
  return {
    isApplicable,
    annotation,
    transform,
    untransform
  };
}
__name(simpleTransformation8, "simpleTransformation");
var simpleRules8 = [
  simpleTransformation8(isUndefined8, "undefined", () => null, () => {
    return;
  }),
  simpleTransformation8(isBigint8, "bigint", (v) => v.toString(), (v) => {
    if (typeof BigInt !== "undefined") {
      return BigInt(v);
    }
    console.error("Please add a BigInt polyfill.");
    return v;
  }),
  simpleTransformation8(isDate8, "Date", (v) => v.toISOString(), (v) => new Date(v)),
  simpleTransformation8(isError8, "Error", (v, superJson) => {
    const baseError = {
      name: v.name,
      message: v.message
    };
    if ("cause" in v) {
      baseError.cause = v.cause;
    }
    superJson.allowedErrorProps.forEach((prop) => {
      baseError[prop] = v[prop];
    });
    return baseError;
  }, (v, superJson) => {
    const e = new Error(v.message, { cause: v.cause });
    e.name = v.name;
    e.stack = v.stack;
    superJson.allowedErrorProps.forEach((prop) => {
      e[prop] = v[prop];
    });
    return e;
  }),
  simpleTransformation8(isRegExp8, "regexp", (v) => "" + v, (regex) => {
    const body = regex.slice(1, regex.lastIndexOf("/"));
    const flags2 = regex.slice(regex.lastIndexOf("/") + 1);
    return new RegExp(body, flags2);
  }),
  simpleTransformation8(isSet8, "set", (v) => [...v.values()], (v) => new Set(v)),
  simpleTransformation8(isMap8, "map", (v) => [...v.entries()], (v) => new Map(v)),
  simpleTransformation8((v) => isNaNValue8(v) || isInfinite8(v), "number", (v) => {
    if (isNaNValue8(v)) {
      return "NaN";
    }
    if (v > 0) {
      return "Infinity";
    } else {
      return "-Infinity";
    }
  }, Number),
  simpleTransformation8((v) => v === 0 && 1 / v === -Infinity, "number", () => {
    return "-0";
  }, Number),
  simpleTransformation8(isURL8, "URL", (v) => v.toString(), (v) => new URL(v))
];
function compositeTransformation8(isApplicable, annotation, transform, untransform) {
  return {
    isApplicable,
    annotation,
    transform,
    untransform
  };
}
__name(compositeTransformation8, "compositeTransformation");
var symbolRule8 = compositeTransformation8((s, superJson) => {
  if (isSymbol8(s)) {
    const isRegistered = !!superJson.symbolRegistry.getIdentifier(s);
    return isRegistered;
  }
  return false;
}, (s, superJson) => {
  const identifier = superJson.symbolRegistry.getIdentifier(s);
  return ["symbol", identifier];
}, (v) => v.description, (_, a, superJson) => {
  const value = superJson.symbolRegistry.getValue(a[1]);
  if (!value) {
    throw new Error("Trying to deserialize unknown symbol");
  }
  return value;
});
var constructorToName8 = [
  Int8Array,
  Uint8Array,
  Int16Array,
  Uint16Array,
  Int32Array,
  Uint32Array,
  Float32Array,
  Float64Array,
  Uint8ClampedArray
].reduce((obj, ctor) => {
  obj[ctor.name] = ctor;
  return obj;
}, {});
var typedArrayRule8 = compositeTransformation8(isTypedArray8, (v) => ["typed-array", v.constructor.name], (v) => [...v], (v, a) => {
  const ctor = constructorToName8[a[1]];
  if (!ctor) {
    throw new Error("Trying to deserialize unknown typed array");
  }
  return new ctor(v);
});
function isInstanceOfRegisteredClass8(potentialClass, superJson) {
  if (potentialClass?.constructor) {
    const isRegistered = !!superJson.classRegistry.getIdentifier(potentialClass.constructor);
    return isRegistered;
  }
  return false;
}
__name(isInstanceOfRegisteredClass8, "isInstanceOfRegisteredClass");
var classRule8 = compositeTransformation8(isInstanceOfRegisteredClass8, (clazz, superJson) => {
  const identifier = superJson.classRegistry.getIdentifier(clazz.constructor);
  return ["class", identifier];
}, (clazz, superJson) => {
  const allowedProps = superJson.classRegistry.getAllowedProps(clazz.constructor);
  if (!allowedProps) {
    return { ...clazz };
  }
  const result = {};
  allowedProps.forEach((prop) => {
    result[prop] = clazz[prop];
  });
  return result;
}, (v, a, superJson) => {
  const clazz = superJson.classRegistry.getValue(a[1]);
  if (!clazz) {
    throw new Error(`Trying to deserialize unknown class '${a[1]}' - check https://github.com/blitz-js/superjson/issues/116#issuecomment-773996564`);
  }
  return Object.assign(Object.create(clazz.prototype), v);
});
var customRule8 = compositeTransformation8((value, superJson) => {
  return !!superJson.customTransformerRegistry.findApplicable(value);
}, (value, superJson) => {
  const transformer = superJson.customTransformerRegistry.findApplicable(value);
  return ["custom", transformer.name];
}, (value, superJson) => {
  const transformer = superJson.customTransformerRegistry.findApplicable(value);
  return transformer.serialize(value);
}, (v, a, superJson) => {
  const transformer = superJson.customTransformerRegistry.findByName(a[1]);
  if (!transformer) {
    throw new Error("Trying to deserialize unknown custom value");
  }
  return transformer.deserialize(v);
});
var compositeRules8 = [classRule8, symbolRule8, customRule8, typedArrayRule8];
var transformValue8 = /* @__PURE__ */ __name((value, superJson) => {
  const applicableCompositeRule = findArr8(compositeRules8, (rule) => rule.isApplicable(value, superJson));
  if (applicableCompositeRule) {
    return {
      value: applicableCompositeRule.transform(value, superJson),
      type: applicableCompositeRule.annotation(value, superJson)
    };
  }
  const applicableSimpleRule = findArr8(simpleRules8, (rule) => rule.isApplicable(value, superJson));
  if (applicableSimpleRule) {
    return {
      value: applicableSimpleRule.transform(value, superJson),
      type: applicableSimpleRule.annotation
    };
  }
  return;
}, "transformValue");
var simpleRulesByAnnotation8 = {};
simpleRules8.forEach((rule) => {
  simpleRulesByAnnotation8[rule.annotation] = rule;
});
var untransformValue8 = /* @__PURE__ */ __name((json, type, superJson) => {
  if (isArray9(type)) {
    switch (type[0]) {
      case "symbol":
        return symbolRule8.untransform(json, type, superJson);
      case "class":
        return classRule8.untransform(json, type, superJson);
      case "custom":
        return customRule8.untransform(json, type, superJson);
      case "typed-array":
        return typedArrayRule8.untransform(json, type, superJson);
      default:
        throw new Error("Unknown transformation: " + type);
    }
  } else {
    const transformation = simpleRulesByAnnotation8[type];
    if (!transformation) {
      throw new Error("Unknown transformation: " + type);
    }
    return transformation.untransform(json, superJson);
  }
}, "untransformValue");
var getNthKey8 = /* @__PURE__ */ __name((value, n) => {
  if (n > value.size)
    throw new Error("index out of bounds");
  const keys = value.keys();
  while (n > 0) {
    keys.next();
    n--;
  }
  return keys.next().value;
}, "getNthKey");
function validatePath8(path) {
  if (includes8(path, "__proto__")) {
    throw new Error("__proto__ is not allowed as a property");
  }
  if (includes8(path, "prototype")) {
    throw new Error("prototype is not allowed as a property");
  }
  if (includes8(path, "constructor")) {
    throw new Error("constructor is not allowed as a property");
  }
}
__name(validatePath8, "validatePath");
var getDeep8 = /* @__PURE__ */ __name((object2, path) => {
  validatePath8(path);
  for (let i = 0; i < path.length; i++) {
    const key = path[i];
    if (isSet8(object2)) {
      object2 = getNthKey8(object2, +key);
    } else if (isMap8(object2)) {
      const row = +key;
      const type = +path[++i] === 0 ? "key" : "value";
      const keyOfRow = getNthKey8(object2, row);
      switch (type) {
        case "key":
          object2 = keyOfRow;
          break;
        case "value":
          object2 = object2.get(keyOfRow);
          break;
      }
    } else {
      object2 = object2[key];
    }
  }
  return object2;
}, "getDeep");
var setDeep8 = /* @__PURE__ */ __name((object2, path, mapper) => {
  validatePath8(path);
  if (path.length === 0) {
    return mapper(object2);
  }
  let parent = object2;
  for (let i = 0; i < path.length - 1; i++) {
    const key = path[i];
    if (isArray9(parent)) {
      const index = +key;
      parent = parent[index];
    } else if (isPlainObject9(parent)) {
      parent = parent[key];
    } else if (isSet8(parent)) {
      const row = +key;
      parent = getNthKey8(parent, row);
    } else if (isMap8(parent)) {
      const isEnd = i === path.length - 2;
      if (isEnd) {
        break;
      }
      const row = +key;
      const type = +path[++i] === 0 ? "key" : "value";
      const keyOfRow = getNthKey8(parent, row);
      switch (type) {
        case "key":
          parent = keyOfRow;
          break;
        case "value":
          parent = parent.get(keyOfRow);
          break;
      }
    }
  }
  const lastKey = path[path.length - 1];
  if (isArray9(parent)) {
    parent[+lastKey] = mapper(parent[+lastKey]);
  } else if (isPlainObject9(parent)) {
    parent[lastKey] = mapper(parent[lastKey]);
  }
  if (isSet8(parent)) {
    const oldValue = getNthKey8(parent, +lastKey);
    const newValue = mapper(oldValue);
    if (oldValue !== newValue) {
      parent.delete(oldValue);
      parent.add(newValue);
    }
  }
  if (isMap8(parent)) {
    const row = +path[path.length - 2];
    const keyToRow = getNthKey8(parent, row);
    const type = +lastKey === 0 ? "key" : "value";
    switch (type) {
      case "key": {
        const newKey = mapper(keyToRow);
        parent.set(newKey, parent.get(keyToRow));
        if (newKey !== keyToRow) {
          parent.delete(keyToRow);
        }
        break;
      }
      case "value": {
        parent.set(keyToRow, mapper(parent.get(keyToRow)));
        break;
      }
    }
  }
  return object2;
}, "setDeep");
var enableLegacyPaths8 = /* @__PURE__ */ __name((version4) => version4 < 1, "enableLegacyPaths");
function traverse8(tree, walker26, version4, origin = []) {
  if (!tree) {
    return;
  }
  const legacyPaths = enableLegacyPaths8(version4);
  if (!isArray9(tree)) {
    forEach8(tree, (subtree, key) => traverse8(subtree, walker26, version4, [
      ...origin,
      ...parsePath8(key, legacyPaths)
    ]));
    return;
  }
  const [nodeValue, children] = tree;
  if (children) {
    forEach8(children, (child, key) => {
      traverse8(child, walker26, version4, [
        ...origin,
        ...parsePath8(key, legacyPaths)
      ]);
    });
  }
  walker26(nodeValue, origin);
}
__name(traverse8, "traverse");
function applyValueAnnotations8(plain, annotations, version4, superJson) {
  traverse8(annotations, (type, path) => {
    plain = setDeep8(plain, path, (v) => untransformValue8(v, type, superJson));
  }, version4);
  return plain;
}
__name(applyValueAnnotations8, "applyValueAnnotations");
function applyReferentialEqualityAnnotations8(plain, annotations, version4) {
  const legacyPaths = enableLegacyPaths8(version4);
  function apply(identicalPaths, path) {
    const object2 = getDeep8(plain, parsePath8(path, legacyPaths));
    identicalPaths.map((path2) => parsePath8(path2, legacyPaths)).forEach((identicalObjectPath) => {
      plain = setDeep8(plain, identicalObjectPath, () => object2);
    });
  }
  __name(apply, "apply");
  if (isArray9(annotations)) {
    const [root, other] = annotations;
    root.forEach((identicalPath) => {
      plain = setDeep8(plain, parsePath8(identicalPath, legacyPaths), () => plain);
    });
    if (other) {
      forEach8(other, apply);
    }
  } else {
    forEach8(annotations, apply);
  }
  return plain;
}
__name(applyReferentialEqualityAnnotations8, "applyReferentialEqualityAnnotations");
var isDeep8 = /* @__PURE__ */ __name((object2, superJson) => isPlainObject9(object2) || isArray9(object2) || isMap8(object2) || isSet8(object2) || isError8(object2) || isInstanceOfRegisteredClass8(object2, superJson), "isDeep");
function addIdentity8(object2, path, identities) {
  const existingSet = identities.get(object2);
  if (existingSet) {
    existingSet.push(path);
  } else {
    identities.set(object2, [path]);
  }
}
__name(addIdentity8, "addIdentity");
function generateReferentialEqualityAnnotations8(identitites, dedupe) {
  const result = {};
  let rootEqualityPaths = void 0;
  identitites.forEach((paths) => {
    if (paths.length <= 1) {
      return;
    }
    if (!dedupe) {
      paths = paths.map((path) => path.map(String)).sort((a, b) => a.length - b.length);
    }
    const [representativePath, ...identicalPaths] = paths;
    if (representativePath.length === 0) {
      rootEqualityPaths = identicalPaths.map(stringifyPath8);
    } else {
      result[stringifyPath8(representativePath)] = identicalPaths.map(stringifyPath8);
    }
  });
  if (rootEqualityPaths) {
    if (isEmptyObject8(result)) {
      return [rootEqualityPaths];
    } else {
      return [rootEqualityPaths, result];
    }
  } else {
    return isEmptyObject8(result) ? void 0 : result;
  }
}
__name(generateReferentialEqualityAnnotations8, "generateReferentialEqualityAnnotations");
var walker8 = /* @__PURE__ */ __name((object2, identities, superJson, dedupe, path = [], objectsInThisPath = [], seenObjects = /* @__PURE__ */ new Map()) => {
  const primitive = isPrimitive8(object2);
  if (!primitive) {
    addIdentity8(object2, path, identities);
    const seen = seenObjects.get(object2);
    if (seen) {
      return dedupe ? {
        transformedValue: null
      } : seen;
    }
  }
  if (!isDeep8(object2, superJson)) {
    const transformed2 = transformValue8(object2, superJson);
    const result2 = transformed2 ? {
      transformedValue: transformed2.value,
      annotations: [transformed2.type]
    } : {
      transformedValue: object2
    };
    if (!primitive) {
      seenObjects.set(object2, result2);
    }
    return result2;
  }
  if (includes8(objectsInThisPath, object2)) {
    return {
      transformedValue: null
    };
  }
  const transformationResult = transformValue8(object2, superJson);
  const transformed = transformationResult?.value ?? object2;
  const transformedValue = isArray9(transformed) ? [] : {};
  const innerAnnotations = {};
  forEach8(transformed, (value, index) => {
    if (index === "__proto__" || index === "constructor" || index === "prototype") {
      throw new Error(`Detected property ${index}. This is a prototype pollution risk, please remove it from your object.`);
    }
    const recursiveResult = walker8(value, identities, superJson, dedupe, [...path, index], [...objectsInThisPath, object2], seenObjects);
    transformedValue[index] = recursiveResult.transformedValue;
    if (isArray9(recursiveResult.annotations)) {
      innerAnnotations[escapeKey8(index)] = recursiveResult.annotations;
    } else if (isPlainObject9(recursiveResult.annotations)) {
      forEach8(recursiveResult.annotations, (tree, key) => {
        innerAnnotations[escapeKey8(index) + "." + key] = tree;
      });
    }
  });
  const result = isEmptyObject8(innerAnnotations) ? {
    transformedValue,
    annotations: transformationResult ? [transformationResult.type] : void 0
  } : {
    transformedValue,
    annotations: transformationResult ? [transformationResult.type, innerAnnotations] : innerAnnotations
  };
  if (!primitive) {
    seenObjects.set(object2, result);
  }
  return result;
}, "walker");
function getType28(payload) {
  return Object.prototype.toString.call(payload).slice(8, -1);
}
__name(getType28, "getType2");
function isArray28(payload) {
  return getType28(payload) === "Array";
}
__name(isArray28, "isArray2");
function isPlainObject28(payload) {
  if (getType28(payload) !== "Object")
    return false;
  const prototype = Object.getPrototypeOf(payload);
  return !!prototype && prototype.constructor === Object && prototype === Object.prototype;
}
__name(isPlainObject28, "isPlainObject2");
function assignProp8(carry, key, newVal, originalObject, includeNonenumerable) {
  const propType = {}.propertyIsEnumerable.call(originalObject, key) ? "enumerable" : "nonenumerable";
  if (propType === "enumerable")
    carry[key] = newVal;
  if (includeNonenumerable && propType === "nonenumerable") {
    Object.defineProperty(carry, key, {
      value: newVal,
      enumerable: false,
      writable: true,
      configurable: true
    });
  }
}
__name(assignProp8, "assignProp");
function copy8(target, options = {}) {
  if (isArray28(target)) {
    return target.map((item) => copy8(item, options));
  }
  if (!isPlainObject28(target)) {
    return target;
  }
  const props = Object.getOwnPropertyNames(target);
  const symbols = Object.getOwnPropertySymbols(target);
  return [...props, ...symbols].reduce((carry, key) => {
    if (key === "__proto__")
      return carry;
    if (isArray28(options.props) && !options.props.includes(key)) {
      return carry;
    }
    const val = target[key];
    const newVal = copy8(val, options);
    assignProp8(carry, key, newVal, target, options.nonenumerable);
    return carry;
  }, {});
}
__name(copy8, "copy");
var SuperJSON8 = class {
  static {
    __name(this, "SuperJSON");
  }
  constructor({ dedupe = false } = {}) {
    this.classRegistry = new ClassRegistry8();
    this.symbolRegistry = new Registry8((s) => s.description ?? "");
    this.customTransformerRegistry = new CustomTransformerRegistry8();
    this.allowedErrorProps = [];
    this.dedupe = dedupe;
  }
  serialize(object2) {
    const identities = /* @__PURE__ */ new Map();
    const output = walker8(object2, identities, this, this.dedupe);
    const res = {
      json: output.transformedValue
    };
    if (output.annotations) {
      res.meta = {
        ...res.meta,
        values: output.annotations
      };
    }
    const equalityAnnotations = generateReferentialEqualityAnnotations8(identities, this.dedupe);
    if (equalityAnnotations) {
      res.meta = {
        ...res.meta,
        referentialEqualities: equalityAnnotations
      };
    }
    if (res.meta)
      res.meta.v = 1;
    return res;
  }
  deserialize(payload, options) {
    const { json, meta } = payload;
    let result = options?.inPlace ? json : copy8(json);
    if (meta?.values) {
      result = applyValueAnnotations8(result, meta.values, meta.v ?? 0, this);
    }
    if (meta?.referentialEqualities) {
      result = applyReferentialEqualityAnnotations8(result, meta.referentialEqualities, meta.v ?? 0);
    }
    return result;
  }
  stringify(object2) {
    return JSON.stringify(this.serialize(object2));
  }
  parse(string2) {
    return this.deserialize(JSON.parse(string2), { inPlace: true });
  }
  registerClass(v, options) {
    this.classRegistry.register(v, options);
  }
  registerSymbol(v, identifier) {
    this.symbolRegistry.register(v, identifier);
  }
  registerCustom(transformer, name) {
    this.customTransformerRegistry.register({
      name,
      ...transformer
    });
  }
  allowErrorProps(...props) {
    this.allowedErrorProps.push(...props);
  }
};
SuperJSON8.defaultInstance = new SuperJSON8();
SuperJSON8.serialize = SuperJSON8.defaultInstance.serialize.bind(SuperJSON8.defaultInstance);
SuperJSON8.deserialize = SuperJSON8.defaultInstance.deserialize.bind(SuperJSON8.defaultInstance);
SuperJSON8.stringify = SuperJSON8.defaultInstance.stringify.bind(SuperJSON8.defaultInstance);
SuperJSON8.parse = SuperJSON8.defaultInstance.parse.bind(SuperJSON8.defaultInstance);
SuperJSON8.registerClass = SuperJSON8.defaultInstance.registerClass.bind(SuperJSON8.defaultInstance);
SuperJSON8.registerSymbol = SuperJSON8.defaultInstance.registerSymbol.bind(SuperJSON8.defaultInstance);
SuperJSON8.registerCustom = SuperJSON8.defaultInstance.registerCustom.bind(SuperJSON8.defaultInstance);
SuperJSON8.allowErrorProps = SuperJSON8.defaultInstance.allowErrorProps.bind(SuperJSON8.defaultInstance);
var dist_default8 = SuperJSON8;
var serialize8 = SuperJSON8.serialize;
var deserialize8 = SuperJSON8.deserialize;
var stringify8 = SuperJSON8.stringify;
var parse8 = SuperJSON8.parse;
var registerClass8 = SuperJSON8.registerClass;
var registerCustom8 = SuperJSON8.registerCustom;
var registerSymbol8 = SuperJSON8.registerSymbol;
var allowErrorProps8 = SuperJSON8.allowErrorProps;
function parseData8(formData) {
  const propsArray = [];
  if (!formData)
    return propsArray;
  const batchsIDs = [];
  for (const [key, value] of Array.from(formData.entries())) {
    if (key.startsWith("FILE_"))
      propsArray.push(value);
    else if (key.startsWith("FILES_")) {
      if (batchsIDs.includes(key))
        continue;
      batchsIDs.push(key);
      propsArray.push(formData.getAll(key));
    } else {
      propsArray.push(JSON.parse(decodeURI(value)));
    }
  }
  return propsArray;
}
__name(parseData8, "parseData");
function paramsFromURL8(url) {
  const params = url.searchParams.entries().toArray().map(([_, v]) => v);
  return params.map((param) => JSON.parse(decodeURIComponent(param)));
}
__name(paramsFromURL8, "paramsFromURL");
async function WrapRequestHandler8(context2, endpoint) {
  const isServerAction = context2.request.headers.get("x-server-action") === "true";
  if (!isServerAction) {
    return new Response("Not Found", { status: 404 });
  }
  const parsedData = context2.request.method === "GET" || context2.request.method === "HEAD" ? paramsFromURL8(new URL(context2.request.url)) : parseData8(context2.request.headers.get("content-type") ? await context2.request.formData() : void 0);
  const missingProps = endpoint.length - parsedData.length;
  for (let i = 0; i < missingProps; i++) {
    parsedData.push(void 0);
  }
  parsedData.push(context2);
  const result = await endpoint(...parsedData);
  switch (typeof result) {
    case "string":
    case "number":
    case "boolean":
    case "bigint":
      return new Response(dist_default8.stringify(result), {
        headers: {
          "Content-Type": "application/json",
          dataType: "json"
        }
      });
    case "undefined":
      return new Response(null, { status: 204 });
    case "object":
      if (result instanceof Response) {
        result.headers.set("dataType", "response");
        return result;
      } else if (result instanceof Blob) {
        const res = new Response(await result.arrayBuffer());
        res.headers.set("dataType", "blob");
        res.headers.set("Content-Type", result.type);
        return res;
      } else if (result instanceof File) {
        return new Response(await result.arrayBuffer(), {
          headers: {
            "Content-Type": result.type,
            dataType: "file",
            fileData: JSON.stringify({
              name: result.name,
              lastModified: result.lastModified
            })
          }
        });
      } else {
        return new Response(dist_default8.stringify(result), {
          headers: { "Content-Type": "application/json", dataType: "json" }
        });
      }
    default:
      throw new Error(`Unsupported return type from action: ${typeof result}`);
  }
}
__name(WrapRequestHandler8, "WrapRequestHandler");
var onRequest8 = /* @__PURE__ */ __name(async (context2) => {
  const method = context2.request.method;
  const options = {
    GET: typeof GET === "function" ? GET : void 0,
    POST: typeof POST9 === "function" ? POST9 : void 0,
    PUT: typeof PUT === "function" ? PUT : void 0,
    DELETE: typeof DELETE5 === "function" ? DELETE5 : void 0,
    PATCH: typeof PATCH === "function" ? PATCH : void 0,
    HEAD: typeof HEAD === "function" ? HEAD : void 0,
    OPTIONS: typeof OPTIONS === "function" ? OPTIONS : void 0
  };
  if (!options[method]) {
    return new Response(`Method "${method}" Not Allowed`, {
      status: 405
    });
  }
  return await WrapRequestHandler8(context2, options[method]);
}, "onRequest");

// chunk-rjcv1pwj.js
function ownerGroupConditions({
  user_group_ids,
  ownerGroupIdColumn,
  otherEq = [],
  self_host
}) {
  const isSelfHosted = self_host === "true" || self_host === true;
  return isSelfHosted ? void 0 : or(...otherEq, ...user_group_ids.map((groupId) => eq(ownerGroupIdColumn, groupId)));
}
__name(ownerGroupConditions, "ownerGroupConditions");
function onSelfHosted(selfHosted, selfHostCallback, otherWiseCallback) {
  const isSelfHosted = selfHosted === "true" || selfHosted === true;
  if (isSelfHosted) {
    return typeof selfHostCallback === "function" ? selfHostCallback() : selfHostCallback;
  } else
    return typeof otherWiseCallback === "function" ? otherWiseCallback() : otherWiseCallback;
}
__name(onSelfHosted, "onSelfHosted");

// api/templates/copy/id.js
async function GET2(params) {
  const ctx = getContext(arguments);
  const { env: env2 } = ctx;
  const session = await ctx.data.client.getUserSession("private");
  if (session instanceof Error) {
    return {
      success: false,
      error: "Unauthorized"
    };
  }
  const db = drizzle(env2.PROJECT_DB);
  const template = await db.select().from(WebUiCopyTemplateTable).where(and(eq(WebUiCopyTemplateTable.name, params.name), ownerGroupConditions({
    user_group_ids: session?.private?.group_ids ?? [],
    ownerGroupIdColumn: WebUiCopyTemplateTable.owner_group_id,
    otherEq: [eq(WebUiCopyTemplateTable.owner_id, session.user_id)],
    self_host: env2.SELF_HOSTED
  }))).limit(1).get();
  if (!template)
    return {
      success: false,
      error: "Copy template not found"
    };
  return {
    success: true,
    data: parseDBCopyTemplate(template)
  };
}
__name(GET2, "GET");
async function PUT2(params) {
  const ctx = getContext(arguments);
  const { env: env2 } = ctx;
  try {
    const session = await ctx.data.client.getUserSession("private");
    if (session instanceof Error) {
      return {
        success: false,
        error: "Unauthorized"
      };
    }
    const db = drizzle(env2.PROJECT_DB);
    const existing = await db.select({ id: WebUiCopyTemplateTable.id }).from(WebUiCopyTemplateTable).where(and(eq(WebUiCopyTemplateTable.name, params.name), ownerGroupConditions({
      user_group_ids: session?.private?.group_ids ?? [],
      ownerGroupIdColumn: WebUiCopyTemplateTable.owner_group_id,
      otherEq: [eq(WebUiCopyTemplateTable.owner_id, session.user_id)],
      self_host: env2.SELF_HOSTED
    }))).limit(1).get();
    if (!existing) {
      return {
        success: false,
        error: "Copy template not found"
      };
    }
    const updateData = {
      updated_at: (/* @__PURE__ */ new Date()).toISOString()
    };
    if (params.data.copyData !== void 0) {
      updateData.copyData = params.data.copyData;
    }
    await db.update(WebUiCopyTemplateTable).set(updateData).where(and(eq(WebUiCopyTemplateTable.name, params.name), eq(WebUiCopyTemplateTable.id, existing.id)));
    const updated = await db.select().from(WebUiCopyTemplateTable).where(and(eq(WebUiCopyTemplateTable.name, params.name), eq(WebUiCopyTemplateTable.id, existing.id))).limit(1).get();
    return {
      success: true,
      data: updated ? parseDBCopyTemplate(updated) : void 0
    };
  } catch (err) {
    return {
      success: false,
      error: err instanceof Error ? err.message : "Failed to update copy template"
    };
  }
}
__name(PUT2, "PUT");
async function DELETE6(params) {
  const ctx = getContext(arguments);
  const { env: env2 } = ctx;
  try {
    const session = await ctx.data.client.getUserSession("private");
    if (session instanceof Error) {
      return {
        success: false,
        error: "Unauthorized"
      };
    }
    const db = drizzle(env2.PROJECT_DB);
    const existing = await db.select({ id: WebUiCopyTemplateTable.id }).from(WebUiCopyTemplateTable).where(and(eq(WebUiCopyTemplateTable.name, params.name), ownerGroupConditions({
      user_group_ids: session?.private?.group_ids ?? [],
      ownerGroupIdColumn: WebUiCopyTemplateTable.owner_group_id,
      otherEq: [eq(WebUiCopyTemplateTable.owner_id, session.user_id)],
      self_host: env2.SELF_HOSTED
    }))).limit(1).get();
    if (!existing) {
      return {
        success: false,
        error: "Copy template not found"
      };
    }
    await db.delete(WebUiCopyTemplateTable).where(and(eq(WebUiCopyTemplateTable.name, params.name), eq(WebUiCopyTemplateTable.id, existing.id)));
    return { success: true };
  } catch (err) {
    return {
      success: false,
      error: err instanceof Error ? err.message : "Failed to delete copy template"
    };
  }
}
__name(DELETE6, "DELETE");
var DoubleIndexedKV9 = class {
  static {
    __name(this, "DoubleIndexedKV");
  }
  constructor() {
    this.keyToValue = /* @__PURE__ */ new Map();
    this.valueToKey = /* @__PURE__ */ new Map();
  }
  set(key, value) {
    this.keyToValue.set(key, value);
    this.valueToKey.set(value, key);
  }
  getByKey(key) {
    return this.keyToValue.get(key);
  }
  getByValue(value) {
    return this.valueToKey.get(value);
  }
  clear() {
    this.keyToValue.clear();
    this.valueToKey.clear();
  }
};
var Registry9 = class {
  static {
    __name(this, "Registry");
  }
  constructor(generateIdentifier) {
    this.generateIdentifier = generateIdentifier;
    this.kv = new DoubleIndexedKV9();
  }
  register(value, identifier) {
    if (this.kv.getByValue(value)) {
      return;
    }
    if (!identifier) {
      identifier = this.generateIdentifier(value);
    }
    this.kv.set(identifier, value);
  }
  clear() {
    this.kv.clear();
  }
  getIdentifier(value) {
    return this.kv.getByValue(value);
  }
  getValue(identifier) {
    return this.kv.getByKey(identifier);
  }
};
var ClassRegistry9 = class extends Registry9 {
  static {
    __name(this, "ClassRegistry");
  }
  constructor() {
    super((c) => c.name);
    this.classToAllowedProps = /* @__PURE__ */ new Map();
  }
  register(value, options) {
    if (typeof options === "object") {
      if (options.allowProps) {
        this.classToAllowedProps.set(value, options.allowProps);
      }
      super.register(value, options.identifier);
    } else {
      super.register(value, options);
    }
  }
  getAllowedProps(value) {
    return this.classToAllowedProps.get(value);
  }
};
function valuesOfObj9(record) {
  if ("values" in Object) {
    return Object.values(record);
  }
  const values = [];
  for (const key in record) {
    if (record.hasOwnProperty(key)) {
      values.push(record[key]);
    }
  }
  return values;
}
__name(valuesOfObj9, "valuesOfObj");
function find9(record, predicate) {
  const values = valuesOfObj9(record);
  if ("find" in values) {
    return values.find(predicate);
  }
  const valuesNotNever = values;
  for (let i = 0; i < valuesNotNever.length; i++) {
    const value = valuesNotNever[i];
    if (predicate(value)) {
      return value;
    }
  }
  return;
}
__name(find9, "find");
function forEach9(record, run) {
  Object.entries(record).forEach(([key, value]) => run(value, key));
}
__name(forEach9, "forEach");
function includes9(arr, value) {
  return arr.indexOf(value) !== -1;
}
__name(includes9, "includes");
function findArr9(record, predicate) {
  for (let i = 0; i < record.length; i++) {
    const value = record[i];
    if (predicate(value)) {
      return value;
    }
  }
  return;
}
__name(findArr9, "findArr");
var CustomTransformerRegistry9 = class {
  static {
    __name(this, "CustomTransformerRegistry");
  }
  constructor() {
    this.transfomers = {};
  }
  register(transformer) {
    this.transfomers[transformer.name] = transformer;
  }
  findApplicable(v) {
    return find9(this.transfomers, (transformer) => transformer.isApplicable(v));
  }
  findByName(name) {
    return this.transfomers[name];
  }
};
var getType10 = /* @__PURE__ */ __name((payload) => Object.prototype.toString.call(payload).slice(8, -1), "getType");
var isUndefined9 = /* @__PURE__ */ __name((payload) => typeof payload === "undefined", "isUndefined");
var isNull10 = /* @__PURE__ */ __name((payload) => payload === null, "isNull");
var isPlainObject10 = /* @__PURE__ */ __name((payload) => {
  if (typeof payload !== "object" || payload === null)
    return false;
  if (payload === Object.prototype)
    return false;
  if (Object.getPrototypeOf(payload) === null)
    return true;
  return Object.getPrototypeOf(payload) === Object.prototype;
}, "isPlainObject");
var isEmptyObject9 = /* @__PURE__ */ __name((payload) => isPlainObject10(payload) && Object.keys(payload).length === 0, "isEmptyObject");
var isArray10 = /* @__PURE__ */ __name((payload) => Array.isArray(payload), "isArray");
var isString9 = /* @__PURE__ */ __name((payload) => typeof payload === "string", "isString");
var isNumber9 = /* @__PURE__ */ __name((payload) => typeof payload === "number" && !isNaN(payload), "isNumber");
var isBoolean9 = /* @__PURE__ */ __name((payload) => typeof payload === "boolean", "isBoolean");
var isRegExp9 = /* @__PURE__ */ __name((payload) => payload instanceof RegExp, "isRegExp");
var isMap9 = /* @__PURE__ */ __name((payload) => payload instanceof Map, "isMap");
var isSet9 = /* @__PURE__ */ __name((payload) => payload instanceof Set, "isSet");
var isSymbol9 = /* @__PURE__ */ __name((payload) => getType10(payload) === "Symbol", "isSymbol");
var isDate9 = /* @__PURE__ */ __name((payload) => payload instanceof Date && !isNaN(payload.valueOf()), "isDate");
var isError9 = /* @__PURE__ */ __name((payload) => payload instanceof Error, "isError");
var isNaNValue9 = /* @__PURE__ */ __name((payload) => typeof payload === "number" && isNaN(payload), "isNaNValue");
var isPrimitive9 = /* @__PURE__ */ __name((payload) => isBoolean9(payload) || isNull10(payload) || isUndefined9(payload) || isNumber9(payload) || isString9(payload) || isSymbol9(payload), "isPrimitive");
var isBigint9 = /* @__PURE__ */ __name((payload) => typeof payload === "bigint", "isBigint");
var isInfinite9 = /* @__PURE__ */ __name((payload) => payload === Infinity || payload === -Infinity, "isInfinite");
var isTypedArray9 = /* @__PURE__ */ __name((payload) => ArrayBuffer.isView(payload) && !(payload instanceof DataView), "isTypedArray");
var isURL9 = /* @__PURE__ */ __name((payload) => payload instanceof URL, "isURL");
var escapeKey9 = /* @__PURE__ */ __name((key) => key.replace(/\\/g, "\\\\").replace(/\./g, "\\."), "escapeKey");
var stringifyPath9 = /* @__PURE__ */ __name((path) => path.map(String).map(escapeKey9).join("."), "stringifyPath");
var parsePath9 = /* @__PURE__ */ __name((string2, legacyPaths) => {
  const result = [];
  let segment = "";
  for (let i = 0; i < string2.length; i++) {
    let char = string2.charAt(i);
    if (!legacyPaths && char === "\\") {
      const escaped = string2.charAt(i + 1);
      if (escaped === "\\") {
        segment += "\\";
        i++;
        continue;
      } else if (escaped !== ".") {
        throw Error("invalid path");
      }
    }
    const isEscapedDot = char === "\\" && string2.charAt(i + 1) === ".";
    if (isEscapedDot) {
      segment += ".";
      i++;
      continue;
    }
    const isEndOfSegment = char === ".";
    if (isEndOfSegment) {
      result.push(segment);
      segment = "";
      continue;
    }
    segment += char;
  }
  const lastSegment = segment;
  result.push(lastSegment);
  return result;
}, "parsePath");
function simpleTransformation9(isApplicable, annotation, transform, untransform) {
  return {
    isApplicable,
    annotation,
    transform,
    untransform
  };
}
__name(simpleTransformation9, "simpleTransformation");
var simpleRules9 = [
  simpleTransformation9(isUndefined9, "undefined", () => null, () => {
    return;
  }),
  simpleTransformation9(isBigint9, "bigint", (v) => v.toString(), (v) => {
    if (typeof BigInt !== "undefined") {
      return BigInt(v);
    }
    console.error("Please add a BigInt polyfill.");
    return v;
  }),
  simpleTransformation9(isDate9, "Date", (v) => v.toISOString(), (v) => new Date(v)),
  simpleTransformation9(isError9, "Error", (v, superJson) => {
    const baseError = {
      name: v.name,
      message: v.message
    };
    if ("cause" in v) {
      baseError.cause = v.cause;
    }
    superJson.allowedErrorProps.forEach((prop) => {
      baseError[prop] = v[prop];
    });
    return baseError;
  }, (v, superJson) => {
    const e = new Error(v.message, { cause: v.cause });
    e.name = v.name;
    e.stack = v.stack;
    superJson.allowedErrorProps.forEach((prop) => {
      e[prop] = v[prop];
    });
    return e;
  }),
  simpleTransformation9(isRegExp9, "regexp", (v) => "" + v, (regex) => {
    const body = regex.slice(1, regex.lastIndexOf("/"));
    const flags2 = regex.slice(regex.lastIndexOf("/") + 1);
    return new RegExp(body, flags2);
  }),
  simpleTransformation9(isSet9, "set", (v) => [...v.values()], (v) => new Set(v)),
  simpleTransformation9(isMap9, "map", (v) => [...v.entries()], (v) => new Map(v)),
  simpleTransformation9((v) => isNaNValue9(v) || isInfinite9(v), "number", (v) => {
    if (isNaNValue9(v)) {
      return "NaN";
    }
    if (v > 0) {
      return "Infinity";
    } else {
      return "-Infinity";
    }
  }, Number),
  simpleTransformation9((v) => v === 0 && 1 / v === -Infinity, "number", () => {
    return "-0";
  }, Number),
  simpleTransformation9(isURL9, "URL", (v) => v.toString(), (v) => new URL(v))
];
function compositeTransformation9(isApplicable, annotation, transform, untransform) {
  return {
    isApplicable,
    annotation,
    transform,
    untransform
  };
}
__name(compositeTransformation9, "compositeTransformation");
var symbolRule9 = compositeTransformation9((s, superJson) => {
  if (isSymbol9(s)) {
    const isRegistered = !!superJson.symbolRegistry.getIdentifier(s);
    return isRegistered;
  }
  return false;
}, (s, superJson) => {
  const identifier = superJson.symbolRegistry.getIdentifier(s);
  return ["symbol", identifier];
}, (v) => v.description, (_, a, superJson) => {
  const value = superJson.symbolRegistry.getValue(a[1]);
  if (!value) {
    throw new Error("Trying to deserialize unknown symbol");
  }
  return value;
});
var constructorToName9 = [
  Int8Array,
  Uint8Array,
  Int16Array,
  Uint16Array,
  Int32Array,
  Uint32Array,
  Float32Array,
  Float64Array,
  Uint8ClampedArray
].reduce((obj, ctor) => {
  obj[ctor.name] = ctor;
  return obj;
}, {});
var typedArrayRule9 = compositeTransformation9(isTypedArray9, (v) => ["typed-array", v.constructor.name], (v) => [...v], (v, a) => {
  const ctor = constructorToName9[a[1]];
  if (!ctor) {
    throw new Error("Trying to deserialize unknown typed array");
  }
  return new ctor(v);
});
function isInstanceOfRegisteredClass9(potentialClass, superJson) {
  if (potentialClass?.constructor) {
    const isRegistered = !!superJson.classRegistry.getIdentifier(potentialClass.constructor);
    return isRegistered;
  }
  return false;
}
__name(isInstanceOfRegisteredClass9, "isInstanceOfRegisteredClass");
var classRule9 = compositeTransformation9(isInstanceOfRegisteredClass9, (clazz, superJson) => {
  const identifier = superJson.classRegistry.getIdentifier(clazz.constructor);
  return ["class", identifier];
}, (clazz, superJson) => {
  const allowedProps = superJson.classRegistry.getAllowedProps(clazz.constructor);
  if (!allowedProps) {
    return { ...clazz };
  }
  const result = {};
  allowedProps.forEach((prop) => {
    result[prop] = clazz[prop];
  });
  return result;
}, (v, a, superJson) => {
  const clazz = superJson.classRegistry.getValue(a[1]);
  if (!clazz) {
    throw new Error(`Trying to deserialize unknown class '${a[1]}' - check https://github.com/blitz-js/superjson/issues/116#issuecomment-773996564`);
  }
  return Object.assign(Object.create(clazz.prototype), v);
});
var customRule9 = compositeTransformation9((value, superJson) => {
  return !!superJson.customTransformerRegistry.findApplicable(value);
}, (value, superJson) => {
  const transformer = superJson.customTransformerRegistry.findApplicable(value);
  return ["custom", transformer.name];
}, (value, superJson) => {
  const transformer = superJson.customTransformerRegistry.findApplicable(value);
  return transformer.serialize(value);
}, (v, a, superJson) => {
  const transformer = superJson.customTransformerRegistry.findByName(a[1]);
  if (!transformer) {
    throw new Error("Trying to deserialize unknown custom value");
  }
  return transformer.deserialize(v);
});
var compositeRules9 = [classRule9, symbolRule9, customRule9, typedArrayRule9];
var transformValue9 = /* @__PURE__ */ __name((value, superJson) => {
  const applicableCompositeRule = findArr9(compositeRules9, (rule) => rule.isApplicable(value, superJson));
  if (applicableCompositeRule) {
    return {
      value: applicableCompositeRule.transform(value, superJson),
      type: applicableCompositeRule.annotation(value, superJson)
    };
  }
  const applicableSimpleRule = findArr9(simpleRules9, (rule) => rule.isApplicable(value, superJson));
  if (applicableSimpleRule) {
    return {
      value: applicableSimpleRule.transform(value, superJson),
      type: applicableSimpleRule.annotation
    };
  }
  return;
}, "transformValue");
var simpleRulesByAnnotation9 = {};
simpleRules9.forEach((rule) => {
  simpleRulesByAnnotation9[rule.annotation] = rule;
});
var untransformValue9 = /* @__PURE__ */ __name((json, type, superJson) => {
  if (isArray10(type)) {
    switch (type[0]) {
      case "symbol":
        return symbolRule9.untransform(json, type, superJson);
      case "class":
        return classRule9.untransform(json, type, superJson);
      case "custom":
        return customRule9.untransform(json, type, superJson);
      case "typed-array":
        return typedArrayRule9.untransform(json, type, superJson);
      default:
        throw new Error("Unknown transformation: " + type);
    }
  } else {
    const transformation = simpleRulesByAnnotation9[type];
    if (!transformation) {
      throw new Error("Unknown transformation: " + type);
    }
    return transformation.untransform(json, superJson);
  }
}, "untransformValue");
var getNthKey9 = /* @__PURE__ */ __name((value, n) => {
  if (n > value.size)
    throw new Error("index out of bounds");
  const keys = value.keys();
  while (n > 0) {
    keys.next();
    n--;
  }
  return keys.next().value;
}, "getNthKey");
function validatePath9(path) {
  if (includes9(path, "__proto__")) {
    throw new Error("__proto__ is not allowed as a property");
  }
  if (includes9(path, "prototype")) {
    throw new Error("prototype is not allowed as a property");
  }
  if (includes9(path, "constructor")) {
    throw new Error("constructor is not allowed as a property");
  }
}
__name(validatePath9, "validatePath");
var getDeep9 = /* @__PURE__ */ __name((object2, path) => {
  validatePath9(path);
  for (let i = 0; i < path.length; i++) {
    const key = path[i];
    if (isSet9(object2)) {
      object2 = getNthKey9(object2, +key);
    } else if (isMap9(object2)) {
      const row = +key;
      const type = +path[++i] === 0 ? "key" : "value";
      const keyOfRow = getNthKey9(object2, row);
      switch (type) {
        case "key":
          object2 = keyOfRow;
          break;
        case "value":
          object2 = object2.get(keyOfRow);
          break;
      }
    } else {
      object2 = object2[key];
    }
  }
  return object2;
}, "getDeep");
var setDeep9 = /* @__PURE__ */ __name((object2, path, mapper) => {
  validatePath9(path);
  if (path.length === 0) {
    return mapper(object2);
  }
  let parent = object2;
  for (let i = 0; i < path.length - 1; i++) {
    const key = path[i];
    if (isArray10(parent)) {
      const index = +key;
      parent = parent[index];
    } else if (isPlainObject10(parent)) {
      parent = parent[key];
    } else if (isSet9(parent)) {
      const row = +key;
      parent = getNthKey9(parent, row);
    } else if (isMap9(parent)) {
      const isEnd = i === path.length - 2;
      if (isEnd) {
        break;
      }
      const row = +key;
      const type = +path[++i] === 0 ? "key" : "value";
      const keyOfRow = getNthKey9(parent, row);
      switch (type) {
        case "key":
          parent = keyOfRow;
          break;
        case "value":
          parent = parent.get(keyOfRow);
          break;
      }
    }
  }
  const lastKey = path[path.length - 1];
  if (isArray10(parent)) {
    parent[+lastKey] = mapper(parent[+lastKey]);
  } else if (isPlainObject10(parent)) {
    parent[lastKey] = mapper(parent[lastKey]);
  }
  if (isSet9(parent)) {
    const oldValue = getNthKey9(parent, +lastKey);
    const newValue = mapper(oldValue);
    if (oldValue !== newValue) {
      parent.delete(oldValue);
      parent.add(newValue);
    }
  }
  if (isMap9(parent)) {
    const row = +path[path.length - 2];
    const keyToRow = getNthKey9(parent, row);
    const type = +lastKey === 0 ? "key" : "value";
    switch (type) {
      case "key": {
        const newKey = mapper(keyToRow);
        parent.set(newKey, parent.get(keyToRow));
        if (newKey !== keyToRow) {
          parent.delete(keyToRow);
        }
        break;
      }
      case "value": {
        parent.set(keyToRow, mapper(parent.get(keyToRow)));
        break;
      }
    }
  }
  return object2;
}, "setDeep");
var enableLegacyPaths9 = /* @__PURE__ */ __name((version4) => version4 < 1, "enableLegacyPaths");
function traverse9(tree, walker26, version4, origin = []) {
  if (!tree) {
    return;
  }
  const legacyPaths = enableLegacyPaths9(version4);
  if (!isArray10(tree)) {
    forEach9(tree, (subtree, key) => traverse9(subtree, walker26, version4, [
      ...origin,
      ...parsePath9(key, legacyPaths)
    ]));
    return;
  }
  const [nodeValue, children] = tree;
  if (children) {
    forEach9(children, (child, key) => {
      traverse9(child, walker26, version4, [
        ...origin,
        ...parsePath9(key, legacyPaths)
      ]);
    });
  }
  walker26(nodeValue, origin);
}
__name(traverse9, "traverse");
function applyValueAnnotations9(plain, annotations, version4, superJson) {
  traverse9(annotations, (type, path) => {
    plain = setDeep9(plain, path, (v) => untransformValue9(v, type, superJson));
  }, version4);
  return plain;
}
__name(applyValueAnnotations9, "applyValueAnnotations");
function applyReferentialEqualityAnnotations9(plain, annotations, version4) {
  const legacyPaths = enableLegacyPaths9(version4);
  function apply(identicalPaths, path) {
    const object2 = getDeep9(plain, parsePath9(path, legacyPaths));
    identicalPaths.map((path2) => parsePath9(path2, legacyPaths)).forEach((identicalObjectPath) => {
      plain = setDeep9(plain, identicalObjectPath, () => object2);
    });
  }
  __name(apply, "apply");
  if (isArray10(annotations)) {
    const [root, other] = annotations;
    root.forEach((identicalPath) => {
      plain = setDeep9(plain, parsePath9(identicalPath, legacyPaths), () => plain);
    });
    if (other) {
      forEach9(other, apply);
    }
  } else {
    forEach9(annotations, apply);
  }
  return plain;
}
__name(applyReferentialEqualityAnnotations9, "applyReferentialEqualityAnnotations");
var isDeep9 = /* @__PURE__ */ __name((object2, superJson) => isPlainObject10(object2) || isArray10(object2) || isMap9(object2) || isSet9(object2) || isError9(object2) || isInstanceOfRegisteredClass9(object2, superJson), "isDeep");
function addIdentity9(object2, path, identities) {
  const existingSet = identities.get(object2);
  if (existingSet) {
    existingSet.push(path);
  } else {
    identities.set(object2, [path]);
  }
}
__name(addIdentity9, "addIdentity");
function generateReferentialEqualityAnnotations9(identitites, dedupe) {
  const result = {};
  let rootEqualityPaths = void 0;
  identitites.forEach((paths) => {
    if (paths.length <= 1) {
      return;
    }
    if (!dedupe) {
      paths = paths.map((path) => path.map(String)).sort((a, b) => a.length - b.length);
    }
    const [representativePath, ...identicalPaths] = paths;
    if (representativePath.length === 0) {
      rootEqualityPaths = identicalPaths.map(stringifyPath9);
    } else {
      result[stringifyPath9(representativePath)] = identicalPaths.map(stringifyPath9);
    }
  });
  if (rootEqualityPaths) {
    if (isEmptyObject9(result)) {
      return [rootEqualityPaths];
    } else {
      return [rootEqualityPaths, result];
    }
  } else {
    return isEmptyObject9(result) ? void 0 : result;
  }
}
__name(generateReferentialEqualityAnnotations9, "generateReferentialEqualityAnnotations");
var walker9 = /* @__PURE__ */ __name((object2, identities, superJson, dedupe, path = [], objectsInThisPath = [], seenObjects = /* @__PURE__ */ new Map()) => {
  const primitive = isPrimitive9(object2);
  if (!primitive) {
    addIdentity9(object2, path, identities);
    const seen = seenObjects.get(object2);
    if (seen) {
      return dedupe ? {
        transformedValue: null
      } : seen;
    }
  }
  if (!isDeep9(object2, superJson)) {
    const transformed2 = transformValue9(object2, superJson);
    const result2 = transformed2 ? {
      transformedValue: transformed2.value,
      annotations: [transformed2.type]
    } : {
      transformedValue: object2
    };
    if (!primitive) {
      seenObjects.set(object2, result2);
    }
    return result2;
  }
  if (includes9(objectsInThisPath, object2)) {
    return {
      transformedValue: null
    };
  }
  const transformationResult = transformValue9(object2, superJson);
  const transformed = transformationResult?.value ?? object2;
  const transformedValue = isArray10(transformed) ? [] : {};
  const innerAnnotations = {};
  forEach9(transformed, (value, index) => {
    if (index === "__proto__" || index === "constructor" || index === "prototype") {
      throw new Error(`Detected property ${index}. This is a prototype pollution risk, please remove it from your object.`);
    }
    const recursiveResult = walker9(value, identities, superJson, dedupe, [...path, index], [...objectsInThisPath, object2], seenObjects);
    transformedValue[index] = recursiveResult.transformedValue;
    if (isArray10(recursiveResult.annotations)) {
      innerAnnotations[escapeKey9(index)] = recursiveResult.annotations;
    } else if (isPlainObject10(recursiveResult.annotations)) {
      forEach9(recursiveResult.annotations, (tree, key) => {
        innerAnnotations[escapeKey9(index) + "." + key] = tree;
      });
    }
  });
  const result = isEmptyObject9(innerAnnotations) ? {
    transformedValue,
    annotations: transformationResult ? [transformationResult.type] : void 0
  } : {
    transformedValue,
    annotations: transformationResult ? [transformationResult.type, innerAnnotations] : innerAnnotations
  };
  if (!primitive) {
    seenObjects.set(object2, result);
  }
  return result;
}, "walker");
function getType29(payload) {
  return Object.prototype.toString.call(payload).slice(8, -1);
}
__name(getType29, "getType2");
function isArray29(payload) {
  return getType29(payload) === "Array";
}
__name(isArray29, "isArray2");
function isPlainObject29(payload) {
  if (getType29(payload) !== "Object")
    return false;
  const prototype = Object.getPrototypeOf(payload);
  return !!prototype && prototype.constructor === Object && prototype === Object.prototype;
}
__name(isPlainObject29, "isPlainObject2");
function assignProp9(carry, key, newVal, originalObject, includeNonenumerable) {
  const propType = {}.propertyIsEnumerable.call(originalObject, key) ? "enumerable" : "nonenumerable";
  if (propType === "enumerable")
    carry[key] = newVal;
  if (includeNonenumerable && propType === "nonenumerable") {
    Object.defineProperty(carry, key, {
      value: newVal,
      enumerable: false,
      writable: true,
      configurable: true
    });
  }
}
__name(assignProp9, "assignProp");
function copy9(target, options = {}) {
  if (isArray29(target)) {
    return target.map((item) => copy9(item, options));
  }
  if (!isPlainObject29(target)) {
    return target;
  }
  const props = Object.getOwnPropertyNames(target);
  const symbols = Object.getOwnPropertySymbols(target);
  return [...props, ...symbols].reduce((carry, key) => {
    if (key === "__proto__")
      return carry;
    if (isArray29(options.props) && !options.props.includes(key)) {
      return carry;
    }
    const val = target[key];
    const newVal = copy9(val, options);
    assignProp9(carry, key, newVal, target, options.nonenumerable);
    return carry;
  }, {});
}
__name(copy9, "copy");
var SuperJSON9 = class {
  static {
    __name(this, "SuperJSON");
  }
  constructor({ dedupe = false } = {}) {
    this.classRegistry = new ClassRegistry9();
    this.symbolRegistry = new Registry9((s) => s.description ?? "");
    this.customTransformerRegistry = new CustomTransformerRegistry9();
    this.allowedErrorProps = [];
    this.dedupe = dedupe;
  }
  serialize(object2) {
    const identities = /* @__PURE__ */ new Map();
    const output = walker9(object2, identities, this, this.dedupe);
    const res = {
      json: output.transformedValue
    };
    if (output.annotations) {
      res.meta = {
        ...res.meta,
        values: output.annotations
      };
    }
    const equalityAnnotations = generateReferentialEqualityAnnotations9(identities, this.dedupe);
    if (equalityAnnotations) {
      res.meta = {
        ...res.meta,
        referentialEqualities: equalityAnnotations
      };
    }
    if (res.meta)
      res.meta.v = 1;
    return res;
  }
  deserialize(payload, options) {
    const { json, meta } = payload;
    let result = options?.inPlace ? json : copy9(json);
    if (meta?.values) {
      result = applyValueAnnotations9(result, meta.values, meta.v ?? 0, this);
    }
    if (meta?.referentialEqualities) {
      result = applyReferentialEqualityAnnotations9(result, meta.referentialEqualities, meta.v ?? 0);
    }
    return result;
  }
  stringify(object2) {
    return JSON.stringify(this.serialize(object2));
  }
  parse(string2) {
    return this.deserialize(JSON.parse(string2), { inPlace: true });
  }
  registerClass(v, options) {
    this.classRegistry.register(v, options);
  }
  registerSymbol(v, identifier) {
    this.symbolRegistry.register(v, identifier);
  }
  registerCustom(transformer, name) {
    this.customTransformerRegistry.register({
      name,
      ...transformer
    });
  }
  allowErrorProps(...props) {
    this.allowedErrorProps.push(...props);
  }
};
SuperJSON9.defaultInstance = new SuperJSON9();
SuperJSON9.serialize = SuperJSON9.defaultInstance.serialize.bind(SuperJSON9.defaultInstance);
SuperJSON9.deserialize = SuperJSON9.defaultInstance.deserialize.bind(SuperJSON9.defaultInstance);
SuperJSON9.stringify = SuperJSON9.defaultInstance.stringify.bind(SuperJSON9.defaultInstance);
SuperJSON9.parse = SuperJSON9.defaultInstance.parse.bind(SuperJSON9.defaultInstance);
SuperJSON9.registerClass = SuperJSON9.defaultInstance.registerClass.bind(SuperJSON9.defaultInstance);
SuperJSON9.registerSymbol = SuperJSON9.defaultInstance.registerSymbol.bind(SuperJSON9.defaultInstance);
SuperJSON9.registerCustom = SuperJSON9.defaultInstance.registerCustom.bind(SuperJSON9.defaultInstance);
SuperJSON9.allowErrorProps = SuperJSON9.defaultInstance.allowErrorProps.bind(SuperJSON9.defaultInstance);
var dist_default9 = SuperJSON9;
var serialize9 = SuperJSON9.serialize;
var deserialize9 = SuperJSON9.deserialize;
var stringify9 = SuperJSON9.stringify;
var parse9 = SuperJSON9.parse;
var registerClass9 = SuperJSON9.registerClass;
var registerCustom9 = SuperJSON9.registerCustom;
var registerSymbol9 = SuperJSON9.registerSymbol;
var allowErrorProps9 = SuperJSON9.allowErrorProps;
function parseData9(formData) {
  const propsArray = [];
  if (!formData)
    return propsArray;
  const batchsIDs = [];
  for (const [key, value] of Array.from(formData.entries())) {
    if (key.startsWith("FILE_"))
      propsArray.push(value);
    else if (key.startsWith("FILES_")) {
      if (batchsIDs.includes(key))
        continue;
      batchsIDs.push(key);
      propsArray.push(formData.getAll(key));
    } else {
      propsArray.push(JSON.parse(decodeURI(value)));
    }
  }
  return propsArray;
}
__name(parseData9, "parseData");
function paramsFromURL9(url) {
  const params = url.searchParams.entries().toArray().map(([_, v]) => v);
  return params.map((param) => JSON.parse(decodeURIComponent(param)));
}
__name(paramsFromURL9, "paramsFromURL");
async function WrapRequestHandler9(context2, endpoint) {
  const isServerAction = context2.request.headers.get("x-server-action") === "true";
  if (!isServerAction) {
    return new Response("Not Found", { status: 404 });
  }
  const parsedData = context2.request.method === "GET" || context2.request.method === "HEAD" ? paramsFromURL9(new URL(context2.request.url)) : parseData9(context2.request.headers.get("content-type") ? await context2.request.formData() : void 0);
  const missingProps = endpoint.length - parsedData.length;
  for (let i = 0; i < missingProps; i++) {
    parsedData.push(void 0);
  }
  parsedData.push(context2);
  const result = await endpoint(...parsedData);
  switch (typeof result) {
    case "string":
    case "number":
    case "boolean":
    case "bigint":
      return new Response(dist_default9.stringify(result), {
        headers: {
          "Content-Type": "application/json",
          dataType: "json"
        }
      });
    case "undefined":
      return new Response(null, { status: 204 });
    case "object":
      if (result instanceof Response) {
        result.headers.set("dataType", "response");
        return result;
      } else if (result instanceof Blob) {
        const res = new Response(await result.arrayBuffer());
        res.headers.set("dataType", "blob");
        res.headers.set("Content-Type", result.type);
        return res;
      } else if (result instanceof File) {
        return new Response(await result.arrayBuffer(), {
          headers: {
            "Content-Type": result.type,
            dataType: "file",
            fileData: JSON.stringify({
              name: result.name,
              lastModified: result.lastModified
            })
          }
        });
      } else {
        return new Response(dist_default9.stringify(result), {
          headers: { "Content-Type": "application/json", dataType: "json" }
        });
      }
    default:
      throw new Error(`Unsupported return type from action: ${typeof result}`);
  }
}
__name(WrapRequestHandler9, "WrapRequestHandler");
var onRequest9 = /* @__PURE__ */ __name(async (context2) => {
  const method = context2.request.method;
  const options = {
    GET: typeof GET2 === "function" ? GET2 : void 0,
    POST: typeof POST === "function" ? POST : void 0,
    PUT: typeof PUT2 === "function" ? PUT2 : void 0,
    DELETE: typeof DELETE6 === "function" ? DELETE6 : void 0,
    PATCH: typeof PATCH === "function" ? PATCH : void 0,
    HEAD: typeof HEAD === "function" ? HEAD : void 0,
    OPTIONS: typeof OPTIONS === "function" ? OPTIONS : void 0
  };
  if (!options[method]) {
    return new Response(`Method "${method}" Not Allowed`, {
      status: 405
    });
  }
  return await WrapRequestHandler9(context2, options[method]);
}, "onRequest");

// api/invites/revoke.js
async function DELETE7({
  owner_group_id,
  user_id
}) {
  const ctx = getContext(arguments);
  const inviteManager = invites(ctx.env.PROJECT_DB);
  const res = await inviteManager.revoke(user_id, owner_group_id, ctx.data.client);
  return {
    success: res.success,
    error: res.error
  };
}
__name(DELETE7, "DELETE");
var DoubleIndexedKV10 = class {
  static {
    __name(this, "DoubleIndexedKV");
  }
  constructor() {
    this.keyToValue = /* @__PURE__ */ new Map();
    this.valueToKey = /* @__PURE__ */ new Map();
  }
  set(key, value) {
    this.keyToValue.set(key, value);
    this.valueToKey.set(value, key);
  }
  getByKey(key) {
    return this.keyToValue.get(key);
  }
  getByValue(value) {
    return this.valueToKey.get(value);
  }
  clear() {
    this.keyToValue.clear();
    this.valueToKey.clear();
  }
};
var Registry10 = class {
  static {
    __name(this, "Registry");
  }
  constructor(generateIdentifier) {
    this.generateIdentifier = generateIdentifier;
    this.kv = new DoubleIndexedKV10();
  }
  register(value, identifier) {
    if (this.kv.getByValue(value)) {
      return;
    }
    if (!identifier) {
      identifier = this.generateIdentifier(value);
    }
    this.kv.set(identifier, value);
  }
  clear() {
    this.kv.clear();
  }
  getIdentifier(value) {
    return this.kv.getByValue(value);
  }
  getValue(identifier) {
    return this.kv.getByKey(identifier);
  }
};
var ClassRegistry10 = class extends Registry10 {
  static {
    __name(this, "ClassRegistry");
  }
  constructor() {
    super((c) => c.name);
    this.classToAllowedProps = /* @__PURE__ */ new Map();
  }
  register(value, options) {
    if (typeof options === "object") {
      if (options.allowProps) {
        this.classToAllowedProps.set(value, options.allowProps);
      }
      super.register(value, options.identifier);
    } else {
      super.register(value, options);
    }
  }
  getAllowedProps(value) {
    return this.classToAllowedProps.get(value);
  }
};
function valuesOfObj10(record) {
  if ("values" in Object) {
    return Object.values(record);
  }
  const values = [];
  for (const key in record) {
    if (record.hasOwnProperty(key)) {
      values.push(record[key]);
    }
  }
  return values;
}
__name(valuesOfObj10, "valuesOfObj");
function find10(record, predicate) {
  const values = valuesOfObj10(record);
  if ("find" in values) {
    return values.find(predicate);
  }
  const valuesNotNever = values;
  for (let i = 0; i < valuesNotNever.length; i++) {
    const value = valuesNotNever[i];
    if (predicate(value)) {
      return value;
    }
  }
  return;
}
__name(find10, "find");
function forEach10(record, run) {
  Object.entries(record).forEach(([key, value]) => run(value, key));
}
__name(forEach10, "forEach");
function includes10(arr, value) {
  return arr.indexOf(value) !== -1;
}
__name(includes10, "includes");
function findArr10(record, predicate) {
  for (let i = 0; i < record.length; i++) {
    const value = record[i];
    if (predicate(value)) {
      return value;
    }
  }
  return;
}
__name(findArr10, "findArr");
var CustomTransformerRegistry10 = class {
  static {
    __name(this, "CustomTransformerRegistry");
  }
  constructor() {
    this.transfomers = {};
  }
  register(transformer) {
    this.transfomers[transformer.name] = transformer;
  }
  findApplicable(v) {
    return find10(this.transfomers, (transformer) => transformer.isApplicable(v));
  }
  findByName(name) {
    return this.transfomers[name];
  }
};
var getType11 = /* @__PURE__ */ __name((payload) => Object.prototype.toString.call(payload).slice(8, -1), "getType");
var isUndefined10 = /* @__PURE__ */ __name((payload) => typeof payload === "undefined", "isUndefined");
var isNull11 = /* @__PURE__ */ __name((payload) => payload === null, "isNull");
var isPlainObject11 = /* @__PURE__ */ __name((payload) => {
  if (typeof payload !== "object" || payload === null)
    return false;
  if (payload === Object.prototype)
    return false;
  if (Object.getPrototypeOf(payload) === null)
    return true;
  return Object.getPrototypeOf(payload) === Object.prototype;
}, "isPlainObject");
var isEmptyObject10 = /* @__PURE__ */ __name((payload) => isPlainObject11(payload) && Object.keys(payload).length === 0, "isEmptyObject");
var isArray11 = /* @__PURE__ */ __name((payload) => Array.isArray(payload), "isArray");
var isString10 = /* @__PURE__ */ __name((payload) => typeof payload === "string", "isString");
var isNumber10 = /* @__PURE__ */ __name((payload) => typeof payload === "number" && !isNaN(payload), "isNumber");
var isBoolean10 = /* @__PURE__ */ __name((payload) => typeof payload === "boolean", "isBoolean");
var isRegExp10 = /* @__PURE__ */ __name((payload) => payload instanceof RegExp, "isRegExp");
var isMap10 = /* @__PURE__ */ __name((payload) => payload instanceof Map, "isMap");
var isSet10 = /* @__PURE__ */ __name((payload) => payload instanceof Set, "isSet");
var isSymbol10 = /* @__PURE__ */ __name((payload) => getType11(payload) === "Symbol", "isSymbol");
var isDate10 = /* @__PURE__ */ __name((payload) => payload instanceof Date && !isNaN(payload.valueOf()), "isDate");
var isError10 = /* @__PURE__ */ __name((payload) => payload instanceof Error, "isError");
var isNaNValue10 = /* @__PURE__ */ __name((payload) => typeof payload === "number" && isNaN(payload), "isNaNValue");
var isPrimitive10 = /* @__PURE__ */ __name((payload) => isBoolean10(payload) || isNull11(payload) || isUndefined10(payload) || isNumber10(payload) || isString10(payload) || isSymbol10(payload), "isPrimitive");
var isBigint10 = /* @__PURE__ */ __name((payload) => typeof payload === "bigint", "isBigint");
var isInfinite10 = /* @__PURE__ */ __name((payload) => payload === Infinity || payload === -Infinity, "isInfinite");
var isTypedArray10 = /* @__PURE__ */ __name((payload) => ArrayBuffer.isView(payload) && !(payload instanceof DataView), "isTypedArray");
var isURL10 = /* @__PURE__ */ __name((payload) => payload instanceof URL, "isURL");
var escapeKey10 = /* @__PURE__ */ __name((key) => key.replace(/\\/g, "\\\\").replace(/\./g, "\\."), "escapeKey");
var stringifyPath10 = /* @__PURE__ */ __name((path) => path.map(String).map(escapeKey10).join("."), "stringifyPath");
var parsePath10 = /* @__PURE__ */ __name((string2, legacyPaths) => {
  const result = [];
  let segment = "";
  for (let i = 0; i < string2.length; i++) {
    let char = string2.charAt(i);
    if (!legacyPaths && char === "\\") {
      const escaped = string2.charAt(i + 1);
      if (escaped === "\\") {
        segment += "\\";
        i++;
        continue;
      } else if (escaped !== ".") {
        throw Error("invalid path");
      }
    }
    const isEscapedDot = char === "\\" && string2.charAt(i + 1) === ".";
    if (isEscapedDot) {
      segment += ".";
      i++;
      continue;
    }
    const isEndOfSegment = char === ".";
    if (isEndOfSegment) {
      result.push(segment);
      segment = "";
      continue;
    }
    segment += char;
  }
  const lastSegment = segment;
  result.push(lastSegment);
  return result;
}, "parsePath");
function simpleTransformation10(isApplicable, annotation, transform, untransform) {
  return {
    isApplicable,
    annotation,
    transform,
    untransform
  };
}
__name(simpleTransformation10, "simpleTransformation");
var simpleRules10 = [
  simpleTransformation10(isUndefined10, "undefined", () => null, () => {
    return;
  }),
  simpleTransformation10(isBigint10, "bigint", (v) => v.toString(), (v) => {
    if (typeof BigInt !== "undefined") {
      return BigInt(v);
    }
    console.error("Please add a BigInt polyfill.");
    return v;
  }),
  simpleTransformation10(isDate10, "Date", (v) => v.toISOString(), (v) => new Date(v)),
  simpleTransformation10(isError10, "Error", (v, superJson) => {
    const baseError = {
      name: v.name,
      message: v.message
    };
    if ("cause" in v) {
      baseError.cause = v.cause;
    }
    superJson.allowedErrorProps.forEach((prop) => {
      baseError[prop] = v[prop];
    });
    return baseError;
  }, (v, superJson) => {
    const e = new Error(v.message, { cause: v.cause });
    e.name = v.name;
    e.stack = v.stack;
    superJson.allowedErrorProps.forEach((prop) => {
      e[prop] = v[prop];
    });
    return e;
  }),
  simpleTransformation10(isRegExp10, "regexp", (v) => "" + v, (regex) => {
    const body = regex.slice(1, regex.lastIndexOf("/"));
    const flags2 = regex.slice(regex.lastIndexOf("/") + 1);
    return new RegExp(body, flags2);
  }),
  simpleTransformation10(isSet10, "set", (v) => [...v.values()], (v) => new Set(v)),
  simpleTransformation10(isMap10, "map", (v) => [...v.entries()], (v) => new Map(v)),
  simpleTransformation10((v) => isNaNValue10(v) || isInfinite10(v), "number", (v) => {
    if (isNaNValue10(v)) {
      return "NaN";
    }
    if (v > 0) {
      return "Infinity";
    } else {
      return "-Infinity";
    }
  }, Number),
  simpleTransformation10((v) => v === 0 && 1 / v === -Infinity, "number", () => {
    return "-0";
  }, Number),
  simpleTransformation10(isURL10, "URL", (v) => v.toString(), (v) => new URL(v))
];
function compositeTransformation10(isApplicable, annotation, transform, untransform) {
  return {
    isApplicable,
    annotation,
    transform,
    untransform
  };
}
__name(compositeTransformation10, "compositeTransformation");
var symbolRule10 = compositeTransformation10((s, superJson) => {
  if (isSymbol10(s)) {
    const isRegistered = !!superJson.symbolRegistry.getIdentifier(s);
    return isRegistered;
  }
  return false;
}, (s, superJson) => {
  const identifier = superJson.symbolRegistry.getIdentifier(s);
  return ["symbol", identifier];
}, (v) => v.description, (_, a, superJson) => {
  const value = superJson.symbolRegistry.getValue(a[1]);
  if (!value) {
    throw new Error("Trying to deserialize unknown symbol");
  }
  return value;
});
var constructorToName10 = [
  Int8Array,
  Uint8Array,
  Int16Array,
  Uint16Array,
  Int32Array,
  Uint32Array,
  Float32Array,
  Float64Array,
  Uint8ClampedArray
].reduce((obj, ctor) => {
  obj[ctor.name] = ctor;
  return obj;
}, {});
var typedArrayRule10 = compositeTransformation10(isTypedArray10, (v) => ["typed-array", v.constructor.name], (v) => [...v], (v, a) => {
  const ctor = constructorToName10[a[1]];
  if (!ctor) {
    throw new Error("Trying to deserialize unknown typed array");
  }
  return new ctor(v);
});
function isInstanceOfRegisteredClass10(potentialClass, superJson) {
  if (potentialClass?.constructor) {
    const isRegistered = !!superJson.classRegistry.getIdentifier(potentialClass.constructor);
    return isRegistered;
  }
  return false;
}
__name(isInstanceOfRegisteredClass10, "isInstanceOfRegisteredClass");
var classRule10 = compositeTransformation10(isInstanceOfRegisteredClass10, (clazz, superJson) => {
  const identifier = superJson.classRegistry.getIdentifier(clazz.constructor);
  return ["class", identifier];
}, (clazz, superJson) => {
  const allowedProps = superJson.classRegistry.getAllowedProps(clazz.constructor);
  if (!allowedProps) {
    return { ...clazz };
  }
  const result = {};
  allowedProps.forEach((prop) => {
    result[prop] = clazz[prop];
  });
  return result;
}, (v, a, superJson) => {
  const clazz = superJson.classRegistry.getValue(a[1]);
  if (!clazz) {
    throw new Error(`Trying to deserialize unknown class '${a[1]}' - check https://github.com/blitz-js/superjson/issues/116#issuecomment-773996564`);
  }
  return Object.assign(Object.create(clazz.prototype), v);
});
var customRule10 = compositeTransformation10((value, superJson) => {
  return !!superJson.customTransformerRegistry.findApplicable(value);
}, (value, superJson) => {
  const transformer = superJson.customTransformerRegistry.findApplicable(value);
  return ["custom", transformer.name];
}, (value, superJson) => {
  const transformer = superJson.customTransformerRegistry.findApplicable(value);
  return transformer.serialize(value);
}, (v, a, superJson) => {
  const transformer = superJson.customTransformerRegistry.findByName(a[1]);
  if (!transformer) {
    throw new Error("Trying to deserialize unknown custom value");
  }
  return transformer.deserialize(v);
});
var compositeRules10 = [classRule10, symbolRule10, customRule10, typedArrayRule10];
var transformValue10 = /* @__PURE__ */ __name((value, superJson) => {
  const applicableCompositeRule = findArr10(compositeRules10, (rule) => rule.isApplicable(value, superJson));
  if (applicableCompositeRule) {
    return {
      value: applicableCompositeRule.transform(value, superJson),
      type: applicableCompositeRule.annotation(value, superJson)
    };
  }
  const applicableSimpleRule = findArr10(simpleRules10, (rule) => rule.isApplicable(value, superJson));
  if (applicableSimpleRule) {
    return {
      value: applicableSimpleRule.transform(value, superJson),
      type: applicableSimpleRule.annotation
    };
  }
  return;
}, "transformValue");
var simpleRulesByAnnotation10 = {};
simpleRules10.forEach((rule) => {
  simpleRulesByAnnotation10[rule.annotation] = rule;
});
var untransformValue10 = /* @__PURE__ */ __name((json, type, superJson) => {
  if (isArray11(type)) {
    switch (type[0]) {
      case "symbol":
        return symbolRule10.untransform(json, type, superJson);
      case "class":
        return classRule10.untransform(json, type, superJson);
      case "custom":
        return customRule10.untransform(json, type, superJson);
      case "typed-array":
        return typedArrayRule10.untransform(json, type, superJson);
      default:
        throw new Error("Unknown transformation: " + type);
    }
  } else {
    const transformation = simpleRulesByAnnotation10[type];
    if (!transformation) {
      throw new Error("Unknown transformation: " + type);
    }
    return transformation.untransform(json, superJson);
  }
}, "untransformValue");
var getNthKey10 = /* @__PURE__ */ __name((value, n) => {
  if (n > value.size)
    throw new Error("index out of bounds");
  const keys = value.keys();
  while (n > 0) {
    keys.next();
    n--;
  }
  return keys.next().value;
}, "getNthKey");
function validatePath10(path) {
  if (includes10(path, "__proto__")) {
    throw new Error("__proto__ is not allowed as a property");
  }
  if (includes10(path, "prototype")) {
    throw new Error("prototype is not allowed as a property");
  }
  if (includes10(path, "constructor")) {
    throw new Error("constructor is not allowed as a property");
  }
}
__name(validatePath10, "validatePath");
var getDeep10 = /* @__PURE__ */ __name((object2, path) => {
  validatePath10(path);
  for (let i = 0; i < path.length; i++) {
    const key = path[i];
    if (isSet10(object2)) {
      object2 = getNthKey10(object2, +key);
    } else if (isMap10(object2)) {
      const row = +key;
      const type = +path[++i] === 0 ? "key" : "value";
      const keyOfRow = getNthKey10(object2, row);
      switch (type) {
        case "key":
          object2 = keyOfRow;
          break;
        case "value":
          object2 = object2.get(keyOfRow);
          break;
      }
    } else {
      object2 = object2[key];
    }
  }
  return object2;
}, "getDeep");
var setDeep10 = /* @__PURE__ */ __name((object2, path, mapper) => {
  validatePath10(path);
  if (path.length === 0) {
    return mapper(object2);
  }
  let parent = object2;
  for (let i = 0; i < path.length - 1; i++) {
    const key = path[i];
    if (isArray11(parent)) {
      const index = +key;
      parent = parent[index];
    } else if (isPlainObject11(parent)) {
      parent = parent[key];
    } else if (isSet10(parent)) {
      const row = +key;
      parent = getNthKey10(parent, row);
    } else if (isMap10(parent)) {
      const isEnd = i === path.length - 2;
      if (isEnd) {
        break;
      }
      const row = +key;
      const type = +path[++i] === 0 ? "key" : "value";
      const keyOfRow = getNthKey10(parent, row);
      switch (type) {
        case "key":
          parent = keyOfRow;
          break;
        case "value":
          parent = parent.get(keyOfRow);
          break;
      }
    }
  }
  const lastKey = path[path.length - 1];
  if (isArray11(parent)) {
    parent[+lastKey] = mapper(parent[+lastKey]);
  } else if (isPlainObject11(parent)) {
    parent[lastKey] = mapper(parent[lastKey]);
  }
  if (isSet10(parent)) {
    const oldValue = getNthKey10(parent, +lastKey);
    const newValue = mapper(oldValue);
    if (oldValue !== newValue) {
      parent.delete(oldValue);
      parent.add(newValue);
    }
  }
  if (isMap10(parent)) {
    const row = +path[path.length - 2];
    const keyToRow = getNthKey10(parent, row);
    const type = +lastKey === 0 ? "key" : "value";
    switch (type) {
      case "key": {
        const newKey = mapper(keyToRow);
        parent.set(newKey, parent.get(keyToRow));
        if (newKey !== keyToRow) {
          parent.delete(keyToRow);
        }
        break;
      }
      case "value": {
        parent.set(keyToRow, mapper(parent.get(keyToRow)));
        break;
      }
    }
  }
  return object2;
}, "setDeep");
var enableLegacyPaths10 = /* @__PURE__ */ __name((version4) => version4 < 1, "enableLegacyPaths");
function traverse10(tree, walker26, version4, origin = []) {
  if (!tree) {
    return;
  }
  const legacyPaths = enableLegacyPaths10(version4);
  if (!isArray11(tree)) {
    forEach10(tree, (subtree, key) => traverse10(subtree, walker26, version4, [
      ...origin,
      ...parsePath10(key, legacyPaths)
    ]));
    return;
  }
  const [nodeValue, children] = tree;
  if (children) {
    forEach10(children, (child, key) => {
      traverse10(child, walker26, version4, [
        ...origin,
        ...parsePath10(key, legacyPaths)
      ]);
    });
  }
  walker26(nodeValue, origin);
}
__name(traverse10, "traverse");
function applyValueAnnotations10(plain, annotations, version4, superJson) {
  traverse10(annotations, (type, path) => {
    plain = setDeep10(plain, path, (v) => untransformValue10(v, type, superJson));
  }, version4);
  return plain;
}
__name(applyValueAnnotations10, "applyValueAnnotations");
function applyReferentialEqualityAnnotations10(plain, annotations, version4) {
  const legacyPaths = enableLegacyPaths10(version4);
  function apply(identicalPaths, path) {
    const object2 = getDeep10(plain, parsePath10(path, legacyPaths));
    identicalPaths.map((path2) => parsePath10(path2, legacyPaths)).forEach((identicalObjectPath) => {
      plain = setDeep10(plain, identicalObjectPath, () => object2);
    });
  }
  __name(apply, "apply");
  if (isArray11(annotations)) {
    const [root, other] = annotations;
    root.forEach((identicalPath) => {
      plain = setDeep10(plain, parsePath10(identicalPath, legacyPaths), () => plain);
    });
    if (other) {
      forEach10(other, apply);
    }
  } else {
    forEach10(annotations, apply);
  }
  return plain;
}
__name(applyReferentialEqualityAnnotations10, "applyReferentialEqualityAnnotations");
var isDeep10 = /* @__PURE__ */ __name((object2, superJson) => isPlainObject11(object2) || isArray11(object2) || isMap10(object2) || isSet10(object2) || isError10(object2) || isInstanceOfRegisteredClass10(object2, superJson), "isDeep");
function addIdentity10(object2, path, identities) {
  const existingSet = identities.get(object2);
  if (existingSet) {
    existingSet.push(path);
  } else {
    identities.set(object2, [path]);
  }
}
__name(addIdentity10, "addIdentity");
function generateReferentialEqualityAnnotations10(identitites, dedupe) {
  const result = {};
  let rootEqualityPaths = void 0;
  identitites.forEach((paths) => {
    if (paths.length <= 1) {
      return;
    }
    if (!dedupe) {
      paths = paths.map((path) => path.map(String)).sort((a, b) => a.length - b.length);
    }
    const [representativePath, ...identicalPaths] = paths;
    if (representativePath.length === 0) {
      rootEqualityPaths = identicalPaths.map(stringifyPath10);
    } else {
      result[stringifyPath10(representativePath)] = identicalPaths.map(stringifyPath10);
    }
  });
  if (rootEqualityPaths) {
    if (isEmptyObject10(result)) {
      return [rootEqualityPaths];
    } else {
      return [rootEqualityPaths, result];
    }
  } else {
    return isEmptyObject10(result) ? void 0 : result;
  }
}
__name(generateReferentialEqualityAnnotations10, "generateReferentialEqualityAnnotations");
var walker10 = /* @__PURE__ */ __name((object2, identities, superJson, dedupe, path = [], objectsInThisPath = [], seenObjects = /* @__PURE__ */ new Map()) => {
  const primitive = isPrimitive10(object2);
  if (!primitive) {
    addIdentity10(object2, path, identities);
    const seen = seenObjects.get(object2);
    if (seen) {
      return dedupe ? {
        transformedValue: null
      } : seen;
    }
  }
  if (!isDeep10(object2, superJson)) {
    const transformed2 = transformValue10(object2, superJson);
    const result2 = transformed2 ? {
      transformedValue: transformed2.value,
      annotations: [transformed2.type]
    } : {
      transformedValue: object2
    };
    if (!primitive) {
      seenObjects.set(object2, result2);
    }
    return result2;
  }
  if (includes10(objectsInThisPath, object2)) {
    return {
      transformedValue: null
    };
  }
  const transformationResult = transformValue10(object2, superJson);
  const transformed = transformationResult?.value ?? object2;
  const transformedValue = isArray11(transformed) ? [] : {};
  const innerAnnotations = {};
  forEach10(transformed, (value, index) => {
    if (index === "__proto__" || index === "constructor" || index === "prototype") {
      throw new Error(`Detected property ${index}. This is a prototype pollution risk, please remove it from your object.`);
    }
    const recursiveResult = walker10(value, identities, superJson, dedupe, [...path, index], [...objectsInThisPath, object2], seenObjects);
    transformedValue[index] = recursiveResult.transformedValue;
    if (isArray11(recursiveResult.annotations)) {
      innerAnnotations[escapeKey10(index)] = recursiveResult.annotations;
    } else if (isPlainObject11(recursiveResult.annotations)) {
      forEach10(recursiveResult.annotations, (tree, key) => {
        innerAnnotations[escapeKey10(index) + "." + key] = tree;
      });
    }
  });
  const result = isEmptyObject10(innerAnnotations) ? {
    transformedValue,
    annotations: transformationResult ? [transformationResult.type] : void 0
  } : {
    transformedValue,
    annotations: transformationResult ? [transformationResult.type, innerAnnotations] : innerAnnotations
  };
  if (!primitive) {
    seenObjects.set(object2, result);
  }
  return result;
}, "walker");
function getType210(payload) {
  return Object.prototype.toString.call(payload).slice(8, -1);
}
__name(getType210, "getType2");
function isArray210(payload) {
  return getType210(payload) === "Array";
}
__name(isArray210, "isArray2");
function isPlainObject210(payload) {
  if (getType210(payload) !== "Object")
    return false;
  const prototype = Object.getPrototypeOf(payload);
  return !!prototype && prototype.constructor === Object && prototype === Object.prototype;
}
__name(isPlainObject210, "isPlainObject2");
function assignProp10(carry, key, newVal, originalObject, includeNonenumerable) {
  const propType = {}.propertyIsEnumerable.call(originalObject, key) ? "enumerable" : "nonenumerable";
  if (propType === "enumerable")
    carry[key] = newVal;
  if (includeNonenumerable && propType === "nonenumerable") {
    Object.defineProperty(carry, key, {
      value: newVal,
      enumerable: false,
      writable: true,
      configurable: true
    });
  }
}
__name(assignProp10, "assignProp");
function copy10(target, options = {}) {
  if (isArray210(target)) {
    return target.map((item) => copy10(item, options));
  }
  if (!isPlainObject210(target)) {
    return target;
  }
  const props = Object.getOwnPropertyNames(target);
  const symbols = Object.getOwnPropertySymbols(target);
  return [...props, ...symbols].reduce((carry, key) => {
    if (key === "__proto__")
      return carry;
    if (isArray210(options.props) && !options.props.includes(key)) {
      return carry;
    }
    const val = target[key];
    const newVal = copy10(val, options);
    assignProp10(carry, key, newVal, target, options.nonenumerable);
    return carry;
  }, {});
}
__name(copy10, "copy");
var SuperJSON10 = class {
  static {
    __name(this, "SuperJSON");
  }
  constructor({ dedupe = false } = {}) {
    this.classRegistry = new ClassRegistry10();
    this.symbolRegistry = new Registry10((s) => s.description ?? "");
    this.customTransformerRegistry = new CustomTransformerRegistry10();
    this.allowedErrorProps = [];
    this.dedupe = dedupe;
  }
  serialize(object2) {
    const identities = /* @__PURE__ */ new Map();
    const output = walker10(object2, identities, this, this.dedupe);
    const res = {
      json: output.transformedValue
    };
    if (output.annotations) {
      res.meta = {
        ...res.meta,
        values: output.annotations
      };
    }
    const equalityAnnotations = generateReferentialEqualityAnnotations10(identities, this.dedupe);
    if (equalityAnnotations) {
      res.meta = {
        ...res.meta,
        referentialEqualities: equalityAnnotations
      };
    }
    if (res.meta)
      res.meta.v = 1;
    return res;
  }
  deserialize(payload, options) {
    const { json, meta } = payload;
    let result = options?.inPlace ? json : copy10(json);
    if (meta?.values) {
      result = applyValueAnnotations10(result, meta.values, meta.v ?? 0, this);
    }
    if (meta?.referentialEqualities) {
      result = applyReferentialEqualityAnnotations10(result, meta.referentialEqualities, meta.v ?? 0);
    }
    return result;
  }
  stringify(object2) {
    return JSON.stringify(this.serialize(object2));
  }
  parse(string2) {
    return this.deserialize(JSON.parse(string2), { inPlace: true });
  }
  registerClass(v, options) {
    this.classRegistry.register(v, options);
  }
  registerSymbol(v, identifier) {
    this.symbolRegistry.register(v, identifier);
  }
  registerCustom(transformer, name) {
    this.customTransformerRegistry.register({
      name,
      ...transformer
    });
  }
  allowErrorProps(...props) {
    this.allowedErrorProps.push(...props);
  }
};
SuperJSON10.defaultInstance = new SuperJSON10();
SuperJSON10.serialize = SuperJSON10.defaultInstance.serialize.bind(SuperJSON10.defaultInstance);
SuperJSON10.deserialize = SuperJSON10.defaultInstance.deserialize.bind(SuperJSON10.defaultInstance);
SuperJSON10.stringify = SuperJSON10.defaultInstance.stringify.bind(SuperJSON10.defaultInstance);
SuperJSON10.parse = SuperJSON10.defaultInstance.parse.bind(SuperJSON10.defaultInstance);
SuperJSON10.registerClass = SuperJSON10.defaultInstance.registerClass.bind(SuperJSON10.defaultInstance);
SuperJSON10.registerSymbol = SuperJSON10.defaultInstance.registerSymbol.bind(SuperJSON10.defaultInstance);
SuperJSON10.registerCustom = SuperJSON10.defaultInstance.registerCustom.bind(SuperJSON10.defaultInstance);
SuperJSON10.allowErrorProps = SuperJSON10.defaultInstance.allowErrorProps.bind(SuperJSON10.defaultInstance);
var dist_default10 = SuperJSON10;
var serialize10 = SuperJSON10.serialize;
var deserialize10 = SuperJSON10.deserialize;
var stringify10 = SuperJSON10.stringify;
var parse10 = SuperJSON10.parse;
var registerClass10 = SuperJSON10.registerClass;
var registerCustom10 = SuperJSON10.registerCustom;
var registerSymbol10 = SuperJSON10.registerSymbol;
var allowErrorProps10 = SuperJSON10.allowErrorProps;
function parseData10(formData) {
  const propsArray = [];
  if (!formData)
    return propsArray;
  const batchsIDs = [];
  for (const [key, value] of Array.from(formData.entries())) {
    if (key.startsWith("FILE_"))
      propsArray.push(value);
    else if (key.startsWith("FILES_")) {
      if (batchsIDs.includes(key))
        continue;
      batchsIDs.push(key);
      propsArray.push(formData.getAll(key));
    } else {
      propsArray.push(JSON.parse(decodeURI(value)));
    }
  }
  return propsArray;
}
__name(parseData10, "parseData");
function paramsFromURL10(url) {
  const params = url.searchParams.entries().toArray().map(([_, v]) => v);
  return params.map((param) => JSON.parse(decodeURIComponent(param)));
}
__name(paramsFromURL10, "paramsFromURL");
async function WrapRequestHandler10(context2, endpoint) {
  const isServerAction = context2.request.headers.get("x-server-action") === "true";
  if (!isServerAction) {
    return new Response("Not Found", { status: 404 });
  }
  const parsedData = context2.request.method === "GET" || context2.request.method === "HEAD" ? paramsFromURL10(new URL(context2.request.url)) : parseData10(context2.request.headers.get("content-type") ? await context2.request.formData() : void 0);
  const missingProps = endpoint.length - parsedData.length;
  for (let i = 0; i < missingProps; i++) {
    parsedData.push(void 0);
  }
  parsedData.push(context2);
  const result = await endpoint(...parsedData);
  switch (typeof result) {
    case "string":
    case "number":
    case "boolean":
    case "bigint":
      return new Response(dist_default10.stringify(result), {
        headers: {
          "Content-Type": "application/json",
          dataType: "json"
        }
      });
    case "undefined":
      return new Response(null, { status: 204 });
    case "object":
      if (result instanceof Response) {
        result.headers.set("dataType", "response");
        return result;
      } else if (result instanceof Blob) {
        const res = new Response(await result.arrayBuffer());
        res.headers.set("dataType", "blob");
        res.headers.set("Content-Type", result.type);
        return res;
      } else if (result instanceof File) {
        return new Response(await result.arrayBuffer(), {
          headers: {
            "Content-Type": result.type,
            dataType: "file",
            fileData: JSON.stringify({
              name: result.name,
              lastModified: result.lastModified
            })
          }
        });
      } else {
        return new Response(dist_default10.stringify(result), {
          headers: { "Content-Type": "application/json", dataType: "json" }
        });
      }
    default:
      throw new Error(`Unsupported return type from action: ${typeof result}`);
  }
}
__name(WrapRequestHandler10, "WrapRequestHandler");
var onRequest10 = /* @__PURE__ */ __name(async (context2) => {
  const method = context2.request.method;
  const options = {
    GET: typeof GET === "function" ? GET : void 0,
    POST: typeof POST === "function" ? POST : void 0,
    PUT: typeof PUT === "function" ? PUT : void 0,
    DELETE: typeof DELETE7 === "function" ? DELETE7 : void 0,
    PATCH: typeof PATCH === "function" ? PATCH : void 0,
    HEAD: typeof HEAD === "function" ? HEAD : void 0,
    OPTIONS: typeof OPTIONS === "function" ? OPTIONS : void 0
  };
  if (!options[method]) {
    return new Response(`Method "${method}" Not Allowed`, {
      status: 405
    });
  }
  return await WrapRequestHandler10(context2, options[method]);
}, "onRequest");

// chunk-fbz1qcvz.js
var default_format = "RFC3986";
var formatters = {
  RFC1738: /* @__PURE__ */ __name((v) => String(v).replace(/%20/g, "+"), "RFC1738"),
  RFC3986: /* @__PURE__ */ __name((v) => String(v), "RFC3986")
};
var RFC1738 = "RFC1738";
var is_array = Array.isArray;
var hex_table = (() => {
  const array2 = [];
  for (let i = 0; i < 256; ++i) {
    array2.push("%" + ((i < 16 ? "0" : "") + i.toString(16)).toUpperCase());
  }
  return array2;
})();
var limit = 1024;
var encode = /* @__PURE__ */ __name((str, _defaultEncoder, charset, _kind, format) => {
  if (str.length === 0) {
    return str;
  }
  let string2 = str;
  if (typeof str === "symbol") {
    string2 = Symbol.prototype.toString.call(str);
  } else if (typeof str !== "string") {
    string2 = String(str);
  }
  if (charset === "iso-8859-1") {
    return escape(string2).replace(/%u[0-9a-f]{4}/gi, function($0) {
      return "%26%23" + parseInt($0.slice(2), 16) + "%3B";
    });
  }
  let out = "";
  for (let j = 0; j < string2.length; j += limit) {
    const segment = string2.length >= limit ? string2.slice(j, j + limit) : string2;
    const arr = [];
    for (let i = 0; i < segment.length; ++i) {
      let c = segment.charCodeAt(i);
      if (c === 45 || c === 46 || c === 95 || c === 126 || c >= 48 && c <= 57 || c >= 65 && c <= 90 || c >= 97 && c <= 122 || format === RFC1738 && (c === 40 || c === 41)) {
        arr[arr.length] = segment.charAt(i);
        continue;
      }
      if (c < 128) {
        arr[arr.length] = hex_table[c];
        continue;
      }
      if (c < 2048) {
        arr[arr.length] = hex_table[192 | c >> 6] + hex_table[128 | c & 63];
        continue;
      }
      if (c < 55296 || c >= 57344) {
        arr[arr.length] = hex_table[224 | c >> 12] + hex_table[128 | c >> 6 & 63] + hex_table[128 | c & 63];
        continue;
      }
      i += 1;
      c = 65536 + ((c & 1023) << 10 | segment.charCodeAt(i) & 1023);
      arr[arr.length] = hex_table[240 | c >> 18] + hex_table[128 | c >> 12 & 63] + hex_table[128 | c >> 6 & 63] + hex_table[128 | c & 63];
    }
    out += arr.join("");
  }
  return out;
}, "encode");
function is_buffer(obj) {
  if (!obj || typeof obj !== "object") {
    return false;
  }
  return !!(obj.constructor && obj.constructor.isBuffer && obj.constructor.isBuffer(obj));
}
__name(is_buffer, "is_buffer");
function maybe_map(val, fn) {
  if (is_array(val)) {
    const mapped = [];
    for (let i = 0; i < val.length; i += 1) {
      mapped.push(fn(val[i]));
    }
    return mapped;
  }
  return fn(val);
}
__name(maybe_map, "maybe_map");
var has = Object.prototype.hasOwnProperty;
var array_prefix_generators = {
  brackets(prefix) {
    return String(prefix) + "[]";
  },
  comma: "comma",
  indices(prefix, key) {
    return String(prefix) + "[" + key + "]";
  },
  repeat(prefix) {
    return String(prefix);
  }
};
var is_array2 = Array.isArray;
var push = Array.prototype.push;
var push_to_array = /* @__PURE__ */ __name(function(arr, value_or_array) {
  push.apply(arr, is_array2(value_or_array) ? value_or_array : [value_or_array]);
}, "push_to_array");
var to_ISO = Date.prototype.toISOString;
var defaults = {
  addQueryPrefix: false,
  allowDots: false,
  allowEmptyArrays: false,
  arrayFormat: "indices",
  charset: "utf-8",
  charsetSentinel: false,
  delimiter: "&",
  encode: true,
  encodeDotInKeys: false,
  encoder: encode,
  encodeValuesOnly: false,
  format: default_format,
  formatter: formatters[default_format],
  indices: false,
  serializeDate(date) {
    return to_ISO.call(date);
  },
  skipNulls: false,
  strictNullHandling: false
};
function is_non_nullish_primitive(v) {
  return typeof v === "string" || typeof v === "number" || typeof v === "boolean" || typeof v === "symbol" || typeof v === "bigint";
}
__name(is_non_nullish_primitive, "is_non_nullish_primitive");
var sentinel = {};
function inner_stringify(object2, prefix, generateArrayPrefix, commaRoundTrip, allowEmptyArrays, strictNullHandling, skipNulls, encodeDotInKeys, encoder2, filter, sort, allowDots, serializeDate, format, formatter, encodeValuesOnly, charset, sideChannel) {
  let obj = object2;
  let tmp_sc = sideChannel;
  let step = 0;
  let find_flag = false;
  while ((tmp_sc = tmp_sc.get(sentinel)) !== void 0 && !find_flag) {
    const pos = tmp_sc.get(object2);
    step += 1;
    if (typeof pos !== "undefined") {
      if (pos === step) {
        throw new RangeError("Cyclic object value");
      } else {
        find_flag = true;
      }
    }
    if (typeof tmp_sc.get(sentinel) === "undefined") {
      step = 0;
    }
  }
  if (typeof filter === "function") {
    obj = filter(prefix, obj);
  } else if (obj instanceof Date) {
    obj = serializeDate?.(obj);
  } else if (generateArrayPrefix === "comma" && is_array2(obj)) {
    obj = maybe_map(obj, function(value) {
      if (value instanceof Date) {
        return serializeDate?.(value);
      }
      return value;
    });
  }
  if (obj === null) {
    if (strictNullHandling) {
      return encoder2 && !encodeValuesOnly ? encoder2(prefix, defaults.encoder, charset, "key", format) : prefix;
    }
    obj = "";
  }
  if (is_non_nullish_primitive(obj) || is_buffer(obj)) {
    if (encoder2) {
      const key_value = encodeValuesOnly ? prefix : encoder2(prefix, defaults.encoder, charset, "key", format);
      return [
        formatter?.(key_value) + "=" + formatter?.(encoder2(obj, defaults.encoder, charset, "value", format))
      ];
    }
    return [formatter?.(prefix) + "=" + formatter?.(String(obj))];
  }
  const values = [];
  if (typeof obj === "undefined") {
    return values;
  }
  let obj_keys;
  if (generateArrayPrefix === "comma" && is_array2(obj)) {
    if (encodeValuesOnly && encoder2) {
      obj = maybe_map(obj, encoder2);
    }
    obj_keys = [{ value: obj.length > 0 ? obj.join(",") || null : void 0 }];
  } else if (is_array2(filter)) {
    obj_keys = filter;
  } else {
    const keys = Object.keys(obj);
    obj_keys = sort ? keys.sort(sort) : keys;
  }
  const encoded_prefix = encodeDotInKeys ? String(prefix).replace(/\./g, "%2E") : String(prefix);
  const adjusted_prefix = commaRoundTrip && is_array2(obj) && obj.length === 1 ? encoded_prefix + "[]" : encoded_prefix;
  if (allowEmptyArrays && is_array2(obj) && obj.length === 0) {
    return adjusted_prefix + "[]";
  }
  for (let j = 0; j < obj_keys.length; ++j) {
    const key = obj_keys[j];
    const value = typeof key === "object" && typeof key.value !== "undefined" ? key.value : obj[key];
    if (skipNulls && value === null) {
      continue;
    }
    const encoded_key = allowDots && encodeDotInKeys ? key.replace(/\./g, "%2E") : key;
    const key_prefix = is_array2(obj) ? typeof generateArrayPrefix === "function" ? generateArrayPrefix(adjusted_prefix, encoded_key) : adjusted_prefix : adjusted_prefix + (allowDots ? "." + encoded_key : "[" + encoded_key + "]");
    sideChannel.set(object2, step);
    const valueSideChannel = /* @__PURE__ */ new WeakMap();
    valueSideChannel.set(sentinel, sideChannel);
    push_to_array(values, inner_stringify(value, key_prefix, generateArrayPrefix, commaRoundTrip, allowEmptyArrays, strictNullHandling, skipNulls, encodeDotInKeys, generateArrayPrefix === "comma" && encodeValuesOnly && is_array2(obj) ? null : encoder2, filter, sort, allowDots, serializeDate, format, formatter, encodeValuesOnly, charset, valueSideChannel));
  }
  return values;
}
__name(inner_stringify, "inner_stringify");
function normalize_stringify_options(opts = defaults) {
  if (typeof opts.allowEmptyArrays !== "undefined" && typeof opts.allowEmptyArrays !== "boolean") {
    throw new TypeError("`allowEmptyArrays` option can only be `true` or `false`, when provided");
  }
  if (typeof opts.encodeDotInKeys !== "undefined" && typeof opts.encodeDotInKeys !== "boolean") {
    throw new TypeError("`encodeDotInKeys` option can only be `true` or `false`, when provided");
  }
  if (opts.encoder !== null && typeof opts.encoder !== "undefined" && typeof opts.encoder !== "function") {
    throw new TypeError("Encoder has to be a function.");
  }
  const charset = opts.charset || defaults.charset;
  if (typeof opts.charset !== "undefined" && opts.charset !== "utf-8" && opts.charset !== "iso-8859-1") {
    throw new TypeError("The charset option must be either utf-8, iso-8859-1, or undefined");
  }
  let format = default_format;
  if (typeof opts.format !== "undefined") {
    if (!has.call(formatters, opts.format)) {
      throw new TypeError("Unknown format option provided.");
    }
    format = opts.format;
  }
  const formatter = formatters[format];
  let filter = defaults.filter;
  if (typeof opts.filter === "function" || is_array2(opts.filter)) {
    filter = opts.filter;
  }
  let arrayFormat;
  if (opts.arrayFormat && opts.arrayFormat in array_prefix_generators) {
    arrayFormat = opts.arrayFormat;
  } else if ("indices" in opts) {
    arrayFormat = opts.indices ? "indices" : "repeat";
  } else {
    arrayFormat = defaults.arrayFormat;
  }
  if ("commaRoundTrip" in opts && typeof opts.commaRoundTrip !== "boolean") {
    throw new TypeError("`commaRoundTrip` must be a boolean, or absent");
  }
  const allowDots = typeof opts.allowDots === "undefined" ? !!opts.encodeDotInKeys === true ? true : defaults.allowDots : !!opts.allowDots;
  return {
    addQueryPrefix: typeof opts.addQueryPrefix === "boolean" ? opts.addQueryPrefix : defaults.addQueryPrefix,
    allowDots,
    allowEmptyArrays: typeof opts.allowEmptyArrays === "boolean" ? !!opts.allowEmptyArrays : defaults.allowEmptyArrays,
    arrayFormat,
    charset,
    charsetSentinel: typeof opts.charsetSentinel === "boolean" ? opts.charsetSentinel : defaults.charsetSentinel,
    commaRoundTrip: !!opts.commaRoundTrip,
    delimiter: typeof opts.delimiter === "undefined" ? defaults.delimiter : opts.delimiter,
    encode: typeof opts.encode === "boolean" ? opts.encode : defaults.encode,
    encodeDotInKeys: typeof opts.encodeDotInKeys === "boolean" ? opts.encodeDotInKeys : defaults.encodeDotInKeys,
    encoder: typeof opts.encoder === "function" ? opts.encoder : defaults.encoder,
    encodeValuesOnly: typeof opts.encodeValuesOnly === "boolean" ? opts.encodeValuesOnly : defaults.encodeValuesOnly,
    filter,
    format,
    formatter,
    serializeDate: typeof opts.serializeDate === "function" ? opts.serializeDate : defaults.serializeDate,
    skipNulls: typeof opts.skipNulls === "boolean" ? opts.skipNulls : defaults.skipNulls,
    sort: typeof opts.sort === "function" ? opts.sort : null,
    strictNullHandling: typeof opts.strictNullHandling === "boolean" ? opts.strictNullHandling : defaults.strictNullHandling
  };
}
__name(normalize_stringify_options, "normalize_stringify_options");
function stringify11(object2, opts = {}) {
  let obj = object2;
  const options = normalize_stringify_options(opts);
  let obj_keys;
  let filter;
  if (typeof options.filter === "function") {
    filter = options.filter;
    obj = filter("", obj);
  } else if (is_array2(options.filter)) {
    filter = options.filter;
    obj_keys = filter;
  }
  const keys = [];
  if (typeof obj !== "object" || obj === null) {
    return "";
  }
  const generateArrayPrefix = array_prefix_generators[options.arrayFormat];
  const commaRoundTrip = generateArrayPrefix === "comma" && options.commaRoundTrip;
  if (!obj_keys) {
    obj_keys = Object.keys(obj);
  }
  if (options.sort) {
    obj_keys.sort(options.sort);
  }
  const sideChannel = /* @__PURE__ */ new WeakMap();
  for (let i = 0; i < obj_keys.length; ++i) {
    const key = obj_keys[i];
    if (options.skipNulls && obj[key] === null) {
      continue;
    }
    push_to_array(keys, inner_stringify(obj[key], key, generateArrayPrefix, commaRoundTrip, options.allowEmptyArrays, options.strictNullHandling, options.skipNulls, options.encodeDotInKeys, options.encode ? options.encoder : null, options.filter, options.sort, options.allowDots, options.serializeDate, options.format, options.formatter, options.encodeValuesOnly, options.charset, sideChannel));
  }
  const joined = keys.join(options.delimiter);
  let prefix = options.addQueryPrefix === true ? "?" : "";
  if (options.charsetSentinel) {
    if (options.charset === "iso-8859-1") {
      prefix += "utf8=%26%2310003%3B&";
    } else {
      prefix += "utf8=%E2%9C%93&";
    }
  }
  return joined.length > 0 ? prefix + joined : "";
}
__name(stringify11, "stringify");
var VERSION = "5.2.0";
var CloudflareError = class extends Error {
  static {
    __name(this, "CloudflareError");
  }
};
var APIError = class _APIError extends CloudflareError {
  static {
    __name(this, "APIError");
  }
  constructor(status, error3, message2, headers) {
    super(`${_APIError.makeMessage(status, error3, message2)}`);
    this.status = status;
    this.headers = headers;
    this.error = error3;
    const data = error3;
    this.errors = data?.["errors"] ?? [];
  }
  static makeMessage(status, error3, message2) {
    const msg = error3?.message ? typeof error3.message === "string" ? error3.message : JSON.stringify(error3.message) : error3 ? JSON.stringify(error3) : message2;
    if (status && msg) {
      return `${status} ${msg}`;
    }
    if (status) {
      return `${status} status code (no body)`;
    }
    if (msg) {
      return msg;
    }
    return "(no status code or body)";
  }
  static generate(status, errorResponse, message2, headers) {
    if (!status || !headers) {
      return new APIConnectionError({ message: message2, cause: castToError(errorResponse) });
    }
    const error3 = errorResponse;
    if (status === 400) {
      return new BadRequestError(status, error3, message2, headers);
    }
    if (status === 401) {
      return new AuthenticationError(status, error3, message2, headers);
    }
    if (status === 403) {
      return new PermissionDeniedError(status, error3, message2, headers);
    }
    if (status === 404) {
      return new NotFoundError(status, error3, message2, headers);
    }
    if (status === 409) {
      return new ConflictError(status, error3, message2, headers);
    }
    if (status === 422) {
      return new UnprocessableEntityError(status, error3, message2, headers);
    }
    if (status === 429) {
      return new RateLimitError(status, error3, message2, headers);
    }
    if (status >= 500) {
      return new InternalServerError(status, error3, message2, headers);
    }
    return new _APIError(status, error3, message2, headers);
  }
};
var APIUserAbortError = class extends APIError {
  static {
    __name(this, "APIUserAbortError");
  }
  constructor({ message: message2 } = {}) {
    super(void 0, void 0, message2 || "Request was aborted.", void 0);
  }
};
var APIConnectionError = class extends APIError {
  static {
    __name(this, "APIConnectionError");
  }
  constructor({ message: message2, cause }) {
    super(void 0, void 0, message2 || "Connection error.", void 0);
    if (cause)
      this.cause = cause;
  }
};
var APIConnectionTimeoutError = class extends APIConnectionError {
  static {
    __name(this, "APIConnectionTimeoutError");
  }
  constructor({ message: message2 } = {}) {
    super({ message: message2 ?? "Request timed out." });
  }
};
var BadRequestError = class extends APIError {
  static {
    __name(this, "BadRequestError");
  }
};
var AuthenticationError = class extends APIError {
  static {
    __name(this, "AuthenticationError");
  }
};
var PermissionDeniedError = class extends APIError {
  static {
    __name(this, "PermissionDeniedError");
  }
};
var NotFoundError = class extends APIError {
  static {
    __name(this, "NotFoundError");
  }
};
var ConflictError = class extends APIError {
  static {
    __name(this, "ConflictError");
  }
};
var UnprocessableEntityError = class extends APIError {
  static {
    __name(this, "UnprocessableEntityError");
  }
};
var RateLimitError = class extends APIError {
  static {
    __name(this, "RateLimitError");
  }
};
var InternalServerError = class extends APIError {
  static {
    __name(this, "InternalServerError");
  }
};
var auto = false;
var kind = void 0;
var fetch2 = void 0;
var Request2 = void 0;
var Response2 = void 0;
var Headers2 = void 0;
var FormData2 = void 0;
var Blob2 = void 0;
var File2 = void 0;
var ReadableStream2 = void 0;
var getMultipartRequestOptions = void 0;
var getDefaultAgent = void 0;
var fileFromPath = void 0;
var isFsReadStream = void 0;
function setShims(shims, options = { auto: false }) {
  if (auto) {
    throw new Error(`you must \`import 'cloudflare/shims/${shims.kind}'\` before importing anything else from cloudflare`);
  }
  if (kind) {
    throw new Error(`can't \`import 'cloudflare/shims/${shims.kind}'\` after \`import 'cloudflare/shims/${kind}'\``);
  }
  auto = options.auto;
  kind = shims.kind;
  fetch2 = shims.fetch;
  Request2 = shims.Request;
  Response2 = shims.Response;
  Headers2 = shims.Headers;
  FormData2 = shims.FormData;
  Blob2 = shims.Blob;
  File2 = shims.File;
  ReadableStream2 = shims.ReadableStream;
  getMultipartRequestOptions = shims.getMultipartRequestOptions;
  getDefaultAgent = shims.getDefaultAgent;
  fileFromPath = shims.fileFromPath;
  isFsReadStream = shims.isFsReadStream;
}
__name(setShims, "setShims");
var MultipartBody = class {
  static {
    __name(this, "MultipartBody");
  }
  constructor(body) {
    this.body = body;
  }
  get [Symbol.toStringTag]() {
    return "MultipartBody";
  }
};
function getRuntime({ manuallyImported } = {}) {
  const recommendation = manuallyImported ? `You may need to use polyfills` : `Add one of these imports before your first \`import \u2026 from 'cloudflare'\`:
- \`import 'cloudflare/shims/node'\` (if you're running on Node)
- \`import 'cloudflare/shims/web'\` (otherwise)
`;
  let _fetch, _Request, _Response, _Headers;
  try {
    _fetch = fetch;
    _Request = Request;
    _Response = Response;
    _Headers = Headers;
  } catch (error3) {
    throw new Error(`this environment is missing the following Web Fetch API type: ${error3.message}. ${recommendation}`);
  }
  return {
    kind: "web",
    fetch: _fetch,
    Request: _Request,
    Response: _Response,
    Headers: _Headers,
    FormData: typeof FormData !== "undefined" ? FormData : class FormData3 {
      static {
        __name(this, "FormData3");
      }
      constructor() {
        throw new Error(`file uploads aren't supported in this environment yet as 'FormData' is undefined. ${recommendation}`);
      }
    },
    Blob: typeof Blob !== "undefined" ? Blob : class Blob3 {
      static {
        __name(this, "Blob3");
      }
      constructor() {
        throw new Error(`file uploads aren't supported in this environment yet as 'Blob' is undefined. ${recommendation}`);
      }
    },
    File: typeof File !== "undefined" ? File : class File3 {
      static {
        __name(this, "File3");
      }
      constructor() {
        throw new Error(`file uploads aren't supported in this environment yet as 'File' is undefined. ${recommendation}`);
      }
    },
    ReadableStream: typeof ReadableStream !== "undefined" ? ReadableStream : class ReadableStream3 {
      static {
        __name(this, "ReadableStream3");
      }
      constructor() {
        throw new Error(`streaming isn't supported in this environment yet as 'ReadableStream' is undefined. ${recommendation}`);
      }
    },
    getMultipartRequestOptions: /* @__PURE__ */ __name(async (form, opts) => ({
      ...opts,
      body: new MultipartBody(form)
    }), "getMultipartRequestOptions"),
    getDefaultAgent: /* @__PURE__ */ __name((url) => {
      return;
    }, "getDefaultAgent"),
    fileFromPath: /* @__PURE__ */ __name(() => {
      throw new Error("The `fileFromPath` function is only supported in Node. See the README for more details: https://www.github.com/cloudflare/cloudflare-typescript#file-uploads");
    }, "fileFromPath"),
    isFsReadStream: /* @__PURE__ */ __name((value) => false, "isFsReadStream")
  };
}
__name(getRuntime, "getRuntime");
var init = /* @__PURE__ */ __name(() => {
  if (!kind)
    setShims(getRuntime(), { auto: true });
}, "init");
init();
var isResponseLike = /* @__PURE__ */ __name((value) => value != null && typeof value === "object" && typeof value.url === "string" && typeof value.blob === "function", "isResponseLike");
var isFileLike = /* @__PURE__ */ __name((value) => value != null && typeof value === "object" && typeof value.name === "string" && typeof value.lastModified === "number" && isBlobLike(value), "isFileLike");
var isBlobLike = /* @__PURE__ */ __name((value) => value != null && typeof value === "object" && typeof value.size === "number" && typeof value.type === "string" && typeof value.text === "function" && typeof value.slice === "function" && typeof value.arrayBuffer === "function", "isBlobLike");
var isUploadable = /* @__PURE__ */ __name((value) => {
  return isFileLike(value) || isResponseLike(value) || isFsReadStream(value);
}, "isUploadable");
async function toFile(value, name, options) {
  value = await value;
  if (isFileLike(value)) {
    return value;
  }
  if (isResponseLike(value)) {
    const blob3 = await value.blob();
    name || (name = new URL(value.url).pathname.split(/[\\/]/).pop() ?? "unknown_file");
    const data = isBlobLike(blob3) ? [await blob3.arrayBuffer()] : [blob3];
    return new File2(data, name, options);
  }
  const bits = await getBytes(value);
  name || (name = getName(value) ?? "unknown_file");
  if (!options?.type) {
    const type = bits[0]?.type;
    if (typeof type === "string") {
      options = { ...options, type };
    }
  }
  return new File2(bits, name, options);
}
__name(toFile, "toFile");
async function getBytes(value) {
  let parts = [];
  if (typeof value === "string" || ArrayBuffer.isView(value) || value instanceof ArrayBuffer) {
    parts.push(value);
  } else if (isBlobLike(value)) {
    parts.push(await value.arrayBuffer());
  } else if (isAsyncIterableIterator(value)) {
    for await (const chunk of value) {
      parts.push(chunk);
    }
  } else {
    throw new Error(`Unexpected data type: ${typeof value}; constructor: ${value?.constructor?.name}; props: ${propsForError(value)}`);
  }
  return parts;
}
__name(getBytes, "getBytes");
function propsForError(value) {
  const props = Object.getOwnPropertyNames(value);
  return `[${props.map((p) => `"${p}"`).join(", ")}]`;
}
__name(propsForError, "propsForError");
function getName(value) {
  return getStringFromMaybeBuffer(value.name) || getStringFromMaybeBuffer(value.filename) || getStringFromMaybeBuffer(value.path)?.split(/[\\/]/).pop();
}
__name(getName, "getName");
var getStringFromMaybeBuffer = /* @__PURE__ */ __name((x) => {
  if (typeof x === "string")
    return x;
  if (typeof Buffer !== "undefined" && x instanceof Buffer)
    return String(x);
  return;
}, "getStringFromMaybeBuffer");
var isAsyncIterableIterator = /* @__PURE__ */ __name((value) => value != null && typeof value === "object" && typeof value[Symbol.asyncIterator] === "function", "isAsyncIterableIterator");
var isMultipartBody = /* @__PURE__ */ __name((body) => body && typeof body === "object" && body.body && body[Symbol.toStringTag] === "MultipartBody", "isMultipartBody");
var maybeMultipartFormRequestOptions = /* @__PURE__ */ __name(async (opts) => {
  if (!hasUploadableValue(opts.body))
    return opts;
  const form = await createForm(opts.body, opts);
  return getMultipartRequestOptions(form, opts);
}, "maybeMultipartFormRequestOptions");
var multipartFormRequestOptions = /* @__PURE__ */ __name(async (opts) => {
  const form = await createForm(opts.body, opts);
  return getMultipartRequestOptions(form, opts);
}, "multipartFormRequestOptions");
var createForm = /* @__PURE__ */ __name(async (body, opts) => {
  const form = new FormData2();
  await Promise.all(Object.entries(body || {}).map(([key, value]) => addFormValue(form, key, value, opts)));
  return form;
}, "createForm");
var hasUploadableValue = /* @__PURE__ */ __name((value) => {
  if (isUploadable(value))
    return true;
  if (Array.isArray(value))
    return value.some(hasUploadableValue);
  if (value && typeof value === "object") {
    for (const k in value) {
      if (hasUploadableValue(value[k]))
        return true;
    }
  }
  return false;
}, "hasUploadableValue");
var addFormValue = /* @__PURE__ */ __name(async (form, key, value, opts) => {
  if (opts?.__multipartSyntax === "json") {
    return await addFormValueJson(form, key, value);
  }
  if (value === void 0)
    return;
  if (value == null) {
    throw new TypeError(`Received null for "${key}"; to pass null in FormData, you must use the string 'null'`);
  }
  if (typeof value === "string" || typeof value === "number" || typeof value === "boolean") {
    form.append(key, String(value));
  } else if (isUploadable(value)) {
    const file = await toFile(value);
    form.append(key, file);
  } else if (Array.isArray(value)) {
    await Promise.all(value.map((entry) => addFormValue(form, key + "[]", entry)));
  } else if (typeof value === "object") {
    await Promise.all(Object.entries(value).map(([name, prop]) => addFormValue(form, `${key}[${name}]`, prop)));
  }
}, "addFormValue");
var addFormValueJson = /* @__PURE__ */ __name(async (form, key, value) => {
  if (value === void 0)
    return;
  if (value == null) {
    throw new TypeError(`Received null for "${key}"; to pass null in FormData, you must use the string 'null'`);
  }
  if (typeof value === "string" || typeof value === "number" || typeof value === "boolean") {
    form.append(key, String(value));
    return;
  }
  if (isUploadable(value)) {
    const file = await toFile(value);
    form.append(key, file);
    return;
  }
  if (Array.isArray(value) && value.some((e) => isUploadable(e))) {
    for (const entry of value) {
      form.append(key, await toFile(entry));
    }
    return;
  }
  if (Array.isArray(value) || typeof value === "object") {
    form.append(key, new File2([JSON.stringify(value)], key, { type: "application/json" }));
    return;
  }
  throw new TypeError(`Invalid value given to form, expected a string, number, boolean, object, Array, File or Blob but got ${value} instead`);
}, "addFormValueJson");
var __classPrivateFieldSet = /* @__PURE__ */ __name(function(receiver, state, value, kind2, f) {
  if (kind2 === "m")
    throw new TypeError("Private method is not writable");
  if (kind2 === "a" && !f)
    throw new TypeError("Private accessor was defined without a setter");
  if (typeof state === "function" ? receiver !== state || !f : !state.has(receiver))
    throw new TypeError("Cannot write private member to an object whose class did not declare it");
  return kind2 === "a" ? f.call(receiver, value) : f ? f.value = value : state.set(receiver, value), value;
}, "__classPrivateFieldSet");
var __classPrivateFieldGet = /* @__PURE__ */ __name(function(receiver, state, kind2, f) {
  if (kind2 === "a" && !f)
    throw new TypeError("Private accessor was defined without a getter");
  if (typeof state === "function" ? receiver !== state || !f : !state.has(receiver))
    throw new TypeError("Cannot read private member from an object whose class did not declare it");
  return kind2 === "m" ? f : kind2 === "a" ? f.call(receiver) : f ? f.value : state.get(receiver);
}, "__classPrivateFieldGet");
var _APIClient_baseURLOverridden;
var _AbstractPage_client;
init();
async function defaultParseResponse(props) {
  const { response } = props;
  if (response.status === 204) {
    return null;
  }
  if (props.options.__binaryResponse) {
    return response;
  }
  const contentType = response.headers.get("content-type");
  const mediaType = contentType?.split(";")[0]?.trim();
  const isJSON = mediaType?.includes("application/json") || mediaType?.endsWith("+json");
  if (isJSON) {
    const json = await response.json();
    debug3("response", response.status, response.url, response.headers, json);
    return json;
  }
  const text3 = await response.text();
  debug3("response", response.status, response.url, response.headers, text3);
  return text3;
}
__name(defaultParseResponse, "defaultParseResponse");
var APIPromise = class _APIPromise extends Promise {
  static {
    __name(this, "APIPromise");
  }
  constructor(responsePromise, parseResponse = defaultParseResponse) {
    super((resolve) => {
      resolve(null);
    });
    this.responsePromise = responsePromise;
    this.parseResponse = parseResponse;
  }
  _thenUnwrap(transform) {
    return new _APIPromise(this.responsePromise, async (props) => transform(await this.parseResponse(props), props));
  }
  asResponse() {
    return this.responsePromise.then((p) => p.response);
  }
  async withResponse() {
    const [data, response] = await Promise.all([this.parse(), this.asResponse()]);
    return { data, response };
  }
  parse() {
    if (!this.parsedPromise) {
      this.parsedPromise = this.responsePromise.then(this.parseResponse);
    }
    return this.parsedPromise;
  }
  then(onfulfilled, onrejected) {
    return this.parse().then(onfulfilled, onrejected);
  }
  catch(onrejected) {
    return this.parse().catch(onrejected);
  }
  finally(onfinally) {
    return this.parse().finally(onfinally);
  }
};
var APIClient = class {
  static {
    __name(this, "APIClient");
  }
  constructor({
    baseURL,
    apiVersion,
    baseURLOverridden,
    maxRetries = 2,
    timeout = 6e4,
    httpAgent,
    fetch: overriddenFetch
  }) {
    _APIClient_baseURLOverridden.set(this, void 0);
    this.baseURL = baseURL;
    this.apiVersion = apiVersion;
    __classPrivateFieldSet(this, _APIClient_baseURLOverridden, baseURLOverridden, "f");
    this.maxRetries = validatePositiveInteger("maxRetries", maxRetries);
    this.timeout = validatePositiveInteger("timeout", timeout);
    this.httpAgent = httpAgent;
    this.fetch = overriddenFetch ?? fetch2;
  }
  authHeaders(opts) {
    return {};
  }
  defaultHeaders(opts) {
    return {
      Accept: "application/json",
      ...["head", "get"].includes(opts.method) ? {} : { "Content-Type": "application/json" },
      "User-Agent": this.getUserAgent(),
      "api-version": this.getAPIVerson(),
      ...getPlatformHeaders(),
      ...this.authHeaders(opts)
    };
  }
  validateHeaders(headers, customHeaders, usingCustomFetch) {
  }
  defaultIdempotencyKey() {
    return `stainless-node-retry-${uuid4()}`;
  }
  get(path, opts) {
    return this.methodRequest("get", path, opts);
  }
  post(path, opts) {
    return this.methodRequest("post", path, opts);
  }
  patch(path, opts) {
    return this.methodRequest("patch", path, opts);
  }
  put(path, opts) {
    return this.methodRequest("put", path, opts);
  }
  delete(path, opts) {
    return this.methodRequest("delete", path, opts);
  }
  methodRequest(method, path, opts) {
    return this.request(Promise.resolve(opts).then(async (opts2) => {
      const body = opts2 && isBlobLike(opts2?.body) ? new DataView(await opts2.body.arrayBuffer()) : opts2?.body instanceof DataView ? opts2.body : opts2?.body instanceof ArrayBuffer ? new DataView(opts2.body) : opts2 && ArrayBuffer.isView(opts2?.body) ? new DataView(opts2.body.buffer) : opts2?.body;
      return { method, path, ...opts2, body };
    }));
  }
  getAPIList(path, Page2, opts) {
    return this.requestAPIList(Page2, { method: "get", path, ...opts });
  }
  calculateContentLength(body) {
    if (typeof body === "string") {
      if (typeof Buffer !== "undefined") {
        return Buffer.byteLength(body, "utf8").toString();
      }
      if (typeof TextEncoder !== "undefined") {
        const encoder2 = new TextEncoder();
        const encoded = encoder2.encode(body);
        return encoded.length.toString();
      }
    } else if (ArrayBuffer.isView(body)) {
      return body.byteLength.toString();
    }
    return null;
  }
  async buildRequest(inputOptions, { retryCount = 0 } = {}) {
    const options = { ...inputOptions };
    const { method, path, query, defaultBaseURL, headers = {} } = options;
    const body = ArrayBuffer.isView(options.body) || options.__binaryRequest && typeof options.body === "string" ? options.body : isMultipartBody(options.body) ? options.body.body : options.body ? JSON.stringify(options.body, null, 2) : null;
    const contentLength = this.calculateContentLength(body);
    const url = this.buildURL(path, query, defaultBaseURL);
    if ("timeout" in options)
      validatePositiveInteger("timeout", options.timeout);
    options.timeout = options.timeout ?? this.timeout;
    const httpAgent = options.httpAgent ?? this.httpAgent ?? getDefaultAgent(url);
    const minAgentTimeout = options.timeout + 1e3;
    if (typeof httpAgent?.options?.timeout === "number" && minAgentTimeout > (httpAgent.options.timeout ?? 0)) {
      httpAgent.options.timeout = minAgentTimeout;
    }
    if (this.idempotencyHeader && method !== "get") {
      if (!inputOptions.idempotencyKey)
        inputOptions.idempotencyKey = this.defaultIdempotencyKey();
      headers[this.idempotencyHeader] = inputOptions.idempotencyKey;
    }
    const reqHeaders = this.buildHeaders({ options, headers, contentLength, retryCount });
    const req = {
      method,
      ...body && { body },
      headers: reqHeaders,
      ...httpAgent && { agent: httpAgent },
      signal: options.signal ?? null
    };
    return { req, url, timeout: options.timeout };
  }
  buildHeaders({ options, headers, contentLength, retryCount }) {
    const reqHeaders = {};
    if (contentLength) {
      reqHeaders["content-length"] = contentLength;
    }
    const defaultHeaders = this.defaultHeaders(options);
    applyHeadersMut(reqHeaders, defaultHeaders);
    applyHeadersMut(reqHeaders, headers);
    if (isMultipartBody(options.body) && kind !== "node") {
      delete reqHeaders["content-type"];
    }
    if (getHeader(defaultHeaders, "x-stainless-retry-count") === void 0 && getHeader(headers, "x-stainless-retry-count") === void 0) {
      reqHeaders["x-stainless-retry-count"] = String(retryCount);
    }
    if (getHeader(defaultHeaders, "x-stainless-timeout") === void 0 && getHeader(headers, "x-stainless-timeout") === void 0 && options.timeout) {
      reqHeaders["x-stainless-timeout"] = String(options.timeout);
    }
    this.validateHeaders(reqHeaders, headers, this.fetch !== fetch2);
    return reqHeaders;
  }
  async prepareOptions(options) {
  }
  async prepareRequest(request, { url, options }) {
  }
  parseHeaders(headers) {
    return !headers ? {} : Symbol.iterator in headers ? Object.fromEntries(Array.from(headers).map((header) => [...header])) : { ...headers };
  }
  makeStatusError(status, error3, message2, headers) {
    return APIError.generate(status, error3, message2, headers);
  }
  request(options, remainingRetries = null) {
    return new APIPromise(this.makeRequest(options, remainingRetries));
  }
  async makeRequest(optionsInput, retriesRemaining) {
    const options = await optionsInput;
    const maxRetries = options.maxRetries ?? this.maxRetries;
    if (retriesRemaining == null) {
      retriesRemaining = maxRetries;
    }
    await this.prepareOptions(options);
    const { req, url, timeout } = await this.buildRequest(options, {
      retryCount: maxRetries - retriesRemaining
    });
    await this.prepareRequest(req, { url, options });
    debug3("request", url, options, req.headers);
    if (options.signal?.aborted) {
      throw new APIUserAbortError();
    }
    const controller = new AbortController();
    const response = await this.fetchWithTimeout(url, req, timeout, controller).catch(castToError);
    if (response instanceof Error) {
      if (options.signal?.aborted) {
        throw new APIUserAbortError();
      }
      if (retriesRemaining) {
        return this.retryRequest(options, retriesRemaining);
      }
      if (response.name === "AbortError") {
        throw new APIConnectionTimeoutError();
      }
      throw new APIConnectionError({ cause: response });
    }
    const responseHeaders = createResponseHeaders(response.headers);
    if (!response.ok) {
      if (retriesRemaining && this.shouldRetry(response)) {
        const retryMessage2 = `retrying, ${retriesRemaining} attempts remaining`;
        debug3(`response (error; ${retryMessage2})`, response.status, url, responseHeaders);
        return this.retryRequest(options, retriesRemaining, responseHeaders);
      }
      const errText = await response.text().catch((e) => castToError(e).message);
      const errJSON = safeJSON(errText);
      const errMessage = errJSON ? void 0 : errText;
      const retryMessage = retriesRemaining ? `(error; no more retries left)` : `(error; not retryable)`;
      debug3(`response (error; ${retryMessage})`, response.status, url, responseHeaders, errMessage);
      const err = this.makeStatusError(response.status, errJSON, errMessage, responseHeaders);
      throw err;
    }
    return { response, options, controller };
  }
  requestAPIList(Page2, options) {
    const request = this.makeRequest(options, null);
    return new PagePromise(this, request, Page2);
  }
  buildURL(path, query, defaultBaseURL) {
    const baseURL = !__classPrivateFieldGet(this, _APIClient_baseURLOverridden, "f") && defaultBaseURL || this.baseURL;
    const url = isAbsoluteURL(path) ? new URL(path) : new URL(baseURL + (baseURL.endsWith("/") && path.startsWith("/") ? path.slice(1) : path));
    const defaultQuery = this.defaultQuery();
    if (!isEmptyObj(defaultQuery)) {
      query = { ...defaultQuery, ...query };
    }
    if (typeof query === "object" && query && !Array.isArray(query)) {
      url.search = this.stringifyQuery(query);
    }
    return url.toString();
  }
  stringifyQuery(query) {
    return Object.entries(query).filter(([_, value]) => typeof value !== "undefined").map(([key, value]) => {
      if (typeof value === "string" || typeof value === "number" || typeof value === "boolean") {
        return `${encodeURIComponent(key)}=${encodeURIComponent(value)}`;
      }
      if (value === null) {
        return `${encodeURIComponent(key)}=`;
      }
      throw new CloudflareError(`Cannot stringify type ${typeof value}; Expected string, number, boolean, or null. If you need to pass nested query parameters, you can manually encode them, e.g. { query: { 'foo[key1]': value1, 'foo[key2]': value2 } }, and please open a GitHub issue requesting better support for your use case.`);
    }).join("&");
  }
  async fetchWithTimeout(url, init2, ms, controller) {
    const { signal, ...options } = init2 || {};
    if (signal)
      signal.addEventListener("abort", () => controller.abort());
    const timeout = setTimeout(() => controller.abort(), ms);
    const fetchOptions = {
      signal: controller.signal,
      ...options
    };
    if (fetchOptions.method) {
      fetchOptions.method = fetchOptions.method.toUpperCase();
    }
    return this.fetch.call(void 0, url, fetchOptions).finally(() => {
      clearTimeout(timeout);
    });
  }
  shouldRetry(response) {
    const shouldRetryHeader = response.headers.get("x-should-retry");
    if (shouldRetryHeader === "true")
      return true;
    if (shouldRetryHeader === "false")
      return false;
    if (response.status === 408)
      return true;
    if (response.status === 409)
      return true;
    if (response.status === 429)
      return true;
    if (response.status >= 500)
      return true;
    return false;
  }
  async retryRequest(options, retriesRemaining, responseHeaders) {
    let timeoutMillis;
    const retryAfterMillisHeader = responseHeaders?.["retry-after-ms"];
    if (retryAfterMillisHeader) {
      const timeoutMs = parseFloat(retryAfterMillisHeader);
      if (!Number.isNaN(timeoutMs)) {
        timeoutMillis = timeoutMs;
      }
    }
    const retryAfterHeader = responseHeaders?.["retry-after"];
    if (retryAfterHeader && !timeoutMillis) {
      const timeoutSeconds = parseFloat(retryAfterHeader);
      if (!Number.isNaN(timeoutSeconds)) {
        timeoutMillis = timeoutSeconds * 1e3;
      } else {
        timeoutMillis = Date.parse(retryAfterHeader) - Date.now();
      }
    }
    if (!(timeoutMillis && 0 <= timeoutMillis && timeoutMillis < 60 * 1e3)) {
      const maxRetries = options.maxRetries ?? this.maxRetries;
      timeoutMillis = this.calculateDefaultRetryTimeoutMillis(retriesRemaining, maxRetries);
    }
    await sleep(timeoutMillis);
    return this.makeRequest(options, retriesRemaining - 1);
  }
  calculateDefaultRetryTimeoutMillis(retriesRemaining, maxRetries) {
    const initialRetryDelay = 0.5;
    const maxRetryDelay = 8;
    const numRetries = maxRetries - retriesRemaining;
    const sleepSeconds = Math.min(initialRetryDelay * Math.pow(2, numRetries), maxRetryDelay);
    const jitter = 1 - Math.random() * 0.25;
    return sleepSeconds * jitter * 1e3;
  }
  getUserAgent() {
    return `${this.constructor.name}/JS ${VERSION}`;
  }
  getAPIVerson() {
    return this.apiVersion;
  }
};
_APIClient_baseURLOverridden = /* @__PURE__ */ new WeakMap();
var AbstractPage = class {
  static {
    __name(this, "AbstractPage");
  }
  constructor(client, response, body, options) {
    _AbstractPage_client.set(this, void 0);
    __classPrivateFieldSet(this, _AbstractPage_client, client, "f");
    this.options = options;
    this.response = response;
    this.body = body;
  }
  hasNextPage() {
    const items = this.getPaginatedItems();
    if (!items.length)
      return false;
    return this.nextPageInfo() != null;
  }
  async getNextPage() {
    const nextInfo = this.nextPageInfo();
    if (!nextInfo) {
      throw new CloudflareError("No next page expected; please check `.hasNextPage()` before calling `.getNextPage()`.");
    }
    const nextOptions = { ...this.options };
    if ("params" in nextInfo && typeof nextOptions.query === "object") {
      nextOptions.query = { ...nextOptions.query, ...nextInfo.params };
    } else if ("url" in nextInfo) {
      const params = [...Object.entries(nextOptions.query || {}), ...nextInfo.url.searchParams.entries()];
      for (const [key, value] of params) {
        nextInfo.url.searchParams.set(key, value);
      }
      nextOptions.query = void 0;
      nextOptions.path = nextInfo.url.toString();
    }
    return await __classPrivateFieldGet(this, _AbstractPage_client, "f").requestAPIList(this.constructor, nextOptions);
  }
  async *iterPages() {
    let page = this;
    yield page;
    while (page.hasNextPage()) {
      page = await page.getNextPage();
      yield page;
    }
  }
  async *[(_AbstractPage_client = /* @__PURE__ */ new WeakMap(), Symbol.asyncIterator)]() {
    for await (const page of this.iterPages()) {
      for (const item of page.getPaginatedItems()) {
        yield item;
      }
    }
  }
};
var PagePromise = class extends APIPromise {
  static {
    __name(this, "PagePromise");
  }
  constructor(client, request, Page2) {
    super(request, async (props) => new Page2(client, props.response, await defaultParseResponse(props), props.options));
  }
  async *[Symbol.asyncIterator]() {
    const page = await this;
    for await (const item of page) {
      yield item;
    }
  }
};
var createResponseHeaders = /* @__PURE__ */ __name((headers) => {
  return new Proxy(Object.fromEntries(headers.entries()), {
    get(target, name) {
      const key = name.toString();
      return target[key.toLowerCase()] || target[key];
    }
  });
}, "createResponseHeaders");
var requestOptionsKeys = {
  method: true,
  path: true,
  query: true,
  body: true,
  headers: true,
  defaultBaseURL: true,
  maxRetries: true,
  stream: true,
  timeout: true,
  httpAgent: true,
  signal: true,
  idempotencyKey: true,
  __multipartSyntax: true,
  __binaryRequest: true,
  __binaryResponse: true
};
var isRequestOptions = /* @__PURE__ */ __name((obj) => {
  return typeof obj === "object" && obj !== null && !isEmptyObj(obj) && Object.keys(obj).every((k) => hasOwn(requestOptionsKeys, k));
}, "isRequestOptions");
var getPlatformProperties = /* @__PURE__ */ __name(() => {
  if (typeof Deno !== "undefined" && Deno.build != null) {
    return {
      "X-Stainless-Lang": "js",
      "X-Stainless-Package-Version": VERSION,
      "X-Stainless-OS": normalizePlatform(Deno.build.os),
      "X-Stainless-Arch": normalizeArch(Deno.build.arch),
      "X-Stainless-Runtime": "deno",
      "X-Stainless-Runtime-Version": typeof Deno.version === "string" ? Deno.version : Deno.version?.deno ?? "unknown"
    };
  }
  if (typeof EdgeRuntime !== "undefined") {
    return {
      "X-Stainless-Lang": "js",
      "X-Stainless-Package-Version": VERSION,
      "X-Stainless-OS": "Unknown",
      "X-Stainless-Arch": `other:${EdgeRuntime}`,
      "X-Stainless-Runtime": "edge",
      "X-Stainless-Runtime-Version": process.version
    };
  }
  if (Object.prototype.toString.call(typeof process !== "undefined" ? process : 0) === "[object process]") {
    return {
      "X-Stainless-Lang": "js",
      "X-Stainless-Package-Version": VERSION,
      "X-Stainless-OS": normalizePlatform(process.platform),
      "X-Stainless-Arch": normalizeArch(process.arch),
      "X-Stainless-Runtime": "node",
      "X-Stainless-Runtime-Version": process.version
    };
  }
  const browserInfo = getBrowserInfo();
  if (browserInfo) {
    return {
      "X-Stainless-Lang": "js",
      "X-Stainless-Package-Version": VERSION,
      "X-Stainless-OS": "Unknown",
      "X-Stainless-Arch": "unknown",
      "X-Stainless-Runtime": `browser:${browserInfo.browser}`,
      "X-Stainless-Runtime-Version": browserInfo.version
    };
  }
  return {
    "X-Stainless-Lang": "js",
    "X-Stainless-Package-Version": VERSION,
    "X-Stainless-OS": "Unknown",
    "X-Stainless-Arch": "unknown",
    "X-Stainless-Runtime": "unknown",
    "X-Stainless-Runtime-Version": "unknown"
  };
}, "getPlatformProperties");
function getBrowserInfo() {
  if (typeof navigator === "undefined" || !navigator) {
    return null;
  }
  const browserPatterns = [
    { key: "edge", pattern: /Edge(?:\W+(\d+)\.(\d+)(?:\.(\d+))?)?/ },
    { key: "ie", pattern: /MSIE(?:\W+(\d+)\.(\d+)(?:\.(\d+))?)?/ },
    { key: "ie", pattern: /Trident(?:.*rv\:(\d+)\.(\d+)(?:\.(\d+))?)?/ },
    { key: "chrome", pattern: /Chrome(?:\W+(\d+)\.(\d+)(?:\.(\d+))?)?/ },
    { key: "firefox", pattern: /Firefox(?:\W+(\d+)\.(\d+)(?:\.(\d+))?)?/ },
    { key: "safari", pattern: /(?:Version\W+(\d+)\.(\d+)(?:\.(\d+))?)?(?:\W+Mobile\S*)?\W+Safari/ }
  ];
  for (const { key, pattern } of browserPatterns) {
    const match2 = pattern.exec("Cloudflare-Workers");
    if (match2) {
      const major = match2[1] || 0;
      const minor = match2[2] || 0;
      const patch = match2[3] || 0;
      return { browser: key, version: `${major}.${minor}.${patch}` };
    }
  }
  return null;
}
__name(getBrowserInfo, "getBrowserInfo");
var normalizeArch = /* @__PURE__ */ __name((arch2) => {
  if (arch2 === "x32")
    return "x32";
  if (arch2 === "x86_64" || arch2 === "x64")
    return "x64";
  if (arch2 === "arm")
    return "arm";
  if (arch2 === "aarch64" || arch2 === "arm64")
    return "arm64";
  if (arch2)
    return `other:${arch2}`;
  return "unknown";
}, "normalizeArch");
var normalizePlatform = /* @__PURE__ */ __name((platform2) => {
  platform2 = platform2.toLowerCase();
  if (platform2.includes("ios"))
    return "iOS";
  if (platform2 === "android")
    return "Android";
  if (platform2 === "darwin")
    return "MacOS";
  if (platform2 === "win32")
    return "Windows";
  if (platform2 === "freebsd")
    return "FreeBSD";
  if (platform2 === "openbsd")
    return "OpenBSD";
  if (platform2 === "linux")
    return "Linux";
  if (platform2)
    return `Other:${platform2}`;
  return "Unknown";
}, "normalizePlatform");
var _platformHeaders;
var getPlatformHeaders = /* @__PURE__ */ __name(() => {
  return _platformHeaders ?? (_platformHeaders = getPlatformProperties());
}, "getPlatformHeaders");
var safeJSON = /* @__PURE__ */ __name((text3) => {
  try {
    return JSON.parse(text3);
  } catch (err) {
    return;
  }
}, "safeJSON");
var startsWithSchemeRegexp = /^[a-z][a-z0-9+.-]*:/i;
var isAbsoluteURL = /* @__PURE__ */ __name((url) => {
  return startsWithSchemeRegexp.test(url);
}, "isAbsoluteURL");
var sleep = /* @__PURE__ */ __name((ms) => new Promise((resolve) => setTimeout(resolve, ms)), "sleep");
var validatePositiveInteger = /* @__PURE__ */ __name((name, n) => {
  if (typeof n !== "number" || !Number.isInteger(n)) {
    throw new CloudflareError(`${name} must be an integer`);
  }
  if (n < 0) {
    throw new CloudflareError(`${name} must be a positive integer`);
  }
  return n;
}, "validatePositiveInteger");
var castToError = /* @__PURE__ */ __name((err) => {
  if (err instanceof Error)
    return err;
  if (typeof err === "object" && err !== null) {
    try {
      return new Error(JSON.stringify(err));
    } catch {
    }
  }
  return new Error(err);
}, "castToError");
var readEnv = /* @__PURE__ */ __name((env2) => {
  if (typeof process !== "undefined") {
    return process.env?.[env2]?.trim() ?? void 0;
  }
  if (typeof Deno !== "undefined") {
    return Deno.env?.get?.(env2)?.trim();
  }
  return;
}, "readEnv");
function isEmptyObj(obj) {
  if (!obj)
    return true;
  for (const _k in obj)
    return false;
  return true;
}
__name(isEmptyObj, "isEmptyObj");
function hasOwn(obj, key) {
  return Object.prototype.hasOwnProperty.call(obj, key);
}
__name(hasOwn, "hasOwn");
function applyHeadersMut(targetHeaders, newHeaders) {
  for (const k in newHeaders) {
    if (!hasOwn(newHeaders, k))
      continue;
    const lowerKey = k.toLowerCase();
    if (!lowerKey)
      continue;
    const val = newHeaders[k];
    if (val === null) {
      delete targetHeaders[lowerKey];
    } else if (val !== void 0) {
      targetHeaders[lowerKey] = val;
    }
  }
}
__name(applyHeadersMut, "applyHeadersMut");
function debug3(action, ...args) {
  if (typeof process !== "undefined" && process?.env?.["DEBUG"] === "true") {
    console.log(`Cloudflare:DEBUG:${action}`, ...args);
  }
}
__name(debug3, "debug");
var uuid4 = /* @__PURE__ */ __name(() => {
  return "xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx".replace(/[xy]/g, (c) => {
    const r = Math.random() * 16 | 0;
    const v = c === "x" ? r : r & 3 | 8;
    return v.toString(16);
  });
}, "uuid4");
var isHeadersProtocol = /* @__PURE__ */ __name((headers) => {
  return typeof headers?.get === "function";
}, "isHeadersProtocol");
var getHeader = /* @__PURE__ */ __name((headers, header) => {
  const lowerCasedHeader = header.toLowerCase();
  if (isHeadersProtocol(headers)) {
    const intercapsHeader = header[0]?.toUpperCase() + header.substring(1).replace(/([^\w])(\w)/g, (_m, g1, g2) => g1 + g2.toUpperCase());
    for (const key of [header, lowerCasedHeader, header.toUpperCase(), intercapsHeader]) {
      const value = headers.get(key);
      if (value) {
        return value;
      }
    }
  }
  for (const [key, value] of Object.entries(headers)) {
    if (key.toLowerCase() === lowerCasedHeader) {
      if (Array.isArray(value)) {
        if (value.length <= 1)
          return value[0];
        console.warn(`Received ${value.length} entries for the ${header} header, using the first entry.`);
        return value[0];
      }
      return value;
    }
  }
  return;
}, "getHeader");
var V4PagePagination = class extends AbstractPage {
  static {
    __name(this, "V4PagePagination");
  }
  constructor(client, response, body, options) {
    super(client, response, body, options);
    this.result = body.result || {};
    this.result_info = body.result_info || {};
  }
  getPaginatedItems() {
    return this.result?.items ?? [];
  }
  nextPageParams() {
    const info3 = this.nextPageInfo();
    if (!info3)
      return null;
    if ("params" in info3)
      return info3.params;
    const params = Object.fromEntries(info3.url.searchParams);
    if (!Object.keys(params).length)
      return null;
    return params;
  }
  nextPageInfo() {
    const query = this.options.query;
    const currentPage = query?.page ?? 1;
    return { params: { page: currentPage + 1 } };
  }
};
var V4PagePaginationArray = class extends AbstractPage {
  static {
    __name(this, "V4PagePaginationArray");
  }
  constructor(client, response, body, options) {
    super(client, response, body, options);
    this.result = body.result || [];
    this.result_info = body.result_info || {};
  }
  getPaginatedItems() {
    return this.result ?? [];
  }
  nextPageParams() {
    const info3 = this.nextPageInfo();
    if (!info3)
      return null;
    if ("params" in info3)
      return info3.params;
    const params = Object.fromEntries(info3.url.searchParams);
    if (!Object.keys(params).length)
      return null;
    return params;
  }
  nextPageInfo() {
    const query = this.options.query;
    const currentPage = query?.page ?? 1;
    return { params: { page: currentPage + 1 } };
  }
};
var CursorPagination = class extends AbstractPage {
  static {
    __name(this, "CursorPagination");
  }
  constructor(client, response, body, options) {
    super(client, response, body, options);
    this.result = body.result || [];
    this.result_info = body.result_info || {};
  }
  getPaginatedItems() {
    return this.result ?? [];
  }
  nextPageParams() {
    const info3 = this.nextPageInfo();
    if (!info3)
      return null;
    if ("params" in info3)
      return info3.params;
    const params = Object.fromEntries(info3.url.searchParams);
    if (!Object.keys(params).length)
      return null;
    return params;
  }
  nextPageInfo() {
    const cursor = this.result_info?.cursor;
    if (!cursor) {
      return null;
    }
    return {
      params: {
        cursor
      }
    };
  }
};
var CursorPaginationAfter = class extends AbstractPage {
  static {
    __name(this, "CursorPaginationAfter");
  }
  constructor(client, response, body, options) {
    super(client, response, body, options);
    this.result = body.result || [];
    this.result_info = body.result_info || {};
  }
  getPaginatedItems() {
    return this.result ?? [];
  }
  nextPageParams() {
    const info3 = this.nextPageInfo();
    if (!info3)
      return null;
    if ("params" in info3)
      return info3.params;
    const params = Object.fromEntries(info3.url.searchParams);
    if (!Object.keys(params).length)
      return null;
    return params;
  }
  nextPageInfo() {
    const cursor = this.result_info?.cursors?.after;
    if (!cursor) {
      return null;
    }
    return {
      params: {
        cursor
      }
    };
  }
};
var SinglePage = class extends AbstractPage {
  static {
    __name(this, "SinglePage");
  }
  constructor(client, response, body, options) {
    super(client, response, body, options);
    this.result = body.result || [];
  }
  getPaginatedItems() {
    return this.result ?? [];
  }
  nextPageParams() {
    return null;
  }
  nextPageInfo() {
    return null;
  }
};
var MembersV4PagePaginationArray = class extends V4PagePaginationArray {
  static {
    __name(this, "MembersV4PagePaginationArray");
  }
};
var RolesV4PagePaginationArray = class extends V4PagePaginationArray {
  static {
    __name(this, "RolesV4PagePaginationArray");
  }
};
var SubscriptionsSinglePage = class extends SinglePage {
  static {
    __name(this, "SubscriptionsSinglePage");
  }
};
var TokensV4PagePaginationArray = class extends V4PagePaginationArray {
  static {
    __name(this, "TokensV4PagePaginationArray");
  }
};
var AuditLogsV4PagePaginationArray = class extends V4PagePaginationArray {
  static {
    __name(this, "AuditLogsV4PagePaginationArray");
  }
};
var APIResource = class {
  static {
    __name(this, "APIResource");
  }
  constructor(client) {
    this._client = client;
  }
};
var TotalTLS = class extends APIResource {
  static {
    __name(this, "TotalTLS");
  }
  create(params, options) {
    const { zone_id, ...body } = params;
    return this._client.post(`/zones/${zone_id}/acm/total_tls`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  get(params, options) {
    const { zone_id } = params;
    return this._client.get(`/zones/${zone_id}/acm/total_tls`, options)._thenUnwrap((obj) => obj.result);
  }
};
var ACM = class extends APIResource {
  static {
    __name(this, "ACM");
  }
  constructor() {
    super(...arguments);
    this.totalTLS = new TotalTLS(this._client);
  }
};
ACM.TotalTLS = TotalTLS;
var Authors = class extends APIResource {
  static {
    __name(this, "Authors");
  }
  list(params, options) {
    const { account_id } = params;
    return this._client.getAPIList(`/accounts/${account_id}/ai/authors/search`, AuthorListResponsesSinglePage, options);
  }
};
var AuthorListResponsesSinglePage = class extends SinglePage {
  static {
    __name(this, "AuthorListResponsesSinglePage");
  }
};
Authors.AuthorListResponsesSinglePage = AuthorListResponsesSinglePage;
var Tasks = class extends APIResource {
  static {
    __name(this, "Tasks");
  }
  list(params, options) {
    const { account_id } = params;
    return this._client.getAPIList(`/accounts/${account_id}/ai/tasks/search`, TaskListResponsesSinglePage, options);
  }
};
var TaskListResponsesSinglePage = class extends SinglePage {
  static {
    __name(this, "TaskListResponsesSinglePage");
  }
};
Tasks.TaskListResponsesSinglePage = TaskListResponsesSinglePage;
var ToMarkdown = class extends APIResource {
  static {
    __name(this, "ToMarkdown");
  }
  transform(file, params, options) {
    const { account_id } = params;
    return this._client.getAPIList(`/accounts/${account_id}/ai/tomarkdown`, ToMarkdownTransformResponsesSinglePage, {
      body: file,
      method: "post",
      ...options,
      headers: { "Content-Type": "application/octet-stream", ...options?.headers },
      __binaryRequest: true
    });
  }
};
var ToMarkdownTransformResponsesSinglePage = class extends SinglePage {
  static {
    __name(this, "ToMarkdownTransformResponsesSinglePage");
  }
};
ToMarkdown.ToMarkdownTransformResponsesSinglePage = ToMarkdownTransformResponsesSinglePage;
var Assets = class extends APIResource {
  static {
    __name(this, "Assets");
  }
  create(finetuneId, params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/ai/finetunes/${finetuneId}/finetune-assets`, multipartFormRequestOptions({ body, ...options }));
  }
};
var Public = class extends APIResource {
  static {
    __name(this, "Public");
  }
  list(params, options) {
    const { account_id, ...query } = params;
    return this._client.getAPIList(`/accounts/${account_id}/ai/finetunes/public`, PublicListResponsesSinglePage, { query, ...options });
  }
};
var PublicListResponsesSinglePage = class extends SinglePage {
  static {
    __name(this, "PublicListResponsesSinglePage");
  }
};
Public.PublicListResponsesSinglePage = PublicListResponsesSinglePage;
var Finetunes = class extends APIResource {
  static {
    __name(this, "Finetunes");
  }
  constructor() {
    super(...arguments);
    this.assets = new Assets(this._client);
    this.public = new Public(this._client);
  }
  create(params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/ai/finetunes`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/ai/finetunes`, options)._thenUnwrap((obj) => obj.result);
  }
};
Finetunes.Assets = Assets;
Finetunes.Public = Public;
Finetunes.PublicListResponsesSinglePage = PublicListResponsesSinglePage;
var Schema2 = class extends APIResource {
  static {
    __name(this, "Schema");
  }
  get(params, options) {
    const { account_id, ...query } = params;
    return this._client.get(`/accounts/${account_id}/ai/models/schema`, { query, ...options })._thenUnwrap((obj) => obj.result);
  }
};
var Models = class extends APIResource {
  static {
    __name(this, "Models");
  }
  constructor() {
    super(...arguments);
    this.schema = new Schema2(this._client);
  }
  list(params, options) {
    const { account_id, ...query } = params;
    return this._client.getAPIList(`/accounts/${account_id}/ai/models/search`, ModelListResponsesV4PagePaginationArray, { query, ...options });
  }
};
var ModelListResponsesV4PagePaginationArray = class extends V4PagePaginationArray {
  static {
    __name(this, "ModelListResponsesV4PagePaginationArray");
  }
};
Models.ModelListResponsesV4PagePaginationArray = ModelListResponsesV4PagePaginationArray;
Models.Schema = Schema2;
var AI = class extends APIResource {
  static {
    __name(this, "AI");
  }
  constructor() {
    super(...arguments);
    this.finetunes = new Finetunes(this._client);
    this.authors = new Authors(this._client);
    this.tasks = new Tasks(this._client);
    this.models = new Models(this._client);
    this.toMarkdown = new ToMarkdown(this._client);
  }
  run(modelName, params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/ai/run/${modelName}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
};
AI.Finetunes = Finetunes;
AI.Authors = Authors;
AI.AuthorListResponsesSinglePage = AuthorListResponsesSinglePage;
AI.Tasks = Tasks;
AI.TaskListResponsesSinglePage = TaskListResponsesSinglePage;
AI.Models = Models;
AI.ModelListResponsesV4PagePaginationArray = ModelListResponsesV4PagePaginationArray;
AI.ToMarkdown = ToMarkdown;
AI.ToMarkdownTransformResponsesSinglePage = ToMarkdownTransformResponsesSinglePage;
var Datasets = class extends APIResource {
  static {
    __name(this, "Datasets");
  }
  create(gatewayId, params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/ai-gateway/gateways/${gatewayId}/datasets`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  update(gatewayId, id, params, options) {
    const { account_id, ...body } = params;
    return this._client.put(`/accounts/${account_id}/ai-gateway/gateways/${gatewayId}/datasets/${id}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(gatewayId, params, options) {
    const { account_id, ...query } = params;
    return this._client.getAPIList(`/accounts/${account_id}/ai-gateway/gateways/${gatewayId}/datasets`, DatasetListResponsesV4PagePaginationArray, { query, ...options });
  }
  delete(gatewayId, id, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/ai-gateway/gateways/${gatewayId}/datasets/${id}`, options)._thenUnwrap((obj) => obj.result);
  }
  get(gatewayId, id, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/ai-gateway/gateways/${gatewayId}/datasets/${id}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var DatasetListResponsesV4PagePaginationArray = class extends V4PagePaginationArray {
  static {
    __name(this, "DatasetListResponsesV4PagePaginationArray");
  }
};
Datasets.DatasetListResponsesV4PagePaginationArray = DatasetListResponsesV4PagePaginationArray;
var EvaluationTypes = class extends APIResource {
  static {
    __name(this, "EvaluationTypes");
  }
  list(params, options) {
    const { account_id, ...query } = params;
    return this._client.getAPIList(`/accounts/${account_id}/ai-gateway/evaluation-types`, EvaluationTypeListResponsesV4PagePaginationArray, { query, ...options });
  }
};
var EvaluationTypeListResponsesV4PagePaginationArray = class extends V4PagePaginationArray {
  static {
    __name(this, "EvaluationTypeListResponsesV4PagePaginationArray");
  }
};
EvaluationTypes.EvaluationTypeListResponsesV4PagePaginationArray = EvaluationTypeListResponsesV4PagePaginationArray;
var Evaluations = class extends APIResource {
  static {
    __name(this, "Evaluations");
  }
  create(gatewayId, params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/ai-gateway/gateways/${gatewayId}/evaluations`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(gatewayId, params, options) {
    const { account_id, ...query } = params;
    return this._client.getAPIList(`/accounts/${account_id}/ai-gateway/gateways/${gatewayId}/evaluations`, EvaluationListResponsesV4PagePaginationArray, { query, ...options });
  }
  delete(gatewayId, id, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/ai-gateway/gateways/${gatewayId}/evaluations/${id}`, options)._thenUnwrap((obj) => obj.result);
  }
  get(gatewayId, id, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/ai-gateway/gateways/${gatewayId}/evaluations/${id}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var EvaluationListResponsesV4PagePaginationArray = class extends V4PagePaginationArray {
  static {
    __name(this, "EvaluationListResponsesV4PagePaginationArray");
  }
};
Evaluations.EvaluationListResponsesV4PagePaginationArray = EvaluationListResponsesV4PagePaginationArray;
var Logs = class extends APIResource {
  static {
    __name(this, "Logs");
  }
  list(gatewayId, params, options) {
    const { account_id, ...query } = params;
    return this._client.getAPIList(`/accounts/${account_id}/ai-gateway/gateways/${gatewayId}/logs`, LogListResponsesV4PagePaginationArray, { query, ...options });
  }
  delete(gatewayId, params, options) {
    const { account_id, filters, limit: limit2, order_by, order_by_direction } = params;
    return this._client.delete(`/accounts/${account_id}/ai-gateway/gateways/${gatewayId}/logs`, {
      query: { filters, limit: limit2, order_by, order_by_direction },
      ...options
    });
  }
  edit(gatewayId, id, params, options) {
    const { account_id, ...body } = params;
    return this._client.patch(`/accounts/${account_id}/ai-gateway/gateways/${gatewayId}/logs/${id}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  get(gatewayId, id, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/ai-gateway/gateways/${gatewayId}/logs/${id}`, options)._thenUnwrap((obj) => obj.result);
  }
  request(gatewayId, id, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/ai-gateway/gateways/${gatewayId}/logs/${id}/request`, options);
  }
  response(gatewayId, id, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/ai-gateway/gateways/${gatewayId}/logs/${id}/response`, options);
  }
};
var LogListResponsesV4PagePaginationArray = class extends V4PagePaginationArray {
  static {
    __name(this, "LogListResponsesV4PagePaginationArray");
  }
};
Logs.LogListResponsesV4PagePaginationArray = LogListResponsesV4PagePaginationArray;
var URLs = class extends APIResource {
  static {
    __name(this, "URLs");
  }
  get(gatewayId, provider, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/ai-gateway/gateways/${gatewayId}/url/${provider}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var AIGateway = class extends APIResource {
  static {
    __name(this, "AIGateway");
  }
  constructor() {
    super(...arguments);
    this.evaluationTypes = new EvaluationTypes(this._client);
    this.logs = new Logs(this._client);
    this.datasets = new Datasets(this._client);
    this.evaluations = new Evaluations(this._client);
    this.urls = new URLs(this._client);
  }
  create(params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/ai-gateway/gateways`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  update(id, params, options) {
    const { account_id, ...body } = params;
    return this._client.put(`/accounts/${account_id}/ai-gateway/gateways/${id}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { account_id, ...query } = params;
    return this._client.getAPIList(`/accounts/${account_id}/ai-gateway/gateways`, AIGatewayListResponsesV4PagePaginationArray, { query, ...options });
  }
  delete(id, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/ai-gateway/gateways/${id}`, options)._thenUnwrap((obj) => obj.result);
  }
  get(id, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/ai-gateway/gateways/${id}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var AIGatewayListResponsesV4PagePaginationArray = class extends V4PagePaginationArray {
  static {
    __name(this, "AIGatewayListResponsesV4PagePaginationArray");
  }
};
AIGateway.AIGatewayListResponsesV4PagePaginationArray = AIGatewayListResponsesV4PagePaginationArray;
AIGateway.EvaluationTypes = EvaluationTypes;
AIGateway.EvaluationTypeListResponsesV4PagePaginationArray = EvaluationTypeListResponsesV4PagePaginationArray;
AIGateway.Logs = Logs;
AIGateway.LogListResponsesV4PagePaginationArray = LogListResponsesV4PagePaginationArray;
AIGateway.Datasets = Datasets;
AIGateway.DatasetListResponsesV4PagePaginationArray = DatasetListResponsesV4PagePaginationArray;
AIGateway.Evaluations = Evaluations;
AIGateway.EvaluationListResponsesV4PagePaginationArray = EvaluationListResponsesV4PagePaginationArray;
AIGateway.URLs = URLs;
var Configurations = class extends APIResource {
  static {
    __name(this, "Configurations");
  }
  update(params, options) {
    const { zone_id, ...body } = params;
    return this._client.put(`/zones/${zone_id}/api_gateway/configuration`, { body, ...options });
  }
  get(params, options) {
    const { zone_id, ...query } = params;
    return this._client.get(`/zones/${zone_id}/api_gateway/configuration`, {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
};
var Schemas = class extends APIResource {
  static {
    __name(this, "Schemas");
  }
  list(params, options) {
    const { zone_id, ...query } = params;
    return this._client.get(`/zones/${zone_id}/api_gateway/schemas`, { query, ...options })._thenUnwrap((obj) => obj.result);
  }
};
var Operations = class extends APIResource {
  static {
    __name(this, "Operations");
  }
  list(params, options) {
    const { zone_id, ...query } = params;
    return this._client.getAPIList(`/zones/${zone_id}/api_gateway/discovery/operations`, DiscoveryOperationsV4PagePaginationArray, { query, ...options });
  }
  bulkEdit(params, options) {
    const { zone_id, body } = params;
    return this._client.patch(`/zones/${zone_id}/api_gateway/discovery/operations`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  edit(operationId, params, options) {
    const { zone_id, ...body } = params;
    return this._client.patch(`/zones/${zone_id}/api_gateway/discovery/operations/${operationId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
};
var Discovery = class extends APIResource {
  static {
    __name(this, "Discovery");
  }
  constructor() {
    super(...arguments);
    this.operations = new Operations(this._client);
  }
  get(params, options) {
    const { zone_id } = params;
    return this._client.get(`/zones/${zone_id}/api_gateway/discovery`, options)._thenUnwrap((obj) => obj.result);
  }
};
var DiscoveryOperationsV4PagePaginationArray = class extends V4PagePaginationArray {
  static {
    __name(this, "DiscoveryOperationsV4PagePaginationArray");
  }
};
Discovery.Operations = Operations;
var Fallthrough = class extends APIResource {
  static {
    __name(this, "Fallthrough");
  }
  create(params, options) {
    const { zone_id, ...body } = params;
    return this._client.post(`/zones/${zone_id}/api_gateway/expression-template/fallthrough`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
};
var ExpressionTemplate = class extends APIResource {
  static {
    __name(this, "ExpressionTemplate");
  }
  constructor() {
    super(...arguments);
    this.fallthrough = new Fallthrough(this._client);
  }
};
ExpressionTemplate.Fallthrough = Fallthrough;
var SchemaValidation = class extends APIResource {
  static {
    __name(this, "SchemaValidation");
  }
  update(operationId, params, options) {
    const { zone_id, ...body } = params;
    return this._client.put(`/zones/${zone_id}/api_gateway/operations/${operationId}/schema_validation`, {
      body,
      ...options
    });
  }
  edit(params, options) {
    const { zone_id, settings_multiple_request } = params;
    return this._client.patch(`/zones/${zone_id}/api_gateway/operations/schema_validation`, {
      body: settings_multiple_request,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  get(operationId, params, options) {
    const { zone_id } = params;
    return this._client.get(`/zones/${zone_id}/api_gateway/operations/${operationId}/schema_validation`, options);
  }
};
var Operations2 = class extends APIResource {
  static {
    __name(this, "Operations2");
  }
  constructor() {
    super(...arguments);
    this.schemaValidation = new SchemaValidation(this._client);
  }
  create(params, options) {
    const { zone_id, ...body } = params;
    return this._client.post(`/zones/${zone_id}/api_gateway/operations/item`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { zone_id, ...query } = params;
    return this._client.getAPIList(`/zones/${zone_id}/api_gateway/operations`, OperationListResponsesV4PagePaginationArray, { query, ...options });
  }
  delete(operationId, params, options) {
    const { zone_id } = params;
    return this._client.delete(`/zones/${zone_id}/api_gateway/operations/${operationId}`, options);
  }
  bulkCreate(params, options) {
    const { zone_id, body } = params;
    return this._client.getAPIList(`/zones/${zone_id}/api_gateway/operations`, OperationBulkCreateResponsesSinglePage, { body, method: "post", ...options });
  }
  bulkDelete(params, options) {
    const { zone_id } = params;
    return this._client.delete(`/zones/${zone_id}/api_gateway/operations`, options);
  }
  get(operationId, params, options) {
    const { zone_id, ...query } = params;
    return this._client.get(`/zones/${zone_id}/api_gateway/operations/${operationId}`, {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
};
var OperationListResponsesV4PagePaginationArray = class extends V4PagePaginationArray {
  static {
    __name(this, "OperationListResponsesV4PagePaginationArray");
  }
};
var OperationBulkCreateResponsesSinglePage = class extends SinglePage {
  static {
    __name(this, "OperationBulkCreateResponsesSinglePage");
  }
};
Operations2.OperationListResponsesV4PagePaginationArray = OperationListResponsesV4PagePaginationArray;
Operations2.OperationBulkCreateResponsesSinglePage = OperationBulkCreateResponsesSinglePage;
Operations2.SchemaValidation = SchemaValidation;
var SchemaValidation2 = class extends APIResource {
  static {
    __name(this, "SchemaValidation2");
  }
  update(params, options) {
    const { zone_id, ...body } = params;
    return this._client.put(`/zones/${zone_id}/api_gateway/settings/schema_validation`, { body, ...options });
  }
  edit(params, options) {
    const { zone_id, ...body } = params;
    return this._client.patch(`/zones/${zone_id}/api_gateway/settings/schema_validation`, {
      body,
      ...options
    });
  }
  get(params, options) {
    const { zone_id } = params;
    return this._client.get(`/zones/${zone_id}/api_gateway/settings/schema_validation`, options);
  }
};
var Settings = class extends APIResource {
  static {
    __name(this, "Settings");
  }
  constructor() {
    super(...arguments);
    this.schemaValidation = new SchemaValidation2(this._client);
  }
};
Settings.SchemaValidation = SchemaValidation2;
var Hosts = class extends APIResource {
  static {
    __name(this, "Hosts");
  }
  list(params, options) {
    const { zone_id, ...query } = params;
    return this._client.getAPIList(`/zones/${zone_id}/api_gateway/user_schemas/hosts`, HostListResponsesV4PagePaginationArray, { query, ...options });
  }
};
var HostListResponsesV4PagePaginationArray = class extends V4PagePaginationArray {
  static {
    __name(this, "HostListResponsesV4PagePaginationArray");
  }
};
Hosts.HostListResponsesV4PagePaginationArray = HostListResponsesV4PagePaginationArray;
var Operations3 = class extends APIResource {
  static {
    __name(this, "Operations3");
  }
  list(schemaId, params, options) {
    const { zone_id, ...query } = params;
    return this._client.getAPIList(`/zones/${zone_id}/api_gateway/user_schemas/${schemaId}/operations`, OperationListResponsesV4PagePaginationArray2, { query, ...options });
  }
};
var OperationListResponsesV4PagePaginationArray2 = class extends V4PagePaginationArray {
  static {
    __name(this, "OperationListResponsesV4PagePaginationArray2");
  }
};
Operations3.OperationListResponsesV4PagePaginationArray = OperationListResponsesV4PagePaginationArray2;
var UserSchemas = class extends APIResource {
  static {
    __name(this, "UserSchemas");
  }
  constructor() {
    super(...arguments);
    this.operations = new Operations3(this._client);
    this.hosts = new Hosts(this._client);
  }
  create(params, options) {
    const { zone_id, ...body } = params;
    return this._client.post(`/zones/${zone_id}/api_gateway/user_schemas`, multipartFormRequestOptions({ body, ...options }))._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { zone_id, ...query } = params;
    return this._client.getAPIList(`/zones/${zone_id}/api_gateway/user_schemas`, PublicSchemasV4PagePaginationArray, { query, ...options });
  }
  delete(schemaId, params, options) {
    const { zone_id } = params;
    return this._client.delete(`/zones/${zone_id}/api_gateway/user_schemas/${schemaId}`, options);
  }
  edit(schemaId, params, options) {
    const { zone_id, ...body } = params;
    return this._client.patch(`/zones/${zone_id}/api_gateway/user_schemas/${schemaId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  get(schemaId, params, options) {
    const { zone_id, ...query } = params;
    return this._client.get(`/zones/${zone_id}/api_gateway/user_schemas/${schemaId}`, {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
};
var PublicSchemasV4PagePaginationArray = class extends V4PagePaginationArray {
  static {
    __name(this, "PublicSchemasV4PagePaginationArray");
  }
};
UserSchemas.PublicSchemasV4PagePaginationArray = PublicSchemasV4PagePaginationArray;
UserSchemas.Operations = Operations3;
UserSchemas.OperationListResponsesV4PagePaginationArray = OperationListResponsesV4PagePaginationArray2;
UserSchemas.Hosts = Hosts;
UserSchemas.HostListResponsesV4PagePaginationArray = HostListResponsesV4PagePaginationArray;
var APIGateway = class extends APIResource {
  static {
    __name(this, "APIGateway");
  }
  constructor() {
    super(...arguments);
    this.configurations = new Configurations(this._client);
    this.discovery = new Discovery(this._client);
    this.operations = new Operations2(this._client);
    this.schemas = new Schemas(this._client);
    this.settings = new Settings(this._client);
    this.userSchemas = new UserSchemas(this._client);
    this.expressionTemplate = new ExpressionTemplate(this._client);
  }
};
APIGateway.Configurations = Configurations;
APIGateway.Discovery = Discovery;
APIGateway.Operations = Operations2;
APIGateway.OperationListResponsesV4PagePaginationArray = OperationListResponsesV4PagePaginationArray;
APIGateway.OperationBulkCreateResponsesSinglePage = OperationBulkCreateResponsesSinglePage;
APIGateway.Schemas = Schemas;
APIGateway.UserSchemas = UserSchemas;
APIGateway.PublicSchemasV4PagePaginationArray = PublicSchemasV4PagePaginationArray;
APIGateway.ExpressionTemplate = ExpressionTemplate;
var AbuseReports = class extends APIResource {
  static {
    __name(this, "AbuseReports");
  }
  create(reportType, params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/abuse-reports/${reportType}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
};
var Members = class extends APIResource {
  static {
    __name(this, "Members");
  }
  create(params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/members`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  update(memberId, params, options) {
    const { account_id, ...body } = params;
    return this._client.put(`/accounts/${account_id}/members/${memberId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { account_id, ...query } = params;
    return this._client.getAPIList(`/accounts/${account_id}/members`, MembersV4PagePaginationArray, {
      query,
      ...options
    });
  }
  delete(memberId, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/members/${memberId}`, options)._thenUnwrap((obj) => obj.result);
  }
  get(memberId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/members/${memberId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var Roles = class extends APIResource {
  static {
    __name(this, "Roles");
  }
  list(params, options) {
    const { account_id, ...query } = params;
    return this._client.getAPIList(`/accounts/${account_id}/roles`, RolesV4PagePaginationArray, {
      query,
      ...options
    });
  }
  get(roleId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/roles/${roleId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var Subscriptions = class extends APIResource {
  static {
    __name(this, "Subscriptions");
  }
  create(params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/subscriptions`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  update(subscriptionIdentifier, params, options) {
    const { account_id, ...body } = params;
    return this._client.put(`/accounts/${account_id}/subscriptions/${subscriptionIdentifier}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  delete(subscriptionIdentifier, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/subscriptions/${subscriptionIdentifier}`, options)._thenUnwrap((obj) => obj.result);
  }
  get(params, options) {
    const { account_id } = params;
    return this._client.getAPIList(`/accounts/${account_id}/subscriptions`, SubscriptionsSinglePage, options);
  }
};
var Audit = class extends APIResource {
  static {
    __name(this, "Audit");
  }
  list(params, options) {
    const { account_id, ...query } = params;
    return this._client.getAPIList(`/accounts/${account_id}/logs/audit`, AuditListResponsesCursorPaginationAfter, { query, ...options });
  }
};
var AuditListResponsesCursorPaginationAfter = class extends CursorPaginationAfter {
  static {
    __name(this, "AuditListResponsesCursorPaginationAfter");
  }
};
Audit.AuditListResponsesCursorPaginationAfter = AuditListResponsesCursorPaginationAfter;
var Logs2 = class extends APIResource {
  static {
    __name(this, "Logs2");
  }
  constructor() {
    super(...arguments);
    this.audit = new Audit(this._client);
  }
};
Logs2.Audit = Audit;
Logs2.AuditListResponsesCursorPaginationAfter = AuditListResponsesCursorPaginationAfter;
var PermissionGroups = class extends APIResource {
  static {
    __name(this, "PermissionGroups");
  }
  list(params, options) {
    const { account_id, ...query } = params;
    return this._client.getAPIList(`/accounts/${account_id}/tokens/permission_groups`, PermissionGroupListResponsesSinglePage, { query, ...options });
  }
  get(params, options) {
    const { account_id, ...query } = params;
    return this._client.get(`/accounts/${account_id}/tokens/permission_groups`, {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
};
var PermissionGroupListResponsesSinglePage = class extends SinglePage {
  static {
    __name(this, "PermissionGroupListResponsesSinglePage");
  }
};
PermissionGroups.PermissionGroupListResponsesSinglePage = PermissionGroupListResponsesSinglePage;
var Value = class extends APIResource {
  static {
    __name(this, "Value");
  }
  update(tokenId, params, options) {
    const { account_id, body } = params;
    return this._client.put(`/accounts/${account_id}/tokens/${tokenId}/value`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
};
var Tokens = class extends APIResource {
  static {
    __name(this, "Tokens");
  }
  constructor() {
    super(...arguments);
    this.permissionGroups = new PermissionGroups(this._client);
    this.value = new Value(this._client);
  }
  create(params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/tokens`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  update(tokenId, params, options) {
    const { account_id, ...body } = params;
    return this._client.put(`/accounts/${account_id}/tokens/${tokenId}`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { account_id, ...query } = params;
    return this._client.getAPIList(`/accounts/${account_id}/tokens`, TokensV4PagePaginationArray, {
      query,
      ...options
    });
  }
  delete(tokenId, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/tokens/${tokenId}`, options)._thenUnwrap((obj) => obj.result);
  }
  get(tokenId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/tokens/${tokenId}`, options)._thenUnwrap((obj) => obj.result);
  }
  verify(params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/tokens/verify`, options)._thenUnwrap((obj) => obj.result);
  }
};
Tokens.PermissionGroups = PermissionGroups;
Tokens.PermissionGroupListResponsesSinglePage = PermissionGroupListResponsesSinglePage;
Tokens.Value = Value;
var Accounts = class extends APIResource {
  static {
    __name(this, "Accounts");
  }
  constructor() {
    super(...arguments);
    this.members = new Members(this._client);
    this.roles = new Roles(this._client);
    this.subscriptions = new Subscriptions(this._client);
    this.tokens = new Tokens(this._client);
    this.logs = new Logs2(this._client);
  }
  create(body, options) {
    return this._client.post("/accounts", { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  update(params, options) {
    const { account_id, ...body } = params;
    return this._client.put(`/accounts/${account_id}`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  list(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.list({}, query);
    }
    return this._client.getAPIList("/accounts", AccountsV4PagePaginationArray, { query, ...options });
  }
  delete(params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}`, options)._thenUnwrap((obj) => obj.result);
  }
  get(params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var AccountsV4PagePaginationArray = class extends V4PagePaginationArray {
  static {
    __name(this, "AccountsV4PagePaginationArray");
  }
};
Accounts.AccountsV4PagePaginationArray = AccountsV4PagePaginationArray;
Accounts.Members = Members;
Accounts.Roles = Roles;
Accounts.Subscriptions = Subscriptions;
Accounts.Tokens = Tokens;
Accounts.Logs = Logs2;
var LOADocuments = class extends APIResource {
  static {
    __name(this, "LOADocuments");
  }
  create(params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/addressing/loa_documents`, multipartFormRequestOptions({ body, ...options }))._thenUnwrap((obj) => obj.result);
  }
  get(loaDocumentId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/addressing/loa_documents/${loaDocumentId}/download`, {
      ...options,
      headers: { Accept: "application/pdf", ...options?.headers },
      __binaryResponse: true
    });
  }
};
var Services = class extends APIResource {
  static {
    __name(this, "Services");
  }
  list(params, options) {
    const { account_id } = params;
    return this._client.getAPIList(`/accounts/${account_id}/addressing/services`, ServiceListResponsesSinglePage, options);
  }
};
var ServiceListResponsesSinglePage = class extends SinglePage {
  static {
    __name(this, "ServiceListResponsesSinglePage");
  }
};
Services.ServiceListResponsesSinglePage = ServiceListResponsesSinglePage;
var Accounts2 = class extends APIResource {
  static {
    __name(this, "Accounts2");
  }
  update(addressMapId, params, options) {
    const { account_id, body } = params;
    return this._client.put(`/accounts/${account_id}/addressing/address_maps/${addressMapId}/accounts/${account_id}`, { body, ...options });
  }
  delete(addressMapId, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/addressing/address_maps/${addressMapId}/accounts/${account_id}`, options);
  }
};
var IPs = class extends APIResource {
  static {
    __name(this, "IPs");
  }
  update(addressMapId, ipAddress, params, options) {
    const { account_id, body } = params;
    return this._client.put(`/accounts/${account_id}/addressing/address_maps/${addressMapId}/ips/${ipAddress}`, { body, ...options });
  }
  delete(addressMapId, ipAddress, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/addressing/address_maps/${addressMapId}/ips/${ipAddress}`, options);
  }
};
var Zones = class extends APIResource {
  static {
    __name(this, "Zones");
  }
  update(addressMapId, params, options) {
    const { zone_id, account_id, body } = params;
    return this._client.put(`/accounts/${account_id}/addressing/address_maps/${addressMapId}/zones/${zone_id}`, { body, ...options });
  }
  delete(addressMapId, params, options) {
    const { zone_id, account_id } = params;
    return this._client.delete(`/accounts/${account_id}/addressing/address_maps/${addressMapId}/zones/${zone_id}`, options);
  }
};
var AddressMaps = class extends APIResource {
  static {
    __name(this, "AddressMaps");
  }
  constructor() {
    super(...arguments);
    this.accounts = new Accounts2(this._client);
    this.ips = new IPs(this._client);
    this.zones = new Zones(this._client);
  }
  create(params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/addressing/address_maps`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { account_id } = params;
    return this._client.getAPIList(`/accounts/${account_id}/addressing/address_maps`, AddressMapsSinglePage, options);
  }
  delete(addressMapId, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/addressing/address_maps/${addressMapId}`, options);
  }
  edit(addressMapId, params, options) {
    const { account_id, ...body } = params;
    return this._client.patch(`/accounts/${account_id}/addressing/address_maps/${addressMapId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  get(addressMapId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/addressing/address_maps/${addressMapId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var AddressMapsSinglePage = class extends SinglePage {
  static {
    __name(this, "AddressMapsSinglePage");
  }
};
AddressMaps.AddressMapsSinglePage = AddressMapsSinglePage;
AddressMaps.Accounts = Accounts2;
AddressMaps.IPs = IPs;
AddressMaps.Zones = Zones;
var AdvertisementStatus = class extends APIResource {
  static {
    __name(this, "AdvertisementStatus");
  }
  edit(prefixId, params, options) {
    const { account_id, ...body } = params;
    return this._client.patch(`/accounts/${account_id}/addressing/prefixes/${prefixId}/bgp/status`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  get(prefixId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/addressing/prefixes/${prefixId}/bgp/status`, options)._thenUnwrap((obj) => obj.result);
  }
};
var BGPPrefixes = class extends APIResource {
  static {
    __name(this, "BGPPrefixes");
  }
  create(prefixId, params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/addressing/prefixes/${prefixId}/bgp/prefixes`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(prefixId, params, options) {
    const { account_id } = params;
    return this._client.getAPIList(`/accounts/${account_id}/addressing/prefixes/${prefixId}/bgp/prefixes`, BGPPrefixesSinglePage, options);
  }
  edit(prefixId, bgpPrefixId, params, options) {
    const { account_id, ...body } = params;
    return this._client.patch(`/accounts/${account_id}/addressing/prefixes/${prefixId}/bgp/prefixes/${bgpPrefixId}`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  get(prefixId, bgpPrefixId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/addressing/prefixes/${prefixId}/bgp/prefixes/${bgpPrefixId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var BGPPrefixesSinglePage = class extends SinglePage {
  static {
    __name(this, "BGPPrefixesSinglePage");
  }
};
BGPPrefixes.BGPPrefixesSinglePage = BGPPrefixesSinglePage;
var Delegations = class extends APIResource {
  static {
    __name(this, "Delegations");
  }
  create(prefixId, params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/addressing/prefixes/${prefixId}/delegations`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(prefixId, params, options) {
    const { account_id } = params;
    return this._client.getAPIList(`/accounts/${account_id}/addressing/prefixes/${prefixId}/delegations`, DelegationsSinglePage, options);
  }
  delete(prefixId, delegationId, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/addressing/prefixes/${prefixId}/delegations/${delegationId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var DelegationsSinglePage = class extends SinglePage {
  static {
    __name(this, "DelegationsSinglePage");
  }
};
Delegations.DelegationsSinglePage = DelegationsSinglePage;
var ServiceBindings = class extends APIResource {
  static {
    __name(this, "ServiceBindings");
  }
  create(prefixId, params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/addressing/prefixes/${prefixId}/bindings`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(prefixId, params, options) {
    const { account_id } = params;
    return this._client.getAPIList(`/accounts/${account_id}/addressing/prefixes/${prefixId}/bindings`, ServiceBindingsSinglePage, options);
  }
  delete(prefixId, bindingId, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/addressing/prefixes/${prefixId}/bindings/${bindingId}`, options);
  }
  get(prefixId, bindingId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/addressing/prefixes/${prefixId}/bindings/${bindingId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var ServiceBindingsSinglePage = class extends SinglePage {
  static {
    __name(this, "ServiceBindingsSinglePage");
  }
};
ServiceBindings.ServiceBindingsSinglePage = ServiceBindingsSinglePage;
var Prefixes = class extends APIResource {
  static {
    __name(this, "Prefixes");
  }
  constructor() {
    super(...arguments);
    this.serviceBindings = new ServiceBindings(this._client);
    this.bgpPrefixes = new BGPPrefixes(this._client);
    this.advertisementStatus = new AdvertisementStatus(this._client);
    this.delegations = new Delegations(this._client);
  }
  create(params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/addressing/prefixes`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { account_id } = params;
    return this._client.getAPIList(`/accounts/${account_id}/addressing/prefixes`, PrefixesSinglePage, options);
  }
  delete(prefixId, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/addressing/prefixes/${prefixId}`, options);
  }
  edit(prefixId, params, options) {
    const { account_id, ...body } = params;
    return this._client.patch(`/accounts/${account_id}/addressing/prefixes/${prefixId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  get(prefixId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/addressing/prefixes/${prefixId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var PrefixesSinglePage = class extends SinglePage {
  static {
    __name(this, "PrefixesSinglePage");
  }
};
Prefixes.PrefixesSinglePage = PrefixesSinglePage;
Prefixes.ServiceBindings = ServiceBindings;
Prefixes.ServiceBindingsSinglePage = ServiceBindingsSinglePage;
Prefixes.BGPPrefixes = BGPPrefixes;
Prefixes.BGPPrefixesSinglePage = BGPPrefixesSinglePage;
Prefixes.AdvertisementStatus = AdvertisementStatus;
Prefixes.DelegationsSinglePage = DelegationsSinglePage;
var Regions = class extends APIResource {
  static {
    __name(this, "Regions");
  }
  list(params, options) {
    const { account_id } = params;
    return this._client.getAPIList(`/accounts/${account_id}/addressing/regional_hostnames/regions`, RegionListResponsesSinglePage, options);
  }
};
var RegionListResponsesSinglePage = class extends SinglePage {
  static {
    __name(this, "RegionListResponsesSinglePage");
  }
};
Regions.RegionListResponsesSinglePage = RegionListResponsesSinglePage;
var RegionalHostnames = class extends APIResource {
  static {
    __name(this, "RegionalHostnames");
  }
  constructor() {
    super(...arguments);
    this.regions = new Regions(this._client);
  }
  create(params, options) {
    const { zone_id, ...body } = params;
    return this._client.post(`/zones/${zone_id}/addressing/regional_hostnames`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { zone_id } = params;
    return this._client.getAPIList(`/zones/${zone_id}/addressing/regional_hostnames`, RegionalHostnameListResponsesSinglePage, options);
  }
  delete(hostname, params, options) {
    const { zone_id } = params;
    return this._client.delete(`/zones/${zone_id}/addressing/regional_hostnames/${hostname}`, options);
  }
  edit(hostname, params, options) {
    const { zone_id, ...body } = params;
    return this._client.patch(`/zones/${zone_id}/addressing/regional_hostnames/${hostname}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  get(hostname, params, options) {
    const { zone_id } = params;
    return this._client.get(`/zones/${zone_id}/addressing/regional_hostnames/${hostname}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var RegionalHostnameListResponsesSinglePage = class extends SinglePage {
  static {
    __name(this, "RegionalHostnameListResponsesSinglePage");
  }
};
RegionalHostnames.RegionalHostnameListResponsesSinglePage = RegionalHostnameListResponsesSinglePage;
RegionalHostnames.Regions = Regions;
RegionalHostnames.RegionListResponsesSinglePage = RegionListResponsesSinglePage;
var Addressing = class extends APIResource {
  static {
    __name(this, "Addressing");
  }
  constructor() {
    super(...arguments);
    this.regionalHostnames = new RegionalHostnames(this._client);
    this.services = new Services(this._client);
    this.addressMaps = new AddressMaps(this._client);
    this.loaDocuments = new LOADocuments(this._client);
    this.prefixes = new Prefixes(this._client);
  }
};
Addressing.RegionalHostnames = RegionalHostnames;
Addressing.RegionalHostnameListResponsesSinglePage = RegionalHostnameListResponsesSinglePage;
Addressing.Services = Services;
Addressing.ServiceListResponsesSinglePage = ServiceListResponsesSinglePage;
Addressing.AddressMaps = AddressMaps;
Addressing.AddressMapsSinglePage = AddressMapsSinglePage;
Addressing.LOADocuments = LOADocuments;
Addressing.Prefixes = Prefixes;
Addressing.PrefixesSinglePage = PrefixesSinglePage;
var AvailableAlerts = class extends APIResource {
  static {
    __name(this, "AvailableAlerts");
  }
  list(params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/alerting/v3/available_alerts`, options)._thenUnwrap((obj) => obj.result);
  }
};
var HistoryResource = class extends APIResource {
  static {
    __name(this, "HistoryResource");
  }
  list(params, options) {
    const { account_id, ...query } = params;
    return this._client.getAPIList(`/accounts/${account_id}/alerting/v3/history`, HistoriesV4PagePaginationArray, { query, ...options });
  }
};
var HistoriesV4PagePaginationArray = class extends V4PagePaginationArray {
  static {
    __name(this, "HistoriesV4PagePaginationArray");
  }
};
HistoryResource.HistoriesV4PagePaginationArray = HistoriesV4PagePaginationArray;
var Policies = class extends APIResource {
  static {
    __name(this, "Policies");
  }
  create(params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/alerting/v3/policies`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  update(policyId, params, options) {
    const { account_id, ...body } = params;
    return this._client.put(`/accounts/${account_id}/alerting/v3/policies/${policyId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { account_id } = params;
    return this._client.getAPIList(`/accounts/${account_id}/alerting/v3/policies`, PoliciesSinglePage, options);
  }
  delete(policyId, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/alerting/v3/policies/${policyId}`, options);
  }
  get(policyId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/alerting/v3/policies/${policyId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var PoliciesSinglePage = class extends SinglePage {
  static {
    __name(this, "PoliciesSinglePage");
  }
};
Policies.PoliciesSinglePage = PoliciesSinglePage;
var Eligible = class extends APIResource {
  static {
    __name(this, "Eligible");
  }
  get(params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/alerting/v3/destinations/eligible`, options)._thenUnwrap((obj) => obj.result);
  }
};
var PagerdutyResource = class extends APIResource {
  static {
    __name(this, "PagerdutyResource");
  }
  create(params, options) {
    const { account_id } = params;
    return this._client.post(`/accounts/${account_id}/alerting/v3/destinations/pagerduty/connect`, options)._thenUnwrap((obj) => obj.result);
  }
  delete(params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/alerting/v3/destinations/pagerduty`, options);
  }
  get(params, options) {
    const { account_id } = params;
    return this._client.getAPIList(`/accounts/${account_id}/alerting/v3/destinations/pagerduty`, PagerdutiesSinglePage, options);
  }
  link(tokenId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/alerting/v3/destinations/pagerduty/connect/${tokenId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var PagerdutiesSinglePage = class extends SinglePage {
  static {
    __name(this, "PagerdutiesSinglePage");
  }
};
PagerdutyResource.PagerdutiesSinglePage = PagerdutiesSinglePage;
var Webhooks = class extends APIResource {
  static {
    __name(this, "Webhooks");
  }
  create(params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/alerting/v3/destinations/webhooks`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  update(webhookId, params, options) {
    const { account_id, ...body } = params;
    return this._client.put(`/accounts/${account_id}/alerting/v3/destinations/webhooks/${webhookId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { account_id } = params;
    return this._client.getAPIList(`/accounts/${account_id}/alerting/v3/destinations/webhooks`, WebhooksSinglePage, options);
  }
  delete(webhookId, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/alerting/v3/destinations/webhooks/${webhookId}`, options);
  }
  get(webhookId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/alerting/v3/destinations/webhooks/${webhookId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var WebhooksSinglePage = class extends SinglePage {
  static {
    __name(this, "WebhooksSinglePage");
  }
};
Webhooks.WebhooksSinglePage = WebhooksSinglePage;
var Destinations = class extends APIResource {
  static {
    __name(this, "Destinations");
  }
  constructor() {
    super(...arguments);
    this.eligible = new Eligible(this._client);
    this.pagerduty = new PagerdutyResource(this._client);
    this.webhooks = new Webhooks(this._client);
  }
};
Destinations.Eligible = Eligible;
Destinations.PagerdutyResource = PagerdutyResource;
Destinations.PagerdutiesSinglePage = PagerdutiesSinglePage;
Destinations.WebhooksSinglePage = WebhooksSinglePage;
var Alerting = class extends APIResource {
  static {
    __name(this, "Alerting");
  }
  constructor() {
    super(...arguments);
    this.availableAlerts = new AvailableAlerts(this._client);
    this.destinations = new Destinations(this._client);
    this.history = new HistoryResource(this._client);
    this.policies = new Policies(this._client);
  }
};
Alerting.AvailableAlerts = AvailableAlerts;
Alerting.Destinations = Destinations;
Alerting.HistoryResource = HistoryResource;
Alerting.HistoriesV4PagePaginationArray = HistoriesV4PagePaginationArray;
Alerting.Policies = Policies;
Alerting.PoliciesSinglePage = PoliciesSinglePage;
var SmartRouting = class extends APIResource {
  static {
    __name(this, "SmartRouting");
  }
  edit(params, options) {
    const { zone_id, ...body } = params;
    return this._client.patch(`/zones/${zone_id}/argo/smart_routing`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  get(params, options) {
    const { zone_id } = params;
    return this._client.get(`/zones/${zone_id}/argo/smart_routing`, options)._thenUnwrap((obj) => obj.result);
  }
};
var TieredCaching = class extends APIResource {
  static {
    __name(this, "TieredCaching");
  }
  edit(params, options) {
    const { zone_id, ...body } = params;
    return this._client.patch(`/zones/${zone_id}/argo/tiered_caching`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  get(params, options) {
    const { zone_id } = params;
    return this._client.get(`/zones/${zone_id}/argo/tiered_caching`, options)._thenUnwrap((obj) => obj.result);
  }
};
var Argo = class extends APIResource {
  static {
    __name(this, "Argo");
  }
  constructor() {
    super(...arguments);
    this.smartRouting = new SmartRouting(this._client);
    this.tieredCaching = new TieredCaching(this._client);
  }
};
Argo.SmartRouting = SmartRouting;
Argo.TieredCaching = TieredCaching;
var AuditLogs = class extends APIResource {
  static {
    __name(this, "AuditLogs");
  }
  list(params, options) {
    const { account_id, ...query } = params;
    return this._client.getAPIList(`/accounts/${account_id}/audit_logs`, AuditLogsV4PagePaginationArray, {
      query,
      ...options
    });
  }
};
var Profiles = class extends APIResource {
  static {
    __name(this, "Profiles");
  }
  get(params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/billing/profile`, options)._thenUnwrap((obj) => obj.result);
  }
};
var Billing = class extends APIResource {
  static {
    __name(this, "Billing");
  }
  constructor() {
    super(...arguments);
    this.profiles = new Profiles(this._client);
  }
};
Billing.Profiles = Profiles;
var BotManagement = class extends APIResource {
  static {
    __name(this, "BotManagement");
  }
  update(params, options) {
    const { zone_id, ...body } = params;
    return this._client.put(`/zones/${zone_id}/bot_management`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  get(params, options) {
    const { zone_id } = params;
    return this._client.get(`/zones/${zone_id}/bot_management`, options)._thenUnwrap((obj) => obj.result);
  }
};
var ASN = class extends APIResource {
  static {
    __name(this, "ASN");
  }
  dayReport(asnId, params, options) {
    const { account_id, ...query } = params;
    return this._client.get(`/accounts/${account_id}/botnet_feed/asn/${asnId}/day_report`, {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  fullReport(asnId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/botnet_feed/asn/${asnId}/full_report`, options)._thenUnwrap((obj) => obj.result);
  }
};
var ASN2 = class extends APIResource {
  static {
    __name(this, "ASN2");
  }
  delete(asnId, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/botnet_feed/configs/asn/${asnId}`, options)._thenUnwrap((obj) => obj.result);
  }
  get(params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/botnet_feed/configs/asn`, options)._thenUnwrap((obj) => obj.result);
  }
};
var Configs = class extends APIResource {
  static {
    __name(this, "Configs");
  }
  constructor() {
    super(...arguments);
    this.asn = new ASN2(this._client);
  }
};
Configs.ASN = ASN2;
var BotnetFeed = class extends APIResource {
  static {
    __name(this, "BotnetFeed");
  }
  constructor() {
    super(...arguments);
    this.asn = new ASN(this._client);
    this.configs = new Configs(this._client);
  }
};
BotnetFeed.ASN = ASN;
BotnetFeed.Configs = Configs;
var LogoMatches = class extends APIResource {
  static {
    __name(this, "LogoMatches");
  }
  download(params, options) {
    const { account_id, ...query } = params;
    return this._client.get(`/accounts/${account_id}/brand-protection/logo-matches/download`, {
      query,
      ...options
    });
  }
  get(params, options) {
    const { account_id, ...query } = params;
    return this._client.get(`/accounts/${account_id}/brand-protection/logo-matches`, { query, ...options });
  }
};
var Logos = class extends APIResource {
  static {
    __name(this, "Logos");
  }
  create(params, options) {
    const { account_id, match_type, tag: tag2, threshold, ...body } = params;
    return this._client.post(`/accounts/${account_id}/brand-protection/logos`, {
      query: { match_type, tag: tag2, threshold },
      body,
      ...options,
      headers: { "Content-Type": "application/x-www-form-urlencoded", ...options?.headers }
    });
  }
  delete(logoId, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/brand-protection/logos/${logoId}`, {
      ...options,
      headers: { Accept: "*/*", ...options?.headers }
    });
  }
};
var Matches = class extends APIResource {
  static {
    __name(this, "Matches");
  }
  download(params, options) {
    const { account_id, ...query } = params;
    return this._client.get(`/accounts/${account_id}/brand-protection/matches/download`, {
      query,
      ...options
    });
  }
  get(params, options) {
    const { account_id, ...query } = params;
    return this._client.get(`/accounts/${account_id}/brand-protection/matches`, { query, ...options });
  }
};
var Queries = class extends APIResource {
  static {
    __name(this, "Queries");
  }
  create(params, options) {
    const { account_id, id, query_scan, query_tag, ...body } = params;
    return this._client.post(`/accounts/${account_id}/brand-protection/queries`, {
      query: { id, scan: query_scan, tag: query_tag },
      body,
      ...options,
      headers: { Accept: "*/*", ...options?.headers }
    });
  }
  delete(params, options) {
    const { account_id, id, scan, tag: tag2 } = params;
    return this._client.delete(`/accounts/${account_id}/brand-protection/queries`, {
      query: { id, scan, tag: tag2 },
      ...options,
      headers: { Accept: "*/*", ...options?.headers }
    });
  }
  bulk(params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/brand-protection/queries/bulk`, {
      body,
      ...options,
      headers: { Accept: "*/*", ...options?.headers }
    });
  }
};
var BrandProtection = class extends APIResource {
  static {
    __name(this, "BrandProtection");
  }
  constructor() {
    super(...arguments);
    this.queries = new Queries(this._client);
    this.matches = new Matches(this._client);
    this.logos = new Logos(this._client);
    this.logoMatches = new LogoMatches(this._client);
  }
  submit(params, options) {
    const { account_id } = params;
    return this._client.post(`/accounts/${account_id}/brand-protection/submit`, options);
  }
  urlInfo(params, options) {
    const { account_id } = params;
    return this._client.getAPIList(`/accounts/${account_id}/brand-protection/url-info`, BrandProtectionURLInfoResponsesSinglePage, options);
  }
};
var BrandProtectionURLInfoResponsesSinglePage = class extends SinglePage {
  static {
    __name(this, "BrandProtectionURLInfoResponsesSinglePage");
  }
};
BrandProtection.BrandProtectionURLInfoResponsesSinglePage = BrandProtectionURLInfoResponsesSinglePage;
BrandProtection.Queries = Queries;
BrandProtection.Matches = Matches;
BrandProtection.Logos = Logos;
BrandProtection.LogoMatches = LogoMatches;
var Content = class extends APIResource {
  static {
    __name(this, "Content");
  }
  create(params, options) {
    const { account_id, cacheTTL, ...body } = params;
    return this._client.post(`/accounts/${account_id}/browser-rendering/content`, {
      query: { cacheTTL },
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
};
var Json = class extends APIResource {
  static {
    __name(this, "Json");
  }
  create(params, options) {
    const { account_id, cacheTTL, ...body } = params;
    return this._client.post(`/accounts/${account_id}/browser-rendering/json`, {
      query: { cacheTTL },
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
};
var Links = class extends APIResource {
  static {
    __name(this, "Links");
  }
  create(params, options) {
    const { account_id, cacheTTL, ...body } = params;
    return this._client.post(`/accounts/${account_id}/browser-rendering/links`, {
      query: { cacheTTL },
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
};
var Markdown = class extends APIResource {
  static {
    __name(this, "Markdown");
  }
  create(params, options) {
    const { account_id, cacheTTL, ...body } = params;
    return this._client.post(`/accounts/${account_id}/browser-rendering/markdown`, {
      query: { cacheTTL },
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
};
var PDF = class extends APIResource {
  static {
    __name(this, "PDF");
  }
  create(params, options) {
    const { account_id, cacheTTL, ...body } = params;
    return this._client.post(`/accounts/${account_id}/browser-rendering/pdf`, {
      query: { cacheTTL },
      body,
      ...options,
      headers: { Accept: "application/pdf", ...options?.headers },
      __binaryResponse: true
    });
  }
};
var Scrape = class extends APIResource {
  static {
    __name(this, "Scrape");
  }
  create(params, options) {
    const { account_id, cacheTTL, ...body } = params;
    return this._client.post(`/accounts/${account_id}/browser-rendering/scrape`, {
      query: { cacheTTL },
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
};
var Screenshot = class extends APIResource {
  static {
    __name(this, "Screenshot");
  }
  create(params, options) {
    const { account_id, cacheTTL, ...body } = params;
    return this._client.post(`/accounts/${account_id}/browser-rendering/screenshot`, {
      query: { cacheTTL },
      body,
      ...options
    });
  }
};
var Snapshot = class extends APIResource {
  static {
    __name(this, "Snapshot");
  }
  create(params, options) {
    const { account_id, cacheTTL, ...body } = params;
    return this._client.post(`/accounts/${account_id}/browser-rendering/snapshot`, {
      query: { cacheTTL },
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
};
var BrowserRendering = class extends APIResource {
  static {
    __name(this, "BrowserRendering");
  }
  constructor() {
    super(...arguments);
    this.content = new Content(this._client);
    this.pdf = new PDF(this._client);
    this.scrape = new Scrape(this._client);
    this.screenshot = new Screenshot(this._client);
    this.snapshot = new Snapshot(this._client);
    this.json = new Json(this._client);
    this.links = new Links(this._client);
    this.markdown = new Markdown(this._client);
  }
};
BrowserRendering.Content = Content;
BrowserRendering.PDF = PDF;
BrowserRendering.Scrape = Scrape;
BrowserRendering.Screenshot = Screenshot;
BrowserRendering.Snapshot = Snapshot;
BrowserRendering.Json = Json;
BrowserRendering.Links = Links;
BrowserRendering.Markdown = Markdown;
var CacheReserveResource = class extends APIResource {
  static {
    __name(this, "CacheReserveResource");
  }
  clear(params, options) {
    const { zone_id, body } = params;
    return this._client.post(`/zones/${zone_id}/cache/cache_reserve_clear`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  edit(params, options) {
    const { zone_id, ...body } = params;
    return this._client.patch(`/zones/${zone_id}/cache/cache_reserve`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  get(params, options) {
    const { zone_id } = params;
    return this._client.get(`/zones/${zone_id}/cache/cache_reserve`, options)._thenUnwrap((obj) => obj.result);
  }
  status(params, options) {
    const { zone_id } = params;
    return this._client.get(`/zones/${zone_id}/cache/cache_reserve_clear`, options)._thenUnwrap((obj) => obj.result);
  }
};
var RegionalTieredCacheResource = class extends APIResource {
  static {
    __name(this, "RegionalTieredCacheResource");
  }
  edit(params, options) {
    const { zone_id, ...body } = params;
    return this._client.patch(`/zones/${zone_id}/cache/regional_tiered_cache`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  get(params, options) {
    const { zone_id } = params;
    return this._client.get(`/zones/${zone_id}/cache/regional_tiered_cache`, options)._thenUnwrap((obj) => obj.result);
  }
};
var SmartTieredCache = class extends APIResource {
  static {
    __name(this, "SmartTieredCache");
  }
  delete(params, options) {
    const { zone_id } = params;
    return this._client.delete(`/zones/${zone_id}/cache/tiered_cache_smart_topology_enable`, options)._thenUnwrap((obj) => obj.result);
  }
  edit(params, options) {
    const { zone_id, ...body } = params;
    return this._client.patch(`/zones/${zone_id}/cache/tiered_cache_smart_topology_enable`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  get(params, options) {
    const { zone_id } = params;
    return this._client.get(`/zones/${zone_id}/cache/tiered_cache_smart_topology_enable`, options)._thenUnwrap((obj) => obj.result);
  }
};
var Variants = class extends APIResource {
  static {
    __name(this, "Variants");
  }
  delete(params, options) {
    const { zone_id } = params;
    return this._client.delete(`/zones/${zone_id}/cache/variants`, options)._thenUnwrap((obj) => obj.result);
  }
  edit(params, options) {
    const { zone_id, ...body } = params;
    return this._client.patch(`/zones/${zone_id}/cache/variants`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  get(params, options) {
    const { zone_id } = params;
    return this._client.get(`/zones/${zone_id}/cache/variants`, options)._thenUnwrap((obj) => obj.result);
  }
};
var Cache2 = class extends APIResource {
  static {
    __name(this, "Cache");
  }
  constructor() {
    super(...arguments);
    this.cacheReserve = new CacheReserveResource(this._client);
    this.smartTieredCache = new SmartTieredCache(this._client);
    this.variants = new Variants(this._client);
    this.regionalTieredCache = new RegionalTieredCacheResource(this._client);
  }
  purge(params, options) {
    const { zone_id, ...body } = params;
    return this._client.post(`/zones/${zone_id}/purge_cache`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
};
Cache2.CacheReserveResource = CacheReserveResource;
Cache2.SmartTieredCache = SmartTieredCache;
Cache2.Variants = Variants;
Cache2.RegionalTieredCacheResource = RegionalTieredCacheResource;
var SFU = class extends APIResource {
  static {
    __name(this, "SFU");
  }
  create(params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/calls/apps`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  update(appId, params, options) {
    const { account_id, ...body } = params;
    return this._client.put(`/accounts/${account_id}/calls/apps/${appId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { account_id } = params;
    return this._client.getAPIList(`/accounts/${account_id}/calls/apps`, SFUListResponsesSinglePage, options);
  }
  delete(appId, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/calls/apps/${appId}`, options)._thenUnwrap((obj) => obj.result);
  }
  get(appId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/calls/apps/${appId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var SFUListResponsesSinglePage = class extends SinglePage {
  static {
    __name(this, "SFUListResponsesSinglePage");
  }
};
SFU.SFUListResponsesSinglePage = SFUListResponsesSinglePage;
var TURN = class extends APIResource {
  static {
    __name(this, "TURN");
  }
  create(params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/calls/turn_keys`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  update(keyId, params, options) {
    const { account_id, ...body } = params;
    return this._client.put(`/accounts/${account_id}/calls/turn_keys/${keyId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { account_id } = params;
    return this._client.getAPIList(`/accounts/${account_id}/calls/turn_keys`, TURNListResponsesSinglePage, options);
  }
  delete(keyId, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/calls/turn_keys/${keyId}`, options)._thenUnwrap((obj) => obj.result);
  }
  get(keyId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/calls/turn_keys/${keyId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var TURNListResponsesSinglePage = class extends SinglePage {
  static {
    __name(this, "TURNListResponsesSinglePage");
  }
};
TURN.TURNListResponsesSinglePage = TURNListResponsesSinglePage;
var Calls = class extends APIResource {
  static {
    __name(this, "Calls");
  }
  constructor() {
    super(...arguments);
    this.sfu = new SFU(this._client);
    this.turn = new TURN(this._client);
  }
};
Calls.SFU = SFU;
Calls.SFUListResponsesSinglePage = SFUListResponsesSinglePage;
Calls.TURN = TURN;
Calls.TURNListResponsesSinglePage = TURNListResponsesSinglePage;
var HostnameAssociations = class extends APIResource {
  static {
    __name(this, "HostnameAssociations");
  }
  update(params, options) {
    const { zone_id, ...body } = params;
    return this._client.put(`/zones/${zone_id}/certificate_authorities/hostname_associations`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  get(params, options) {
    const { zone_id, ...query } = params;
    return this._client.get(`/zones/${zone_id}/certificate_authorities/hostname_associations`, {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
};
var CertificateAuthorities = class extends APIResource {
  static {
    __name(this, "CertificateAuthorities");
  }
  constructor() {
    super(...arguments);
    this.hostnameAssociations = new HostnameAssociations(this._client);
  }
};
CertificateAuthorities.HostnameAssociations = HostnameAssociations;
var ClientCertificates = class extends APIResource {
  static {
    __name(this, "ClientCertificates");
  }
  create(params, options) {
    const { zone_id, ...body } = params;
    return this._client.post(`/zones/${zone_id}/client_certificates`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { zone_id, ...query } = params;
    return this._client.getAPIList(`/zones/${zone_id}/client_certificates`, ClientCertificatesV4PagePaginationArray, { query, ...options });
  }
  delete(clientCertificateId, params, options) {
    const { zone_id } = params;
    return this._client.delete(`/zones/${zone_id}/client_certificates/${clientCertificateId}`, options)._thenUnwrap((obj) => obj.result);
  }
  edit(clientCertificateId, params, options) {
    const { zone_id } = params;
    return this._client.patch(`/zones/${zone_id}/client_certificates/${clientCertificateId}`, options)._thenUnwrap((obj) => obj.result);
  }
  get(clientCertificateId, params, options) {
    const { zone_id } = params;
    return this._client.get(`/zones/${zone_id}/client_certificates/${clientCertificateId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var ClientCertificatesV4PagePaginationArray = class extends V4PagePaginationArray {
  static {
    __name(this, "ClientCertificatesV4PagePaginationArray");
  }
};
ClientCertificates.ClientCertificatesV4PagePaginationArray = ClientCertificatesV4PagePaginationArray;
var Rules = class extends APIResource {
  static {
    __name(this, "Rules");
  }
  update(params, options) {
    const { zone_id, rules } = params ?? {};
    return this._client.getAPIList(`/zones/${zone_id}/cloud_connector/rules`, RuleUpdateResponsesSinglePage, {
      body: rules,
      method: "put",
      ...options
    });
  }
  list(params, options) {
    const { zone_id } = params;
    return this._client.getAPIList(`/zones/${zone_id}/cloud_connector/rules`, RuleListResponsesSinglePage, options);
  }
};
var RuleUpdateResponsesSinglePage = class extends SinglePage {
  static {
    __name(this, "RuleUpdateResponsesSinglePage");
  }
};
var RuleListResponsesSinglePage = class extends SinglePage {
  static {
    __name(this, "RuleListResponsesSinglePage");
  }
};
Rules.RuleUpdateResponsesSinglePage = RuleUpdateResponsesSinglePage;
Rules.RuleListResponsesSinglePage = RuleListResponsesSinglePage;
var CloudConnector = class extends APIResource {
  static {
    __name(this, "CloudConnector");
  }
  constructor() {
    super(...arguments);
    this.rules = new Rules(this._client);
  }
};
CloudConnector.Rules = Rules;
CloudConnector.RuleUpdateResponsesSinglePage = RuleUpdateResponsesSinglePage;
CloudConnector.RuleListResponsesSinglePage = RuleListResponsesSinglePage;
var BinaryStorage = class extends APIResource {
  static {
    __name(this, "BinaryStorage");
  }
  create(params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/cloudforce-one/binary`, multipartFormRequestOptions({ body, ...options }));
  }
  get(hash, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/cloudforce-one/binary/${hash}`, {
      ...options,
      headers: { Accept: "*/*", ...options?.headers }
    });
  }
};
var Assets2 = class extends APIResource {
  static {
    __name(this, "Assets2");
  }
  create(requestId, params, options) {
    const { account_id, ...body } = params;
    return this._client.getAPIList(`/accounts/${account_id}/cloudforce-one/requests/${requestId}/asset`, AssetCreateResponsesSinglePage, { body, method: "post", ...options });
  }
  update(requestId, assetId, params, options) {
    const { account_id, ...body } = params;
    return this._client.put(`/accounts/${account_id}/cloudforce-one/requests/${requestId}/asset/${assetId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  delete(requestId, assetId, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/cloudforce-one/requests/${requestId}/asset/${assetId}`, options);
  }
  get(requestId, assetId, params, options) {
    const { account_id } = params;
    return this._client.getAPIList(`/accounts/${account_id}/cloudforce-one/requests/${requestId}/asset/${assetId}`, AssetGetResponsesSinglePage, options);
  }
};
var AssetCreateResponsesSinglePage = class extends SinglePage {
  static {
    __name(this, "AssetCreateResponsesSinglePage");
  }
};
var AssetGetResponsesSinglePage = class extends SinglePage {
  static {
    __name(this, "AssetGetResponsesSinglePage");
  }
};
Assets2.AssetCreateResponsesSinglePage = AssetCreateResponsesSinglePage;
Assets2.AssetGetResponsesSinglePage = AssetGetResponsesSinglePage;
var MessageResource = class extends APIResource {
  static {
    __name(this, "MessageResource");
  }
  create(requestId, params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/cloudforce-one/requests/${requestId}/message/new`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  update(requestId, messageId, params, options) {
    const { account_id, ...body } = params;
    return this._client.put(`/accounts/${account_id}/cloudforce-one/requests/${requestId}/message/${messageId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  delete(requestId, messageId, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/cloudforce-one/requests/${requestId}/message/${messageId}`, options);
  }
  get(requestId, params, options) {
    const { account_id, ...body } = params;
    return this._client.getAPIList(`/accounts/${account_id}/cloudforce-one/requests/${requestId}/message`, MessagesSinglePage, { body, method: "post", ...options });
  }
};
var MessagesSinglePage = class extends SinglePage {
  static {
    __name(this, "MessagesSinglePage");
  }
};
MessageResource.MessagesSinglePage = MessagesSinglePage;
var PriorityResource = class extends APIResource {
  static {
    __name(this, "PriorityResource");
  }
  create(params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/cloudforce-one/requests/priority/new`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  update(priorityId, params, options) {
    const { account_id, ...body } = params;
    return this._client.put(`/accounts/${account_id}/cloudforce-one/requests/priority/${priorityId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  delete(priorityId, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/cloudforce-one/requests/priority/${priorityId}`, options);
  }
  get(priorityId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/cloudforce-one/requests/priority/${priorityId}`, options)._thenUnwrap((obj) => obj.result);
  }
  quota(params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/cloudforce-one/requests/priority/quota`, options)._thenUnwrap((obj) => obj.result);
  }
};
var Requests = class extends APIResource {
  static {
    __name(this, "Requests");
  }
  constructor() {
    super(...arguments);
    this.message = new MessageResource(this._client);
    this.priority = new PriorityResource(this._client);
    this.assets = new Assets2(this._client);
  }
  create(params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/cloudforce-one/requests/new`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  update(requestId, params, options) {
    const { account_id, ...body } = params;
    return this._client.put(`/accounts/${account_id}/cloudforce-one/requests/${requestId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { account_id, ...body } = params;
    return this._client.getAPIList(`/accounts/${account_id}/cloudforce-one/requests`, ListItemsSinglePage, {
      body,
      method: "post",
      ...options
    });
  }
  delete(requestId, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/cloudforce-one/requests/${requestId}`, options);
  }
  constants(params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/cloudforce-one/requests/constants`, options)._thenUnwrap((obj) => obj.result);
  }
  get(requestId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/cloudforce-one/requests/${requestId}`, options)._thenUnwrap((obj) => obj.result);
  }
  quota(params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/cloudforce-one/requests/quota`, options)._thenUnwrap((obj) => obj.result);
  }
  types(params, options) {
    const { account_id } = params;
    return this._client.getAPIList(`/accounts/${account_id}/cloudforce-one/requests/types`, RequestTypesResponsesSinglePage, options);
  }
};
var ListItemsSinglePage = class extends SinglePage {
  static {
    __name(this, "ListItemsSinglePage");
  }
};
var RequestTypesResponsesSinglePage = class extends SinglePage {
  static {
    __name(this, "RequestTypesResponsesSinglePage");
  }
};
Requests.ListItemsSinglePage = ListItemsSinglePage;
Requests.RequestTypesResponsesSinglePage = RequestTypesResponsesSinglePage;
Requests.MessageResource = MessageResource;
Requests.MessagesSinglePage = MessagesSinglePage;
Requests.PriorityResource = PriorityResource;
Requests.Assets = Assets2;
Requests.AssetCreateResponsesSinglePage = AssetCreateResponsesSinglePage;
Requests.AssetGetResponsesSinglePage = AssetGetResponsesSinglePage;
var Config = class extends APIResource {
  static {
    __name(this, "Config");
  }
  create(params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/cloudforce-one/scans/config`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { account_id } = params;
    return this._client.getAPIList(`/accounts/${account_id}/cloudforce-one/scans/config`, ConfigListResponsesSinglePage, options);
  }
  delete(configId, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/cloudforce-one/scans/config/${configId}`, options)._thenUnwrap((obj) => obj.result);
  }
  edit(configId, params, options) {
    const { account_id, ...body } = params;
    return this._client.patch(`/accounts/${account_id}/cloudforce-one/scans/config/${configId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
};
var ConfigListResponsesSinglePage = class extends SinglePage {
  static {
    __name(this, "ConfigListResponsesSinglePage");
  }
};
Config.ConfigListResponsesSinglePage = ConfigListResponsesSinglePage;
var Results = class extends APIResource {
  static {
    __name(this, "Results");
  }
  get(configId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/cloudforce-one/scans/results/${configId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var Scans = class extends APIResource {
  static {
    __name(this, "Scans");
  }
  constructor() {
    super(...arguments);
    this.results = new Results(this._client);
    this.config = new Config(this._client);
  }
};
Scans.Results = Results;
Scans.Config = Config;
Scans.ConfigListResponsesSinglePage = ConfigListResponsesSinglePage;
var Attackers = class extends APIResource {
  static {
    __name(this, "Attackers");
  }
  list(params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/cloudforce-one/events/attackers`, options);
  }
};
var Categories = class extends APIResource {
  static {
    __name(this, "Categories");
  }
  create(params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/cloudforce-one/events/categories/create`, {
      body,
      ...options
    });
  }
  list(params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/cloudforce-one/events/categories`, options);
  }
  delete(categoryId, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/cloudforce-one/events/categories/${categoryId}`, options);
  }
  edit(categoryId, params, options) {
    const { account_id, ...body } = params;
    return this._client.patch(`/accounts/${account_id}/cloudforce-one/events/categories/${categoryId}`, {
      body,
      ...options
    });
  }
  get(categoryId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/cloudforce-one/events/categories/${categoryId}`, options);
  }
};
var Countries = class extends APIResource {
  static {
    __name(this, "Countries");
  }
  list(params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/cloudforce-one/events/countries`, options);
  }
};
var Crons = class extends APIResource {
  static {
    __name(this, "Crons");
  }
};
var EventTags = class extends APIResource {
  static {
    __name(this, "EventTags");
  }
  create(eventId, params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/cloudforce-one/events/event_tag/${eventId}/create`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  delete(eventId, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/cloudforce-one/events/event_tag/${eventId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var IndicatorTypes = class extends APIResource {
  static {
    __name(this, "IndicatorTypes");
  }
  list(params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/cloudforce-one/events/indicatorTypes`, options);
  }
};
var Insights = class extends APIResource {
  static {
    __name(this, "Insights");
  }
};
var Raw = class extends APIResource {
  static {
    __name(this, "Raw");
  }
  edit(eventId, rawId, params, options) {
    const { account_id, ...body } = params;
    return this._client.patch(`/accounts/${account_id}/cloudforce-one/events/${eventId}/raw/${rawId}`, {
      body,
      ...options
    });
  }
  get(eventId, rawId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/cloudforce-one/events/${eventId}/raw/${rawId}`, options);
  }
};
var Relate = class extends APIResource {
  static {
    __name(this, "Relate");
  }
  delete(eventId, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/cloudforce-one/events/relate/${eventId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var Tags = class extends APIResource {
  static {
    __name(this, "Tags");
  }
  create(params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/cloudforce-one/events/tags/create`, {
      body,
      ...options
    });
  }
};
var TargetIndustries = class extends APIResource {
  static {
    __name(this, "TargetIndustries");
  }
  list(params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/cloudforce-one/events/targetIndustries`, options);
  }
};
var Health = class extends APIResource {
  static {
    __name(this, "Health");
  }
};
var Datasets2 = class extends APIResource {
  static {
    __name(this, "Datasets2");
  }
  constructor() {
    super(...arguments);
    this.health = new Health(this._client);
  }
  create(params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/cloudforce-one/events/dataset/create`, {
      body,
      ...options
    });
  }
  list(params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/cloudforce-one/events/dataset`, options);
  }
  edit(datasetId, params, options) {
    const { account_id, ...body } = params;
    return this._client.patch(`/accounts/${account_id}/cloudforce-one/events/dataset/${datasetId}`, {
      body,
      ...options
    });
  }
  get(datasetId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/cloudforce-one/events/dataset/${datasetId}`, options);
  }
  raw(datasetId, eventId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/cloudforce-one/events/raw/${datasetId}/${eventId}`, options);
  }
};
Datasets2.Health = Health;
var ThreatEvents = class extends APIResource {
  static {
    __name(this, "ThreatEvents");
  }
  constructor() {
    super(...arguments);
    this.attackers = new Attackers(this._client);
    this.categories = new Categories(this._client);
    this.countries = new Countries(this._client);
    this.crons = new Crons(this._client);
    this.datasets = new Datasets2(this._client);
    this.indicatorTypes = new IndicatorTypes(this._client);
    this.raw = new Raw(this._client);
    this.relate = new Relate(this._client);
    this.tags = new Tags(this._client);
    this.eventTags = new EventTags(this._client);
    this.targetIndustries = new TargetIndustries(this._client);
    this.insights = new Insights(this._client);
  }
  create(params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/cloudforce-one/events/create`, { body, ...options });
  }
  list(params, options) {
    const { account_id, ...query } = params;
    return this._client.get(`/accounts/${account_id}/cloudforce-one/events`, { query, ...options });
  }
  delete(eventId, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/cloudforce-one/events/${eventId}`, options);
  }
  bulkCreate(params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/cloudforce-one/events/create/bulk`, {
      body,
      ...options
    });
  }
  edit(eventId, params, options) {
    const { account_id, ...body } = params;
    return this._client.patch(`/accounts/${account_id}/cloudforce-one/events/${eventId}`, {
      body,
      ...options
    });
  }
  get(eventId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/cloudforce-one/events/${eventId}`, options);
  }
};
ThreatEvents.Attackers = Attackers;
ThreatEvents.Categories = Categories;
ThreatEvents.Countries = Countries;
ThreatEvents.Crons = Crons;
ThreatEvents.Datasets = Datasets2;
ThreatEvents.IndicatorTypes = IndicatorTypes;
ThreatEvents.Raw = Raw;
ThreatEvents.Relate = Relate;
ThreatEvents.Tags = Tags;
ThreatEvents.EventTags = EventTags;
ThreatEvents.TargetIndustries = TargetIndustries;
ThreatEvents.Insights = Insights;
var CloudforceOne = class extends APIResource {
  static {
    __name(this, "CloudforceOne");
  }
  constructor() {
    super(...arguments);
    this.scans = new Scans(this._client);
    this.binaryStorage = new BinaryStorage(this._client);
    this.requests = new Requests(this._client);
    this.threatEvents = new ThreatEvents(this._client);
  }
};
CloudforceOne.Scans = Scans;
CloudforceOne.BinaryStorage = BinaryStorage;
CloudforceOne.Requests = Requests;
CloudforceOne.ListItemsSinglePage = ListItemsSinglePage;
CloudforceOne.RequestTypesResponsesSinglePage = RequestTypesResponsesSinglePage;
CloudforceOne.ThreatEvents = ThreatEvents;
var Payloads = class extends APIResource {
  static {
    __name(this, "Payloads");
  }
  create(params, options) {
    const { zone_id, body } = params;
    return this._client.getAPIList(`/zones/${zone_id}/content-upload-scan/payloads`, PayloadCreateResponsesSinglePage, { body, method: "post", ...options });
  }
  list(params, options) {
    const { zone_id } = params;
    return this._client.getAPIList(`/zones/${zone_id}/content-upload-scan/payloads`, PayloadListResponsesSinglePage, options);
  }
  delete(expressionId, params, options) {
    const { zone_id } = params;
    return this._client.getAPIList(`/zones/${zone_id}/content-upload-scan/payloads/${expressionId}`, PayloadDeleteResponsesSinglePage, { method: "delete", ...options });
  }
};
var PayloadCreateResponsesSinglePage = class extends SinglePage {
  static {
    __name(this, "PayloadCreateResponsesSinglePage");
  }
};
var PayloadListResponsesSinglePage = class extends SinglePage {
  static {
    __name(this, "PayloadListResponsesSinglePage");
  }
};
var PayloadDeleteResponsesSinglePage = class extends SinglePage {
  static {
    __name(this, "PayloadDeleteResponsesSinglePage");
  }
};
Payloads.PayloadCreateResponsesSinglePage = PayloadCreateResponsesSinglePage;
Payloads.PayloadListResponsesSinglePage = PayloadListResponsesSinglePage;
Payloads.PayloadDeleteResponsesSinglePage = PayloadDeleteResponsesSinglePage;
var Settings2 = class extends APIResource {
  static {
    __name(this, "Settings2");
  }
  get(params, options) {
    const { zone_id } = params;
    return this._client.get(`/zones/${zone_id}/content-upload-scan/settings`, options)._thenUnwrap((obj) => obj.result);
  }
};
var ContentScanning = class extends APIResource {
  static {
    __name(this, "ContentScanning");
  }
  constructor() {
    super(...arguments);
    this.payloads = new Payloads(this._client);
    this.settings = new Settings2(this._client);
  }
  disable(params, options) {
    const { zone_id } = params;
    return this._client.post(`/zones/${zone_id}/content-upload-scan/disable`, options)._thenUnwrap((obj) => obj.result);
  }
  enable(params, options) {
    const { zone_id } = params;
    return this._client.post(`/zones/${zone_id}/content-upload-scan/enable`, options)._thenUnwrap((obj) => obj.result);
  }
};
ContentScanning.Payloads = Payloads;
ContentScanning.PayloadCreateResponsesSinglePage = PayloadCreateResponsesSinglePage;
ContentScanning.PayloadListResponsesSinglePage = PayloadListResponsesSinglePage;
ContentScanning.PayloadDeleteResponsesSinglePage = PayloadDeleteResponsesSinglePage;
ContentScanning.Settings = Settings2;
var Prioritize = class extends APIResource {
  static {
    __name(this, "Prioritize");
  }
  update(params, options) {
    const { zone_id, ...body } = params;
    return this._client.getAPIList(`/zones/${zone_id}/custom_certificates/prioritize`, CustomCertificatesSinglePage, { body, method: "put", ...options });
  }
};
var CustomCertificates = class extends APIResource {
  static {
    __name(this, "CustomCertificates");
  }
  constructor() {
    super(...arguments);
    this.prioritize = new Prioritize(this._client);
  }
  create(params, options) {
    const { zone_id, ...body } = params;
    return this._client.post(`/zones/${zone_id}/custom_certificates`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { zone_id, ...query } = params;
    return this._client.getAPIList(`/zones/${zone_id}/custom_certificates`, CustomCertificatesV4PagePaginationArray, { query, ...options });
  }
  delete(customCertificateId, params, options) {
    const { zone_id } = params;
    return this._client.delete(`/zones/${zone_id}/custom_certificates/${customCertificateId}`, options)._thenUnwrap((obj) => obj.result);
  }
  edit(customCertificateId, params, options) {
    const { zone_id, ...body } = params;
    return this._client.patch(`/zones/${zone_id}/custom_certificates/${customCertificateId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  get(customCertificateId, params, options) {
    const { zone_id } = params;
    return this._client.get(`/zones/${zone_id}/custom_certificates/${customCertificateId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var CustomCertificatesV4PagePaginationArray = class extends V4PagePaginationArray {
  static {
    __name(this, "CustomCertificatesV4PagePaginationArray");
  }
};
var CustomCertificatesSinglePage = class extends SinglePage {
  static {
    __name(this, "CustomCertificatesSinglePage");
  }
};
CustomCertificates.CustomCertificatesV4PagePaginationArray = CustomCertificatesV4PagePaginationArray;
CustomCertificates.Prioritize = Prioritize;
var FallbackOrigin = class extends APIResource {
  static {
    __name(this, "FallbackOrigin");
  }
  update(params, options) {
    const { zone_id, ...body } = params;
    return this._client.put(`/zones/${zone_id}/custom_hostnames/fallback_origin`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  delete(params, options) {
    const { zone_id } = params;
    return this._client.delete(`/zones/${zone_id}/custom_hostnames/fallback_origin`, options)._thenUnwrap((obj) => obj.result);
  }
  get(params, options) {
    const { zone_id } = params;
    return this._client.get(`/zones/${zone_id}/custom_hostnames/fallback_origin`, options)._thenUnwrap((obj) => obj.result);
  }
};
var Certificates = class extends APIResource {
  static {
    __name(this, "Certificates");
  }
  update(customHostnameId, certificatePackId, certificateId, params, options) {
    const { zone_id, ...body } = params;
    return this._client.put(`/zones/${zone_id}/custom_hostnames/${customHostnameId}/certificate_pack/${certificatePackId}/certificates/${certificateId}`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  delete(customHostnameId, certificatePackId, certificateId, params, options) {
    const { zone_id } = params;
    return this._client.delete(`/zones/${zone_id}/custom_hostnames/${customHostnameId}/certificate_pack/${certificatePackId}/certificates/${certificateId}`, options);
  }
};
var CertificatePack = class extends APIResource {
  static {
    __name(this, "CertificatePack");
  }
  constructor() {
    super(...arguments);
    this.certificates = new Certificates(this._client);
  }
};
CertificatePack.Certificates = Certificates;
var CustomHostnames = class extends APIResource {
  static {
    __name(this, "CustomHostnames");
  }
  constructor() {
    super(...arguments);
    this.fallbackOrigin = new FallbackOrigin(this._client);
    this.certificatePack = new CertificatePack(this._client);
  }
  create(params, options) {
    const { zone_id, ...body } = params;
    return this._client.post(`/zones/${zone_id}/custom_hostnames`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { zone_id, ...query } = params;
    return this._client.getAPIList(`/zones/${zone_id}/custom_hostnames`, CustomHostnameListResponsesV4PagePaginationArray, { query, ...options });
  }
  delete(customHostnameId, params, options) {
    const { zone_id } = params;
    return this._client.delete(`/zones/${zone_id}/custom_hostnames/${customHostnameId}`, options);
  }
  edit(customHostnameId, params, options) {
    const { zone_id, ...body } = params;
    return this._client.patch(`/zones/${zone_id}/custom_hostnames/${customHostnameId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  get(customHostnameId, params, options) {
    const { zone_id } = params;
    return this._client.get(`/zones/${zone_id}/custom_hostnames/${customHostnameId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var CustomHostnameListResponsesV4PagePaginationArray = class extends V4PagePaginationArray {
  static {
    __name(this, "CustomHostnameListResponsesV4PagePaginationArray");
  }
};
CustomHostnames.CustomHostnameListResponsesV4PagePaginationArray = CustomHostnameListResponsesV4PagePaginationArray;
CustomHostnames.FallbackOrigin = FallbackOrigin;
CustomHostnames.CertificatePack = CertificatePack;
var CustomNameservers = class extends APIResource {
  static {
    __name(this, "CustomNameservers");
  }
  create(params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/custom_ns`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  delete(customNSId, params, options) {
    const { account_id } = params;
    return this._client.getAPIList(`/accounts/${account_id}/custom_ns/${customNSId}`, CustomNameserverDeleteResponsesSinglePage, { method: "delete", ...options });
  }
  get(params, options) {
    const { account_id } = params;
    return this._client.getAPIList(`/accounts/${account_id}/custom_ns`, CustomNameserversSinglePage, options);
  }
};
var CustomNameserverDeleteResponsesSinglePage = class extends SinglePage {
  static {
    __name(this, "CustomNameserverDeleteResponsesSinglePage");
  }
};
var CustomNameserversSinglePage = class extends SinglePage {
  static {
    __name(this, "CustomNameserversSinglePage");
  }
};
CustomNameservers.CustomNameserverDeleteResponsesSinglePage = CustomNameserverDeleteResponsesSinglePage;
CustomNameservers.CustomNameserversSinglePage = CustomNameserversSinglePage;
var CustomPages = class extends APIResource {
  static {
    __name(this, "CustomPages");
  }
  update(identifier, params, options) {
    const { account_id, zone_id, ...body } = params;
    if (!account_id && !zone_id) {
      throw new CloudflareError("You must provide either account_id or zone_id.");
    }
    if (account_id && zone_id) {
      throw new CloudflareError("You cannot provide both account_id and zone_id.");
    }
    const { accountOrZone, accountOrZoneId } = account_id ? {
      accountOrZone: "accounts",
      accountOrZoneId: account_id
    } : {
      accountOrZone: "zones",
      accountOrZoneId: zone_id
    };
    return this._client.put(`/${accountOrZone}/${accountOrZoneId}/custom_pages/${identifier}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(params = {}, options) {
    if (isRequestOptions(params)) {
      return this.list({}, params);
    }
    const { account_id, zone_id } = params;
    if (!account_id && !zone_id) {
      throw new CloudflareError("You must provide either account_id or zone_id.");
    }
    if (account_id && zone_id) {
      throw new CloudflareError("You cannot provide both account_id and zone_id.");
    }
    const { accountOrZone, accountOrZoneId } = account_id ? {
      accountOrZone: "accounts",
      accountOrZoneId: account_id
    } : {
      accountOrZone: "zones",
      accountOrZoneId: zone_id
    };
    return this._client.getAPIList(`/${accountOrZone}/${accountOrZoneId}/custom_pages`, CustomPageListResponsesSinglePage, options);
  }
  get(identifier, params = {}, options) {
    if (isRequestOptions(params)) {
      return this.get(identifier, {}, params);
    }
    const { account_id, zone_id } = params;
    if (!account_id && !zone_id) {
      throw new CloudflareError("You must provide either account_id or zone_id.");
    }
    if (account_id && zone_id) {
      throw new CloudflareError("You cannot provide both account_id and zone_id.");
    }
    const { accountOrZone, accountOrZoneId } = account_id ? {
      accountOrZone: "accounts",
      accountOrZoneId: account_id
    } : {
      accountOrZone: "zones",
      accountOrZoneId: zone_id
    };
    return this._client.get(`/${accountOrZone}/${accountOrZoneId}/custom_pages/${identifier}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var CustomPageListResponsesSinglePage = class extends SinglePage {
  static {
    __name(this, "CustomPageListResponsesSinglePage");
  }
};
CustomPages.CustomPageListResponsesSinglePage = CustomPageListResponsesSinglePage;
var Database = class extends APIResource {
  static {
    __name(this, "Database");
  }
  create(params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/d1/database`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  update(databaseId, params, options) {
    const { account_id, ...body } = params;
    return this._client.put(`/accounts/${account_id}/d1/database/${databaseId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { account_id, ...query } = params;
    return this._client.getAPIList(`/accounts/${account_id}/d1/database`, DatabaseListResponsesV4PagePaginationArray, { query, ...options });
  }
  delete(databaseId, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/d1/database/${databaseId}`, options)._thenUnwrap((obj) => obj.result);
  }
  edit(databaseId, params, options) {
    const { account_id, ...body } = params;
    return this._client.patch(`/accounts/${account_id}/d1/database/${databaseId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  export(databaseId, params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/d1/database/${databaseId}/export`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  get(databaseId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/d1/database/${databaseId}`, options)._thenUnwrap((obj) => obj.result);
  }
  import(databaseId, params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/d1/database/${databaseId}/import`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  query(databaseId, params, options) {
    const { account_id, ...body } = params;
    return this._client.getAPIList(`/accounts/${account_id}/d1/database/${databaseId}/query`, QueryResultsSinglePage, { body, method: "post", ...options });
  }
  raw(databaseId, params, options) {
    const { account_id, ...body } = params;
    return this._client.getAPIList(`/accounts/${account_id}/d1/database/${databaseId}/raw`, DatabaseRawResponsesSinglePage, { body, method: "post", ...options });
  }
};
var DatabaseListResponsesV4PagePaginationArray = class extends V4PagePaginationArray {
  static {
    __name(this, "DatabaseListResponsesV4PagePaginationArray");
  }
};
var QueryResultsSinglePage = class extends SinglePage {
  static {
    __name(this, "QueryResultsSinglePage");
  }
};
var DatabaseRawResponsesSinglePage = class extends SinglePage {
  static {
    __name(this, "DatabaseRawResponsesSinglePage");
  }
};
Database.DatabaseListResponsesV4PagePaginationArray = DatabaseListResponsesV4PagePaginationArray;
Database.QueryResultsSinglePage = QueryResultsSinglePage;
Database.DatabaseRawResponsesSinglePage = DatabaseRawResponsesSinglePage;
var D1Resource = class extends APIResource {
  static {
    __name(this, "D1Resource");
  }
  constructor() {
    super(...arguments);
    this.database = new Database(this._client);
  }
};
D1Resource.Database = Database;
D1Resource.DatabaseListResponsesV4PagePaginationArray = DatabaseListResponsesV4PagePaginationArray;
D1Resource.QueryResultsSinglePage = QueryResultsSinglePage;
D1Resource.DatabaseRawResponsesSinglePage = DatabaseRawResponsesSinglePage;
var DCVDelegation = class extends APIResource {
  static {
    __name(this, "DCVDelegation");
  }
  get(params, options) {
    const { zone_id } = params;
    return this._client.get(`/zones/${zone_id}/dcv_delegation/uuid`, options)._thenUnwrap((obj) => obj.result);
  }
};
var DNSSECResource = class extends APIResource {
  static {
    __name(this, "DNSSECResource");
  }
  delete(params, options) {
    const { zone_id } = params;
    return this._client.delete(`/zones/${zone_id}/dnssec`, options)._thenUnwrap((obj) => obj.result);
  }
  edit(params, options) {
    const { zone_id, ...body } = params;
    return this._client.patch(`/zones/${zone_id}/dnssec`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  get(params, options) {
    const { zone_id } = params;
    return this._client.get(`/zones/${zone_id}/dnssec`, options)._thenUnwrap((obj) => obj.result);
  }
};
var Records = class extends APIResource {
  static {
    __name(this, "Records");
  }
  create(params, options) {
    const { zone_id, ...body } = params;
    return this._client.post(`/zones/${zone_id}/dns_records`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  update(dnsRecordId, params, options) {
    const { zone_id, ...body } = params;
    return this._client.put(`/zones/${zone_id}/dns_records/${dnsRecordId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { zone_id, ...query } = params;
    return this._client.getAPIList(`/zones/${zone_id}/dns_records`, RecordResponsesV4PagePaginationArray, {
      query,
      ...options
    });
  }
  delete(dnsRecordId, params, options) {
    const { zone_id } = params;
    return this._client.delete(`/zones/${zone_id}/dns_records/${dnsRecordId}`, options)._thenUnwrap((obj) => obj.result);
  }
  batch(params, options) {
    const { zone_id, ...body } = params;
    return this._client.post(`/zones/${zone_id}/dns_records/batch`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  edit(dnsRecordId, params, options) {
    const { zone_id, ...body } = params;
    return this._client.patch(`/zones/${zone_id}/dns_records/${dnsRecordId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  export(params, options) {
    const { zone_id } = params;
    return this._client.get(`/zones/${zone_id}/dns_records/export`, {
      ...options,
      headers: { Accept: "text/plain", ...options?.headers }
    });
  }
  get(dnsRecordId, params, options) {
    const { zone_id } = params;
    return this._client.get(`/zones/${zone_id}/dns_records/${dnsRecordId}`, options)._thenUnwrap((obj) => obj.result);
  }
  import(params, options) {
    const { zone_id, ...body } = params;
    return this._client.post(`/zones/${zone_id}/dns_records/import`, multipartFormRequestOptions({ body, ...options }))._thenUnwrap((obj) => obj.result);
  }
  scan(params, options) {
    const { zone_id, body } = params;
    return this._client.post(`/zones/${zone_id}/dns_records/scan`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  scanList(params, options) {
    const { zone_id } = params;
    return this._client.getAPIList(`/zones/${zone_id}/dns_records/scan/review`, RecordResponsesSinglePage, options);
  }
  scanReview(params, options) {
    const { zone_id, ...body } = params;
    return this._client.post(`/zones/${zone_id}/dns_records/scan/review`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  scanTrigger(params, options) {
    const { zone_id } = params;
    return this._client.post(`/zones/${zone_id}/dns_records/scan/trigger`, options);
  }
};
var RecordResponsesV4PagePaginationArray = class extends V4PagePaginationArray {
  static {
    __name(this, "RecordResponsesV4PagePaginationArray");
  }
};
var RecordResponsesSinglePage = class extends SinglePage {
  static {
    __name(this, "RecordResponsesSinglePage");
  }
};
Records.RecordResponsesV4PagePaginationArray = RecordResponsesV4PagePaginationArray;
Records.RecordResponsesSinglePage = RecordResponsesSinglePage;
var Bytimes = class extends APIResource {
  static {
    __name(this, "Bytimes");
  }
  get(params, options) {
    const { zone_id, ...query } = params;
    return this._client.get(`/zones/${zone_id}/dns_analytics/report/bytime`, {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
};
var Reports = class extends APIResource {
  static {
    __name(this, "Reports");
  }
  constructor() {
    super(...arguments);
    this.bytimes = new Bytimes(this._client);
  }
  get(params, options) {
    const { zone_id, ...query } = params;
    return this._client.get(`/zones/${zone_id}/dns_analytics/report`, { query, ...options })._thenUnwrap((obj) => obj.result);
  }
};
Reports.Bytimes = Bytimes;
var Analytics = class extends APIResource {
  static {
    __name(this, "Analytics");
  }
  constructor() {
    super(...arguments);
    this.reports = new Reports(this._client);
  }
};
Analytics.Reports = Reports;
var Zone = class extends APIResource {
  static {
    __name(this, "Zone");
  }
  edit(params, options) {
    const { zone_id, ...body } = params;
    return this._client.patch(`/zones/${zone_id}/dns_settings`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  get(params, options) {
    const { zone_id } = params;
    return this._client.get(`/zones/${zone_id}/dns_settings`, options)._thenUnwrap((obj) => obj.result);
  }
};
var Views = class extends APIResource {
  static {
    __name(this, "Views");
  }
  create(params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/dns_settings/views`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { account_id, ...query } = params;
    return this._client.getAPIList(`/accounts/${account_id}/dns_settings/views`, ViewListResponsesV4PagePaginationArray, { query, ...options });
  }
  delete(viewId, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/dns_settings/views/${viewId}`, options)._thenUnwrap((obj) => obj.result);
  }
  edit(viewId, params, options) {
    const { account_id, ...body } = params;
    return this._client.patch(`/accounts/${account_id}/dns_settings/views/${viewId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  get(viewId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/dns_settings/views/${viewId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var ViewListResponsesV4PagePaginationArray = class extends V4PagePaginationArray {
  static {
    __name(this, "ViewListResponsesV4PagePaginationArray");
  }
};
Views.ViewListResponsesV4PagePaginationArray = ViewListResponsesV4PagePaginationArray;
var Account = class extends APIResource {
  static {
    __name(this, "Account");
  }
  constructor() {
    super(...arguments);
    this.views = new Views(this._client);
  }
  edit(params, options) {
    const { account_id, ...body } = params;
    return this._client.patch(`/accounts/${account_id}/dns_settings`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  get(params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/dns_settings`, options)._thenUnwrap((obj) => obj.result);
  }
};
Account.Views = Views;
Account.ViewListResponsesV4PagePaginationArray = ViewListResponsesV4PagePaginationArray;
var Settings3 = class extends APIResource {
  static {
    __name(this, "Settings3");
  }
  constructor() {
    super(...arguments);
    this.zone = new Zone(this._client);
    this.account = new Account(this._client);
  }
};
Settings3.Zone = Zone;
Settings3.Account = Account;
var ACLs = class extends APIResource {
  static {
    __name(this, "ACLs");
  }
  create(params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/secondary_dns/acls`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  update(aclId, params, options) {
    const { account_id, ...body } = params;
    return this._client.put(`/accounts/${account_id}/secondary_dns/acls/${aclId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { account_id } = params;
    return this._client.getAPIList(`/accounts/${account_id}/secondary_dns/acls`, ACLsSinglePage, options);
  }
  delete(aclId, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/secondary_dns/acls/${aclId}`, options)._thenUnwrap((obj) => obj.result);
  }
  get(aclId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/secondary_dns/acls/${aclId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var ACLsSinglePage = class extends SinglePage {
  static {
    __name(this, "ACLsSinglePage");
  }
};
ACLs.ACLsSinglePage = ACLsSinglePage;
var ForceAXFRResource = class extends APIResource {
  static {
    __name(this, "ForceAXFRResource");
  }
  create(params, options) {
    const { zone_id, body } = params;
    return this._client.post(`/zones/${zone_id}/secondary_dns/force_axfr`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
};
var IncomingResource = class extends APIResource {
  static {
    __name(this, "IncomingResource");
  }
  create(params, options) {
    const { zone_id, ...body } = params;
    return this._client.post(`/zones/${zone_id}/secondary_dns/incoming`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  update(params, options) {
    const { zone_id, ...body } = params;
    return this._client.put(`/zones/${zone_id}/secondary_dns/incoming`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  delete(params, options) {
    const { zone_id } = params;
    return this._client.delete(`/zones/${zone_id}/secondary_dns/incoming`, options)._thenUnwrap((obj) => obj.result);
  }
  get(params, options) {
    const { zone_id } = params;
    return this._client.get(`/zones/${zone_id}/secondary_dns/incoming`, options)._thenUnwrap((obj) => obj.result);
  }
};
var Peers = class extends APIResource {
  static {
    __name(this, "Peers");
  }
  create(params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/secondary_dns/peers`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  update(peerId, params, options) {
    const { account_id, ...body } = params;
    return this._client.put(`/accounts/${account_id}/secondary_dns/peers/${peerId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { account_id } = params;
    return this._client.getAPIList(`/accounts/${account_id}/secondary_dns/peers`, PeersSinglePage, options);
  }
  delete(peerId, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/secondary_dns/peers/${peerId}`, options)._thenUnwrap((obj) => obj.result);
  }
  get(peerId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/secondary_dns/peers/${peerId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var PeersSinglePage = class extends SinglePage {
  static {
    __name(this, "PeersSinglePage");
  }
};
Peers.PeersSinglePage = PeersSinglePage;
var TSIGs = class extends APIResource {
  static {
    __name(this, "TSIGs");
  }
  create(params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/secondary_dns/tsigs`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  update(tsigId, params, options) {
    const { account_id, ...body } = params;
    return this._client.put(`/accounts/${account_id}/secondary_dns/tsigs/${tsigId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { account_id } = params;
    return this._client.getAPIList(`/accounts/${account_id}/secondary_dns/tsigs`, TSIGsSinglePage, options);
  }
  delete(tsigId, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/secondary_dns/tsigs/${tsigId}`, options)._thenUnwrap((obj) => obj.result);
  }
  get(tsigId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/secondary_dns/tsigs/${tsigId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var TSIGsSinglePage = class extends SinglePage {
  static {
    __name(this, "TSIGsSinglePage");
  }
};
TSIGs.TSIGsSinglePage = TSIGsSinglePage;
var Status = class extends APIResource {
  static {
    __name(this, "Status");
  }
  get(params, options) {
    const { zone_id } = params;
    return this._client.get(`/zones/${zone_id}/secondary_dns/outgoing/status`, options)._thenUnwrap((obj) => obj.result);
  }
};
var OutgoingResource = class extends APIResource {
  static {
    __name(this, "OutgoingResource");
  }
  constructor() {
    super(...arguments);
    this.status = new Status(this._client);
  }
  create(params, options) {
    const { zone_id, ...body } = params;
    return this._client.post(`/zones/${zone_id}/secondary_dns/outgoing`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  update(params, options) {
    const { zone_id, ...body } = params;
    return this._client.put(`/zones/${zone_id}/secondary_dns/outgoing`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  delete(params, options) {
    const { zone_id } = params;
    return this._client.delete(`/zones/${zone_id}/secondary_dns/outgoing`, options)._thenUnwrap((obj) => obj.result);
  }
  disable(params, options) {
    const { zone_id, body } = params;
    return this._client.post(`/zones/${zone_id}/secondary_dns/outgoing/disable`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  enable(params, options) {
    const { zone_id, body } = params;
    return this._client.post(`/zones/${zone_id}/secondary_dns/outgoing/enable`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  forceNotify(params, options) {
    const { zone_id, body } = params;
    return this._client.post(`/zones/${zone_id}/secondary_dns/outgoing/force_notify`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  get(params, options) {
    const { zone_id } = params;
    return this._client.get(`/zones/${zone_id}/secondary_dns/outgoing`, options)._thenUnwrap((obj) => obj.result);
  }
};
OutgoingResource.Status = Status;
var ZoneTransfers = class extends APIResource {
  static {
    __name(this, "ZoneTransfers");
  }
  constructor() {
    super(...arguments);
    this.forceAXFR = new ForceAXFRResource(this._client);
    this.incoming = new IncomingResource(this._client);
    this.outgoing = new OutgoingResource(this._client);
    this.acls = new ACLs(this._client);
    this.peers = new Peers(this._client);
    this.tsigs = new TSIGs(this._client);
  }
};
ZoneTransfers.ForceAXFRResource = ForceAXFRResource;
ZoneTransfers.IncomingResource = IncomingResource;
ZoneTransfers.OutgoingResource = OutgoingResource;
ZoneTransfers.ACLs = ACLs;
ZoneTransfers.ACLsSinglePage = ACLsSinglePage;
ZoneTransfers.Peers = Peers;
ZoneTransfers.PeersSinglePage = PeersSinglePage;
ZoneTransfers.TSIGs = TSIGs;
ZoneTransfers.TSIGsSinglePage = TSIGsSinglePage;
var DNS = class extends APIResource {
  static {
    __name(this, "DNS");
  }
  constructor() {
    super(...arguments);
    this.dnssec = new DNSSECResource(this._client);
    this.records = new Records(this._client);
    this.settings = new Settings3(this._client);
    this.analytics = new Analytics(this._client);
    this.zoneTransfers = new ZoneTransfers(this._client);
  }
};
DNS.DNSSECResource = DNSSECResource;
DNS.Records = Records;
DNS.RecordResponsesV4PagePaginationArray = RecordResponsesV4PagePaginationArray;
DNS.RecordResponsesSinglePage = RecordResponsesSinglePage;
DNS.Settings = Settings3;
DNS.Analytics = Analytics;
DNS.ZoneTransfers = ZoneTransfers;
var ReverseDNS = class extends APIResource {
  static {
    __name(this, "ReverseDNS");
  }
  edit(dnsFirewallId, params, options) {
    const { account_id, ...body } = params;
    return this._client.patch(`/accounts/${account_id}/dns_firewall/${dnsFirewallId}/reverse_dns`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  get(dnsFirewallId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/dns_firewall/${dnsFirewallId}/reverse_dns`, options)._thenUnwrap((obj) => obj.result);
  }
};
var Bytimes2 = class extends APIResource {
  static {
    __name(this, "Bytimes2");
  }
  get(dnsFirewallId, params, options) {
    const { account_id, ...query } = params;
    return this._client.get(`/accounts/${account_id}/dns_firewall/${dnsFirewallId}/dns_analytics/report/bytime`, {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
};
var Reports2 = class extends APIResource {
  static {
    __name(this, "Reports2");
  }
  constructor() {
    super(...arguments);
    this.bytimes = new Bytimes2(this._client);
  }
  get(dnsFirewallId, params, options) {
    const { account_id, ...query } = params;
    return this._client.get(`/accounts/${account_id}/dns_firewall/${dnsFirewallId}/dns_analytics/report`, {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
};
Reports2.Bytimes = Bytimes2;
var Analytics2 = class extends APIResource {
  static {
    __name(this, "Analytics2");
  }
  constructor() {
    super(...arguments);
    this.reports = new Reports2(this._client);
  }
};
Analytics2.Reports = Reports2;
var DNSFirewall = class extends APIResource {
  static {
    __name(this, "DNSFirewall");
  }
  constructor() {
    super(...arguments);
    this.analytics = new Analytics2(this._client);
    this.reverseDNS = new ReverseDNS(this._client);
  }
  create(params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/dns_firewall`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { account_id, ...query } = params;
    return this._client.getAPIList(`/accounts/${account_id}/dns_firewall`, DNSFirewallListResponsesV4PagePaginationArray, { query, ...options });
  }
  delete(dnsFirewallId, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/dns_firewall/${dnsFirewallId}`, options)._thenUnwrap((obj) => obj.result);
  }
  edit(dnsFirewallId, params, options) {
    const { account_id, ...body } = params;
    return this._client.patch(`/accounts/${account_id}/dns_firewall/${dnsFirewallId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  get(dnsFirewallId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/dns_firewall/${dnsFirewallId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var DNSFirewallListResponsesV4PagePaginationArray = class extends V4PagePaginationArray {
  static {
    __name(this, "DNSFirewallListResponsesV4PagePaginationArray");
  }
};
DNSFirewall.DNSFirewallListResponsesV4PagePaginationArray = DNSFirewallListResponsesV4PagePaginationArray;
DNSFirewall.Analytics = Analytics2;
DNSFirewall.ReverseDNS = ReverseDNS;
var EndpointHealthchecks = class extends APIResource {
  static {
    __name(this, "EndpointHealthchecks");
  }
  create(params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/diagnostics/endpoint-healthchecks`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  update(id, params, options) {
    const { account_id, ...body } = params;
    return this._client.put(`/accounts/${account_id}/diagnostics/endpoint-healthchecks/${id}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/diagnostics/endpoint-healthchecks`, options)._thenUnwrap((obj) => obj.result);
  }
  delete(id, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/diagnostics/endpoint-healthchecks/${id}`, options);
  }
  get(id, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/diagnostics/endpoint-healthchecks/${id}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var Traceroutes = class extends APIResource {
  static {
    __name(this, "Traceroutes");
  }
  create(params, options) {
    const { account_id, ...body } = params;
    return this._client.getAPIList(`/accounts/${account_id}/diagnostics/traceroute`, TraceroutesSinglePage, {
      body,
      method: "post",
      ...options
    });
  }
};
var TraceroutesSinglePage = class extends SinglePage {
  static {
    __name(this, "TraceroutesSinglePage");
  }
};
Traceroutes.TraceroutesSinglePage = TraceroutesSinglePage;
var Diagnostics = class extends APIResource {
  static {
    __name(this, "Diagnostics");
  }
  constructor() {
    super(...arguments);
    this.traceroutes = new Traceroutes(this._client);
    this.endpointHealthchecks = new EndpointHealthchecks(this._client);
  }
};
Diagnostics.Traceroutes = Traceroutes;
Diagnostics.TraceroutesSinglePage = TraceroutesSinglePage;
Diagnostics.EndpointHealthchecks = EndpointHealthchecks;
var Objects = class extends APIResource {
  static {
    __name(this, "Objects");
  }
  list(id, params, options) {
    const { account_id, ...query } = params;
    return this._client.getAPIList(`/accounts/${account_id}/workers/durable_objects/namespaces/${id}/objects`, DurableObjectsCursorPaginationAfter, { query, ...options });
  }
};
var DurableObjectsCursorPaginationAfter = class extends CursorPaginationAfter {
  static {
    __name(this, "DurableObjectsCursorPaginationAfter");
  }
};
Objects.DurableObjectsCursorPaginationAfter = DurableObjectsCursorPaginationAfter;
var Namespaces = class extends APIResource {
  static {
    __name(this, "Namespaces");
  }
  constructor() {
    super(...arguments);
    this.objects = new Objects(this._client);
  }
  list(params, options) {
    const { account_id, ...query } = params;
    return this._client.getAPIList(`/accounts/${account_id}/workers/durable_objects/namespaces`, NamespacesV4PagePaginationArray, { query, ...options });
  }
};
var NamespacesV4PagePaginationArray = class extends V4PagePaginationArray {
  static {
    __name(this, "NamespacesV4PagePaginationArray");
  }
};
Namespaces.NamespacesV4PagePaginationArray = NamespacesV4PagePaginationArray;
Namespaces.Objects = Objects;
Namespaces.DurableObjectsCursorPaginationAfter = DurableObjectsCursorPaginationAfter;
var DurableObjects = class extends APIResource {
  static {
    __name(this, "DurableObjects");
  }
  constructor() {
    super(...arguments);
    this.namespaces = new Namespaces(this._client);
  }
};
DurableObjects.Namespaces = Namespaces;
DurableObjects.NamespacesV4PagePaginationArray = NamespacesV4PagePaginationArray;
var Addresses = class extends APIResource {
  static {
    __name(this, "Addresses");
  }
  create(params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/email/routing/addresses`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { account_id, ...query } = params;
    return this._client.getAPIList(`/accounts/${account_id}/email/routing/addresses`, AddressesV4PagePaginationArray, { query, ...options });
  }
  delete(destinationAddressIdentifier, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/email/routing/addresses/${destinationAddressIdentifier}`, options)._thenUnwrap((obj) => obj.result);
  }
  get(destinationAddressIdentifier, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/email/routing/addresses/${destinationAddressIdentifier}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var AddressesV4PagePaginationArray = class extends V4PagePaginationArray {
  static {
    __name(this, "AddressesV4PagePaginationArray");
  }
};
Addresses.AddressesV4PagePaginationArray = AddressesV4PagePaginationArray;
var DNS2 = class extends APIResource {
  static {
    __name(this, "DNS2");
  }
  create(params, options) {
    const { zone_id, ...body } = params;
    return this._client.post(`/zones/${zone_id}/email/routing/dns`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  delete(params, options) {
    const { zone_id } = params;
    return this._client.getAPIList(`/zones/${zone_id}/email/routing/dns`, DNSRecordsSinglePage, {
      method: "delete",
      ...options
    });
  }
  edit(params, options) {
    const { zone_id, ...body } = params;
    return this._client.patch(`/zones/${zone_id}/email/routing/dns`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  get(params, options) {
    const { zone_id, ...query } = params;
    return this._client.get(`/zones/${zone_id}/email/routing/dns`, { query, ...options });
  }
};
var DNSRecordsSinglePage = class extends SinglePage {
  static {
    __name(this, "DNSRecordsSinglePage");
  }
};
DNS2.DNSRecordsSinglePage = DNSRecordsSinglePage;
var CatchAlls = class extends APIResource {
  static {
    __name(this, "CatchAlls");
  }
  update(params, options) {
    const { zone_id, ...body } = params;
    return this._client.put(`/zones/${zone_id}/email/routing/rules/catch_all`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  get(params, options) {
    const { zone_id } = params;
    return this._client.get(`/zones/${zone_id}/email/routing/rules/catch_all`, options)._thenUnwrap((obj) => obj.result);
  }
};
var Rules2 = class extends APIResource {
  static {
    __name(this, "Rules2");
  }
  constructor() {
    super(...arguments);
    this.catchAlls = new CatchAlls(this._client);
  }
  create(params, options) {
    const { zone_id, ...body } = params;
    return this._client.post(`/zones/${zone_id}/email/routing/rules`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  update(ruleIdentifier, params, options) {
    const { zone_id, ...body } = params;
    return this._client.put(`/zones/${zone_id}/email/routing/rules/${ruleIdentifier}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { zone_id, ...query } = params;
    return this._client.getAPIList(`/zones/${zone_id}/email/routing/rules`, EmailRoutingRulesV4PagePaginationArray, { query, ...options });
  }
  delete(ruleIdentifier, params, options) {
    const { zone_id } = params;
    return this._client.delete(`/zones/${zone_id}/email/routing/rules/${ruleIdentifier}`, options)._thenUnwrap((obj) => obj.result);
  }
  get(ruleIdentifier, params, options) {
    const { zone_id } = params;
    return this._client.get(`/zones/${zone_id}/email/routing/rules/${ruleIdentifier}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var EmailRoutingRulesV4PagePaginationArray = class extends V4PagePaginationArray {
  static {
    __name(this, "EmailRoutingRulesV4PagePaginationArray");
  }
};
Rules2.EmailRoutingRulesV4PagePaginationArray = EmailRoutingRulesV4PagePaginationArray;
Rules2.CatchAlls = CatchAlls;
var EmailRouting = class extends APIResource {
  static {
    __name(this, "EmailRouting");
  }
  constructor() {
    super(...arguments);
    this.dns = new DNS2(this._client);
    this.rules = new Rules2(this._client);
    this.addresses = new Addresses(this._client);
  }
  disable(params, options) {
    const { zone_id, body } = params;
    return this._client.post(`/zones/${zone_id}/email/routing/disable`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  enable(params, options) {
    const { zone_id, body } = params;
    return this._client.post(`/zones/${zone_id}/email/routing/enable`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  get(params, options) {
    const { zone_id } = params;
    return this._client.get(`/zones/${zone_id}/email/routing`, options)._thenUnwrap((obj) => obj.result);
  }
};
EmailRouting.DNS = DNS2;
EmailRouting.DNSRecordsSinglePage = DNSRecordsSinglePage;
EmailRouting.Rules = Rules2;
EmailRouting.EmailRoutingRulesV4PagePaginationArray = EmailRoutingRulesV4PagePaginationArray;
EmailRouting.Addresses = Addresses;
EmailRouting.AddressesV4PagePaginationArray = AddressesV4PagePaginationArray;
var Submissions = class extends APIResource {
  static {
    __name(this, "Submissions");
  }
  list(params, options) {
    const { account_id, ...query } = params;
    return this._client.getAPIList(`/accounts/${account_id}/email-security/submissions`, SubmissionListResponsesV4PagePaginationArray, { query, ...options });
  }
};
var SubmissionListResponsesV4PagePaginationArray = class extends V4PagePaginationArray {
  static {
    __name(this, "SubmissionListResponsesV4PagePaginationArray");
  }
};
Submissions.SubmissionListResponsesV4PagePaginationArray = SubmissionListResponsesV4PagePaginationArray;
var Detections = class extends APIResource {
  static {
    __name(this, "Detections");
  }
  get(postfixId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/email-security/investigate/${postfixId}/detections`, options)._thenUnwrap((obj) => obj.result);
  }
};
var Move = class extends APIResource {
  static {
    __name(this, "Move");
  }
  create(postfixId, params, options) {
    const { account_id, ...body } = params;
    return this._client.getAPIList(`/accounts/${account_id}/email-security/investigate/${postfixId}/move`, MoveCreateResponsesSinglePage, { body, method: "post", ...options });
  }
  bulk(params, options) {
    const { account_id, ...body } = params;
    return this._client.getAPIList(`/accounts/${account_id}/email-security/investigate/move`, MoveBulkResponsesSinglePage, { body, method: "post", ...options });
  }
};
var MoveCreateResponsesSinglePage = class extends SinglePage {
  static {
    __name(this, "MoveCreateResponsesSinglePage");
  }
};
var MoveBulkResponsesSinglePage = class extends SinglePage {
  static {
    __name(this, "MoveBulkResponsesSinglePage");
  }
};
Move.MoveCreateResponsesSinglePage = MoveCreateResponsesSinglePage;
Move.MoveBulkResponsesSinglePage = MoveBulkResponsesSinglePage;
var Preview = class extends APIResource {
  static {
    __name(this, "Preview");
  }
  create(params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/email-security/investigate/preview`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  get(postfixId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/email-security/investigate/${postfixId}/preview`, options)._thenUnwrap((obj) => obj.result);
  }
};
var Raw2 = class extends APIResource {
  static {
    __name(this, "Raw2");
  }
  get(postfixId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/email-security/investigate/${postfixId}/raw`, options)._thenUnwrap((obj) => obj.result);
  }
};
var Reclassify = class extends APIResource {
  static {
    __name(this, "Reclassify");
  }
  create(postfixId, params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/email-security/investigate/${postfixId}/reclassify`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
};
var Release = class extends APIResource {
  static {
    __name(this, "Release");
  }
  bulk(params, options) {
    const { account_id, body } = params;
    return this._client.getAPIList(`/accounts/${account_id}/email-security/investigate/release`, ReleaseBulkResponsesSinglePage, { body, method: "post", ...options });
  }
};
var ReleaseBulkResponsesSinglePage = class extends SinglePage {
  static {
    __name(this, "ReleaseBulkResponsesSinglePage");
  }
};
Release.ReleaseBulkResponsesSinglePage = ReleaseBulkResponsesSinglePage;
var Trace = class extends APIResource {
  static {
    __name(this, "Trace");
  }
  get(postfixId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/email-security/investigate/${postfixId}/trace`, options)._thenUnwrap((obj) => obj.result);
  }
};
var Investigate = class extends APIResource {
  static {
    __name(this, "Investigate");
  }
  constructor() {
    super(...arguments);
    this.detections = new Detections(this._client);
    this.preview = new Preview(this._client);
    this.raw = new Raw2(this._client);
    this.trace = new Trace(this._client);
    this.move = new Move(this._client);
    this.reclassify = new Reclassify(this._client);
    this.release = new Release(this._client);
  }
  list(params, options) {
    const { account_id, ...query } = params;
    return this._client.getAPIList(`/accounts/${account_id}/email-security/investigate`, InvestigateListResponsesV4PagePaginationArray, { query, ...options });
  }
  get(postfixId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/email-security/investigate/${postfixId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var InvestigateListResponsesV4PagePaginationArray = class extends V4PagePaginationArray {
  static {
    __name(this, "InvestigateListResponsesV4PagePaginationArray");
  }
};
Investigate.InvestigateListResponsesV4PagePaginationArray = InvestigateListResponsesV4PagePaginationArray;
Investigate.Detections = Detections;
Investigate.Preview = Preview;
Investigate.Raw = Raw2;
Investigate.Trace = Trace;
Investigate.Move = Move;
Investigate.MoveCreateResponsesSinglePage = MoveCreateResponsesSinglePage;
Investigate.MoveBulkResponsesSinglePage = MoveBulkResponsesSinglePage;
Investigate.Reclassify = Reclassify;
Investigate.Release = Release;
Investigate.ReleaseBulkResponsesSinglePage = ReleaseBulkResponsesSinglePage;
var AllowPolicies = class extends APIResource {
  static {
    __name(this, "AllowPolicies");
  }
  create(params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/email-security/settings/allow_policies`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { account_id, ...query } = params;
    return this._client.getAPIList(`/accounts/${account_id}/email-security/settings/allow_policies`, AllowPolicyListResponsesV4PagePaginationArray, { query, ...options });
  }
  delete(policyId, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/email-security/settings/allow_policies/${policyId}`, options)._thenUnwrap((obj) => obj.result);
  }
  edit(policyId, params, options) {
    const { account_id, ...body } = params;
    return this._client.patch(`/accounts/${account_id}/email-security/settings/allow_policies/${policyId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  get(policyId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/email-security/settings/allow_policies/${policyId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var AllowPolicyListResponsesV4PagePaginationArray = class extends V4PagePaginationArray {
  static {
    __name(this, "AllowPolicyListResponsesV4PagePaginationArray");
  }
};
AllowPolicies.AllowPolicyListResponsesV4PagePaginationArray = AllowPolicyListResponsesV4PagePaginationArray;
var BlockSenders = class extends APIResource {
  static {
    __name(this, "BlockSenders");
  }
  create(params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/email-security/settings/block_senders`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { account_id, ...query } = params;
    return this._client.getAPIList(`/accounts/${account_id}/email-security/settings/block_senders`, BlockSenderListResponsesV4PagePaginationArray, { query, ...options });
  }
  delete(patternId, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/email-security/settings/block_senders/${patternId}`, options)._thenUnwrap((obj) => obj.result);
  }
  edit(patternId, params, options) {
    const { account_id, ...body } = params;
    return this._client.patch(`/accounts/${account_id}/email-security/settings/block_senders/${patternId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  get(patternId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/email-security/settings/block_senders/${patternId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var BlockSenderListResponsesV4PagePaginationArray = class extends V4PagePaginationArray {
  static {
    __name(this, "BlockSenderListResponsesV4PagePaginationArray");
  }
};
BlockSenders.BlockSenderListResponsesV4PagePaginationArray = BlockSenderListResponsesV4PagePaginationArray;
var Domains = class extends APIResource {
  static {
    __name(this, "Domains");
  }
  list(params, options) {
    const { account_id, ...query } = params;
    return this._client.getAPIList(`/accounts/${account_id}/email-security/settings/domains`, DomainListResponsesV4PagePaginationArray, { query, ...options });
  }
  delete(domainId, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/email-security/settings/domains/${domainId}`, options)._thenUnwrap((obj) => obj.result);
  }
  bulkDelete(params, options) {
    const { account_id } = params;
    return this._client.getAPIList(`/accounts/${account_id}/email-security/settings/domains`, DomainBulkDeleteResponsesSinglePage, { method: "delete", ...options });
  }
  edit(domainId, params, options) {
    const { account_id, ...body } = params;
    return this._client.patch(`/accounts/${account_id}/email-security/settings/domains/${domainId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  get(domainId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/email-security/settings/domains/${domainId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var DomainListResponsesV4PagePaginationArray = class extends V4PagePaginationArray {
  static {
    __name(this, "DomainListResponsesV4PagePaginationArray");
  }
};
var DomainBulkDeleteResponsesSinglePage = class extends SinglePage {
  static {
    __name(this, "DomainBulkDeleteResponsesSinglePage");
  }
};
Domains.DomainListResponsesV4PagePaginationArray = DomainListResponsesV4PagePaginationArray;
Domains.DomainBulkDeleteResponsesSinglePage = DomainBulkDeleteResponsesSinglePage;
var ImpersonationRegistry = class extends APIResource {
  static {
    __name(this, "ImpersonationRegistry");
  }
  create(params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/email-security/settings/impersonation_registry`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { account_id, ...query } = params;
    return this._client.getAPIList(`/accounts/${account_id}/email-security/settings/impersonation_registry`, ImpersonationRegistryListResponsesV4PagePaginationArray, { query, ...options });
  }
  delete(displayNameId, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/email-security/settings/impersonation_registry/${displayNameId}`, options)._thenUnwrap((obj) => obj.result);
  }
  edit(displayNameId, params, options) {
    const { account_id, ...body } = params;
    return this._client.patch(`/accounts/${account_id}/email-security/settings/impersonation_registry/${displayNameId}`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  get(displayNameId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/email-security/settings/impersonation_registry/${displayNameId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var ImpersonationRegistryListResponsesV4PagePaginationArray = class extends V4PagePaginationArray {
  static {
    __name(this, "ImpersonationRegistryListResponsesV4PagePaginationArray");
  }
};
ImpersonationRegistry.ImpersonationRegistryListResponsesV4PagePaginationArray = ImpersonationRegistryListResponsesV4PagePaginationArray;
var TrustedDomains = class extends APIResource {
  static {
    __name(this, "TrustedDomains");
  }
  create(params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/email-security/settings/trusted_domains`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { account_id, ...query } = params;
    return this._client.getAPIList(`/accounts/${account_id}/email-security/settings/trusted_domains`, TrustedDomainListResponsesV4PagePaginationArray, { query, ...options });
  }
  delete(trustedDomainId, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/email-security/settings/trusted_domains/${trustedDomainId}`, options)._thenUnwrap((obj) => obj.result);
  }
  edit(trustedDomainId, params, options) {
    const { account_id, ...body } = params;
    return this._client.patch(`/accounts/${account_id}/email-security/settings/trusted_domains/${trustedDomainId}`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  get(trustedDomainId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/email-security/settings/trusted_domains/${trustedDomainId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var TrustedDomainListResponsesV4PagePaginationArray = class extends V4PagePaginationArray {
  static {
    __name(this, "TrustedDomainListResponsesV4PagePaginationArray");
  }
};
TrustedDomains.TrustedDomainListResponsesV4PagePaginationArray = TrustedDomainListResponsesV4PagePaginationArray;
var Settings4 = class extends APIResource {
  static {
    __name(this, "Settings4");
  }
  constructor() {
    super(...arguments);
    this.allowPolicies = new AllowPolicies(this._client);
    this.blockSenders = new BlockSenders(this._client);
    this.domains = new Domains(this._client);
    this.impersonationRegistry = new ImpersonationRegistry(this._client);
    this.trustedDomains = new TrustedDomains(this._client);
  }
};
Settings4.AllowPolicies = AllowPolicies;
Settings4.AllowPolicyListResponsesV4PagePaginationArray = AllowPolicyListResponsesV4PagePaginationArray;
Settings4.BlockSenders = BlockSenders;
Settings4.BlockSenderListResponsesV4PagePaginationArray = BlockSenderListResponsesV4PagePaginationArray;
Settings4.Domains = Domains;
Settings4.DomainListResponsesV4PagePaginationArray = DomainListResponsesV4PagePaginationArray;
Settings4.DomainBulkDeleteResponsesSinglePage = DomainBulkDeleteResponsesSinglePage;
Settings4.ImpersonationRegistry = ImpersonationRegistry;
Settings4.ImpersonationRegistryListResponsesV4PagePaginationArray = ImpersonationRegistryListResponsesV4PagePaginationArray;
Settings4.TrustedDomains = TrustedDomains;
Settings4.TrustedDomainListResponsesV4PagePaginationArray = TrustedDomainListResponsesV4PagePaginationArray;
var EmailSecurity = class extends APIResource {
  static {
    __name(this, "EmailSecurity");
  }
  constructor() {
    super(...arguments);
    this.investigate = new Investigate(this._client);
    this.settings = new Settings4(this._client);
    this.submissions = new Submissions(this._client);
  }
};
EmailSecurity.Investigate = Investigate;
EmailSecurity.InvestigateListResponsesV4PagePaginationArray = InvestigateListResponsesV4PagePaginationArray;
EmailSecurity.Settings = Settings4;
EmailSecurity.Submissions = Submissions;
EmailSecurity.SubmissionListResponsesV4PagePaginationArray = SubmissionListResponsesV4PagePaginationArray;
var Filters = class extends APIResource {
  static {
    __name(this, "Filters");
  }
  create(params, options) {
    const { zone_id, body } = params;
    return this._client.getAPIList(`/zones/${zone_id}/filters`, FirewallFiltersSinglePage, {
      body,
      method: "post",
      ...options
    });
  }
  update(filterId, params, options) {
    const { zone_id, ...body } = params;
    return this._client.put(`/zones/${zone_id}/filters/${filterId}`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { zone_id, ...query } = params;
    return this._client.getAPIList(`/zones/${zone_id}/filters`, FirewallFiltersV4PagePaginationArray, {
      query,
      ...options
    });
  }
  delete(filterId, params, options) {
    const { zone_id } = params;
    return this._client.delete(`/zones/${zone_id}/filters/${filterId}`, options)._thenUnwrap((obj) => obj.result);
  }
  bulkDelete(params, options) {
    const { zone_id, id } = params;
    return this._client.delete(`/zones/${zone_id}/filters`, { query: { id }, ...options })._thenUnwrap((obj) => obj.result);
  }
  bulkUpdate(params, options) {
    const { zone_id, body } = params;
    return this._client.getAPIList(`/zones/${zone_id}/filters`, FirewallFiltersSinglePage, {
      body,
      method: "put",
      ...options
    });
  }
  get(filterId, params, options) {
    const { zone_id } = params;
    return this._client.get(`/zones/${zone_id}/filters/${filterId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var FirewallFiltersSinglePage = class extends SinglePage {
  static {
    __name(this, "FirewallFiltersSinglePage");
  }
};
var FirewallFiltersV4PagePaginationArray = class extends V4PagePaginationArray {
  static {
    __name(this, "FirewallFiltersV4PagePaginationArray");
  }
};
Filters.FirewallFiltersSinglePage = FirewallFiltersSinglePage;
Filters.FirewallFiltersV4PagePaginationArray = FirewallFiltersV4PagePaginationArray;
var AccessRules = class extends APIResource {
  static {
    __name(this, "AccessRules");
  }
  create(params, options) {
    const { account_id, zone_id, ...body } = params;
    if (!account_id && !zone_id) {
      throw new CloudflareError("You must provide either account_id or zone_id.");
    }
    if (account_id && zone_id) {
      throw new CloudflareError("You cannot provide both account_id and zone_id.");
    }
    const { accountOrZone, accountOrZoneId } = account_id ? {
      accountOrZone: "accounts",
      accountOrZoneId: account_id
    } : {
      accountOrZone: "zones",
      accountOrZoneId: zone_id
    };
    return this._client.post(`/${accountOrZone}/${accountOrZoneId}/firewall/access_rules/rules`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(params = {}, options) {
    if (isRequestOptions(params)) {
      return this.list({}, params);
    }
    const { account_id, zone_id, ...query } = params;
    if (!account_id && !zone_id) {
      throw new CloudflareError("You must provide either account_id or zone_id.");
    }
    if (account_id && zone_id) {
      throw new CloudflareError("You cannot provide both account_id and zone_id.");
    }
    const { accountOrZone, accountOrZoneId } = account_id ? {
      accountOrZone: "accounts",
      accountOrZoneId: account_id
    } : {
      accountOrZone: "zones",
      accountOrZoneId: zone_id
    };
    return this._client.getAPIList(`/${accountOrZone}/${accountOrZoneId}/firewall/access_rules/rules`, AccessRuleListResponsesV4PagePaginationArray, { query, ...options });
  }
  delete(ruleId, params = {}, options) {
    if (isRequestOptions(params)) {
      return this.delete(ruleId, {}, params);
    }
    const { account_id, zone_id } = params;
    if (!account_id && !zone_id) {
      throw new CloudflareError("You must provide either account_id or zone_id.");
    }
    if (account_id && zone_id) {
      throw new CloudflareError("You cannot provide both account_id and zone_id.");
    }
    const { accountOrZone, accountOrZoneId } = account_id ? {
      accountOrZone: "accounts",
      accountOrZoneId: account_id
    } : {
      accountOrZone: "zones",
      accountOrZoneId: zone_id
    };
    return this._client.delete(`/${accountOrZone}/${accountOrZoneId}/firewall/access_rules/rules/${ruleId}`, options)._thenUnwrap((obj) => obj.result);
  }
  edit(ruleId, params, options) {
    const { account_id, zone_id, ...body } = params;
    if (!account_id && !zone_id) {
      throw new CloudflareError("You must provide either account_id or zone_id.");
    }
    if (account_id && zone_id) {
      throw new CloudflareError("You cannot provide both account_id and zone_id.");
    }
    const { accountOrZone, accountOrZoneId } = account_id ? {
      accountOrZone: "accounts",
      accountOrZoneId: account_id
    } : {
      accountOrZone: "zones",
      accountOrZoneId: zone_id
    };
    return this._client.patch(`/${accountOrZone}/${accountOrZoneId}/firewall/access_rules/rules/${ruleId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  get(ruleId, params = {}, options) {
    if (isRequestOptions(params)) {
      return this.get(ruleId, {}, params);
    }
    const { account_id, zone_id } = params;
    if (!account_id && !zone_id) {
      throw new CloudflareError("You must provide either account_id or zone_id.");
    }
    if (account_id && zone_id) {
      throw new CloudflareError("You cannot provide both account_id and zone_id.");
    }
    const { accountOrZone, accountOrZoneId } = account_id ? {
      accountOrZone: "accounts",
      accountOrZoneId: account_id
    } : {
      accountOrZone: "zones",
      accountOrZoneId: zone_id
    };
    return this._client.get(`/${accountOrZone}/${accountOrZoneId}/firewall/access_rules/rules/${ruleId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var AccessRuleListResponsesV4PagePaginationArray = class extends V4PagePaginationArray {
  static {
    __name(this, "AccessRuleListResponsesV4PagePaginationArray");
  }
};
AccessRules.AccessRuleListResponsesV4PagePaginationArray = AccessRuleListResponsesV4PagePaginationArray;
var Lockdowns = class extends APIResource {
  static {
    __name(this, "Lockdowns");
  }
  create(params, options) {
    const { zone_id, ...body } = params;
    return this._client.post(`/zones/${zone_id}/firewall/lockdowns`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  update(lockDownsId, params, options) {
    const { zone_id, ...body } = params;
    return this._client.put(`/zones/${zone_id}/firewall/lockdowns/${lockDownsId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { zone_id, ...query } = params;
    return this._client.getAPIList(`/zones/${zone_id}/firewall/lockdowns`, LockdownsV4PagePaginationArray, {
      query,
      ...options
    });
  }
  delete(lockDownsId, params, options) {
    const { zone_id } = params;
    return this._client.delete(`/zones/${zone_id}/firewall/lockdowns/${lockDownsId}`, options)._thenUnwrap((obj) => obj.result);
  }
  get(lockDownsId, params, options) {
    const { zone_id } = params;
    return this._client.get(`/zones/${zone_id}/firewall/lockdowns/${lockDownsId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var LockdownsV4PagePaginationArray = class extends V4PagePaginationArray {
  static {
    __name(this, "LockdownsV4PagePaginationArray");
  }
};
Lockdowns.LockdownsV4PagePaginationArray = LockdownsV4PagePaginationArray;
var Rules3 = class extends APIResource {
  static {
    __name(this, "Rules3");
  }
  create(params, options) {
    const { zone_id, ...body } = params;
    return this._client.getAPIList(`/zones/${zone_id}/firewall/rules`, FirewallRulesSinglePage, {
      body,
      method: "post",
      ...options
    });
  }
  update(ruleId, params, options) {
    const { zone_id, ...body } = params;
    return this._client.put(`/zones/${zone_id}/firewall/rules/${ruleId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { zone_id, ...query } = params;
    return this._client.getAPIList(`/zones/${zone_id}/firewall/rules`, FirewallRulesV4PagePaginationArray, {
      query,
      ...options
    });
  }
  delete(ruleId, params, options) {
    const { zone_id } = params;
    return this._client.delete(`/zones/${zone_id}/firewall/rules/${ruleId}`, options)._thenUnwrap((obj) => obj.result);
  }
  bulkDelete(params, options) {
    const { zone_id } = params;
    return this._client.getAPIList(`/zones/${zone_id}/firewall/rules`, FirewallRulesSinglePage, {
      method: "delete",
      ...options
    });
  }
  bulkEdit(params, options) {
    const { zone_id, body } = params;
    return this._client.getAPIList(`/zones/${zone_id}/firewall/rules`, FirewallRulesSinglePage, {
      body,
      method: "patch",
      ...options
    });
  }
  bulkUpdate(params, options) {
    const { zone_id, body } = params;
    return this._client.getAPIList(`/zones/${zone_id}/firewall/rules`, FirewallRulesSinglePage, {
      body,
      method: "put",
      ...options
    });
  }
  edit(ruleId, params, options) {
    const { zone_id, ...body } = params;
    return this._client.getAPIList(`/zones/${zone_id}/firewall/rules/${ruleId}`, FirewallRulesSinglePage, {
      body,
      method: "patch",
      ...options
    });
  }
  get(ruleId, params, options) {
    const { zone_id } = params;
    return this._client.get(`/zones/${zone_id}/firewall/rules/${ruleId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var FirewallRulesSinglePage = class extends SinglePage {
  static {
    __name(this, "FirewallRulesSinglePage");
  }
};
var FirewallRulesV4PagePaginationArray = class extends V4PagePaginationArray {
  static {
    __name(this, "FirewallRulesV4PagePaginationArray");
  }
};
Rules3.FirewallRulesSinglePage = FirewallRulesSinglePage;
Rules3.FirewallRulesV4PagePaginationArray = FirewallRulesV4PagePaginationArray;
var UARules = class extends APIResource {
  static {
    __name(this, "UARules");
  }
  create(params, options) {
    const { zone_id, ...body } = params;
    return this._client.post(`/zones/${zone_id}/firewall/ua_rules`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  update(uaRuleId, params, options) {
    const { zone_id, ...body } = params;
    return this._client.put(`/zones/${zone_id}/firewall/ua_rules/${uaRuleId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { zone_id, ...query } = params;
    return this._client.getAPIList(`/zones/${zone_id}/firewall/ua_rules`, UARuleListResponsesV4PagePaginationArray, { query, ...options });
  }
  delete(uaRuleId, params, options) {
    const { zone_id } = params;
    return this._client.delete(`/zones/${zone_id}/firewall/ua_rules/${uaRuleId}`, options)._thenUnwrap((obj) => obj.result);
  }
  get(uaRuleId, params, options) {
    const { zone_id } = params;
    return this._client.get(`/zones/${zone_id}/firewall/ua_rules/${uaRuleId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var UARuleListResponsesV4PagePaginationArray = class extends V4PagePaginationArray {
  static {
    __name(this, "UARuleListResponsesV4PagePaginationArray");
  }
};
UARules.UARuleListResponsesV4PagePaginationArray = UARuleListResponsesV4PagePaginationArray;
var Overrides = class extends APIResource {
  static {
    __name(this, "Overrides");
  }
  create(params, options) {
    const { zone_id, ...body } = params;
    return this._client.post(`/zones/${zone_id}/firewall/waf/overrides`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  update(overridesId, params, options) {
    const { zone_id, ...body } = params;
    return this._client.put(`/zones/${zone_id}/firewall/waf/overrides/${overridesId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { zone_id, ...query } = params;
    return this._client.getAPIList(`/zones/${zone_id}/firewall/waf/overrides`, OverridesV4PagePaginationArray, { query, ...options });
  }
  delete(overridesId, params, options) {
    const { zone_id } = params;
    return this._client.delete(`/zones/${zone_id}/firewall/waf/overrides/${overridesId}`, options)._thenUnwrap((obj) => obj.result);
  }
  get(overridesId, params, options) {
    const { zone_id } = params;
    return this._client.get(`/zones/${zone_id}/firewall/waf/overrides/${overridesId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var OverridesV4PagePaginationArray = class extends V4PagePaginationArray {
  static {
    __name(this, "OverridesV4PagePaginationArray");
  }
};
Overrides.OverridesV4PagePaginationArray = OverridesV4PagePaginationArray;
var Groups = class extends APIResource {
  static {
    __name(this, "Groups");
  }
  list(packageId, params, options) {
    const { zone_id, ...query } = params;
    return this._client.getAPIList(`/zones/${zone_id}/firewall/waf/packages/${packageId}/groups`, GroupsV4PagePaginationArray, { query, ...options });
  }
  edit(packageId, groupId, params, options) {
    const { zone_id, ...body } = params;
    return this._client.patch(`/zones/${zone_id}/firewall/waf/packages/${packageId}/groups/${groupId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  get(packageId, groupId, params, options) {
    const { zone_id } = params;
    return this._client.get(`/zones/${zone_id}/firewall/waf/packages/${packageId}/groups/${groupId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var GroupsV4PagePaginationArray = class extends V4PagePaginationArray {
  static {
    __name(this, "GroupsV4PagePaginationArray");
  }
};
Groups.GroupsV4PagePaginationArray = GroupsV4PagePaginationArray;
var Rules4 = class extends APIResource {
  static {
    __name(this, "Rules4");
  }
  list(packageId, params, options) {
    const { zone_id, ...query } = params;
    return this._client.getAPIList(`/zones/${zone_id}/firewall/waf/packages/${packageId}/rules`, RuleListResponsesV4PagePaginationArray, { query, ...options });
  }
  edit(packageId, ruleId, params, options) {
    const { zone_id, ...body } = params;
    return this._client.patch(`/zones/${zone_id}/firewall/waf/packages/${packageId}/rules/${ruleId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  get(packageId, ruleId, params, options) {
    const { zone_id } = params;
    return this._client.get(`/zones/${zone_id}/firewall/waf/packages/${packageId}/rules/${ruleId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var RuleListResponsesV4PagePaginationArray = class extends V4PagePaginationArray {
  static {
    __name(this, "RuleListResponsesV4PagePaginationArray");
  }
};
Rules4.RuleListResponsesV4PagePaginationArray = RuleListResponsesV4PagePaginationArray;
var Packages = class extends APIResource {
  static {
    __name(this, "Packages");
  }
  constructor() {
    super(...arguments);
    this.groups = new Groups(this._client);
    this.rules = new Rules4(this._client);
  }
  list(params, options) {
    const { zone_id, ...query } = params;
    return this._client.getAPIList(`/zones/${zone_id}/firewall/waf/packages`, PackageListResponsesV4PagePaginationArray, { query, ...options });
  }
  get(packageId, params, options) {
    const { zone_id } = params;
    return this._client.get(`/zones/${zone_id}/firewall/waf/packages/${packageId}`, options);
  }
};
var PackageListResponsesV4PagePaginationArray = class extends V4PagePaginationArray {
  static {
    __name(this, "PackageListResponsesV4PagePaginationArray");
  }
};
Packages.PackageListResponsesV4PagePaginationArray = PackageListResponsesV4PagePaginationArray;
Packages.Groups = Groups;
Packages.GroupsV4PagePaginationArray = GroupsV4PagePaginationArray;
Packages.Rules = Rules4;
Packages.RuleListResponsesV4PagePaginationArray = RuleListResponsesV4PagePaginationArray;
var WAF = class extends APIResource {
  static {
    __name(this, "WAF");
  }
  constructor() {
    super(...arguments);
    this.overrides = new Overrides(this._client);
    this.packages = new Packages(this._client);
  }
};
WAF.Overrides = Overrides;
WAF.OverridesV4PagePaginationArray = OverridesV4PagePaginationArray;
WAF.Packages = Packages;
WAF.PackageListResponsesV4PagePaginationArray = PackageListResponsesV4PagePaginationArray;
var Firewall = class extends APIResource {
  static {
    __name(this, "Firewall");
  }
  constructor() {
    super(...arguments);
    this.lockdowns = new Lockdowns(this._client);
    this.rules = new Rules3(this._client);
    this.accessRules = new AccessRules(this._client);
    this.uaRules = new UARules(this._client);
    this.waf = new WAF(this._client);
  }
};
Firewall.Lockdowns = Lockdowns;
Firewall.LockdownsV4PagePaginationArray = LockdownsV4PagePaginationArray;
Firewall.Rules = Rules3;
Firewall.FirewallRulesSinglePage = FirewallRulesSinglePage;
Firewall.FirewallRulesV4PagePaginationArray = FirewallRulesV4PagePaginationArray;
Firewall.AccessRules = AccessRules;
Firewall.AccessRuleListResponsesV4PagePaginationArray = AccessRuleListResponsesV4PagePaginationArray;
Firewall.UARules = UARules;
Firewall.UARuleListResponsesV4PagePaginationArray = UARuleListResponsesV4PagePaginationArray;
Firewall.WAF = WAF;
var Previews = class extends APIResource {
  static {
    __name(this, "Previews");
  }
  create(params, options) {
    const { zone_id, ...body } = params;
    return this._client.post(`/zones/${zone_id}/healthchecks/preview`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  delete(healthcheckId, params, options) {
    const { zone_id } = params;
    return this._client.delete(`/zones/${zone_id}/healthchecks/preview/${healthcheckId}`, options)._thenUnwrap((obj) => obj.result);
  }
  get(healthcheckId, params, options) {
    const { zone_id } = params;
    return this._client.get(`/zones/${zone_id}/healthchecks/preview/${healthcheckId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var Healthchecks = class extends APIResource {
  static {
    __name(this, "Healthchecks");
  }
  constructor() {
    super(...arguments);
    this.previews = new Previews(this._client);
  }
  create(params, options) {
    const { zone_id, ...body } = params;
    return this._client.post(`/zones/${zone_id}/healthchecks`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  update(healthcheckId, params, options) {
    const { zone_id, ...body } = params;
    return this._client.put(`/zones/${zone_id}/healthchecks/${healthcheckId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { zone_id, ...query } = params;
    return this._client.getAPIList(`/zones/${zone_id}/healthchecks`, HealthchecksV4PagePaginationArray, {
      query,
      ...options
    });
  }
  delete(healthcheckId, params, options) {
    const { zone_id } = params;
    return this._client.delete(`/zones/${zone_id}/healthchecks/${healthcheckId}`, options)._thenUnwrap((obj) => obj.result);
  }
  edit(healthcheckId, params, options) {
    const { zone_id, ...body } = params;
    return this._client.patch(`/zones/${zone_id}/healthchecks/${healthcheckId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  get(healthcheckId, params, options) {
    const { zone_id } = params;
    return this._client.get(`/zones/${zone_id}/healthchecks/${healthcheckId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var HealthchecksV4PagePaginationArray = class extends V4PagePaginationArray {
  static {
    __name(this, "HealthchecksV4PagePaginationArray");
  }
};
Healthchecks.HealthchecksV4PagePaginationArray = HealthchecksV4PagePaginationArray;
Healthchecks.Previews = Previews;
var TLS = class extends APIResource {
  static {
    __name(this, "TLS");
  }
  update(settingId, hostname, params, options) {
    const { zone_id, ...body } = params;
    return this._client.put(`/zones/${zone_id}/hostnames/settings/${settingId}/${hostname}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  delete(settingId, hostname, params, options) {
    const { zone_id } = params;
    return this._client.delete(`/zones/${zone_id}/hostnames/settings/${settingId}/${hostname}`, options)._thenUnwrap((obj) => obj.result);
  }
  get(settingId, params, options) {
    const { zone_id } = params;
    return this._client.getAPIList(`/zones/${zone_id}/hostnames/settings/${settingId}`, TLSGetResponsesSinglePage, options);
  }
};
var TLSGetResponsesSinglePage = class extends SinglePage {
  static {
    __name(this, "TLSGetResponsesSinglePage");
  }
};
TLS.TLSGetResponsesSinglePage = TLSGetResponsesSinglePage;
var Settings5 = class extends APIResource {
  static {
    __name(this, "Settings5");
  }
  constructor() {
    super(...arguments);
    this.tls = new TLS(this._client);
  }
};
Settings5.TLS = TLS;
Settings5.TLSGetResponsesSinglePage = TLSGetResponsesSinglePage;
var Hostnames = class extends APIResource {
  static {
    __name(this, "Hostnames");
  }
  constructor() {
    super(...arguments);
    this.settings = new Settings5(this._client);
  }
};
Hostnames.Settings = Settings5;
var Configs2 = class extends APIResource {
  static {
    __name(this, "Configs2");
  }
  create(params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/hyperdrive/configs`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  update(hyperdriveId, params, options) {
    const { account_id, ...body } = params;
    return this._client.put(`/accounts/${account_id}/hyperdrive/configs/${hyperdriveId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { account_id } = params;
    return this._client.getAPIList(`/accounts/${account_id}/hyperdrive/configs`, HyperdrivesSinglePage, options);
  }
  delete(hyperdriveId, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/hyperdrive/configs/${hyperdriveId}`, options)._thenUnwrap((obj) => obj.result);
  }
  edit(hyperdriveId, params, options) {
    const { account_id, ...body } = params;
    return this._client.patch(`/accounts/${account_id}/hyperdrive/configs/${hyperdriveId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  get(hyperdriveId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/hyperdrive/configs/${hyperdriveId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var HyperdriveResource = class extends APIResource {
  static {
    __name(this, "HyperdriveResource");
  }
  constructor() {
    super(...arguments);
    this.configs = new Configs2(this._client);
  }
};
var HyperdrivesSinglePage = class extends SinglePage {
  static {
    __name(this, "HyperdrivesSinglePage");
  }
};
HyperdriveResource.Configs = Configs2;
var PermissionGroups2 = class extends APIResource {
  static {
    __name(this, "PermissionGroups2");
  }
  list(params, options) {
    const { account_id, ...query } = params;
    return this._client.getAPIList(`/accounts/${account_id}/iam/permission_groups`, PermissionGroupListResponsesV4PagePaginationArray, { query, ...options });
  }
  get(permissionGroupId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/iam/permission_groups/${permissionGroupId}`, options);
  }
};
var PermissionGroupListResponsesV4PagePaginationArray = class extends V4PagePaginationArray {
  static {
    __name(this, "PermissionGroupListResponsesV4PagePaginationArray");
  }
};
PermissionGroups2.PermissionGroupListResponsesV4PagePaginationArray = PermissionGroupListResponsesV4PagePaginationArray;
var ResourceGroups = class extends APIResource {
  static {
    __name(this, "ResourceGroups");
  }
  create(params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/iam/resource_groups`, { body, ...options });
  }
  update(resourceGroupId, params, options) {
    const { account_id, ...body } = params;
    return this._client.put(`/accounts/${account_id}/iam/resource_groups/${resourceGroupId}`, {
      body,
      ...options
    });
  }
  list(params, options) {
    const { account_id, ...query } = params;
    return this._client.getAPIList(`/accounts/${account_id}/iam/resource_groups`, ResourceGroupListResponsesSinglePage, { query, ...options });
  }
  delete(resourceGroupId, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/iam/resource_groups/${resourceGroupId}`, options)._thenUnwrap((obj) => obj.result);
  }
  get(resourceGroupId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/iam/resource_groups/${resourceGroupId}`, options);
  }
};
var ResourceGroupListResponsesSinglePage = class extends SinglePage {
  static {
    __name(this, "ResourceGroupListResponsesSinglePage");
  }
};
ResourceGroups.ResourceGroupListResponsesSinglePage = ResourceGroupListResponsesSinglePage;
var Members2 = class extends APIResource {
  static {
    __name(this, "Members2");
  }
  create(userGroupId, params, options) {
    const { account_id, body } = params;
    return this._client.post(`/accounts/${account_id}/iam/user_groups/${userGroupId}/members`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  update(userGroupId, params, options) {
    const { account_id, body } = params;
    return this._client.getAPIList(`/accounts/${account_id}/iam/user_groups/${userGroupId}/members`, MemberUpdateResponsesSinglePage, { body, method: "put", ...options });
  }
  list(userGroupId, params, options) {
    const { account_id, ...query } = params;
    return this._client.getAPIList(`/accounts/${account_id}/iam/user_groups/${userGroupId}/members`, MemberListResponsesV4PagePaginationArray, { query, ...options });
  }
  delete(userGroupId, memberId, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/iam/user_groups/${userGroupId}/members/${memberId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var MemberUpdateResponsesSinglePage = class extends SinglePage {
  static {
    __name(this, "MemberUpdateResponsesSinglePage");
  }
};
var MemberListResponsesV4PagePaginationArray = class extends V4PagePaginationArray {
  static {
    __name(this, "MemberListResponsesV4PagePaginationArray");
  }
};
Members2.MemberUpdateResponsesSinglePage = MemberUpdateResponsesSinglePage;
Members2.MemberListResponsesV4PagePaginationArray = MemberListResponsesV4PagePaginationArray;
var UserGroups = class extends APIResource {
  static {
    __name(this, "UserGroups");
  }
  constructor() {
    super(...arguments);
    this.members = new Members2(this._client);
  }
  create(params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/iam/user_groups`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  update(userGroupId, params, options) {
    const { account_id, ...body } = params;
    return this._client.put(`/accounts/${account_id}/iam/user_groups/${userGroupId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { account_id, ...query } = params;
    return this._client.getAPIList(`/accounts/${account_id}/iam/user_groups`, UserGroupListResponsesV4PagePaginationArray, { query, ...options });
  }
  delete(userGroupId, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/iam/user_groups/${userGroupId}`, options)._thenUnwrap((obj) => obj.result);
  }
  get(userGroupId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/iam/user_groups/${userGroupId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var UserGroupListResponsesV4PagePaginationArray = class extends V4PagePaginationArray {
  static {
    __name(this, "UserGroupListResponsesV4PagePaginationArray");
  }
};
UserGroups.UserGroupListResponsesV4PagePaginationArray = UserGroupListResponsesV4PagePaginationArray;
UserGroups.Members = Members2;
UserGroups.MemberUpdateResponsesSinglePage = MemberUpdateResponsesSinglePage;
UserGroups.MemberListResponsesV4PagePaginationArray = MemberListResponsesV4PagePaginationArray;
var IAM = class extends APIResource {
  static {
    __name(this, "IAM");
  }
  constructor() {
    super(...arguments);
    this.permissionGroups = new PermissionGroups2(this._client);
    this.resourceGroups = new ResourceGroups(this._client);
    this.userGroups = new UserGroups(this._client);
  }
};
IAM.PermissionGroups = PermissionGroups2;
IAM.PermissionGroupListResponsesV4PagePaginationArray = PermissionGroupListResponsesV4PagePaginationArray;
IAM.ResourceGroups = ResourceGroups;
IAM.ResourceGroupListResponsesSinglePage = ResourceGroupListResponsesSinglePage;
IAM.UserGroups = UserGroups;
IAM.UserGroupListResponsesV4PagePaginationArray = UserGroupListResponsesV4PagePaginationArray;
var IPs2 = class extends APIResource {
  static {
    __name(this, "IPs2");
  }
  list(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.list({}, query);
    }
    return this._client.get("/ips", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
};
var Blobs = class extends APIResource {
  static {
    __name(this, "Blobs");
  }
  get(imageId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/images/v1/${imageId}/blob`, {
      ...options,
      headers: { Accept: "image/*", ...options?.headers },
      __binaryResponse: true
    });
  }
};
var Keys = class extends APIResource {
  static {
    __name(this, "Keys");
  }
  update(signingKeyName, params, options) {
    const { account_id } = params;
    return this._client.put(`/accounts/${account_id}/images/v1/keys/${signingKeyName}`, options)._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/images/v1/keys`, options)._thenUnwrap((obj) => obj.result);
  }
  delete(signingKeyName, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/images/v1/keys/${signingKeyName}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var Stats = class extends APIResource {
  static {
    __name(this, "Stats");
  }
  get(params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/images/v1/stats`, options)._thenUnwrap((obj) => obj.result);
  }
};
var Variants2 = class extends APIResource {
  static {
    __name(this, "Variants2");
  }
  create(params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/images/v1/variants`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/images/v1/variants`, options)._thenUnwrap((obj) => obj.result);
  }
  delete(variantId, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/images/v1/variants/${variantId}`, options)._thenUnwrap((obj) => obj.result);
  }
  edit(variantId, params, options) {
    const { account_id, ...body } = params;
    return this._client.patch(`/accounts/${account_id}/images/v1/variants/${variantId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  get(variantId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/images/v1/variants/${variantId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var V1 = class extends APIResource {
  static {
    __name(this, "V1");
  }
  constructor() {
    super(...arguments);
    this.keys = new Keys(this._client);
    this.stats = new Stats(this._client);
    this.variants = new Variants2(this._client);
    this.blobs = new Blobs(this._client);
  }
  create(params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/images/v1`, multipartFormRequestOptions({ body, ...options }))._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { account_id, ...query } = params;
    return this._client.getAPIList(`/accounts/${account_id}/images/v1`, V1ListResponsesV4PagePagination, {
      query,
      ...options
    });
  }
  delete(imageId, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/images/v1/${imageId}`, options)._thenUnwrap((obj) => obj.result);
  }
  edit(imageId, params, options) {
    const { account_id, ...body } = params;
    return this._client.patch(`/accounts/${account_id}/images/v1/${imageId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  get(imageId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/images/v1/${imageId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var V1ListResponsesV4PagePagination = class extends V4PagePagination {
  static {
    __name(this, "V1ListResponsesV4PagePagination");
  }
};
V1.V1ListResponsesV4PagePagination = V1ListResponsesV4PagePagination;
V1.Keys = Keys;
V1.Stats = Stats;
V1.Variants = Variants2;
V1.Blobs = Blobs;
var DirectUploads = class extends APIResource {
  static {
    __name(this, "DirectUploads");
  }
  create(params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/images/v2/direct_upload`, multipartFormRequestOptions({ body, ...options }))._thenUnwrap((obj) => obj.result);
  }
};
var V2 = class extends APIResource {
  static {
    __name(this, "V2");
  }
  constructor() {
    super(...arguments);
    this.directUploads = new DirectUploads(this._client);
  }
  list(params, options) {
    const { account_id, ...query } = params;
    return this._client.get(`/accounts/${account_id}/images/v2`, { query, ...options })._thenUnwrap((obj) => obj.result);
  }
};
V2.DirectUploads = DirectUploads;
var Images = class extends APIResource {
  static {
    __name(this, "Images");
  }
  constructor() {
    super(...arguments);
    this.v1 = new V1(this._client);
    this.v2 = new V2(this._client);
  }
};
Images.V1 = V1;
Images.V1ListResponsesV4PagePagination = V1ListResponsesV4PagePagination;
Images.V2 = V2;
var DNS3 = class extends APIResource {
  static {
    __name(this, "DNS3");
  }
  list(params, options) {
    const { account_id, ...query } = params;
    return this._client.getAPIList(`/accounts/${account_id}/intel/dns`, DNSV4PagePagination, {
      query,
      ...options
    });
  }
};
var DNSV4PagePagination = class extends V4PagePagination {
  static {
    __name(this, "DNSV4PagePagination");
  }
};
DNS3.DNSV4PagePagination = DNSV4PagePagination;
var DomainHistoryResource = class extends APIResource {
  static {
    __name(this, "DomainHistoryResource");
  }
  get(params, options) {
    const { account_id, ...query } = params;
    return this._client.get(`/accounts/${account_id}/intel/domain-history`, {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
};
var IPLists = class extends APIResource {
  static {
    __name(this, "IPLists");
  }
  get(params, options) {
    const { account_id } = params;
    return this._client.getAPIList(`/accounts/${account_id}/intel/ip-list`, IPListsSinglePage, options);
  }
};
var IPListsSinglePage = class extends SinglePage {
  static {
    __name(this, "IPListsSinglePage");
  }
};
IPLists.IPListsSinglePage = IPListsSinglePage;
var IPs3 = class extends APIResource {
  static {
    __name(this, "IPs3");
  }
  get(params, options) {
    const { account_id, ...query } = params;
    return this._client.get(`/accounts/${account_id}/intel/ip`, { query, ...options })._thenUnwrap((obj) => obj.result);
  }
};
var Miscategorizations = class extends APIResource {
  static {
    __name(this, "Miscategorizations");
  }
  create(params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/intel/miscategorization`, { body, ...options });
  }
};
var Sinkholes = class extends APIResource {
  static {
    __name(this, "Sinkholes");
  }
  list(params, options) {
    const { account_id } = params;
    return this._client.getAPIList(`/accounts/${account_id}/intel/sinkholes`, SinkholesSinglePage, options);
  }
};
var SinkholesSinglePage = class extends SinglePage {
  static {
    __name(this, "SinkholesSinglePage");
  }
};
Sinkholes.SinkholesSinglePage = SinkholesSinglePage;
var Whois = class extends APIResource {
  static {
    __name(this, "Whois");
  }
  get(params, options) {
    const { account_id, ...query } = params;
    return this._client.get(`/accounts/${account_id}/intel/whois`, { query, ...options })._thenUnwrap((obj) => obj.result);
  }
};
var Subnets = class extends APIResource {
  static {
    __name(this, "Subnets");
  }
  get(asn, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/intel/asn/${asn}/subnets`, options);
  }
};
var ASN3 = class extends APIResource {
  static {
    __name(this, "ASN3");
  }
  constructor() {
    super(...arguments);
    this.subnets = new Subnets(this._client);
  }
  get(asn, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/intel/asn/${asn}`, options)._thenUnwrap((obj) => obj.result);
  }
};
ASN3.Subnets = Subnets;
var IssueTypes = class extends APIResource {
  static {
    __name(this, "IssueTypes");
  }
  get(params, options) {
    const { account_id } = params;
    return this._client.getAPIList(`/accounts/${account_id}/intel/attack-surface-report/issue-types`, IssueTypeGetResponsesSinglePage, options);
  }
};
var IssueTypeGetResponsesSinglePage = class extends SinglePage {
  static {
    __name(this, "IssueTypeGetResponsesSinglePage");
  }
};
IssueTypes.IssueTypeGetResponsesSinglePage = IssueTypeGetResponsesSinglePage;
var Issues = class extends APIResource {
  static {
    __name(this, "Issues");
  }
  list(params, options) {
    const { account_id, ...query } = params;
    return this._client.getAPIList(`/accounts/${account_id}/intel/attack-surface-report/issues`, IssueListResponsesV4PagePagination, { query, ...options });
  }
  class(params, options) {
    const { account_id, ...query } = params;
    return this._client.get(`/accounts/${account_id}/intel/attack-surface-report/issues/class`, {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  dismiss(issueId, params, options) {
    const { account_id, ...body } = params;
    return this._client.put(`/accounts/${account_id}/intel/attack-surface-report/${issueId}/dismiss`, {
      body,
      ...options
    });
  }
  severity(params, options) {
    const { account_id, ...query } = params;
    return this._client.get(`/accounts/${account_id}/intel/attack-surface-report/issues/severity`, {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  type(params, options) {
    const { account_id, ...query } = params;
    return this._client.get(`/accounts/${account_id}/intel/attack-surface-report/issues/type`, {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
};
var IssueListResponsesV4PagePagination = class extends V4PagePagination {
  static {
    __name(this, "IssueListResponsesV4PagePagination");
  }
};
Issues.IssueListResponsesV4PagePagination = IssueListResponsesV4PagePagination;
var AttackSurfaceReport = class extends APIResource {
  static {
    __name(this, "AttackSurfaceReport");
  }
  constructor() {
    super(...arguments);
    this.issueTypes = new IssueTypes(this._client);
    this.issues = new Issues(this._client);
  }
};
AttackSurfaceReport.IssueTypes = IssueTypes;
AttackSurfaceReport.IssueTypeGetResponsesSinglePage = IssueTypeGetResponsesSinglePage;
AttackSurfaceReport.Issues = Issues;
AttackSurfaceReport.IssueListResponsesV4PagePagination = IssueListResponsesV4PagePagination;
var Bulks = class extends APIResource {
  static {
    __name(this, "Bulks");
  }
  get(params, options) {
    const { account_id, ...query } = params;
    return this._client.get(`/accounts/${account_id}/intel/domain/bulk`, {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
};
var Domains2 = class extends APIResource {
  static {
    __name(this, "Domains2");
  }
  constructor() {
    super(...arguments);
    this.bulks = new Bulks(this._client);
  }
  get(params, options) {
    const { account_id, ...query } = params;
    return this._client.get(`/accounts/${account_id}/intel/domain`, { query, ...options })._thenUnwrap((obj) => obj.result);
  }
};
Domains2.Bulks = Bulks;
var Downloads = class extends APIResource {
  static {
    __name(this, "Downloads");
  }
};
var Permissions = class extends APIResource {
  static {
    __name(this, "Permissions");
  }
  create(params, options) {
    const { account_id, ...body } = params;
    return this._client.put(`/accounts/${account_id}/intel/indicator-feeds/permissions/add`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/intel/indicator-feeds/permissions/view`, options)._thenUnwrap((obj) => obj.result);
  }
  delete(params, options) {
    const { account_id, ...body } = params;
    return this._client.put(`/accounts/${account_id}/intel/indicator-feeds/permissions/remove`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
};
var Snapshots = class extends APIResource {
  static {
    __name(this, "Snapshots");
  }
  update(feedId, params, options) {
    const { account_id, ...body } = params;
    return this._client.put(`/accounts/${account_id}/intel/indicator-feeds/${feedId}/snapshot`, multipartFormRequestOptions({ body, ...options }))._thenUnwrap((obj) => obj.result);
  }
};
var IndicatorFeeds = class extends APIResource {
  static {
    __name(this, "IndicatorFeeds");
  }
  constructor() {
    super(...arguments);
    this.snapshots = new Snapshots(this._client);
    this.permissions = new Permissions(this._client);
    this.downloads = new Downloads(this._client);
  }
  create(params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/intel/indicator-feeds`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  update(feedId, params, options) {
    const { account_id, ...body } = params;
    return this._client.put(`/accounts/${account_id}/intel/indicator-feeds/${feedId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { account_id } = params;
    return this._client.getAPIList(`/accounts/${account_id}/intel/indicator-feeds`, IndicatorFeedListResponsesSinglePage, options);
  }
  data(feedId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/intel/indicator-feeds/${feedId}/data`, {
      ...options,
      headers: { Accept: "text/csv", ...options?.headers }
    });
  }
  get(feedId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/intel/indicator-feeds/${feedId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var IndicatorFeedListResponsesSinglePage = class extends SinglePage {
  static {
    __name(this, "IndicatorFeedListResponsesSinglePage");
  }
};
IndicatorFeeds.IndicatorFeedListResponsesSinglePage = IndicatorFeedListResponsesSinglePage;
IndicatorFeeds.Snapshots = Snapshots;
IndicatorFeeds.Permissions = Permissions;
IndicatorFeeds.Downloads = Downloads;
var Intel = class extends APIResource {
  static {
    __name(this, "Intel");
  }
  constructor() {
    super(...arguments);
    this.asn = new ASN3(this._client);
    this.dns = new DNS3(this._client);
    this.domains = new Domains2(this._client);
    this.domainHistory = new DomainHistoryResource(this._client);
    this.ips = new IPs3(this._client);
    this.ipLists = new IPLists(this._client);
    this.miscategorizations = new Miscategorizations(this._client);
    this.whois = new Whois(this._client);
    this.indicatorFeeds = new IndicatorFeeds(this._client);
    this.sinkholes = new Sinkholes(this._client);
    this.attackSurfaceReport = new AttackSurfaceReport(this._client);
  }
};
Intel.ASN = ASN3;
Intel.DNSV4PagePagination = DNSV4PagePagination;
Intel.Domains = Domains2;
Intel.DomainHistoryResource = DomainHistoryResource;
Intel.IPs = IPs3;
Intel.IPLists = IPLists;
Intel.IPListsSinglePage = IPListsSinglePage;
Intel.Miscategorizations = Miscategorizations;
Intel.IndicatorFeeds = IndicatorFeeds;
Intel.IndicatorFeedListResponsesSinglePage = IndicatorFeedListResponsesSinglePage;
Intel.Sinkholes = Sinkholes;
Intel.SinkholesSinglePage = SinkholesSinglePage;
Intel.AttackSurfaceReport = AttackSurfaceReport;
var Keys2 = class extends APIResource {
  static {
    __name(this, "Keys2");
  }
  list(namespaceId, params, options) {
    const { account_id, ...query } = params;
    return this._client.getAPIList(`/accounts/${account_id}/storage/kv/namespaces/${namespaceId}/keys`, KeysCursorPaginationAfter, { query, ...options });
  }
  bulkDelete(namespaceId, params, options) {
    const { account_id, body } = params;
    return this._client.post(`/accounts/${account_id}/storage/kv/namespaces/${namespaceId}/bulk/delete`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  bulkGet(namespaceId, params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/storage/kv/namespaces/${namespaceId}/bulk/get`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  bulkUpdate(namespaceId, params, options) {
    const { account_id, body } = params;
    return this._client.put(`/accounts/${account_id}/storage/kv/namespaces/${namespaceId}/bulk`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
};
var KeysCursorPaginationAfter = class extends CursorPaginationAfter {
  static {
    __name(this, "KeysCursorPaginationAfter");
  }
};
Keys2.KeysCursorPaginationAfter = KeysCursorPaginationAfter;
var Metadata = class extends APIResource {
  static {
    __name(this, "Metadata");
  }
  get(namespaceId, keyName, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/storage/kv/namespaces/${namespaceId}/metadata/${keyName}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var Values = class extends APIResource {
  static {
    __name(this, "Values");
  }
  update(namespaceId, keyName, params, options) {
    const { account_id, expiration, expiration_ttl, ...body } = params;
    return this._client.put(`/accounts/${account_id}/storage/kv/namespaces/${namespaceId}/values/${keyName}`, multipartFormRequestOptions({
      query: { expiration, expiration_ttl },
      body,
      __multipartSyntax: "json",
      ...options
    }))._thenUnwrap((obj) => obj.result);
  }
  delete(namespaceId, keyName, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/storage/kv/namespaces/${namespaceId}/values/${keyName}`, options)._thenUnwrap((obj) => obj.result);
  }
  get(namespaceId, keyName, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/storage/kv/namespaces/${namespaceId}/values/${keyName}`, {
      ...options,
      headers: { Accept: "application/octet-stream", ...options?.headers },
      __binaryResponse: true
    });
  }
};
var Namespaces2 = class extends APIResource {
  static {
    __name(this, "Namespaces2");
  }
  constructor() {
    super(...arguments);
    this.keys = new Keys2(this._client);
    this.metadata = new Metadata(this._client);
    this.values = new Values(this._client);
  }
  create(params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/storage/kv/namespaces`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  update(namespaceId, params, options) {
    const { account_id, ...body } = params;
    return this._client.put(`/accounts/${account_id}/storage/kv/namespaces/${namespaceId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { account_id, ...query } = params;
    return this._client.getAPIList(`/accounts/${account_id}/storage/kv/namespaces`, NamespacesV4PagePaginationArray2, { query, ...options });
  }
  delete(namespaceId, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/storage/kv/namespaces/${namespaceId}`, options)._thenUnwrap((obj) => obj.result);
  }
  bulkDelete(namespaceId, params, options) {
    const { account_id, body } = params;
    return this._client.post(`/accounts/${account_id}/storage/kv/namespaces/${namespaceId}/bulk/delete`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  bulkGet(namespaceId, params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/storage/kv/namespaces/${namespaceId}/bulk/get`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  bulkUpdate(namespaceId, params, options) {
    const { account_id, body } = params;
    return this._client.put(`/accounts/${account_id}/storage/kv/namespaces/${namespaceId}/bulk`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  get(namespaceId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/storage/kv/namespaces/${namespaceId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var NamespacesV4PagePaginationArray2 = class extends V4PagePaginationArray {
  static {
    __name(this, "NamespacesV4PagePaginationArray2");
  }
};
Namespaces2.NamespacesV4PagePaginationArray = NamespacesV4PagePaginationArray2;
Namespaces2.Keys = Keys2;
Namespaces2.KeysCursorPaginationAfter = KeysCursorPaginationAfter;
Namespaces2.Metadata = Metadata;
Namespaces2.Values = Values;
var KV = class extends APIResource {
  static {
    __name(this, "KV");
  }
  constructor() {
    super(...arguments);
    this.namespaces = new Namespaces2(this._client);
  }
};
KV.Namespaces = Namespaces2;
KV.NamespacesV4PagePaginationArray = NamespacesV4PagePaginationArray2;
var KeylessCertificates = class extends APIResource {
  static {
    __name(this, "KeylessCertificates");
  }
  create(params, options) {
    const { zone_id, ...body } = params;
    return this._client.post(`/zones/${zone_id}/keyless_certificates`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { zone_id } = params;
    return this._client.getAPIList(`/zones/${zone_id}/keyless_certificates`, KeylessCertificatesSinglePage, options);
  }
  delete(keylessCertificateId, params, options) {
    const { zone_id } = params;
    return this._client.delete(`/zones/${zone_id}/keyless_certificates/${keylessCertificateId}`, options)._thenUnwrap((obj) => obj.result);
  }
  edit(keylessCertificateId, params, options) {
    const { zone_id, ...body } = params;
    return this._client.patch(`/zones/${zone_id}/keyless_certificates/${keylessCertificateId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  get(keylessCertificateId, params, options) {
    const { zone_id } = params;
    return this._client.get(`/zones/${zone_id}/keyless_certificates/${keylessCertificateId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var KeylessCertificatesSinglePage = class extends SinglePage {
  static {
    __name(this, "KeylessCertificatesSinglePage");
  }
};
KeylessCertificates.KeylessCertificatesSinglePage = KeylessCertificatesSinglePage;
var Detections2 = class extends APIResource {
  static {
    __name(this, "Detections2");
  }
  create(params, options) {
    const { zone_id, ...body } = params;
    return this._client.post(`/zones/${zone_id}/leaked-credential-checks/detections`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  update(detectionId, params, options) {
    const { zone_id, ...body } = params;
    return this._client.put(`/zones/${zone_id}/leaked-credential-checks/detections/${detectionId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { zone_id } = params;
    return this._client.getAPIList(`/zones/${zone_id}/leaked-credential-checks/detections`, DetectionListResponsesSinglePage, options);
  }
  delete(detectionId, params, options) {
    const { zone_id } = params;
    return this._client.delete(`/zones/${zone_id}/leaked-credential-checks/detections/${detectionId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var DetectionListResponsesSinglePage = class extends SinglePage {
  static {
    __name(this, "DetectionListResponsesSinglePage");
  }
};
Detections2.DetectionListResponsesSinglePage = DetectionListResponsesSinglePage;
var LeakedCredentialChecks = class extends APIResource {
  static {
    __name(this, "LeakedCredentialChecks");
  }
  constructor() {
    super(...arguments);
    this.detections = new Detections2(this._client);
  }
  create(params, options) {
    const { zone_id, ...body } = params;
    return this._client.post(`/zones/${zone_id}/leaked-credential-checks`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  get(params, options) {
    const { zone_id } = params;
    return this._client.get(`/zones/${zone_id}/leaked-credential-checks`, options)._thenUnwrap((obj) => obj.result);
  }
};
LeakedCredentialChecks.Detections = Detections2;
LeakedCredentialChecks.DetectionListResponsesSinglePage = DetectionListResponsesSinglePage;
var MonitorGroups = class extends APIResource {
  static {
    __name(this, "MonitorGroups");
  }
  create(params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/load_balancers/monitor_groups`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  update(monitorGroupId, params, options) {
    const { account_id, ...body } = params;
    return this._client.put(`/accounts/${account_id}/load_balancers/monitor_groups/${monitorGroupId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { account_id } = params;
    return this._client.getAPIList(`/accounts/${account_id}/load_balancers/monitor_groups`, MonitorGroupsSinglePage, options);
  }
  delete(monitorGroupId, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/load_balancers/monitor_groups/${monitorGroupId}`, options)._thenUnwrap((obj) => obj.result);
  }
  edit(monitorGroupId, params, options) {
    const { account_id, ...body } = params;
    return this._client.patch(`/accounts/${account_id}/load_balancers/monitor_groups/${monitorGroupId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  get(monitorGroupId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/load_balancers/monitor_groups/${monitorGroupId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var MonitorGroupsSinglePage = class extends SinglePage {
  static {
    __name(this, "MonitorGroupsSinglePage");
  }
};
MonitorGroups.MonitorGroupsSinglePage = MonitorGroupsSinglePage;
var Previews2 = class extends APIResource {
  static {
    __name(this, "Previews2");
  }
  get(previewId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/load_balancers/preview/${previewId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var Regions2 = class extends APIResource {
  static {
    __name(this, "Regions2");
  }
  list(params, options) {
    const { account_id, ...query } = params;
    return this._client.get(`/accounts/${account_id}/load_balancers/regions`, {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  get(regionId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/load_balancers/regions/${regionId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var Searches = class extends APIResource {
  static {
    __name(this, "Searches");
  }
  list(params, options) {
    const { account_id, ...query } = params;
    return this._client.getAPIList(`/accounts/${account_id}/load_balancers/search`, SearchListResponsesV4PagePagination, { query, ...options });
  }
};
var SearchListResponsesV4PagePagination = class extends V4PagePagination {
  static {
    __name(this, "SearchListResponsesV4PagePagination");
  }
};
Searches.SearchListResponsesV4PagePagination = SearchListResponsesV4PagePagination;
var Previews3 = class extends APIResource {
  static {
    __name(this, "Previews3");
  }
  create(monitorId, params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/load_balancers/monitors/${monitorId}/preview`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
};
var References = class extends APIResource {
  static {
    __name(this, "References");
  }
  get(monitorId, params, options) {
    const { account_id } = params;
    return this._client.getAPIList(`/accounts/${account_id}/load_balancers/monitors/${monitorId}/references`, ReferenceGetResponsesSinglePage, options);
  }
};
var ReferenceGetResponsesSinglePage = class extends SinglePage {
  static {
    __name(this, "ReferenceGetResponsesSinglePage");
  }
};
References.ReferenceGetResponsesSinglePage = ReferenceGetResponsesSinglePage;
var Monitors = class extends APIResource {
  static {
    __name(this, "Monitors");
  }
  constructor() {
    super(...arguments);
    this.previews = new Previews3(this._client);
    this.references = new References(this._client);
  }
  create(params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/load_balancers/monitors`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  update(monitorId, params, options) {
    const { account_id, ...body } = params;
    return this._client.put(`/accounts/${account_id}/load_balancers/monitors/${monitorId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { account_id } = params;
    return this._client.getAPIList(`/accounts/${account_id}/load_balancers/monitors`, MonitorsSinglePage, options);
  }
  delete(monitorId, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/load_balancers/monitors/${monitorId}`, options)._thenUnwrap((obj) => obj.result);
  }
  edit(monitorId, params, options) {
    const { account_id, ...body } = params;
    return this._client.patch(`/accounts/${account_id}/load_balancers/monitors/${monitorId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  get(monitorId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/load_balancers/monitors/${monitorId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var MonitorsSinglePage = class extends SinglePage {
  static {
    __name(this, "MonitorsSinglePage");
  }
};
Monitors.MonitorsSinglePage = MonitorsSinglePage;
Monitors.Previews = Previews3;
Monitors.References = References;
Monitors.ReferenceGetResponsesSinglePage = ReferenceGetResponsesSinglePage;
var Health2 = class extends APIResource {
  static {
    __name(this, "Health2");
  }
  create(poolId, params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/load_balancers/pools/${poolId}/preview`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  get(poolId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/load_balancers/pools/${poolId}/health`, options)._thenUnwrap((obj) => obj.result);
  }
};
var References2 = class extends APIResource {
  static {
    __name(this, "References2");
  }
  get(poolId, params, options) {
    const { account_id } = params;
    return this._client.getAPIList(`/accounts/${account_id}/load_balancers/pools/${poolId}/references`, ReferenceGetResponsesSinglePage2, options);
  }
};
var ReferenceGetResponsesSinglePage2 = class extends SinglePage {
  static {
    __name(this, "ReferenceGetResponsesSinglePage2");
  }
};
References2.ReferenceGetResponsesSinglePage = ReferenceGetResponsesSinglePage2;
var Pools = class extends APIResource {
  static {
    __name(this, "Pools");
  }
  constructor() {
    super(...arguments);
    this.health = new Health2(this._client);
    this.references = new References2(this._client);
  }
  create(params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/load_balancers/pools`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  update(poolId, params, options) {
    const { account_id, ...body } = params;
    return this._client.put(`/accounts/${account_id}/load_balancers/pools/${poolId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { account_id, ...query } = params;
    return this._client.getAPIList(`/accounts/${account_id}/load_balancers/pools`, PoolsSinglePage, {
      query,
      ...options
    });
  }
  delete(poolId, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/load_balancers/pools/${poolId}`, options)._thenUnwrap((obj) => obj.result);
  }
  bulkEdit(params, options) {
    const { account_id, ...body } = params;
    return this._client.getAPIList(`/accounts/${account_id}/load_balancers/pools`, PoolsSinglePage, {
      body,
      method: "patch",
      ...options
    });
  }
  edit(poolId, params, options) {
    const { account_id, ...body } = params;
    return this._client.patch(`/accounts/${account_id}/load_balancers/pools/${poolId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  get(poolId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/load_balancers/pools/${poolId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var PoolsSinglePage = class extends SinglePage {
  static {
    __name(this, "PoolsSinglePage");
  }
};
Pools.PoolsSinglePage = PoolsSinglePage;
Pools.Health = Health2;
Pools.References = References2;
Pools.ReferenceGetResponsesSinglePage = ReferenceGetResponsesSinglePage2;
var LoadBalancers = class extends APIResource {
  static {
    __name(this, "LoadBalancers");
  }
  constructor() {
    super(...arguments);
    this.monitors = new Monitors(this._client);
    this.monitorGroups = new MonitorGroups(this._client);
    this.pools = new Pools(this._client);
    this.previews = new Previews2(this._client);
    this.regions = new Regions2(this._client);
    this.searches = new Searches(this._client);
  }
  create(params, options) {
    const { zone_id, ...body } = params;
    return this._client.post(`/zones/${zone_id}/load_balancers`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  update(loadBalancerId, params, options) {
    const { zone_id, ...body } = params;
    return this._client.put(`/zones/${zone_id}/load_balancers/${loadBalancerId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { zone_id } = params;
    return this._client.getAPIList(`/zones/${zone_id}/load_balancers`, LoadBalancersSinglePage, options);
  }
  delete(loadBalancerId, params, options) {
    const { zone_id } = params;
    return this._client.delete(`/zones/${zone_id}/load_balancers/${loadBalancerId}`, options)._thenUnwrap((obj) => obj.result);
  }
  edit(loadBalancerId, params, options) {
    const { zone_id, ...body } = params;
    return this._client.patch(`/zones/${zone_id}/load_balancers/${loadBalancerId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  get(loadBalancerId, params, options) {
    const { zone_id } = params;
    return this._client.get(`/zones/${zone_id}/load_balancers/${loadBalancerId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var LoadBalancersSinglePage = class extends SinglePage {
  static {
    __name(this, "LoadBalancersSinglePage");
  }
};
LoadBalancers.LoadBalancersSinglePage = LoadBalancersSinglePage;
LoadBalancers.Monitors = Monitors;
LoadBalancers.MonitorsSinglePage = MonitorsSinglePage;
LoadBalancers.MonitorGroups = MonitorGroups;
LoadBalancers.MonitorGroupsSinglePage = MonitorGroupsSinglePage;
LoadBalancers.Pools = Pools;
LoadBalancers.PoolsSinglePage = PoolsSinglePage;
LoadBalancers.Previews = Previews2;
LoadBalancers.Regions = Regions2;
LoadBalancers.Searches = Searches;
LoadBalancers.SearchListResponsesV4PagePagination = SearchListResponsesV4PagePagination;
var Edge = class extends APIResource {
  static {
    __name(this, "Edge");
  }
  create(params, options) {
    const { zone_id, ...body } = params;
    return this._client.post(`/zones/${zone_id}/logpush/edge/jobs`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  get(params, options) {
    const { zone_id } = params;
    return this._client.getAPIList(`/zones/${zone_id}/logpush/edge/jobs`, InstantLogpushJobsSinglePage, options);
  }
};
var InstantLogpushJobsSinglePage = class extends SinglePage {
  static {
    __name(this, "InstantLogpushJobsSinglePage");
  }
};
Edge.InstantLogpushJobsSinglePage = InstantLogpushJobsSinglePage;
var Jobs = class extends APIResource {
  static {
    __name(this, "Jobs");
  }
  create(params, options) {
    const { account_id, zone_id, ...body } = params;
    if (!account_id && !zone_id) {
      throw new CloudflareError("You must provide either account_id or zone_id.");
    }
    if (account_id && zone_id) {
      throw new CloudflareError("You cannot provide both account_id and zone_id.");
    }
    const { accountOrZone, accountOrZoneId } = account_id ? {
      accountOrZone: "accounts",
      accountOrZoneId: account_id
    } : {
      accountOrZone: "zones",
      accountOrZoneId: zone_id
    };
    return this._client.post(`/${accountOrZone}/${accountOrZoneId}/logpush/jobs`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  update(jobId, params, options) {
    const { account_id, zone_id, ...body } = params;
    if (!account_id && !zone_id) {
      throw new CloudflareError("You must provide either account_id or zone_id.");
    }
    if (account_id && zone_id) {
      throw new CloudflareError("You cannot provide both account_id and zone_id.");
    }
    const { accountOrZone, accountOrZoneId } = account_id ? {
      accountOrZone: "accounts",
      accountOrZoneId: account_id
    } : {
      accountOrZone: "zones",
      accountOrZoneId: zone_id
    };
    return this._client.put(`/${accountOrZone}/${accountOrZoneId}/logpush/jobs/${jobId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(params = {}, options) {
    if (isRequestOptions(params)) {
      return this.list({}, params);
    }
    const { account_id, zone_id } = params;
    if (!account_id && !zone_id) {
      throw new CloudflareError("You must provide either account_id or zone_id.");
    }
    if (account_id && zone_id) {
      throw new CloudflareError("You cannot provide both account_id and zone_id.");
    }
    const { accountOrZone, accountOrZoneId } = account_id ? {
      accountOrZone: "accounts",
      accountOrZoneId: account_id
    } : {
      accountOrZone: "zones",
      accountOrZoneId: zone_id
    };
    return this._client.getAPIList(`/${accountOrZone}/${accountOrZoneId}/logpush/jobs`, LogpushJobsSinglePage, options);
  }
  delete(jobId, params = {}, options) {
    if (isRequestOptions(params)) {
      return this.delete(jobId, {}, params);
    }
    const { account_id, zone_id } = params;
    if (!account_id && !zone_id) {
      throw new CloudflareError("You must provide either account_id or zone_id.");
    }
    if (account_id && zone_id) {
      throw new CloudflareError("You cannot provide both account_id and zone_id.");
    }
    const { accountOrZone, accountOrZoneId } = account_id ? {
      accountOrZone: "accounts",
      accountOrZoneId: account_id
    } : {
      accountOrZone: "zones",
      accountOrZoneId: zone_id
    };
    return this._client.delete(`/${accountOrZone}/${accountOrZoneId}/logpush/jobs/${jobId}`, options)._thenUnwrap((obj) => obj.result);
  }
  get(jobId, params = {}, options) {
    if (isRequestOptions(params)) {
      return this.get(jobId, {}, params);
    }
    const { account_id, zone_id } = params;
    if (!account_id && !zone_id) {
      throw new CloudflareError("You must provide either account_id or zone_id.");
    }
    if (account_id && zone_id) {
      throw new CloudflareError("You cannot provide both account_id and zone_id.");
    }
    const { accountOrZone, accountOrZoneId } = account_id ? {
      accountOrZone: "accounts",
      accountOrZoneId: account_id
    } : {
      accountOrZone: "zones",
      accountOrZoneId: zone_id
    };
    return this._client.get(`/${accountOrZone}/${accountOrZoneId}/logpush/jobs/${jobId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var LogpushJobsSinglePage = class extends SinglePage {
  static {
    __name(this, "LogpushJobsSinglePage");
  }
};
Jobs.LogpushJobsSinglePage = LogpushJobsSinglePage;
var Ownership = class extends APIResource {
  static {
    __name(this, "Ownership");
  }
  create(params, options) {
    const { account_id, zone_id, ...body } = params;
    if (!account_id && !zone_id) {
      throw new CloudflareError("You must provide either account_id or zone_id.");
    }
    if (account_id && zone_id) {
      throw new CloudflareError("You cannot provide both account_id and zone_id.");
    }
    const { accountOrZone, accountOrZoneId } = account_id ? {
      accountOrZone: "accounts",
      accountOrZoneId: account_id
    } : {
      accountOrZone: "zones",
      accountOrZoneId: zone_id
    };
    return this._client.post(`/${accountOrZone}/${accountOrZoneId}/logpush/ownership`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  validate(params, options) {
    const { account_id, zone_id, ...body } = params;
    if (!account_id && !zone_id) {
      throw new CloudflareError("You must provide either account_id or zone_id.");
    }
    if (account_id && zone_id) {
      throw new CloudflareError("You cannot provide both account_id and zone_id.");
    }
    const { accountOrZone, accountOrZoneId } = account_id ? {
      accountOrZone: "accounts",
      accountOrZoneId: account_id
    } : {
      accountOrZone: "zones",
      accountOrZoneId: zone_id
    };
    return this._client.post(`/${accountOrZone}/${accountOrZoneId}/logpush/ownership/validate`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
};
var Validate = class extends APIResource {
  static {
    __name(this, "Validate");
  }
  destination(params, options) {
    const { account_id, zone_id, ...body } = params;
    if (!account_id && !zone_id) {
      throw new CloudflareError("You must provide either account_id or zone_id.");
    }
    if (account_id && zone_id) {
      throw new CloudflareError("You cannot provide both account_id and zone_id.");
    }
    const { accountOrZone, accountOrZoneId } = account_id ? {
      accountOrZone: "accounts",
      accountOrZoneId: account_id
    } : {
      accountOrZone: "zones",
      accountOrZoneId: zone_id
    };
    return this._client.post(`/${accountOrZone}/${accountOrZoneId}/logpush/validate/destination`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  destinationExists(params, options) {
    const { account_id, zone_id, ...body } = params;
    if (!account_id && !zone_id) {
      throw new CloudflareError("You must provide either account_id or zone_id.");
    }
    if (account_id && zone_id) {
      throw new CloudflareError("You cannot provide both account_id and zone_id.");
    }
    const { accountOrZone, accountOrZoneId } = account_id ? {
      accountOrZone: "accounts",
      accountOrZoneId: account_id
    } : {
      accountOrZone: "zones",
      accountOrZoneId: zone_id
    };
    return this._client.post(`/${accountOrZone}/${accountOrZoneId}/logpush/validate/destination/exists`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  origin(params, options) {
    const { account_id, zone_id, ...body } = params;
    if (!account_id && !zone_id) {
      throw new CloudflareError("You must provide either account_id or zone_id.");
    }
    if (account_id && zone_id) {
      throw new CloudflareError("You cannot provide both account_id and zone_id.");
    }
    const { accountOrZone, accountOrZoneId } = account_id ? {
      accountOrZone: "accounts",
      accountOrZoneId: account_id
    } : {
      accountOrZone: "zones",
      accountOrZoneId: zone_id
    };
    return this._client.post(`/${accountOrZone}/${accountOrZoneId}/logpush/validate/origin`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
};
var Fields = class extends APIResource {
  static {
    __name(this, "Fields");
  }
  get(datasetId, params = {}, options) {
    if (isRequestOptions(params)) {
      return this.get(datasetId, {}, params);
    }
    const { account_id, zone_id } = params;
    if (!account_id && !zone_id) {
      throw new CloudflareError("You must provide either account_id or zone_id.");
    }
    if (account_id && zone_id) {
      throw new CloudflareError("You cannot provide both account_id and zone_id.");
    }
    const { accountOrZone, accountOrZoneId } = account_id ? {
      accountOrZone: "accounts",
      accountOrZoneId: account_id
    } : {
      accountOrZone: "zones",
      accountOrZoneId: zone_id
    };
    return this._client.get(`/${accountOrZone}/${accountOrZoneId}/logpush/datasets/${datasetId}/fields`, options)._thenUnwrap((obj) => obj.result);
  }
};
var Jobs2 = class extends APIResource {
  static {
    __name(this, "Jobs2");
  }
  get(datasetId, params = {}, options) {
    if (isRequestOptions(params)) {
      return this.get(datasetId, {}, params);
    }
    const { account_id, zone_id } = params;
    if (!account_id && !zone_id) {
      throw new CloudflareError("You must provide either account_id or zone_id.");
    }
    if (account_id && zone_id) {
      throw new CloudflareError("You cannot provide both account_id and zone_id.");
    }
    const { accountOrZone, accountOrZoneId } = account_id ? {
      accountOrZone: "accounts",
      accountOrZoneId: account_id
    } : {
      accountOrZone: "zones",
      accountOrZoneId: zone_id
    };
    return this._client.getAPIList(`/${accountOrZone}/${accountOrZoneId}/logpush/datasets/${datasetId}/jobs`, LogpushJobsSinglePage, options);
  }
};
var Datasets3 = class extends APIResource {
  static {
    __name(this, "Datasets3");
  }
  constructor() {
    super(...arguments);
    this.fields = new Fields(this._client);
    this.jobs = new Jobs2(this._client);
  }
};
Datasets3.Fields = Fields;
Datasets3.Jobs = Jobs2;
var Logpush = class extends APIResource {
  static {
    __name(this, "Logpush");
  }
  constructor() {
    super(...arguments);
    this.datasets = new Datasets3(this._client);
    this.edge = new Edge(this._client);
    this.jobs = new Jobs(this._client);
    this.ownership = new Ownership(this._client);
    this.validate = new Validate(this._client);
  }
};
Logpush.Datasets = Datasets3;
Logpush.Edge = Edge;
Logpush.InstantLogpushJobsSinglePage = InstantLogpushJobsSinglePage;
Logpush.Jobs = Jobs;
Logpush.LogpushJobsSinglePage = LogpushJobsSinglePage;
Logpush.Ownership = Ownership;
Logpush.Validate = Validate;
var RayID = class extends APIResource {
  static {
    __name(this, "RayID");
  }
  get(RayID2, params, options) {
    const { zone_id, ...query } = params;
    return this._client.get(`/zones/${zone_id}/logs/rayids/${RayID2}`, { query, ...options });
  }
};
var Retention = class extends APIResource {
  static {
    __name(this, "Retention");
  }
  create(params, options) {
    const { zone_id, ...body } = params;
    return this._client.post(`/zones/${zone_id}/logs/control/retention/flag`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  get(params, options) {
    const { zone_id } = params;
    return this._client.get(`/zones/${zone_id}/logs/control/retention/flag`, options)._thenUnwrap((obj) => obj.result);
  }
};
var Config2 = class extends APIResource {
  static {
    __name(this, "Config2");
  }
  create(params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/logs/control/cmb/config`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  delete(params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/logs/control/cmb/config`, options)._thenUnwrap((obj) => obj.result);
  }
  get(params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/logs/control/cmb/config`, options)._thenUnwrap((obj) => obj.result);
  }
};
var Cmb = class extends APIResource {
  static {
    __name(this, "Cmb");
  }
  constructor() {
    super(...arguments);
    this.config = new Config2(this._client);
  }
};
Cmb.Config = Config2;
var Control = class extends APIResource {
  static {
    __name(this, "Control");
  }
  constructor() {
    super(...arguments);
    this.retention = new Retention(this._client);
    this.cmb = new Cmb(this._client);
  }
};
Control.Retention = Retention;
Control.Cmb = Cmb;
var Fields2 = class extends APIResource {
  static {
    __name(this, "Fields2");
  }
  get(params, options) {
    const { zone_id } = params;
    return this._client.get(`/zones/${zone_id}/logs/received/fields`, options);
  }
};
var Received = class extends APIResource {
  static {
    __name(this, "Received");
  }
  constructor() {
    super(...arguments);
    this.fields = new Fields2(this._client);
  }
  get(params, options) {
    const { zone_id, ...query } = params;
    return this._client.get(`/zones/${zone_id}/logs/received`, { query, ...options });
  }
};
Received.Fields = Fields2;
var Logs3 = class extends APIResource {
  static {
    __name(this, "Logs3");
  }
  constructor() {
    super(...arguments);
    this.control = new Control(this._client);
    this.RayID = new RayID(this._client);
    this.received = new Received(this._client);
  }
};
Logs3.Control = Control;
Logs3.RayID = RayID;
Logs3.Received = Received;
var Associations = class extends APIResource {
  static {
    __name(this, "Associations");
  }
  get(mtlsCertificateId, params, options) {
    const { account_id } = params;
    return this._client.getAPIList(`/accounts/${account_id}/mtls_certificates/${mtlsCertificateId}/associations`, CertificateAsssociationsSinglePage, options);
  }
};
var CertificateAsssociationsSinglePage = class extends SinglePage {
  static {
    __name(this, "CertificateAsssociationsSinglePage");
  }
};
Associations.CertificateAsssociationsSinglePage = CertificateAsssociationsSinglePage;
var MTLSCertificates = class extends APIResource {
  static {
    __name(this, "MTLSCertificates");
  }
  constructor() {
    super(...arguments);
    this.associations = new Associations(this._client);
  }
  create(params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/mtls_certificates`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { account_id } = params;
    return this._client.getAPIList(`/accounts/${account_id}/mtls_certificates`, MTLSCertificatesSinglePage, options);
  }
  delete(mtlsCertificateId, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/mtls_certificates/${mtlsCertificateId}`, options)._thenUnwrap((obj) => obj.result);
  }
  get(mtlsCertificateId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/mtls_certificates/${mtlsCertificateId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var MTLSCertificatesSinglePage = class extends SinglePage {
  static {
    __name(this, "MTLSCertificatesSinglePage");
  }
};
MTLSCertificates.MTLSCertificatesSinglePage = MTLSCertificatesSinglePage;
MTLSCertificates.Associations = Associations;
MTLSCertificates.CertificateAsssociationsSinglePage = CertificateAsssociationsSinglePage;
var CloudIntegrations = class extends APIResource {
  static {
    __name(this, "CloudIntegrations");
  }
  create(params, options) {
    const { account_id, forwarded, ...body } = params;
    return this._client.post(`/accounts/${account_id}/magic/cloud/providers`, {
      body,
      ...options,
      headers: { ...forwarded != null ? { forwarded } : void 0, ...options?.headers }
    })._thenUnwrap((obj) => obj.result);
  }
  update(providerId, params, options) {
    const { account_id, ...body } = params;
    return this._client.put(`/accounts/${account_id}/magic/cloud/providers/${providerId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { account_id, ...query } = params;
    return this._client.getAPIList(`/accounts/${account_id}/magic/cloud/providers`, CloudIntegrationListResponsesSinglePage, { query, ...options });
  }
  delete(providerId, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/magic/cloud/providers/${providerId}`, options)._thenUnwrap((obj) => obj.result);
  }
  discover(providerId, params, options) {
    const { account_id, v2 } = params;
    return this._client.post(`/accounts/${account_id}/magic/cloud/providers/${providerId}/discover`, {
      query: { v2 },
      ...options
    });
  }
  discoverAll(params, options) {
    const { account_id } = params;
    return this._client.post(`/accounts/${account_id}/magic/cloud/providers/discover`, options);
  }
  edit(providerId, params, options) {
    const { account_id, ...body } = params;
    return this._client.patch(`/accounts/${account_id}/magic/cloud/providers/${providerId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  get(providerId, params, options) {
    const { account_id, ...query } = params;
    return this._client.get(`/accounts/${account_id}/magic/cloud/providers/${providerId}`, {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  initialSetup(providerId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/magic/cloud/providers/${providerId}/initial_setup`, options)._thenUnwrap((obj) => obj.result);
  }
};
var CloudIntegrationListResponsesSinglePage = class extends SinglePage {
  static {
    __name(this, "CloudIntegrationListResponsesSinglePage");
  }
};
CloudIntegrations.CloudIntegrationListResponsesSinglePage = CloudIntegrationListResponsesSinglePage;
var Resources = class extends APIResource {
  static {
    __name(this, "Resources");
  }
  list(params, options) {
    const { account_id, ...query } = params;
    return this._client.getAPIList(`/accounts/${account_id}/magic/cloud/resources`, ResourceListResponsesV4PagePaginationArray, { query, ...options });
  }
  export(params, options) {
    const { account_id, ...query } = params;
    return this._client.get(`/accounts/${account_id}/magic/cloud/resources/export`, {
      query,
      ...options,
      headers: { Accept: "application/octet-stream", ...options?.headers },
      __binaryResponse: true
    });
  }
  get(resourceId, params, options) {
    const { account_id, ...query } = params;
    return this._client.get(`/accounts/${account_id}/magic/cloud/resources/${resourceId}`, {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  policyPreview(params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/magic/cloud/resources/policy-preview`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
};
var ResourceListResponsesV4PagePaginationArray = class extends V4PagePaginationArray {
  static {
    __name(this, "ResourceListResponsesV4PagePaginationArray");
  }
};
Resources.ResourceListResponsesV4PagePaginationArray = ResourceListResponsesV4PagePaginationArray;
var PrebuiltPolicies = class extends APIResource {
  static {
    __name(this, "PrebuiltPolicies");
  }
  list(params, options) {
    const { account_id, ...query } = params;
    return this._client.getAPIList(`/accounts/${account_id}/magic/cloud/catalog-syncs/prebuilt-policies`, PrebuiltPolicyListResponsesSinglePage, { query, ...options });
  }
};
var PrebuiltPolicyListResponsesSinglePage = class extends SinglePage {
  static {
    __name(this, "PrebuiltPolicyListResponsesSinglePage");
  }
};
PrebuiltPolicies.PrebuiltPolicyListResponsesSinglePage = PrebuiltPolicyListResponsesSinglePage;
var CatalogSyncs = class extends APIResource {
  static {
    __name(this, "CatalogSyncs");
  }
  constructor() {
    super(...arguments);
    this.prebuiltPolicies = new PrebuiltPolicies(this._client);
  }
  create(params, options) {
    const { account_id, forwarded, ...body } = params;
    return this._client.post(`/accounts/${account_id}/magic/cloud/catalog-syncs`, {
      body,
      ...options,
      headers: { ...forwarded != null ? { forwarded } : void 0, ...options?.headers }
    })._thenUnwrap((obj) => obj.result);
  }
  update(syncId, params, options) {
    const { account_id, ...body } = params;
    return this._client.put(`/accounts/${account_id}/magic/cloud/catalog-syncs/${syncId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { account_id } = params;
    return this._client.getAPIList(`/accounts/${account_id}/magic/cloud/catalog-syncs`, CatalogSyncListResponsesSinglePage, options);
  }
  delete(syncId, params, options) {
    const { account_id, delete_destination } = params;
    return this._client.delete(`/accounts/${account_id}/magic/cloud/catalog-syncs/${syncId}`, {
      query: { delete_destination },
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  edit(syncId, params, options) {
    const { account_id, ...body } = params;
    return this._client.patch(`/accounts/${account_id}/magic/cloud/catalog-syncs/${syncId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  get(syncId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/magic/cloud/catalog-syncs/${syncId}`, options)._thenUnwrap((obj) => obj.result);
  }
  refresh(syncId, params, options) {
    const { account_id } = params;
    return this._client.post(`/accounts/${account_id}/magic/cloud/catalog-syncs/${syncId}/refresh`, options)._thenUnwrap((obj) => obj.result);
  }
};
var CatalogSyncListResponsesSinglePage = class extends SinglePage {
  static {
    __name(this, "CatalogSyncListResponsesSinglePage");
  }
};
CatalogSyncs.CatalogSyncListResponsesSinglePage = CatalogSyncListResponsesSinglePage;
CatalogSyncs.PrebuiltPolicies = PrebuiltPolicies;
CatalogSyncs.PrebuiltPolicyListResponsesSinglePage = PrebuiltPolicyListResponsesSinglePage;
var AddressSpaces = class extends APIResource {
  static {
    __name(this, "AddressSpaces");
  }
  update(params, options) {
    const { account_id, ...body } = params;
    return this._client.put(`/accounts/${account_id}/magic/cloud/onramps/magic_wan_address_space`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/magic/cloud/onramps/magic_wan_address_space`, options)._thenUnwrap((obj) => obj.result);
  }
  edit(params, options) {
    const { account_id, ...body } = params;
    return this._client.patch(`/accounts/${account_id}/magic/cloud/onramps/magic_wan_address_space`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
};
var OnRamps = class extends APIResource {
  static {
    __name(this, "OnRamps");
  }
  constructor() {
    super(...arguments);
    this.addressSpaces = new AddressSpaces(this._client);
  }
  create(params, options) {
    const { account_id, forwarded, ...body } = params;
    return this._client.post(`/accounts/${account_id}/magic/cloud/onramps`, {
      body,
      ...options,
      headers: { ...forwarded != null ? { forwarded } : void 0, ...options?.headers }
    })._thenUnwrap((obj) => obj.result);
  }
  update(onrampId, params, options) {
    const { account_id, ...body } = params;
    return this._client.put(`/accounts/${account_id}/magic/cloud/onramps/${onrampId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { account_id, ...query } = params;
    return this._client.getAPIList(`/accounts/${account_id}/magic/cloud/onramps`, OnRampListResponsesSinglePage, { query, ...options });
  }
  delete(onrampId, params, options) {
    const { account_id, destroy, force } = params;
    return this._client.delete(`/accounts/${account_id}/magic/cloud/onramps/${onrampId}`, {
      query: { destroy, force },
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  apply(onrampId, params, options) {
    const { account_id } = params;
    return this._client.post(`/accounts/${account_id}/magic/cloud/onramps/${onrampId}/apply`, options);
  }
  edit(onrampId, params, options) {
    const { account_id, ...body } = params;
    return this._client.patch(`/accounts/${account_id}/magic/cloud/onramps/${onrampId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  export(onrampId, params, options) {
    const { account_id } = params;
    return this._client.post(`/accounts/${account_id}/magic/cloud/onramps/${onrampId}/export`, {
      ...options,
      headers: { Accept: "application/zip", ...options?.headers },
      __binaryResponse: true
    });
  }
  get(onrampId, params, options) {
    const { account_id, ...query } = params;
    return this._client.get(`/accounts/${account_id}/magic/cloud/onramps/${onrampId}`, {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  plan(onrampId, params, options) {
    const { account_id } = params;
    return this._client.post(`/accounts/${account_id}/magic/cloud/onramps/${onrampId}/plan`, options);
  }
};
var OnRampListResponsesSinglePage = class extends SinglePage {
  static {
    __name(this, "OnRampListResponsesSinglePage");
  }
};
OnRamps.OnRampListResponsesSinglePage = OnRampListResponsesSinglePage;
OnRamps.AddressSpaces = AddressSpaces;
var MagicCloudNetworking = class extends APIResource {
  static {
    __name(this, "MagicCloudNetworking");
  }
  constructor() {
    super(...arguments);
    this.catalogSyncs = new CatalogSyncs(this._client);
    this.onRamps = new OnRamps(this._client);
    this.cloudIntegrations = new CloudIntegrations(this._client);
    this.resources = new Resources(this._client);
  }
};
MagicCloudNetworking.CatalogSyncs = CatalogSyncs;
MagicCloudNetworking.CatalogSyncListResponsesSinglePage = CatalogSyncListResponsesSinglePage;
MagicCloudNetworking.OnRamps = OnRamps;
MagicCloudNetworking.OnRampListResponsesSinglePage = OnRampListResponsesSinglePage;
MagicCloudNetworking.CloudIntegrations = CloudIntegrations;
MagicCloudNetworking.CloudIntegrationListResponsesSinglePage = CloudIntegrationListResponsesSinglePage;
MagicCloudNetworking.Resources = Resources;
MagicCloudNetworking.ResourceListResponsesV4PagePaginationArray = ResourceListResponsesV4PagePaginationArray;
var Full = class extends APIResource {
  static {
    __name(this, "Full");
  }
  get(params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/mnm/config/full`, options)._thenUnwrap((obj) => obj.result);
  }
};
var Configs3 = class extends APIResource {
  static {
    __name(this, "Configs3");
  }
  constructor() {
    super(...arguments);
    this.full = new Full(this._client);
  }
  create(params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/mnm/config`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  update(params, options) {
    const { account_id, ...body } = params;
    return this._client.put(`/accounts/${account_id}/mnm/config`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  delete(params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/mnm/config`, options)._thenUnwrap((obj) => obj.result);
  }
  edit(params, options) {
    const { account_id, ...body } = params;
    return this._client.patch(`/accounts/${account_id}/mnm/config`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  get(params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/mnm/config`, options)._thenUnwrap((obj) => obj.result);
  }
};
Configs3.Full = Full;
var Advertisements = class extends APIResource {
  static {
    __name(this, "Advertisements");
  }
  edit(ruleId, params, options) {
    const { account_id, body } = params;
    return this._client.patch(`/accounts/${account_id}/mnm/rules/${ruleId}/advertisement`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
};
var Rules5 = class extends APIResource {
  static {
    __name(this, "Rules5");
  }
  constructor() {
    super(...arguments);
    this.advertisements = new Advertisements(this._client);
  }
  create(params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/mnm/rules`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  update(params, options) {
    const { account_id, ...body } = params;
    return this._client.put(`/accounts/${account_id}/mnm/rules`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { account_id } = params;
    return this._client.getAPIList(`/accounts/${account_id}/mnm/rules`, MagicNetworkMonitoringRulesSinglePage, options);
  }
  delete(ruleId, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/mnm/rules/${ruleId}`, options)._thenUnwrap((obj) => obj.result);
  }
  edit(ruleId, params, options) {
    const { account_id, ...body } = params;
    return this._client.patch(`/accounts/${account_id}/mnm/rules/${ruleId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  get(ruleId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/mnm/rules/${ruleId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var MagicNetworkMonitoringRulesSinglePage = class extends SinglePage {
  static {
    __name(this, "MagicNetworkMonitoringRulesSinglePage");
  }
};
Rules5.MagicNetworkMonitoringRulesSinglePage = MagicNetworkMonitoringRulesSinglePage;
Rules5.Advertisements = Advertisements;
var Tokens2 = class extends APIResource {
  static {
    __name(this, "Tokens2");
  }
  create(params, options) {
    const { account_id } = params;
    return this._client.post(`/accounts/${account_id}/mnm/vpc-flows/token`, options)._thenUnwrap((obj) => obj.result);
  }
};
var VPCFlows = class extends APIResource {
  static {
    __name(this, "VPCFlows");
  }
  constructor() {
    super(...arguments);
    this.tokens = new Tokens2(this._client);
  }
};
VPCFlows.Tokens = Tokens2;
var MagicNetworkMonitoring = class extends APIResource {
  static {
    __name(this, "MagicNetworkMonitoring");
  }
  constructor() {
    super(...arguments);
    this.vpcFlows = new VPCFlows(this._client);
    this.configs = new Configs3(this._client);
    this.rules = new Rules5(this._client);
  }
};
MagicNetworkMonitoring.VPCFlows = VPCFlows;
MagicNetworkMonitoring.Configs = Configs3;
MagicNetworkMonitoring.Rules = Rules5;
MagicNetworkMonitoring.MagicNetworkMonitoringRulesSinglePage = MagicNetworkMonitoringRulesSinglePage;
var Apps = class extends APIResource {
  static {
    __name(this, "Apps");
  }
  create(params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/magic/apps`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  update(accountAppId, params, options) {
    const { account_id, ...body } = params;
    return this._client.put(`/accounts/${account_id}/magic/apps/${accountAppId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { account_id } = params;
    return this._client.getAPIList(`/accounts/${account_id}/magic/apps`, AppListResponsesSinglePage, options);
  }
  delete(accountAppId, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/magic/apps/${accountAppId}`, options)._thenUnwrap((obj) => obj.result);
  }
  edit(accountAppId, params, options) {
    const { account_id, ...body } = params;
    return this._client.patch(`/accounts/${account_id}/magic/apps/${accountAppId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
};
var AppListResponsesSinglePage = class extends SinglePage {
  static {
    __name(this, "AppListResponsesSinglePage");
  }
};
Apps.AppListResponsesSinglePage = AppListResponsesSinglePage;
var CfInterconnects = class extends APIResource {
  static {
    __name(this, "CfInterconnects");
  }
  update(cfInterconnectId, params, options) {
    const { account_id, "x-magic-new-hc-target": xMagicNewHcTarget, ...body } = params;
    return this._client.put(`/accounts/${account_id}/magic/cf_interconnects/${cfInterconnectId}`, {
      body,
      ...options,
      headers: {
        ...xMagicNewHcTarget?.toString() != null ? { "x-magic-new-hc-target": xMagicNewHcTarget?.toString() } : void 0,
        ...options?.headers
      }
    })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { account_id, "x-magic-new-hc-target": xMagicNewHcTarget } = params;
    return this._client.get(`/accounts/${account_id}/magic/cf_interconnects`, {
      ...options,
      headers: {
        ...xMagicNewHcTarget?.toString() != null ? { "x-magic-new-hc-target": xMagicNewHcTarget?.toString() } : void 0,
        ...options?.headers
      }
    })._thenUnwrap((obj) => obj.result);
  }
  bulkUpdate(params, options) {
    const { account_id, body, "x-magic-new-hc-target": xMagicNewHcTarget } = params;
    return this._client.put(`/accounts/${account_id}/magic/cf_interconnects`, {
      body,
      ...options,
      headers: {
        ...xMagicNewHcTarget?.toString() != null ? { "x-magic-new-hc-target": xMagicNewHcTarget?.toString() } : void 0,
        ...options?.headers
      }
    })._thenUnwrap((obj) => obj.result);
  }
  get(cfInterconnectId, params, options) {
    const { account_id, "x-magic-new-hc-target": xMagicNewHcTarget } = params;
    return this._client.get(`/accounts/${account_id}/magic/cf_interconnects/${cfInterconnectId}`, {
      ...options,
      headers: {
        ...xMagicNewHcTarget?.toString() != null ? { "x-magic-new-hc-target": xMagicNewHcTarget?.toString() } : void 0,
        ...options?.headers
      }
    })._thenUnwrap((obj) => obj.result);
  }
};
var GRETunnels = class extends APIResource {
  static {
    __name(this, "GRETunnels");
  }
  create(params, options) {
    const { account_id, "x-magic-new-hc-target": xMagicNewHcTarget, ...body } = params;
    return this._client.post(`/accounts/${account_id}/magic/gre_tunnels`, {
      body,
      ...options,
      headers: {
        ...xMagicNewHcTarget?.toString() != null ? { "x-magic-new-hc-target": xMagicNewHcTarget?.toString() } : void 0,
        ...options?.headers
      }
    })._thenUnwrap((obj) => obj.result);
  }
  update(greTunnelId, params, options) {
    const { account_id, "x-magic-new-hc-target": xMagicNewHcTarget, ...body } = params;
    return this._client.put(`/accounts/${account_id}/magic/gre_tunnels/${greTunnelId}`, {
      body,
      ...options,
      headers: {
        ...xMagicNewHcTarget?.toString() != null ? { "x-magic-new-hc-target": xMagicNewHcTarget?.toString() } : void 0,
        ...options?.headers
      }
    })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { account_id, "x-magic-new-hc-target": xMagicNewHcTarget } = params;
    return this._client.get(`/accounts/${account_id}/magic/gre_tunnels`, {
      ...options,
      headers: {
        ...xMagicNewHcTarget?.toString() != null ? { "x-magic-new-hc-target": xMagicNewHcTarget?.toString() } : void 0,
        ...options?.headers
      }
    })._thenUnwrap((obj) => obj.result);
  }
  delete(greTunnelId, params, options) {
    const { account_id, "x-magic-new-hc-target": xMagicNewHcTarget } = params;
    return this._client.delete(`/accounts/${account_id}/magic/gre_tunnels/${greTunnelId}`, {
      ...options,
      headers: {
        ...xMagicNewHcTarget?.toString() != null ? { "x-magic-new-hc-target": xMagicNewHcTarget?.toString() } : void 0,
        ...options?.headers
      }
    })._thenUnwrap((obj) => obj.result);
  }
  bulkUpdate(params, options) {
    const { account_id, body, "x-magic-new-hc-target": xMagicNewHcTarget } = params;
    return this._client.put(`/accounts/${account_id}/magic/gre_tunnels`, {
      body,
      ...options,
      headers: {
        ...xMagicNewHcTarget?.toString() != null ? { "x-magic-new-hc-target": xMagicNewHcTarget?.toString() } : void 0,
        ...options?.headers
      }
    })._thenUnwrap((obj) => obj.result);
  }
  get(greTunnelId, params, options) {
    const { account_id, "x-magic-new-hc-target": xMagicNewHcTarget } = params;
    return this._client.get(`/accounts/${account_id}/magic/gre_tunnels/${greTunnelId}`, {
      ...options,
      headers: {
        ...xMagicNewHcTarget?.toString() != null ? { "x-magic-new-hc-target": xMagicNewHcTarget?.toString() } : void 0,
        ...options?.headers
      }
    })._thenUnwrap((obj) => obj.result);
  }
};
var IPSECTunnels = class extends APIResource {
  static {
    __name(this, "IPSECTunnels");
  }
  create(params, options) {
    const { account_id, "x-magic-new-hc-target": xMagicNewHcTarget, ...body } = params;
    return this._client.post(`/accounts/${account_id}/magic/ipsec_tunnels`, {
      body,
      ...options,
      headers: {
        ...xMagicNewHcTarget?.toString() != null ? { "x-magic-new-hc-target": xMagicNewHcTarget?.toString() } : void 0,
        ...options?.headers
      }
    })._thenUnwrap((obj) => obj.result);
  }
  update(ipsecTunnelId, params, options) {
    const { account_id, "x-magic-new-hc-target": xMagicNewHcTarget, ...body } = params;
    return this._client.put(`/accounts/${account_id}/magic/ipsec_tunnels/${ipsecTunnelId}`, {
      body,
      ...options,
      headers: {
        ...xMagicNewHcTarget?.toString() != null ? { "x-magic-new-hc-target": xMagicNewHcTarget?.toString() } : void 0,
        ...options?.headers
      }
    })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { account_id, "x-magic-new-hc-target": xMagicNewHcTarget } = params;
    return this._client.get(`/accounts/${account_id}/magic/ipsec_tunnels`, {
      ...options,
      headers: {
        ...xMagicNewHcTarget?.toString() != null ? { "x-magic-new-hc-target": xMagicNewHcTarget?.toString() } : void 0,
        ...options?.headers
      }
    })._thenUnwrap((obj) => obj.result);
  }
  delete(ipsecTunnelId, params, options) {
    const { account_id, "x-magic-new-hc-target": xMagicNewHcTarget } = params;
    return this._client.delete(`/accounts/${account_id}/magic/ipsec_tunnels/${ipsecTunnelId}`, {
      ...options,
      headers: {
        ...xMagicNewHcTarget?.toString() != null ? { "x-magic-new-hc-target": xMagicNewHcTarget?.toString() } : void 0,
        ...options?.headers
      }
    })._thenUnwrap((obj) => obj.result);
  }
  bulkUpdate(params, options) {
    const { account_id, body, "x-magic-new-hc-target": xMagicNewHcTarget } = params;
    return this._client.put(`/accounts/${account_id}/magic/ipsec_tunnels`, {
      body,
      ...options,
      headers: {
        ...xMagicNewHcTarget?.toString() != null ? { "x-magic-new-hc-target": xMagicNewHcTarget?.toString() } : void 0,
        ...options?.headers
      }
    })._thenUnwrap((obj) => obj.result);
  }
  get(ipsecTunnelId, params, options) {
    const { account_id, "x-magic-new-hc-target": xMagicNewHcTarget } = params;
    return this._client.get(`/accounts/${account_id}/magic/ipsec_tunnels/${ipsecTunnelId}`, {
      ...options,
      headers: {
        ...xMagicNewHcTarget?.toString() != null ? { "x-magic-new-hc-target": xMagicNewHcTarget?.toString() } : void 0,
        ...options?.headers
      }
    })._thenUnwrap((obj) => obj.result);
  }
  pskGenerate(ipsecTunnelId, params, options) {
    const { account_id, body } = params;
    return this._client.post(`/accounts/${account_id}/magic/ipsec_tunnels/${ipsecTunnelId}/psk_generate`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
};
var Routes = class extends APIResource {
  static {
    __name(this, "Routes");
  }
  create(params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/magic/routes`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  update(routeId, params, options) {
    const { account_id, ...body } = params;
    return this._client.put(`/accounts/${account_id}/magic/routes/${routeId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/magic/routes`, options)._thenUnwrap((obj) => obj.result);
  }
  delete(routeId, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/magic/routes/${routeId}`, options)._thenUnwrap((obj) => obj.result);
  }
  bulkUpdate(params, options) {
    const { account_id, ...body } = params;
    return this._client.put(`/accounts/${account_id}/magic/routes`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  empty(params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/magic/routes`, options)._thenUnwrap((obj) => obj.result);
  }
  get(routeId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/magic/routes/${routeId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var Latest = class extends APIResource {
  static {
    __name(this, "Latest");
  }
  list(connectorId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/magic/connectors/${connectorId}/telemetry/events/latest`, options)._thenUnwrap((obj) => obj.result);
  }
};
var Events = class extends APIResource {
  static {
    __name(this, "Events");
  }
  constructor() {
    super(...arguments);
    this.latest = new Latest(this._client);
  }
  list(connectorId, params, options) {
    const { account_id, ...query } = params;
    return this._client.get(`/accounts/${account_id}/magic/connectors/${connectorId}/telemetry/events`, {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  get(connectorId, eventT, eventN, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/magic/connectors/${connectorId}/telemetry/events/${eventT}.${eventN}`, options)._thenUnwrap((obj) => obj.result);
  }
};
Events.Latest = Latest;
var Latest2 = class extends APIResource {
  static {
    __name(this, "Latest2");
  }
  list(connectorId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/magic/connectors/${connectorId}/telemetry/snapshots/latest`, options)._thenUnwrap((obj) => obj.result);
  }
};
var Snapshots2 = class extends APIResource {
  static {
    __name(this, "Snapshots2");
  }
  constructor() {
    super(...arguments);
    this.latest = new Latest2(this._client);
  }
  list(connectorId, params, options) {
    const { account_id, ...query } = params;
    return this._client.get(`/accounts/${account_id}/magic/connectors/${connectorId}/telemetry/snapshots`, {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  get(connectorId, snapshotT, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/magic/connectors/${connectorId}/telemetry/snapshots/${snapshotT}`, options)._thenUnwrap((obj) => obj.result);
  }
};
Snapshots2.Latest = Latest2;
var Connectors = class extends APIResource {
  static {
    __name(this, "Connectors");
  }
  constructor() {
    super(...arguments);
    this.events = new Events(this._client);
    this.snapshots = new Snapshots2(this._client);
  }
  create(params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/magic/connectors`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  update(connectorId, params, options) {
    const { account_id, ...body } = params;
    return this._client.put(`/accounts/${account_id}/magic/connectors/${connectorId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { account_id } = params;
    return this._client.getAPIList(`/accounts/${account_id}/magic/connectors`, ConnectorListResponsesSinglePage, options);
  }
  delete(connectorId, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/magic/connectors/${connectorId}`, options)._thenUnwrap((obj) => obj.result);
  }
  edit(connectorId, params, options) {
    const { account_id, ...body } = params;
    return this._client.patch(`/accounts/${account_id}/magic/connectors/${connectorId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  get(connectorId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/magic/connectors/${connectorId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var ConnectorListResponsesSinglePage = class extends SinglePage {
  static {
    __name(this, "ConnectorListResponsesSinglePage");
  }
};
Connectors.ConnectorListResponsesSinglePage = ConnectorListResponsesSinglePage;
Connectors.Events = Events;
Connectors.Snapshots = Snapshots2;
var Download = class extends APIResource {
  static {
    __name(this, "Download");
  }
  get(pcapId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/pcaps/${pcapId}/download`, {
      ...options,
      headers: { Accept: "application/vnd.tcpdump.pcap", ...options?.headers },
      __binaryResponse: true
    });
  }
};
var OwnershipResource = class extends APIResource {
  static {
    __name(this, "OwnershipResource");
  }
  create(params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/pcaps/ownership`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  delete(ownershipId, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/pcaps/ownership/${ownershipId}`, {
      ...options,
      headers: { Accept: "*/*", ...options?.headers }
    });
  }
  get(params, options) {
    const { account_id } = params;
    return this._client.getAPIList(`/accounts/${account_id}/pcaps/ownership`, OwnershipsSinglePage, options);
  }
  validate(params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/pcaps/ownership/validate`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
};
var OwnershipsSinglePage = class extends SinglePage {
  static {
    __name(this, "OwnershipsSinglePage");
  }
};
OwnershipResource.OwnershipsSinglePage = OwnershipsSinglePage;
var PCAPs = class extends APIResource {
  static {
    __name(this, "PCAPs");
  }
  constructor() {
    super(...arguments);
    this.ownership = new OwnershipResource(this._client);
    this.download = new Download(this._client);
  }
  create(params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/pcaps`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { account_id } = params;
    return this._client.getAPIList(`/accounts/${account_id}/pcaps`, PCAPListResponsesSinglePage, options);
  }
  get(pcapId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/pcaps/${pcapId}`, options)._thenUnwrap((obj) => obj.result);
  }
  stop(pcapId, params, options) {
    const { account_id } = params;
    return this._client.put(`/accounts/${account_id}/pcaps/${pcapId}/stop`, {
      ...options,
      headers: { Accept: "*/*", ...options?.headers }
    });
  }
};
var PCAPListResponsesSinglePage = class extends SinglePage {
  static {
    __name(this, "PCAPListResponsesSinglePage");
  }
};
PCAPs.PCAPListResponsesSinglePage = PCAPListResponsesSinglePage;
PCAPs.OwnershipResource = OwnershipResource;
PCAPs.OwnershipsSinglePage = OwnershipsSinglePage;
PCAPs.Download = Download;
var ACLs2 = class extends APIResource {
  static {
    __name(this, "ACLs2");
  }
  create(siteId, params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/magic/sites/${siteId}/acls`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  update(siteId, aclId, params, options) {
    const { account_id, ...body } = params;
    return this._client.put(`/accounts/${account_id}/magic/sites/${siteId}/acls/${aclId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(siteId, params, options) {
    const { account_id } = params;
    return this._client.getAPIList(`/accounts/${account_id}/magic/sites/${siteId}/acls`, ACLsSinglePage2, options);
  }
  delete(siteId, aclId, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/magic/sites/${siteId}/acls/${aclId}`, options)._thenUnwrap((obj) => obj.result);
  }
  edit(siteId, aclId, params, options) {
    const { account_id, ...body } = params;
    return this._client.patch(`/accounts/${account_id}/magic/sites/${siteId}/acls/${aclId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  get(siteId, aclId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/magic/sites/${siteId}/acls/${aclId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var ACLsSinglePage2 = class extends SinglePage {
  static {
    __name(this, "ACLsSinglePage2");
  }
};
ACLs2.ACLsSinglePage = ACLsSinglePage2;
var LANs = class extends APIResource {
  static {
    __name(this, "LANs");
  }
  create(siteId, params, options) {
    const { account_id, ...body } = params;
    return this._client.getAPIList(`/accounts/${account_id}/magic/sites/${siteId}/lans`, LANsSinglePage, {
      body,
      method: "post",
      ...options
    });
  }
  update(siteId, lanId, params, options) {
    const { account_id, ...body } = params;
    return this._client.put(`/accounts/${account_id}/magic/sites/${siteId}/lans/${lanId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(siteId, params, options) {
    const { account_id } = params;
    return this._client.getAPIList(`/accounts/${account_id}/magic/sites/${siteId}/lans`, LANsSinglePage, options);
  }
  delete(siteId, lanId, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/magic/sites/${siteId}/lans/${lanId}`, options)._thenUnwrap((obj) => obj.result);
  }
  edit(siteId, lanId, params, options) {
    const { account_id, ...body } = params;
    return this._client.patch(`/accounts/${account_id}/magic/sites/${siteId}/lans/${lanId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  get(siteId, lanId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/magic/sites/${siteId}/lans/${lanId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var LANsSinglePage = class extends SinglePage {
  static {
    __name(this, "LANsSinglePage");
  }
};
LANs.LANsSinglePage = LANsSinglePage;
var WANs = class extends APIResource {
  static {
    __name(this, "WANs");
  }
  create(siteId, params, options) {
    const { account_id, ...body } = params;
    return this._client.getAPIList(`/accounts/${account_id}/magic/sites/${siteId}/wans`, WANsSinglePage, {
      body,
      method: "post",
      ...options
    });
  }
  update(siteId, wanId, params, options) {
    const { account_id, ...body } = params;
    return this._client.put(`/accounts/${account_id}/magic/sites/${siteId}/wans/${wanId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(siteId, params, options) {
    const { account_id } = params;
    return this._client.getAPIList(`/accounts/${account_id}/magic/sites/${siteId}/wans`, WANsSinglePage, options);
  }
  delete(siteId, wanId, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/magic/sites/${siteId}/wans/${wanId}`, options)._thenUnwrap((obj) => obj.result);
  }
  edit(siteId, wanId, params, options) {
    const { account_id, ...body } = params;
    return this._client.patch(`/accounts/${account_id}/magic/sites/${siteId}/wans/${wanId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  get(siteId, wanId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/magic/sites/${siteId}/wans/${wanId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var WANsSinglePage = class extends SinglePage {
  static {
    __name(this, "WANsSinglePage");
  }
};
WANs.WANsSinglePage = WANsSinglePage;
var Sites = class extends APIResource {
  static {
    __name(this, "Sites");
  }
  constructor() {
    super(...arguments);
    this.acls = new ACLs2(this._client);
    this.lans = new LANs(this._client);
    this.wans = new WANs(this._client);
  }
  create(params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/magic/sites`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  update(siteId, params, options) {
    const { account_id, ...body } = params;
    return this._client.put(`/accounts/${account_id}/magic/sites/${siteId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { account_id, ...query } = params;
    return this._client.getAPIList(`/accounts/${account_id}/magic/sites`, SitesSinglePage, {
      query,
      ...options
    });
  }
  delete(siteId, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/magic/sites/${siteId}`, options)._thenUnwrap((obj) => obj.result);
  }
  edit(siteId, params, options) {
    const { account_id, ...body } = params;
    return this._client.patch(`/accounts/${account_id}/magic/sites/${siteId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  get(siteId, params, options) {
    const { account_id, "x-magic-new-hc-target": xMagicNewHcTarget } = params;
    return this._client.get(`/accounts/${account_id}/magic/sites/${siteId}`, {
      ...options,
      headers: {
        ...xMagicNewHcTarget?.toString() != null ? { "x-magic-new-hc-target": xMagicNewHcTarget?.toString() } : void 0,
        ...options?.headers
      }
    })._thenUnwrap((obj) => obj.result);
  }
};
var SitesSinglePage = class extends SinglePage {
  static {
    __name(this, "SitesSinglePage");
  }
};
Sites.SitesSinglePage = SitesSinglePage;
Sites.ACLs = ACLs2;
Sites.ACLsSinglePage = ACLsSinglePage2;
Sites.LANs = LANs;
Sites.LANsSinglePage = LANsSinglePage;
Sites.WANs = WANs;
Sites.WANsSinglePage = WANsSinglePage;
var MagicTransit = class extends APIResource {
  static {
    __name(this, "MagicTransit");
  }
  constructor() {
    super(...arguments);
    this.apps = new Apps(this._client);
    this.cfInterconnects = new CfInterconnects(this._client);
    this.greTunnels = new GRETunnels(this._client);
    this.ipsecTunnels = new IPSECTunnels(this._client);
    this.routes = new Routes(this._client);
    this.sites = new Sites(this._client);
    this.connectors = new Connectors(this._client);
    this.pcaps = new PCAPs(this._client);
  }
};
MagicTransit.Apps = Apps;
MagicTransit.AppListResponsesSinglePage = AppListResponsesSinglePage;
MagicTransit.CfInterconnects = CfInterconnects;
MagicTransit.GRETunnels = GRETunnels;
MagicTransit.IPSECTunnels = IPSECTunnels;
MagicTransit.Routes = Routes;
MagicTransit.Sites = Sites;
MagicTransit.SitesSinglePage = SitesSinglePage;
MagicTransit.Connectors = Connectors;
MagicTransit.ConnectorListResponsesSinglePage = ConnectorListResponsesSinglePage;
MagicTransit.PCAPs = PCAPs;
MagicTransit.PCAPListResponsesSinglePage = PCAPListResponsesSinglePage;
var ManagedTransforms = class extends APIResource {
  static {
    __name(this, "ManagedTransforms");
  }
  list(params, options) {
    const { zone_id } = params;
    return this._client.get(`/zones/${zone_id}/managed_headers`, options)._thenUnwrap((obj) => obj.result);
  }
  delete(params, options) {
    const { zone_id } = params;
    return this._client.delete(`/zones/${zone_id}/managed_headers`, {
      ...options,
      headers: { Accept: "*/*", ...options?.headers }
    });
  }
  edit(params, options) {
    const { zone_id, ...body } = params;
    return this._client.patch(`/zones/${zone_id}/managed_headers`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
};
var Memberships = class extends APIResource {
  static {
    __name(this, "Memberships");
  }
  update(membershipId, body, options) {
    return this._client.put(`/memberships/${membershipId}`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  list(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.list({}, query);
    }
    return this._client.getAPIList("/memberships", MembershipsV4PagePaginationArray, { query, ...options });
  }
  delete(membershipId, options) {
    return this._client.delete(`/memberships/${membershipId}`, options)._thenUnwrap((obj) => obj.result);
  }
  get(membershipId, options) {
    return this._client.get(`/memberships/${membershipId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var MembershipsV4PagePaginationArray = class extends V4PagePaginationArray {
  static {
    __name(this, "MembershipsV4PagePaginationArray");
  }
};
Memberships.MembershipsV4PagePaginationArray = MembershipsV4PagePaginationArray;
var CNIs = class extends APIResource {
  static {
    __name(this, "CNIs");
  }
  create(params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/cni/cnis`, { body, ...options });
  }
  update(cni, params, options) {
    const { account_id, ...body } = params;
    return this._client.put(`/accounts/${account_id}/cni/cnis/${cni}`, { body, ...options });
  }
  list(params, options) {
    const { account_id, ...query } = params;
    return this._client.get(`/accounts/${account_id}/cni/cnis`, { query, ...options });
  }
  delete(cni, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/cni/cnis/${cni}`, {
      ...options,
      headers: { Accept: "*/*", ...options?.headers }
    });
  }
  get(cni, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/cni/cnis/${cni}`, options);
  }
};
var Interconnects = class extends APIResource {
  static {
    __name(this, "Interconnects");
  }
  create(params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/cni/interconnects`, { body, ...options });
  }
  list(params, options) {
    const { account_id, ...query } = params;
    return this._client.get(`/accounts/${account_id}/cni/interconnects`, { query, ...options });
  }
  delete(icon, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/cni/interconnects/${icon}`, {
      ...options,
      headers: { Accept: "*/*", ...options?.headers }
    });
  }
  get(icon, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/cni/interconnects/${icon}`, options);
  }
  loa(icon, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/cni/interconnects/${icon}/loa`, {
      ...options,
      headers: { Accept: "*/*", ...options?.headers }
    });
  }
  status(icon, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/cni/interconnects/${icon}/status`, options);
  }
};
var Settings6 = class extends APIResource {
  static {
    __name(this, "Settings6");
  }
  update(params, options) {
    const { account_id, ...body } = params;
    return this._client.put(`/accounts/${account_id}/cni/settings`, { body, ...options });
  }
  get(params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/cni/settings`, options);
  }
};
var Slots = class extends APIResource {
  static {
    __name(this, "Slots");
  }
  list(params, options) {
    const { account_id, ...query } = params;
    return this._client.get(`/accounts/${account_id}/cni/slots`, { query, ...options });
  }
  get(slot, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/cni/slots/${slot}`, options);
  }
};
var NetworkInterconnects = class extends APIResource {
  static {
    __name(this, "NetworkInterconnects");
  }
  constructor() {
    super(...arguments);
    this.cnis = new CNIs(this._client);
    this.interconnects = new Interconnects(this._client);
    this.settings = new Settings6(this._client);
    this.slots = new Slots(this._client);
  }
};
NetworkInterconnects.CNIs = CNIs;
NetworkInterconnects.Interconnects = Interconnects;
NetworkInterconnects.Settings = Settings6;
NetworkInterconnects.Slots = Slots;
var OriginCACertificates = class extends APIResource {
  static {
    __name(this, "OriginCACertificates");
  }
  create(body, options) {
    return this._client.post("/certificates", { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  list(query, options) {
    return this._client.getAPIList("/certificates", OriginCACertificatesV4PagePaginationArray, {
      query,
      ...options
    });
  }
  delete(certificateId, options) {
    return this._client.delete(`/certificates/${certificateId}`, options)._thenUnwrap((obj) => obj.result);
  }
  get(certificateId, options) {
    return this._client.get(`/certificates/${certificateId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var OriginCACertificatesV4PagePaginationArray = class extends V4PagePaginationArray {
  static {
    __name(this, "OriginCACertificatesV4PagePaginationArray");
  }
};
OriginCACertificates.OriginCACertificatesV4PagePaginationArray = OriginCACertificatesV4PagePaginationArray;
var OriginPostQuantumEncryption = class extends APIResource {
  static {
    __name(this, "OriginPostQuantumEncryption");
  }
  update(params, options) {
    const { zone_id, ...body } = params;
    return this._client.put(`/zones/${zone_id}/cache/origin_post_quantum_encryption`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  get(params, options) {
    const { zone_id } = params;
    return this._client.get(`/zones/${zone_id}/cache/origin_post_quantum_encryption`, options)._thenUnwrap((obj) => obj.result);
  }
};
var Settings7 = class extends APIResource {
  static {
    __name(this, "Settings7");
  }
  update(params, options) {
    const { zone_id, ...body } = params;
    return this._client.put(`/zones/${zone_id}/origin_tls_client_auth/settings`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  get(params, options) {
    const { zone_id } = params;
    return this._client.get(`/zones/${zone_id}/origin_tls_client_auth/settings`, options)._thenUnwrap((obj) => obj.result);
  }
};
var Certificates2 = class extends APIResource {
  static {
    __name(this, "Certificates2");
  }
  create(params, options) {
    const { zone_id, ...body } = params;
    return this._client.post(`/zones/${zone_id}/origin_tls_client_auth/hostnames/certificates`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { zone_id } = params;
    return this._client.getAPIList(`/zones/${zone_id}/origin_tls_client_auth/hostnames/certificates`, CertificateListResponsesSinglePage, options);
  }
  delete(certificateId, params, options) {
    const { zone_id } = params;
    return this._client.delete(`/zones/${zone_id}/origin_tls_client_auth/hostnames/certificates/${certificateId}`, options)._thenUnwrap((obj) => obj.result);
  }
  get(certificateId, params, options) {
    const { zone_id } = params;
    return this._client.get(`/zones/${zone_id}/origin_tls_client_auth/hostnames/certificates/${certificateId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var CertificateListResponsesSinglePage = class extends SinglePage {
  static {
    __name(this, "CertificateListResponsesSinglePage");
  }
};
Certificates2.CertificateListResponsesSinglePage = CertificateListResponsesSinglePage;
var Hostnames2 = class extends APIResource {
  static {
    __name(this, "Hostnames2");
  }
  constructor() {
    super(...arguments);
    this.certificates = new Certificates2(this._client);
  }
  update(params, options) {
    const { zone_id, ...body } = params;
    return this._client.getAPIList(`/zones/${zone_id}/origin_tls_client_auth/hostnames`, HostnameUpdateResponsesSinglePage, { body, method: "put", ...options });
  }
  get(hostname, params, options) {
    const { zone_id } = params;
    return this._client.get(`/zones/${zone_id}/origin_tls_client_auth/hostnames/${hostname}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var HostnameUpdateResponsesSinglePage = class extends SinglePage {
  static {
    __name(this, "HostnameUpdateResponsesSinglePage");
  }
};
Hostnames2.HostnameUpdateResponsesSinglePage = HostnameUpdateResponsesSinglePage;
Hostnames2.Certificates = Certificates2;
Hostnames2.CertificateListResponsesSinglePage = CertificateListResponsesSinglePage;
var OriginTLSClientAuth = class extends APIResource {
  static {
    __name(this, "OriginTLSClientAuth");
  }
  constructor() {
    super(...arguments);
    this.hostnames = new Hostnames2(this._client);
    this.settings = new Settings7(this._client);
  }
  create(params, options) {
    const { zone_id, ...body } = params;
    return this._client.post(`/zones/${zone_id}/origin_tls_client_auth`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { zone_id } = params;
    return this._client.getAPIList(`/zones/${zone_id}/origin_tls_client_auth`, OriginTLSClientAuthListResponsesSinglePage, options);
  }
  delete(certificateId, params, options) {
    const { zone_id } = params;
    return this._client.delete(`/zones/${zone_id}/origin_tls_client_auth/${certificateId}`, options)._thenUnwrap((obj) => obj.result);
  }
  get(certificateId, params, options) {
    const { zone_id } = params;
    return this._client.get(`/zones/${zone_id}/origin_tls_client_auth/${certificateId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var OriginTLSClientAuthListResponsesSinglePage = class extends SinglePage {
  static {
    __name(this, "OriginTLSClientAuthListResponsesSinglePage");
  }
};
OriginTLSClientAuth.OriginTLSClientAuthListResponsesSinglePage = OriginTLSClientAuthListResponsesSinglePage;
OriginTLSClientAuth.Hostnames = Hostnames2;
OriginTLSClientAuth.HostnameUpdateResponsesSinglePage = HostnameUpdateResponsesSinglePage;
OriginTLSClientAuth.Settings = Settings7;
var PageRules = class extends APIResource {
  static {
    __name(this, "PageRules");
  }
  create(params, options) {
    const { zone_id, ...body } = params;
    return this._client.post(`/zones/${zone_id}/pagerules`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  update(pageruleId, params, options) {
    const { zone_id, ...body } = params;
    return this._client.put(`/zones/${zone_id}/pagerules/${pageruleId}`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { zone_id, ...query } = params;
    return this._client.get(`/zones/${zone_id}/pagerules`, { query, ...options })._thenUnwrap((obj) => obj.result);
  }
  delete(pageruleId, params, options) {
    const { zone_id } = params;
    return this._client.delete(`/zones/${zone_id}/pagerules/${pageruleId}`, options)._thenUnwrap((obj) => obj.result);
  }
  edit(pageruleId, params, options) {
    const { zone_id, ...body } = params;
    return this._client.patch(`/zones/${zone_id}/pagerules/${pageruleId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  get(pageruleId, params, options) {
    const { zone_id } = params;
    return this._client.get(`/zones/${zone_id}/pagerules/${pageruleId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var Connections = class extends APIResource {
  static {
    __name(this, "Connections");
  }
  list(params, options) {
    const { zone_id, ...query } = params;
    return this._client.getAPIList(`/zones/${zone_id}/page_shield/connections`, ConnectionsSinglePage, {
      query,
      ...options
    });
  }
  get(connectionId, params, options) {
    const { zone_id } = params;
    return this._client.get(`/zones/${zone_id}/page_shield/connections/${connectionId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var ConnectionsSinglePage = class extends SinglePage {
  static {
    __name(this, "ConnectionsSinglePage");
  }
};
Connections.ConnectionsSinglePage = ConnectionsSinglePage;
var Cookies = class extends APIResource {
  static {
    __name(this, "Cookies");
  }
  list(params, options) {
    const { zone_id, ...query } = params;
    return this._client.getAPIList(`/zones/${zone_id}/page_shield/cookies`, CookieListResponsesSinglePage, {
      query,
      ...options
    });
  }
  get(cookieId, params, options) {
    const { zone_id } = params;
    return this._client.get(`/zones/${zone_id}/page_shield/cookies/${cookieId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var CookieListResponsesSinglePage = class extends SinglePage {
  static {
    __name(this, "CookieListResponsesSinglePage");
  }
};
Cookies.CookieListResponsesSinglePage = CookieListResponsesSinglePage;
var Policies2 = class extends APIResource {
  static {
    __name(this, "Policies2");
  }
  create(params, options) {
    const { zone_id, ...body } = params;
    return this._client.post(`/zones/${zone_id}/page_shield/policies`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  update(policyId, params, options) {
    const { zone_id, ...body } = params;
    return this._client.put(`/zones/${zone_id}/page_shield/policies/${policyId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { zone_id } = params;
    return this._client.getAPIList(`/zones/${zone_id}/page_shield/policies`, PolicyListResponsesSinglePage, options);
  }
  delete(policyId, params, options) {
    const { zone_id } = params;
    return this._client.delete(`/zones/${zone_id}/page_shield/policies/${policyId}`, {
      ...options,
      headers: { Accept: "*/*", ...options?.headers }
    });
  }
  get(policyId, params, options) {
    const { zone_id } = params;
    return this._client.get(`/zones/${zone_id}/page_shield/policies/${policyId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var PolicyListResponsesSinglePage = class extends SinglePage {
  static {
    __name(this, "PolicyListResponsesSinglePage");
  }
};
Policies2.PolicyListResponsesSinglePage = PolicyListResponsesSinglePage;
var Scripts = class extends APIResource {
  static {
    __name(this, "Scripts");
  }
  list(params, options) {
    const { zone_id, ...query } = params;
    return this._client.getAPIList(`/zones/${zone_id}/page_shield/scripts`, ScriptsSinglePage, {
      query,
      ...options
    });
  }
  get(scriptId, params, options) {
    const { zone_id } = params;
    return this._client.get(`/zones/${zone_id}/page_shield/scripts/${scriptId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var ScriptsSinglePage = class extends SinglePage {
  static {
    __name(this, "ScriptsSinglePage");
  }
};
Scripts.ScriptsSinglePage = ScriptsSinglePage;
var PageShield = class extends APIResource {
  static {
    __name(this, "PageShield");
  }
  constructor() {
    super(...arguments);
    this.policies = new Policies2(this._client);
    this.connections = new Connections(this._client);
    this.scripts = new Scripts(this._client);
    this.cookies = new Cookies(this._client);
  }
  update(params, options) {
    const { zone_id, ...body } = params;
    return this._client.put(`/zones/${zone_id}/page_shield`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  get(params, options) {
    const { zone_id } = params;
    return this._client.get(`/zones/${zone_id}/page_shield`, options)._thenUnwrap((obj) => obj.result);
  }
};
PageShield.Policies = Policies2;
PageShield.PolicyListResponsesSinglePage = PolicyListResponsesSinglePage;
PageShield.Connections = Connections;
PageShield.ConnectionsSinglePage = ConnectionsSinglePage;
PageShield.Scripts = Scripts;
PageShield.ScriptsSinglePage = ScriptsSinglePage;
PageShield.Cookies = Cookies;
PageShield.CookieListResponsesSinglePage = CookieListResponsesSinglePage;
var Domains3 = class extends APIResource {
  static {
    __name(this, "Domains3");
  }
  create(projectName, params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/pages/projects/${projectName}/domains`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(projectName, params, options) {
    const { account_id } = params;
    return this._client.getAPIList(`/accounts/${account_id}/pages/projects/${projectName}/domains`, DomainListResponsesSinglePage, options);
  }
  delete(projectName, domainName, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/pages/projects/${projectName}/domains/${domainName}`, options)._thenUnwrap((obj) => obj.result);
  }
  edit(projectName, domainName, params, options) {
    const { account_id, body } = params;
    return this._client.patch(`/accounts/${account_id}/pages/projects/${projectName}/domains/${domainName}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  get(projectName, domainName, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/pages/projects/${projectName}/domains/${domainName}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var DomainListResponsesSinglePage = class extends SinglePage {
  static {
    __name(this, "DomainListResponsesSinglePage");
  }
};
Domains3.DomainListResponsesSinglePage = DomainListResponsesSinglePage;
var Logs4 = class extends APIResource {
  static {
    __name(this, "Logs4");
  }
  get(projectName, deploymentId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/pages/projects/${projectName}/deployments/${deploymentId}/history/logs`, options)._thenUnwrap((obj) => obj.result);
  }
};
var History = class extends APIResource {
  static {
    __name(this, "History");
  }
  constructor() {
    super(...arguments);
    this.logs = new Logs4(this._client);
  }
};
History.Logs = Logs4;
var Deployments = class extends APIResource {
  static {
    __name(this, "Deployments");
  }
  constructor() {
    super(...arguments);
    this.history = new History(this._client);
  }
  create(projectName, params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/pages/projects/${projectName}/deployments`, multipartFormRequestOptions({ body, ...options }))._thenUnwrap((obj) => obj.result);
  }
  list(projectName, params, options) {
    const { account_id, ...query } = params;
    return this._client.getAPIList(`/accounts/${account_id}/pages/projects/${projectName}/deployments`, DeploymentsSinglePage, { query, ...options });
  }
  delete(projectName, deploymentId, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/pages/projects/${projectName}/deployments/${deploymentId}`, options)._thenUnwrap((obj) => obj.result);
  }
  get(projectName, deploymentId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/pages/projects/${projectName}/deployments/${deploymentId}`, options)._thenUnwrap((obj) => obj.result);
  }
  retry(projectName, deploymentId, params, options) {
    const { account_id, body } = params;
    return this._client.post(`/accounts/${account_id}/pages/projects/${projectName}/deployments/${deploymentId}/retry`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  rollback(projectName, deploymentId, params, options) {
    const { account_id, body } = params;
    return this._client.post(`/accounts/${account_id}/pages/projects/${projectName}/deployments/${deploymentId}/rollback`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
};
Deployments.History = History;
var Projects = class extends APIResource {
  static {
    __name(this, "Projects");
  }
  constructor() {
    super(...arguments);
    this.deployments = new Deployments(this._client);
    this.domains = new Domains3(this._client);
  }
  create(params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/pages/projects`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { account_id } = params;
    return this._client.getAPIList(`/accounts/${account_id}/pages/projects`, DeploymentsSinglePage, options);
  }
  delete(projectName, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/pages/projects/${projectName}`, options)._thenUnwrap((obj) => obj.result);
  }
  edit(projectName, params, options) {
    const { account_id, ...body } = params;
    return this._client.patch(`/accounts/${account_id}/pages/projects/${projectName}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  get(projectName, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/pages/projects/${projectName}`, options)._thenUnwrap((obj) => obj.result);
  }
  purgeBuildCache(projectName, params, options) {
    const { account_id } = params;
    return this._client.post(`/accounts/${account_id}/pages/projects/${projectName}/purge_build_cache`, options)._thenUnwrap((obj) => obj.result);
  }
};
var DeploymentsSinglePage = class extends SinglePage {
  static {
    __name(this, "DeploymentsSinglePage");
  }
};
Projects.DeploymentsSinglePage = DeploymentsSinglePage;
Projects.Deployments = Deployments;
Projects.Domains = Domains3;
Projects.DomainListResponsesSinglePage = DomainListResponsesSinglePage;
var Pages = class extends APIResource {
  static {
    __name(this, "Pages");
  }
  constructor() {
    super(...arguments);
    this.projects = new Projects(this._client);
  }
};
Pages.Projects = Projects;
Pages.DeploymentsSinglePage = DeploymentsSinglePage;
var Pipelines = class extends APIResource {
  static {
    __name(this, "Pipelines");
  }
  create(params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/pipelines`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  update(pipelineName, params, options) {
    const { account_id, ...body } = params;
    return this._client.put(`/accounts/${account_id}/pipelines/${pipelineName}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { account_id, ...query } = params;
    return this._client.get(`/accounts/${account_id}/pipelines`, { query, ...options });
  }
  delete(pipelineName, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/pipelines/${pipelineName}`, {
      ...options,
      headers: { Accept: "*/*", ...options?.headers }
    });
  }
  get(pipelineName, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/pipelines/${pipelineName}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var Consumers = class extends APIResource {
  static {
    __name(this, "Consumers");
  }
  create(queueId, params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/queues/${queueId}/consumers`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  update(queueId, consumerId, params, options) {
    const { account_id, ...body } = params;
    return this._client.put(`/accounts/${account_id}/queues/${queueId}/consumers/${consumerId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(queueId, params, options) {
    const { account_id } = params;
    return this._client.getAPIList(`/accounts/${account_id}/queues/${queueId}/consumers`, ConsumersSinglePage, options);
  }
  delete(queueId, consumerId, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/queues/${queueId}/consumers/${consumerId}`, options);
  }
  get(queueId, consumerId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/queues/${queueId}/consumers/${consumerId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var ConsumersSinglePage = class extends SinglePage {
  static {
    __name(this, "ConsumersSinglePage");
  }
};
Consumers.ConsumersSinglePage = ConsumersSinglePage;
var Messages = class extends APIResource {
  static {
    __name(this, "Messages");
  }
  ack(queueId, params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/queues/${queueId}/messages/ack`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  bulkPush(queueId, params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/queues/${queueId}/messages/batch`, {
      body,
      ...options
    });
  }
  pull(queueId, params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/queues/${queueId}/messages/pull`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  push(queueId, params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/queues/${queueId}/messages`, { body, ...options });
  }
};
var Purge = class extends APIResource {
  static {
    __name(this, "Purge");
  }
  start(queueId, params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/queues/${queueId}/purge`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  status(queueId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/queues/${queueId}/purge`, options)._thenUnwrap((obj) => obj.result);
  }
};
var Subscriptions2 = class extends APIResource {
  static {
    __name(this, "Subscriptions2");
  }
  create(params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/event_subscriptions/subscriptions`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  update(subscriptionId, params, options) {
    const { account_id, ...body } = params;
    return this._client.patch(`/accounts/${account_id}/event_subscriptions/subscriptions/${subscriptionId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { account_id, ...query } = params;
    return this._client.getAPIList(`/accounts/${account_id}/event_subscriptions/subscriptions`, SubscriptionListResponsesV4PagePaginationArray, { query, ...options });
  }
  delete(subscriptionId, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/event_subscriptions/subscriptions/${subscriptionId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var SubscriptionListResponsesV4PagePaginationArray = class extends V4PagePaginationArray {
  static {
    __name(this, "SubscriptionListResponsesV4PagePaginationArray");
  }
};
Subscriptions2.SubscriptionListResponsesV4PagePaginationArray = SubscriptionListResponsesV4PagePaginationArray;
var Queues = class extends APIResource {
  static {
    __name(this, "Queues");
  }
  constructor() {
    super(...arguments);
    this.messages = new Messages(this._client);
    this.purge = new Purge(this._client);
    this.consumers = new Consumers(this._client);
    this.subscriptions = new Subscriptions2(this._client);
  }
  create(params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/queues`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  update(queueId, params, options) {
    const { account_id, ...body } = params;
    return this._client.put(`/accounts/${account_id}/queues/${queueId}`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { account_id } = params;
    return this._client.getAPIList(`/accounts/${account_id}/queues`, QueuesSinglePage, options);
  }
  delete(queueId, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/queues/${queueId}`, options);
  }
  edit(queueId, params, options) {
    const { account_id, ...body } = params;
    return this._client.patch(`/accounts/${account_id}/queues/${queueId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  get(queueId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/queues/${queueId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var QueuesSinglePage = class extends SinglePage {
  static {
    __name(this, "QueuesSinglePage");
  }
};
Queues.QueuesSinglePage = QueuesSinglePage;
Queues.Messages = Messages;
Queues.Purge = Purge;
Queues.Consumers = Consumers;
Queues.ConsumersSinglePage = ConsumersSinglePage;
Queues.Subscriptions = Subscriptions2;
Queues.SubscriptionListResponsesV4PagePaginationArray = SubscriptionListResponsesV4PagePaginationArray;
var TemporaryCredentials = class extends APIResource {
  static {
    __name(this, "TemporaryCredentials");
  }
  create(params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/r2/temp-access-credentials`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
};
var CORS = class extends APIResource {
  static {
    __name(this, "CORS");
  }
  update(bucketName, params, options) {
    const { account_id, jurisdiction, ...body } = params;
    return this._client.put(`/accounts/${account_id}/r2/buckets/${bucketName}/cors`, {
      body,
      ...options,
      headers: {
        ...jurisdiction?.toString() != null ? { "cf-r2-jurisdiction": jurisdiction?.toString() } : void 0,
        ...options?.headers
      }
    })._thenUnwrap((obj) => obj.result);
  }
  delete(bucketName, params, options) {
    const { account_id, jurisdiction } = params;
    return this._client.delete(`/accounts/${account_id}/r2/buckets/${bucketName}/cors`, {
      ...options,
      headers: {
        ...jurisdiction?.toString() != null ? { "cf-r2-jurisdiction": jurisdiction?.toString() } : void 0,
        ...options?.headers
      }
    })._thenUnwrap((obj) => obj.result);
  }
  get(bucketName, params, options) {
    const { account_id, jurisdiction } = params;
    return this._client.get(`/accounts/${account_id}/r2/buckets/${bucketName}/cors`, {
      ...options,
      headers: {
        ...jurisdiction?.toString() != null ? { "cf-r2-jurisdiction": jurisdiction?.toString() } : void 0,
        ...options?.headers
      }
    })._thenUnwrap((obj) => obj.result);
  }
};
var EventNotifications = class extends APIResource {
  static {
    __name(this, "EventNotifications");
  }
  update(bucketName, queueId, params, options) {
    const { account_id, jurisdiction, ...body } = params;
    return this._client.put(`/accounts/${account_id}/event_notifications/r2/${bucketName}/configuration/queues/${queueId}`, {
      body,
      ...options,
      headers: {
        ...jurisdiction?.toString() != null ? { "cf-r2-jurisdiction": jurisdiction?.toString() } : void 0,
        ...options?.headers
      }
    })._thenUnwrap((obj) => obj.result);
  }
  list(bucketName, params, options) {
    const { account_id, jurisdiction } = params;
    return this._client.get(`/accounts/${account_id}/event_notifications/r2/${bucketName}/configuration`, {
      ...options,
      headers: {
        ...jurisdiction?.toString() != null ? { "cf-r2-jurisdiction": jurisdiction?.toString() } : void 0,
        ...options?.headers
      }
    })._thenUnwrap((obj) => obj.result);
  }
  delete(bucketName, queueId, params, options) {
    const { account_id, jurisdiction } = params;
    return this._client.delete(`/accounts/${account_id}/event_notifications/r2/${bucketName}/configuration/queues/${queueId}`, {
      ...options,
      headers: {
        ...jurisdiction?.toString() != null ? { "cf-r2-jurisdiction": jurisdiction?.toString() } : void 0,
        ...options?.headers
      }
    })._thenUnwrap((obj) => obj.result);
  }
  get(bucketName, queueId, params, options) {
    const { account_id, jurisdiction } = params;
    return this._client.get(`/accounts/${account_id}/event_notifications/r2/${bucketName}/configuration/queues/${queueId}`, {
      ...options,
      headers: {
        ...jurisdiction?.toString() != null ? { "cf-r2-jurisdiction": jurisdiction?.toString() } : void 0,
        ...options?.headers
      }
    })._thenUnwrap((obj) => obj.result);
  }
};
var Lifecycle = class extends APIResource {
  static {
    __name(this, "Lifecycle");
  }
  update(bucketName, params, options) {
    const { account_id, jurisdiction, ...body } = params;
    return this._client.put(`/accounts/${account_id}/r2/buckets/${bucketName}/lifecycle`, {
      body,
      ...options,
      headers: {
        ...jurisdiction?.toString() != null ? { "cf-r2-jurisdiction": jurisdiction?.toString() } : void 0,
        ...options?.headers
      }
    })._thenUnwrap((obj) => obj.result);
  }
  get(bucketName, params, options) {
    const { account_id, jurisdiction } = params;
    return this._client.get(`/accounts/${account_id}/r2/buckets/${bucketName}/lifecycle`, {
      ...options,
      headers: {
        ...jurisdiction?.toString() != null ? { "cf-r2-jurisdiction": jurisdiction?.toString() } : void 0,
        ...options?.headers
      }
    })._thenUnwrap((obj) => obj.result);
  }
};
var Locks = class extends APIResource {
  static {
    __name(this, "Locks");
  }
  update(bucketName, params, options) {
    const { account_id, jurisdiction, ...body } = params;
    return this._client.put(`/accounts/${account_id}/r2/buckets/${bucketName}/lock`, {
      body,
      ...options,
      headers: {
        ...jurisdiction?.toString() != null ? { "cf-r2-jurisdiction": jurisdiction?.toString() } : void 0,
        ...options?.headers
      }
    })._thenUnwrap((obj) => obj.result);
  }
  get(bucketName, params, options) {
    const { account_id, jurisdiction } = params;
    return this._client.get(`/accounts/${account_id}/r2/buckets/${bucketName}/lock`, {
      ...options,
      headers: {
        ...jurisdiction?.toString() != null ? { "cf-r2-jurisdiction": jurisdiction?.toString() } : void 0,
        ...options?.headers
      }
    })._thenUnwrap((obj) => obj.result);
  }
};
var Metrics = class extends APIResource {
  static {
    __name(this, "Metrics");
  }
  list(params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/r2/metrics`, options)._thenUnwrap((obj) => obj.result);
  }
};
var SippyResource = class extends APIResource {
  static {
    __name(this, "SippyResource");
  }
  update(bucketName, params, options) {
    const { account_id, jurisdiction, ...body } = params;
    return this._client.put(`/accounts/${account_id}/r2/buckets/${bucketName}/sippy`, {
      body,
      ...options,
      headers: {
        ...jurisdiction?.toString() != null ? { "cf-r2-jurisdiction": jurisdiction?.toString() } : void 0,
        ...options?.headers
      }
    })._thenUnwrap((obj) => obj.result);
  }
  delete(bucketName, params, options) {
    const { account_id, jurisdiction } = params;
    return this._client.delete(`/accounts/${account_id}/r2/buckets/${bucketName}/sippy`, {
      ...options,
      headers: {
        ...jurisdiction?.toString() != null ? { "cf-r2-jurisdiction": jurisdiction?.toString() } : void 0,
        ...options?.headers
      }
    })._thenUnwrap((obj) => obj.result);
  }
  get(bucketName, params, options) {
    const { account_id, jurisdiction } = params;
    return this._client.get(`/accounts/${account_id}/r2/buckets/${bucketName}/sippy`, {
      ...options,
      headers: {
        ...jurisdiction?.toString() != null ? { "cf-r2-jurisdiction": jurisdiction?.toString() } : void 0,
        ...options?.headers
      }
    })._thenUnwrap((obj) => obj.result);
  }
};
var Custom = class extends APIResource {
  static {
    __name(this, "Custom");
  }
  create(bucketName, params, options) {
    const { account_id, jurisdiction, ...body } = params;
    return this._client.post(`/accounts/${account_id}/r2/buckets/${bucketName}/domains/custom`, {
      body,
      ...options,
      headers: {
        ...jurisdiction?.toString() != null ? { "cf-r2-jurisdiction": jurisdiction?.toString() } : void 0,
        ...options?.headers
      }
    })._thenUnwrap((obj) => obj.result);
  }
  update(bucketName, domain2, params, options) {
    const { account_id, jurisdiction, ...body } = params;
    return this._client.put(`/accounts/${account_id}/r2/buckets/${bucketName}/domains/custom/${domain2}`, {
      body,
      ...options,
      headers: {
        ...jurisdiction?.toString() != null ? { "cf-r2-jurisdiction": jurisdiction?.toString() } : void 0,
        ...options?.headers
      }
    })._thenUnwrap((obj) => obj.result);
  }
  list(bucketName, params, options) {
    const { account_id, jurisdiction } = params;
    return this._client.get(`/accounts/${account_id}/r2/buckets/${bucketName}/domains/custom`, {
      ...options,
      headers: {
        ...jurisdiction?.toString() != null ? { "cf-r2-jurisdiction": jurisdiction?.toString() } : void 0,
        ...options?.headers
      }
    })._thenUnwrap((obj) => obj.result);
  }
  delete(bucketName, domain2, params, options) {
    const { account_id, jurisdiction } = params;
    return this._client.delete(`/accounts/${account_id}/r2/buckets/${bucketName}/domains/custom/${domain2}`, {
      ...options,
      headers: {
        ...jurisdiction?.toString() != null ? { "cf-r2-jurisdiction": jurisdiction?.toString() } : void 0,
        ...options?.headers
      }
    })._thenUnwrap((obj) => obj.result);
  }
  get(bucketName, domain2, params, options) {
    const { account_id, jurisdiction } = params;
    return this._client.get(`/accounts/${account_id}/r2/buckets/${bucketName}/domains/custom/${domain2}`, {
      ...options,
      headers: {
        ...jurisdiction?.toString() != null ? { "cf-r2-jurisdiction": jurisdiction?.toString() } : void 0,
        ...options?.headers
      }
    })._thenUnwrap((obj) => obj.result);
  }
};
var Managed = class extends APIResource {
  static {
    __name(this, "Managed");
  }
  update(bucketName, params, options) {
    const { account_id, jurisdiction, ...body } = params;
    return this._client.put(`/accounts/${account_id}/r2/buckets/${bucketName}/domains/managed`, {
      body,
      ...options,
      headers: {
        ...jurisdiction?.toString() != null ? { "cf-r2-jurisdiction": jurisdiction?.toString() } : void 0,
        ...options?.headers
      }
    })._thenUnwrap((obj) => obj.result);
  }
  list(bucketName, params, options) {
    const { account_id, jurisdiction } = params;
    return this._client.get(`/accounts/${account_id}/r2/buckets/${bucketName}/domains/managed`, {
      ...options,
      headers: {
        ...jurisdiction?.toString() != null ? { "cf-r2-jurisdiction": jurisdiction?.toString() } : void 0,
        ...options?.headers
      }
    })._thenUnwrap((obj) => obj.result);
  }
};
var Domains4 = class extends APIResource {
  static {
    __name(this, "Domains4");
  }
  constructor() {
    super(...arguments);
    this.custom = new Custom(this._client);
    this.managed = new Managed(this._client);
  }
};
Domains4.Custom = Custom;
Domains4.Managed = Managed;
var Buckets = class extends APIResource {
  static {
    __name(this, "Buckets");
  }
  constructor() {
    super(...arguments);
    this.lifecycle = new Lifecycle(this._client);
    this.cors = new CORS(this._client);
    this.domains = new Domains4(this._client);
    this.eventNotifications = new EventNotifications(this._client);
    this.locks = new Locks(this._client);
    this.metrics = new Metrics(this._client);
    this.sippy = new SippyResource(this._client);
  }
  create(params, options) {
    const { account_id, jurisdiction, ...body } = params;
    return this._client.post(`/accounts/${account_id}/r2/buckets`, {
      body,
      ...options,
      headers: {
        ...jurisdiction?.toString() != null ? { "cf-r2-jurisdiction": jurisdiction?.toString() } : void 0,
        ...options?.headers
      }
    })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { account_id, jurisdiction, ...query } = params;
    return this._client.get(`/accounts/${account_id}/r2/buckets`, {
      query,
      ...options,
      headers: {
        ...jurisdiction?.toString() != null ? { "cf-r2-jurisdiction": jurisdiction?.toString() } : void 0,
        ...options?.headers
      }
    })._thenUnwrap((obj) => obj.result);
  }
  delete(bucketName, params, options) {
    const { account_id, jurisdiction } = params;
    return this._client.delete(`/accounts/${account_id}/r2/buckets/${bucketName}`, {
      ...options,
      headers: {
        ...jurisdiction?.toString() != null ? { "cf-r2-jurisdiction": jurisdiction?.toString() } : void 0,
        ...options?.headers
      }
    })._thenUnwrap((obj) => obj.result);
  }
  edit(bucketName, params, options) {
    const { account_id, storage_class, jurisdiction } = params;
    return this._client.patch(`/accounts/${account_id}/r2/buckets/${bucketName}`, {
      ...options,
      headers: {
        "cf-r2-storage-class": storage_class.toString(),
        ...jurisdiction?.toString() != null ? { "cf-r2-jurisdiction": jurisdiction?.toString() } : void 0,
        ...options?.headers
      }
    })._thenUnwrap((obj) => obj.result);
  }
  get(bucketName, params, options) {
    const { account_id, jurisdiction } = params;
    return this._client.get(`/accounts/${account_id}/r2/buckets/${bucketName}`, {
      ...options,
      headers: {
        ...jurisdiction?.toString() != null ? { "cf-r2-jurisdiction": jurisdiction?.toString() } : void 0,
        ...options?.headers
      }
    })._thenUnwrap((obj) => obj.result);
  }
};
Buckets.Lifecycle = Lifecycle;
Buckets.CORS = CORS;
Buckets.Domains = Domains4;
Buckets.EventNotifications = EventNotifications;
Buckets.Locks = Locks;
Buckets.Metrics = Metrics;
Buckets.SippyResource = SippyResource;
var ConnectivityPrecheck = class extends APIResource {
  static {
    __name(this, "ConnectivityPrecheck");
  }
  source(params, options) {
    const { account_id, ...body } = params;
    return this._client.put(`/accounts/${account_id}/slurper/source/connectivity-precheck`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  target(params, options) {
    const { account_id, ...body } = params;
    return this._client.put(`/accounts/${account_id}/slurper/target/connectivity-precheck`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
};
var Logs5 = class extends APIResource {
  static {
    __name(this, "Logs5");
  }
  list(jobId, params, options) {
    const { account_id, ...query } = params;
    return this._client.getAPIList(`/accounts/${account_id}/slurper/jobs/${jobId}/logs`, LogListResponsesSinglePage, { query, ...options });
  }
};
var LogListResponsesSinglePage = class extends SinglePage {
  static {
    __name(this, "LogListResponsesSinglePage");
  }
};
Logs5.LogListResponsesSinglePage = LogListResponsesSinglePage;
var Jobs3 = class extends APIResource {
  static {
    __name(this, "Jobs3");
  }
  constructor() {
    super(...arguments);
    this.logs = new Logs5(this._client);
  }
  create(params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/slurper/jobs`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { account_id, ...query } = params;
    return this._client.getAPIList(`/accounts/${account_id}/slurper/jobs`, JobListResponsesSinglePage, {
      query,
      ...options
    });
  }
  abort(jobId, params, options) {
    const { account_id } = params;
    return this._client.put(`/accounts/${account_id}/slurper/jobs/${jobId}/abort`, options)._thenUnwrap((obj) => obj.result);
  }
  abortAll(params, options) {
    const { account_id } = params;
    return this._client.put(`/accounts/${account_id}/slurper/jobs/abortAll`, options)._thenUnwrap((obj) => obj.result);
  }
  get(jobId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/slurper/jobs/${jobId}`, options)._thenUnwrap((obj) => obj.result);
  }
  pause(jobId, params, options) {
    const { account_id } = params;
    return this._client.put(`/accounts/${account_id}/slurper/jobs/${jobId}/pause`, options)._thenUnwrap((obj) => obj.result);
  }
  progress(jobId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/slurper/jobs/${jobId}/progress`, options)._thenUnwrap((obj) => obj.result);
  }
  resume(jobId, params, options) {
    const { account_id } = params;
    return this._client.put(`/accounts/${account_id}/slurper/jobs/${jobId}/resume`, options)._thenUnwrap((obj) => obj.result);
  }
};
var JobListResponsesSinglePage = class extends SinglePage {
  static {
    __name(this, "JobListResponsesSinglePage");
  }
};
Jobs3.JobListResponsesSinglePage = JobListResponsesSinglePage;
Jobs3.Logs = Logs5;
Jobs3.LogListResponsesSinglePage = LogListResponsesSinglePage;
var SuperSlurper = class extends APIResource {
  static {
    __name(this, "SuperSlurper");
  }
  constructor() {
    super(...arguments);
    this.jobs = new Jobs3(this._client);
    this.connectivityPrecheck = new ConnectivityPrecheck(this._client);
  }
};
SuperSlurper.Jobs = Jobs3;
SuperSlurper.JobListResponsesSinglePage = JobListResponsesSinglePage;
SuperSlurper.ConnectivityPrecheck = ConnectivityPrecheck;
var R2 = class extends APIResource {
  static {
    __name(this, "R2");
  }
  constructor() {
    super(...arguments);
    this.buckets = new Buckets(this._client);
    this.temporaryCredentials = new TemporaryCredentials(this._client);
    this.superSlurper = new SuperSlurper(this._client);
  }
};
R2.Buckets = Buckets;
R2.TemporaryCredentials = TemporaryCredentials;
R2.SuperSlurper = SuperSlurper;
var Rules6 = class extends APIResource {
  static {
    __name(this, "Rules6");
  }
  create(rulesetId, params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/rum/v2/${rulesetId}/rule`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  update(rulesetId, ruleId, params, options) {
    const { account_id, ...body } = params;
    return this._client.put(`/accounts/${account_id}/rum/v2/${rulesetId}/rule/${ruleId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(rulesetId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/rum/v2/${rulesetId}/rules`, options)._thenUnwrap((obj) => obj.result);
  }
  delete(rulesetId, ruleId, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/rum/v2/${rulesetId}/rule/${ruleId}`, options)._thenUnwrap((obj) => obj.result);
  }
  bulkCreate(rulesetId, params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/rum/v2/${rulesetId}/rules`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
};
var SiteInfo = class extends APIResource {
  static {
    __name(this, "SiteInfo");
  }
  create(params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/rum/site_info`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  update(siteId, params, options) {
    const { account_id, ...body } = params;
    return this._client.put(`/accounts/${account_id}/rum/site_info/${siteId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { account_id, ...query } = params;
    return this._client.getAPIList(`/accounts/${account_id}/rum/site_info/list`, SitesV4PagePaginationArray, {
      query,
      ...options
    });
  }
  delete(siteId, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/rum/site_info/${siteId}`, options)._thenUnwrap((obj) => obj.result);
  }
  get(siteId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/rum/site_info/${siteId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var SitesV4PagePaginationArray = class extends V4PagePaginationArray {
  static {
    __name(this, "SitesV4PagePaginationArray");
  }
};
SiteInfo.SitesV4PagePaginationArray = SitesV4PagePaginationArray;
var RUM = class extends APIResource {
  static {
    __name(this, "RUM");
  }
  constructor() {
    super(...arguments);
    this.siteInfo = new SiteInfo(this._client);
    this.rules = new Rules6(this._client);
  }
};
RUM.SiteInfo = SiteInfo;
RUM.SitesV4PagePaginationArray = SitesV4PagePaginationArray;
RUM.Rules = Rules6;
var Datasets4 = class extends APIResource {
  static {
    __name(this, "Datasets4");
  }
  list(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.list({}, query);
    }
    return this._client.get("/radar/datasets", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
  download(params, options) {
    const { format, ...body } = params;
    return this._client.post("/radar/datasets/download", {
      query: { format },
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  get(alias, options) {
    return this._client.get(`/radar/datasets/${alias}`, {
      ...options,
      headers: { Accept: "text/csv", ...options?.headers }
    });
  }
};
var Search = class extends APIResource {
  static {
    __name(this, "Search");
  }
  global(query, options) {
    return this._client.get("/radar/search/global", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
};
var TCPResetsTimeouts = class extends APIResource {
  static {
    __name(this, "TCPResetsTimeouts");
  }
  summary(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.summary({}, query);
    }
    return this._client.get("/radar/tcp_resets_timeouts/summary", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
  timeseriesGroups(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.timeseriesGroups({}, query);
    }
    return this._client.get("/radar/tcp_resets_timeouts/timeseries_groups", {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
};
var TimeseriesGroups = class extends APIResource {
  static {
    __name(this, "TimeseriesGroups");
  }
  summary(dimension, query = {}, options) {
    if (isRequestOptions(query)) {
      return this.summary(dimension, {}, query);
    }
    return this._client.get(`/radar/ai/bots/summary/${dimension}`, { query, ...options })._thenUnwrap((obj) => obj.result);
  }
  timeseries(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.timeseries({}, query);
    }
    return this._client.get("/radar/ai/bots/timeseries", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
  timeseriesGroups(dimension, query = {}, options) {
    if (isRequestOptions(query)) {
      return this.timeseriesGroups(dimension, {}, query);
    }
    return this._client.get(`/radar/ai/bots/timeseries_groups/${dimension}`, {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  userAgent(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.userAgent({}, query);
    }
    return this._client.get("/radar/ai/bots/timeseries_groups/user_agent", {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
};
var ToMarkdown2 = class extends APIResource {
  static {
    __name(this, "ToMarkdown2");
  }
  create(body, params, options) {
    const { account_id } = params;
    return this._client.getAPIList(`/accounts/${account_id}/ai/tomarkdown`, ToMarkdownCreateResponsesSinglePage, {
      body,
      method: "post",
      ...options,
      headers: { "Content-Type": "application/octet-stream", ...options?.headers },
      __binaryRequest: true
    });
  }
};
var ToMarkdownCreateResponsesSinglePage = class extends SinglePage {
  static {
    __name(this, "ToMarkdownCreateResponsesSinglePage");
  }
};
ToMarkdown2.ToMarkdownCreateResponsesSinglePage = ToMarkdownCreateResponsesSinglePage;
var Summary = class extends APIResource {
  static {
    __name(this, "Summary");
  }
  userAgent(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.userAgent({}, query);
    }
    return this._client.get("/radar/ai/bots/summary/user_agent", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
};
var Bots = class extends APIResource {
  static {
    __name(this, "Bots");
  }
  constructor() {
    super(...arguments);
    this.summary = new Summary(this._client);
  }
};
Bots.Summary = Summary;
var Summary2 = class extends APIResource {
  static {
    __name(this, "Summary2");
  }
  model(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.model({}, query);
    }
    return this._client.get("/radar/ai/inference/summary/model", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
  task(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.task({}, query);
    }
    return this._client.get("/radar/ai/inference/summary/task", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
};
var Summary3 = class extends APIResource {
  static {
    __name(this, "Summary3");
  }
  model(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.model({}, query);
    }
    return this._client.get("/radar/ai/inference/timeseries_groups/model", {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  task(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.task({}, query);
    }
    return this._client.get("/radar/ai/inference/timeseries_groups/task", {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
};
var TimeseriesGroups2 = class extends APIResource {
  static {
    __name(this, "TimeseriesGroups2");
  }
  constructor() {
    super(...arguments);
    this.summary = new Summary3(this._client);
  }
};
TimeseriesGroups2.Summary = Summary3;
var Inference = class extends APIResource {
  static {
    __name(this, "Inference");
  }
  constructor() {
    super(...arguments);
    this.summary = new Summary2(this._client);
    this.timeseriesGroups = new TimeseriesGroups2(this._client);
  }
};
Inference.Summary = Summary2;
Inference.TimeseriesGroups = TimeseriesGroups2;
var AI2 = class extends APIResource {
  static {
    __name(this, "AI2");
  }
  constructor() {
    super(...arguments);
    this.toMarkdown = new ToMarkdown2(this._client);
    this.inference = new Inference(this._client);
    this.bots = new Bots(this._client);
    this.timeseriesGroups = new TimeseriesGroups(this._client);
  }
};
AI2.ToMarkdown = ToMarkdown2;
AI2.ToMarkdownCreateResponsesSinglePage = ToMarkdownCreateResponsesSinglePage;
AI2.Inference = Inference;
AI2.Bots = Bots;
AI2.TimeseriesGroups = TimeseriesGroups;
var Outages = class extends APIResource {
  static {
    __name(this, "Outages");
  }
  get(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.get({}, query);
    }
    return this._client.get("/radar/annotations/outages", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
  locations(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.locations({}, query);
    }
    return this._client.get("/radar/annotations/outages/locations", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
};
var Annotations = class extends APIResource {
  static {
    __name(this, "Annotations");
  }
  constructor() {
    super(...arguments);
    this.outages = new Outages(this._client);
  }
  list(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.list({}, query);
    }
    return this._client.get("/radar/annotations", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
};
Annotations.Outages = Outages;
var Summary4 = class extends APIResource {
  static {
    __name(this, "Summary4");
  }
  dnssec(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.dnssec({}, query);
    }
    return this._client.get("/radar/as112/summary/dnssec", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
  edns(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.edns({}, query);
    }
    return this._client.get("/radar/as112/summary/edns", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
  ipVersion(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.ipVersion({}, query);
    }
    return this._client.get("/radar/as112/summary/ip_version", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
  protocol(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.protocol({}, query);
    }
    return this._client.get("/radar/as112/summary/protocol", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
  queryType(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.queryType({}, query);
    }
    return this._client.get("/radar/as112/summary/query_type", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
  responseCodes(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.responseCodes({}, query);
    }
    return this._client.get("/radar/as112/summary/response_codes", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
};
var TimeseriesGroups3 = class extends APIResource {
  static {
    __name(this, "TimeseriesGroups3");
  }
  dnssec(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.dnssec({}, query);
    }
    return this._client.get("/radar/as112/timeseries_groups/dnssec", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
  edns(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.edns({}, query);
    }
    return this._client.get("/radar/as112/timeseries_groups/edns", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
  ipVersion(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.ipVersion({}, query);
    }
    return this._client.get("/radar/as112/timeseries_groups/ip_version", {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  protocol(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.protocol({}, query);
    }
    return this._client.get("/radar/as112/timeseries_groups/protocol", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
  queryType(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.queryType({}, query);
    }
    return this._client.get("/radar/as112/timeseries_groups/query_type", {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  responseCodes(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.responseCodes({}, query);
    }
    return this._client.get("/radar/as112/timeseries_groups/response_codes", {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
};
var Top = class extends APIResource {
  static {
    __name(this, "Top");
  }
  dnssec(dnssec, query = {}, options) {
    if (isRequestOptions(query)) {
      return this.dnssec(dnssec, {}, query);
    }
    return this._client.get(`/radar/as112/top/locations/dnssec/${dnssec}`, {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  edns(edns, query = {}, options) {
    if (isRequestOptions(query)) {
      return this.edns(edns, {}, query);
    }
    return this._client.get(`/radar/as112/top/locations/edns/${edns}`, { query, ...options })._thenUnwrap((obj) => obj.result);
  }
  ipVersion(ipVersion, query = {}, options) {
    if (isRequestOptions(query)) {
      return this.ipVersion(ipVersion, {}, query);
    }
    return this._client.get(`/radar/as112/top/locations/ip_version/${ipVersion}`, {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  locations(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.locations({}, query);
    }
    return this._client.get("/radar/as112/top/locations", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
};
var AS112 = class extends APIResource {
  static {
    __name(this, "AS112");
  }
  constructor() {
    super(...arguments);
    this.summary = new Summary4(this._client);
    this.timeseriesGroups = new TimeseriesGroups3(this._client);
    this.top = new Top(this._client);
  }
  timeseries(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.timeseries({}, query);
    }
    return this._client.get("/radar/as112/timeseries", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
};
AS112.Summary = Summary4;
AS112.TimeseriesGroups = TimeseriesGroups3;
AS112.Top = Top;
var Summary5 = class extends APIResource {
  static {
    __name(this, "Summary5");
  }
  bitrate(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.bitrate({}, query);
    }
    return this._client.get("/radar/attacks/layer3/summary/bitrate", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
  duration(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.duration({}, query);
    }
    return this._client.get("/radar/attacks/layer3/summary/duration", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
  industry(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.industry({}, query);
    }
    return this._client.get("/radar/attacks/layer3/summary/industry", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
  ipVersion(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.ipVersion({}, query);
    }
    return this._client.get("/radar/attacks/layer3/summary/ip_version", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
  protocol(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.protocol({}, query);
    }
    return this._client.get("/radar/attacks/layer3/summary/protocol", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
  vector(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.vector({}, query);
    }
    return this._client.get("/radar/attacks/layer3/summary/vector", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
  vertical(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.vertical({}, query);
    }
    return this._client.get("/radar/attacks/layer3/summary/vertical", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
};
var TimeseriesGroups4 = class extends APIResource {
  static {
    __name(this, "TimeseriesGroups4");
  }
  bitrate(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.bitrate({}, query);
    }
    return this._client.get("/radar/attacks/layer3/timeseries_groups/bitrate", {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  duration(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.duration({}, query);
    }
    return this._client.get("/radar/attacks/layer3/timeseries_groups/duration", {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  industry(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.industry({}, query);
    }
    return this._client.get("/radar/attacks/layer3/timeseries_groups/industry", {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  ipVersion(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.ipVersion({}, query);
    }
    return this._client.get("/radar/attacks/layer3/timeseries_groups/ip_version", {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  protocol(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.protocol({}, query);
    }
    return this._client.get("/radar/attacks/layer3/timeseries_groups/protocol", {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  vector(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.vector({}, query);
    }
    return this._client.get("/radar/attacks/layer3/timeseries_groups/vector", {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  vertical(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.vertical({}, query);
    }
    return this._client.get("/radar/attacks/layer3/timeseries_groups/vertical", {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
};
var Locations = class extends APIResource {
  static {
    __name(this, "Locations");
  }
  origin(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.origin({}, query);
    }
    return this._client.get("/radar/attacks/layer3/top/locations/origin", {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  target(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.target({}, query);
    }
    return this._client.get("/radar/attacks/layer3/top/locations/target", {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
};
var Top2 = class extends APIResource {
  static {
    __name(this, "Top2");
  }
  constructor() {
    super(...arguments);
    this.locations = new Locations(this._client);
  }
  attacks(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.attacks({}, query);
    }
    return this._client.get("/radar/attacks/layer3/top/attacks", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
  industry(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.industry({}, query);
    }
    return this._client.get("/radar/attacks/layer3/top/industry", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
  vertical(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.vertical({}, query);
    }
    return this._client.get("/radar/attacks/layer3/top/vertical", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
};
Top2.Locations = Locations;
var Layer3 = class extends APIResource {
  static {
    __name(this, "Layer3");
  }
  constructor() {
    super(...arguments);
    this.summary = new Summary5(this._client);
    this.timeseriesGroups = new TimeseriesGroups4(this._client);
    this.top = new Top2(this._client);
  }
  timeseries(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.timeseries({}, query);
    }
    return this._client.get("/radar/attacks/layer3/timeseries", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
};
Layer3.Summary = Summary5;
Layer3.TimeseriesGroups = TimeseriesGroups4;
Layer3.Top = Top2;
var Summary6 = class extends APIResource {
  static {
    __name(this, "Summary6");
  }
  httpMethod(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.httpMethod({}, query);
    }
    return this._client.get("/radar/attacks/layer7/summary/http_method", {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  httpVersion(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.httpVersion({}, query);
    }
    return this._client.get("/radar/attacks/layer7/summary/http_version", {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  industry(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.industry({}, query);
    }
    return this._client.get("/radar/attacks/layer7/summary/industry", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
  ipVersion(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.ipVersion({}, query);
    }
    return this._client.get("/radar/attacks/layer7/summary/ip_version", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
  managedRules(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.managedRules({}, query);
    }
    return this._client.get("/radar/attacks/layer7/summary/managed_rules", {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  mitigationProduct(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.mitigationProduct({}, query);
    }
    return this._client.get("/radar/attacks/layer7/summary/mitigation_product", {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  vertical(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.vertical({}, query);
    }
    return this._client.get("/radar/attacks/layer7/summary/vertical", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
};
var TimeseriesGroups5 = class extends APIResource {
  static {
    __name(this, "TimeseriesGroups5");
  }
  httpMethod(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.httpMethod({}, query);
    }
    return this._client.get("/radar/attacks/layer7/timeseries_groups/http_method", {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  httpVersion(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.httpVersion({}, query);
    }
    return this._client.get("/radar/attacks/layer7/timeseries_groups/http_version", {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  industry(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.industry({}, query);
    }
    return this._client.get("/radar/attacks/layer7/timeseries_groups/industry", {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  ipVersion(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.ipVersion({}, query);
    }
    return this._client.get("/radar/attacks/layer7/timeseries_groups/ip_version", {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  managedRules(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.managedRules({}, query);
    }
    return this._client.get("/radar/attacks/layer7/timeseries_groups/managed_rules", {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  mitigationProduct(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.mitigationProduct({}, query);
    }
    return this._client.get("/radar/attacks/layer7/timeseries_groups/mitigation_product", {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  vertical(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.vertical({}, query);
    }
    return this._client.get("/radar/attacks/layer7/timeseries_groups/vertical", {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
};
var Ases = class extends APIResource {
  static {
    __name(this, "Ases");
  }
  origin(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.origin({}, query);
    }
    return this._client.get("/radar/attacks/layer7/top/ases/origin", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
};
var Locations2 = class extends APIResource {
  static {
    __name(this, "Locations2");
  }
  origin(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.origin({}, query);
    }
    return this._client.get("/radar/attacks/layer7/top/locations/origin", {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  target(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.target({}, query);
    }
    return this._client.get("/radar/attacks/layer7/top/locations/target", {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
};
var Top3 = class extends APIResource {
  static {
    __name(this, "Top3");
  }
  constructor() {
    super(...arguments);
    this.locations = new Locations2(this._client);
    this.ases = new Ases(this._client);
  }
  attacks(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.attacks({}, query);
    }
    return this._client.get("/radar/attacks/layer7/top/attacks", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
  industry(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.industry({}, query);
    }
    return this._client.get("/radar/attacks/layer7/top/industry", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
  vertical(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.vertical({}, query);
    }
    return this._client.get("/radar/attacks/layer7/top/vertical", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
};
Top3.Locations = Locations2;
Top3.Ases = Ases;
var Layer7 = class extends APIResource {
  static {
    __name(this, "Layer7");
  }
  constructor() {
    super(...arguments);
    this.summary = new Summary6(this._client);
    this.timeseriesGroups = new TimeseriesGroups5(this._client);
    this.top = new Top3(this._client);
  }
  timeseries(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.timeseries({}, query);
    }
    return this._client.get("/radar/attacks/layer7/timeseries", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
};
Layer7.Summary = Summary6;
Layer7.TimeseriesGroups = TimeseriesGroups5;
Layer7.Top = Top3;
var Attacks = class extends APIResource {
  static {
    __name(this, "Attacks");
  }
  constructor() {
    super(...arguments);
    this.layer3 = new Layer3(this._client);
    this.layer7 = new Layer7(this._client);
  }
};
Attacks.Layer3 = Layer3;
Attacks.Layer7 = Layer7;
var IPs4 = class extends APIResource {
  static {
    __name(this, "IPs4");
  }
  timeseries(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.timeseries({}, query);
    }
    return this._client.get("/radar/bgp/ips/timeseries", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
};
var Routes2 = class extends APIResource {
  static {
    __name(this, "Routes2");
  }
  ases(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.ases({}, query);
    }
    return this._client.get("/radar/bgp/routes/ases", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
  moas(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.moas({}, query);
    }
    return this._client.get("/radar/bgp/routes/moas", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
  pfx2as(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.pfx2as({}, query);
    }
    return this._client.get("/radar/bgp/routes/pfx2as", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
  realtime(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.realtime({}, query);
    }
    return this._client.get("/radar/bgp/routes/realtime", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
  stats(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.stats({}, query);
    }
    return this._client.get("/radar/bgp/routes/stats", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
};
var Events2 = class extends APIResource {
  static {
    __name(this, "Events2");
  }
  list(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.list({}, query);
    }
    return this._client.getAPIList("/radar/bgp/hijacks/events", EventListResponsesV4PagePagination, {
      query,
      ...options
    });
  }
};
var EventListResponsesV4PagePagination = class extends V4PagePagination {
  static {
    __name(this, "EventListResponsesV4PagePagination");
  }
};
Events2.EventListResponsesV4PagePagination = EventListResponsesV4PagePagination;
var Hijacks = class extends APIResource {
  static {
    __name(this, "Hijacks");
  }
  constructor() {
    super(...arguments);
    this.events = new Events2(this._client);
  }
};
Hijacks.Events = Events2;
Hijacks.EventListResponsesV4PagePagination = EventListResponsesV4PagePagination;
var Events3 = class extends APIResource {
  static {
    __name(this, "Events3");
  }
  list(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.list({}, query);
    }
    return this._client.getAPIList("/radar/bgp/leaks/events", EventListResponsesV4PagePagination2, {
      query,
      ...options
    });
  }
};
var EventListResponsesV4PagePagination2 = class extends V4PagePagination {
  static {
    __name(this, "EventListResponsesV4PagePagination2");
  }
};
Events3.EventListResponsesV4PagePagination = EventListResponsesV4PagePagination2;
var Leaks = class extends APIResource {
  static {
    __name(this, "Leaks");
  }
  constructor() {
    super(...arguments);
    this.events = new Events3(this._client);
  }
};
Leaks.Events = Events3;
Leaks.EventListResponsesV4PagePagination = EventListResponsesV4PagePagination2;
var Ases2 = class extends APIResource {
  static {
    __name(this, "Ases2");
  }
  get(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.get({}, query);
    }
    return this._client.get("/radar/bgp/top/ases", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
  prefixes(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.prefixes({}, query);
    }
    return this._client.get("/radar/bgp/top/ases/prefixes", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
};
var Top4 = class extends APIResource {
  static {
    __name(this, "Top4");
  }
  constructor() {
    super(...arguments);
    this.ases = new Ases2(this._client);
  }
  prefixes(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.prefixes({}, query);
    }
    return this._client.get("/radar/bgp/top/prefixes", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
};
Top4.Ases = Ases2;
var BGP = class extends APIResource {
  static {
    __name(this, "BGP");
  }
  constructor() {
    super(...arguments);
    this.leaks = new Leaks(this._client);
    this.top = new Top4(this._client);
    this.hijacks = new Hijacks(this._client);
    this.routes = new Routes2(this._client);
    this.ips = new IPs4(this._client);
  }
  timeseries(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.timeseries({}, query);
    }
    return this._client.get("/radar/bgp/timeseries", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
};
BGP.Leaks = Leaks;
BGP.Top = Top4;
BGP.Hijacks = Hijacks;
BGP.Routes = Routes2;
BGP.IPs = IPs4;
var WebCrawlers = class extends APIResource {
  static {
    __name(this, "WebCrawlers");
  }
  summary(dimension, query = {}, options) {
    if (isRequestOptions(query)) {
      return this.summary(dimension, {}, query);
    }
    return this._client.get(`/radar/bots/crawlers/summary/${dimension}`, {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  timeseriesGroups(dimension, query = {}, options) {
    if (isRequestOptions(query)) {
      return this.timeseriesGroups(dimension, {}, query);
    }
    return this._client.get(`/radar/bots/crawlers/timeseries_groups/${dimension}`, {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
};
var Bots2 = class extends APIResource {
  static {
    __name(this, "Bots2");
  }
  constructor() {
    super(...arguments);
    this.webCrawlers = new WebCrawlers(this._client);
  }
  list(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.list({}, query);
    }
    return this._client.get("/radar/bots", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
  get(botSlug, query = {}, options) {
    if (isRequestOptions(query)) {
      return this.get(botSlug, {}, query);
    }
    return this._client.get(`/radar/bots/${botSlug}`, { query, ...options })._thenUnwrap((obj) => obj.result);
  }
  summary(dimension, query = {}, options) {
    if (isRequestOptions(query)) {
      return this.summary(dimension, {}, query);
    }
    return this._client.get(`/radar/bots/summary/${dimension}`, { query, ...options })._thenUnwrap((obj) => obj.result);
  }
  timeseries(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.timeseries({}, query);
    }
    return this._client.get("/radar/bots/timeseries", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
  timeseriesGroups(dimension, query = {}, options) {
    if (isRequestOptions(query)) {
      return this.timeseriesGroups(dimension, {}, query);
    }
    return this._client.get(`/radar/bots/timeseries_groups/${dimension}`, {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
};
Bots2.WebCrawlers = WebCrawlers;
var Authorities = class extends APIResource {
  static {
    __name(this, "Authorities");
  }
  list(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.list({}, query);
    }
    return this._client.get("/radar/ct/authorities", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
  get(caSlug, query = {}, options) {
    if (isRequestOptions(query)) {
      return this.get(caSlug, {}, query);
    }
    return this._client.get(`/radar/ct/authorities/${caSlug}`, { query, ...options })._thenUnwrap((obj) => obj.result);
  }
};
var Logs6 = class extends APIResource {
  static {
    __name(this, "Logs6");
  }
  list(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.list({}, query);
    }
    return this._client.get("/radar/ct/logs", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
  get(logSlug, query = {}, options) {
    if (isRequestOptions(query)) {
      return this.get(logSlug, {}, query);
    }
    return this._client.get(`/radar/ct/logs/${logSlug}`, { query, ...options })._thenUnwrap((obj) => obj.result);
  }
};
var Ct = class extends APIResource {
  static {
    __name(this, "Ct");
  }
  constructor() {
    super(...arguments);
    this.authorities = new Authorities(this._client);
    this.logs = new Logs6(this._client);
  }
  summary(dimension, query = {}, options) {
    if (isRequestOptions(query)) {
      return this.summary(dimension, {}, query);
    }
    return this._client.get(`/radar/ct/summary/${dimension}`, { query, ...options })._thenUnwrap((obj) => obj.result);
  }
  timeseries(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.timeseries({}, query);
    }
    return this._client.get("/radar/ct/timeseries", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
  timeseriesGroups(dimension, query = {}, options) {
    if (isRequestOptions(query)) {
      return this.timeseriesGroups(dimension, {}, query);
    }
    return this._client.get(`/radar/ct/timeseries_groups/${dimension}`, { query, ...options })._thenUnwrap((obj) => obj.result);
  }
};
Ct.Authorities = Authorities;
Ct.Logs = Logs6;
var Summary7 = class extends APIResource {
  static {
    __name(this, "Summary7");
  }
  cacheHit(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.cacheHit({}, query);
    }
    return this._client.get("/radar/dns/summary/cache_hit", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
  dnssec(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.dnssec({}, query);
    }
    return this._client.get("/radar/dns/summary/dnssec", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
  dnssecAware(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.dnssecAware({}, query);
    }
    return this._client.get("/radar/dns/summary/dnssec_aware", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
  dnssecE2E(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.dnssecE2E({}, query);
    }
    return this._client.get("/radar/dns/summary/dnssec_e2e", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
  ipVersion(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.ipVersion({}, query);
    }
    return this._client.get("/radar/dns/summary/ip_version", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
  matchingAnswer(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.matchingAnswer({}, query);
    }
    return this._client.get("/radar/dns/summary/matching_answer", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
  protocol(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.protocol({}, query);
    }
    return this._client.get("/radar/dns/summary/protocol", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
  queryType(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.queryType({}, query);
    }
    return this._client.get("/radar/dns/summary/query_type", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
  responseCode(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.responseCode({}, query);
    }
    return this._client.get("/radar/dns/summary/response_code", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
  responseTTL(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.responseTTL({}, query);
    }
    return this._client.get("/radar/dns/summary/response_ttl", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
};
var TimeseriesGroups6 = class extends APIResource {
  static {
    __name(this, "TimeseriesGroups6");
  }
  cacheHit(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.cacheHit({}, query);
    }
    return this._client.get("/radar/dns/timeseries_groups/cache_hit", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
  dnssec(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.dnssec({}, query);
    }
    return this._client.get("/radar/dns/timeseries_groups/dnssec", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
  dnssecAware(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.dnssecAware({}, query);
    }
    return this._client.get("/radar/dns/timeseries_groups/dnssec_aware", {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  dnssecE2E(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.dnssecE2E({}, query);
    }
    return this._client.get("/radar/dns/timeseries_groups/dnssec_e2e", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
  ipVersion(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.ipVersion({}, query);
    }
    return this._client.get("/radar/dns/timeseries_groups/ip_version", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
  matchingAnswer(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.matchingAnswer({}, query);
    }
    return this._client.get("/radar/dns/timeseries_groups/matching_answer", {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  protocol(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.protocol({}, query);
    }
    return this._client.get("/radar/dns/timeseries_groups/protocol", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
  queryType(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.queryType({}, query);
    }
    return this._client.get("/radar/dns/timeseries_groups/query_type", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
  responseCode(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.responseCode({}, query);
    }
    return this._client.get("/radar/dns/timeseries_groups/response_code", {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  responseTTL(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.responseTTL({}, query);
    }
    return this._client.get("/radar/dns/timeseries_groups/response_ttl", {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
};
var Top5 = class extends APIResource {
  static {
    __name(this, "Top5");
  }
  ases(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.ases({}, query);
    }
    return this._client.get("/radar/dns/top/ases", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
  locations(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.locations({}, query);
    }
    return this._client.get("/radar/dns/top/locations", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
};
var DNS4 = class extends APIResource {
  static {
    __name(this, "DNS4");
  }
  constructor() {
    super(...arguments);
    this.top = new Top5(this._client);
    this.summary = new Summary7(this._client);
    this.timeseriesGroups = new TimeseriesGroups6(this._client);
  }
  timeseries(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.timeseries({}, query);
    }
    return this._client.get("/radar/dns/timeseries", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
};
DNS4.Top = Top5;
DNS4.Summary = Summary7;
DNS4.TimeseriesGroups = TimeseriesGroups6;
var Summary8 = class extends APIResource {
  static {
    __name(this, "Summary8");
  }
  arc(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.arc({}, query);
    }
    return this._client.get("/radar/email/routing/summary/arc", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
  dkim(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.dkim({}, query);
    }
    return this._client.get("/radar/email/routing/summary/dkim", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
  dmarc(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.dmarc({}, query);
    }
    return this._client.get("/radar/email/routing/summary/dmarc", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
  encrypted(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.encrypted({}, query);
    }
    return this._client.get("/radar/email/routing/summary/encrypted", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
  ipVersion(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.ipVersion({}, query);
    }
    return this._client.get("/radar/email/routing/summary/ip_version", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
  spf(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.spf({}, query);
    }
    return this._client.get("/radar/email/routing/summary/spf", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
};
var TimeseriesGroups7 = class extends APIResource {
  static {
    __name(this, "TimeseriesGroups7");
  }
  arc(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.arc({}, query);
    }
    return this._client.get("/radar/email/routing/timeseries_groups/arc", {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  dkim(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.dkim({}, query);
    }
    return this._client.get("/radar/email/routing/timeseries_groups/dkim", {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  dmarc(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.dmarc({}, query);
    }
    return this._client.get("/radar/email/routing/timeseries_groups/dmarc", {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  encrypted(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.encrypted({}, query);
    }
    return this._client.get("/radar/email/routing/timeseries_groups/encrypted", {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  ipVersion(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.ipVersion({}, query);
    }
    return this._client.get("/radar/email/routing/timeseries_groups/ip_version", {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  spf(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.spf({}, query);
    }
    return this._client.get("/radar/email/routing/timeseries_groups/spf", {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
};
var Routing = class extends APIResource {
  static {
    __name(this, "Routing");
  }
  constructor() {
    super(...arguments);
    this.summary = new Summary8(this._client);
    this.timeseriesGroups = new TimeseriesGroups7(this._client);
  }
};
Routing.Summary = Summary8;
Routing.TimeseriesGroups = TimeseriesGroups7;
var Summary9 = class extends APIResource {
  static {
    __name(this, "Summary9");
  }
  arc(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.arc({}, query);
    }
    return this._client.get("/radar/email/security/summary/arc", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
  dkim(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.dkim({}, query);
    }
    return this._client.get("/radar/email/security/summary/dkim", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
  dmarc(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.dmarc({}, query);
    }
    return this._client.get("/radar/email/security/summary/dmarc", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
  malicious(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.malicious({}, query);
    }
    return this._client.get("/radar/email/security/summary/malicious", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
  spam(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.spam({}, query);
    }
    return this._client.get("/radar/email/security/summary/spam", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
  spf(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.spf({}, query);
    }
    return this._client.get("/radar/email/security/summary/spf", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
  spoof(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.spoof({}, query);
    }
    return this._client.get("/radar/email/security/summary/spoof", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
  threatCategory(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.threatCategory({}, query);
    }
    return this._client.get("/radar/email/security/summary/threat_category", {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  tlsVersion(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.tlsVersion({}, query);
    }
    return this._client.get("/radar/email/security/summary/tls_version", {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
};
var TimeseriesGroups8 = class extends APIResource {
  static {
    __name(this, "TimeseriesGroups8");
  }
  arc(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.arc({}, query);
    }
    return this._client.get("/radar/email/security/timeseries_groups/arc", {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  dkim(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.dkim({}, query);
    }
    return this._client.get("/radar/email/security/timeseries_groups/dkim", {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  dmarc(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.dmarc({}, query);
    }
    return this._client.get("/radar/email/security/timeseries_groups/dmarc", {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  malicious(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.malicious({}, query);
    }
    return this._client.get("/radar/email/security/timeseries_groups/malicious", {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  spam(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.spam({}, query);
    }
    return this._client.get("/radar/email/security/timeseries_groups/spam", {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  spf(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.spf({}, query);
    }
    return this._client.get("/radar/email/security/timeseries_groups/spf", {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  spoof(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.spoof({}, query);
    }
    return this._client.get("/radar/email/security/timeseries_groups/spoof", {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  threatCategory(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.threatCategory({}, query);
    }
    return this._client.get("/radar/email/security/timeseries_groups/threat_category", {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  tlsVersion(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.tlsVersion({}, query);
    }
    return this._client.get("/radar/email/security/timeseries_groups/tls_version", {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
};
var Malicious = class extends APIResource {
  static {
    __name(this, "Malicious");
  }
  get(malicious, query = {}, options) {
    if (isRequestOptions(query)) {
      return this.get(malicious, {}, query);
    }
    return this._client.get(`/radar/email/security/top/tlds/malicious/${malicious}`, {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
};
var Spam = class extends APIResource {
  static {
    __name(this, "Spam");
  }
  get(spam, query = {}, options) {
    if (isRequestOptions(query)) {
      return this.get(spam, {}, query);
    }
    return this._client.get(`/radar/email/security/top/tlds/spam/${spam}`, {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
};
var Spoof = class extends APIResource {
  static {
    __name(this, "Spoof");
  }
  get(spoof, query = {}, options) {
    if (isRequestOptions(query)) {
      return this.get(spoof, {}, query);
    }
    return this._client.get(`/radar/email/security/top/tlds/spoof/${spoof}`, {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
};
var Tlds = class extends APIResource {
  static {
    __name(this, "Tlds");
  }
  constructor() {
    super(...arguments);
    this.malicious = new Malicious(this._client);
    this.spam = new Spam(this._client);
    this.spoof = new Spoof(this._client);
  }
  get(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.get({}, query);
    }
    return this._client.get("/radar/email/security/top/tlds", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
};
Tlds.Malicious = Malicious;
Tlds.Spam = Spam;
Tlds.Spoof = Spoof;
var Top6 = class extends APIResource {
  static {
    __name(this, "Top6");
  }
  constructor() {
    super(...arguments);
    this.tlds = new Tlds(this._client);
  }
};
Top6.Tlds = Tlds;
var Security = class extends APIResource {
  static {
    __name(this, "Security");
  }
  constructor() {
    super(...arguments);
    this.top = new Top6(this._client);
    this.summary = new Summary9(this._client);
    this.timeseriesGroups = new TimeseriesGroups8(this._client);
  }
};
Security.Top = Top6;
Security.Summary = Summary9;
Security.TimeseriesGroups = TimeseriesGroups8;
var Email = class extends APIResource {
  static {
    __name(this, "Email");
  }
  constructor() {
    super(...arguments);
    this.routing = new Routing(this._client);
    this.security = new Security(this._client);
  }
};
Email.Routing = Routing;
Email.Security = Security;
var ASNs = class extends APIResource {
  static {
    __name(this, "ASNs");
  }
  list(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.list({}, query);
    }
    return this._client.get("/radar/entities/asns", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
  asSet(asn, query = {}, options) {
    if (isRequestOptions(query)) {
      return this.asSet(asn, {}, query);
    }
    return this._client.get(`/radar/entities/asns/${asn}/as_set`, { query, ...options })._thenUnwrap((obj) => obj.result);
  }
  get(asn, query = {}, options) {
    if (isRequestOptions(query)) {
      return this.get(asn, {}, query);
    }
    return this._client.get(`/radar/entities/asns/${asn}`, { query, ...options })._thenUnwrap((obj) => obj.result);
  }
  ip(query, options) {
    return this._client.get("/radar/entities/asns/ip", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
  rel(asn, query = {}, options) {
    if (isRequestOptions(query)) {
      return this.rel(asn, {}, query);
    }
    return this._client.get(`/radar/entities/asns/${asn}/rel`, { query, ...options })._thenUnwrap((obj) => obj.result);
  }
};
var Locations3 = class extends APIResource {
  static {
    __name(this, "Locations3");
  }
  list(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.list({}, query);
    }
    return this._client.get("/radar/entities/locations", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
  get(location2, query = {}, options) {
    if (isRequestOptions(query)) {
      return this.get(location2, {}, query);
    }
    return this._client.get(`/radar/entities/locations/${location2}`, { query, ...options })._thenUnwrap((obj) => obj.result);
  }
};
var Entities = class extends APIResource {
  static {
    __name(this, "Entities");
  }
  constructor() {
    super(...arguments);
    this.asns = new ASNs(this._client);
    this.locations = new Locations3(this._client);
  }
  get(query, options) {
    return this._client.get("/radar/entities/ip", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
};
Entities.ASNs = ASNs;
Entities.Locations = Locations3;
var Summary10 = class extends APIResource {
  static {
    __name(this, "Summary10");
  }
  botClass(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.botClass({}, query);
    }
    return this._client.get("/radar/http/summary/bot_class", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
  deviceType(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.deviceType({}, query);
    }
    return this._client.get("/radar/http/summary/device_type", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
  httpProtocol(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.httpProtocol({}, query);
    }
    return this._client.get("/radar/http/summary/http_protocol", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
  httpVersion(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.httpVersion({}, query);
    }
    return this._client.get("/radar/http/summary/http_version", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
  ipVersion(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.ipVersion({}, query);
    }
    return this._client.get("/radar/http/summary/ip_version", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
  os(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.os({}, query);
    }
    return this._client.get("/radar/http/summary/os", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
  postQuantum(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.postQuantum({}, query);
    }
    return this._client.get("/radar/http/summary/post_quantum", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
  tlsVersion(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.tlsVersion({}, query);
    }
    return this._client.get("/radar/http/summary/tls_version", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
};
var TimeseriesGroups9 = class extends APIResource {
  static {
    __name(this, "TimeseriesGroups9");
  }
  botClass(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.botClass({}, query);
    }
    return this._client.get("/radar/http/timeseries_groups/bot_class", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
  browser(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.browser({}, query);
    }
    return this._client.get("/radar/http/timeseries_groups/browser", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
  browserFamily(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.browserFamily({}, query);
    }
    return this._client.get("/radar/http/timeseries_groups/browser_family", {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  deviceType(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.deviceType({}, query);
    }
    return this._client.get("/radar/http/timeseries_groups/device_type", {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  httpProtocol(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.httpProtocol({}, query);
    }
    return this._client.get("/radar/http/timeseries_groups/http_protocol", {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  httpVersion(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.httpVersion({}, query);
    }
    return this._client.get("/radar/http/timeseries_groups/http_version", {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  ipVersion(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.ipVersion({}, query);
    }
    return this._client.get("/radar/http/timeseries_groups/ip_version", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
  os(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.os({}, query);
    }
    return this._client.get("/radar/http/timeseries_groups/os", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
  postQuantum(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.postQuantum({}, query);
    }
    return this._client.get("/radar/http/timeseries_groups/post_quantum", {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  tlsVersion(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.tlsVersion({}, query);
    }
    return this._client.get("/radar/http/timeseries_groups/tls_version", {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
};
var Top7 = class extends APIResource {
  static {
    __name(this, "Top7");
  }
  browser(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.browser({}, query);
    }
    return this._client.get("/radar/http/top/browser", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
  browserFamily(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.browserFamily({}, query);
    }
    return this._client.get("/radar/http/top/browser_family", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
};
var BotClass = class extends APIResource {
  static {
    __name(this, "BotClass");
  }
  get(botClass, query = {}, options) {
    if (isRequestOptions(query)) {
      return this.get(botClass, {}, query);
    }
    return this._client.get(`/radar/http/top/ases/bot_class/${botClass}`, {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
};
var BrowserFamily = class extends APIResource {
  static {
    __name(this, "BrowserFamily");
  }
  get(browserFamily, query = {}, options) {
    if (isRequestOptions(query)) {
      return this.get(browserFamily, {}, query);
    }
    return this._client.get(`/radar/http/top/ases/browser_family/${browserFamily}`, {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
};
var DeviceType = class extends APIResource {
  static {
    __name(this, "DeviceType");
  }
  get(deviceType, query = {}, options) {
    if (isRequestOptions(query)) {
      return this.get(deviceType, {}, query);
    }
    return this._client.get(`/radar/http/top/ases/device_type/${deviceType}`, {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
};
var HTTPMethod = class extends APIResource {
  static {
    __name(this, "HTTPMethod");
  }
  get(httpVersion, query = {}, options) {
    if (isRequestOptions(query)) {
      return this.get(httpVersion, {}, query);
    }
    return this._client.get(`/radar/http/top/ases/http_version/${httpVersion}`, {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
};
var HTTPProtocol = class extends APIResource {
  static {
    __name(this, "HTTPProtocol");
  }
  get(httpProtocol, query = {}, options) {
    if (isRequestOptions(query)) {
      return this.get(httpProtocol, {}, query);
    }
    return this._client.get(`/radar/http/top/ases/http_protocol/${httpProtocol}`, {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
};
var IPVersion = class extends APIResource {
  static {
    __name(this, "IPVersion");
  }
  get(ipVersion, query = {}, options) {
    if (isRequestOptions(query)) {
      return this.get(ipVersion, {}, query);
    }
    return this._client.get(`/radar/http/top/ases/ip_version/${ipVersion}`, {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
};
var OS = class extends APIResource {
  static {
    __name(this, "OS");
  }
  get(os, query = {}, options) {
    if (isRequestOptions(query)) {
      return this.get(os, {}, query);
    }
    return this._client.get(`/radar/http/top/ases/os/${os}`, { query, ...options })._thenUnwrap((obj) => obj.result);
  }
};
var TLSVersion = class extends APIResource {
  static {
    __name(this, "TLSVersion");
  }
  get(tlsVersion, query = {}, options) {
    if (isRequestOptions(query)) {
      return this.get(tlsVersion, {}, query);
    }
    return this._client.get(`/radar/http/top/ases/tls_version/${tlsVersion}`, {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
};
var Ases3 = class extends APIResource {
  static {
    __name(this, "Ases3");
  }
  constructor() {
    super(...arguments);
    this.botClass = new BotClass(this._client);
    this.deviceType = new DeviceType(this._client);
    this.httpProtocol = new HTTPProtocol(this._client);
    this.httpMethod = new HTTPMethod(this._client);
    this.ipVersion = new IPVersion(this._client);
    this.os = new OS(this._client);
    this.tlsVersion = new TLSVersion(this._client);
    this.browserFamily = new BrowserFamily(this._client);
  }
  get(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.get({}, query);
    }
    return this._client.get("/radar/http/top/ases", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
};
Ases3.BotClass = BotClass;
Ases3.DeviceType = DeviceType;
Ases3.HTTPProtocol = HTTPProtocol;
Ases3.HTTPMethod = HTTPMethod;
Ases3.IPVersion = IPVersion;
Ases3.OS = OS;
Ases3.TLSVersion = TLSVersion;
Ases3.BrowserFamily = BrowserFamily;
var BotClass2 = class extends APIResource {
  static {
    __name(this, "BotClass2");
  }
  get(botClass, query = {}, options) {
    if (isRequestOptions(query)) {
      return this.get(botClass, {}, query);
    }
    return this._client.get(`/radar/http/top/locations/bot_class/${botClass}`, {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
};
var BrowserFamily2 = class extends APIResource {
  static {
    __name(this, "BrowserFamily2");
  }
  get(browserFamily, query = {}, options) {
    if (isRequestOptions(query)) {
      return this.get(browserFamily, {}, query);
    }
    return this._client.get(`/radar/http/top/locations/browser_family/${browserFamily}`, {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
};
var DeviceType2 = class extends APIResource {
  static {
    __name(this, "DeviceType2");
  }
  get(deviceType, query = {}, options) {
    if (isRequestOptions(query)) {
      return this.get(deviceType, {}, query);
    }
    return this._client.get(`/radar/http/top/locations/device_type/${deviceType}`, {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
};
var HTTPMethod2 = class extends APIResource {
  static {
    __name(this, "HTTPMethod2");
  }
  get(httpVersion, query = {}, options) {
    if (isRequestOptions(query)) {
      return this.get(httpVersion, {}, query);
    }
    return this._client.get(`/radar/http/top/locations/http_version/${httpVersion}`, {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
};
var HTTPProtocol2 = class extends APIResource {
  static {
    __name(this, "HTTPProtocol2");
  }
  get(httpProtocol, query = {}, options) {
    if (isRequestOptions(query)) {
      return this.get(httpProtocol, {}, query);
    }
    return this._client.get(`/radar/http/top/locations/http_protocol/${httpProtocol}`, {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
};
var IPVersion2 = class extends APIResource {
  static {
    __name(this, "IPVersion2");
  }
  get(ipVersion, query = {}, options) {
    if (isRequestOptions(query)) {
      return this.get(ipVersion, {}, query);
    }
    return this._client.get(`/radar/http/top/locations/ip_version/${ipVersion}`, {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
};
var OS2 = class extends APIResource {
  static {
    __name(this, "OS2");
  }
  get(os, query = {}, options) {
    if (isRequestOptions(query)) {
      return this.get(os, {}, query);
    }
    return this._client.get(`/radar/http/top/locations/os/${os}`, { query, ...options })._thenUnwrap((obj) => obj.result);
  }
};
var TLSVersion2 = class extends APIResource {
  static {
    __name(this, "TLSVersion2");
  }
  get(tlsVersion, query = {}, options) {
    if (isRequestOptions(query)) {
      return this.get(tlsVersion, {}, query);
    }
    return this._client.get(`/radar/http/top/locations/tls_version/${tlsVersion}`, {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
};
var Locations4 = class extends APIResource {
  static {
    __name(this, "Locations4");
  }
  constructor() {
    super(...arguments);
    this.botClass = new BotClass2(this._client);
    this.deviceType = new DeviceType2(this._client);
    this.httpProtocol = new HTTPProtocol2(this._client);
    this.httpMethod = new HTTPMethod2(this._client);
    this.ipVersion = new IPVersion2(this._client);
    this.os = new OS2(this._client);
    this.tlsVersion = new TLSVersion2(this._client);
    this.browserFamily = new BrowserFamily2(this._client);
  }
  get(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.get({}, query);
    }
    return this._client.get("/radar/http/top/locations", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
};
Locations4.BotClass = BotClass2;
Locations4.DeviceType = DeviceType2;
Locations4.HTTPProtocol = HTTPProtocol2;
Locations4.HTTPMethod = HTTPMethod2;
Locations4.IPVersion = IPVersion2;
Locations4.OS = OS2;
Locations4.TLSVersion = TLSVersion2;
Locations4.BrowserFamily = BrowserFamily2;
var HTTP = class extends APIResource {
  static {
    __name(this, "HTTP");
  }
  constructor() {
    super(...arguments);
    this.locations = new Locations4(this._client);
    this.ases = new Ases3(this._client);
    this.summary = new Summary10(this._client);
    this.timeseriesGroups = new TimeseriesGroups9(this._client);
    this.top = new Top7(this._client);
  }
  timeseries(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.timeseries({}, query);
    }
    return this._client.get("/radar/http/timeseries", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
};
HTTP.Locations = Locations4;
HTTP.Ases = Ases3;
HTTP.Summary = Summary10;
HTTP.TimeseriesGroups = TimeseriesGroups9;
HTTP.Top = Top7;
var Summary11 = class extends APIResource {
  static {
    __name(this, "Summary11");
  }
  botClass(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.botClass({}, query);
    }
    return this._client.get("/radar/leaked_credential_checks/summary/bot_class", {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  compromised(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.compromised({}, query);
    }
    return this._client.get("/radar/leaked_credential_checks/summary/compromised", {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
};
var TimeseriesGroups10 = class extends APIResource {
  static {
    __name(this, "TimeseriesGroups10");
  }
  botClass(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.botClass({}, query);
    }
    return this._client.get("/radar/leaked_credential_checks/timeseries_groups/bot_class", {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  compromised(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.compromised({}, query);
    }
    return this._client.get("/radar/leaked_credential_checks/timeseries_groups/compromised", {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
};
var LeakedCredentials = class extends APIResource {
  static {
    __name(this, "LeakedCredentials");
  }
  constructor() {
    super(...arguments);
    this.summary = new Summary11(this._client);
    this.timeseriesGroups = new TimeseriesGroups10(this._client);
  }
};
LeakedCredentials.Summary = Summary11;
LeakedCredentials.TimeseriesGroups = TimeseriesGroups10;
var Top8 = class extends APIResource {
  static {
    __name(this, "Top8");
  }
  ases(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.ases({}, query);
    }
    return this._client.get("/radar/netflows/top/ases", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
  locations(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.locations({}, query);
    }
    return this._client.get("/radar/netflows/top/locations", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
};
var Netflows = class extends APIResource {
  static {
    __name(this, "Netflows");
  }
  constructor() {
    super(...arguments);
    this.top = new Top8(this._client);
  }
  summary(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.summary({}, query);
    }
    return this._client.get("/radar/netflows/summary", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
  timeseries(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.timeseries({}, query);
    }
    return this._client.get("/radar/netflows/timeseries", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
};
Netflows.Top = Top8;
var IQI = class extends APIResource {
  static {
    __name(this, "IQI");
  }
  summary(query, options) {
    return this._client.get("/radar/quality/iqi/summary", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
  timeseriesGroups(query, options) {
    return this._client.get("/radar/quality/iqi/timeseries_groups", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
};
var Top9 = class extends APIResource {
  static {
    __name(this, "Top9");
  }
  ases(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.ases({}, query);
    }
    return this._client.get("/radar/quality/speed/top/ases", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
  locations(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.locations({}, query);
    }
    return this._client.get("/radar/quality/speed/top/locations", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
};
var Speed = class extends APIResource {
  static {
    __name(this, "Speed");
  }
  constructor() {
    super(...arguments);
    this.top = new Top9(this._client);
  }
  histogram(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.histogram({}, query);
    }
    return this._client.get("/radar/quality/speed/histogram", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
  summary(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.summary({}, query);
    }
    return this._client.get("/radar/quality/speed/summary", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
};
Speed.Top = Top9;
var Quality = class extends APIResource {
  static {
    __name(this, "Quality");
  }
  constructor() {
    super(...arguments);
    this.iqi = new IQI(this._client);
    this.speed = new Speed(this._client);
  }
};
Quality.IQI = IQI;
Quality.Speed = Speed;
var Domain = class extends APIResource {
  static {
    __name(this, "Domain");
  }
  get(domain2, query = {}, options) {
    if (isRequestOptions(query)) {
      return this.get(domain2, {}, query);
    }
    return this._client.get(`/radar/ranking/domain/${domain2}`, { query, ...options })._thenUnwrap((obj) => obj.result);
  }
};
var InternetServices = class extends APIResource {
  static {
    __name(this, "InternetServices");
  }
  categories(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.categories({}, query);
    }
    return this._client.get("/radar/ranking/internet_services/categories", {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  timeseriesGroups(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.timeseriesGroups({}, query);
    }
    return this._client.get("/radar/ranking/internet_services/timeseries_groups", {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  top(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.top({}, query);
    }
    return this._client.get("/radar/ranking/internet_services/top", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
};
var Ranking = class extends APIResource {
  static {
    __name(this, "Ranking");
  }
  constructor() {
    super(...arguments);
    this.domain = new Domain(this._client);
    this.internetServices = new InternetServices(this._client);
  }
  timeseriesGroups(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.timeseriesGroups({}, query);
    }
    return this._client.get("/radar/ranking/timeseries_groups", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
  top(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.top({}, query);
    }
    return this._client.get("/radar/ranking/top", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
};
Ranking.Domain = Domain;
Ranking.InternetServices = InternetServices;
var UserAgents = class extends APIResource {
  static {
    __name(this, "UserAgents");
  }
  directive(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.directive({}, query);
    }
    return this._client.get("/radar/robots_txt/top/user_agents/directive", {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
};
var Top10 = class extends APIResource {
  static {
    __name(this, "Top10");
  }
  constructor() {
    super(...arguments);
    this.userAgents = new UserAgents(this._client);
  }
  domainCategories(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.domainCategories({}, query);
    }
    return this._client.get("/radar/robots_txt/top/domain_categories", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
};
Top10.UserAgents = UserAgents;
var RobotsTXT = class extends APIResource {
  static {
    __name(this, "RobotsTXT");
  }
  constructor() {
    super(...arguments);
    this.top = new Top10(this._client);
  }
};
RobotsTXT.Top = Top10;
var Locations5 = class extends APIResource {
  static {
    __name(this, "Locations5");
  }
  get(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.get({}, query);
    }
    return this._client.get("/radar/traffic_anomalies/locations", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
};
var TrafficAnomalies = class extends APIResource {
  static {
    __name(this, "TrafficAnomalies");
  }
  constructor() {
    super(...arguments);
    this.locations = new Locations5(this._client);
  }
  get(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.get({}, query);
    }
    return this._client.get("/radar/traffic_anomalies", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
};
TrafficAnomalies.Locations = Locations5;
var Top11 = class extends APIResource {
  static {
    __name(this, "Top11");
  }
  bots(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.bots({}, query);
    }
    return this._client.get("/radar/verified_bots/top/bots", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
  categories(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.categories({}, query);
    }
    return this._client.get("/radar/verified_bots/top/categories", { query, ...options })._thenUnwrap((obj) => obj.result);
  }
};
var VerifiedBots = class extends APIResource {
  static {
    __name(this, "VerifiedBots");
  }
  constructor() {
    super(...arguments);
    this.top = new Top11(this._client);
  }
};
VerifiedBots.Top = Top11;
var Radar = class extends APIResource {
  static {
    __name(this, "Radar");
  }
  constructor() {
    super(...arguments);
    this.ai = new AI2(this._client);
    this.ct = new Ct(this._client);
    this.annotations = new Annotations(this._client);
    this.bgp = new BGP(this._client);
    this.bots = new Bots2(this._client);
    this.datasets = new Datasets4(this._client);
    this.dns = new DNS4(this._client);
    this.netflows = new Netflows(this._client);
    this.search = new Search(this._client);
    this.verifiedBots = new VerifiedBots(this._client);
    this.as112 = new AS112(this._client);
    this.email = new Email(this._client);
    this.attacks = new Attacks(this._client);
    this.entities = new Entities(this._client);
    this.http = new HTTP(this._client);
    this.quality = new Quality(this._client);
    this.ranking = new Ranking(this._client);
    this.trafficAnomalies = new TrafficAnomalies(this._client);
    this.tcpResetsTimeouts = new TCPResetsTimeouts(this._client);
    this.robotsTXT = new RobotsTXT(this._client);
    this.leakedCredentials = new LeakedCredentials(this._client);
  }
};
Radar.AI = AI2;
Radar.Ct = Ct;
Radar.Annotations = Annotations;
Radar.BGP = BGP;
Radar.Bots = Bots2;
Radar.Datasets = Datasets4;
Radar.DNS = DNS4;
Radar.Netflows = Netflows;
Radar.Search = Search;
Radar.VerifiedBots = VerifiedBots;
Radar.AS112 = AS112;
Radar.Email = Email;
Radar.Attacks = Attacks;
Radar.Entities = Entities;
Radar.HTTP = HTTP;
Radar.Quality = Quality;
Radar.Ranking = Ranking;
Radar.TrafficAnomalies = TrafficAnomalies;
Radar.TCPResetsTimeouts = TCPResetsTimeouts;
Radar.RobotsTXT = RobotsTXT;
Radar.LeakedCredentials = LeakedCredentials;
var RateLimits = class extends APIResource {
  static {
    __name(this, "RateLimits");
  }
  create(params, options) {
    const { zone_id, ...body } = params;
    return this._client.post(`/zones/${zone_id}/rate_limits`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { zone_id, ...query } = params;
    return this._client.getAPIList(`/zones/${zone_id}/rate_limits`, RateLimitsV4PagePaginationArray, {
      query,
      ...options
    });
  }
  delete(rateLimitId, params, options) {
    const { zone_id } = params;
    return this._client.delete(`/zones/${zone_id}/rate_limits/${rateLimitId}`, options)._thenUnwrap((obj) => obj.result);
  }
  edit(rateLimitId, params, options) {
    const { zone_id, ...body } = params;
    return this._client.put(`/zones/${zone_id}/rate_limits/${rateLimitId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  get(rateLimitId, params, options) {
    const { zone_id } = params;
    return this._client.get(`/zones/${zone_id}/rate_limits/${rateLimitId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var RateLimitsV4PagePaginationArray = class extends V4PagePaginationArray {
  static {
    __name(this, "RateLimitsV4PagePaginationArray");
  }
};
RateLimits.RateLimitsV4PagePaginationArray = RateLimitsV4PagePaginationArray;
var Domains5 = class extends APIResource {
  static {
    __name(this, "Domains5");
  }
  update(domainName, params, options) {
    const { account_id, ...body } = params;
    return this._client.put(`/accounts/${account_id}/registrar/domains/${domainName}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { account_id } = params;
    return this._client.getAPIList(`/accounts/${account_id}/registrar/domains`, DomainsSinglePage, options);
  }
  get(domainName, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/registrar/domains/${domainName}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var DomainsSinglePage = class extends SinglePage {
  static {
    __name(this, "DomainsSinglePage");
  }
};
Domains5.DomainsSinglePage = DomainsSinglePage;
var Registrar = class extends APIResource {
  static {
    __name(this, "Registrar");
  }
  constructor() {
    super(...arguments);
    this.domains = new Domains5(this._client);
  }
};
Registrar.Domains = Domains5;
Registrar.DomainsSinglePage = DomainsSinglePage;
var Traces = class extends APIResource {
  static {
    __name(this, "Traces");
  }
  create(params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/request-tracer/trace`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
};
var RequestTracers = class extends APIResource {
  static {
    __name(this, "RequestTracers");
  }
  constructor() {
    super(...arguments);
    this.traces = new Traces(this._client);
  }
};
RequestTracers.Traces = Traces;
var Recipients = class extends APIResource {
  static {
    __name(this, "Recipients");
  }
  create(shareId, params, options) {
    const { path_account_id, ...body } = params;
    return this._client.post(`/accounts/${path_account_id}/shares/${shareId}/recipients`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(shareId, params, options) {
    const { account_id, ...query } = params;
    return this._client.getAPIList(`/accounts/${account_id}/shares/${shareId}/recipients`, RecipientListResponsesV4PagePaginationArray, { query, ...options });
  }
  delete(shareId, recipientId, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/shares/${shareId}/recipients/${recipientId}`, options)._thenUnwrap((obj) => obj.result);
  }
  get(shareId, recipientId, params, options) {
    const { account_id, ...query } = params;
    return this._client.get(`/accounts/${account_id}/shares/${shareId}/recipients/${recipientId}`, {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
};
var RecipientListResponsesV4PagePaginationArray = class extends V4PagePaginationArray {
  static {
    __name(this, "RecipientListResponsesV4PagePaginationArray");
  }
};
Recipients.RecipientListResponsesV4PagePaginationArray = RecipientListResponsesV4PagePaginationArray;
var Resources2 = class extends APIResource {
  static {
    __name(this, "Resources2");
  }
  create(shareId, params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/shares/${shareId}/resources`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  update(shareId, resourceId, params, options) {
    const { account_id, ...body } = params;
    return this._client.put(`/accounts/${account_id}/shares/${shareId}/resources/${resourceId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(shareId, params, options) {
    const { account_id, ...query } = params;
    return this._client.getAPIList(`/accounts/${account_id}/shares/${shareId}/resources`, ResourceListResponsesV4PagePaginationArray2, { query, ...options });
  }
  delete(shareId, resourceId, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/shares/${shareId}/resources/${resourceId}`, options)._thenUnwrap((obj) => obj.result);
  }
  get(shareId, resourceId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/shares/${shareId}/resources/${resourceId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var ResourceListResponsesV4PagePaginationArray2 = class extends V4PagePaginationArray {
  static {
    __name(this, "ResourceListResponsesV4PagePaginationArray2");
  }
};
Resources2.ResourceListResponsesV4PagePaginationArray = ResourceListResponsesV4PagePaginationArray2;
var ResourceSharing = class extends APIResource {
  static {
    __name(this, "ResourceSharing");
  }
  constructor() {
    super(...arguments);
    this.recipients = new Recipients(this._client);
    this.resources = new Resources2(this._client);
  }
  create(params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/shares`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  update(shareId, params, options) {
    const { account_id, ...body } = params;
    return this._client.put(`/accounts/${account_id}/shares/${shareId}`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { account_id, ...query } = params;
    return this._client.getAPIList(`/accounts/${account_id}/shares`, ResourceSharingListResponsesV4PagePaginationArray, { query, ...options });
  }
  delete(shareId, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/shares/${shareId}`, options)._thenUnwrap((obj) => obj.result);
  }
  get(shareId, params, options) {
    const { account_id, ...query } = params;
    return this._client.get(`/accounts/${account_id}/shares/${shareId}`, {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
};
var ResourceSharingListResponsesV4PagePaginationArray = class extends V4PagePaginationArray {
  static {
    __name(this, "ResourceSharingListResponsesV4PagePaginationArray");
  }
};
ResourceSharing.ResourceSharingListResponsesV4PagePaginationArray = ResourceSharingListResponsesV4PagePaginationArray;
ResourceSharing.Recipients = Recipients;
ResourceSharing.RecipientListResponsesV4PagePaginationArray = RecipientListResponsesV4PagePaginationArray;
ResourceSharing.Resources = Resources2;
ResourceSharing.ResourceListResponsesV4PagePaginationArray = ResourceListResponsesV4PagePaginationArray2;
var BulkOperations = class extends APIResource {
  static {
    __name(this, "BulkOperations");
  }
  get(operationId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/rules/lists/bulk_operations/${operationId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var Items = class extends APIResource {
  static {
    __name(this, "Items");
  }
  create(listId, params, options) {
    const { account_id, body } = params;
    return this._client.post(`/accounts/${account_id}/rules/lists/${listId}/items`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  update(listId, params, options) {
    const { account_id, body } = params;
    return this._client.put(`/accounts/${account_id}/rules/lists/${listId}/items`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(listId, params, options) {
    const { account_id, ...query } = params;
    return this._client.getAPIList(`/accounts/${account_id}/rules/lists/${listId}/items`, ItemListResponsesCursorPaginationAfter, { query, ...options });
  }
  delete(listId, params, options) {
    const { account_id, ...body } = params;
    return this._client.delete(`/accounts/${account_id}/rules/lists/${listId}/items`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  get(listId, itemId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/rules/lists/${listId}/items/${itemId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var ItemListResponsesCursorPaginationAfter = class extends CursorPaginationAfter {
  static {
    __name(this, "ItemListResponsesCursorPaginationAfter");
  }
};
Items.ItemListResponsesCursorPaginationAfter = ItemListResponsesCursorPaginationAfter;
var Lists = class extends APIResource {
  static {
    __name(this, "Lists");
  }
  constructor() {
    super(...arguments);
    this.bulkOperations = new BulkOperations(this._client);
    this.items = new Items(this._client);
  }
  create(params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/rules/lists`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  update(listId, params, options) {
    const { account_id, ...body } = params;
    return this._client.put(`/accounts/${account_id}/rules/lists/${listId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { account_id } = params;
    return this._client.getAPIList(`/accounts/${account_id}/rules/lists`, ListsListsSinglePage, options);
  }
  delete(listId, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/rules/lists/${listId}`, options)._thenUnwrap((obj) => obj.result);
  }
  get(listId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/rules/lists/${listId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var ListsListsSinglePage = class extends SinglePage {
  static {
    __name(this, "ListsListsSinglePage");
  }
};
Lists.ListsListsSinglePage = ListsListsSinglePage;
Lists.BulkOperations = BulkOperations;
Lists.Items = Items;
Lists.ItemListResponsesCursorPaginationAfter = ItemListResponsesCursorPaginationAfter;
var Rules7 = class extends APIResource {
  static {
    __name(this, "Rules7");
  }
  constructor() {
    super(...arguments);
    this.lists = new Lists(this._client);
  }
};
Rules7.Lists = Lists;
Rules7.ListsListsSinglePage = ListsListsSinglePage;
var Rules8 = class extends APIResource {
  static {
    __name(this, "Rules8");
  }
  create(rulesetId, params, options) {
    const { account_id, zone_id, ...body } = params;
    if (!account_id && !zone_id) {
      throw new CloudflareError("You must provide either account_id or zone_id.");
    }
    if (account_id && zone_id) {
      throw new CloudflareError("You cannot provide both account_id and zone_id.");
    }
    const { accountOrZone, accountOrZoneId } = account_id ? {
      accountOrZone: "accounts",
      accountOrZoneId: account_id
    } : {
      accountOrZone: "zones",
      accountOrZoneId: zone_id
    };
    return this._client.post(`/${accountOrZone}/${accountOrZoneId}/rulesets/${rulesetId}/rules`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  delete(rulesetId, ruleId, params = {}, options) {
    if (isRequestOptions(params)) {
      return this.delete(rulesetId, ruleId, {}, params);
    }
    const { account_id, zone_id } = params;
    if (!account_id && !zone_id) {
      throw new CloudflareError("You must provide either account_id or zone_id.");
    }
    if (account_id && zone_id) {
      throw new CloudflareError("You cannot provide both account_id and zone_id.");
    }
    const { accountOrZone, accountOrZoneId } = account_id ? {
      accountOrZone: "accounts",
      accountOrZoneId: account_id
    } : {
      accountOrZone: "zones",
      accountOrZoneId: zone_id
    };
    return this._client.delete(`/${accountOrZone}/${accountOrZoneId}/rulesets/${rulesetId}/rules/${ruleId}`, options)._thenUnwrap((obj) => obj.result);
  }
  edit(rulesetId, ruleId, params, options) {
    const { account_id, zone_id, ...body } = params;
    if (!account_id && !zone_id) {
      throw new CloudflareError("You must provide either account_id or zone_id.");
    }
    if (account_id && zone_id) {
      throw new CloudflareError("You cannot provide both account_id and zone_id.");
    }
    const { accountOrZone, accountOrZoneId } = account_id ? {
      accountOrZone: "accounts",
      accountOrZoneId: account_id
    } : {
      accountOrZone: "zones",
      accountOrZoneId: zone_id
    };
    return this._client.patch(`/${accountOrZone}/${accountOrZoneId}/rulesets/${rulesetId}/rules/${ruleId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
};
var Versions = class extends APIResource {
  static {
    __name(this, "Versions");
  }
  list(rulesetId, params = {}, options) {
    if (isRequestOptions(params)) {
      return this.list(rulesetId, {}, params);
    }
    const { account_id, zone_id } = params;
    if (!account_id && !zone_id) {
      throw new CloudflareError("You must provide either account_id or zone_id.");
    }
    if (account_id && zone_id) {
      throw new CloudflareError("You cannot provide both account_id and zone_id.");
    }
    const { accountOrZone, accountOrZoneId } = account_id ? {
      accountOrZone: "accounts",
      accountOrZoneId: account_id
    } : {
      accountOrZone: "zones",
      accountOrZoneId: zone_id
    };
    return this._client.getAPIList(`/${accountOrZone}/${accountOrZoneId}/rulesets/${rulesetId}/versions`, VersionListResponsesSinglePage, options);
  }
  delete(rulesetId, rulesetVersion, params = {}, options) {
    if (isRequestOptions(params)) {
      return this.delete(rulesetId, rulesetVersion, {}, params);
    }
    const { account_id, zone_id } = params;
    if (!account_id && !zone_id) {
      throw new CloudflareError("You must provide either account_id or zone_id.");
    }
    if (account_id && zone_id) {
      throw new CloudflareError("You cannot provide both account_id and zone_id.");
    }
    const { accountOrZone, accountOrZoneId } = account_id ? {
      accountOrZone: "accounts",
      accountOrZoneId: account_id
    } : {
      accountOrZone: "zones",
      accountOrZoneId: zone_id
    };
    return this._client.delete(`/${accountOrZone}/${accountOrZoneId}/rulesets/${rulesetId}/versions/${rulesetVersion}`, { ...options, headers: { Accept: "*/*", ...options?.headers } });
  }
  get(rulesetId, rulesetVersion, params = {}, options) {
    if (isRequestOptions(params)) {
      return this.get(rulesetId, rulesetVersion, {}, params);
    }
    const { account_id, zone_id } = params;
    if (!account_id && !zone_id) {
      throw new CloudflareError("You must provide either account_id or zone_id.");
    }
    if (account_id && zone_id) {
      throw new CloudflareError("You cannot provide both account_id and zone_id.");
    }
    const { accountOrZone, accountOrZoneId } = account_id ? {
      accountOrZone: "accounts",
      accountOrZoneId: account_id
    } : {
      accountOrZone: "zones",
      accountOrZoneId: zone_id
    };
    return this._client.get(`/${accountOrZone}/${accountOrZoneId}/rulesets/${rulesetId}/versions/${rulesetVersion}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var VersionListResponsesSinglePage = class extends SinglePage {
  static {
    __name(this, "VersionListResponsesSinglePage");
  }
};
Versions.VersionListResponsesSinglePage = VersionListResponsesSinglePage;
var Versions2 = class extends APIResource {
  static {
    __name(this, "Versions2");
  }
  list(rulesetPhase, params = {}, options) {
    if (isRequestOptions(params)) {
      return this.list(rulesetPhase, {}, params);
    }
    const { account_id, zone_id } = params;
    if (!account_id && !zone_id) {
      throw new CloudflareError("You must provide either account_id or zone_id.");
    }
    if (account_id && zone_id) {
      throw new CloudflareError("You cannot provide both account_id and zone_id.");
    }
    const { accountOrZone, accountOrZoneId } = account_id ? {
      accountOrZone: "accounts",
      accountOrZoneId: account_id
    } : {
      accountOrZone: "zones",
      accountOrZoneId: zone_id
    };
    return this._client.getAPIList(`/${accountOrZone}/${accountOrZoneId}/rulesets/phases/${rulesetPhase}/entrypoint/versions`, VersionListResponsesSinglePage2, options);
  }
  get(rulesetPhase, rulesetVersion, params = {}, options) {
    if (isRequestOptions(params)) {
      return this.get(rulesetPhase, rulesetVersion, {}, params);
    }
    const { account_id, zone_id } = params;
    if (!account_id && !zone_id) {
      throw new CloudflareError("You must provide either account_id or zone_id.");
    }
    if (account_id && zone_id) {
      throw new CloudflareError("You cannot provide both account_id and zone_id.");
    }
    const { accountOrZone, accountOrZoneId } = account_id ? {
      accountOrZone: "accounts",
      accountOrZoneId: account_id
    } : {
      accountOrZone: "zones",
      accountOrZoneId: zone_id
    };
    return this._client.get(`/${accountOrZone}/${accountOrZoneId}/rulesets/phases/${rulesetPhase}/entrypoint/versions/${rulesetVersion}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var VersionListResponsesSinglePage2 = class extends SinglePage {
  static {
    __name(this, "VersionListResponsesSinglePage2");
  }
};
Versions2.VersionListResponsesSinglePage = VersionListResponsesSinglePage2;
var Phases = class extends APIResource {
  static {
    __name(this, "Phases");
  }
  constructor() {
    super(...arguments);
    this.versions = new Versions2(this._client);
  }
  update(rulesetPhase, params, options) {
    const { account_id, zone_id, ...body } = params;
    if (!account_id && !zone_id) {
      throw new CloudflareError("You must provide either account_id or zone_id.");
    }
    if (account_id && zone_id) {
      throw new CloudflareError("You cannot provide both account_id and zone_id.");
    }
    const { accountOrZone, accountOrZoneId } = account_id ? {
      accountOrZone: "accounts",
      accountOrZoneId: account_id
    } : {
      accountOrZone: "zones",
      accountOrZoneId: zone_id
    };
    return this._client.put(`/${accountOrZone}/${accountOrZoneId}/rulesets/phases/${rulesetPhase}/entrypoint`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  get(rulesetPhase, params = {}, options) {
    if (isRequestOptions(params)) {
      return this.get(rulesetPhase, {}, params);
    }
    const { account_id, zone_id } = params;
    if (!account_id && !zone_id) {
      throw new CloudflareError("You must provide either account_id or zone_id.");
    }
    if (account_id && zone_id) {
      throw new CloudflareError("You cannot provide both account_id and zone_id.");
    }
    const { accountOrZone, accountOrZoneId } = account_id ? {
      accountOrZone: "accounts",
      accountOrZoneId: account_id
    } : {
      accountOrZone: "zones",
      accountOrZoneId: zone_id
    };
    return this._client.get(`/${accountOrZone}/${accountOrZoneId}/rulesets/phases/${rulesetPhase}/entrypoint`, options)._thenUnwrap((obj) => obj.result);
  }
};
Phases.Versions = Versions2;
Phases.VersionListResponsesSinglePage = VersionListResponsesSinglePage2;
var Rulesets = class extends APIResource {
  static {
    __name(this, "Rulesets");
  }
  constructor() {
    super(...arguments);
    this.phases = new Phases(this._client);
    this.rules = new Rules8(this._client);
    this.versions = new Versions(this._client);
  }
  create(params, options) {
    const { account_id, zone_id, ...body } = params;
    if (!account_id && !zone_id) {
      throw new CloudflareError("You must provide either account_id or zone_id.");
    }
    if (account_id && zone_id) {
      throw new CloudflareError("You cannot provide both account_id and zone_id.");
    }
    const { accountOrZone, accountOrZoneId } = account_id ? {
      accountOrZone: "accounts",
      accountOrZoneId: account_id
    } : {
      accountOrZone: "zones",
      accountOrZoneId: zone_id
    };
    return this._client.post(`/${accountOrZone}/${accountOrZoneId}/rulesets`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  update(rulesetId, params, options) {
    const { account_id, zone_id, ...body } = params;
    if (!account_id && !zone_id) {
      throw new CloudflareError("You must provide either account_id or zone_id.");
    }
    if (account_id && zone_id) {
      throw new CloudflareError("You cannot provide both account_id and zone_id.");
    }
    const { accountOrZone, accountOrZoneId } = account_id ? {
      accountOrZone: "accounts",
      accountOrZoneId: account_id
    } : {
      accountOrZone: "zones",
      accountOrZoneId: zone_id
    };
    return this._client.put(`/${accountOrZone}/${accountOrZoneId}/rulesets/${rulesetId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(params = {}, options) {
    if (isRequestOptions(params)) {
      return this.list({}, params);
    }
    const { account_id, zone_id, ...query } = params;
    if (!account_id && !zone_id) {
      throw new CloudflareError("You must provide either account_id or zone_id.");
    }
    if (account_id && zone_id) {
      throw new CloudflareError("You cannot provide both account_id and zone_id.");
    }
    const { accountOrZone, accountOrZoneId } = account_id ? {
      accountOrZone: "accounts",
      accountOrZoneId: account_id
    } : {
      accountOrZone: "zones",
      accountOrZoneId: zone_id
    };
    return this._client.getAPIList(`/${accountOrZone}/${accountOrZoneId}/rulesets`, RulesetListResponsesCursorPagination, { query, ...options });
  }
  delete(rulesetId, params = {}, options) {
    if (isRequestOptions(params)) {
      return this.delete(rulesetId, {}, params);
    }
    const { account_id, zone_id } = params;
    if (!account_id && !zone_id) {
      throw new CloudflareError("You must provide either account_id or zone_id.");
    }
    if (account_id && zone_id) {
      throw new CloudflareError("You cannot provide both account_id and zone_id.");
    }
    const { accountOrZone, accountOrZoneId } = account_id ? {
      accountOrZone: "accounts",
      accountOrZoneId: account_id
    } : {
      accountOrZone: "zones",
      accountOrZoneId: zone_id
    };
    return this._client.delete(`/${accountOrZone}/${accountOrZoneId}/rulesets/${rulesetId}`, {
      ...options,
      headers: { Accept: "*/*", ...options?.headers }
    });
  }
  get(rulesetId, params = {}, options) {
    if (isRequestOptions(params)) {
      return this.get(rulesetId, {}, params);
    }
    const { account_id, zone_id } = params;
    if (!account_id && !zone_id) {
      throw new CloudflareError("You must provide either account_id or zone_id.");
    }
    if (account_id && zone_id) {
      throw new CloudflareError("You cannot provide both account_id and zone_id.");
    }
    const { accountOrZone, accountOrZoneId } = account_id ? {
      accountOrZone: "accounts",
      accountOrZoneId: account_id
    } : {
      accountOrZone: "zones",
      accountOrZoneId: zone_id
    };
    return this._client.get(`/${accountOrZone}/${accountOrZoneId}/rulesets/${rulesetId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var RulesetListResponsesCursorPagination = class extends CursorPagination {
  static {
    __name(this, "RulesetListResponsesCursorPagination");
  }
};
Rulesets.RulesetListResponsesCursorPagination = RulesetListResponsesCursorPagination;
Rulesets.Phases = Phases;
Rulesets.Rules = Rules8;
Rulesets.Versions = Versions;
Rulesets.VersionListResponsesSinglePage = VersionListResponsesSinglePage;
var Analyze = class extends APIResource {
  static {
    __name(this, "Analyze");
  }
  create(params, options) {
    const { zone_id, ...body } = params;
    return this._client.post(`/zones/${zone_id}/ssl/analyze`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
};
var Recommendations = class extends APIResource {
  static {
    __name(this, "Recommendations");
  }
  get(params, options) {
    const { zone_id } = params;
    return this._client.get(`/zones/${zone_id}/ssl/recommendation`, options)._thenUnwrap((obj) => obj.result);
  }
};
var VerificationResource = class extends APIResource {
  static {
    __name(this, "VerificationResource");
  }
  edit(certificatePackId, params, options) {
    const { zone_id, ...body } = params;
    return this._client.patch(`/zones/${zone_id}/ssl/verification/${certificatePackId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  get(params, options) {
    const { zone_id, ...query } = params;
    return this._client.get(`/zones/${zone_id}/ssl/verification`, { query, ...options })._thenUnwrap((obj) => obj.result);
  }
};
var Quota = class extends APIResource {
  static {
    __name(this, "Quota");
  }
  get(params, options) {
    const { zone_id } = params;
    return this._client.get(`/zones/${zone_id}/ssl/certificate_packs/quota`, options)._thenUnwrap((obj) => obj.result);
  }
};
var CertificatePacks = class extends APIResource {
  static {
    __name(this, "CertificatePacks");
  }
  constructor() {
    super(...arguments);
    this.quota = new Quota(this._client);
  }
  create(params, options) {
    const { zone_id, ...body } = params;
    return this._client.post(`/zones/${zone_id}/ssl/certificate_packs/order`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { zone_id, ...query } = params;
    return this._client.getAPIList(`/zones/${zone_id}/ssl/certificate_packs`, CertificatePackListResponsesSinglePage, { query, ...options });
  }
  delete(certificatePackId, params, options) {
    const { zone_id } = params;
    return this._client.delete(`/zones/${zone_id}/ssl/certificate_packs/${certificatePackId}`, options)._thenUnwrap((obj) => obj.result);
  }
  edit(certificatePackId, params, options) {
    const { zone_id, ...body } = params;
    return this._client.patch(`/zones/${zone_id}/ssl/certificate_packs/${certificatePackId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  get(certificatePackId, params, options) {
    const { zone_id } = params;
    return this._client.get(`/zones/${zone_id}/ssl/certificate_packs/${certificatePackId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var CertificatePackListResponsesSinglePage = class extends SinglePage {
  static {
    __name(this, "CertificatePackListResponsesSinglePage");
  }
};
CertificatePacks.CertificatePackListResponsesSinglePage = CertificatePackListResponsesSinglePage;
CertificatePacks.Quota = Quota;
var Settings8 = class extends APIResource {
  static {
    __name(this, "Settings8");
  }
  edit(params, options) {
    const { zone_id, ...body } = params;
    return this._client.patch(`/zones/${zone_id}/ssl/universal/settings`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  get(params, options) {
    const { zone_id } = params;
    return this._client.get(`/zones/${zone_id}/ssl/universal/settings`, options)._thenUnwrap((obj) => obj.result);
  }
};
var Universal = class extends APIResource {
  static {
    __name(this, "Universal");
  }
  constructor() {
    super(...arguments);
    this.settings = new Settings8(this._client);
  }
};
Universal.Settings = Settings8;
var SSL = class extends APIResource {
  static {
    __name(this, "SSL");
  }
  constructor() {
    super(...arguments);
    this.analyze = new Analyze(this._client);
    this.certificatePacks = new CertificatePacks(this._client);
    this.recommendations = new Recommendations(this._client);
    this.universal = new Universal(this._client);
    this.verification = new VerificationResource(this._client);
  }
};
SSL.Analyze = Analyze;
SSL.CertificatePacks = CertificatePacks;
SSL.CertificatePackListResponsesSinglePage = CertificatePackListResponsesSinglePage;
SSL.Recommendations = Recommendations;
SSL.Universal = Universal;
SSL.VerificationResource = VerificationResource;
var Schemas2 = class extends APIResource {
  static {
    __name(this, "Schemas2");
  }
  create(params, options) {
    const { zone_id, ...body } = params;
    return this._client.post(`/zones/${zone_id}/schema_validation/schemas`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { zone_id, ...query } = params;
    return this._client.getAPIList(`/zones/${zone_id}/schema_validation/schemas`, SchemaListResponsesV4PagePaginationArray, { query, ...options });
  }
  delete(schemaId, params, options) {
    const { zone_id } = params;
    return this._client.delete(`/zones/${zone_id}/schema_validation/schemas/${schemaId}`, options)._thenUnwrap((obj) => obj.result);
  }
  edit(schemaId, params, options) {
    const { zone_id, ...body } = params;
    return this._client.patch(`/zones/${zone_id}/schema_validation/schemas/${schemaId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  get(schemaId, params, options) {
    const { zone_id, ...query } = params;
    return this._client.get(`/zones/${zone_id}/schema_validation/schemas/${schemaId}`, {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
};
var SchemaListResponsesV4PagePaginationArray = class extends V4PagePaginationArray {
  static {
    __name(this, "SchemaListResponsesV4PagePaginationArray");
  }
};
Schemas2.SchemaListResponsesV4PagePaginationArray = SchemaListResponsesV4PagePaginationArray;
var Operations4 = class extends APIResource {
  static {
    __name(this, "Operations4");
  }
  update(operationId, params, options) {
    const { zone_id, ...body } = params;
    return this._client.put(`/zones/${zone_id}/schema_validation/settings/operations/${operationId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { zone_id, ...query } = params;
    return this._client.getAPIList(`/zones/${zone_id}/schema_validation/settings/operations`, OperationListResponsesV4PagePaginationArray3, { query, ...options });
  }
  delete(operationId, params, options) {
    const { zone_id } = params;
    return this._client.delete(`/zones/${zone_id}/schema_validation/settings/operations/${operationId}`, options)._thenUnwrap((obj) => obj.result);
  }
  bulkEdit(params, options) {
    const { zone_id, body } = params;
    return this._client.patch(`/zones/${zone_id}/schema_validation/settings/operations`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  get(operationId, params, options) {
    const { zone_id } = params;
    return this._client.get(`/zones/${zone_id}/schema_validation/settings/operations/${operationId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var OperationListResponsesV4PagePaginationArray3 = class extends V4PagePaginationArray {
  static {
    __name(this, "OperationListResponsesV4PagePaginationArray3");
  }
};
Operations4.OperationListResponsesV4PagePaginationArray = OperationListResponsesV4PagePaginationArray3;
var Settings9 = class extends APIResource {
  static {
    __name(this, "Settings9");
  }
  constructor() {
    super(...arguments);
    this.operations = new Operations4(this._client);
  }
  update(params, options) {
    const { zone_id, ...body } = params;
    return this._client.put(`/zones/${zone_id}/schema_validation/settings`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  edit(params, options) {
    const { zone_id, ...body } = params;
    return this._client.patch(`/zones/${zone_id}/schema_validation/settings`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  get(params, options) {
    const { zone_id } = params;
    return this._client.get(`/zones/${zone_id}/schema_validation/settings`, options)._thenUnwrap((obj) => obj.result);
  }
};
Settings9.Operations = Operations4;
Settings9.OperationListResponsesV4PagePaginationArray = OperationListResponsesV4PagePaginationArray3;
var SchemaValidation3 = class extends APIResource {
  static {
    __name(this, "SchemaValidation3");
  }
  constructor() {
    super(...arguments);
    this.schemas = new Schemas2(this._client);
    this.settings = new Settings9(this._client);
  }
};
SchemaValidation3.Schemas = Schemas2;
SchemaValidation3.SchemaListResponsesV4PagePaginationArray = SchemaListResponsesV4PagePaginationArray;
SchemaValidation3.Settings = Settings9;
var Quota2 = class extends APIResource {
  static {
    __name(this, "Quota2");
  }
  get(params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/secrets_store/quota`, options)._thenUnwrap((obj) => obj.result);
  }
};
var Secrets = class extends APIResource {
  static {
    __name(this, "Secrets");
  }
  create(storeId, params, options) {
    const { account_id, body } = params;
    return this._client.getAPIList(`/accounts/${account_id}/secrets_store/stores/${storeId}/secrets`, SecretCreateResponsesSinglePage, { body, method: "post", ...options });
  }
  list(storeId, params, options) {
    const { account_id, ...query } = params;
    return this._client.getAPIList(`/accounts/${account_id}/secrets_store/stores/${storeId}/secrets`, SecretListResponsesV4PagePaginationArray, { query, ...options });
  }
  delete(storeId, secretId, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/secrets_store/stores/${storeId}/secrets/${secretId}`, options)._thenUnwrap((obj) => obj.result);
  }
  bulkDelete(storeId, params, options) {
    const { account_id } = params;
    return this._client.getAPIList(`/accounts/${account_id}/secrets_store/stores/${storeId}/secrets`, SecretBulkDeleteResponsesSinglePage, { method: "delete", ...options });
  }
  duplicate(storeId, secretId, params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/secrets_store/stores/${storeId}/secrets/${secretId}/duplicate`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  edit(storeId, secretId, params, options) {
    const { account_id, ...body } = params;
    return this._client.patch(`/accounts/${account_id}/secrets_store/stores/${storeId}/secrets/${secretId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  get(storeId, secretId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/secrets_store/stores/${storeId}/secrets/${secretId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var SecretCreateResponsesSinglePage = class extends SinglePage {
  static {
    __name(this, "SecretCreateResponsesSinglePage");
  }
};
var SecretListResponsesV4PagePaginationArray = class extends V4PagePaginationArray {
  static {
    __name(this, "SecretListResponsesV4PagePaginationArray");
  }
};
var SecretBulkDeleteResponsesSinglePage = class extends SinglePage {
  static {
    __name(this, "SecretBulkDeleteResponsesSinglePage");
  }
};
Secrets.SecretCreateResponsesSinglePage = SecretCreateResponsesSinglePage;
Secrets.SecretListResponsesV4PagePaginationArray = SecretListResponsesV4PagePaginationArray;
Secrets.SecretBulkDeleteResponsesSinglePage = SecretBulkDeleteResponsesSinglePage;
var Stores = class extends APIResource {
  static {
    __name(this, "Stores");
  }
  constructor() {
    super(...arguments);
    this.secrets = new Secrets(this._client);
  }
  create(params, options) {
    const { account_id, body } = params;
    return this._client.getAPIList(`/accounts/${account_id}/secrets_store/stores`, StoreCreateResponsesSinglePage, { body, method: "post", ...options });
  }
  list(params, options) {
    const { account_id, ...query } = params;
    return this._client.getAPIList(`/accounts/${account_id}/secrets_store/stores`, StoreListResponsesV4PagePaginationArray, { query, ...options });
  }
  delete(storeId, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/secrets_store/stores/${storeId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var StoreCreateResponsesSinglePage = class extends SinglePage {
  static {
    __name(this, "StoreCreateResponsesSinglePage");
  }
};
var StoreListResponsesV4PagePaginationArray = class extends V4PagePaginationArray {
  static {
    __name(this, "StoreListResponsesV4PagePaginationArray");
  }
};
Stores.StoreCreateResponsesSinglePage = StoreCreateResponsesSinglePage;
Stores.StoreListResponsesV4PagePaginationArray = StoreListResponsesV4PagePaginationArray;
Stores.Secrets = Secrets;
Stores.SecretCreateResponsesSinglePage = SecretCreateResponsesSinglePage;
Stores.SecretListResponsesV4PagePaginationArray = SecretListResponsesV4PagePaginationArray;
Stores.SecretBulkDeleteResponsesSinglePage = SecretBulkDeleteResponsesSinglePage;
var SecretsStore = class extends APIResource {
  static {
    __name(this, "SecretsStore");
  }
  constructor() {
    super(...arguments);
    this.stores = new Stores(this._client);
    this.quota = new Quota2(this._client);
  }
};
SecretsStore.Stores = Stores;
SecretsStore.StoreCreateResponsesSinglePage = StoreCreateResponsesSinglePage;
SecretsStore.StoreListResponsesV4PagePaginationArray = StoreListResponsesV4PagePaginationArray;
SecretsStore.Quota = Quota2;
var Class = class extends APIResource {
  static {
    __name(this, "Class");
  }
  get(params = {}, options) {
    if (isRequestOptions(params)) {
      return this.get({}, params);
    }
    const { account_id, zone_id, ...query } = params;
    if (!account_id && !zone_id) {
      throw new CloudflareError("You must provide either account_id or zone_id.");
    }
    if (account_id && zone_id) {
      throw new CloudflareError("You cannot provide both account_id and zone_id.");
    }
    const { accountOrZone, accountOrZoneId } = account_id ? {
      accountOrZone: "accounts",
      accountOrZoneId: account_id
    } : {
      accountOrZone: "zones",
      accountOrZoneId: zone_id
    };
    return this._client.get(`/${accountOrZone}/${accountOrZoneId}/security-center/insights/class`, {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
};
var Severity = class extends APIResource {
  static {
    __name(this, "Severity");
  }
  get(params = {}, options) {
    if (isRequestOptions(params)) {
      return this.get({}, params);
    }
    const { account_id, zone_id, ...query } = params;
    if (!account_id && !zone_id) {
      throw new CloudflareError("You must provide either account_id or zone_id.");
    }
    if (account_id && zone_id) {
      throw new CloudflareError("You cannot provide both account_id and zone_id.");
    }
    const { accountOrZone, accountOrZoneId } = account_id ? {
      accountOrZone: "accounts",
      accountOrZoneId: account_id
    } : {
      accountOrZone: "zones",
      accountOrZoneId: zone_id
    };
    return this._client.get(`/${accountOrZone}/${accountOrZoneId}/security-center/insights/severity`, {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
};
var Type = class extends APIResource {
  static {
    __name(this, "Type");
  }
  get(params = {}, options) {
    if (isRequestOptions(params)) {
      return this.get({}, params);
    }
    const { account_id, zone_id, ...query } = params;
    if (!account_id && !zone_id) {
      throw new CloudflareError("You must provide either account_id or zone_id.");
    }
    if (account_id && zone_id) {
      throw new CloudflareError("You cannot provide both account_id and zone_id.");
    }
    const { accountOrZone, accountOrZoneId } = account_id ? {
      accountOrZone: "accounts",
      accountOrZoneId: account_id
    } : {
      accountOrZone: "zones",
      accountOrZoneId: zone_id
    };
    return this._client.get(`/${accountOrZone}/${accountOrZoneId}/security-center/insights/type`, {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
};
var Insights2 = class extends APIResource {
  static {
    __name(this, "Insights2");
  }
  constructor() {
    super(...arguments);
    this.class = new Class(this._client);
    this.severity = new Severity(this._client);
    this.type = new Type(this._client);
  }
  list(params = {}, options) {
    if (isRequestOptions(params)) {
      return this.list({}, params);
    }
    const { account_id, zone_id, ...query } = params;
    if (!account_id && !zone_id) {
      throw new CloudflareError("You must provide either account_id or zone_id.");
    }
    if (account_id && zone_id) {
      throw new CloudflareError("You cannot provide both account_id and zone_id.");
    }
    const { accountOrZone, accountOrZoneId } = account_id ? {
      accountOrZone: "accounts",
      accountOrZoneId: account_id
    } : {
      accountOrZone: "zones",
      accountOrZoneId: zone_id
    };
    return this._client.getAPIList(`/${accountOrZone}/${accountOrZoneId}/security-center/insights`, InsightListResponsesV4PagePagination, { query, ...options });
  }
  dismiss(issueId, params, options) {
    const { account_id, zone_id, ...body } = params;
    if (!account_id && !zone_id) {
      throw new CloudflareError("You must provide either account_id or zone_id.");
    }
    if (account_id && zone_id) {
      throw new CloudflareError("You cannot provide both account_id and zone_id.");
    }
    const { accountOrZone, accountOrZoneId } = account_id ? {
      accountOrZone: "accounts",
      accountOrZoneId: account_id
    } : {
      accountOrZone: "zones",
      accountOrZoneId: zone_id
    };
    return this._client.put(`/${accountOrZone}/${accountOrZoneId}/security-center/insights/${issueId}/dismiss`, { body, ...options });
  }
};
var InsightListResponsesV4PagePagination = class extends V4PagePagination {
  static {
    __name(this, "InsightListResponsesV4PagePagination");
  }
};
Insights2.InsightListResponsesV4PagePagination = InsightListResponsesV4PagePagination;
Insights2.Class = Class;
Insights2.Severity = Severity;
Insights2.Type = Type;
var SecurityCenter = class extends APIResource {
  static {
    __name(this, "SecurityCenter");
  }
  constructor() {
    super(...arguments);
    this.insights = new Insights2(this._client);
  }
};
SecurityCenter.Insights = Insights2;
SecurityCenter.InsightListResponsesV4PagePagination = InsightListResponsesV4PagePagination;
var SecurityTXT = class extends APIResource {
  static {
    __name(this, "SecurityTXT");
  }
  update(params, options) {
    const { zone_id, ...body } = params;
    return this._client.put(`/zones/${zone_id}/security-center/securitytxt`, { body, ...options });
  }
  delete(params, options) {
    const { zone_id } = params;
    return this._client.delete(`/zones/${zone_id}/security-center/securitytxt`, options);
  }
  get(params, options) {
    const { zone_id } = params;
    return this._client.get(`/zones/${zone_id}/security-center/securitytxt`, options)._thenUnwrap((obj) => obj.result);
  }
};
var Content2 = class extends APIResource {
  static {
    __name(this, "Content2");
  }
  get(snippetName, params, options) {
    const { zone_id } = params;
    return this._client.get(`/zones/${zone_id}/snippets/${snippetName}/content`, {
      ...options,
      headers: { Accept: "multipart/form-data", ...options?.headers },
      __binaryResponse: true
    });
  }
};
var Rules9 = class extends APIResource {
  static {
    __name(this, "Rules9");
  }
  update(params, options) {
    const { zone_id, ...body } = params;
    return this._client.getAPIList(`/zones/${zone_id}/snippets/snippet_rules`, RuleUpdateResponsesSinglePage2, { body, method: "put", ...options });
  }
  list(params, options) {
    const { zone_id } = params;
    return this._client.getAPIList(`/zones/${zone_id}/snippets/snippet_rules`, RuleListResponsesSinglePage2, options);
  }
  delete(params, options) {
    const { zone_id } = params;
    return this._client.getAPIList(`/zones/${zone_id}/snippets/snippet_rules`, RuleDeleteResponsesSinglePage, { method: "delete", ...options });
  }
};
var RuleUpdateResponsesSinglePage2 = class extends SinglePage {
  static {
    __name(this, "RuleUpdateResponsesSinglePage2");
  }
};
var RuleListResponsesSinglePage2 = class extends SinglePage {
  static {
    __name(this, "RuleListResponsesSinglePage2");
  }
};
var RuleDeleteResponsesSinglePage = class extends SinglePage {
  static {
    __name(this, "RuleDeleteResponsesSinglePage");
  }
};
Rules9.RuleUpdateResponsesSinglePage = RuleUpdateResponsesSinglePage2;
Rules9.RuleListResponsesSinglePage = RuleListResponsesSinglePage2;
Rules9.RuleDeleteResponsesSinglePage = RuleDeleteResponsesSinglePage;
var Snippets = class extends APIResource {
  static {
    __name(this, "Snippets");
  }
  constructor() {
    super(...arguments);
    this.content = new Content2(this._client);
    this.rules = new Rules9(this._client);
  }
  update(snippetName, params, options) {
    const { zone_id, ...body } = params;
    return this._client.put(`/zones/${zone_id}/snippets/${snippetName}`, multipartFormRequestOptions({ body, ...options, __multipartSyntax: "json" }))._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { zone_id, ...query } = params;
    return this._client.getAPIList(`/zones/${zone_id}/snippets`, SnippetListResponsesV4PagePaginationArray, {
      query,
      ...options
    });
  }
  delete(snippetName, params, options) {
    const { zone_id } = params;
    return this._client.delete(`/zones/${zone_id}/snippets/${snippetName}`, options)._thenUnwrap((obj) => obj.result);
  }
  get(snippetName, params, options) {
    const { zone_id } = params;
    return this._client.get(`/zones/${zone_id}/snippets/${snippetName}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var SnippetListResponsesV4PagePaginationArray = class extends V4PagePaginationArray {
  static {
    __name(this, "SnippetListResponsesV4PagePaginationArray");
  }
};
Snippets.SnippetListResponsesV4PagePaginationArray = SnippetListResponsesV4PagePaginationArray;
Snippets.Content = Content2;
Snippets.Rules = Rules9;
Snippets.RuleUpdateResponsesSinglePage = RuleUpdateResponsesSinglePage2;
Snippets.RuleListResponsesSinglePage = RuleListResponsesSinglePage2;
Snippets.RuleDeleteResponsesSinglePage = RuleDeleteResponsesSinglePage;
var Apps2 = class extends APIResource {
  static {
    __name(this, "Apps2");
  }
  create(params, options) {
    const { zone_id, ...body } = params;
    return this._client.post(`/zones/${zone_id}/spectrum/apps`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  update(appId, params, options) {
    const { zone_id, ...body } = params;
    return this._client.put(`/zones/${zone_id}/spectrum/apps/${appId}`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { zone_id, ...query } = params;
    return this._client.getAPIList(`/zones/${zone_id}/spectrum/apps`, AppListResponsesV4PagePaginationArray, {
      query,
      ...options
    });
  }
  delete(appId, params, options) {
    const { zone_id } = params;
    return this._client.delete(`/zones/${zone_id}/spectrum/apps/${appId}`, options)._thenUnwrap((obj) => obj.result);
  }
  get(appId, params, options) {
    const { zone_id } = params;
    return this._client.get(`/zones/${zone_id}/spectrum/apps/${appId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var AppListResponsesV4PagePaginationArray = class extends V4PagePaginationArray {
  static {
    __name(this, "AppListResponsesV4PagePaginationArray");
  }
};
Apps2.AppListResponsesV4PagePaginationArray = AppListResponsesV4PagePaginationArray;
var Currents = class extends APIResource {
  static {
    __name(this, "Currents");
  }
  get(params, options) {
    const { zone_id, ...query } = params;
    return this._client.get(`/zones/${zone_id}/spectrum/analytics/aggregate/current`, {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
};
var Aggregates = class extends APIResource {
  static {
    __name(this, "Aggregates");
  }
  constructor() {
    super(...arguments);
    this.currents = new Currents(this._client);
  }
};
Aggregates.Currents = Currents;
var Bytimes3 = class extends APIResource {
  static {
    __name(this, "Bytimes3");
  }
  get(params, options) {
    const { zone_id, ...query } = params;
    return this._client.get(`/zones/${zone_id}/spectrum/analytics/events/bytime`, {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
};
var Summaries = class extends APIResource {
  static {
    __name(this, "Summaries");
  }
  get(params, options) {
    const { zone_id, ...query } = params;
    return this._client.get(`/zones/${zone_id}/spectrum/analytics/events/summary`, {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
};
var Events4 = class extends APIResource {
  static {
    __name(this, "Events4");
  }
  constructor() {
    super(...arguments);
    this.bytimes = new Bytimes3(this._client);
    this.summaries = new Summaries(this._client);
  }
};
Events4.Bytimes = Bytimes3;
Events4.Summaries = Summaries;
var Analytics3 = class extends APIResource {
  static {
    __name(this, "Analytics3");
  }
  constructor() {
    super(...arguments);
    this.aggregates = new Aggregates(this._client);
    this.events = new Events4(this._client);
  }
};
Analytics3.Aggregates = Aggregates;
Analytics3.Events = Events4;
var Spectrum = class extends APIResource {
  static {
    __name(this, "Spectrum");
  }
  constructor() {
    super(...arguments);
    this.analytics = new Analytics3(this._client);
    this.apps = new Apps2(this._client);
  }
};
Spectrum.Analytics = Analytics3;
Spectrum.Apps = Apps2;
Spectrum.AppListResponsesV4PagePaginationArray = AppListResponsesV4PagePaginationArray;
var Availabilities = class extends APIResource {
  static {
    __name(this, "Availabilities");
  }
  list(params, options) {
    const { zone_id } = params;
    return this._client.get(`/zones/${zone_id}/speed_api/availabilities`, options)._thenUnwrap((obj) => obj.result);
  }
};
var ScheduleResource = class extends APIResource {
  static {
    __name(this, "ScheduleResource");
  }
  create(url, params, options) {
    const { zone_id, region } = params;
    return this._client.post(`/zones/${zone_id}/speed_api/schedule/${url}`, {
      query: { region },
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  delete(url, params, options) {
    const { zone_id, region } = params;
    return this._client.delete(`/zones/${zone_id}/speed_api/schedule/${url}`, {
      query: { region },
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  get(url, params, options) {
    const { zone_id, ...query } = params;
    return this._client.get(`/zones/${zone_id}/speed_api/schedule/${url}`, {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
};
var Tests = class extends APIResource {
  static {
    __name(this, "Tests");
  }
  create(url, params, options) {
    const { zone_id, ...body } = params;
    return this._client.post(`/zones/${zone_id}/speed_api/pages/${url}/tests`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(url, params, options) {
    const { zone_id, ...query } = params;
    return this._client.getAPIList(`/zones/${zone_id}/speed_api/pages/${url}/tests`, TestsV4PagePaginationArray, { query, ...options });
  }
  delete(url, params, options) {
    const { zone_id, region } = params;
    return this._client.delete(`/zones/${zone_id}/speed_api/pages/${url}/tests`, {
      query: { region },
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  get(url, testId, params, options) {
    const { zone_id } = params;
    return this._client.get(`/zones/${zone_id}/speed_api/pages/${url}/tests/${testId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var TestsV4PagePaginationArray = class extends V4PagePaginationArray {
  static {
    __name(this, "TestsV4PagePaginationArray");
  }
};
Tests.TestsV4PagePaginationArray = TestsV4PagePaginationArray;
var Pages2 = class extends APIResource {
  static {
    __name(this, "Pages2");
  }
  constructor() {
    super(...arguments);
    this.tests = new Tests(this._client);
  }
  list(params, options) {
    const { zone_id } = params;
    return this._client.getAPIList(`/zones/${zone_id}/speed_api/pages`, PageListResponsesSinglePage, options);
  }
  trend(url, params, options) {
    const { zone_id, ...query } = params;
    return this._client.get(`/zones/${zone_id}/speed_api/pages/${url}/trend`, {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
};
var PageListResponsesSinglePage = class extends SinglePage {
  static {
    __name(this, "PageListResponsesSinglePage");
  }
};
Pages2.PageListResponsesSinglePage = PageListResponsesSinglePage;
Pages2.Tests = Tests;
Pages2.TestsV4PagePaginationArray = TestsV4PagePaginationArray;
var Speed2 = class extends APIResource {
  static {
    __name(this, "Speed2");
  }
  constructor() {
    super(...arguments);
    this.schedule = new ScheduleResource(this._client);
    this.availabilities = new Availabilities(this._client);
    this.pages = new Pages2(this._client);
  }
};
Speed2.ScheduleResource = ScheduleResource;
Speed2.Availabilities = Availabilities;
Speed2.Pages = Pages2;
Speed2.PageListResponsesSinglePage = PageListResponsesSinglePage;
var AudioTracks = class extends APIResource {
  static {
    __name(this, "AudioTracks");
  }
  delete(identifier, audioIdentifier, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/stream/${identifier}/audio/${audioIdentifier}`, options)._thenUnwrap((obj) => obj.result);
  }
  copy(identifier, params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/stream/${identifier}/audio/copy`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  edit(identifier, audioIdentifier, params, options) {
    const { account_id, ...body } = params;
    return this._client.patch(`/accounts/${account_id}/stream/${identifier}/audio/${audioIdentifier}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  get(identifier, params, options) {
    const { account_id } = params;
    return this._client.getAPIList(`/accounts/${account_id}/stream/${identifier}/audio`, AudioSinglePage, options);
  }
};
var AudioSinglePage = class extends SinglePage {
  static {
    __name(this, "AudioSinglePage");
  }
};
AudioTracks.AudioSinglePage = AudioSinglePage;
var ClipResource = class extends APIResource {
  static {
    __name(this, "ClipResource");
  }
  create(params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/stream/clip`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
};
var Copy = class extends APIResource {
  static {
    __name(this, "Copy");
  }
  create(params, options) {
    const { account_id, "Upload-Creator": uploadCreator, ...body } = params;
    return this._client.post(`/accounts/${account_id}/stream/copy`, {
      body,
      ...options,
      headers: {
        ...uploadCreator != null ? { "Upload-Creator": uploadCreator } : void 0,
        ...options?.headers
      }
    })._thenUnwrap((obj) => obj.result);
  }
};
var DirectUpload = class extends APIResource {
  static {
    __name(this, "DirectUpload");
  }
  create(params, options) {
    const { account_id, "Upload-Creator": uploadCreator, ...body } = params;
    return this._client.post(`/accounts/${account_id}/stream/direct_upload`, {
      body,
      ...options,
      headers: {
        ...uploadCreator != null ? { "Upload-Creator": uploadCreator } : void 0,
        ...options?.headers
      }
    })._thenUnwrap((obj) => obj.result);
  }
};
var Downloads2 = class extends APIResource {
  static {
    __name(this, "Downloads2");
  }
  create(identifier, params, options) {
    const { account_id, body } = params;
    return this._client.post(`/accounts/${account_id}/stream/${identifier}/downloads`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  delete(identifier, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/stream/${identifier}/downloads`, options)._thenUnwrap((obj) => obj.result);
  }
  get(identifier, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/stream/${identifier}/downloads`, options)._thenUnwrap((obj) => obj.result);
  }
};
var Embed = class extends APIResource {
  static {
    __name(this, "Embed");
  }
  get(identifier, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/stream/${identifier}/embed`, options);
  }
};
var Keys3 = class extends APIResource {
  static {
    __name(this, "Keys3");
  }
  create(params, options) {
    const { account_id, body } = params;
    return this._client.post(`/accounts/${account_id}/stream/keys`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  delete(identifier, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/stream/keys/${identifier}`, options)._thenUnwrap((obj) => obj.result);
  }
  get(params, options) {
    const { account_id } = params;
    return this._client.getAPIList(`/accounts/${account_id}/stream/keys`, KeyGetResponsesSinglePage, options);
  }
};
var KeyGetResponsesSinglePage = class extends SinglePage {
  static {
    __name(this, "KeyGetResponsesSinglePage");
  }
};
Keys3.KeyGetResponsesSinglePage = KeyGetResponsesSinglePage;
var Token = class extends APIResource {
  static {
    __name(this, "Token");
  }
  create(identifier, params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/stream/${identifier}/token`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
};
var Videos = class extends APIResource {
  static {
    __name(this, "Videos");
  }
  storageUsage(params, options) {
    const { account_id, ...query } = params;
    return this._client.get(`/accounts/${account_id}/stream/storage-usage`, {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
};
var Watermarks = class extends APIResource {
  static {
    __name(this, "Watermarks");
  }
  create(params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/stream/watermarks`, multipartFormRequestOptions({ body, ...options }))._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { account_id } = params;
    return this._client.getAPIList(`/accounts/${account_id}/stream/watermarks`, WatermarksSinglePage, options);
  }
  delete(identifier, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/stream/watermarks/${identifier}`, options)._thenUnwrap((obj) => obj.result);
  }
  get(identifier, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/stream/watermarks/${identifier}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var WatermarksSinglePage = class extends SinglePage {
  static {
    __name(this, "WatermarksSinglePage");
  }
};
Watermarks.WatermarksSinglePage = WatermarksSinglePage;
var Webhooks2 = class extends APIResource {
  static {
    __name(this, "Webhooks2");
  }
  update(params, options) {
    const { account_id, ...body } = params;
    return this._client.put(`/accounts/${account_id}/stream/webhook`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  delete(params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/stream/webhook`, options)._thenUnwrap((obj) => obj.result);
  }
  get(params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/stream/webhook`, options)._thenUnwrap((obj) => obj.result);
  }
};
var Vtt = class extends APIResource {
  static {
    __name(this, "Vtt");
  }
  get(identifier, language, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/stream/${identifier}/captions/${language}/vtt`, {
      ...options,
      headers: { Accept: "text/vtt", ...options?.headers }
    });
  }
};
var Language = class extends APIResource {
  static {
    __name(this, "Language");
  }
  constructor() {
    super(...arguments);
    this.vtt = new Vtt(this._client);
  }
  create(identifier, language, params, options) {
    const { account_id } = params;
    return this._client.post(`/accounts/${account_id}/stream/${identifier}/captions/${language}/generate`, options)._thenUnwrap((obj) => obj.result);
  }
  update(identifier, language, params, options) {
    const { account_id, ...body } = params;
    return this._client.put(`/accounts/${account_id}/stream/${identifier}/captions/${language}`, multipartFormRequestOptions({ body, ...options }))._thenUnwrap((obj) => obj.result);
  }
  delete(identifier, language, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/stream/${identifier}/captions/${language}`, options)._thenUnwrap((obj) => obj.result);
  }
  get(identifier, language, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/stream/${identifier}/captions/${language}`, options)._thenUnwrap((obj) => obj.result);
  }
};
Language.Vtt = Vtt;
var Captions = class extends APIResource {
  static {
    __name(this, "Captions");
  }
  constructor() {
    super(...arguments);
    this.language = new Language(this._client);
  }
  get(identifier, params, options) {
    const { account_id } = params;
    return this._client.getAPIList(`/accounts/${account_id}/stream/${identifier}/captions`, CaptionsSinglePage, options);
  }
};
var CaptionsSinglePage = class extends SinglePage {
  static {
    __name(this, "CaptionsSinglePage");
  }
};
Captions.CaptionsSinglePage = CaptionsSinglePage;
Captions.Language = Language;
var Outputs = class extends APIResource {
  static {
    __name(this, "Outputs");
  }
  create(liveInputIdentifier, params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/stream/live_inputs/${liveInputIdentifier}/outputs`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  update(liveInputIdentifier, outputIdentifier, params, options) {
    const { account_id, ...body } = params;
    return this._client.put(`/accounts/${account_id}/stream/live_inputs/${liveInputIdentifier}/outputs/${outputIdentifier}`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  list(liveInputIdentifier, params, options) {
    const { account_id } = params;
    return this._client.getAPIList(`/accounts/${account_id}/stream/live_inputs/${liveInputIdentifier}/outputs`, OutputsSinglePage, options);
  }
  delete(liveInputIdentifier, outputIdentifier, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/stream/live_inputs/${liveInputIdentifier}/outputs/${outputIdentifier}`, { ...options, headers: { Accept: "*/*", ...options?.headers } });
  }
};
var OutputsSinglePage = class extends SinglePage {
  static {
    __name(this, "OutputsSinglePage");
  }
};
Outputs.OutputsSinglePage = OutputsSinglePage;
var LiveInputs = class extends APIResource {
  static {
    __name(this, "LiveInputs");
  }
  constructor() {
    super(...arguments);
    this.outputs = new Outputs(this._client);
  }
  create(params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/stream/live_inputs`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  update(liveInputIdentifier, params, options) {
    const { account_id, ...body } = params;
    return this._client.put(`/accounts/${account_id}/stream/live_inputs/${liveInputIdentifier}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { account_id, ...query } = params;
    return this._client.get(`/accounts/${account_id}/stream/live_inputs`, {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  delete(liveInputIdentifier, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/stream/live_inputs/${liveInputIdentifier}`, {
      ...options,
      headers: { Accept: "*/*", ...options?.headers }
    });
  }
  get(liveInputIdentifier, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/stream/live_inputs/${liveInputIdentifier}`, options)._thenUnwrap((obj) => obj.result);
  }
};
LiveInputs.Outputs = Outputs;
LiveInputs.OutputsSinglePage = OutputsSinglePage;
var Stream = class extends APIResource {
  static {
    __name(this, "Stream");
  }
  constructor() {
    super(...arguments);
    this.audioTracks = new AudioTracks(this._client);
    this.videos = new Videos(this._client);
    this.clip = new ClipResource(this._client);
    this.copy = new Copy(this._client);
    this.directUpload = new DirectUpload(this._client);
    this.keys = new Keys3(this._client);
    this.liveInputs = new LiveInputs(this._client);
    this.watermarks = new Watermarks(this._client);
    this.webhooks = new Webhooks2(this._client);
    this.captions = new Captions(this._client);
    this.downloads = new Downloads2(this._client);
    this.embed = new Embed(this._client);
    this.token = new Token(this._client);
  }
  create(params, options) {
    const { account_id, body, "Tus-Resumable": tusResumable, "Upload-Length": uploadLength, direct_user, "Upload-Creator": uploadCreator, "Upload-Metadata": uploadMetadata } = params;
    return this._client.post(`/accounts/${account_id}/stream`, {
      query: { direct_user },
      body,
      ...options,
      headers: {
        Accept: "*/*",
        "Tus-Resumable": tusResumable.toString(),
        "Upload-Length": uploadLength.toString(),
        ...uploadCreator != null ? { "Upload-Creator": uploadCreator } : void 0,
        ...uploadMetadata != null ? { "Upload-Metadata": uploadMetadata } : void 0,
        ...options?.headers
      }
    });
  }
  list(params, options) {
    const { account_id, ...query } = params;
    return this._client.getAPIList(`/accounts/${account_id}/stream`, VideosSinglePage, { query, ...options });
  }
  delete(identifier, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/stream/${identifier}`, {
      ...options,
      headers: { Accept: "*/*", ...options?.headers }
    });
  }
  edit(identifier, params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/stream/${identifier}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  get(identifier, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/stream/${identifier}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var VideosSinglePage = class extends SinglePage {
  static {
    __name(this, "VideosSinglePage");
  }
};
Stream.VideosSinglePage = VideosSinglePage;
Stream.AudioTracks = AudioTracks;
Stream.AudioSinglePage = AudioSinglePage;
Stream.Videos = Videos;
Stream.ClipResource = ClipResource;
Stream.Copy = Copy;
Stream.DirectUpload = DirectUpload;
Stream.KeyGetResponsesSinglePage = KeyGetResponsesSinglePage;
Stream.LiveInputs = LiveInputs;
Stream.Watermarks = Watermarks;
Stream.WatermarksSinglePage = WatermarksSinglePage;
Stream.Webhooks = Webhooks2;
Stream.Captions = Captions;
Stream.CaptionsSinglePage = CaptionsSinglePage;
Stream.Downloads = Downloads2;
Stream.Embed = Embed;
Stream.Token = Token;
var Widgets = class extends APIResource {
  static {
    __name(this, "Widgets");
  }
  create(params, options) {
    const { account_id, direction, order, page, per_page, ...body } = params;
    return this._client.post(`/accounts/${account_id}/challenges/widgets`, {
      query: { direction, order, page, per_page },
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  update(sitekey, params, options) {
    const { account_id, ...body } = params;
    return this._client.put(`/accounts/${account_id}/challenges/widgets/${sitekey}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { account_id, ...query } = params;
    return this._client.getAPIList(`/accounts/${account_id}/challenges/widgets`, WidgetListResponsesV4PagePaginationArray, { query, ...options });
  }
  delete(sitekey, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/challenges/widgets/${sitekey}`, options)._thenUnwrap((obj) => obj.result);
  }
  get(sitekey, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/challenges/widgets/${sitekey}`, options)._thenUnwrap((obj) => obj.result);
  }
  rotateSecret(sitekey, params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/challenges/widgets/${sitekey}/rotate_secret`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
};
var WidgetListResponsesV4PagePaginationArray = class extends V4PagePaginationArray {
  static {
    __name(this, "WidgetListResponsesV4PagePaginationArray");
  }
};
Widgets.WidgetListResponsesV4PagePaginationArray = WidgetListResponsesV4PagePaginationArray;
var Turnstile = class extends APIResource {
  static {
    __name(this, "Turnstile");
  }
  constructor() {
    super(...arguments);
    this.widgets = new Widgets(this._client);
  }
};
Turnstile.Widgets = Widgets;
Turnstile.WidgetListResponsesV4PagePaginationArray = WidgetListResponsesV4PagePaginationArray;
var URLNormalization = class extends APIResource {
  static {
    __name(this, "URLNormalization");
  }
  update(params, options) {
    const { zone_id, ...body } = params;
    return this._client.put(`/zones/${zone_id}/url_normalization`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  delete(params, options) {
    const { zone_id } = params;
    return this._client.delete(`/zones/${zone_id}/url_normalization`, {
      ...options,
      headers: { Accept: "*/*", ...options?.headers }
    });
  }
  get(params, options) {
    const { zone_id } = params;
    return this._client.get(`/zones/${zone_id}/url_normalization`, options)._thenUnwrap((obj) => obj.result);
  }
};
var Responses = class extends APIResource {
  static {
    __name(this, "Responses");
  }
  get(responseId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/urlscanner/v2/responses/${responseId}`, {
      ...options,
      headers: { Accept: "text/plain", ...options?.headers }
    });
  }
};
var Scans2 = class extends APIResource {
  static {
    __name(this, "Scans2");
  }
  create(params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/urlscanner/v2/scan`, { body, ...options });
  }
  list(params, options) {
    const { account_id, ...query } = params;
    return this._client.get(`/accounts/${account_id}/urlscanner/v2/search`, { query, ...options });
  }
  bulkCreate(params, options) {
    const { account_id, body } = params ?? {};
    return this._client.post(`/accounts/${account_id}/urlscanner/v2/bulk`, { body, ...options });
  }
  dom(scanId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/urlscanner/v2/dom/${scanId}`, {
      ...options,
      headers: { Accept: "text/plain", ...options?.headers }
    });
  }
  get(scanId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/urlscanner/v2/result/${scanId}`, options);
  }
  har(scanId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/urlscanner/v2/har/${scanId}`, options);
  }
  screenshot(scanId, params, options) {
    const { account_id, ...query } = params;
    return this._client.get(`/accounts/${account_id}/urlscanner/v2/screenshots/${scanId}.png`, {
      query,
      ...options,
      headers: { Accept: "image/png", ...options?.headers },
      __binaryResponse: true
    });
  }
};
var URLScanner = class extends APIResource {
  static {
    __name(this, "URLScanner");
  }
  constructor() {
    super(...arguments);
    this.responses = new Responses(this._client);
    this.scans = new Scans2(this._client);
  }
};
URLScanner.Responses = Responses;
URLScanner.Scans = Scans2;
var AuditLogs2 = class extends APIResource {
  static {
    __name(this, "AuditLogs2");
  }
  list(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.list({}, query);
    }
    return this._client.getAPIList("/user/audit_logs", AuditLogsV4PagePaginationArray, { query, ...options });
  }
};
var Invites = class extends APIResource {
  static {
    __name(this, "Invites");
  }
  list(options) {
    return this._client.getAPIList("/user/invites", InvitesSinglePage, options);
  }
  edit(inviteId, body, options) {
    return this._client.patch(`/user/invites/${inviteId}`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  get(inviteId, options) {
    return this._client.get(`/user/invites/${inviteId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var InvitesSinglePage = class extends SinglePage {
  static {
    __name(this, "InvitesSinglePage");
  }
};
Invites.InvitesSinglePage = InvitesSinglePage;
var Organizations = class extends APIResource {
  static {
    __name(this, "Organizations");
  }
  list(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.list({}, query);
    }
    return this._client.getAPIList("/user/organizations", OrganizationsV4PagePaginationArray, {
      query,
      ...options
    });
  }
  delete(organizationId, options) {
    return this._client.delete(`/user/organizations/${organizationId}`, options);
  }
  get(organizationId, options) {
    return this._client.get(`/user/organizations/${organizationId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var OrganizationsV4PagePaginationArray = class extends V4PagePaginationArray {
  static {
    __name(this, "OrganizationsV4PagePaginationArray");
  }
};
Organizations.OrganizationsV4PagePaginationArray = OrganizationsV4PagePaginationArray;
var Subscriptions3 = class extends APIResource {
  static {
    __name(this, "Subscriptions3");
  }
  update(identifier, body, options) {
    return this._client.put(`/user/subscriptions/${identifier}`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  delete(identifier, options) {
    return this._client.delete(`/user/subscriptions/${identifier}`, options);
  }
  get(options) {
    return this._client.getAPIList("/user/subscriptions", SubscriptionsSinglePage, options);
  }
};
var History2 = class extends APIResource {
  static {
    __name(this, "History2");
  }
  list(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.list({}, query);
    }
    return this._client.getAPIList("/user/billing/history", BillingHistoriesV4PagePaginationArray, {
      query,
      ...options
    });
  }
};
var BillingHistoriesV4PagePaginationArray = class extends V4PagePaginationArray {
  static {
    __name(this, "BillingHistoriesV4PagePaginationArray");
  }
};
History2.BillingHistoriesV4PagePaginationArray = BillingHistoriesV4PagePaginationArray;
var Profile = class extends APIResource {
  static {
    __name(this, "Profile");
  }
  get(options) {
    return this._client.get("/user/billing/profile", options)._thenUnwrap((obj) => obj.result);
  }
};
var Billing2 = class extends APIResource {
  static {
    __name(this, "Billing2");
  }
  constructor() {
    super(...arguments);
    this.history = new History2(this._client);
    this.profile = new Profile(this._client);
  }
};
Billing2.History = History2;
Billing2.BillingHistoriesV4PagePaginationArray = BillingHistoriesV4PagePaginationArray;
Billing2.Profile = Profile;
var PermissionGroups3 = class extends APIResource {
  static {
    __name(this, "PermissionGroups3");
  }
  list(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.list({}, query);
    }
    return this._client.getAPIList("/user/tokens/permission_groups", PermissionGroupListResponsesSinglePage2, {
      query,
      ...options
    });
  }
};
var PermissionGroupListResponsesSinglePage2 = class extends SinglePage {
  static {
    __name(this, "PermissionGroupListResponsesSinglePage2");
  }
};
PermissionGroups3.PermissionGroupListResponsesSinglePage = PermissionGroupListResponsesSinglePage2;
var Value2 = class extends APIResource {
  static {
    __name(this, "Value2");
  }
  update(tokenId, body, options) {
    return this._client.put(`/user/tokens/${tokenId}/value`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
};
var Tokens3 = class extends APIResource {
  static {
    __name(this, "Tokens3");
  }
  constructor() {
    super(...arguments);
    this.permissionGroups = new PermissionGroups3(this._client);
    this.value = new Value2(this._client);
  }
  create(body, options) {
    return this._client.post("/user/tokens", { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  update(tokenId, body, options) {
    return this._client.put(`/user/tokens/${tokenId}`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  list(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.list({}, query);
    }
    return this._client.getAPIList("/user/tokens", TokensV4PagePaginationArray, { query, ...options });
  }
  delete(tokenId, options) {
    return this._client.delete(`/user/tokens/${tokenId}`, options)._thenUnwrap((obj) => obj.result);
  }
  get(tokenId, options) {
    return this._client.get(`/user/tokens/${tokenId}`, options)._thenUnwrap((obj) => obj.result);
  }
  verify(options) {
    return this._client.get("/user/tokens/verify", options)._thenUnwrap((obj) => obj.result);
  }
};
Tokens3.PermissionGroups = PermissionGroups3;
Tokens3.PermissionGroupListResponsesSinglePage = PermissionGroupListResponsesSinglePage2;
Tokens3.Value = Value2;
var User = class extends APIResource {
  static {
    __name(this, "User");
  }
  constructor() {
    super(...arguments);
    this.auditLogs = new AuditLogs2(this._client);
    this.billing = new Billing2(this._client);
    this.invites = new Invites(this._client);
    this.organizations = new Organizations(this._client);
    this.subscriptions = new Subscriptions3(this._client);
    this.tokens = new Tokens3(this._client);
  }
  edit(body, options) {
    return this._client.patch("/user", { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  get(options) {
    return this._client.get("/user", options)._thenUnwrap((obj) => obj.result);
  }
};
User.AuditLogs = AuditLogs2;
User.Billing = Billing2;
User.Invites = Invites;
User.InvitesSinglePage = InvitesSinglePage;
User.Organizations = Organizations;
User.OrganizationsV4PagePaginationArray = OrganizationsV4PagePaginationArray;
User.Subscriptions = Subscriptions3;
User.Tokens = Tokens3;
var MetadataIndex = class extends APIResource {
  static {
    __name(this, "MetadataIndex");
  }
  create(indexName, params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/vectorize/v2/indexes/${indexName}/metadata_index/create`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(indexName, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/vectorize/v2/indexes/${indexName}/metadata_index/list`, options)._thenUnwrap((obj) => obj.result);
  }
  delete(indexName, params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/vectorize/v2/indexes/${indexName}/metadata_index/delete`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
};
var Indexes = class extends APIResource {
  static {
    __name(this, "Indexes");
  }
  constructor() {
    super(...arguments);
    this.metadataIndex = new MetadataIndex(this._client);
  }
  create(params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/vectorize/v2/indexes`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { account_id } = params;
    return this._client.getAPIList(`/accounts/${account_id}/vectorize/v2/indexes`, CreateIndicesSinglePage, options);
  }
  delete(indexName, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/vectorize/v2/indexes/${indexName}`, options)._thenUnwrap((obj) => obj.result);
  }
  deleteByIds(indexName, params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/vectorize/v2/indexes/${indexName}/delete_by_ids`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  get(indexName, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/vectorize/v2/indexes/${indexName}`, options)._thenUnwrap((obj) => obj.result);
  }
  getByIds(indexName, params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/vectorize/v2/indexes/${indexName}/get_by_ids`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  info(indexName, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/vectorize/v2/indexes/${indexName}/info`, options)._thenUnwrap((obj) => obj.result);
  }
  insert(indexName, params, options) {
    const { account_id, body, "unparsable-behavior": unparsableBehavior } = params;
    return this._client.post(`/accounts/${account_id}/vectorize/v2/indexes/${indexName}/insert`, {
      query: { "unparsable-behavior": unparsableBehavior },
      body,
      ...options,
      headers: { "Content-Type": "application/x-ndjson", ...options?.headers },
      __binaryRequest: true
    })._thenUnwrap((obj) => obj.result);
  }
  listVectors(indexName, params, options) {
    const { account_id, ...query } = params;
    return this._client.get(`/accounts/${account_id}/vectorize/v2/indexes/${indexName}/list`, {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  query(indexName, params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/vectorize/v2/indexes/${indexName}/query`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  upsert(indexName, params, options) {
    const { account_id, body, "unparsable-behavior": unparsableBehavior } = params;
    return this._client.post(`/accounts/${account_id}/vectorize/v2/indexes/${indexName}/upsert`, {
      query: { "unparsable-behavior": unparsableBehavior },
      body,
      ...options,
      headers: { "Content-Type": "application/x-ndjson", ...options?.headers },
      __binaryRequest: true
    })._thenUnwrap((obj) => obj.result);
  }
};
var CreateIndicesSinglePage = class extends SinglePage {
  static {
    __name(this, "CreateIndicesSinglePage");
  }
};
Indexes.CreateIndicesSinglePage = CreateIndicesSinglePage;
Indexes.MetadataIndex = MetadataIndex;
var Vectorize = class extends APIResource {
  static {
    __name(this, "Vectorize");
  }
  constructor() {
    super(...arguments);
    this.indexes = new Indexes(this._client);
  }
};
Vectorize.Indexes = Indexes;
Vectorize.CreateIndicesSinglePage = CreateIndicesSinglePage;
var Page = class extends APIResource {
  static {
    __name(this, "Page");
  }
  preview(params, options) {
    const { zone_id, ...body } = params;
    return this._client.post(`/zones/${zone_id}/waiting_rooms/preview`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
};
var Rules10 = class extends APIResource {
  static {
    __name(this, "Rules10");
  }
  create(waitingRoomId, params, options) {
    const { zone_id, rules } = params;
    return this._client.getAPIList(`/zones/${zone_id}/waiting_rooms/${waitingRoomId}/rules`, WaitingRoomRulesSinglePage, { body: rules, method: "post", ...options });
  }
  update(waitingRoomId, params, options) {
    const { zone_id, rules } = params;
    return this._client.getAPIList(`/zones/${zone_id}/waiting_rooms/${waitingRoomId}/rules`, WaitingRoomRulesSinglePage, { body: rules, method: "put", ...options });
  }
  delete(waitingRoomId, ruleId, params, options) {
    const { zone_id } = params;
    return this._client.getAPIList(`/zones/${zone_id}/waiting_rooms/${waitingRoomId}/rules/${ruleId}`, WaitingRoomRulesSinglePage, { method: "delete", ...options });
  }
  edit(waitingRoomId, ruleId, params, options) {
    const { zone_id, ...body } = params;
    return this._client.getAPIList(`/zones/${zone_id}/waiting_rooms/${waitingRoomId}/rules/${ruleId}`, WaitingRoomRulesSinglePage, { body, method: "patch", ...options });
  }
  get(waitingRoomId, params, options) {
    const { zone_id } = params;
    return this._client.getAPIList(`/zones/${zone_id}/waiting_rooms/${waitingRoomId}/rules`, WaitingRoomRulesSinglePage, options);
  }
};
var WaitingRoomRulesSinglePage = class extends SinglePage {
  static {
    __name(this, "WaitingRoomRulesSinglePage");
  }
};
Rules10.WaitingRoomRulesSinglePage = WaitingRoomRulesSinglePage;
var Settings10 = class extends APIResource {
  static {
    __name(this, "Settings10");
  }
  update(params, options) {
    const { zone_id, ...body } = params;
    return this._client.put(`/zones/${zone_id}/waiting_rooms/settings`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  edit(params, options) {
    const { zone_id, ...body } = params;
    return this._client.patch(`/zones/${zone_id}/waiting_rooms/settings`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  get(params, options) {
    const { zone_id } = params;
    return this._client.get(`/zones/${zone_id}/waiting_rooms/settings`, options)._thenUnwrap((obj) => obj.result);
  }
};
var Statuses = class extends APIResource {
  static {
    __name(this, "Statuses");
  }
  get(waitingRoomId, params, options) {
    const { zone_id } = params;
    return this._client.get(`/zones/${zone_id}/waiting_rooms/${waitingRoomId}/status`, options)._thenUnwrap((obj) => obj.result);
  }
};
var Details = class extends APIResource {
  static {
    __name(this, "Details");
  }
  get(waitingRoomId, eventId, params, options) {
    const { zone_id } = params;
    return this._client.get(`/zones/${zone_id}/waiting_rooms/${waitingRoomId}/events/${eventId}/details`, options)._thenUnwrap((obj) => obj.result);
  }
};
var Events5 = class extends APIResource {
  static {
    __name(this, "Events5");
  }
  constructor() {
    super(...arguments);
    this.details = new Details(this._client);
  }
  create(waitingRoomId, params, options) {
    const { zone_id, ...body } = params;
    return this._client.post(`/zones/${zone_id}/waiting_rooms/${waitingRoomId}/events`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  update(waitingRoomId, eventId, params, options) {
    const { zone_id, ...body } = params;
    return this._client.put(`/zones/${zone_id}/waiting_rooms/${waitingRoomId}/events/${eventId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(waitingRoomId, params, options) {
    const { zone_id, ...query } = params;
    return this._client.getAPIList(`/zones/${zone_id}/waiting_rooms/${waitingRoomId}/events`, EventsV4PagePaginationArray, { query, ...options });
  }
  delete(waitingRoomId, eventId, params, options) {
    const { zone_id } = params;
    return this._client.delete(`/zones/${zone_id}/waiting_rooms/${waitingRoomId}/events/${eventId}`, options)._thenUnwrap((obj) => obj.result);
  }
  edit(waitingRoomId, eventId, params, options) {
    const { zone_id, ...body } = params;
    return this._client.patch(`/zones/${zone_id}/waiting_rooms/${waitingRoomId}/events/${eventId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  get(waitingRoomId, eventId, params, options) {
    const { zone_id } = params;
    return this._client.get(`/zones/${zone_id}/waiting_rooms/${waitingRoomId}/events/${eventId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var EventsV4PagePaginationArray = class extends V4PagePaginationArray {
  static {
    __name(this, "EventsV4PagePaginationArray");
  }
};
Events5.EventsV4PagePaginationArray = EventsV4PagePaginationArray;
Events5.Details = Details;
var WaitingRooms = class extends APIResource {
  static {
    __name(this, "WaitingRooms");
  }
  constructor() {
    super(...arguments);
    this.page = new Page(this._client);
    this.events = new Events5(this._client);
    this.rules = new Rules10(this._client);
    this.statuses = new Statuses(this._client);
    this.settings = new Settings10(this._client);
  }
  create(params, options) {
    const { zone_id, ...body } = params;
    return this._client.post(`/zones/${zone_id}/waiting_rooms`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  update(waitingRoomId, params, options) {
    const { zone_id, ...body } = params;
    return this._client.put(`/zones/${zone_id}/waiting_rooms/${waitingRoomId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(params = {}, options) {
    if (isRequestOptions(params)) {
      return this.list({}, params);
    }
    const { account_id, zone_id, ...query } = params;
    if (!account_id && !zone_id) {
      throw new CloudflareError("You must provide either account_id or zone_id.");
    }
    if (account_id && zone_id) {
      throw new CloudflareError("You cannot provide both account_id and zone_id.");
    }
    const { accountOrZone, accountOrZoneId } = account_id ? {
      accountOrZone: "accounts",
      accountOrZoneId: account_id
    } : {
      accountOrZone: "zones",
      accountOrZoneId: zone_id
    };
    return this._client.getAPIList(`/${accountOrZone}/${accountOrZoneId}/waiting_rooms`, WaitingRoomsV4PagePaginationArray, { query, ...options });
  }
  delete(waitingRoomId, params, options) {
    const { zone_id } = params;
    return this._client.delete(`/zones/${zone_id}/waiting_rooms/${waitingRoomId}`, options)._thenUnwrap((obj) => obj.result);
  }
  edit(waitingRoomId, params, options) {
    const { zone_id, ...body } = params;
    return this._client.patch(`/zones/${zone_id}/waiting_rooms/${waitingRoomId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  get(waitingRoomId, params, options) {
    const { zone_id } = params;
    return this._client.get(`/zones/${zone_id}/waiting_rooms/${waitingRoomId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var WaitingRoomsV4PagePaginationArray = class extends V4PagePaginationArray {
  static {
    __name(this, "WaitingRoomsV4PagePaginationArray");
  }
};
WaitingRooms.WaitingRoomsV4PagePaginationArray = WaitingRoomsV4PagePaginationArray;
WaitingRooms.Page = Page;
WaitingRooms.Events = Events5;
WaitingRooms.EventsV4PagePaginationArray = EventsV4PagePaginationArray;
WaitingRooms.Rules = Rules10;
WaitingRooms.WaitingRoomRulesSinglePage = WaitingRoomRulesSinglePage;
WaitingRooms.Statuses = Statuses;
WaitingRooms.Settings = Settings10;
var Entries = class extends APIResource {
  static {
    __name(this, "Entries");
  }
  create(identifier, params, options) {
    const { zone_id, ...body } = params;
    return this._client.post(`/zones/${zone_id}/web3/hostnames/${identifier}/ipfs_universal_path/content_list/entries`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  update(identifier, contentListEntryIdentifier, params, options) {
    const { zone_id, ...body } = params;
    return this._client.put(`/zones/${zone_id}/web3/hostnames/${identifier}/ipfs_universal_path/content_list/entries/${contentListEntryIdentifier}`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  list(identifier, params, options) {
    const { zone_id } = params;
    return this._client.get(`/zones/${zone_id}/web3/hostnames/${identifier}/ipfs_universal_path/content_list/entries`, options)._thenUnwrap((obj) => obj.result);
  }
  delete(identifier, contentListEntryIdentifier, params, options) {
    const { zone_id } = params;
    return this._client.delete(`/zones/${zone_id}/web3/hostnames/${identifier}/ipfs_universal_path/content_list/entries/${contentListEntryIdentifier}`, options)._thenUnwrap((obj) => obj.result);
  }
  get(identifier, contentListEntryIdentifier, params, options) {
    const { zone_id } = params;
    return this._client.get(`/zones/${zone_id}/web3/hostnames/${identifier}/ipfs_universal_path/content_list/entries/${contentListEntryIdentifier}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var ContentLists = class extends APIResource {
  static {
    __name(this, "ContentLists");
  }
  constructor() {
    super(...arguments);
    this.entries = new Entries(this._client);
  }
  update(identifier, params, options) {
    const { zone_id, ...body } = params;
    return this._client.put(`/zones/${zone_id}/web3/hostnames/${identifier}/ipfs_universal_path/content_list`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  get(identifier, params, options) {
    const { zone_id } = params;
    return this._client.get(`/zones/${zone_id}/web3/hostnames/${identifier}/ipfs_universal_path/content_list`, options)._thenUnwrap((obj) => obj.result);
  }
};
ContentLists.Entries = Entries;
var IPFSUniversalPaths = class extends APIResource {
  static {
    __name(this, "IPFSUniversalPaths");
  }
  constructor() {
    super(...arguments);
    this.contentLists = new ContentLists(this._client);
  }
};
IPFSUniversalPaths.ContentLists = ContentLists;
var Hostnames3 = class extends APIResource {
  static {
    __name(this, "Hostnames3");
  }
  constructor() {
    super(...arguments);
    this.ipfsUniversalPaths = new IPFSUniversalPaths(this._client);
  }
  create(params, options) {
    const { zone_id, ...body } = params;
    return this._client.post(`/zones/${zone_id}/web3/hostnames`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { zone_id } = params;
    return this._client.getAPIList(`/zones/${zone_id}/web3/hostnames`, HostnamesSinglePage, options);
  }
  delete(identifier, params, options) {
    const { zone_id } = params;
    return this._client.delete(`/zones/${zone_id}/web3/hostnames/${identifier}`, options)._thenUnwrap((obj) => obj.result);
  }
  edit(identifier, params, options) {
    const { zone_id, ...body } = params;
    return this._client.patch(`/zones/${zone_id}/web3/hostnames/${identifier}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  get(identifier, params, options) {
    const { zone_id } = params;
    return this._client.get(`/zones/${zone_id}/web3/hostnames/${identifier}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var HostnamesSinglePage = class extends SinglePage {
  static {
    __name(this, "HostnamesSinglePage");
  }
};
Hostnames3.HostnamesSinglePage = HostnamesSinglePage;
Hostnames3.IPFSUniversalPaths = IPFSUniversalPaths;
var Web3 = class extends APIResource {
  static {
    __name(this, "Web3");
  }
  constructor() {
    super(...arguments);
    this.hostnames = new Hostnames3(this._client);
  }
};
Web3.Hostnames = Hostnames3;
Web3.HostnamesSinglePage = HostnamesSinglePage;
var AccountSettings = class extends APIResource {
  static {
    __name(this, "AccountSettings");
  }
  update(params, options) {
    const { account_id, ...body } = params;
    return this._client.put(`/accounts/${account_id}/workers/account-settings`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  get(params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/workers/account-settings`, options)._thenUnwrap((obj) => obj.result);
  }
};
var Domains6 = class extends APIResource {
  static {
    __name(this, "Domains6");
  }
  update(params, options) {
    const { account_id, ...body } = params;
    return this._client.put(`/accounts/${account_id}/workers/domains`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { account_id, ...query } = params;
    return this._client.getAPIList(`/accounts/${account_id}/workers/domains`, DomainsSinglePage2, {
      query,
      ...options
    });
  }
  delete(domainId, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/workers/domains/${domainId}`, {
      ...options,
      headers: { Accept: "*/*", ...options?.headers }
    });
  }
  get(domainId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/workers/domains/${domainId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var DomainsSinglePage2 = class extends SinglePage {
  static {
    __name(this, "DomainsSinglePage2");
  }
};
Domains6.DomainsSinglePage = DomainsSinglePage2;
var Routes3 = class extends APIResource {
  static {
    __name(this, "Routes3");
  }
  create(params, options) {
    const { zone_id, ...body } = params;
    return this._client.post(`/zones/${zone_id}/workers/routes`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  update(routeId, params, options) {
    const { zone_id, ...body } = params;
    return this._client.put(`/zones/${zone_id}/workers/routes/${routeId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { zone_id } = params;
    return this._client.getAPIList(`/zones/${zone_id}/workers/routes`, RouteListResponsesSinglePage, options);
  }
  delete(routeId, params, options) {
    const { zone_id } = params;
    return this._client.delete(`/zones/${zone_id}/workers/routes/${routeId}`, options)._thenUnwrap((obj) => obj.result);
  }
  get(routeId, params, options) {
    const { zone_id } = params;
    return this._client.get(`/zones/${zone_id}/workers/routes/${routeId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var RouteListResponsesSinglePage = class extends SinglePage {
  static {
    __name(this, "RouteListResponsesSinglePage");
  }
};
Routes3.RouteListResponsesSinglePage = RouteListResponsesSinglePage;
var Subdomains = class extends APIResource {
  static {
    __name(this, "Subdomains");
  }
  update(params, options) {
    const { account_id, ...body } = params;
    return this._client.put(`/accounts/${account_id}/workers/subdomain`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  get(params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/workers/subdomain`, options)._thenUnwrap((obj) => obj.result);
  }
};
var Upload = class extends APIResource {
  static {
    __name(this, "Upload");
  }
  create(params, options) {
    const { account_id, base64, body } = params;
    return this._client.post(`/accounts/${account_id}/workers/assets/upload`, multipartFormRequestOptions({ query: { base64 }, body, ...options }))._thenUnwrap((obj) => obj.result);
  }
};
var Assets3 = class extends APIResource {
  static {
    __name(this, "Assets3");
  }
  constructor() {
    super(...arguments);
    this.upload = new Upload(this._client);
  }
};
Assets3.Upload = Upload;
var Versions3 = class extends APIResource {
  static {
    __name(this, "Versions3");
  }
  create(workerId, params, options) {
    const { account_id, deploy, ...body } = params;
    return this._client.post(`/accounts/${account_id}/workers/workers/${workerId}/versions`, {
      query: { deploy },
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(workerId, params, options) {
    const { account_id, ...query } = params;
    return this._client.getAPIList(`/accounts/${account_id}/workers/workers/${workerId}/versions`, VersionsV4PagePaginationArray, { query, ...options });
  }
  delete(workerId, versionId, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/workers/workers/${workerId}/versions/${versionId}`, options);
  }
  get(workerId, versionId, params, options) {
    const { account_id, ...query } = params;
    return this._client.get(`/accounts/${account_id}/workers/workers/${workerId}/versions/${versionId}`, {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
};
var VersionsV4PagePaginationArray = class extends V4PagePaginationArray {
  static {
    __name(this, "VersionsV4PagePaginationArray");
  }
};
Versions3.VersionsV4PagePaginationArray = VersionsV4PagePaginationArray;
var Workers = class extends APIResource {
  static {
    __name(this, "Workers");
  }
  constructor() {
    super(...arguments);
    this.versions = new Versions3(this._client);
  }
  create(params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/workers/workers`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  update(workerId, params, options) {
    const { account_id, ...body } = params;
    return this._client.put(`/accounts/${account_id}/workers/workers/${workerId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { account_id, ...query } = params;
    return this._client.getAPIList(`/accounts/${account_id}/workers/workers`, WorkersV4PagePaginationArray, {
      query,
      ...options
    });
  }
  delete(workerId, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/workers/workers/${workerId}`, options);
  }
  edit(workerId, params, options) {
    const { account_id, ...body } = params;
    return this._client.patch(`/accounts/${account_id}/workers/workers/${workerId}`, {
      body,
      ...options,
      headers: { "Content-Type": "application/merge-patch+json", ...options?.headers }
    })._thenUnwrap((obj) => obj.result);
  }
  get(workerId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/workers/workers/${workerId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var WorkersV4PagePaginationArray = class extends V4PagePaginationArray {
  static {
    __name(this, "WorkersV4PagePaginationArray");
  }
};
Workers.WorkersV4PagePaginationArray = WorkersV4PagePaginationArray;
Workers.Versions = Versions3;
Workers.VersionsV4PagePaginationArray = VersionsV4PagePaginationArray;
var Beta = class extends APIResource {
  static {
    __name(this, "Beta");
  }
  constructor() {
    super(...arguments);
    this.workers = new Workers(this._client);
  }
};
Beta.Workers = Workers;
Beta.WorkersV4PagePaginationArray = WorkersV4PagePaginationArray;
var Telemetry = class extends APIResource {
  static {
    __name(this, "Telemetry");
  }
  keys(params, options) {
    const { account_id, ...body } = params;
    return this._client.getAPIList(`/accounts/${account_id}/workers/observability/telemetry/keys`, TelemetryKeysResponsesSinglePage, { body, method: "post", ...options });
  }
  query(params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/workers/observability/telemetry/query`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  values(params, options) {
    const { account_id, ...body } = params;
    return this._client.getAPIList(`/accounts/${account_id}/workers/observability/telemetry/values`, TelemetryValuesResponsesSinglePage, { body, method: "post", ...options });
  }
};
var TelemetryKeysResponsesSinglePage = class extends SinglePage {
  static {
    __name(this, "TelemetryKeysResponsesSinglePage");
  }
};
var TelemetryValuesResponsesSinglePage = class extends SinglePage {
  static {
    __name(this, "TelemetryValuesResponsesSinglePage");
  }
};
Telemetry.TelemetryKeysResponsesSinglePage = TelemetryKeysResponsesSinglePage;
Telemetry.TelemetryValuesResponsesSinglePage = TelemetryValuesResponsesSinglePage;
var Observability = class extends APIResource {
  static {
    __name(this, "Observability");
  }
  constructor() {
    super(...arguments);
    this.telemetry = new Telemetry(this._client);
  }
};
Observability.Telemetry = Telemetry;
Observability.TelemetryKeysResponsesSinglePage = TelemetryKeysResponsesSinglePage;
Observability.TelemetryValuesResponsesSinglePage = TelemetryValuesResponsesSinglePage;
var Content3 = class extends APIResource {
  static {
    __name(this, "Content3");
  }
  update(scriptName, params, options) {
    const { account_id, "CF-WORKER-BODY-PART": cfWorkerBodyPart, "CF-WORKER-MAIN-MODULE-PART": cfWorkerMainModulePart, ...body } = params;
    return this._client.put(`/accounts/${account_id}/workers/scripts/${scriptName}/content`, multipartFormRequestOptions({
      body,
      ...options,
      __multipartSyntax: "json",
      headers: {
        ...cfWorkerBodyPart != null ? { "CF-WORKER-BODY-PART": cfWorkerBodyPart } : void 0,
        ...cfWorkerMainModulePart != null ? { "CF-WORKER-MAIN-MODULE-PART": cfWorkerMainModulePart } : void 0,
        ...options?.headers
      }
    }))._thenUnwrap((obj) => obj.result);
  }
  get(scriptName, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/workers/scripts/${scriptName}/content/v2`, {
      ...options,
      headers: { Accept: "string", ...options?.headers },
      __binaryResponse: true
    });
  }
};
var Deployments2 = class extends APIResource {
  static {
    __name(this, "Deployments2");
  }
  create(scriptName, params, options) {
    const { account_id, force, ...body } = params;
    return this._client.post(`/accounts/${account_id}/workers/scripts/${scriptName}/deployments`, {
      query: { force },
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(scriptName, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/workers/scripts/${scriptName}/deployments`, options)._thenUnwrap((obj) => obj.result);
  }
  delete(scriptName, deploymentId, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/workers/scripts/${scriptName}/deployments/${deploymentId}`, options);
  }
  get(scriptName, deploymentId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/workers/scripts/${scriptName}/deployments/${deploymentId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var Schedules = class extends APIResource {
  static {
    __name(this, "Schedules");
  }
  update(scriptName, params, options) {
    const { account_id, body } = params;
    return this._client.put(`/accounts/${account_id}/workers/scripts/${scriptName}/schedules`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  get(scriptName, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/workers/scripts/${scriptName}/schedules`, options)._thenUnwrap((obj) => obj.result);
  }
};
var ScriptAndVersionSettings = class extends APIResource {
  static {
    __name(this, "ScriptAndVersionSettings");
  }
  edit(scriptName, params, options) {
    const { account_id, ...body } = params;
    return this._client.patch(`/accounts/${account_id}/workers/scripts/${scriptName}/settings`, multipartFormRequestOptions({ body, ...options, __multipartSyntax: "json" }))._thenUnwrap((obj) => obj.result);
  }
  get(scriptName, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/workers/scripts/${scriptName}/settings`, options)._thenUnwrap((obj) => obj.result);
  }
};
var Secrets2 = class extends APIResource {
  static {
    __name(this, "Secrets2");
  }
  update(scriptName, params, options) {
    const { account_id, ...body } = params;
    return this._client.put(`/accounts/${account_id}/workers/scripts/${scriptName}/secrets`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(scriptName, params, options) {
    const { account_id } = params;
    return this._client.getAPIList(`/accounts/${account_id}/workers/scripts/${scriptName}/secrets`, SecretListResponsesSinglePage, options);
  }
  delete(scriptName, secretName, params, options) {
    const { account_id, url_encoded } = params;
    return this._client.delete(`/accounts/${account_id}/workers/scripts/${scriptName}/secrets/${secretName}`, {
      query: { url_encoded },
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  get(scriptName, secretName, params, options) {
    const { account_id, ...query } = params;
    return this._client.get(`/accounts/${account_id}/workers/scripts/${scriptName}/secrets/${secretName}`, {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
};
var SecretListResponsesSinglePage = class extends SinglePage {
  static {
    __name(this, "SecretListResponsesSinglePage");
  }
};
Secrets2.SecretListResponsesSinglePage = SecretListResponsesSinglePage;
var Settings11 = class extends APIResource {
  static {
    __name(this, "Settings11");
  }
  edit(scriptName, params, options) {
    const { account_id, ...body } = params;
    return this._client.patch(`/accounts/${account_id}/workers/scripts/${scriptName}/script-settings`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  get(scriptName, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/workers/scripts/${scriptName}/script-settings`, options)._thenUnwrap((obj) => obj.result);
  }
};
var Subdomain = class extends APIResource {
  static {
    __name(this, "Subdomain");
  }
  create(scriptName, params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/workers/scripts/${scriptName}/subdomain`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  delete(scriptName, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/workers/scripts/${scriptName}/subdomain`, options)._thenUnwrap((obj) => obj.result);
  }
  get(scriptName, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/workers/scripts/${scriptName}/subdomain`, options)._thenUnwrap((obj) => obj.result);
  }
};
var Tail = class extends APIResource {
  static {
    __name(this, "Tail");
  }
  create(scriptName, params, options) {
    const { account_id, body } = params;
    return this._client.post(`/accounts/${account_id}/workers/scripts/${scriptName}/tails`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  delete(scriptName, id, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/workers/scripts/${scriptName}/tails/${id}`, options);
  }
  get(scriptName, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/workers/scripts/${scriptName}/tails`, options)._thenUnwrap((obj) => obj.result);
  }
};
var Versions4 = class extends APIResource {
  static {
    __name(this, "Versions4");
  }
  create(scriptName, params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/workers/scripts/${scriptName}/versions`, multipartFormRequestOptions({ body, ...options, __multipartSyntax: "json" }))._thenUnwrap((obj) => obj.result);
  }
  list(scriptName, params, options) {
    const { account_id, ...query } = params;
    return this._client.getAPIList(`/accounts/${account_id}/workers/scripts/${scriptName}/versions`, VersionListResponsesV4PagePagination, { query, ...options });
  }
  get(scriptName, versionId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/workers/scripts/${scriptName}/versions/${versionId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var VersionListResponsesV4PagePagination = class extends V4PagePagination {
  static {
    __name(this, "VersionListResponsesV4PagePagination");
  }
};
Versions4.VersionListResponsesV4PagePagination = VersionListResponsesV4PagePagination;
var Upload2 = class extends APIResource {
  static {
    __name(this, "Upload2");
  }
  create(scriptName, params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/workers/scripts/${scriptName}/assets-upload-session`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
};
var Assets4 = class extends APIResource {
  static {
    __name(this, "Assets4");
  }
  constructor() {
    super(...arguments);
    this.upload = new Upload2(this._client);
  }
};
Assets4.Upload = Upload2;
var Scripts2 = class extends APIResource {
  static {
    __name(this, "Scripts2");
  }
  constructor() {
    super(...arguments);
    this.assets = new Assets4(this._client);
    this.subdomain = new Subdomain(this._client);
    this.schedules = new Schedules(this._client);
    this.tail = new Tail(this._client);
    this.content = new Content3(this._client);
    this.settings = new Settings11(this._client);
    this.deployments = new Deployments2(this._client);
    this.versions = new Versions4(this._client);
    this.secrets = new Secrets2(this._client);
    this.scriptAndVersionSettings = new ScriptAndVersionSettings(this._client);
  }
  update(scriptName, params, options) {
    const { account_id, ...body } = params;
    return this._client.put(`/accounts/${account_id}/workers/scripts/${scriptName}`, maybeMultipartFormRequestOptions({
      body,
      ...options,
      __multipartSyntax: "json",
      headers: { "Content-Type": "application/javascript", ...options?.headers }
    }))._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { account_id, ...query } = params;
    return this._client.getAPIList(`/accounts/${account_id}/workers/scripts`, ScriptsSinglePage2, {
      query,
      ...options
    });
  }
  delete(scriptName, params, options) {
    const { account_id, force } = params;
    return this._client.delete(`/accounts/${account_id}/workers/scripts/${scriptName}`, {
      query: { force },
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  get(scriptName, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/workers/scripts/${scriptName}`, {
      ...options,
      headers: { Accept: "application/javascript", ...options?.headers }
    });
  }
  search(params, options) {
    const { account_id, ...query } = params;
    return this._client.get(`/accounts/${account_id}/workers/scripts-search`, {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
};
var ScriptsSinglePage2 = class extends SinglePage {
  static {
    __name(this, "ScriptsSinglePage2");
  }
};
Scripts2.ScriptsSinglePage = ScriptsSinglePage2;
Scripts2.Assets = Assets4;
Scripts2.Subdomain = Subdomain;
Scripts2.Schedules = Schedules;
Scripts2.Tail = Tail;
Scripts2.Content = Content3;
Scripts2.Settings = Settings11;
Scripts2.Deployments = Deployments2;
Scripts2.Versions = Versions4;
Scripts2.VersionListResponsesV4PagePagination = VersionListResponsesV4PagePagination;
Scripts2.Secrets = Secrets2;
Scripts2.SecretListResponsesSinglePage = SecretListResponsesSinglePage;
Scripts2.ScriptAndVersionSettings = ScriptAndVersionSettings;
var Workers2 = class extends APIResource {
  static {
    __name(this, "Workers2");
  }
  constructor() {
    super(...arguments);
    this.beta = new Beta(this._client);
    this.routes = new Routes3(this._client);
    this.assets = new Assets3(this._client);
    this.scripts = new Scripts2(this._client);
    this.accountSettings = new AccountSettings(this._client);
    this.domains = new Domains6(this._client);
    this.subdomains = new Subdomains(this._client);
    this.observability = new Observability(this._client);
  }
};
Workers2.Beta = Beta;
Workers2.Routes = Routes3;
Workers2.RouteListResponsesSinglePage = RouteListResponsesSinglePage;
Workers2.Assets = Assets3;
Workers2.Scripts = Scripts2;
Workers2.ScriptsSinglePage = ScriptsSinglePage2;
Workers2.AccountSettings = AccountSettings;
Workers2.Domains = Domains6;
Workers2.DomainsSinglePage = DomainsSinglePage2;
Workers2.Subdomains = Subdomains;
Workers2.Observability = Observability;
var AssetUpload = class extends APIResource {
  static {
    __name(this, "AssetUpload");
  }
  create(dispatchNamespace, scriptName, params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/workers/dispatch/namespaces/${dispatchNamespace}/scripts/${scriptName}/assets-upload-session`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
};
var Bindings = class extends APIResource {
  static {
    __name(this, "Bindings");
  }
  get(dispatchNamespace, scriptName, params, options) {
    const { account_id } = params;
    return this._client.getAPIList(`/accounts/${account_id}/workers/dispatch/namespaces/${dispatchNamespace}/scripts/${scriptName}/bindings`, BindingGetResponsesSinglePage, options);
  }
};
var BindingGetResponsesSinglePage = class extends SinglePage {
  static {
    __name(this, "BindingGetResponsesSinglePage");
  }
};
Bindings.BindingGetResponsesSinglePage = BindingGetResponsesSinglePage;
var Content4 = class extends APIResource {
  static {
    __name(this, "Content4");
  }
  update(dispatchNamespace, scriptName, params, options) {
    const { account_id, "CF-WORKER-BODY-PART": cfWorkerBodyPart, "CF-WORKER-MAIN-MODULE-PART": cfWorkerMainModulePart, ...body } = params;
    return this._client.put(`/accounts/${account_id}/workers/dispatch/namespaces/${dispatchNamespace}/scripts/${scriptName}/content`, multipartFormRequestOptions({
      body,
      ...options,
      __multipartSyntax: "json",
      headers: {
        ...cfWorkerBodyPart != null ? { "CF-WORKER-BODY-PART": cfWorkerBodyPart } : void 0,
        ...cfWorkerMainModulePart != null ? { "CF-WORKER-MAIN-MODULE-PART": cfWorkerMainModulePart } : void 0,
        ...options?.headers
      }
    }))._thenUnwrap((obj) => obj.result);
  }
  get(dispatchNamespace, scriptName, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/workers/dispatch/namespaces/${dispatchNamespace}/scripts/${scriptName}/content`, { ...options, headers: { Accept: "string", ...options?.headers }, __binaryResponse: true });
  }
};
var Secrets3 = class extends APIResource {
  static {
    __name(this, "Secrets3");
  }
  update(dispatchNamespace, scriptName, params, options) {
    const { account_id, ...body } = params;
    return this._client.put(`/accounts/${account_id}/workers/dispatch/namespaces/${dispatchNamespace}/scripts/${scriptName}/secrets`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  list(dispatchNamespace, scriptName, params, options) {
    const { account_id } = params;
    return this._client.getAPIList(`/accounts/${account_id}/workers/dispatch/namespaces/${dispatchNamespace}/scripts/${scriptName}/secrets`, SecretListResponsesSinglePage2, options);
  }
  delete(dispatchNamespace, scriptName, secretName, params, options) {
    const { account_id, url_encoded } = params;
    return this._client.delete(`/accounts/${account_id}/workers/dispatch/namespaces/${dispatchNamespace}/scripts/${scriptName}/secrets/${secretName}`, { query: { url_encoded }, ...options })._thenUnwrap((obj) => obj.result);
  }
  get(dispatchNamespace, scriptName, secretName, params, options) {
    const { account_id, ...query } = params;
    return this._client.get(`/accounts/${account_id}/workers/dispatch/namespaces/${dispatchNamespace}/scripts/${scriptName}/secrets/${secretName}`, { query, ...options })._thenUnwrap((obj) => obj.result);
  }
};
var SecretListResponsesSinglePage2 = class extends SinglePage {
  static {
    __name(this, "SecretListResponsesSinglePage2");
  }
};
Secrets3.SecretListResponsesSinglePage = SecretListResponsesSinglePage2;
var Settings12 = class extends APIResource {
  static {
    __name(this, "Settings12");
  }
  edit(dispatchNamespace, scriptName, params, options) {
    const { account_id, ...body } = params;
    return this._client.patch(`/accounts/${account_id}/workers/dispatch/namespaces/${dispatchNamespace}/scripts/${scriptName}/settings`, multipartFormRequestOptions({ body, ...options, __multipartSyntax: "json" }))._thenUnwrap((obj) => obj.result);
  }
  get(dispatchNamespace, scriptName, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/workers/dispatch/namespaces/${dispatchNamespace}/scripts/${scriptName}/settings`, options)._thenUnwrap((obj) => obj.result);
  }
};
var Tags2 = class extends APIResource {
  static {
    __name(this, "Tags2");
  }
  update(dispatchNamespace, scriptName, params, options) {
    const { account_id, body } = params;
    return this._client.getAPIList(`/accounts/${account_id}/workers/dispatch/namespaces/${dispatchNamespace}/scripts/${scriptName}/tags`, TagUpdateResponsesSinglePage, { body, method: "put", ...options });
  }
  list(dispatchNamespace, scriptName, params, options) {
    const { account_id } = params;
    return this._client.getAPIList(`/accounts/${account_id}/workers/dispatch/namespaces/${dispatchNamespace}/scripts/${scriptName}/tags`, TagListResponsesSinglePage, options);
  }
  delete(dispatchNamespace, scriptName, tag2, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/workers/dispatch/namespaces/${dispatchNamespace}/scripts/${scriptName}/tags/${tag2}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var TagUpdateResponsesSinglePage = class extends SinglePage {
  static {
    __name(this, "TagUpdateResponsesSinglePage");
  }
};
var TagListResponsesSinglePage = class extends SinglePage {
  static {
    __name(this, "TagListResponsesSinglePage");
  }
};
Tags2.TagUpdateResponsesSinglePage = TagUpdateResponsesSinglePage;
Tags2.TagListResponsesSinglePage = TagListResponsesSinglePage;
var Scripts3 = class extends APIResource {
  static {
    __name(this, "Scripts3");
  }
  constructor() {
    super(...arguments);
    this.assetUpload = new AssetUpload(this._client);
    this.content = new Content4(this._client);
    this.settings = new Settings12(this._client);
    this.bindings = new Bindings(this._client);
    this.secrets = new Secrets3(this._client);
    this.tags = new Tags2(this._client);
  }
  update(dispatchNamespace, scriptName, params, options) {
    const { account_id, ...body } = params;
    return this._client.put(`/accounts/${account_id}/workers/dispatch/namespaces/${dispatchNamespace}/scripts/${scriptName}`, maybeMultipartFormRequestOptions({
      body,
      ...options,
      __multipartSyntax: "json",
      headers: { "Content-Type": "application/javascript", ...options?.headers }
    }))._thenUnwrap((obj) => obj.result);
  }
  delete(dispatchNamespace, scriptName, params, options) {
    const { account_id, force } = params;
    return this._client.delete(`/accounts/${account_id}/workers/dispatch/namespaces/${dispatchNamespace}/scripts/${scriptName}`, { query: { force }, ...options })._thenUnwrap((obj) => obj.result);
  }
  get(dispatchNamespace, scriptName, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/workers/dispatch/namespaces/${dispatchNamespace}/scripts/${scriptName}`, options)._thenUnwrap((obj) => obj.result);
  }
};
Scripts3.AssetUpload = AssetUpload;
Scripts3.Content = Content4;
Scripts3.Settings = Settings12;
Scripts3.Bindings = Bindings;
Scripts3.BindingGetResponsesSinglePage = BindingGetResponsesSinglePage;
Scripts3.Secrets = Secrets3;
Scripts3.SecretListResponsesSinglePage = SecretListResponsesSinglePage2;
Scripts3.Tags = Tags2;
Scripts3.TagUpdateResponsesSinglePage = TagUpdateResponsesSinglePage;
Scripts3.TagListResponsesSinglePage = TagListResponsesSinglePage;
var Namespaces3 = class extends APIResource {
  static {
    __name(this, "Namespaces3");
  }
  constructor() {
    super(...arguments);
    this.scripts = new Scripts3(this._client);
  }
  create(params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/workers/dispatch/namespaces`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { account_id } = params;
    return this._client.getAPIList(`/accounts/${account_id}/workers/dispatch/namespaces`, NamespaceListResponsesSinglePage, options);
  }
  delete(dispatchNamespace, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/workers/dispatch/namespaces/${dispatchNamespace}`, options)._thenUnwrap((obj) => obj.result);
  }
  get(dispatchNamespace, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/workers/dispatch/namespaces/${dispatchNamespace}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var NamespaceListResponsesSinglePage = class extends SinglePage {
  static {
    __name(this, "NamespaceListResponsesSinglePage");
  }
};
Namespaces3.NamespaceListResponsesSinglePage = NamespaceListResponsesSinglePage;
Namespaces3.Scripts = Scripts3;
var Dispatch = class extends APIResource {
  static {
    __name(this, "Dispatch");
  }
  constructor() {
    super(...arguments);
    this.namespaces = new Namespaces3(this._client);
  }
};
Dispatch.Namespaces = Namespaces3;
Dispatch.NamespaceListResponsesSinglePage = NamespaceListResponsesSinglePage;
var WorkersForPlatforms = class extends APIResource {
  static {
    __name(this, "WorkersForPlatforms");
  }
  constructor() {
    super(...arguments);
    this.dispatch = new Dispatch(this._client);
  }
};
WorkersForPlatforms.Dispatch = Dispatch;
var Versions5 = class extends APIResource {
  static {
    __name(this, "Versions5");
  }
  list(workflowName, params, options) {
    const { account_id, ...query } = params;
    return this._client.getAPIList(`/accounts/${account_id}/workflows/${workflowName}/versions`, VersionListResponsesV4PagePaginationArray, { query, ...options });
  }
  get(workflowName, versionId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/workflows/${workflowName}/versions/${versionId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var VersionListResponsesV4PagePaginationArray = class extends V4PagePaginationArray {
  static {
    __name(this, "VersionListResponsesV4PagePaginationArray");
  }
};
Versions5.VersionListResponsesV4PagePaginationArray = VersionListResponsesV4PagePaginationArray;
var Events6 = class extends APIResource {
  static {
    __name(this, "Events6");
  }
  create(workflowName, instanceId, eventType, params, options) {
    const { account_id, body } = params ?? {};
    return this._client.post(`/accounts/${account_id}/workflows/${workflowName}/instances/${instanceId}/events/${eventType}`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
};
var Status2 = class extends APIResource {
  static {
    __name(this, "Status2");
  }
  edit(workflowName, instanceId, params, options) {
    const { account_id, ...body } = params;
    return this._client.patch(`/accounts/${account_id}/workflows/${workflowName}/instances/${instanceId}/status`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
};
var Instances = class extends APIResource {
  static {
    __name(this, "Instances");
  }
  constructor() {
    super(...arguments);
    this.status = new Status2(this._client);
    this.events = new Events6(this._client);
  }
  create(workflowName, params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/workflows/${workflowName}/instances`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(workflowName, params, options) {
    const { account_id, ...query } = params;
    return this._client.getAPIList(`/accounts/${account_id}/workflows/${workflowName}/instances`, InstanceListResponsesV4PagePaginationArray, { query, ...options });
  }
  bulk(workflowName, params, options) {
    const { account_id, body } = params ?? {};
    return this._client.getAPIList(`/accounts/${account_id}/workflows/${workflowName}/instances/batch`, InstanceBulkResponsesSinglePage, { body, method: "post", ...options });
  }
  get(workflowName, instanceId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/workflows/${workflowName}/instances/${instanceId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var InstanceListResponsesV4PagePaginationArray = class extends V4PagePaginationArray {
  static {
    __name(this, "InstanceListResponsesV4PagePaginationArray");
  }
};
var InstanceBulkResponsesSinglePage = class extends SinglePage {
  static {
    __name(this, "InstanceBulkResponsesSinglePage");
  }
};
Instances.InstanceListResponsesV4PagePaginationArray = InstanceListResponsesV4PagePaginationArray;
Instances.InstanceBulkResponsesSinglePage = InstanceBulkResponsesSinglePage;
Instances.Status = Status2;
Instances.Events = Events6;
var Workflows = class extends APIResource {
  static {
    __name(this, "Workflows");
  }
  constructor() {
    super(...arguments);
    this.instances = new Instances(this._client);
    this.versions = new Versions5(this._client);
  }
  update(workflowName, params, options) {
    const { account_id, ...body } = params;
    return this._client.put(`/accounts/${account_id}/workflows/${workflowName}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { account_id, ...query } = params;
    return this._client.getAPIList(`/accounts/${account_id}/workflows`, WorkflowListResponsesV4PagePaginationArray, { query, ...options });
  }
  delete(workflowName, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/workflows/${workflowName}`, options)._thenUnwrap((obj) => obj.result);
  }
  get(workflowName, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/workflows/${workflowName}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var WorkflowListResponsesV4PagePaginationArray = class extends V4PagePaginationArray {
  static {
    __name(this, "WorkflowListResponsesV4PagePaginationArray");
  }
};
Workflows.WorkflowListResponsesV4PagePaginationArray = WorkflowListResponsesV4PagePaginationArray;
Workflows.Instances = Instances;
Workflows.InstanceListResponsesV4PagePaginationArray = InstanceListResponsesV4PagePaginationArray;
Workflows.InstanceBulkResponsesSinglePage = InstanceBulkResponsesSinglePage;
Workflows.Versions = Versions5;
Workflows.VersionListResponsesV4PagePaginationArray = VersionListResponsesV4PagePaginationArray;
var Config3 = class extends APIResource {
  static {
    __name(this, "Config3");
  }
  update(params, options) {
    const { zone_id, ...body } = params;
    return this._client.put(`/zones/${zone_id}/settings/zaraz/config`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  get(params, options) {
    const { zone_id } = params;
    return this._client.get(`/zones/${zone_id}/settings/zaraz/config`, options)._thenUnwrap((obj) => obj.result);
  }
};
var Default = class extends APIResource {
  static {
    __name(this, "Default");
  }
  get(params, options) {
    const { zone_id } = params;
    return this._client.get(`/zones/${zone_id}/settings/zaraz/default`, options)._thenUnwrap((obj) => obj.result);
  }
};
var Export = class extends APIResource {
  static {
    __name(this, "Export");
  }
  get(params, options) {
    const { zone_id } = params;
    return this._client.get(`/zones/${zone_id}/settings/zaraz/export`, options);
  }
};
var Publish = class extends APIResource {
  static {
    __name(this, "Publish");
  }
  create(params, options) {
    const { zone_id, body } = params ?? {};
    return this._client.post(`/zones/${zone_id}/settings/zaraz/publish`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
};
var WorkflowResource = class extends APIResource {
  static {
    __name(this, "WorkflowResource");
  }
  get(params, options) {
    const { zone_id } = params;
    return this._client.get(`/zones/${zone_id}/settings/zaraz/workflow`, options)._thenUnwrap((obj) => obj.result);
  }
};
var Configs4 = class extends APIResource {
  static {
    __name(this, "Configs4");
  }
  get(params, options) {
    const { zone_id, ...query } = params;
    return this._client.get(`/zones/${zone_id}/settings/zaraz/history/configs`, {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
};
var History3 = class extends APIResource {
  static {
    __name(this, "History3");
  }
  constructor() {
    super(...arguments);
    this.configs = new Configs4(this._client);
  }
  update(params, options) {
    const { zone_id, body } = params;
    return this._client.put(`/zones/${zone_id}/settings/zaraz/history`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { zone_id, ...query } = params;
    return this._client.getAPIList(`/zones/${zone_id}/settings/zaraz/history`, HistoryListResponsesSinglePage, { query, ...options });
  }
};
var HistoryListResponsesSinglePage = class extends SinglePage {
  static {
    __name(this, "HistoryListResponsesSinglePage");
  }
};
History3.HistoryListResponsesSinglePage = HistoryListResponsesSinglePage;
History3.Configs = Configs4;
var Zaraz = class extends APIResource {
  static {
    __name(this, "Zaraz");
  }
  constructor() {
    super(...arguments);
    this.config = new Config3(this._client);
    this.default = new Default(this._client);
    this.export = new Export(this._client);
    this.history = new History3(this._client);
    this.publish = new Publish(this._client);
    this.workflow = new WorkflowResource(this._client);
  }
  update(params, options) {
    const { zone_id, workflow } = params;
    return this._client.put(`/zones/${zone_id}/settings/zaraz/workflow`, {
      body: workflow,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
};
Zaraz.Config = Config3;
Zaraz.Default = Default;
Zaraz.Export = Export;
Zaraz.History = History3;
Zaraz.HistoryListResponsesSinglePage = HistoryListResponsesSinglePage;
Zaraz.Publish = Publish;
Zaraz.WorkflowResource = WorkflowResource;
var ConnectivitySettings = class extends APIResource {
  static {
    __name(this, "ConnectivitySettings");
  }
  edit(params, options) {
    const { account_id, ...body } = params;
    return this._client.patch(`/accounts/${account_id}/zerotrust/connectivity_settings`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  get(params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/zerotrust/connectivity_settings`, options)._thenUnwrap((obj) => obj.result);
  }
};
var Seats = class extends APIResource {
  static {
    __name(this, "Seats");
  }
  edit(params, options) {
    const { account_id, body } = params;
    return this._client.getAPIList(`/accounts/${account_id}/access/seats`, SeatsSinglePage, {
      body,
      method: "patch",
      ...options
    });
  }
};
var SeatsSinglePage = class extends SinglePage {
  static {
    __name(this, "SeatsSinglePage");
  }
};
Seats.SeatsSinglePage = SeatsSinglePage;
var Bookmarks = class extends APIResource {
  static {
    __name(this, "Bookmarks");
  }
  create(bookmarkId, params, options) {
    const { account_id, body } = params;
    return this._client.post(`/accounts/${account_id}/access/bookmarks/${bookmarkId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  update(bookmarkId, params, options) {
    const { account_id, body } = params;
    return this._client.put(`/accounts/${account_id}/access/bookmarks/${bookmarkId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { account_id } = params;
    return this._client.getAPIList(`/accounts/${account_id}/access/bookmarks`, BookmarksSinglePage, options);
  }
  delete(bookmarkId, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/access/bookmarks/${bookmarkId}`, options)._thenUnwrap((obj) => obj.result);
  }
  get(bookmarkId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/access/bookmarks/${bookmarkId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var BookmarksSinglePage = class extends SinglePage {
  static {
    __name(this, "BookmarksSinglePage");
  }
};
Bookmarks.BookmarksSinglePage = BookmarksSinglePage;
var CustomPages2 = class extends APIResource {
  static {
    __name(this, "CustomPages2");
  }
  create(params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/access/custom_pages`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  update(customPageId, params, options) {
    const { account_id, ...body } = params;
    return this._client.put(`/accounts/${account_id}/access/custom_pages/${customPageId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { account_id, ...query } = params;
    return this._client.getAPIList(`/accounts/${account_id}/access/custom_pages`, CustomPageWithoutHTMLsV4PagePaginationArray, { query, ...options });
  }
  delete(customPageId, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/access/custom_pages/${customPageId}`, options)._thenUnwrap((obj) => obj.result);
  }
  get(customPageId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/access/custom_pages/${customPageId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var CustomPageWithoutHTMLsV4PagePaginationArray = class extends V4PagePaginationArray {
  static {
    __name(this, "CustomPageWithoutHTMLsV4PagePaginationArray");
  }
};
CustomPages2.CustomPageWithoutHTMLsV4PagePaginationArray = CustomPageWithoutHTMLsV4PagePaginationArray;
var GatewayCA = class extends APIResource {
  static {
    __name(this, "GatewayCA");
  }
  create(params, options) {
    const { account_id } = params;
    return this._client.post(`/accounts/${account_id}/access/gateway_ca`, options)._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { account_id } = params;
    return this._client.getAPIList(`/accounts/${account_id}/access/gateway_ca`, GatewayCAListResponsesSinglePage, options);
  }
  delete(certificateId, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/access/gateway_ca/${certificateId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var GatewayCAListResponsesSinglePage = class extends SinglePage {
  static {
    __name(this, "GatewayCAListResponsesSinglePage");
  }
};
GatewayCA.GatewayCAListResponsesSinglePage = GatewayCAListResponsesSinglePage;
var Groups2 = class extends APIResource {
  static {
    __name(this, "Groups2");
  }
  create(params, options) {
    const { account_id, zone_id, ...body } = params;
    if (!account_id && !zone_id) {
      throw new CloudflareError("You must provide either account_id or zone_id.");
    }
    if (account_id && zone_id) {
      throw new CloudflareError("You cannot provide both account_id and zone_id.");
    }
    const { accountOrZone, accountOrZoneId } = account_id ? {
      accountOrZone: "accounts",
      accountOrZoneId: account_id
    } : {
      accountOrZone: "zones",
      accountOrZoneId: zone_id
    };
    return this._client.post(`/${accountOrZone}/${accountOrZoneId}/access/groups`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  update(groupId, params, options) {
    const { account_id, zone_id, ...body } = params;
    if (!account_id && !zone_id) {
      throw new CloudflareError("You must provide either account_id or zone_id.");
    }
    if (account_id && zone_id) {
      throw new CloudflareError("You cannot provide both account_id and zone_id.");
    }
    const { accountOrZone, accountOrZoneId } = account_id ? {
      accountOrZone: "accounts",
      accountOrZoneId: account_id
    } : {
      accountOrZone: "zones",
      accountOrZoneId: zone_id
    };
    return this._client.put(`/${accountOrZone}/${accountOrZoneId}/access/groups/${groupId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(params = {}, options) {
    if (isRequestOptions(params)) {
      return this.list({}, params);
    }
    const { account_id, zone_id, ...query } = params;
    if (!account_id && !zone_id) {
      throw new CloudflareError("You must provide either account_id or zone_id.");
    }
    if (account_id && zone_id) {
      throw new CloudflareError("You cannot provide both account_id and zone_id.");
    }
    const { accountOrZone, accountOrZoneId } = account_id ? {
      accountOrZone: "accounts",
      accountOrZoneId: account_id
    } : {
      accountOrZone: "zones",
      accountOrZoneId: zone_id
    };
    return this._client.getAPIList(`/${accountOrZone}/${accountOrZoneId}/access/groups`, GroupListResponsesV4PagePaginationArray, { query, ...options });
  }
  delete(groupId, params = {}, options) {
    if (isRequestOptions(params)) {
      return this.delete(groupId, {}, params);
    }
    const { account_id, zone_id } = params;
    if (!account_id && !zone_id) {
      throw new CloudflareError("You must provide either account_id or zone_id.");
    }
    if (account_id && zone_id) {
      throw new CloudflareError("You cannot provide both account_id and zone_id.");
    }
    const { accountOrZone, accountOrZoneId } = account_id ? {
      accountOrZone: "accounts",
      accountOrZoneId: account_id
    } : {
      accountOrZone: "zones",
      accountOrZoneId: zone_id
    };
    return this._client.delete(`/${accountOrZone}/${accountOrZoneId}/access/groups/${groupId}`, options)._thenUnwrap((obj) => obj.result);
  }
  get(groupId, params = {}, options) {
    if (isRequestOptions(params)) {
      return this.get(groupId, {}, params);
    }
    const { account_id, zone_id } = params;
    if (!account_id && !zone_id) {
      throw new CloudflareError("You must provide either account_id or zone_id.");
    }
    if (account_id && zone_id) {
      throw new CloudflareError("You cannot provide both account_id and zone_id.");
    }
    const { accountOrZone, accountOrZoneId } = account_id ? {
      accountOrZone: "accounts",
      accountOrZoneId: account_id
    } : {
      accountOrZone: "zones",
      accountOrZoneId: zone_id
    };
    return this._client.get(`/${accountOrZone}/${accountOrZoneId}/access/groups/${groupId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var GroupListResponsesV4PagePaginationArray = class extends V4PagePaginationArray {
  static {
    __name(this, "GroupListResponsesV4PagePaginationArray");
  }
};
var ZeroTrustGroupsV4PagePaginationArray = class extends V4PagePaginationArray {
  static {
    __name(this, "ZeroTrustGroupsV4PagePaginationArray");
  }
};
Groups2.GroupListResponsesV4PagePaginationArray = GroupListResponsesV4PagePaginationArray;
var Keys4 = class extends APIResource {
  static {
    __name(this, "Keys4");
  }
  update(params, options) {
    const { account_id, ...body } = params;
    return this._client.put(`/accounts/${account_id}/access/keys`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  get(params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/access/keys`, options)._thenUnwrap((obj) => obj.result);
  }
  rotate(params, options) {
    const { account_id } = params;
    return this._client.post(`/accounts/${account_id}/access/keys/rotate`, options)._thenUnwrap((obj) => obj.result);
  }
};
var Policies3 = class extends APIResource {
  static {
    __name(this, "Policies3");
  }
  create(params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/access/policies`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  update(policyId, params, options) {
    const { account_id, ...body } = params;
    return this._client.put(`/accounts/${account_id}/access/policies/${policyId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { account_id, ...query } = params;
    return this._client.getAPIList(`/accounts/${account_id}/access/policies`, PolicyListResponsesV4PagePaginationArray, { query, ...options });
  }
  delete(policyId, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/access/policies/${policyId}`, options)._thenUnwrap((obj) => obj.result);
  }
  get(policyId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/access/policies/${policyId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var PolicyListResponsesV4PagePaginationArray = class extends V4PagePaginationArray {
  static {
    __name(this, "PolicyListResponsesV4PagePaginationArray");
  }
};
Policies3.PolicyListResponsesV4PagePaginationArray = PolicyListResponsesV4PagePaginationArray;
var ServiceTokens = class extends APIResource {
  static {
    __name(this, "ServiceTokens");
  }
  create(params, options) {
    const { account_id, zone_id, ...body } = params;
    if (!account_id && !zone_id) {
      throw new CloudflareError("You must provide either account_id or zone_id.");
    }
    if (account_id && zone_id) {
      throw new CloudflareError("You cannot provide both account_id and zone_id.");
    }
    const { accountOrZone, accountOrZoneId } = account_id ? {
      accountOrZone: "accounts",
      accountOrZoneId: account_id
    } : {
      accountOrZone: "zones",
      accountOrZoneId: zone_id
    };
    return this._client.post(`/${accountOrZone}/${accountOrZoneId}/access/service_tokens`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  update(serviceTokenId, params, options) {
    const { account_id, zone_id, ...body } = params;
    if (!account_id && !zone_id) {
      throw new CloudflareError("You must provide either account_id or zone_id.");
    }
    if (account_id && zone_id) {
      throw new CloudflareError("You cannot provide both account_id and zone_id.");
    }
    const { accountOrZone, accountOrZoneId } = account_id ? {
      accountOrZone: "accounts",
      accountOrZoneId: account_id
    } : {
      accountOrZone: "zones",
      accountOrZoneId: zone_id
    };
    return this._client.put(`/${accountOrZone}/${accountOrZoneId}/access/service_tokens/${serviceTokenId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(params = {}, options) {
    if (isRequestOptions(params)) {
      return this.list({}, params);
    }
    const { account_id, zone_id, ...query } = params;
    if (!account_id && !zone_id) {
      throw new CloudflareError("You must provide either account_id or zone_id.");
    }
    if (account_id && zone_id) {
      throw new CloudflareError("You cannot provide both account_id and zone_id.");
    }
    const { accountOrZone, accountOrZoneId } = account_id ? {
      accountOrZone: "accounts",
      accountOrZoneId: account_id
    } : {
      accountOrZone: "zones",
      accountOrZoneId: zone_id
    };
    return this._client.getAPIList(`/${accountOrZone}/${accountOrZoneId}/access/service_tokens`, ServiceTokensV4PagePaginationArray, { query, ...options });
  }
  delete(serviceTokenId, params = {}, options) {
    if (isRequestOptions(params)) {
      return this.delete(serviceTokenId, {}, params);
    }
    const { account_id, zone_id } = params;
    if (!account_id && !zone_id) {
      throw new CloudflareError("You must provide either account_id or zone_id.");
    }
    if (account_id && zone_id) {
      throw new CloudflareError("You cannot provide both account_id and zone_id.");
    }
    const { accountOrZone, accountOrZoneId } = account_id ? {
      accountOrZone: "accounts",
      accountOrZoneId: account_id
    } : {
      accountOrZone: "zones",
      accountOrZoneId: zone_id
    };
    return this._client.delete(`/${accountOrZone}/${accountOrZoneId}/access/service_tokens/${serviceTokenId}`, options)._thenUnwrap((obj) => obj.result);
  }
  get(serviceTokenId, params = {}, options) {
    if (isRequestOptions(params)) {
      return this.get(serviceTokenId, {}, params);
    }
    const { account_id, zone_id } = params;
    if (!account_id && !zone_id) {
      throw new CloudflareError("You must provide either account_id or zone_id.");
    }
    if (account_id && zone_id) {
      throw new CloudflareError("You cannot provide both account_id and zone_id.");
    }
    const { accountOrZone, accountOrZoneId } = account_id ? {
      accountOrZone: "accounts",
      accountOrZoneId: account_id
    } : {
      accountOrZone: "zones",
      accountOrZoneId: zone_id
    };
    return this._client.get(`/${accountOrZone}/${accountOrZoneId}/access/service_tokens/${serviceTokenId}`, options)._thenUnwrap((obj) => obj.result);
  }
  refresh(serviceTokenId, params, options) {
    const { account_id } = params;
    return this._client.post(`/accounts/${account_id}/access/service_tokens/${serviceTokenId}/refresh`, options)._thenUnwrap((obj) => obj.result);
  }
  rotate(serviceTokenId, params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/access/service_tokens/${serviceTokenId}/rotate`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
};
var ServiceTokensV4PagePaginationArray = class extends V4PagePaginationArray {
  static {
    __name(this, "ServiceTokensV4PagePaginationArray");
  }
};
ServiceTokens.ServiceTokensV4PagePaginationArray = ServiceTokensV4PagePaginationArray;
var Tags3 = class extends APIResource {
  static {
    __name(this, "Tags3");
  }
  create(params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/access/tags`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  update(tagName, params, options) {
    const { account_id, ...body } = params;
    return this._client.put(`/accounts/${account_id}/access/tags/${tagName}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { account_id, ...query } = params;
    return this._client.getAPIList(`/accounts/${account_id}/access/tags`, TagsV4PagePaginationArray, {
      query,
      ...options
    });
  }
  delete(tagName, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/access/tags/${tagName}`, options)._thenUnwrap((obj) => obj.result);
  }
  get(tagName, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/access/tags/${tagName}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var TagsV4PagePaginationArray = class extends V4PagePaginationArray {
  static {
    __name(this, "TagsV4PagePaginationArray");
  }
};
Tags3.TagsV4PagePaginationArray = TagsV4PagePaginationArray;
var CAs = class extends APIResource {
  static {
    __name(this, "CAs");
  }
  create(appId, params = {}, options) {
    if (isRequestOptions(params)) {
      return this.create(appId, {}, params);
    }
    const { account_id, zone_id } = params;
    if (!account_id && !zone_id) {
      throw new CloudflareError("You must provide either account_id or zone_id.");
    }
    if (account_id && zone_id) {
      throw new CloudflareError("You cannot provide both account_id and zone_id.");
    }
    const { accountOrZone, accountOrZoneId } = account_id ? {
      accountOrZone: "accounts",
      accountOrZoneId: account_id
    } : {
      accountOrZone: "zones",
      accountOrZoneId: zone_id
    };
    return this._client.post(`/${accountOrZone}/${accountOrZoneId}/access/apps/${appId}/ca`, options)._thenUnwrap((obj) => obj.result);
  }
  list(params = {}, options) {
    if (isRequestOptions(params)) {
      return this.list({}, params);
    }
    const { account_id, zone_id, ...query } = params;
    if (!account_id && !zone_id) {
      throw new CloudflareError("You must provide either account_id or zone_id.");
    }
    if (account_id && zone_id) {
      throw new CloudflareError("You cannot provide both account_id and zone_id.");
    }
    const { accountOrZone, accountOrZoneId } = account_id ? {
      accountOrZone: "accounts",
      accountOrZoneId: account_id
    } : {
      accountOrZone: "zones",
      accountOrZoneId: zone_id
    };
    return this._client.getAPIList(`/${accountOrZone}/${accountOrZoneId}/access/apps/ca`, CAsV4PagePaginationArray, { query, ...options });
  }
  delete(appId, params = {}, options) {
    if (isRequestOptions(params)) {
      return this.delete(appId, {}, params);
    }
    const { account_id, zone_id } = params;
    if (!account_id && !zone_id) {
      throw new CloudflareError("You must provide either account_id or zone_id.");
    }
    if (account_id && zone_id) {
      throw new CloudflareError("You cannot provide both account_id and zone_id.");
    }
    const { accountOrZone, accountOrZoneId } = account_id ? {
      accountOrZone: "accounts",
      accountOrZoneId: account_id
    } : {
      accountOrZone: "zones",
      accountOrZoneId: zone_id
    };
    return this._client.delete(`/${accountOrZone}/${accountOrZoneId}/access/apps/${appId}/ca`, options)._thenUnwrap((obj) => obj.result);
  }
  get(appId, params = {}, options) {
    if (isRequestOptions(params)) {
      return this.get(appId, {}, params);
    }
    const { account_id, zone_id } = params;
    if (!account_id && !zone_id) {
      throw new CloudflareError("You must provide either account_id or zone_id.");
    }
    if (account_id && zone_id) {
      throw new CloudflareError("You cannot provide both account_id and zone_id.");
    }
    const { accountOrZone, accountOrZoneId } = account_id ? {
      accountOrZone: "accounts",
      accountOrZoneId: account_id
    } : {
      accountOrZone: "zones",
      accountOrZoneId: zone_id
    };
    return this._client.get(`/${accountOrZone}/${accountOrZoneId}/access/apps/${appId}/ca`, options)._thenUnwrap((obj) => obj.result);
  }
};
var CAsV4PagePaginationArray = class extends V4PagePaginationArray {
  static {
    __name(this, "CAsV4PagePaginationArray");
  }
};
CAs.CAsV4PagePaginationArray = CAsV4PagePaginationArray;
var Policies4 = class extends APIResource {
  static {
    __name(this, "Policies4");
  }
  create(appId, params, options) {
    const { account_id, zone_id, ...body } = params;
    if (!account_id && !zone_id) {
      throw new CloudflareError("You must provide either account_id or zone_id.");
    }
    if (account_id && zone_id) {
      throw new CloudflareError("You cannot provide both account_id and zone_id.");
    }
    const { accountOrZone, accountOrZoneId } = account_id ? {
      accountOrZone: "accounts",
      accountOrZoneId: account_id
    } : {
      accountOrZone: "zones",
      accountOrZoneId: zone_id
    };
    return this._client.post(`/${accountOrZone}/${accountOrZoneId}/access/apps/${appId}/policies`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  update(appId, policyId, params, options) {
    const { account_id, zone_id, ...body } = params;
    if (!account_id && !zone_id) {
      throw new CloudflareError("You must provide either account_id or zone_id.");
    }
    if (account_id && zone_id) {
      throw new CloudflareError("You cannot provide both account_id and zone_id.");
    }
    const { accountOrZone, accountOrZoneId } = account_id ? {
      accountOrZone: "accounts",
      accountOrZoneId: account_id
    } : {
      accountOrZone: "zones",
      accountOrZoneId: zone_id
    };
    return this._client.put(`/${accountOrZone}/${accountOrZoneId}/access/apps/${appId}/policies/${policyId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(appId, params = {}, options) {
    if (isRequestOptions(params)) {
      return this.list(appId, {}, params);
    }
    const { account_id, zone_id, ...query } = params;
    if (!account_id && !zone_id) {
      throw new CloudflareError("You must provide either account_id or zone_id.");
    }
    if (account_id && zone_id) {
      throw new CloudflareError("You cannot provide both account_id and zone_id.");
    }
    const { accountOrZone, accountOrZoneId } = account_id ? {
      accountOrZone: "accounts",
      accountOrZoneId: account_id
    } : {
      accountOrZone: "zones",
      accountOrZoneId: zone_id
    };
    return this._client.getAPIList(`/${accountOrZone}/${accountOrZoneId}/access/apps/${appId}/policies`, PolicyListResponsesV4PagePaginationArray2, { query, ...options });
  }
  delete(appId, policyId, params = {}, options) {
    if (isRequestOptions(params)) {
      return this.delete(appId, policyId, {}, params);
    }
    const { account_id, zone_id } = params;
    if (!account_id && !zone_id) {
      throw new CloudflareError("You must provide either account_id or zone_id.");
    }
    if (account_id && zone_id) {
      throw new CloudflareError("You cannot provide both account_id and zone_id.");
    }
    const { accountOrZone, accountOrZoneId } = account_id ? {
      accountOrZone: "accounts",
      accountOrZoneId: account_id
    } : {
      accountOrZone: "zones",
      accountOrZoneId: zone_id
    };
    return this._client.delete(`/${accountOrZone}/${accountOrZoneId}/access/apps/${appId}/policies/${policyId}`, options)._thenUnwrap((obj) => obj.result);
  }
  get(appId, policyId, params = {}, options) {
    if (isRequestOptions(params)) {
      return this.get(appId, policyId, {}, params);
    }
    const { account_id, zone_id } = params;
    if (!account_id && !zone_id) {
      throw new CloudflareError("You must provide either account_id or zone_id.");
    }
    if (account_id && zone_id) {
      throw new CloudflareError("You cannot provide both account_id and zone_id.");
    }
    const { accountOrZone, accountOrZoneId } = account_id ? {
      accountOrZone: "accounts",
      accountOrZoneId: account_id
    } : {
      accountOrZone: "zones",
      accountOrZoneId: zone_id
    };
    return this._client.get(`/${accountOrZone}/${accountOrZoneId}/access/apps/${appId}/policies/${policyId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var PolicyListResponsesV4PagePaginationArray2 = class extends V4PagePaginationArray {
  static {
    __name(this, "PolicyListResponsesV4PagePaginationArray2");
  }
};
Policies4.PolicyListResponsesV4PagePaginationArray = PolicyListResponsesV4PagePaginationArray2;
var Settings13 = class extends APIResource {
  static {
    __name(this, "Settings13");
  }
  update(appId, params, options) {
    const { account_id, zone_id, ...body } = params;
    if (!account_id && !zone_id) {
      throw new CloudflareError("You must provide either account_id or zone_id.");
    }
    if (account_id && zone_id) {
      throw new CloudflareError("You cannot provide both account_id and zone_id.");
    }
    const { accountOrZone, accountOrZoneId } = account_id ? {
      accountOrZone: "accounts",
      accountOrZoneId: account_id
    } : {
      accountOrZone: "zones",
      accountOrZoneId: zone_id
    };
    return this._client.put(`/${accountOrZone}/${accountOrZoneId}/access/apps/${appId}/settings`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  edit(appId, params, options) {
    const { account_id, zone_id, ...body } = params;
    if (!account_id && !zone_id) {
      throw new CloudflareError("You must provide either account_id or zone_id.");
    }
    if (account_id && zone_id) {
      throw new CloudflareError("You cannot provide both account_id and zone_id.");
    }
    const { accountOrZone, accountOrZoneId } = account_id ? {
      accountOrZone: "accounts",
      accountOrZoneId: account_id
    } : {
      accountOrZone: "zones",
      accountOrZoneId: zone_id
    };
    return this._client.patch(`/${accountOrZone}/${accountOrZoneId}/access/apps/${appId}/settings`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
};
var UserPolicyChecks = class extends APIResource {
  static {
    __name(this, "UserPolicyChecks");
  }
  list(appId, params = {}, options) {
    if (isRequestOptions(params)) {
      return this.list(appId, {}, params);
    }
    const { account_id, zone_id } = params;
    if (!account_id && !zone_id) {
      throw new CloudflareError("You must provide either account_id or zone_id.");
    }
    if (account_id && zone_id) {
      throw new CloudflareError("You cannot provide both account_id and zone_id.");
    }
    const { accountOrZone, accountOrZoneId } = account_id ? {
      accountOrZone: "accounts",
      accountOrZoneId: account_id
    } : {
      accountOrZone: "zones",
      accountOrZoneId: zone_id
    };
    return this._client.get(`/${accountOrZone}/${accountOrZoneId}/access/apps/${appId}/user_policy_checks`, options)._thenUnwrap((obj) => obj.result);
  }
};
var Users = class extends APIResource {
  static {
    __name(this, "Users");
  }
  list(policyTestId, params, options) {
    const { account_id, ...query } = params;
    return this._client.getAPIList(`/accounts/${account_id}/access/policy-tests/${policyTestId}/users`, UserListResponsesV4PagePaginationArray, { query, ...options });
  }
};
var UserListResponsesV4PagePaginationArray = class extends V4PagePaginationArray {
  static {
    __name(this, "UserListResponsesV4PagePaginationArray");
  }
};
Users.UserListResponsesV4PagePaginationArray = UserListResponsesV4PagePaginationArray;
var PolicyTests = class extends APIResource {
  static {
    __name(this, "PolicyTests");
  }
  constructor() {
    super(...arguments);
    this.users = new Users(this._client);
  }
  create(params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/access/policy-tests`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  get(policyTestId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/access/policy-tests/${policyTestId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
PolicyTests.Users = Users;
PolicyTests.UserListResponsesV4PagePaginationArray = UserListResponsesV4PagePaginationArray;
var Applications = class extends APIResource {
  static {
    __name(this, "Applications");
  }
  constructor() {
    super(...arguments);
    this.cas = new CAs(this._client);
    this.userPolicyChecks = new UserPolicyChecks(this._client);
    this.policies = new Policies4(this._client);
    this.policyTests = new PolicyTests(this._client);
    this.settings = new Settings13(this._client);
  }
  create(params, options) {
    const { account_id, zone_id, ...body } = params;
    if (!account_id && !zone_id) {
      throw new CloudflareError("You must provide either account_id or zone_id.");
    }
    if (account_id && zone_id) {
      throw new CloudflareError("You cannot provide both account_id and zone_id.");
    }
    const { accountOrZone, accountOrZoneId } = account_id ? {
      accountOrZone: "accounts",
      accountOrZoneId: account_id
    } : {
      accountOrZone: "zones",
      accountOrZoneId: zone_id
    };
    return this._client.post(`/${accountOrZone}/${accountOrZoneId}/access/apps`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  update(appId, params, options) {
    const { account_id, zone_id, ...body } = params;
    if (!account_id && !zone_id) {
      throw new CloudflareError("You must provide either account_id or zone_id.");
    }
    if (account_id && zone_id) {
      throw new CloudflareError("You cannot provide both account_id and zone_id.");
    }
    const { accountOrZone, accountOrZoneId } = account_id ? {
      accountOrZone: "accounts",
      accountOrZoneId: account_id
    } : {
      accountOrZone: "zones",
      accountOrZoneId: zone_id
    };
    return this._client.put(`/${accountOrZone}/${accountOrZoneId}/access/apps/${appId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(params = {}, options) {
    if (isRequestOptions(params)) {
      return this.list({}, params);
    }
    const { account_id, zone_id, ...query } = params;
    if (!account_id && !zone_id) {
      throw new CloudflareError("You must provide either account_id or zone_id.");
    }
    if (account_id && zone_id) {
      throw new CloudflareError("You cannot provide both account_id and zone_id.");
    }
    const { accountOrZone, accountOrZoneId } = account_id ? {
      accountOrZone: "accounts",
      accountOrZoneId: account_id
    } : {
      accountOrZone: "zones",
      accountOrZoneId: zone_id
    };
    return this._client.getAPIList(`/${accountOrZone}/${accountOrZoneId}/access/apps`, ApplicationListResponsesV4PagePaginationArray, { query, ...options });
  }
  delete(appId, params = {}, options) {
    if (isRequestOptions(params)) {
      return this.delete(appId, {}, params);
    }
    const { account_id, zone_id } = params;
    if (!account_id && !zone_id) {
      throw new CloudflareError("You must provide either account_id or zone_id.");
    }
    if (account_id && zone_id) {
      throw new CloudflareError("You cannot provide both account_id and zone_id.");
    }
    const { accountOrZone, accountOrZoneId } = account_id ? {
      accountOrZone: "accounts",
      accountOrZoneId: account_id
    } : {
      accountOrZone: "zones",
      accountOrZoneId: zone_id
    };
    return this._client.delete(`/${accountOrZone}/${accountOrZoneId}/access/apps/${appId}`, options)._thenUnwrap((obj) => obj.result);
  }
  get(appId, params = {}, options) {
    if (isRequestOptions(params)) {
      return this.get(appId, {}, params);
    }
    const { account_id, zone_id } = params;
    if (!account_id && !zone_id) {
      throw new CloudflareError("You must provide either account_id or zone_id.");
    }
    if (account_id && zone_id) {
      throw new CloudflareError("You cannot provide both account_id and zone_id.");
    }
    const { accountOrZone, accountOrZoneId } = account_id ? {
      accountOrZone: "accounts",
      accountOrZoneId: account_id
    } : {
      accountOrZone: "zones",
      accountOrZoneId: zone_id
    };
    return this._client.get(`/${accountOrZone}/${accountOrZoneId}/access/apps/${appId}`, options)._thenUnwrap((obj) => obj.result);
  }
  revokeTokens(appId, params = {}, options) {
    if (isRequestOptions(params)) {
      return this.revokeTokens(appId, {}, params);
    }
    const { account_id, zone_id } = params;
    if (!account_id && !zone_id) {
      throw new CloudflareError("You must provide either account_id or zone_id.");
    }
    if (account_id && zone_id) {
      throw new CloudflareError("You cannot provide both account_id and zone_id.");
    }
    const { accountOrZone, accountOrZoneId } = account_id ? {
      accountOrZone: "accounts",
      accountOrZoneId: account_id
    } : {
      accountOrZone: "zones",
      accountOrZoneId: zone_id
    };
    return this._client.post(`/${accountOrZone}/${accountOrZoneId}/access/apps/${appId}/revoke_tokens`, options)._thenUnwrap((obj) => obj.result);
  }
};
var ApplicationListResponsesV4PagePaginationArray = class extends V4PagePaginationArray {
  static {
    __name(this, "ApplicationListResponsesV4PagePaginationArray");
  }
};
Applications.ApplicationListResponsesV4PagePaginationArray = ApplicationListResponsesV4PagePaginationArray;
Applications.CAs = CAs;
Applications.CAsV4PagePaginationArray = CAsV4PagePaginationArray;
Applications.UserPolicyChecks = UserPolicyChecks;
Applications.Policies = Policies4;
Applications.PolicyListResponsesV4PagePaginationArray = PolicyListResponsesV4PagePaginationArray2;
Applications.PolicyTests = PolicyTests;
Applications.Settings = Settings13;
var Settings14 = class extends APIResource {
  static {
    __name(this, "Settings14");
  }
  update(params, options) {
    const { account_id, zone_id, ...body } = params;
    if (!account_id && !zone_id) {
      throw new CloudflareError("You must provide either account_id or zone_id.");
    }
    if (account_id && zone_id) {
      throw new CloudflareError("You cannot provide both account_id and zone_id.");
    }
    const { accountOrZone, accountOrZoneId } = account_id ? {
      accountOrZone: "accounts",
      accountOrZoneId: account_id
    } : {
      accountOrZone: "zones",
      accountOrZoneId: zone_id
    };
    return this._client.getAPIList(`/${accountOrZone}/${accountOrZoneId}/access/certificates/settings`, CertificateSettingsSinglePage, { body, method: "put", ...options });
  }
  get(params = {}, options) {
    if (isRequestOptions(params)) {
      return this.get({}, params);
    }
    const { account_id, zone_id } = params;
    if (!account_id && !zone_id) {
      throw new CloudflareError("You must provide either account_id or zone_id.");
    }
    if (account_id && zone_id) {
      throw new CloudflareError("You cannot provide both account_id and zone_id.");
    }
    const { accountOrZone, accountOrZoneId } = account_id ? {
      accountOrZone: "accounts",
      accountOrZoneId: account_id
    } : {
      accountOrZone: "zones",
      accountOrZoneId: zone_id
    };
    return this._client.getAPIList(`/${accountOrZone}/${accountOrZoneId}/access/certificates/settings`, CertificateSettingsSinglePage, options);
  }
};
var CertificateSettingsSinglePage = class extends SinglePage {
  static {
    __name(this, "CertificateSettingsSinglePage");
  }
};
Settings14.CertificateSettingsSinglePage = CertificateSettingsSinglePage;
var Certificates3 = class extends APIResource {
  static {
    __name(this, "Certificates3");
  }
  constructor() {
    super(...arguments);
    this.settings = new Settings14(this._client);
  }
  create(params, options) {
    const { account_id, zone_id, ...body } = params;
    if (!account_id && !zone_id) {
      throw new CloudflareError("You must provide either account_id or zone_id.");
    }
    if (account_id && zone_id) {
      throw new CloudflareError("You cannot provide both account_id and zone_id.");
    }
    const { accountOrZone, accountOrZoneId } = account_id ? {
      accountOrZone: "accounts",
      accountOrZoneId: account_id
    } : {
      accountOrZone: "zones",
      accountOrZoneId: zone_id
    };
    return this._client.post(`/${accountOrZone}/${accountOrZoneId}/access/certificates`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  update(certificateId, params, options) {
    const { account_id, zone_id, ...body } = params;
    if (!account_id && !zone_id) {
      throw new CloudflareError("You must provide either account_id or zone_id.");
    }
    if (account_id && zone_id) {
      throw new CloudflareError("You cannot provide both account_id and zone_id.");
    }
    const { accountOrZone, accountOrZoneId } = account_id ? {
      accountOrZone: "accounts",
      accountOrZoneId: account_id
    } : {
      accountOrZone: "zones",
      accountOrZoneId: zone_id
    };
    return this._client.put(`/${accountOrZone}/${accountOrZoneId}/access/certificates/${certificateId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(params = {}, options) {
    if (isRequestOptions(params)) {
      return this.list({}, params);
    }
    const { account_id, zone_id, ...query } = params;
    if (!account_id && !zone_id) {
      throw new CloudflareError("You must provide either account_id or zone_id.");
    }
    if (account_id && zone_id) {
      throw new CloudflareError("You cannot provide both account_id and zone_id.");
    }
    const { accountOrZone, accountOrZoneId } = account_id ? {
      accountOrZone: "accounts",
      accountOrZoneId: account_id
    } : {
      accountOrZone: "zones",
      accountOrZoneId: zone_id
    };
    return this._client.getAPIList(`/${accountOrZone}/${accountOrZoneId}/access/certificates`, CertificatesV4PagePaginationArray, { query, ...options });
  }
  delete(certificateId, params = {}, options) {
    if (isRequestOptions(params)) {
      return this.delete(certificateId, {}, params);
    }
    const { account_id, zone_id } = params;
    if (!account_id && !zone_id) {
      throw new CloudflareError("You must provide either account_id or zone_id.");
    }
    if (account_id && zone_id) {
      throw new CloudflareError("You cannot provide both account_id and zone_id.");
    }
    const { accountOrZone, accountOrZoneId } = account_id ? {
      accountOrZone: "accounts",
      accountOrZoneId: account_id
    } : {
      accountOrZone: "zones",
      accountOrZoneId: zone_id
    };
    return this._client.delete(`/${accountOrZone}/${accountOrZoneId}/access/certificates/${certificateId}`, options)._thenUnwrap((obj) => obj.result);
  }
  get(certificateId, params = {}, options) {
    if (isRequestOptions(params)) {
      return this.get(certificateId, {}, params);
    }
    const { account_id, zone_id } = params;
    if (!account_id && !zone_id) {
      throw new CloudflareError("You must provide either account_id or zone_id.");
    }
    if (account_id && zone_id) {
      throw new CloudflareError("You cannot provide both account_id and zone_id.");
    }
    const { accountOrZone, accountOrZoneId } = account_id ? {
      accountOrZone: "accounts",
      accountOrZoneId: account_id
    } : {
      accountOrZone: "zones",
      accountOrZoneId: zone_id
    };
    return this._client.get(`/${accountOrZone}/${accountOrZoneId}/access/certificates/${certificateId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var CertificatesV4PagePaginationArray = class extends V4PagePaginationArray {
  static {
    __name(this, "CertificatesV4PagePaginationArray");
  }
};
Certificates3.CertificatesV4PagePaginationArray = CertificatesV4PagePaginationArray;
Certificates3.Settings = Settings14;
Certificates3.CertificateSettingsSinglePage = CertificateSettingsSinglePage;
var Targets = class extends APIResource {
  static {
    __name(this, "Targets");
  }
  create(params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/infrastructure/targets`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  update(targetId, params, options) {
    const { account_id, ...body } = params;
    return this._client.put(`/accounts/${account_id}/infrastructure/targets/${targetId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { account_id, ...query } = params;
    return this._client.getAPIList(`/accounts/${account_id}/infrastructure/targets`, TargetListResponsesV4PagePaginationArray, { query, ...options });
  }
  delete(targetId, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/infrastructure/targets/${targetId}`, {
      ...options,
      headers: { Accept: "*/*", ...options?.headers }
    });
  }
  bulkDelete(params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/infrastructure/targets/batch`, {
      ...options,
      headers: { Accept: "*/*", ...options?.headers }
    });
  }
  bulkDeleteV2(params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/infrastructure/targets/batch_delete`, {
      body,
      ...options,
      headers: { Accept: "*/*", ...options?.headers }
    });
  }
  bulkUpdate(params, options) {
    const { account_id, body } = params;
    return this._client.getAPIList(`/accounts/${account_id}/infrastructure/targets/batch`, TargetBulkUpdateResponsesSinglePage, { body, method: "put", ...options });
  }
  get(targetId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/infrastructure/targets/${targetId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var TargetListResponsesV4PagePaginationArray = class extends V4PagePaginationArray {
  static {
    __name(this, "TargetListResponsesV4PagePaginationArray");
  }
};
var TargetBulkUpdateResponsesSinglePage = class extends SinglePage {
  static {
    __name(this, "TargetBulkUpdateResponsesSinglePage");
  }
};
Targets.TargetListResponsesV4PagePaginationArray = TargetListResponsesV4PagePaginationArray;
Targets.TargetBulkUpdateResponsesSinglePage = TargetBulkUpdateResponsesSinglePage;
var Infrastructure = class extends APIResource {
  static {
    __name(this, "Infrastructure");
  }
  constructor() {
    super(...arguments);
    this.targets = new Targets(this._client);
  }
};
Infrastructure.Targets = Targets;
Infrastructure.TargetListResponsesV4PagePaginationArray = TargetListResponsesV4PagePaginationArray;
Infrastructure.TargetBulkUpdateResponsesSinglePage = TargetBulkUpdateResponsesSinglePage;
var AccessRequests = class extends APIResource {
  static {
    __name(this, "AccessRequests");
  }
  list(params, options) {
    const { account_id, ...query } = params;
    return this._client.get(`/accounts/${account_id}/access/logs/access_requests`, {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
};
var Updates = class extends APIResource {
  static {
    __name(this, "Updates");
  }
  list(params, options) {
    const { account_id, ...query } = params;
    return this._client.getAPIList(`/accounts/${account_id}/access/logs/scim/updates`, UpdateListResponsesV4PagePaginationArray, { query, ...options });
  }
};
var UpdateListResponsesV4PagePaginationArray = class extends V4PagePaginationArray {
  static {
    __name(this, "UpdateListResponsesV4PagePaginationArray");
  }
};
Updates.UpdateListResponsesV4PagePaginationArray = UpdateListResponsesV4PagePaginationArray;
var SCIM = class extends APIResource {
  static {
    __name(this, "SCIM");
  }
  constructor() {
    super(...arguments);
    this.updates = new Updates(this._client);
  }
};
SCIM.Updates = Updates;
SCIM.UpdateListResponsesV4PagePaginationArray = UpdateListResponsesV4PagePaginationArray;
var Logs7 = class extends APIResource {
  static {
    __name(this, "Logs7");
  }
  constructor() {
    super(...arguments);
    this.accessRequests = new AccessRequests(this._client);
    this.scim = new SCIM(this._client);
  }
};
Logs7.AccessRequests = AccessRequests;
Logs7.SCIM = SCIM;
var ActiveSessions = class extends APIResource {
  static {
    __name(this, "ActiveSessions");
  }
  list(userId, params, options) {
    const { account_id } = params;
    return this._client.getAPIList(`/accounts/${account_id}/access/users/${userId}/active_sessions`, ActiveSessionListResponsesSinglePage, options);
  }
  get(userId, nonce, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/access/users/${userId}/active_sessions/${nonce}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var ActiveSessionListResponsesSinglePage = class extends SinglePage {
  static {
    __name(this, "ActiveSessionListResponsesSinglePage");
  }
};
ActiveSessions.ActiveSessionListResponsesSinglePage = ActiveSessionListResponsesSinglePage;
var FailedLogins = class extends APIResource {
  static {
    __name(this, "FailedLogins");
  }
  list(userId, params, options) {
    const { account_id } = params;
    return this._client.getAPIList(`/accounts/${account_id}/access/users/${userId}/failed_logins`, FailedLoginListResponsesSinglePage, options);
  }
};
var FailedLoginListResponsesSinglePage = class extends SinglePage {
  static {
    __name(this, "FailedLoginListResponsesSinglePage");
  }
};
FailedLogins.FailedLoginListResponsesSinglePage = FailedLoginListResponsesSinglePage;
var LastSeenIdentity = class extends APIResource {
  static {
    __name(this, "LastSeenIdentity");
  }
  get(userId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/access/users/${userId}/last_seen_identity`, options)._thenUnwrap((obj) => obj.result);
  }
};
var Users2 = class extends APIResource {
  static {
    __name(this, "Users2");
  }
  constructor() {
    super(...arguments);
    this.activeSessions = new ActiveSessions(this._client);
    this.lastSeenIdentity = new LastSeenIdentity(this._client);
    this.failedLogins = new FailedLogins(this._client);
  }
  list(params, options) {
    const { account_id, ...query } = params;
    return this._client.getAPIList(`/accounts/${account_id}/access/users`, UserListResponsesV4PagePaginationArray2, { query, ...options });
  }
};
var UserListResponsesV4PagePaginationArray2 = class extends V4PagePaginationArray {
  static {
    __name(this, "UserListResponsesV4PagePaginationArray2");
  }
};
var AccessUsersV4PagePaginationArray = class extends V4PagePaginationArray {
  static {
    __name(this, "AccessUsersV4PagePaginationArray");
  }
};
Users2.UserListResponsesV4PagePaginationArray = UserListResponsesV4PagePaginationArray2;
Users2.ActiveSessions = ActiveSessions;
Users2.ActiveSessionListResponsesSinglePage = ActiveSessionListResponsesSinglePage;
Users2.LastSeenIdentity = LastSeenIdentity;
Users2.FailedLogins = FailedLogins;
Users2.FailedLoginListResponsesSinglePage = FailedLoginListResponsesSinglePage;
var Access = class extends APIResource {
  static {
    __name(this, "Access");
  }
  constructor() {
    super(...arguments);
    this.gatewayCA = new GatewayCA(this._client);
    this.infrastructure = new Infrastructure(this._client);
    this.applications = new Applications(this._client);
    this.certificates = new Certificates3(this._client);
    this.groups = new Groups2(this._client);
    this.serviceTokens = new ServiceTokens(this._client);
    this.bookmarks = new Bookmarks(this._client);
    this.keys = new Keys4(this._client);
    this.logs = new Logs7(this._client);
    this.users = new Users2(this._client);
    this.customPages = new CustomPages2(this._client);
    this.tags = new Tags3(this._client);
    this.policies = new Policies3(this._client);
  }
};
Access.GatewayCA = GatewayCA;
Access.GatewayCAListResponsesSinglePage = GatewayCAListResponsesSinglePage;
Access.Infrastructure = Infrastructure;
Access.Applications = Applications;
Access.ApplicationListResponsesV4PagePaginationArray = ApplicationListResponsesV4PagePaginationArray;
Access.Certificates = Certificates3;
Access.CertificatesV4PagePaginationArray = CertificatesV4PagePaginationArray;
Access.Groups = Groups2;
Access.GroupListResponsesV4PagePaginationArray = GroupListResponsesV4PagePaginationArray;
Access.ServiceTokens = ServiceTokens;
Access.ServiceTokensV4PagePaginationArray = ServiceTokensV4PagePaginationArray;
Access.Bookmarks = Bookmarks;
Access.BookmarksSinglePage = BookmarksSinglePage;
Access.Keys = Keys4;
Access.Logs = Logs7;
Access.Users = Users2;
Access.UserListResponsesV4PagePaginationArray = UserListResponsesV4PagePaginationArray2;
Access.CustomPages = CustomPages2;
Access.CustomPageWithoutHTMLsV4PagePaginationArray = CustomPageWithoutHTMLsV4PagePaginationArray;
Access.Tags = Tags3;
Access.TagsV4PagePaginationArray = TagsV4PagePaginationArray;
Access.Policies = Policies3;
Access.PolicyListResponsesV4PagePaginationArray = PolicyListResponsesV4PagePaginationArray;
var Devices = class extends APIResource {
  static {
    __name(this, "Devices");
  }
  list(params, options) {
    const { account_id, ...query } = params;
    return this._client.getAPIList(`/accounts/${account_id}/devices/physical-devices`, DeviceListResponsesCursorPagination, { query, ...options });
  }
  delete(deviceId, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/devices/physical-devices/${deviceId}`, options)._thenUnwrap((obj) => obj.result);
  }
  get(deviceId, params, options) {
    const { account_id, ...query } = params;
    return this._client.get(`/accounts/${account_id}/devices/physical-devices/${deviceId}`, {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  revoke(deviceId, params, options) {
    const { account_id } = params;
    return this._client.post(`/accounts/${account_id}/devices/physical-devices/${deviceId}/revoke`, options)._thenUnwrap((obj) => obj.result);
  }
};
var DeviceListResponsesCursorPagination = class extends CursorPagination {
  static {
    __name(this, "DeviceListResponsesCursorPagination");
  }
};
Devices.DeviceListResponsesCursorPagination = DeviceListResponsesCursorPagination;
var DEXTests = class extends APIResource {
  static {
    __name(this, "DEXTests");
  }
  create(params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/dex/devices/dex_tests`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  update(dexTestId, params, options) {
    const { account_id, ...body } = params;
    return this._client.put(`/accounts/${account_id}/dex/devices/dex_tests/${dexTestId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { account_id } = params;
    return this._client.getAPIList(`/accounts/${account_id}/dex/devices/dex_tests`, DEXTestListResponsesSinglePage, options);
  }
  delete(dexTestId, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/dex/devices/dex_tests/${dexTestId}`, options)._thenUnwrap((obj) => obj.result);
  }
  get(dexTestId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/dex/devices/dex_tests/${dexTestId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var DEXTestListResponsesSinglePage = class extends SinglePage {
  static {
    __name(this, "DEXTestListResponsesSinglePage");
  }
};
DEXTests.DEXTestListResponsesSinglePage = DEXTestListResponsesSinglePage;
var FleetStatus = class extends APIResource {
  static {
    __name(this, "FleetStatus");
  }
  get(deviceId, params, options) {
    const { account_id, ...query } = params;
    return this._client.get(`/accounts/${account_id}/dex/devices/${deviceId}/fleet-status/live`, {
      query,
      ...options
    });
  }
};
var Networks = class extends APIResource {
  static {
    __name(this, "Networks");
  }
  create(params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/devices/networks`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  update(networkId, params, options) {
    const { account_id, ...body } = params;
    return this._client.put(`/accounts/${account_id}/devices/networks/${networkId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { account_id } = params;
    return this._client.getAPIList(`/accounts/${account_id}/devices/networks`, DeviceNetworksSinglePage, options);
  }
  delete(networkId, params, options) {
    const { account_id } = params;
    return this._client.getAPIList(`/accounts/${account_id}/devices/networks/${networkId}`, DeviceNetworksSinglePage, { method: "delete", ...options });
  }
  get(networkId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/devices/networks/${networkId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var DeviceNetworksSinglePage = class extends SinglePage {
  static {
    __name(this, "DeviceNetworksSinglePage");
  }
};
Networks.DeviceNetworksSinglePage = DeviceNetworksSinglePage;
var OverrideCodes = class extends APIResource {
  static {
    __name(this, "OverrideCodes");
  }
  list(deviceId, params, options) {
    const { account_id } = params;
    return this._client.getAPIList(`/accounts/${account_id}/devices/${deviceId}/override_codes`, OverrideCodeListResponsesSinglePage, options);
  }
  get(registrationId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/devices/registrations/${registrationId}/override_codes`, options)._thenUnwrap((obj) => obj.result);
  }
};
var OverrideCodeListResponsesSinglePage = class extends SinglePage {
  static {
    __name(this, "OverrideCodeListResponsesSinglePage");
  }
};
OverrideCodes.OverrideCodeListResponsesSinglePage = OverrideCodeListResponsesSinglePage;
var Registrations = class extends APIResource {
  static {
    __name(this, "Registrations");
  }
  list(params, options) {
    const { account_id, ...query } = params;
    return this._client.getAPIList(`/accounts/${account_id}/devices/registrations`, RegistrationListResponsesCursorPagination, { query, ...options });
  }
  delete(registrationId, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/devices/registrations/${registrationId}`, options)._thenUnwrap((obj) => obj.result);
  }
  bulkDelete(params, options) {
    const { account_id, id } = params;
    return this._client.delete(`/accounts/${account_id}/devices/registrations`, {
      query: { id },
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  get(registrationId, params, options) {
    const { account_id, ...query } = params;
    return this._client.get(`/accounts/${account_id}/devices/registrations/${registrationId}`, {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  revoke(params, options) {
    const { account_id, id } = params;
    return this._client.post(`/accounts/${account_id}/devices/registrations/revoke`, {
      query: { id },
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  unrevoke(params, options) {
    const { account_id, id } = params;
    return this._client.post(`/accounts/${account_id}/devices/registrations/unrevoke`, {
      query: { id },
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
};
var RegistrationListResponsesCursorPagination = class extends CursorPagination {
  static {
    __name(this, "RegistrationListResponsesCursorPagination");
  }
};
Registrations.RegistrationListResponsesCursorPagination = RegistrationListResponsesCursorPagination;
var Revoke = class extends APIResource {
  static {
    __name(this, "Revoke");
  }
  create(params, options) {
    const { account_id, body } = params;
    return this._client.post(`/accounts/${account_id}/devices/revoke`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
};
var Settings15 = class extends APIResource {
  static {
    __name(this, "Settings15");
  }
  update(params, options) {
    const { account_id, ...body } = params;
    return this._client.put(`/accounts/${account_id}/devices/settings`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  delete(params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/devices/settings`, options)._thenUnwrap((obj) => obj.result);
  }
  edit(params, options) {
    const { account_id, ...body } = params;
    return this._client.patch(`/accounts/${account_id}/devices/settings`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  get(params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/devices/settings`, options)._thenUnwrap((obj) => obj.result);
  }
};
var Unrevoke = class extends APIResource {
  static {
    __name(this, "Unrevoke");
  }
  create(params, options) {
    const { account_id, body } = params;
    return this._client.post(`/accounts/${account_id}/devices/unrevoke`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
};
var Excludes = class extends APIResource {
  static {
    __name(this, "Excludes");
  }
  update(policyId, params, options) {
    const { account_id, body } = params;
    return this._client.getAPIList(`/accounts/${account_id}/devices/policy/${policyId}/exclude`, SplitTunnelExcludesSinglePage, { body, method: "put", ...options });
  }
  get(policyId, params, options) {
    const { account_id } = params;
    return this._client.getAPIList(`/accounts/${account_id}/devices/policy/${policyId}/exclude`, SplitTunnelExcludesSinglePage, options);
  }
};
var FallbackDomains = class extends APIResource {
  static {
    __name(this, "FallbackDomains");
  }
  update(policyId, params, options) {
    const { account_id, domains } = params;
    return this._client.getAPIList(`/accounts/${account_id}/devices/policy/${policyId}/fallback_domains`, FallbackDomainsSinglePage, { body: domains, method: "put", ...options });
  }
  get(policyId, params, options) {
    const { account_id } = params;
    return this._client.getAPIList(`/accounts/${account_id}/devices/policy/${policyId}/fallback_domains`, FallbackDomainsSinglePage, options);
  }
};
var Includes = class extends APIResource {
  static {
    __name(this, "Includes");
  }
  update(policyId, params, options) {
    const { account_id, body } = params;
    return this._client.getAPIList(`/accounts/${account_id}/devices/policy/${policyId}/include`, SplitTunnelIncludesSinglePage, { body, method: "put", ...options });
  }
  get(policyId, params, options) {
    const { account_id } = params;
    return this._client.getAPIList(`/accounts/${account_id}/devices/policy/${policyId}/include`, SplitTunnelIncludesSinglePage, options);
  }
};
var Custom2 = class extends APIResource {
  static {
    __name(this, "Custom2");
  }
  constructor() {
    super(...arguments);
    this.excludes = new Excludes(this._client);
    this.includes = new Includes(this._client);
    this.fallbackDomains = new FallbackDomains(this._client);
  }
  create(params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/devices/policy`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { account_id } = params;
    return this._client.getAPIList(`/accounts/${account_id}/devices/policies`, SettingsPoliciesSinglePage, options);
  }
  delete(policyId, params, options) {
    const { account_id } = params;
    return this._client.getAPIList(`/accounts/${account_id}/devices/policy/${policyId}`, SettingsPoliciesSinglePage, { method: "delete", ...options });
  }
  edit(policyId, params, options) {
    const { account_id, ...body } = params;
    return this._client.patch(`/accounts/${account_id}/devices/policy/${policyId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  get(policyId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/devices/policy/${policyId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
Custom2.Excludes = Excludes;
Custom2.Includes = Includes;
Custom2.FallbackDomains = FallbackDomains;
var Certificates4 = class extends APIResource {
  static {
    __name(this, "Certificates4");
  }
  edit(params, options) {
    const { zone_id, ...body } = params;
    return this._client.patch(`/zones/${zone_id}/devices/policy/certificates`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  get(params, options) {
    const { zone_id } = params;
    return this._client.get(`/zones/${zone_id}/devices/policy/certificates`, options)._thenUnwrap((obj) => obj.result);
  }
};
var Excludes2 = class extends APIResource {
  static {
    __name(this, "Excludes2");
  }
  update(params, options) {
    const { account_id, body } = params;
    return this._client.getAPIList(`/accounts/${account_id}/devices/policy/exclude`, SplitTunnelExcludesSinglePage, { body, method: "put", ...options });
  }
  get(params, options) {
    const { account_id } = params;
    return this._client.getAPIList(`/accounts/${account_id}/devices/policy/exclude`, SplitTunnelExcludesSinglePage, options);
  }
};
var FallbackDomains2 = class extends APIResource {
  static {
    __name(this, "FallbackDomains2");
  }
  update(params, options) {
    const { account_id, domains } = params;
    return this._client.getAPIList(`/accounts/${account_id}/devices/policy/fallback_domains`, FallbackDomainsSinglePage, { body: domains, method: "put", ...options });
  }
  get(params, options) {
    const { account_id } = params;
    return this._client.getAPIList(`/accounts/${account_id}/devices/policy/fallback_domains`, FallbackDomainsSinglePage, options);
  }
};
var Includes2 = class extends APIResource {
  static {
    __name(this, "Includes2");
  }
  update(params, options) {
    const { account_id, body } = params;
    return this._client.getAPIList(`/accounts/${account_id}/devices/policy/include`, SplitTunnelIncludesSinglePage, { body, method: "put", ...options });
  }
  get(params, options) {
    const { account_id } = params;
    return this._client.getAPIList(`/accounts/${account_id}/devices/policy/include`, SplitTunnelIncludesSinglePage, options);
  }
};
var Default2 = class extends APIResource {
  static {
    __name(this, "Default2");
  }
  constructor() {
    super(...arguments);
    this.excludes = new Excludes2(this._client);
    this.includes = new Includes2(this._client);
    this.fallbackDomains = new FallbackDomains2(this._client);
    this.certificates = new Certificates4(this._client);
  }
  edit(params, options) {
    const { account_id, ...body } = params;
    return this._client.patch(`/accounts/${account_id}/devices/policy`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  get(params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/devices/policy`, options)._thenUnwrap((obj) => obj.result);
  }
};
Default2.Excludes = Excludes2;
Default2.Includes = Includes2;
Default2.FallbackDomains = FallbackDomains2;
Default2.Certificates = Certificates4;
var Policies5 = class extends APIResource {
  static {
    __name(this, "Policies5");
  }
  constructor() {
    super(...arguments);
    this.default = new Default2(this._client);
    this.custom = new Custom2(this._client);
  }
};
var SplitTunnelExcludesSinglePage = class extends SinglePage {
  static {
    __name(this, "SplitTunnelExcludesSinglePage");
  }
};
var SplitTunnelIncludesSinglePage = class extends SinglePage {
  static {
    __name(this, "SplitTunnelIncludesSinglePage");
  }
};
var FallbackDomainsSinglePage = class extends SinglePage {
  static {
    __name(this, "FallbackDomainsSinglePage");
  }
};
var SettingsPoliciesSinglePage = class extends SinglePage {
  static {
    __name(this, "SettingsPoliciesSinglePage");
  }
};
Policies5.Default = Default2;
Policies5.Custom = Custom2;
var Integrations = class extends APIResource {
  static {
    __name(this, "Integrations");
  }
  create(params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/devices/posture/integration`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { account_id } = params;
    return this._client.getAPIList(`/accounts/${account_id}/devices/posture/integration`, IntegrationsSinglePage, options);
  }
  delete(integrationId, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/devices/posture/integration/${integrationId}`, options)._thenUnwrap((obj) => obj.result);
  }
  edit(integrationId, params, options) {
    const { account_id, ...body } = params;
    return this._client.patch(`/accounts/${account_id}/devices/posture/integration/${integrationId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  get(integrationId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/devices/posture/integration/${integrationId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var IntegrationsSinglePage = class extends SinglePage {
  static {
    __name(this, "IntegrationsSinglePage");
  }
};
Integrations.IntegrationsSinglePage = IntegrationsSinglePage;
var Posture = class extends APIResource {
  static {
    __name(this, "Posture");
  }
  constructor() {
    super(...arguments);
    this.integrations = new Integrations(this._client);
  }
  create(params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/devices/posture`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  update(ruleId, params, options) {
    const { account_id, ...body } = params;
    return this._client.put(`/accounts/${account_id}/devices/posture/${ruleId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { account_id } = params;
    return this._client.getAPIList(`/accounts/${account_id}/devices/posture`, DevicePostureRulesSinglePage, options);
  }
  delete(ruleId, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/devices/posture/${ruleId}`, options)._thenUnwrap((obj) => obj.result);
  }
  get(ruleId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/devices/posture/${ruleId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var DevicePostureRulesSinglePage = class extends SinglePage {
  static {
    __name(this, "DevicePostureRulesSinglePage");
  }
};
Posture.DevicePostureRulesSinglePage = DevicePostureRulesSinglePage;
Posture.Integrations = Integrations;
Posture.IntegrationsSinglePage = IntegrationsSinglePage;
var GlobalWARPOverride = class extends APIResource {
  static {
    __name(this, "GlobalWARPOverride");
  }
  create(params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/devices/resilience/disconnect`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  get(params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/devices/resilience/disconnect`, options)._thenUnwrap((obj) => obj.result);
  }
};
var Resilience = class extends APIResource {
  static {
    __name(this, "Resilience");
  }
  constructor() {
    super(...arguments);
    this.globalWARPOverride = new GlobalWARPOverride(this._client);
  }
};
Resilience.GlobalWARPOverride = GlobalWARPOverride;
var Devices2 = class extends APIResource {
  static {
    __name(this, "Devices2");
  }
  constructor() {
    super(...arguments);
    this.devices = new Devices(this._client);
    this.resilience = new Resilience(this._client);
    this.registrations = new Registrations(this._client);
    this.dexTests = new DEXTests(this._client);
    this.networks = new Networks(this._client);
    this.fleetStatus = new FleetStatus(this._client);
    this.policies = new Policies5(this._client);
    this.posture = new Posture(this._client);
    this.revoke = new Revoke(this._client);
    this.settings = new Settings15(this._client);
    this.unrevoke = new Unrevoke(this._client);
    this.overrideCodes = new OverrideCodes(this._client);
  }
  list(params, options) {
    const { account_id } = params;
    return this._client.getAPIList(`/accounts/${account_id}/devices`, DevicesSinglePage, options);
  }
  get(deviceId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/devices/${deviceId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var DevicesSinglePage = class extends SinglePage {
  static {
    __name(this, "DevicesSinglePage");
  }
};
Devices2.DevicesSinglePage = DevicesSinglePage;
Devices2.Devices = Devices;
Devices2.DeviceListResponsesCursorPagination = DeviceListResponsesCursorPagination;
Devices2.Resilience = Resilience;
Devices2.Registrations = Registrations;
Devices2.RegistrationListResponsesCursorPagination = RegistrationListResponsesCursorPagination;
Devices2.DEXTests = DEXTests;
Devices2.DEXTestListResponsesSinglePage = DEXTestListResponsesSinglePage;
Devices2.Networks = Networks;
Devices2.DeviceNetworksSinglePage = DeviceNetworksSinglePage;
Devices2.FleetStatus = FleetStatus;
Devices2.Policies = Policies5;
Devices2.Posture = Posture;
Devices2.DevicePostureRulesSinglePage = DevicePostureRulesSinglePage;
Devices2.Revoke = Revoke;
Devices2.Settings = Settings15;
Devices2.Unrevoke = Unrevoke;
Devices2.OverrideCodes = OverrideCodes;
Devices2.OverrideCodeListResponsesSinglePage = OverrideCodeListResponsesSinglePage;
var Colos = class extends APIResource {
  static {
    __name(this, "Colos");
  }
  list(params, options) {
    const { account_id, ...query } = params;
    return this._client.getAPIList(`/accounts/${account_id}/dex/colos`, ColoListResponsesSinglePage, {
      query,
      ...options
    });
  }
};
var ColoListResponsesSinglePage = class extends SinglePage {
  static {
    __name(this, "ColoListResponsesSinglePage");
  }
};
Colos.ColoListResponsesSinglePage = ColoListResponsesSinglePage;
var TracerouteTests = class extends APIResource {
  static {
    __name(this, "TracerouteTests");
  }
  get(testId, params, options) {
    const { account_id, ...query } = params;
    return this._client.get(`/accounts/${account_id}/dex/traceroute-tests/${testId}`, {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  networkPath(testId, params, options) {
    const { account_id, ...query } = params;
    return this._client.get(`/accounts/${account_id}/dex/traceroute-tests/${testId}/network-path`, {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  percentiles(testId, params, options) {
    const { account_id, ...query } = params;
    return this._client.get(`/accounts/${account_id}/dex/traceroute-tests/${testId}/percentiles`, {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
};
var WARPChangeEvents = class extends APIResource {
  static {
    __name(this, "WARPChangeEvents");
  }
  get(params, options) {
    const { account_id, ...query } = params;
    return this._client.get(`/accounts/${account_id}/dex/warp-change-events`, {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
};
var Devices3 = class extends APIResource {
  static {
    __name(this, "Devices3");
  }
  list(params, options) {
    const { account_id, ...query } = params;
    return this._client.getAPIList(`/accounts/${account_id}/dex/commands/devices`, DeviceListResponsesV4PagePagination, { query, ...options });
  }
};
var DeviceListResponsesV4PagePagination = class extends V4PagePagination {
  static {
    __name(this, "DeviceListResponsesV4PagePagination");
  }
};
Devices3.DeviceListResponsesV4PagePagination = DeviceListResponsesV4PagePagination;
var Downloads3 = class extends APIResource {
  static {
    __name(this, "Downloads3");
  }
  get(commandId, filename, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/dex/commands/${commandId}/downloads/${filename}`, {
      ...options,
      headers: { Accept: "application/zip", ...options?.headers },
      __binaryResponse: true
    });
  }
};
var Quota3 = class extends APIResource {
  static {
    __name(this, "Quota3");
  }
  get(params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/dex/commands/quota`, options)._thenUnwrap((obj) => obj.result);
  }
};
var Commands = class extends APIResource {
  static {
    __name(this, "Commands");
  }
  constructor() {
    super(...arguments);
    this.devices = new Devices3(this._client);
    this.downloads = new Downloads3(this._client);
    this.quota = new Quota3(this._client);
  }
  create(params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/dex/commands`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { account_id, ...query } = params;
    return this._client.getAPIList(`/accounts/${account_id}/dex/commands`, CommandListResponsesV4PagePagination, { query, ...options });
  }
};
var CommandListResponsesV4PagePagination = class extends V4PagePagination {
  static {
    __name(this, "CommandListResponsesV4PagePagination");
  }
};
Commands.CommandListResponsesV4PagePagination = CommandListResponsesV4PagePagination;
Commands.Devices = Devices3;
Commands.DeviceListResponsesV4PagePagination = DeviceListResponsesV4PagePagination;
Commands.Downloads = Downloads3;
Commands.Quota = Quota3;
var Devices4 = class extends APIResource {
  static {
    __name(this, "Devices4");
  }
  list(params, options) {
    const { account_id, ...query } = params;
    return this._client.getAPIList(`/accounts/${account_id}/dex/fleet-status/devices`, DeviceListResponsesV4PagePaginationArray, { query, ...options });
  }
};
var DeviceListResponsesV4PagePaginationArray = class extends V4PagePaginationArray {
  static {
    __name(this, "DeviceListResponsesV4PagePaginationArray");
  }
};
Devices4.DeviceListResponsesV4PagePaginationArray = DeviceListResponsesV4PagePaginationArray;
var FleetStatus2 = class extends APIResource {
  static {
    __name(this, "FleetStatus2");
  }
  constructor() {
    super(...arguments);
    this.devices = new Devices4(this._client);
  }
  live(params, options) {
    const { account_id, ...query } = params;
    return this._client.get(`/accounts/${account_id}/dex/fleet-status/live`, {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  overTime(params, options) {
    const { account_id, ...query } = params;
    return this._client.get(`/accounts/${account_id}/dex/fleet-status/over-time`, {
      query,
      ...options,
      headers: { Accept: "*/*", ...options?.headers }
    });
  }
};
FleetStatus2.Devices = Devices4;
FleetStatus2.DeviceListResponsesV4PagePaginationArray = DeviceListResponsesV4PagePaginationArray;
var Percentiles = class extends APIResource {
  static {
    __name(this, "Percentiles");
  }
  get(testId, params, options) {
    const { account_id, ...query } = params;
    return this._client.get(`/accounts/${account_id}/dex/http-tests/${testId}/percentiles`, {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
};
var HTTPTests = class extends APIResource {
  static {
    __name(this, "HTTPTests");
  }
  constructor() {
    super(...arguments);
    this.percentiles = new Percentiles(this._client);
  }
  get(testId, params, options) {
    const { account_id, ...query } = params;
    return this._client.get(`/accounts/${account_id}/dex/http-tests/${testId}`, {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
};
HTTPTests.Percentiles = Percentiles;
var UniqueDevices = class extends APIResource {
  static {
    __name(this, "UniqueDevices");
  }
  list(params, options) {
    const { account_id, ...query } = params;
    return this._client.get(`/accounts/${account_id}/dex/tests/unique-devices`, {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
};
var Tests2 = class extends APIResource {
  static {
    __name(this, "Tests2");
  }
  constructor() {
    super(...arguments);
    this.uniqueDevices = new UniqueDevices(this._client);
  }
  list(params, options) {
    const { account_id, ...query } = params;
    return this._client.getAPIList(`/accounts/${account_id}/dex/tests/overview`, TestsV4PagePagination, {
      query,
      ...options
    });
  }
};
var TestsV4PagePagination = class extends V4PagePagination {
  static {
    __name(this, "TestsV4PagePagination");
  }
};
Tests2.TestsV4PagePagination = TestsV4PagePagination;
var NetworkPath = class extends APIResource {
  static {
    __name(this, "NetworkPath");
  }
  get(testResultId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/dex/traceroute-test-results/${testResultId}/network-path`, options)._thenUnwrap((obj) => obj.result);
  }
};
var TracerouteTestResults = class extends APIResource {
  static {
    __name(this, "TracerouteTestResults");
  }
  constructor() {
    super(...arguments);
    this.networkPath = new NetworkPath(this._client);
  }
};
TracerouteTestResults.NetworkPath = NetworkPath;
var DEX = class extends APIResource {
  static {
    __name(this, "DEX");
  }
  constructor() {
    super(...arguments);
    this.warpChangeEvents = new WARPChangeEvents(this._client);
    this.commands = new Commands(this._client);
    this.colos = new Colos(this._client);
    this.fleetStatus = new FleetStatus2(this._client);
    this.httpTests = new HTTPTests(this._client);
    this.tests = new Tests2(this._client);
    this.tracerouteTestResults = new TracerouteTestResults(this._client);
    this.tracerouteTests = new TracerouteTests(this._client);
  }
};
DEX.WARPChangeEvents = WARPChangeEvents;
DEX.Commands = Commands;
DEX.CommandListResponsesV4PagePagination = CommandListResponsesV4PagePagination;
DEX.Colos = Colos;
DEX.ColoListResponsesSinglePage = ColoListResponsesSinglePage;
DEX.FleetStatus = FleetStatus2;
DEX.HTTPTests = HTTPTests;
DEX.TestsV4PagePagination = TestsV4PagePagination;
DEX.TracerouteTestResults = TracerouteTestResults;
DEX.TracerouteTests = TracerouteTests;
var Limits = class extends APIResource {
  static {
    __name(this, "Limits");
  }
  list(params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/dlp/limits`, options)._thenUnwrap((obj) => obj.result);
  }
};
var Patterns = class extends APIResource {
  static {
    __name(this, "Patterns");
  }
  validate(params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/dlp/patterns/validate`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
};
var PayloadLogs = class extends APIResource {
  static {
    __name(this, "PayloadLogs");
  }
  update(params, options) {
    const { account_id, ...body } = params;
    return this._client.put(`/accounts/${account_id}/dlp/payload_log`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  get(params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/dlp/payload_log`, options)._thenUnwrap((obj) => obj.result);
  }
};
var Upload3 = class extends APIResource {
  static {
    __name(this, "Upload3");
  }
  create(datasetId, params, options) {
    const { account_id } = params;
    return this._client.post(`/accounts/${account_id}/dlp/datasets/${datasetId}/upload`, options)._thenUnwrap((obj) => obj.result);
  }
  edit(datasetId, version4, dataset, params, options) {
    const { account_id } = params;
    return this._client.post(`/accounts/${account_id}/dlp/datasets/${datasetId}/upload/${version4}`, {
      body: dataset,
      ...options,
      headers: { "Content-Type": "application/octet-stream", ...options?.headers },
      __binaryRequest: true
    })._thenUnwrap((obj) => obj.result);
  }
};
var Entries2 = class extends APIResource {
  static {
    __name(this, "Entries2");
  }
  create(datasetId, version4, entryId, datasetVersionEntry, params, options) {
    const { account_id } = params;
    return this._client.post(`/accounts/${account_id}/dlp/datasets/${datasetId}/versions/${version4}/entries/${entryId}`, {
      body: datasetVersionEntry,
      ...options,
      headers: { "Content-Type": "application/octet-stream", ...options?.headers },
      __binaryRequest: true
    })._thenUnwrap((obj) => obj.result);
  }
};
var Versions6 = class extends APIResource {
  static {
    __name(this, "Versions6");
  }
  constructor() {
    super(...arguments);
    this.entries = new Entries2(this._client);
  }
  create(datasetId, version4, params, options) {
    const { account_id, body } = params;
    return this._client.getAPIList(`/accounts/${account_id}/dlp/datasets/${datasetId}/versions/${version4}`, VersionCreateResponsesSinglePage, { body, method: "post", ...options });
  }
};
var VersionCreateResponsesSinglePage = class extends SinglePage {
  static {
    __name(this, "VersionCreateResponsesSinglePage");
  }
};
Versions6.VersionCreateResponsesSinglePage = VersionCreateResponsesSinglePage;
Versions6.Entries = Entries2;
var Datasets5 = class extends APIResource {
  static {
    __name(this, "Datasets5");
  }
  constructor() {
    super(...arguments);
    this.upload = new Upload3(this._client);
    this.versions = new Versions6(this._client);
  }
  create(params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/dlp/datasets`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  update(datasetId, params, options) {
    const { account_id, ...body } = params;
    return this._client.put(`/accounts/${account_id}/dlp/datasets/${datasetId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { account_id } = params;
    return this._client.getAPIList(`/accounts/${account_id}/dlp/datasets`, DatasetsSinglePage, options);
  }
  delete(datasetId, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/dlp/datasets/${datasetId}`, {
      ...options,
      headers: { Accept: "*/*", ...options?.headers }
    });
  }
  get(datasetId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/dlp/datasets/${datasetId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var DatasetsSinglePage = class extends SinglePage {
  static {
    __name(this, "DatasetsSinglePage");
  }
};
Datasets5.DatasetsSinglePage = DatasetsSinglePage;
Datasets5.Upload = Upload3;
Datasets5.Versions = Versions6;
Datasets5.VersionCreateResponsesSinglePage = VersionCreateResponsesSinglePage;
var AccountMapping = class extends APIResource {
  static {
    __name(this, "AccountMapping");
  }
  create(params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/dlp/email/account_mapping`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  get(params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/dlp/email/account_mapping`, options)._thenUnwrap((obj) => obj.result);
  }
};
var Rules11 = class extends APIResource {
  static {
    __name(this, "Rules11");
  }
  create(params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/dlp/email/rules`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  update(ruleId, params, options) {
    const { account_id, ...body } = params;
    return this._client.put(`/accounts/${account_id}/dlp/email/rules/${ruleId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { account_id } = params;
    return this._client.getAPIList(`/accounts/${account_id}/dlp/email/rules`, RuleListResponsesSinglePage3, options);
  }
  delete(ruleId, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/dlp/email/rules/${ruleId}`, options)._thenUnwrap((obj) => obj.result);
  }
  bulkEdit(params, options) {
    const { account_id, ...body } = params;
    return this._client.patch(`/accounts/${account_id}/dlp/email/rules`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  get(ruleId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/dlp/email/rules/${ruleId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var RuleListResponsesSinglePage3 = class extends SinglePage {
  static {
    __name(this, "RuleListResponsesSinglePage3");
  }
};
Rules11.RuleListResponsesSinglePage = RuleListResponsesSinglePage3;
var Email2 = class extends APIResource {
  static {
    __name(this, "Email2");
  }
  constructor() {
    super(...arguments);
    this.accountMapping = new AccountMapping(this._client);
    this.rules = new Rules11(this._client);
  }
};
Email2.AccountMapping = AccountMapping;
Email2.Rules = Rules11;
Email2.RuleListResponsesSinglePage = RuleListResponsesSinglePage3;
var Custom3 = class extends APIResource {
  static {
    __name(this, "Custom3");
  }
  create(params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/dlp/entries`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  update(entryId, params, options) {
    const { account_id, ...body } = params;
    return this._client.put(`/accounts/${account_id}/dlp/entries/custom/${entryId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { account_id } = params;
    return this._client.getAPIList(`/accounts/${account_id}/dlp/entries`, CustomListResponsesSinglePage, options);
  }
  delete(entryId, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/dlp/entries/${entryId}`, options)._thenUnwrap((obj) => obj.result);
  }
  get(entryId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/dlp/entries/${entryId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var CustomListResponsesSinglePage = class extends SinglePage {
  static {
    __name(this, "CustomListResponsesSinglePage");
  }
};
Custom3.CustomListResponsesSinglePage = CustomListResponsesSinglePage;
var Integration = class extends APIResource {
  static {
    __name(this, "Integration");
  }
  create(params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/dlp/entries/integration`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  update(entryId, params, options) {
    const { account_id, ...body } = params;
    return this._client.put(`/accounts/${account_id}/dlp/entries/integration/${entryId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { account_id } = params;
    return this._client.getAPIList(`/accounts/${account_id}/dlp/entries`, IntegrationListResponsesSinglePage, options);
  }
  delete(entryId, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/dlp/entries/integration/${entryId}`, options)._thenUnwrap((obj) => obj.result);
  }
  get(entryId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/dlp/entries/${entryId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var IntegrationListResponsesSinglePage = class extends SinglePage {
  static {
    __name(this, "IntegrationListResponsesSinglePage");
  }
};
Integration.IntegrationListResponsesSinglePage = IntegrationListResponsesSinglePage;
var Predefined = class extends APIResource {
  static {
    __name(this, "Predefined");
  }
  create(params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/dlp/entries/predefined`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  update(entryId, params, options) {
    const { account_id, ...body } = params;
    return this._client.put(`/accounts/${account_id}/dlp/entries/predefined/${entryId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { account_id } = params;
    return this._client.getAPIList(`/accounts/${account_id}/dlp/entries`, PredefinedListResponsesSinglePage, options);
  }
  delete(entryId, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/dlp/entries/predefined/${entryId}`, options)._thenUnwrap((obj) => obj.result);
  }
  get(entryId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/dlp/entries/${entryId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var PredefinedListResponsesSinglePage = class extends SinglePage {
  static {
    __name(this, "PredefinedListResponsesSinglePage");
  }
};
Predefined.PredefinedListResponsesSinglePage = PredefinedListResponsesSinglePage;
var Entries3 = class extends APIResource {
  static {
    __name(this, "Entries3");
  }
  constructor() {
    super(...arguments);
    this.custom = new Custom3(this._client);
    this.predefined = new Predefined(this._client);
    this.integration = new Integration(this._client);
  }
  create(params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/dlp/entries`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  update(entryId, params, options) {
    const { account_id, ...body } = params;
    return this._client.put(`/accounts/${account_id}/dlp/entries/${entryId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { account_id } = params;
    return this._client.getAPIList(`/accounts/${account_id}/dlp/entries`, EntryListResponsesSinglePage, options);
  }
  delete(entryId, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/dlp/entries/${entryId}`, options)._thenUnwrap((obj) => obj.result);
  }
  get(entryId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/dlp/entries/${entryId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var EntryListResponsesSinglePage = class extends SinglePage {
  static {
    __name(this, "EntryListResponsesSinglePage");
  }
};
Entries3.EntryListResponsesSinglePage = EntryListResponsesSinglePage;
Entries3.Custom = Custom3;
Entries3.CustomListResponsesSinglePage = CustomListResponsesSinglePage;
Entries3.Predefined = Predefined;
Entries3.PredefinedListResponsesSinglePage = PredefinedListResponsesSinglePage;
Entries3.Integration = Integration;
Entries3.IntegrationListResponsesSinglePage = IntegrationListResponsesSinglePage;
var Custom4 = class extends APIResource {
  static {
    __name(this, "Custom4");
  }
  create(params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/dlp/profiles/custom`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  update(profileId, params, options) {
    const { account_id, ...body } = params;
    return this._client.put(`/accounts/${account_id}/dlp/profiles/custom/${profileId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  delete(profileId, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/dlp/profiles/custom/${profileId}`, options)._thenUnwrap((obj) => obj.result);
  }
  get(profileId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/dlp/profiles/custom/${profileId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var Predefined2 = class extends APIResource {
  static {
    __name(this, "Predefined2");
  }
  create(params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/dlp/profiles/predefined`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  update(profileId, params, options) {
    const { account_id, ...body } = params;
    return this._client.put(`/accounts/${account_id}/dlp/profiles/predefined/${profileId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  delete(profileId, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/dlp/profiles/predefined/${profileId}`, options)._thenUnwrap((obj) => obj.result);
  }
  get(profileId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/dlp/profiles/predefined/${profileId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var Profiles2 = class extends APIResource {
  static {
    __name(this, "Profiles2");
  }
  constructor() {
    super(...arguments);
    this.custom = new Custom4(this._client);
    this.predefined = new Predefined2(this._client);
  }
  list(params, options) {
    const { account_id, ...query } = params;
    return this._client.getAPIList(`/accounts/${account_id}/dlp/profiles`, ProfilesSinglePage, {
      query,
      ...options
    });
  }
  get(profileId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/dlp/profiles/${profileId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var ProfilesSinglePage = class extends SinglePage {
  static {
    __name(this, "ProfilesSinglePage");
  }
};
Profiles2.ProfilesSinglePage = ProfilesSinglePage;
Profiles2.Custom = Custom4;
Profiles2.Predefined = Predefined2;
var DLP = class extends APIResource {
  static {
    __name(this, "DLP");
  }
  constructor() {
    super(...arguments);
    this.datasets = new Datasets5(this._client);
    this.patterns = new Patterns(this._client);
    this.payloadLogs = new PayloadLogs(this._client);
    this.email = new Email2(this._client);
    this.profiles = new Profiles2(this._client);
    this.limits = new Limits(this._client);
    this.entries = new Entries3(this._client);
  }
};
DLP.Datasets = Datasets5;
DLP.DatasetsSinglePage = DatasetsSinglePage;
DLP.Patterns = Patterns;
DLP.PayloadLogs = PayloadLogs;
DLP.Email = Email2;
DLP.Profiles = Profiles2;
DLP.ProfilesSinglePage = ProfilesSinglePage;
DLP.Limits = Limits;
DLP.Entries = Entries3;
DLP.EntryListResponsesSinglePage = EntryListResponsesSinglePage;
var AppTypes = class extends APIResource {
  static {
    __name(this, "AppTypes");
  }
  list(params, options) {
    const { account_id } = params;
    return this._client.getAPIList(`/accounts/${account_id}/gateway/app_types`, AppTypesSinglePage, options);
  }
};
var AppTypesSinglePage = class extends SinglePage {
  static {
    __name(this, "AppTypesSinglePage");
  }
};
AppTypes.AppTypesSinglePage = AppTypesSinglePage;
var AuditSSHSettings = class extends APIResource {
  static {
    __name(this, "AuditSSHSettings");
  }
  update(params, options) {
    const { account_id, ...body } = params;
    return this._client.put(`/accounts/${account_id}/gateway/audit_ssh_settings`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  get(params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/gateway/audit_ssh_settings`, options)._thenUnwrap((obj) => obj.result);
  }
  rotateSeed(params, options) {
    const { account_id } = params;
    return this._client.post(`/accounts/${account_id}/gateway/audit_ssh_settings/rotate_seed`, options)._thenUnwrap((obj) => obj.result);
  }
};
var Categories2 = class extends APIResource {
  static {
    __name(this, "Categories2");
  }
  list(params, options) {
    const { account_id } = params;
    return this._client.getAPIList(`/accounts/${account_id}/gateway/categories`, CategoriesSinglePage, options);
  }
};
var CategoriesSinglePage = class extends SinglePage {
  static {
    __name(this, "CategoriesSinglePage");
  }
};
Categories2.CategoriesSinglePage = CategoriesSinglePage;
var Certificates5 = class extends APIResource {
  static {
    __name(this, "Certificates5");
  }
  create(params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/gateway/certificates`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { account_id } = params;
    return this._client.getAPIList(`/accounts/${account_id}/gateway/certificates`, CertificateListResponsesSinglePage2, options);
  }
  delete(certificateId, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/gateway/certificates/${certificateId}`, options)._thenUnwrap((obj) => obj.result);
  }
  activate(certificateId, params, options) {
    const { account_id, body } = params;
    return this._client.post(`/accounts/${account_id}/gateway/certificates/${certificateId}/activate`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  deactivate(certificateId, params, options) {
    const { account_id, body } = params;
    return this._client.post(`/accounts/${account_id}/gateway/certificates/${certificateId}/deactivate`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  get(certificateId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/gateway/certificates/${certificateId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var CertificateListResponsesSinglePage2 = class extends SinglePage {
  static {
    __name(this, "CertificateListResponsesSinglePage2");
  }
};
Certificates5.CertificateListResponsesSinglePage = CertificateListResponsesSinglePage2;
var Locations6 = class extends APIResource {
  static {
    __name(this, "Locations6");
  }
  create(params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/gateway/locations`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  update(locationId, params, options) {
    const { account_id, ...body } = params;
    return this._client.put(`/accounts/${account_id}/gateway/locations/${locationId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { account_id } = params;
    return this._client.getAPIList(`/accounts/${account_id}/gateway/locations`, LocationsSinglePage, options);
  }
  delete(locationId, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/gateway/locations/${locationId}`, options)._thenUnwrap((obj) => obj.result);
  }
  get(locationId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/gateway/locations/${locationId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var LocationsSinglePage = class extends SinglePage {
  static {
    __name(this, "LocationsSinglePage");
  }
};
Locations6.LocationsSinglePage = LocationsSinglePage;
var Logging = class extends APIResource {
  static {
    __name(this, "Logging");
  }
  update(params, options) {
    const { account_id, ...body } = params;
    return this._client.put(`/accounts/${account_id}/gateway/logging`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  get(params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/gateway/logging`, options)._thenUnwrap((obj) => obj.result);
  }
};
var ProxyEndpoints = class extends APIResource {
  static {
    __name(this, "ProxyEndpoints");
  }
  create(params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/gateway/proxy_endpoints`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/gateway/proxy_endpoints`, options)._thenUnwrap((obj) => obj.result);
  }
  delete(proxyEndpointId, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/gateway/proxy_endpoints/${proxyEndpointId}`, options)._thenUnwrap((obj) => obj.result);
  }
  edit(proxyEndpointId, params, options) {
    const { account_id, ...body } = params;
    return this._client.patch(`/accounts/${account_id}/gateway/proxy_endpoints/${proxyEndpointId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  get(proxyEndpointId, params, options) {
    const { account_id } = params;
    return this._client.getAPIList(`/accounts/${account_id}/gateway/proxy_endpoints/${proxyEndpointId}`, ProxyEndpointsSinglePage, options);
  }
};
var ProxyEndpointsSinglePage = class extends SinglePage {
  static {
    __name(this, "ProxyEndpointsSinglePage");
  }
};
ProxyEndpoints.ProxyEndpointsSinglePage = ProxyEndpointsSinglePage;
var Rules12 = class extends APIResource {
  static {
    __name(this, "Rules12");
  }
  create(params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/gateway/rules`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  update(ruleId, params, options) {
    const { account_id, ...body } = params;
    return this._client.put(`/accounts/${account_id}/gateway/rules/${ruleId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { account_id } = params;
    return this._client.getAPIList(`/accounts/${account_id}/gateway/rules`, GatewayRulesSinglePage, options);
  }
  delete(ruleId, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/gateway/rules/${ruleId}`, options)._thenUnwrap((obj) => obj.result);
  }
  get(ruleId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/gateway/rules/${ruleId}`, options)._thenUnwrap((obj) => obj.result);
  }
  resetExpiration(ruleId, params, options) {
    const { account_id } = params;
    return this._client.post(`/accounts/${account_id}/gateway/rules/${ruleId}/reset_expiration`, options)._thenUnwrap((obj) => obj.result);
  }
};
var GatewayRulesSinglePage = class extends SinglePage {
  static {
    __name(this, "GatewayRulesSinglePage");
  }
};
Rules12.GatewayRulesSinglePage = GatewayRulesSinglePage;
var CustomCertificate = class extends APIResource {
  static {
    __name(this, "CustomCertificate");
  }
  get(params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/gateway/configuration/custom_certificate`, options);
  }
};
var Configurations2 = class extends APIResource {
  static {
    __name(this, "Configurations2");
  }
  constructor() {
    super(...arguments);
    this.customCertificate = new CustomCertificate(this._client);
  }
  update(params, options) {
    const { account_id, ...body } = params;
    return this._client.put(`/accounts/${account_id}/gateway/configuration`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  edit(params, options) {
    const { account_id, ...body } = params;
    return this._client.patch(`/accounts/${account_id}/gateway/configuration`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  get(params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/gateway/configuration`, options)._thenUnwrap((obj) => obj.result);
  }
};
Configurations2.CustomCertificate = CustomCertificate;
var Items2 = class extends APIResource {
  static {
    __name(this, "Items2");
  }
  list(listId, params, options) {
    const { account_id } = params;
    return this._client.getAPIList(`/accounts/${account_id}/gateway/lists/${listId}/items`, ItemListResponsesSinglePage, options);
  }
};
var ItemListResponsesSinglePage = class extends SinglePage {
  static {
    __name(this, "ItemListResponsesSinglePage");
  }
};
Items2.ItemListResponsesSinglePage = ItemListResponsesSinglePage;
var Lists2 = class extends APIResource {
  static {
    __name(this, "Lists2");
  }
  constructor() {
    super(...arguments);
    this.items = new Items2(this._client);
  }
  create(params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/gateway/lists`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  update(listId, params, options) {
    const { account_id, ...body } = params;
    return this._client.put(`/accounts/${account_id}/gateway/lists/${listId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { account_id, ...query } = params;
    return this._client.getAPIList(`/accounts/${account_id}/gateway/lists`, GatewayListsSinglePage, {
      query,
      ...options
    });
  }
  delete(listId, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/gateway/lists/${listId}`, options)._thenUnwrap((obj) => obj.result);
  }
  edit(listId, params, options) {
    const { account_id, ...body } = params;
    return this._client.patch(`/accounts/${account_id}/gateway/lists/${listId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  get(listId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/gateway/lists/${listId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var GatewayListsSinglePage = class extends SinglePage {
  static {
    __name(this, "GatewayListsSinglePage");
  }
};
Lists2.GatewayListsSinglePage = GatewayListsSinglePage;
Lists2.Items = Items2;
Lists2.ItemListResponsesSinglePage = ItemListResponsesSinglePage;
var Gateway = class extends APIResource {
  static {
    __name(this, "Gateway");
  }
  constructor() {
    super(...arguments);
    this.auditSSHSettings = new AuditSSHSettings(this._client);
    this.categories = new Categories2(this._client);
    this.appTypes = new AppTypes(this._client);
    this.configurations = new Configurations2(this._client);
    this.lists = new Lists2(this._client);
    this.locations = new Locations6(this._client);
    this.logging = new Logging(this._client);
    this.proxyEndpoints = new ProxyEndpoints(this._client);
    this.rules = new Rules12(this._client);
    this.certificates = new Certificates5(this._client);
  }
  create(params, options) {
    const { account_id } = params;
    return this._client.post(`/accounts/${account_id}/gateway`, options)._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/gateway`, options)._thenUnwrap((obj) => obj.result);
  }
};
Gateway.AuditSSHSettings = AuditSSHSettings;
Gateway.Categories = Categories2;
Gateway.CategoriesSinglePage = CategoriesSinglePage;
Gateway.AppTypes = AppTypes;
Gateway.AppTypesSinglePage = AppTypesSinglePage;
Gateway.Configurations = Configurations2;
Gateway.Lists = Lists2;
Gateway.GatewayListsSinglePage = GatewayListsSinglePage;
Gateway.Locations = Locations6;
Gateway.LocationsSinglePage = LocationsSinglePage;
Gateway.Logging = Logging;
Gateway.ProxyEndpoints = ProxyEndpoints;
Gateway.ProxyEndpointsSinglePage = ProxyEndpointsSinglePage;
Gateway.Rules = Rules12;
Gateway.GatewayRulesSinglePage = GatewayRulesSinglePage;
Gateway.Certificates = Certificates5;
Gateway.CertificateListResponsesSinglePage = CertificateListResponsesSinglePage2;
var Groups3 = class extends APIResource {
  static {
    __name(this, "Groups3");
  }
  list(identityProviderId, params, options) {
    const { account_id, ...query } = params;
    return this._client.getAPIList(`/accounts/${account_id}/access/identity_providers/${identityProviderId}/scim/groups`, ZeroTrustGroupsV4PagePaginationArray, { query, ...options });
  }
};
var Users3 = class extends APIResource {
  static {
    __name(this, "Users3");
  }
  list(identityProviderId, params, options) {
    const { account_id, ...query } = params;
    return this._client.getAPIList(`/accounts/${account_id}/access/identity_providers/${identityProviderId}/scim/users`, AccessUsersV4PagePaginationArray, { query, ...options });
  }
};
var SCIM2 = class extends APIResource {
  static {
    __name(this, "SCIM2");
  }
  constructor() {
    super(...arguments);
    this.groups = new Groups3(this._client);
    this.users = new Users3(this._client);
  }
};
SCIM2.Groups = Groups3;
SCIM2.Users = Users3;
var IdentityProviders = class extends APIResource {
  static {
    __name(this, "IdentityProviders");
  }
  constructor() {
    super(...arguments);
    this.scim = new SCIM2(this._client);
  }
  create(params, options) {
    const { account_id, zone_id, ...body } = params;
    if (!account_id && !zone_id) {
      throw new CloudflareError("You must provide either account_id or zone_id.");
    }
    if (account_id && zone_id) {
      throw new CloudflareError("You cannot provide both account_id and zone_id.");
    }
    const { accountOrZone, accountOrZoneId } = account_id ? {
      accountOrZone: "accounts",
      accountOrZoneId: account_id
    } : {
      accountOrZone: "zones",
      accountOrZoneId: zone_id
    };
    return this._client.post(`/${accountOrZone}/${accountOrZoneId}/access/identity_providers`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  update(identityProviderId, params, options) {
    const { account_id, zone_id, ...body } = params;
    if (!account_id && !zone_id) {
      throw new CloudflareError("You must provide either account_id or zone_id.");
    }
    if (account_id && zone_id) {
      throw new CloudflareError("You cannot provide both account_id and zone_id.");
    }
    const { accountOrZone, accountOrZoneId } = account_id ? {
      accountOrZone: "accounts",
      accountOrZoneId: account_id
    } : {
      accountOrZone: "zones",
      accountOrZoneId: zone_id
    };
    return this._client.put(`/${accountOrZone}/${accountOrZoneId}/access/identity_providers/${identityProviderId}`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  list(params = {}, options) {
    if (isRequestOptions(params)) {
      return this.list({}, params);
    }
    const { account_id, zone_id, ...query } = params;
    if (!account_id && !zone_id) {
      throw new CloudflareError("You must provide either account_id or zone_id.");
    }
    if (account_id && zone_id) {
      throw new CloudflareError("You cannot provide both account_id and zone_id.");
    }
    const { accountOrZone, accountOrZoneId } = account_id ? {
      accountOrZone: "accounts",
      accountOrZoneId: account_id
    } : {
      accountOrZone: "zones",
      accountOrZoneId: zone_id
    };
    return this._client.getAPIList(`/${accountOrZone}/${accountOrZoneId}/access/identity_providers`, IdentityProviderListResponsesV4PagePaginationArray, { query, ...options });
  }
  delete(identityProviderId, params = {}, options) {
    if (isRequestOptions(params)) {
      return this.delete(identityProviderId, {}, params);
    }
    const { account_id, zone_id } = params;
    if (!account_id && !zone_id) {
      throw new CloudflareError("You must provide either account_id or zone_id.");
    }
    if (account_id && zone_id) {
      throw new CloudflareError("You cannot provide both account_id and zone_id.");
    }
    const { accountOrZone, accountOrZoneId } = account_id ? {
      accountOrZone: "accounts",
      accountOrZoneId: account_id
    } : {
      accountOrZone: "zones",
      accountOrZoneId: zone_id
    };
    return this._client.delete(`/${accountOrZone}/${accountOrZoneId}/access/identity_providers/${identityProviderId}`, options)._thenUnwrap((obj) => obj.result);
  }
  get(identityProviderId, params = {}, options) {
    if (isRequestOptions(params)) {
      return this.get(identityProviderId, {}, params);
    }
    const { account_id, zone_id } = params;
    if (!account_id && !zone_id) {
      throw new CloudflareError("You must provide either account_id or zone_id.");
    }
    if (account_id && zone_id) {
      throw new CloudflareError("You cannot provide both account_id and zone_id.");
    }
    const { accountOrZone, accountOrZoneId } = account_id ? {
      accountOrZone: "accounts",
      accountOrZoneId: account_id
    } : {
      accountOrZone: "zones",
      accountOrZoneId: zone_id
    };
    return this._client.get(`/${accountOrZone}/${accountOrZoneId}/access/identity_providers/${identityProviderId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var IdentityProviderListResponsesV4PagePaginationArray = class extends V4PagePaginationArray {
  static {
    __name(this, "IdentityProviderListResponsesV4PagePaginationArray");
  }
};
IdentityProviders.IdentityProviderListResponsesV4PagePaginationArray = IdentityProviderListResponsesV4PagePaginationArray;
IdentityProviders.SCIM = SCIM2;
var HostnameRoutes = class extends APIResource {
  static {
    __name(this, "HostnameRoutes");
  }
  create(params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/zerotrust/routes/hostname`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { account_id, ...query } = params;
    return this._client.getAPIList(`/accounts/${account_id}/zerotrust/routes/hostname`, HostnameRoutesV4PagePaginationArray, { query, ...options });
  }
  delete(hostnameRouteId, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/zerotrust/routes/hostname/${hostnameRouteId}`, options)._thenUnwrap((obj) => obj.result);
  }
  edit(hostnameRouteId, params, options) {
    const { account_id, ...body } = params;
    return this._client.patch(`/accounts/${account_id}/zerotrust/routes/hostname/${hostnameRouteId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  get(hostnameRouteId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/zerotrust/routes/hostname/${hostnameRouteId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var HostnameRoutesV4PagePaginationArray = class extends V4PagePaginationArray {
  static {
    __name(this, "HostnameRoutesV4PagePaginationArray");
  }
};
HostnameRoutes.HostnameRoutesV4PagePaginationArray = HostnameRoutesV4PagePaginationArray;
var VirtualNetworks = class extends APIResource {
  static {
    __name(this, "VirtualNetworks");
  }
  create(params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/teamnet/virtual_networks`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { account_id, ...query } = params;
    return this._client.getAPIList(`/accounts/${account_id}/teamnet/virtual_networks`, VirtualNetworksSinglePage, { query, ...options });
  }
  delete(virtualNetworkId, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/teamnet/virtual_networks/${virtualNetworkId}`, options)._thenUnwrap((obj) => obj.result);
  }
  edit(virtualNetworkId, params, options) {
    const { account_id, ...body } = params;
    return this._client.patch(`/accounts/${account_id}/teamnet/virtual_networks/${virtualNetworkId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  get(virtualNetworkId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/teamnet/virtual_networks/${virtualNetworkId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var VirtualNetworksSinglePage = class extends SinglePage {
  static {
    __name(this, "VirtualNetworksSinglePage");
  }
};
VirtualNetworks.VirtualNetworksSinglePage = VirtualNetworksSinglePage;
var IPs5 = class extends APIResource {
  static {
    __name(this, "IPs5");
  }
  get(ip, params, options) {
    const { account_id, ...query } = params;
    return this._client.get(`/accounts/${account_id}/teamnet/routes/ip/${ip}`, {
      query,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
};
var Networks2 = class extends APIResource {
  static {
    __name(this, "Networks2");
  }
  create(ipNetworkEncoded, params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/teamnet/routes/network/${ipNetworkEncoded}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  delete(ipNetworkEncoded, params, options) {
    const { account_id, tun_type, tunnel_id, virtual_network_id } = params;
    return this._client.delete(`/accounts/${account_id}/teamnet/routes/network/${ipNetworkEncoded}`, {
      query: { tun_type, tunnel_id, virtual_network_id },
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  edit(ipNetworkEncoded, params, options) {
    const { account_id } = params;
    return this._client.patch(`/accounts/${account_id}/teamnet/routes/network/${ipNetworkEncoded}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var Routes4 = class extends APIResource {
  static {
    __name(this, "Routes4");
  }
  constructor() {
    super(...arguments);
    this.ips = new IPs5(this._client);
    this.networks = new Networks2(this._client);
  }
  create(params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/teamnet/routes`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { account_id, ...query } = params;
    return this._client.getAPIList(`/accounts/${account_id}/teamnet/routes`, TeamnetsV4PagePaginationArray, {
      query,
      ...options
    });
  }
  delete(routeId, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/teamnet/routes/${routeId}`, options)._thenUnwrap((obj) => obj.result);
  }
  edit(routeId, params, options) {
    const { account_id, ...body } = params;
    return this._client.patch(`/accounts/${account_id}/teamnet/routes/${routeId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  get(routeId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/teamnet/routes/${routeId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var TeamnetsV4PagePaginationArray = class extends V4PagePaginationArray {
  static {
    __name(this, "TeamnetsV4PagePaginationArray");
  }
};
Routes4.TeamnetsV4PagePaginationArray = TeamnetsV4PagePaginationArray;
Routes4.IPs = IPs5;
Routes4.Networks = Networks2;
var CloudflareSource = class extends APIResource {
  static {
    __name(this, "CloudflareSource");
  }
  update(addressFamily, params, options) {
    const { account_id, ...body } = params;
    return this._client.patch(`/accounts/${account_id}/zerotrust/subnets/cloudflare_source/${addressFamily}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
};
var Subnets2 = class extends APIResource {
  static {
    __name(this, "Subnets2");
  }
  constructor() {
    super(...arguments);
    this.cloudflareSource = new CloudflareSource(this._client);
  }
  list(params, options) {
    const { account_id, ...query } = params;
    return this._client.getAPIList(`/accounts/${account_id}/zerotrust/subnets`, SubnetListResponsesV4PagePaginationArray, { query, ...options });
  }
};
var SubnetListResponsesV4PagePaginationArray = class extends V4PagePaginationArray {
  static {
    __name(this, "SubnetListResponsesV4PagePaginationArray");
  }
};
Subnets2.SubnetListResponsesV4PagePaginationArray = SubnetListResponsesV4PagePaginationArray;
Subnets2.CloudflareSource = CloudflareSource;
var Networks3 = class extends APIResource {
  static {
    __name(this, "Networks3");
  }
  constructor() {
    super(...arguments);
    this.routes = new Routes4(this._client);
    this.virtualNetworks = new VirtualNetworks(this._client);
    this.subnets = new Subnets2(this._client);
    this.hostnameRoutes = new HostnameRoutes(this._client);
  }
};
Networks3.Routes = Routes4;
Networks3.TeamnetsV4PagePaginationArray = TeamnetsV4PagePaginationArray;
Networks3.VirtualNetworks = VirtualNetworks;
Networks3.VirtualNetworksSinglePage = VirtualNetworksSinglePage;
Networks3.Subnets = Subnets2;
Networks3.SubnetListResponsesV4PagePaginationArray = SubnetListResponsesV4PagePaginationArray;
Networks3.HostnameRoutes = HostnameRoutes;
Networks3.HostnameRoutesV4PagePaginationArray = HostnameRoutesV4PagePaginationArray;
var DOH = class extends APIResource {
  static {
    __name(this, "DOH");
  }
  update(params, options) {
    const { account_id, ...body } = params;
    return this._client.put(`/accounts/${account_id}/access/organizations/doh`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  get(params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/access/organizations/doh`, options)._thenUnwrap((obj) => obj.result);
  }
};
var Organizations2 = class extends APIResource {
  static {
    __name(this, "Organizations2");
  }
  constructor() {
    super(...arguments);
    this.doh = new DOH(this._client);
  }
  create(params, options) {
    const { account_id, zone_id, ...body } = params;
    if (!account_id && !zone_id) {
      throw new CloudflareError("You must provide either account_id or zone_id.");
    }
    if (account_id && zone_id) {
      throw new CloudflareError("You cannot provide both account_id and zone_id.");
    }
    const { accountOrZone, accountOrZoneId } = account_id ? {
      accountOrZone: "accounts",
      accountOrZoneId: account_id
    } : {
      accountOrZone: "zones",
      accountOrZoneId: zone_id
    };
    return this._client.post(`/${accountOrZone}/${accountOrZoneId}/access/organizations`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  update(params, options) {
    const { account_id, zone_id, ...body } = params;
    if (!account_id && !zone_id) {
      throw new CloudflareError("You must provide either account_id or zone_id.");
    }
    if (account_id && zone_id) {
      throw new CloudflareError("You cannot provide both account_id and zone_id.");
    }
    const { accountOrZone, accountOrZoneId } = account_id ? {
      accountOrZone: "accounts",
      accountOrZoneId: account_id
    } : {
      accountOrZone: "zones",
      accountOrZoneId: zone_id
    };
    return this._client.put(`/${accountOrZone}/${accountOrZoneId}/access/organizations`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(params = {}, options) {
    if (isRequestOptions(params)) {
      return this.list({}, params);
    }
    const { account_id, zone_id } = params;
    if (!account_id && !zone_id) {
      throw new CloudflareError("You must provide either account_id or zone_id.");
    }
    if (account_id && zone_id) {
      throw new CloudflareError("You cannot provide both account_id and zone_id.");
    }
    const { accountOrZone, accountOrZoneId } = account_id ? {
      accountOrZone: "accounts",
      accountOrZoneId: account_id
    } : {
      accountOrZone: "zones",
      accountOrZoneId: zone_id
    };
    return this._client.get(`/${accountOrZone}/${accountOrZoneId}/access/organizations`, options)._thenUnwrap((obj) => obj.result);
  }
  revokeUsers(params, options) {
    const { account_id, zone_id, query_devices, ...body } = params;
    if (!account_id && !zone_id) {
      throw new CloudflareError("You must provide either account_id or zone_id.");
    }
    if (account_id && zone_id) {
      throw new CloudflareError("You cannot provide both account_id and zone_id.");
    }
    const { accountOrZone, accountOrZoneId } = account_id ? {
      accountOrZone: "accounts",
      accountOrZoneId: account_id
    } : {
      accountOrZone: "zones",
      accountOrZoneId: zone_id
    };
    return this._client.post(`/${accountOrZone}/${accountOrZoneId}/access/organizations/revoke_user`, {
      query: { devices: query_devices },
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
};
Organizations2.DOH = DOH;
var Behaviours = class extends APIResource {
  static {
    __name(this, "Behaviours");
  }
  update(params, options) {
    const { account_id, ...body } = params;
    return this._client.put(`/accounts/${account_id}/zt_risk_scoring/behaviors`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  get(params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/zt_risk_scoring/behaviors`, options)._thenUnwrap((obj) => obj.result);
  }
};
var Summary12 = class extends APIResource {
  static {
    __name(this, "Summary12");
  }
  get(params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/zt_risk_scoring/summary`, options)._thenUnwrap((obj) => obj.result);
  }
};
var References3 = class extends APIResource {
  static {
    __name(this, "References3");
  }
  get(referenceId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/zt_risk_scoring/integrations/reference_id/${referenceId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var Integrations2 = class extends APIResource {
  static {
    __name(this, "Integrations2");
  }
  constructor() {
    super(...arguments);
    this.references = new References3(this._client);
  }
  create(params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/zt_risk_scoring/integrations`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  update(integrationId, params, options) {
    const { account_id, ...body } = params;
    return this._client.put(`/accounts/${account_id}/zt_risk_scoring/integrations/${integrationId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { account_id } = params;
    return this._client.getAPIList(`/accounts/${account_id}/zt_risk_scoring/integrations`, IntegrationListResponsesSinglePage2, options);
  }
  delete(integrationId, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/zt_risk_scoring/integrations/${integrationId}`, options)._thenUnwrap((obj) => obj.result);
  }
  get(integrationId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/zt_risk_scoring/integrations/${integrationId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var IntegrationListResponsesSinglePage2 = class extends SinglePage {
  static {
    __name(this, "IntegrationListResponsesSinglePage2");
  }
};
Integrations2.IntegrationListResponsesSinglePage = IntegrationListResponsesSinglePage2;
Integrations2.References = References3;
var RiskScoring = class extends APIResource {
  static {
    __name(this, "RiskScoring");
  }
  constructor() {
    super(...arguments);
    this.behaviours = new Behaviours(this._client);
    this.summary = new Summary12(this._client);
    this.integrations = new Integrations2(this._client);
  }
  get(userId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/zt_risk_scoring/${userId}`, options)._thenUnwrap((obj) => obj.result);
  }
  reset(userId, params, options) {
    const { account_id } = params;
    return this._client.post(`/accounts/${account_id}/zt_risk_scoring/${userId}/reset`, options)._thenUnwrap((obj) => obj.result);
  }
};
RiskScoring.Behaviours = Behaviours;
RiskScoring.Summary = Summary12;
RiskScoring.Integrations = Integrations2;
RiskScoring.IntegrationListResponsesSinglePage = IntegrationListResponsesSinglePage2;
var Configurations3 = class extends APIResource {
  static {
    __name(this, "Configurations3");
  }
  update(tunnelId, params, options) {
    const { account_id, ...body } = params;
    return this._client.put(`/accounts/${account_id}/cfd_tunnel/${tunnelId}/configurations`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  get(tunnelId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/cfd_tunnel/${tunnelId}/configurations`, options)._thenUnwrap((obj) => obj.result);
  }
};
var Connections2 = class extends APIResource {
  static {
    __name(this, "Connections2");
  }
  delete(tunnelId, params, options) {
    const { account_id, client_id } = params;
    return this._client.delete(`/accounts/${account_id}/cfd_tunnel/${tunnelId}/connections`, {
      query: { client_id },
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  get(tunnelId, params, options) {
    const { account_id } = params;
    return this._client.getAPIList(`/accounts/${account_id}/cfd_tunnel/${tunnelId}/connections`, ClientsSinglePage, options);
  }
};
var ClientsSinglePage = class extends SinglePage {
  static {
    __name(this, "ClientsSinglePage");
  }
};
Connections2.ClientsSinglePage = ClientsSinglePage;
var Connectors2 = class extends APIResource {
  static {
    __name(this, "Connectors2");
  }
  get(tunnelId, connectorId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/cfd_tunnel/${tunnelId}/connectors/${connectorId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var Management = class extends APIResource {
  static {
    __name(this, "Management");
  }
  create(tunnelId, params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/cfd_tunnel/${tunnelId}/management`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
};
var Token2 = class extends APIResource {
  static {
    __name(this, "Token2");
  }
  get(tunnelId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/cfd_tunnel/${tunnelId}/token`, options)._thenUnwrap((obj) => obj.result);
  }
};
var Cloudflared = class extends APIResource {
  static {
    __name(this, "Cloudflared");
  }
  constructor() {
    super(...arguments);
    this.configurations = new Configurations3(this._client);
    this.connections = new Connections2(this._client);
    this.token = new Token2(this._client);
    this.connectors = new Connectors2(this._client);
    this.management = new Management(this._client);
  }
  create(params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/cfd_tunnel`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { account_id, ...query } = params;
    return this._client.getAPIList(`/accounts/${account_id}/cfd_tunnel`, CloudflaredListResponsesV4PagePaginationArray, { query, ...options });
  }
  delete(tunnelId, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/cfd_tunnel/${tunnelId}`, options)._thenUnwrap((obj) => obj.result);
  }
  edit(tunnelId, params, options) {
    const { account_id, ...body } = params;
    return this._client.patch(`/accounts/${account_id}/cfd_tunnel/${tunnelId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  get(tunnelId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/cfd_tunnel/${tunnelId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var CloudflaredListResponsesV4PagePaginationArray = class extends V4PagePaginationArray {
  static {
    __name(this, "CloudflaredListResponsesV4PagePaginationArray");
  }
};
Cloudflared.CloudflaredListResponsesV4PagePaginationArray = CloudflaredListResponsesV4PagePaginationArray;
Cloudflared.Configurations = Configurations3;
Cloudflared.Connections = Connections2;
Cloudflared.ClientsSinglePage = ClientsSinglePage;
Cloudflared.Token = Token2;
Cloudflared.Connectors = Connectors2;
Cloudflared.Management = Management;
var Token3 = class extends APIResource {
  static {
    __name(this, "Token3");
  }
  get(tunnelId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/warp_connector/${tunnelId}/token`, options)._thenUnwrap((obj) => obj.result);
  }
};
var WARPConnector = class extends APIResource {
  static {
    __name(this, "WARPConnector");
  }
  constructor() {
    super(...arguments);
    this.token = new Token3(this._client);
  }
  create(params, options) {
    const { account_id, ...body } = params;
    return this._client.post(`/accounts/${account_id}/warp_connector`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  list(params, options) {
    const { account_id, ...query } = params;
    return this._client.getAPIList(`/accounts/${account_id}/warp_connector`, WARPConnectorListResponsesV4PagePaginationArray, { query, ...options });
  }
  delete(tunnelId, params, options) {
    const { account_id } = params;
    return this._client.delete(`/accounts/${account_id}/warp_connector/${tunnelId}`, options)._thenUnwrap((obj) => obj.result);
  }
  edit(tunnelId, params, options) {
    const { account_id, ...body } = params;
    return this._client.patch(`/accounts/${account_id}/warp_connector/${tunnelId}`, {
      body,
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  get(tunnelId, params, options) {
    const { account_id } = params;
    return this._client.get(`/accounts/${account_id}/warp_connector/${tunnelId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var WARPConnectorListResponsesV4PagePaginationArray = class extends V4PagePaginationArray {
  static {
    __name(this, "WARPConnectorListResponsesV4PagePaginationArray");
  }
};
WARPConnector.WARPConnectorListResponsesV4PagePaginationArray = WARPConnectorListResponsesV4PagePaginationArray;
WARPConnector.Token = Token3;
var Tunnels = class extends APIResource {
  static {
    __name(this, "Tunnels");
  }
  constructor() {
    super(...arguments);
    this.cloudflared = new Cloudflared(this._client);
    this.warpConnector = new WARPConnector(this._client);
  }
  list(params, options) {
    const { account_id, ...query } = params;
    return this._client.getAPIList(`/accounts/${account_id}/tunnels`, TunnelListResponsesV4PagePaginationArray, { query, ...options });
  }
};
var TunnelListResponsesV4PagePaginationArray = class extends V4PagePaginationArray {
  static {
    __name(this, "TunnelListResponsesV4PagePaginationArray");
  }
};
Tunnels.TunnelListResponsesV4PagePaginationArray = TunnelListResponsesV4PagePaginationArray;
Tunnels.Cloudflared = Cloudflared;
Tunnels.CloudflaredListResponsesV4PagePaginationArray = CloudflaredListResponsesV4PagePaginationArray;
Tunnels.WARPConnector = WARPConnector;
Tunnels.WARPConnectorListResponsesV4PagePaginationArray = WARPConnectorListResponsesV4PagePaginationArray;
var ZeroTrust = class extends APIResource {
  static {
    __name(this, "ZeroTrust");
  }
  constructor() {
    super(...arguments);
    this.devices = new Devices2(this._client);
    this.identityProviders = new IdentityProviders(this._client);
    this.organizations = new Organizations2(this._client);
    this.seats = new Seats(this._client);
    this.access = new Access(this._client);
    this.dex = new DEX(this._client);
    this.tunnels = new Tunnels(this._client);
    this.connectivitySettings = new ConnectivitySettings(this._client);
    this.dlp = new DLP(this._client);
    this.gateway = new Gateway(this._client);
    this.networks = new Networks3(this._client);
    this.riskScoring = new RiskScoring(this._client);
  }
};
ZeroTrust.Devices = Devices2;
ZeroTrust.DevicesSinglePage = DevicesSinglePage;
ZeroTrust.IdentityProviders = IdentityProviders;
ZeroTrust.IdentityProviderListResponsesV4PagePaginationArray = IdentityProviderListResponsesV4PagePaginationArray;
ZeroTrust.Organizations = Organizations2;
ZeroTrust.Seats = Seats;
ZeroTrust.SeatsSinglePage = SeatsSinglePage;
ZeroTrust.Access = Access;
ZeroTrust.DEX = DEX;
ZeroTrust.Tunnels = Tunnels;
ZeroTrust.TunnelListResponsesV4PagePaginationArray = TunnelListResponsesV4PagePaginationArray;
ZeroTrust.ConnectivitySettings = ConnectivitySettings;
ZeroTrust.DLP = DLP;
ZeroTrust.Gateway = Gateway;
ZeroTrust.Networks = Networks3;
ZeroTrust.RiskScoring = RiskScoring;
var ActivationCheck = class extends APIResource {
  static {
    __name(this, "ActivationCheck");
  }
  trigger(params, options) {
    const { zone_id } = params;
    return this._client.put(`/zones/${zone_id}/activation_check`, options)._thenUnwrap((obj) => obj.result);
  }
};
var CustomNameservers2 = class extends APIResource {
  static {
    __name(this, "CustomNameservers2");
  }
  update(params, options) {
    const { zone_id, ...body } = params;
    return this._client.getAPIList(`/zones/${zone_id}/custom_ns`, CustomNameserverUpdateResponsesSinglePage, {
      body,
      method: "put",
      ...options
    });
  }
  get(params, options) {
    const { zone_id } = params;
    return this._client.get(`/zones/${zone_id}/custom_ns`, options);
  }
};
var CustomNameserverUpdateResponsesSinglePage = class extends SinglePage {
  static {
    __name(this, "CustomNameserverUpdateResponsesSinglePage");
  }
};
CustomNameservers2.CustomNameserverUpdateResponsesSinglePage = CustomNameserverUpdateResponsesSinglePage;
var Holds = class extends APIResource {
  static {
    __name(this, "Holds");
  }
  create(params, options) {
    const { zone_id, include_subdomains } = params;
    return this._client.post(`/zones/${zone_id}/hold`, {
      query: { include_subdomains },
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  delete(params, options) {
    const { zone_id, hold_after } = params;
    return this._client.delete(`/zones/${zone_id}/hold`, {
      query: { hold_after },
      ...options
    })._thenUnwrap((obj) => obj.result);
  }
  edit(params, options) {
    const { zone_id, ...body } = params;
    return this._client.patch(`/zones/${zone_id}/hold`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  get(params, options) {
    const { zone_id } = params;
    return this._client.get(`/zones/${zone_id}/hold`, options)._thenUnwrap((obj) => obj.result);
  }
};
var Plans = class extends APIResource {
  static {
    __name(this, "Plans");
  }
  list(params, options) {
    const { zone_id } = params;
    return this._client.getAPIList(`/zones/${zone_id}/available_plans`, AvailableRatePlansSinglePage, options);
  }
  get(planIdentifier, params, options) {
    const { zone_id } = params;
    return this._client.get(`/zones/${zone_id}/available_plans/${planIdentifier}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var AvailableRatePlansSinglePage = class extends SinglePage {
  static {
    __name(this, "AvailableRatePlansSinglePage");
  }
};
Plans.AvailableRatePlansSinglePage = AvailableRatePlansSinglePage;
var RatePlans = class extends APIResource {
  static {
    __name(this, "RatePlans");
  }
  get(params, options) {
    const { zone_id } = params;
    return this._client.getAPIList(`/zones/${zone_id}/available_rate_plans`, RatePlanGetResponsesSinglePage, options);
  }
};
var RatePlanGetResponsesSinglePage = class extends SinglePage {
  static {
    __name(this, "RatePlanGetResponsesSinglePage");
  }
};
RatePlans.RatePlanGetResponsesSinglePage = RatePlanGetResponsesSinglePage;
var Settings16 = class extends APIResource {
  static {
    __name(this, "Settings16");
  }
  edit(settingId, params, options) {
    const { zone_id, ...body } = params;
    return this._client.patch(`/zones/${zone_id}/settings/${settingId}`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  get(settingId, params, options) {
    const { zone_id } = params;
    return this._client.get(`/zones/${zone_id}/settings/${settingId}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var Subscriptions4 = class extends APIResource {
  static {
    __name(this, "Subscriptions4");
  }
  create(params, options) {
    const { zone_id, ...body } = params;
    return this._client.post(`/zones/${zone_id}/subscription`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  update(params, options) {
    const { zone_id, ...body } = params;
    return this._client.put(`/zones/${zone_id}/subscription`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  get(params, options) {
    const { zone_id } = params;
    return this._client.get(`/zones/${zone_id}/subscription`, options)._thenUnwrap((obj) => obj.result);
  }
};
var Zones2 = class extends APIResource {
  static {
    __name(this, "Zones2");
  }
  constructor() {
    super(...arguments);
    this.activationCheck = new ActivationCheck(this._client);
    this.settings = new Settings16(this._client);
    this.customNameservers = new CustomNameservers2(this._client);
    this.holds = new Holds(this._client);
    this.subscriptions = new Subscriptions4(this._client);
    this.plans = new Plans(this._client);
    this.ratePlans = new RatePlans(this._client);
  }
  create(body, options) {
    return this._client.post("/zones", { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  list(query = {}, options) {
    if (isRequestOptions(query)) {
      return this.list({}, query);
    }
    return this._client.getAPIList("/zones", ZonesV4PagePaginationArray, { query, ...options });
  }
  delete(params, options) {
    const { zone_id } = params;
    return this._client.delete(`/zones/${zone_id}`, options)._thenUnwrap((obj) => obj.result);
  }
  edit(params, options) {
    const { zone_id, ...body } = params;
    return this._client.patch(`/zones/${zone_id}`, { body, ...options })._thenUnwrap((obj) => obj.result);
  }
  get(params, options) {
    const { zone_id } = params;
    return this._client.get(`/zones/${zone_id}`, options)._thenUnwrap((obj) => obj.result);
  }
};
var ZonesV4PagePaginationArray = class extends V4PagePaginationArray {
  static {
    __name(this, "ZonesV4PagePaginationArray");
  }
};
Zones2.ZonesV4PagePaginationArray = ZonesV4PagePaginationArray;
Zones2.ActivationCheck = ActivationCheck;
Zones2.Settings = Settings16;
Zones2.CustomNameservers = CustomNameservers2;
Zones2.CustomNameserverUpdateResponsesSinglePage = CustomNameserverUpdateResponsesSinglePage;
Zones2.Holds = Holds;
Zones2.Subscriptions = Subscriptions4;
Zones2.Plans = Plans;
Zones2.AvailableRatePlansSinglePage = AvailableRatePlansSinglePage;
Zones2.RatePlans = RatePlans;
Zones2.RatePlanGetResponsesSinglePage = RatePlanGetResponsesSinglePage;
var _Cloudflare_instances;
var _a;
var _Cloudflare_baseURLOverridden;
var Cloudflare = class extends APIClient {
  static {
    __name(this, "Cloudflare");
  }
  constructor({ baseURL = readEnv("CLOUDFLARE_BASE_URL"), apiVersion = null, apiToken = readEnv("CLOUDFLARE_API_TOKEN") ?? null, apiKey = readEnv("CLOUDFLARE_API_KEY") ?? null, apiEmail = readEnv("CLOUDFLARE_EMAIL") ?? null, userServiceKey = readEnv("CLOUDFLARE_API_USER_SERVICE_KEY") ?? null, ...opts } = {}) {
    const options = {
      apiToken,
      apiKey,
      apiEmail,
      userServiceKey,
      ...opts,
      baseURL: baseURL || `https://api.cloudflare.com/client/v4`,
      apiVersion: apiVersion || (/* @__PURE__ */ new Date()).toISOString().slice(0, 10)
    };
    super({
      baseURL: options.baseURL,
      apiVersion: options.apiVersion,
      baseURLOverridden: baseURL ? baseURL !== "https://api.cloudflare.com/client/v4" : false,
      timeout: options.timeout ?? 6e4,
      httpAgent: options.httpAgent,
      maxRetries: options.maxRetries,
      fetch: options.fetch
    });
    _Cloudflare_instances.add(this);
    this.accounts = new Accounts(this);
    this.originCACertificates = new OriginCACertificates(this);
    this.ips = new IPs2(this);
    this.memberships = new Memberships(this);
    this.user = new User(this);
    this.zones = new Zones2(this);
    this.loadBalancers = new LoadBalancers(this);
    this.cache = new Cache2(this);
    this.ssl = new SSL(this);
    this.acm = new ACM(this);
    this.argo = new Argo(this);
    this.certificateAuthorities = new CertificateAuthorities(this);
    this.clientCertificates = new ClientCertificates(this);
    this.customCertificates = new CustomCertificates(this);
    this.customHostnames = new CustomHostnames(this);
    this.customNameservers = new CustomNameservers(this);
    this.dnsFirewall = new DNSFirewall(this);
    this.dns = new DNS(this);
    this.emailSecurity = new EmailSecurity(this);
    this.emailRouting = new EmailRouting(this);
    this.filters = new Filters(this);
    this.firewall = new Firewall(this);
    this.healthchecks = new Healthchecks(this);
    this.keylessCertificates = new KeylessCertificates(this);
    this.logpush = new Logpush(this);
    this.logs = new Logs3(this);
    this.originTLSClientAuth = new OriginTLSClientAuth(this);
    this.pageRules = new PageRules(this);
    this.rateLimits = new RateLimits(this);
    this.waitingRooms = new WaitingRooms(this);
    this.web3 = new Web3(this);
    this.workers = new Workers2(this);
    this.kv = new KV(this);
    this.durableObjects = new DurableObjects(this);
    this.queues = new Queues(this);
    this.apiGateway = new APIGateway(this);
    this.managedTransforms = new ManagedTransforms(this);
    this.pageShield = new PageShield(this);
    this.rulesets = new Rulesets(this);
    this.urlNormalization = new URLNormalization(this);
    this.spectrum = new Spectrum(this);
    this.addressing = new Addressing(this);
    this.auditLogs = new AuditLogs(this);
    this.billing = new Billing(this);
    this.brandProtection = new BrandProtection(this);
    this.diagnostics = new Diagnostics(this);
    this.images = new Images(this);
    this.intel = new Intel(this);
    this.magicTransit = new MagicTransit(this);
    this.magicNetworkMonitoring = new MagicNetworkMonitoring(this);
    this.magicCloudNetworking = new MagicCloudNetworking(this);
    this.networkInterconnects = new NetworkInterconnects(this);
    this.mtlsCertificates = new MTLSCertificates(this);
    this.pages = new Pages(this);
    this.registrar = new Registrar(this);
    this.requestTracers = new RequestTracers(this);
    this.rules = new Rules7(this);
    this.stream = new Stream(this);
    this.alerting = new Alerting(this);
    this.d1 = new D1Resource(this);
    this.r2 = new R2(this);
    this.workersForPlatforms = new WorkersForPlatforms(this);
    this.zeroTrust = new ZeroTrust(this);
    this.turnstile = new Turnstile(this);
    this.hyperdrive = new HyperdriveResource(this);
    this.rum = new RUM(this);
    this.vectorize = new Vectorize(this);
    this.urlScanner = new URLScanner(this);
    this.radar = new Radar(this);
    this.botManagement = new BotManagement(this);
    this.originPostQuantumEncryption = new OriginPostQuantumEncryption(this);
    this.zaraz = new Zaraz(this);
    this.speed = new Speed2(this);
    this.dcvDelegation = new DCVDelegation(this);
    this.hostnames = new Hostnames(this);
    this.snippets = new Snippets(this);
    this.calls = new Calls(this);
    this.cloudforceOne = new CloudforceOne(this);
    this.aiGateway = new AIGateway(this);
    this.iam = new IAM(this);
    this.cloudConnector = new CloudConnector(this);
    this.botnetFeed = new BotnetFeed(this);
    this.securityTXT = new SecurityTXT(this);
    this.workflows = new Workflows(this);
    this.resourceSharing = new ResourceSharing(this);
    this.leakedCredentialChecks = new LeakedCredentialChecks(this);
    this.contentScanning = new ContentScanning(this);
    this.abuseReports = new AbuseReports(this);
    this.ai = new AI(this);
    this.securityCenter = new SecurityCenter(this);
    this.browserRendering = new BrowserRendering(this);
    this.customPages = new CustomPages(this);
    this.secretsStore = new SecretsStore(this);
    this.pipelines = new Pipelines(this);
    this.schemaValidation = new SchemaValidation3(this);
    this._options = options;
    this.apiToken = apiToken;
    this.apiKey = apiKey;
    this.apiEmail = apiEmail;
    this.userServiceKey = userServiceKey;
  }
  defaultQuery() {
    return this._options.defaultQuery;
  }
  defaultHeaders(opts) {
    return {
      ...super.defaultHeaders(opts),
      "X-Auth-Key": this.apiKey,
      "X-Auth-Email": this.apiEmail,
      ...this._options.defaultHeaders
    };
  }
  validateHeaders(headers, customHeaders, usingCustomFetch) {
    if (this.apiEmail && headers["x-auth-email"]) {
      return;
    }
    if (customHeaders["x-auth-email"] === null) {
      return;
    }
    if (this.apiKey && headers["x-auth-key"]) {
      return;
    }
    if (customHeaders["x-auth-key"] === null) {
      return;
    }
    if (this.apiToken && headers["authorization"]) {
      return;
    }
    if (customHeaders["authorization"] === null) {
      return;
    }
    if (this.userServiceKey && headers["x-auth-user-service-key"]) {
      return;
    }
    if (customHeaders["x-auth-user-service-key"] === null) {
      return;
    }
    if (usingCustomFetch) {
      return;
    }
    throw new Error('Could not resolve authentication method. Expected one of apiEmail, apiKey, apiToken or userServiceKey to be set. Or for one of the "X-Auth-Email", "X-Auth-Key", "Authorization" or "X-Auth-User-Service-Key" headers to be explicitly omitted');
  }
  authHeaders(opts) {
    const apiEmailAuth = this.apiEmailAuth(opts);
    const apiKeyAuth = this.apiKeyAuth(opts);
    const apiTokenAuth = this.apiTokenAuth(opts);
    const userServiceKeyAuth = this.userServiceKeyAuth(opts);
    if (apiEmailAuth != null && !isEmptyObj(apiEmailAuth) && apiKeyAuth != null && !isEmptyObj(apiKeyAuth)) {
      return { ...apiEmailAuth, ...apiKeyAuth };
    }
    if (apiTokenAuth != null && !isEmptyObj(apiTokenAuth)) {
      return apiTokenAuth;
    }
    if (userServiceKeyAuth != null && !isEmptyObj(userServiceKeyAuth)) {
      return userServiceKeyAuth;
    }
    return {};
  }
  apiEmailAuth(opts) {
    if (this.apiEmail == null) {
      return {};
    }
    return { "X-Auth-Email": this.apiEmail };
  }
  apiKeyAuth(opts) {
    if (this.apiKey == null) {
      return {};
    }
    return { "X-Auth-Key": this.apiKey };
  }
  apiTokenAuth(opts) {
    if (this.apiToken == null) {
      return {};
    }
    return { Authorization: `Bearer ${this.apiToken}` };
  }
  userServiceKeyAuth(opts) {
    if (this.userServiceKey == null) {
      return {};
    }
    return { "X-Auth-User-Service-Key": this.userServiceKey };
  }
  stringifyQuery(query) {
    return stringify11(query, { allowDots: true, arrayFormat: "repeat" });
  }
};
_a = Cloudflare, _Cloudflare_instances = /* @__PURE__ */ new WeakSet(), _Cloudflare_baseURLOverridden = /* @__PURE__ */ __name(function _Cloudflare_baseURLOverridden2() {
  return this.baseURL !== "https://api.cloudflare.com/client/v4";
}, "_Cloudflare_baseURLOverridden2");
Cloudflare.Cloudflare = _a;
Cloudflare.DEFAULT_TIMEOUT = 6e4;
Cloudflare.CloudflareError = CloudflareError;
Cloudflare.APIError = APIError;
Cloudflare.APIConnectionError = APIConnectionError;
Cloudflare.APIConnectionTimeoutError = APIConnectionTimeoutError;
Cloudflare.APIUserAbortError = APIUserAbortError;
Cloudflare.NotFoundError = NotFoundError;
Cloudflare.ConflictError = ConflictError;
Cloudflare.RateLimitError = RateLimitError;
Cloudflare.BadRequestError = BadRequestError;
Cloudflare.AuthenticationError = AuthenticationError;
Cloudflare.InternalServerError = InternalServerError;
Cloudflare.PermissionDeniedError = PermissionDeniedError;
Cloudflare.UnprocessableEntityError = UnprocessableEntityError;
Cloudflare.toFile = toFile;
Cloudflare.fileFromPath = fileFromPath;
Cloudflare.Accounts = Accounts;
Cloudflare.OriginCACertificates = OriginCACertificates;
Cloudflare.IPs = IPs2;
Cloudflare.Memberships = Memberships;
Cloudflare.User = User;
Cloudflare.Zones = Zones2;
Cloudflare.LoadBalancers = LoadBalancers;
Cloudflare.Cache = Cache2;
Cloudflare.SSL = SSL;
Cloudflare.ACM = ACM;
Cloudflare.Argo = Argo;
Cloudflare.CertificateAuthorities = CertificateAuthorities;
Cloudflare.ClientCertificates = ClientCertificates;
Cloudflare.CustomCertificates = CustomCertificates;
Cloudflare.CustomHostnames = CustomHostnames;
Cloudflare.CustomNameservers = CustomNameservers;
Cloudflare.DNSFirewall = DNSFirewall;
Cloudflare.DNS = DNS;
Cloudflare.EmailSecurity = EmailSecurity;
Cloudflare.EmailRouting = EmailRouting;
Cloudflare.Filters = Filters;
Cloudflare.Firewall = Firewall;
Cloudflare.Healthchecks = Healthchecks;
Cloudflare.KeylessCertificates = KeylessCertificates;
Cloudflare.Logpush = Logpush;
Cloudflare.Logs = Logs3;
Cloudflare.OriginTLSClientAuth = OriginTLSClientAuth;
Cloudflare.PageRules = PageRules;
Cloudflare.RateLimits = RateLimits;
Cloudflare.WaitingRooms = WaitingRooms;
Cloudflare.Web3 = Web3;
Cloudflare.Workers = Workers2;
Cloudflare.KV = KV;
Cloudflare.DurableObjects = DurableObjects;
Cloudflare.Queues = Queues;
Cloudflare.APIGateway = APIGateway;
Cloudflare.ManagedTransforms = ManagedTransforms;
Cloudflare.PageShield = PageShield;
Cloudflare.Rulesets = Rulesets;
Cloudflare.URLNormalization = URLNormalization;
Cloudflare.Spectrum = Spectrum;
Cloudflare.Addressing = Addressing;
Cloudflare.AuditLogs = AuditLogs;
Cloudflare.Billing = Billing;
Cloudflare.BrandProtection = BrandProtection;
Cloudflare.Diagnostics = Diagnostics;
Cloudflare.Images = Images;
Cloudflare.Intel = Intel;
Cloudflare.MagicTransit = MagicTransit;
Cloudflare.MagicNetworkMonitoring = MagicNetworkMonitoring;
Cloudflare.MagicCloudNetworking = MagicCloudNetworking;
Cloudflare.NetworkInterconnects = NetworkInterconnects;
Cloudflare.MTLSCertificates = MTLSCertificates;
Cloudflare.Pages = Pages;
Cloudflare.Registrar = Registrar;
Cloudflare.RequestTracers = RequestTracers;
Cloudflare.Rules = Rules7;
Cloudflare.Stream = Stream;
Cloudflare.Alerting = Alerting;
Cloudflare.D1Resource = D1Resource;
Cloudflare.R2 = R2;
Cloudflare.WorkersForPlatforms = WorkersForPlatforms;
Cloudflare.ZeroTrust = ZeroTrust;
Cloudflare.Turnstile = Turnstile;
Cloudflare.HyperdriveResource = HyperdriveResource;
Cloudflare.RUM = RUM;
Cloudflare.Vectorize = Vectorize;
Cloudflare.URLScanner = URLScanner;
Cloudflare.Radar = Radar;
Cloudflare.BotManagement = BotManagement;
Cloudflare.OriginPostQuantumEncryption = OriginPostQuantumEncryption;
Cloudflare.Zaraz = Zaraz;
Cloudflare.Speed = Speed2;
Cloudflare.DCVDelegation = DCVDelegation;
Cloudflare.Hostnames = Hostnames;
Cloudflare.Snippets = Snippets;
Cloudflare.Calls = Calls;
Cloudflare.CloudforceOne = CloudforceOne;
Cloudflare.AIGateway = AIGateway;
Cloudflare.IAM = IAM;
Cloudflare.CloudConnector = CloudConnector;
Cloudflare.BotnetFeed = BotnetFeed;
Cloudflare.SecurityTXT = SecurityTXT;
Cloudflare.Workflows = Workflows;
Cloudflare.ResourceSharing = ResourceSharing;
Cloudflare.LeakedCredentialChecks = LeakedCredentialChecks;
Cloudflare.ContentScanning = ContentScanning;
Cloudflare.AbuseReports = AbuseReports;
Cloudflare.AI = AI;
Cloudflare.SecurityCenter = SecurityCenter;
Cloudflare.BrowserRendering = BrowserRendering;
Cloudflare.CustomPages = CustomPages;
Cloudflare.SecretsStore = SecretsStore;
Cloudflare.Pipelines = Pipelines;
Cloudflare.SchemaValidation = SchemaValidation3;
var cloudflare_default = Cloudflare;
var CloudflareClientError = class extends Error {
  static {
    __name(this, "CloudflareClientError");
  }
  data;
  constructor(message2, cause, data) {
    super(message2, { cause });
    this.name = "CloudflareClientError";
    this.data = data;
  }
};
async function createCustomDomainForProject(env2, client) {
  const cf = client;
  const issuer_url = new URL(env2.CLOUDFLARE_AUTH_ENDPOINT_DOMAIN);
  const subdomainLabel = crypto.randomUUID().replaceAll("-", "").slice(0, 63);
  const dotCount = issuer_url.hostname.split(".").length - 1;
  const newDomaineName = dotCount > 1 ? `${subdomainLabel}-${issuer_url.hostname}` : `${subdomainLabel}.${issuer_url.hostname}`;
  try {
    const domaine = await cf.workers.domains.update({
      account_id: env2.CLOUDFLARE_ACCOUNT_ID,
      zone_id: env2.CLOUDFLARE_AUTH_DOMAIN_ZONE_ID,
      hostname: newDomaineName,
      service: env2.CLOUDFLARE_WORKER_SERVICE_NAME,
      environment: "production"
    });
    return domaine;
  } catch (error3) {
    throw new CloudflareClientError(`Cloudflare domain creation failed: ${newDomaineName}`, error3, {
      domaineName: newDomaineName,
      accountId: env2.CLOUDFLARE_ACCOUNT_ID,
      zoneId: env2.CLOUDFLARE_AUTH_DOMAIN_ZONE_ID,
      service: env2.CLOUDFLARE_WORKER_SERVICE_NAME
    });
  }
}
__name(createCustomDomainForProject, "createCustomDomainForProject");
async function deleteCustomDomainForProject(env2, client, domaineID) {
  return client.workers.domains.delete(domaineID, {
    account_id: env2.CLOUDFLARE_ACCOUNT_ID
  });
}
__name(deleteCustomDomainForProject, "deleteCustomDomainForProject");
function createClient(env2) {
  return new cloudflare_default({
    apiToken: env2.CLOUDFLARE_API_TOKEN
  });
}
__name(createClient, "createClient");

// api/projects/manage.js
async function GET3(params) {
  const ctx = getContext(arguments);
  const session = await ctx.data.client.getUserSession("private");
  if (session instanceof Error) {
    return {
      success: false,
      error: "Unauthorized"
    };
  }
  const db = drizzle(ctx.env.PROJECT_DB);
  const projects = await db.select().from(projectTable).where(and(eq(projectTable.clientID, params.clientID), ownerGroupConditions({
    user_group_ids: session?.private?.group_ids ?? [],
    ownerGroupIdColumn: projectTable.owner_group_id,
    otherEq: [eq(projectTable.owner_id, session.user_id)],
    self_host: ctx.env.SELF_HOSTED
  }))).limit(1);
  const project = projects.at(0);
  if (!project) {
    return {
      success: false,
      error: "Project not found"
    };
  }
  return {
    success: true,
    data: parseDBProject(project)
  };
}
__name(GET3, "GET");
async function PUT3(params) {
  const ctx = getContext(arguments);
  const { env: env2 } = ctx;
  const session = await ctx.data.client.getUserSession("private");
  if (session instanceof Error) {
    return {
      success: false,
      error: "Unauthorized"
    };
  }
  const db = drizzle(env2.PROJECT_DB);
  if (params.data.theme_id && !await db.select().from(uiStyleTable).where(and(eq(uiStyleTable.id, params.data.theme_id), ownerGroupConditions({
    user_group_ids: session?.private?.group_ids ?? [],
    ownerGroupIdColumn: uiStyleTable.owner_group_id,
    otherEq: [eq(uiStyleTable.owner_id, session.user_id)],
    self_host: env2.SELF_HOSTED
  }))).get().then((e) => Boolean(e)))
    return {
      success: false,
      error: "Invalid theme_id"
    };
  if (params.data.emailTemplateId && !await db.select().from(emailTemplatesTable).where(and(eq(emailTemplatesTable.id, params.data.emailTemplateId), ownerGroupConditions({
    user_group_ids: session?.private?.group_ids ?? [],
    ownerGroupIdColumn: emailTemplatesTable.owner_group_id,
    otherEq: [eq(emailTemplatesTable.owner_id, session.user_id)],
    self_host: env2.SELF_HOSTED
  }))).get().then((e) => Boolean(e)))
    return {
      success: false,
      error: "Invalid emailTemplateId"
    };
  try {
    const existing = (await db.select().from(projectTable).where(and(eq(projectTable.clientID, params.clientID), ownerGroupConditions({
      user_group_ids: session?.private?.group_ids ?? [],
      ownerGroupIdColumn: projectTable.owner_group_id,
      otherEq: [eq(projectTable.owner_id, session.user_id)],
      self_host: env2.SELF_HOSTED
    }))).limit(1)).at(0);
    if (!existing) {
      return {
        success: false,
        error: "Project not found"
      };
    }
    const updates = {};
    if (typeof params.data.active === "boolean") {
      updates.active = params.data.active;
    }
    if (params.data.providers_data !== void 0) {
      updates.providers_data = JSON.stringify(params.data.providers_data);
    }
    if (params.data.theme_id !== void 0) {
      updates.theme_id = params.data.theme_id;
    }
    if (params.data.emailTemplateId !== void 0) {
      updates.emailTemplateId = params.data.emailTemplateId;
    }
    if (params.data.projectData !== void 0) {
      updates.projectData = JSON.stringify(params.data.projectData);
    }
    if (params.data.codeMode !== void 0) {
      updates.codeMode = params.data.codeMode;
    }
    if (params.data.originURL !== void 0) {
      updates.originURL = params.data.originURL;
    }
    if (params.data.registerOnInvite !== void 0) {
      updates.registerOnInvite = params.data.registerOnInvite ? 1 : 0;
    }
    if (Object.keys(updates).length === 0)
      return {
        success: false,
        error: "No valid fields to update"
      };
    const updatedProjects = (await db.update(projectTable).set(updates).where(and(eq(projectTable.clientID, params.clientID), ownerGroupConditions({
      user_group_ids: session?.private?.group_ids ?? [],
      ownerGroupIdColumn: projectTable.owner_group_id,
      otherEq: [eq(projectTable.owner_id, session.user_id)],
      self_host: env2.SELF_HOSTED
    }))).returning()).at(0);
    if (!updatedProjects)
      return {
        success: false,
        error: "Project not found after update"
      };
    return {
      success: true,
      data: parseDBProject(updatedProjects)
    };
  } catch (error3) {
    await insertLog({
      type: "error",
      clientID: env2.PUBLIC_CLIENT_ID,
      message: error3 instanceof Error ? error3.message : String(error3),
      database: env2.PROJECT_DB,
      endpoint: "/api/projects/manage"
    });
    return {
      success: false,
      error: "Invalid request body"
    };
  }
}
__name(PUT3, "PUT");
async function DELETE8(params) {
  const ctx = getContext(arguments);
  const { env: env2 } = ctx;
  const session = await ctx.data.client.getUserSession("private");
  if (session instanceof Error) {
    return {
      success: false,
      error: "Unauthorized"
    };
  }
  const db = drizzle(env2.PROJECT_DB);
  const existing = await db.select().from(projectTable).where(or(and(eq(projectTable.clientID, params.clientID), ownerGroupConditions({
    user_group_ids: session?.private?.group_ids ?? [],
    ownerGroupIdColumn: projectTable.owner_group_id,
    otherEq: [eq(projectTable.owner_id, session.user_id)],
    self_host: env2.SELF_HOSTED
  })), and(eq(projectTable.clientID, params.clientID), or(...session.private?.group_ids?.map((id) => eq(projectTable.owner_group_id, id)) || [])))).limit(1).get();
  if (!existing)
    return {
      success: false,
      error: "Project not found"
    };
  await Promise.allSettled([
    db.delete(WebHookTable).where(eq(WebHookTable.clientID, params.clientID)),
    db.delete(totpTable).where(eq(totpTable.clientID, params.clientID)),
    db.delete(totpTokenTable).where(eq(totpTokenTable.clientID, params.clientID)),
    db.delete(webauthnCredentialsTable).where(eq(webauthnCredentialsTable.clientID, params.clientID)),
    db.delete(webauthnChallengesTable).where(eq(webauthnChallengesTable.clientID, params.clientID)),
    db.delete(webAuthnTokenAccessTable).where(eq(webAuthnTokenAccessTable.clientID, params.clientID)),
    db.delete(WebUiInviteLinkTable).where(eq(WebUiInviteLinkTable.clientID, params.clientID))
  ]);
  await db.delete(projectTable).where(eq(projectTable.clientID, params.clientID));
  const cfClient = createClient(env2);
  try {
    await deleteCustomDomainForProject(env2, cfClient, existing.cloudflareDomaineID);
  } catch (error3) {
    console.error(`Failed to delete CF domain ${existing.cloudflareDomaineID}:`, error3);
  }
  try {
    await DeleteOTFusersTable(params.clientID, env2.PROJECT_DB);
    return { success: true };
  } catch (error3) {
    return {
      success: false,
      error: "Failed to delete associated user table"
    };
  }
}
__name(DELETE8, "DELETE");
var DoubleIndexedKV11 = class {
  static {
    __name(this, "DoubleIndexedKV");
  }
  constructor() {
    this.keyToValue = /* @__PURE__ */ new Map();
    this.valueToKey = /* @__PURE__ */ new Map();
  }
  set(key, value) {
    this.keyToValue.set(key, value);
    this.valueToKey.set(value, key);
  }
  getByKey(key) {
    return this.keyToValue.get(key);
  }
  getByValue(value) {
    return this.valueToKey.get(value);
  }
  clear() {
    this.keyToValue.clear();
    this.valueToKey.clear();
  }
};
var Registry11 = class {
  static {
    __name(this, "Registry");
  }
  constructor(generateIdentifier) {
    this.generateIdentifier = generateIdentifier;
    this.kv = new DoubleIndexedKV11();
  }
  register(value, identifier) {
    if (this.kv.getByValue(value)) {
      return;
    }
    if (!identifier) {
      identifier = this.generateIdentifier(value);
    }
    this.kv.set(identifier, value);
  }
  clear() {
    this.kv.clear();
  }
  getIdentifier(value) {
    return this.kv.getByValue(value);
  }
  getValue(identifier) {
    return this.kv.getByKey(identifier);
  }
};
var ClassRegistry11 = class extends Registry11 {
  static {
    __name(this, "ClassRegistry");
  }
  constructor() {
    super((c) => c.name);
    this.classToAllowedProps = /* @__PURE__ */ new Map();
  }
  register(value, options) {
    if (typeof options === "object") {
      if (options.allowProps) {
        this.classToAllowedProps.set(value, options.allowProps);
      }
      super.register(value, options.identifier);
    } else {
      super.register(value, options);
    }
  }
  getAllowedProps(value) {
    return this.classToAllowedProps.get(value);
  }
};
function valuesOfObj11(record) {
  if ("values" in Object) {
    return Object.values(record);
  }
  const values = [];
  for (const key in record) {
    if (record.hasOwnProperty(key)) {
      values.push(record[key]);
    }
  }
  return values;
}
__name(valuesOfObj11, "valuesOfObj");
function find11(record, predicate) {
  const values = valuesOfObj11(record);
  if ("find" in values) {
    return values.find(predicate);
  }
  const valuesNotNever = values;
  for (let i = 0; i < valuesNotNever.length; i++) {
    const value = valuesNotNever[i];
    if (predicate(value)) {
      return value;
    }
  }
  return;
}
__name(find11, "find");
function forEach11(record, run) {
  Object.entries(record).forEach(([key, value]) => run(value, key));
}
__name(forEach11, "forEach");
function includes11(arr, value) {
  return arr.indexOf(value) !== -1;
}
__name(includes11, "includes");
function findArr11(record, predicate) {
  for (let i = 0; i < record.length; i++) {
    const value = record[i];
    if (predicate(value)) {
      return value;
    }
  }
  return;
}
__name(findArr11, "findArr");
var CustomTransformerRegistry11 = class {
  static {
    __name(this, "CustomTransformerRegistry");
  }
  constructor() {
    this.transfomers = {};
  }
  register(transformer) {
    this.transfomers[transformer.name] = transformer;
  }
  findApplicable(v) {
    return find11(this.transfomers, (transformer) => transformer.isApplicable(v));
  }
  findByName(name) {
    return this.transfomers[name];
  }
};
var getType12 = /* @__PURE__ */ __name((payload) => Object.prototype.toString.call(payload).slice(8, -1), "getType");
var isUndefined11 = /* @__PURE__ */ __name((payload) => typeof payload === "undefined", "isUndefined");
var isNull12 = /* @__PURE__ */ __name((payload) => payload === null, "isNull");
var isPlainObject12 = /* @__PURE__ */ __name((payload) => {
  if (typeof payload !== "object" || payload === null)
    return false;
  if (payload === Object.prototype)
    return false;
  if (Object.getPrototypeOf(payload) === null)
    return true;
  return Object.getPrototypeOf(payload) === Object.prototype;
}, "isPlainObject");
var isEmptyObject11 = /* @__PURE__ */ __name((payload) => isPlainObject12(payload) && Object.keys(payload).length === 0, "isEmptyObject");
var isArray12 = /* @__PURE__ */ __name((payload) => Array.isArray(payload), "isArray");
var isString11 = /* @__PURE__ */ __name((payload) => typeof payload === "string", "isString");
var isNumber11 = /* @__PURE__ */ __name((payload) => typeof payload === "number" && !isNaN(payload), "isNumber");
var isBoolean11 = /* @__PURE__ */ __name((payload) => typeof payload === "boolean", "isBoolean");
var isRegExp11 = /* @__PURE__ */ __name((payload) => payload instanceof RegExp, "isRegExp");
var isMap11 = /* @__PURE__ */ __name((payload) => payload instanceof Map, "isMap");
var isSet11 = /* @__PURE__ */ __name((payload) => payload instanceof Set, "isSet");
var isSymbol11 = /* @__PURE__ */ __name((payload) => getType12(payload) === "Symbol", "isSymbol");
var isDate11 = /* @__PURE__ */ __name((payload) => payload instanceof Date && !isNaN(payload.valueOf()), "isDate");
var isError11 = /* @__PURE__ */ __name((payload) => payload instanceof Error, "isError");
var isNaNValue11 = /* @__PURE__ */ __name((payload) => typeof payload === "number" && isNaN(payload), "isNaNValue");
var isPrimitive11 = /* @__PURE__ */ __name((payload) => isBoolean11(payload) || isNull12(payload) || isUndefined11(payload) || isNumber11(payload) || isString11(payload) || isSymbol11(payload), "isPrimitive");
var isBigint11 = /* @__PURE__ */ __name((payload) => typeof payload === "bigint", "isBigint");
var isInfinite11 = /* @__PURE__ */ __name((payload) => payload === Infinity || payload === -Infinity, "isInfinite");
var isTypedArray11 = /* @__PURE__ */ __name((payload) => ArrayBuffer.isView(payload) && !(payload instanceof DataView), "isTypedArray");
var isURL11 = /* @__PURE__ */ __name((payload) => payload instanceof URL, "isURL");
var escapeKey11 = /* @__PURE__ */ __name((key) => key.replace(/\\/g, "\\\\").replace(/\./g, "\\."), "escapeKey");
var stringifyPath11 = /* @__PURE__ */ __name((path) => path.map(String).map(escapeKey11).join("."), "stringifyPath");
var parsePath11 = /* @__PURE__ */ __name((string2, legacyPaths) => {
  const result = [];
  let segment = "";
  for (let i = 0; i < string2.length; i++) {
    let char = string2.charAt(i);
    if (!legacyPaths && char === "\\") {
      const escaped = string2.charAt(i + 1);
      if (escaped === "\\") {
        segment += "\\";
        i++;
        continue;
      } else if (escaped !== ".") {
        throw Error("invalid path");
      }
    }
    const isEscapedDot = char === "\\" && string2.charAt(i + 1) === ".";
    if (isEscapedDot) {
      segment += ".";
      i++;
      continue;
    }
    const isEndOfSegment = char === ".";
    if (isEndOfSegment) {
      result.push(segment);
      segment = "";
      continue;
    }
    segment += char;
  }
  const lastSegment = segment;
  result.push(lastSegment);
  return result;
}, "parsePath");
function simpleTransformation11(isApplicable, annotation, transform, untransform) {
  return {
    isApplicable,
    annotation,
    transform,
    untransform
  };
}
__name(simpleTransformation11, "simpleTransformation");
var simpleRules11 = [
  simpleTransformation11(isUndefined11, "undefined", () => null, () => {
    return;
  }),
  simpleTransformation11(isBigint11, "bigint", (v) => v.toString(), (v) => {
    if (typeof BigInt !== "undefined") {
      return BigInt(v);
    }
    console.error("Please add a BigInt polyfill.");
    return v;
  }),
  simpleTransformation11(isDate11, "Date", (v) => v.toISOString(), (v) => new Date(v)),
  simpleTransformation11(isError11, "Error", (v, superJson) => {
    const baseError = {
      name: v.name,
      message: v.message
    };
    if ("cause" in v) {
      baseError.cause = v.cause;
    }
    superJson.allowedErrorProps.forEach((prop) => {
      baseError[prop] = v[prop];
    });
    return baseError;
  }, (v, superJson) => {
    const e = new Error(v.message, { cause: v.cause });
    e.name = v.name;
    e.stack = v.stack;
    superJson.allowedErrorProps.forEach((prop) => {
      e[prop] = v[prop];
    });
    return e;
  }),
  simpleTransformation11(isRegExp11, "regexp", (v) => "" + v, (regex) => {
    const body = regex.slice(1, regex.lastIndexOf("/"));
    const flags2 = regex.slice(regex.lastIndexOf("/") + 1);
    return new RegExp(body, flags2);
  }),
  simpleTransformation11(isSet11, "set", (v) => [...v.values()], (v) => new Set(v)),
  simpleTransformation11(isMap11, "map", (v) => [...v.entries()], (v) => new Map(v)),
  simpleTransformation11((v) => isNaNValue11(v) || isInfinite11(v), "number", (v) => {
    if (isNaNValue11(v)) {
      return "NaN";
    }
    if (v > 0) {
      return "Infinity";
    } else {
      return "-Infinity";
    }
  }, Number),
  simpleTransformation11((v) => v === 0 && 1 / v === -Infinity, "number", () => {
    return "-0";
  }, Number),
  simpleTransformation11(isURL11, "URL", (v) => v.toString(), (v) => new URL(v))
];
function compositeTransformation11(isApplicable, annotation, transform, untransform) {
  return {
    isApplicable,
    annotation,
    transform,
    untransform
  };
}
__name(compositeTransformation11, "compositeTransformation");
var symbolRule11 = compositeTransformation11((s, superJson) => {
  if (isSymbol11(s)) {
    const isRegistered = !!superJson.symbolRegistry.getIdentifier(s);
    return isRegistered;
  }
  return false;
}, (s, superJson) => {
  const identifier = superJson.symbolRegistry.getIdentifier(s);
  return ["symbol", identifier];
}, (v) => v.description, (_, a, superJson) => {
  const value = superJson.symbolRegistry.getValue(a[1]);
  if (!value) {
    throw new Error("Trying to deserialize unknown symbol");
  }
  return value;
});
var constructorToName11 = [
  Int8Array,
  Uint8Array,
  Int16Array,
  Uint16Array,
  Int32Array,
  Uint32Array,
  Float32Array,
  Float64Array,
  Uint8ClampedArray
].reduce((obj, ctor) => {
  obj[ctor.name] = ctor;
  return obj;
}, {});
var typedArrayRule11 = compositeTransformation11(isTypedArray11, (v) => ["typed-array", v.constructor.name], (v) => [...v], (v, a) => {
  const ctor = constructorToName11[a[1]];
  if (!ctor) {
    throw new Error("Trying to deserialize unknown typed array");
  }
  return new ctor(v);
});
function isInstanceOfRegisteredClass11(potentialClass, superJson) {
  if (potentialClass?.constructor) {
    const isRegistered = !!superJson.classRegistry.getIdentifier(potentialClass.constructor);
    return isRegistered;
  }
  return false;
}
__name(isInstanceOfRegisteredClass11, "isInstanceOfRegisteredClass");
var classRule11 = compositeTransformation11(isInstanceOfRegisteredClass11, (clazz, superJson) => {
  const identifier = superJson.classRegistry.getIdentifier(clazz.constructor);
  return ["class", identifier];
}, (clazz, superJson) => {
  const allowedProps = superJson.classRegistry.getAllowedProps(clazz.constructor);
  if (!allowedProps) {
    return { ...clazz };
  }
  const result = {};
  allowedProps.forEach((prop) => {
    result[prop] = clazz[prop];
  });
  return result;
}, (v, a, superJson) => {
  const clazz = superJson.classRegistry.getValue(a[1]);
  if (!clazz) {
    throw new Error(`Trying to deserialize unknown class '${a[1]}' - check https://github.com/blitz-js/superjson/issues/116#issuecomment-773996564`);
  }
  return Object.assign(Object.create(clazz.prototype), v);
});
var customRule11 = compositeTransformation11((value, superJson) => {
  return !!superJson.customTransformerRegistry.findApplicable(value);
}, (value, superJson) => {
  const transformer = superJson.customTransformerRegistry.findApplicable(value);
  return ["custom", transformer.name];
}, (value, superJson) => {
  const transformer = superJson.customTransformerRegistry.findApplicable(value);
  return transformer.serialize(value);
}, (v, a, superJson) => {
  const transformer = superJson.customTransformerRegistry.findByName(a[1]);
  if (!transformer) {
    throw new Error("Trying to deserialize unknown custom value");
  }
  return transformer.deserialize(v);
});
var compositeRules11 = [classRule11, symbolRule11, customRule11, typedArrayRule11];
var transformValue11 = /* @__PURE__ */ __name((value, superJson) => {
  const applicableCompositeRule = findArr11(compositeRules11, (rule) => rule.isApplicable(value, superJson));
  if (applicableCompositeRule) {
    return {
      value: applicableCompositeRule.transform(value, superJson),
      type: applicableCompositeRule.annotation(value, superJson)
    };
  }
  const applicableSimpleRule = findArr11(simpleRules11, (rule) => rule.isApplicable(value, superJson));
  if (applicableSimpleRule) {
    return {
      value: applicableSimpleRule.transform(value, superJson),
      type: applicableSimpleRule.annotation
    };
  }
  return;
}, "transformValue");
var simpleRulesByAnnotation11 = {};
simpleRules11.forEach((rule) => {
  simpleRulesByAnnotation11[rule.annotation] = rule;
});
var untransformValue11 = /* @__PURE__ */ __name((json, type, superJson) => {
  if (isArray12(type)) {
    switch (type[0]) {
      case "symbol":
        return symbolRule11.untransform(json, type, superJson);
      case "class":
        return classRule11.untransform(json, type, superJson);
      case "custom":
        return customRule11.untransform(json, type, superJson);
      case "typed-array":
        return typedArrayRule11.untransform(json, type, superJson);
      default:
        throw new Error("Unknown transformation: " + type);
    }
  } else {
    const transformation = simpleRulesByAnnotation11[type];
    if (!transformation) {
      throw new Error("Unknown transformation: " + type);
    }
    return transformation.untransform(json, superJson);
  }
}, "untransformValue");
var getNthKey11 = /* @__PURE__ */ __name((value, n) => {
  if (n > value.size)
    throw new Error("index out of bounds");
  const keys = value.keys();
  while (n > 0) {
    keys.next();
    n--;
  }
  return keys.next().value;
}, "getNthKey");
function validatePath11(path) {
  if (includes11(path, "__proto__")) {
    throw new Error("__proto__ is not allowed as a property");
  }
  if (includes11(path, "prototype")) {
    throw new Error("prototype is not allowed as a property");
  }
  if (includes11(path, "constructor")) {
    throw new Error("constructor is not allowed as a property");
  }
}
__name(validatePath11, "validatePath");
var getDeep11 = /* @__PURE__ */ __name((object2, path) => {
  validatePath11(path);
  for (let i = 0; i < path.length; i++) {
    const key = path[i];
    if (isSet11(object2)) {
      object2 = getNthKey11(object2, +key);
    } else if (isMap11(object2)) {
      const row = +key;
      const type = +path[++i] === 0 ? "key" : "value";
      const keyOfRow = getNthKey11(object2, row);
      switch (type) {
        case "key":
          object2 = keyOfRow;
          break;
        case "value":
          object2 = object2.get(keyOfRow);
          break;
      }
    } else {
      object2 = object2[key];
    }
  }
  return object2;
}, "getDeep");
var setDeep11 = /* @__PURE__ */ __name((object2, path, mapper) => {
  validatePath11(path);
  if (path.length === 0) {
    return mapper(object2);
  }
  let parent = object2;
  for (let i = 0; i < path.length - 1; i++) {
    const key = path[i];
    if (isArray12(parent)) {
      const index = +key;
      parent = parent[index];
    } else if (isPlainObject12(parent)) {
      parent = parent[key];
    } else if (isSet11(parent)) {
      const row = +key;
      parent = getNthKey11(parent, row);
    } else if (isMap11(parent)) {
      const isEnd = i === path.length - 2;
      if (isEnd) {
        break;
      }
      const row = +key;
      const type = +path[++i] === 0 ? "key" : "value";
      const keyOfRow = getNthKey11(parent, row);
      switch (type) {
        case "key":
          parent = keyOfRow;
          break;
        case "value":
          parent = parent.get(keyOfRow);
          break;
      }
    }
  }
  const lastKey = path[path.length - 1];
  if (isArray12(parent)) {
    parent[+lastKey] = mapper(parent[+lastKey]);
  } else if (isPlainObject12(parent)) {
    parent[lastKey] = mapper(parent[lastKey]);
  }
  if (isSet11(parent)) {
    const oldValue = getNthKey11(parent, +lastKey);
    const newValue = mapper(oldValue);
    if (oldValue !== newValue) {
      parent.delete(oldValue);
      parent.add(newValue);
    }
  }
  if (isMap11(parent)) {
    const row = +path[path.length - 2];
    const keyToRow = getNthKey11(parent, row);
    const type = +lastKey === 0 ? "key" : "value";
    switch (type) {
      case "key": {
        const newKey = mapper(keyToRow);
        parent.set(newKey, parent.get(keyToRow));
        if (newKey !== keyToRow) {
          parent.delete(keyToRow);
        }
        break;
      }
      case "value": {
        parent.set(keyToRow, mapper(parent.get(keyToRow)));
        break;
      }
    }
  }
  return object2;
}, "setDeep");
var enableLegacyPaths11 = /* @__PURE__ */ __name((version4) => version4 < 1, "enableLegacyPaths");
function traverse11(tree, walker26, version4, origin = []) {
  if (!tree) {
    return;
  }
  const legacyPaths = enableLegacyPaths11(version4);
  if (!isArray12(tree)) {
    forEach11(tree, (subtree, key) => traverse11(subtree, walker26, version4, [
      ...origin,
      ...parsePath11(key, legacyPaths)
    ]));
    return;
  }
  const [nodeValue, children] = tree;
  if (children) {
    forEach11(children, (child, key) => {
      traverse11(child, walker26, version4, [
        ...origin,
        ...parsePath11(key, legacyPaths)
      ]);
    });
  }
  walker26(nodeValue, origin);
}
__name(traverse11, "traverse");
function applyValueAnnotations11(plain, annotations, version4, superJson) {
  traverse11(annotations, (type, path) => {
    plain = setDeep11(plain, path, (v) => untransformValue11(v, type, superJson));
  }, version4);
  return plain;
}
__name(applyValueAnnotations11, "applyValueAnnotations");
function applyReferentialEqualityAnnotations11(plain, annotations, version4) {
  const legacyPaths = enableLegacyPaths11(version4);
  function apply(identicalPaths, path) {
    const object2 = getDeep11(plain, parsePath11(path, legacyPaths));
    identicalPaths.map((path2) => parsePath11(path2, legacyPaths)).forEach((identicalObjectPath) => {
      plain = setDeep11(plain, identicalObjectPath, () => object2);
    });
  }
  __name(apply, "apply");
  if (isArray12(annotations)) {
    const [root, other] = annotations;
    root.forEach((identicalPath) => {
      plain = setDeep11(plain, parsePath11(identicalPath, legacyPaths), () => plain);
    });
    if (other) {
      forEach11(other, apply);
    }
  } else {
    forEach11(annotations, apply);
  }
  return plain;
}
__name(applyReferentialEqualityAnnotations11, "applyReferentialEqualityAnnotations");
var isDeep11 = /* @__PURE__ */ __name((object2, superJson) => isPlainObject12(object2) || isArray12(object2) || isMap11(object2) || isSet11(object2) || isError11(object2) || isInstanceOfRegisteredClass11(object2, superJson), "isDeep");
function addIdentity11(object2, path, identities) {
  const existingSet = identities.get(object2);
  if (existingSet) {
    existingSet.push(path);
  } else {
    identities.set(object2, [path]);
  }
}
__name(addIdentity11, "addIdentity");
function generateReferentialEqualityAnnotations11(identitites, dedupe) {
  const result = {};
  let rootEqualityPaths = void 0;
  identitites.forEach((paths) => {
    if (paths.length <= 1) {
      return;
    }
    if (!dedupe) {
      paths = paths.map((path) => path.map(String)).sort((a, b) => a.length - b.length);
    }
    const [representativePath, ...identicalPaths] = paths;
    if (representativePath.length === 0) {
      rootEqualityPaths = identicalPaths.map(stringifyPath11);
    } else {
      result[stringifyPath11(representativePath)] = identicalPaths.map(stringifyPath11);
    }
  });
  if (rootEqualityPaths) {
    if (isEmptyObject11(result)) {
      return [rootEqualityPaths];
    } else {
      return [rootEqualityPaths, result];
    }
  } else {
    return isEmptyObject11(result) ? void 0 : result;
  }
}
__name(generateReferentialEqualityAnnotations11, "generateReferentialEqualityAnnotations");
var walker11 = /* @__PURE__ */ __name((object2, identities, superJson, dedupe, path = [], objectsInThisPath = [], seenObjects = /* @__PURE__ */ new Map()) => {
  const primitive = isPrimitive11(object2);
  if (!primitive) {
    addIdentity11(object2, path, identities);
    const seen = seenObjects.get(object2);
    if (seen) {
      return dedupe ? {
        transformedValue: null
      } : seen;
    }
  }
  if (!isDeep11(object2, superJson)) {
    const transformed2 = transformValue11(object2, superJson);
    const result2 = transformed2 ? {
      transformedValue: transformed2.value,
      annotations: [transformed2.type]
    } : {
      transformedValue: object2
    };
    if (!primitive) {
      seenObjects.set(object2, result2);
    }
    return result2;
  }
  if (includes11(objectsInThisPath, object2)) {
    return {
      transformedValue: null
    };
  }
  const transformationResult = transformValue11(object2, superJson);
  const transformed = transformationResult?.value ?? object2;
  const transformedValue = isArray12(transformed) ? [] : {};
  const innerAnnotations = {};
  forEach11(transformed, (value, index) => {
    if (index === "__proto__" || index === "constructor" || index === "prototype") {
      throw new Error(`Detected property ${index}. This is a prototype pollution risk, please remove it from your object.`);
    }
    const recursiveResult = walker11(value, identities, superJson, dedupe, [...path, index], [...objectsInThisPath, object2], seenObjects);
    transformedValue[index] = recursiveResult.transformedValue;
    if (isArray12(recursiveResult.annotations)) {
      innerAnnotations[escapeKey11(index)] = recursiveResult.annotations;
    } else if (isPlainObject12(recursiveResult.annotations)) {
      forEach11(recursiveResult.annotations, (tree, key) => {
        innerAnnotations[escapeKey11(index) + "." + key] = tree;
      });
    }
  });
  const result = isEmptyObject11(innerAnnotations) ? {
    transformedValue,
    annotations: transformationResult ? [transformationResult.type] : void 0
  } : {
    transformedValue,
    annotations: transformationResult ? [transformationResult.type, innerAnnotations] : innerAnnotations
  };
  if (!primitive) {
    seenObjects.set(object2, result);
  }
  return result;
}, "walker");
function getType211(payload) {
  return Object.prototype.toString.call(payload).slice(8, -1);
}
__name(getType211, "getType2");
function isArray211(payload) {
  return getType211(payload) === "Array";
}
__name(isArray211, "isArray2");
function isPlainObject211(payload) {
  if (getType211(payload) !== "Object")
    return false;
  const prototype = Object.getPrototypeOf(payload);
  return !!prototype && prototype.constructor === Object && prototype === Object.prototype;
}
__name(isPlainObject211, "isPlainObject2");
function assignProp11(carry, key, newVal, originalObject, includeNonenumerable) {
  const propType = {}.propertyIsEnumerable.call(originalObject, key) ? "enumerable" : "nonenumerable";
  if (propType === "enumerable")
    carry[key] = newVal;
  if (includeNonenumerable && propType === "nonenumerable") {
    Object.defineProperty(carry, key, {
      value: newVal,
      enumerable: false,
      writable: true,
      configurable: true
    });
  }
}
__name(assignProp11, "assignProp");
function copy11(target, options = {}) {
  if (isArray211(target)) {
    return target.map((item) => copy11(item, options));
  }
  if (!isPlainObject211(target)) {
    return target;
  }
  const props = Object.getOwnPropertyNames(target);
  const symbols = Object.getOwnPropertySymbols(target);
  return [...props, ...symbols].reduce((carry, key) => {
    if (key === "__proto__")
      return carry;
    if (isArray211(options.props) && !options.props.includes(key)) {
      return carry;
    }
    const val = target[key];
    const newVal = copy11(val, options);
    assignProp11(carry, key, newVal, target, options.nonenumerable);
    return carry;
  }, {});
}
__name(copy11, "copy");
var SuperJSON11 = class {
  static {
    __name(this, "SuperJSON");
  }
  constructor({ dedupe = false } = {}) {
    this.classRegistry = new ClassRegistry11();
    this.symbolRegistry = new Registry11((s) => s.description ?? "");
    this.customTransformerRegistry = new CustomTransformerRegistry11();
    this.allowedErrorProps = [];
    this.dedupe = dedupe;
  }
  serialize(object2) {
    const identities = /* @__PURE__ */ new Map();
    const output = walker11(object2, identities, this, this.dedupe);
    const res = {
      json: output.transformedValue
    };
    if (output.annotations) {
      res.meta = {
        ...res.meta,
        values: output.annotations
      };
    }
    const equalityAnnotations = generateReferentialEqualityAnnotations11(identities, this.dedupe);
    if (equalityAnnotations) {
      res.meta = {
        ...res.meta,
        referentialEqualities: equalityAnnotations
      };
    }
    if (res.meta)
      res.meta.v = 1;
    return res;
  }
  deserialize(payload, options) {
    const { json, meta } = payload;
    let result = options?.inPlace ? json : copy11(json);
    if (meta?.values) {
      result = applyValueAnnotations11(result, meta.values, meta.v ?? 0, this);
    }
    if (meta?.referentialEqualities) {
      result = applyReferentialEqualityAnnotations11(result, meta.referentialEqualities, meta.v ?? 0);
    }
    return result;
  }
  stringify(object2) {
    return JSON.stringify(this.serialize(object2));
  }
  parse(string2) {
    return this.deserialize(JSON.parse(string2), { inPlace: true });
  }
  registerClass(v, options) {
    this.classRegistry.register(v, options);
  }
  registerSymbol(v, identifier) {
    this.symbolRegistry.register(v, identifier);
  }
  registerCustom(transformer, name) {
    this.customTransformerRegistry.register({
      name,
      ...transformer
    });
  }
  allowErrorProps(...props) {
    this.allowedErrorProps.push(...props);
  }
};
SuperJSON11.defaultInstance = new SuperJSON11();
SuperJSON11.serialize = SuperJSON11.defaultInstance.serialize.bind(SuperJSON11.defaultInstance);
SuperJSON11.deserialize = SuperJSON11.defaultInstance.deserialize.bind(SuperJSON11.defaultInstance);
SuperJSON11.stringify = SuperJSON11.defaultInstance.stringify.bind(SuperJSON11.defaultInstance);
SuperJSON11.parse = SuperJSON11.defaultInstance.parse.bind(SuperJSON11.defaultInstance);
SuperJSON11.registerClass = SuperJSON11.defaultInstance.registerClass.bind(SuperJSON11.defaultInstance);
SuperJSON11.registerSymbol = SuperJSON11.defaultInstance.registerSymbol.bind(SuperJSON11.defaultInstance);
SuperJSON11.registerCustom = SuperJSON11.defaultInstance.registerCustom.bind(SuperJSON11.defaultInstance);
SuperJSON11.allowErrorProps = SuperJSON11.defaultInstance.allowErrorProps.bind(SuperJSON11.defaultInstance);
var dist_default11 = SuperJSON11;
var serialize11 = SuperJSON11.serialize;
var deserialize11 = SuperJSON11.deserialize;
var stringify12 = SuperJSON11.stringify;
var parse11 = SuperJSON11.parse;
var registerClass11 = SuperJSON11.registerClass;
var registerCustom11 = SuperJSON11.registerCustom;
var registerSymbol11 = SuperJSON11.registerSymbol;
var allowErrorProps11 = SuperJSON11.allowErrorProps;
function parseData11(formData) {
  const propsArray = [];
  if (!formData)
    return propsArray;
  const batchsIDs = [];
  for (const [key, value] of Array.from(formData.entries())) {
    if (key.startsWith("FILE_"))
      propsArray.push(value);
    else if (key.startsWith("FILES_")) {
      if (batchsIDs.includes(key))
        continue;
      batchsIDs.push(key);
      propsArray.push(formData.getAll(key));
    } else {
      propsArray.push(JSON.parse(decodeURI(value)));
    }
  }
  return propsArray;
}
__name(parseData11, "parseData");
function paramsFromURL11(url) {
  const params = url.searchParams.entries().toArray().map(([_, v]) => v);
  return params.map((param) => JSON.parse(decodeURIComponent(param)));
}
__name(paramsFromURL11, "paramsFromURL");
async function WrapRequestHandler11(context2, endpoint) {
  const isServerAction = context2.request.headers.get("x-server-action") === "true";
  if (!isServerAction) {
    return new Response("Not Found", { status: 404 });
  }
  const parsedData = context2.request.method === "GET" || context2.request.method === "HEAD" ? paramsFromURL11(new URL(context2.request.url)) : parseData11(context2.request.headers.get("content-type") ? await context2.request.formData() : void 0);
  const missingProps = endpoint.length - parsedData.length;
  for (let i = 0; i < missingProps; i++) {
    parsedData.push(void 0);
  }
  parsedData.push(context2);
  const result = await endpoint(...parsedData);
  switch (typeof result) {
    case "string":
    case "number":
    case "boolean":
    case "bigint":
      return new Response(dist_default11.stringify(result), {
        headers: {
          "Content-Type": "application/json",
          dataType: "json"
        }
      });
    case "undefined":
      return new Response(null, { status: 204 });
    case "object":
      if (result instanceof Response) {
        result.headers.set("dataType", "response");
        return result;
      } else if (result instanceof Blob) {
        const res = new Response(await result.arrayBuffer());
        res.headers.set("dataType", "blob");
        res.headers.set("Content-Type", result.type);
        return res;
      } else if (result instanceof File) {
        return new Response(await result.arrayBuffer(), {
          headers: {
            "Content-Type": result.type,
            dataType: "file",
            fileData: JSON.stringify({
              name: result.name,
              lastModified: result.lastModified
            })
          }
        });
      } else {
        return new Response(dist_default11.stringify(result), {
          headers: { "Content-Type": "application/json", dataType: "json" }
        });
      }
    default:
      throw new Error(`Unsupported return type from action: ${typeof result}`);
  }
}
__name(WrapRequestHandler11, "WrapRequestHandler");
var onRequest11 = /* @__PURE__ */ __name(async (context2) => {
  const method = context2.request.method;
  const options = {
    GET: typeof GET3 === "function" ? GET3 : void 0,
    POST: typeof POST === "function" ? POST : void 0,
    PUT: typeof PUT3 === "function" ? PUT3 : void 0,
    DELETE: typeof DELETE8 === "function" ? DELETE8 : void 0,
    PATCH: typeof PATCH === "function" ? PATCH : void 0,
    HEAD: typeof HEAD === "function" ? HEAD : void 0,
    OPTIONS: typeof OPTIONS === "function" ? OPTIONS : void 0
  };
  if (!options[method]) {
    return new Response(`Method "${method}" Not Allowed`, {
      status: 405
    });
  }
  return await WrapRequestHandler11(context2, options[method]);
}, "onRequest");

// api/templates/copy/index.js
async function GET4() {
  const ctx = getContext(arguments);
  const { env: env2 } = ctx;
  const session = await ctx.data.client.getUserSession("private");
  if (session instanceof Error) {
    return {
      success: false,
      error: "Unauthorized"
    };
  }
  const db = drizzle(env2.PROJECT_DB);
  const templates = await db.select().from(WebUiCopyTemplateTable).where(ownerGroupConditions({
    user_group_ids: session?.private?.group_ids ?? [],
    ownerGroupIdColumn: WebUiCopyTemplateTable.owner_group_id,
    otherEq: [eq(WebUiCopyTemplateTable.owner_id, session.user_id)],
    self_host: env2.SELF_HOSTED
  }));
  return {
    success: true,
    data: templates.map((t) => parseDBCopyTemplate(t))
  };
}
__name(GET4, "GET");
async function POST10(params) {
  const ctx = getContext(arguments);
  const { env: env2 } = ctx;
  try {
    const { name, copyData } = params;
    if (!name || typeof name !== "string" || name.trim().length === 0) {
      return {
        success: false,
        error: "Invalid or missing template name"
      };
    }
    if (!copyData || typeof copyData !== "object") {
      return {
        success: false,
        error: "Invalid or missing copy data"
      };
    }
    const db = drizzle(env2.PROJECT_DB);
    const now = (/* @__PURE__ */ new Date()).toISOString();
    const userData = await ctx.data.client.getMetaData();
    if (!userData || !userData.id) {
      insertLog({
        type: "error",
        message: "Unauthorized attempt to create copy template",
        database: env2.PROJECT_DB,
        clientID: PUBLIC_CLIENT_ID,
        context: {
          action: "create_copy_template",
          userData
        }
      });
      return {
        success: false,
        error: "Unauthorized"
      };
    }
    const newTemplateEntry = (await db.insert(WebUiCopyTemplateTable).values({
      name: name.trim(),
      owner_id: userData.id,
      owner_group_id: crypto.randomUUID(),
      copyData,
      created_at: now,
      updated_at: now
    }).returning()).at(0);
    if (!newTemplateEntry) {
      return {
        success: false,
        error: "Failed to create copy template"
      };
    }
    return {
      success: true,
      data: parseDBCopyTemplate(newTemplateEntry)
    };
  } catch (err) {
    return {
      success: false,
      error: err instanceof Error ? err.message : "Failed to create copy template"
    };
  }
}
__name(POST10, "POST");
var DoubleIndexedKV12 = class {
  static {
    __name(this, "DoubleIndexedKV");
  }
  constructor() {
    this.keyToValue = /* @__PURE__ */ new Map();
    this.valueToKey = /* @__PURE__ */ new Map();
  }
  set(key, value) {
    this.keyToValue.set(key, value);
    this.valueToKey.set(value, key);
  }
  getByKey(key) {
    return this.keyToValue.get(key);
  }
  getByValue(value) {
    return this.valueToKey.get(value);
  }
  clear() {
    this.keyToValue.clear();
    this.valueToKey.clear();
  }
};
var Registry12 = class {
  static {
    __name(this, "Registry");
  }
  constructor(generateIdentifier) {
    this.generateIdentifier = generateIdentifier;
    this.kv = new DoubleIndexedKV12();
  }
  register(value, identifier) {
    if (this.kv.getByValue(value)) {
      return;
    }
    if (!identifier) {
      identifier = this.generateIdentifier(value);
    }
    this.kv.set(identifier, value);
  }
  clear() {
    this.kv.clear();
  }
  getIdentifier(value) {
    return this.kv.getByValue(value);
  }
  getValue(identifier) {
    return this.kv.getByKey(identifier);
  }
};
var ClassRegistry12 = class extends Registry12 {
  static {
    __name(this, "ClassRegistry");
  }
  constructor() {
    super((c) => c.name);
    this.classToAllowedProps = /* @__PURE__ */ new Map();
  }
  register(value, options) {
    if (typeof options === "object") {
      if (options.allowProps) {
        this.classToAllowedProps.set(value, options.allowProps);
      }
      super.register(value, options.identifier);
    } else {
      super.register(value, options);
    }
  }
  getAllowedProps(value) {
    return this.classToAllowedProps.get(value);
  }
};
function valuesOfObj12(record) {
  if ("values" in Object) {
    return Object.values(record);
  }
  const values = [];
  for (const key in record) {
    if (record.hasOwnProperty(key)) {
      values.push(record[key]);
    }
  }
  return values;
}
__name(valuesOfObj12, "valuesOfObj");
function find12(record, predicate) {
  const values = valuesOfObj12(record);
  if ("find" in values) {
    return values.find(predicate);
  }
  const valuesNotNever = values;
  for (let i = 0; i < valuesNotNever.length; i++) {
    const value = valuesNotNever[i];
    if (predicate(value)) {
      return value;
    }
  }
  return;
}
__name(find12, "find");
function forEach12(record, run) {
  Object.entries(record).forEach(([key, value]) => run(value, key));
}
__name(forEach12, "forEach");
function includes12(arr, value) {
  return arr.indexOf(value) !== -1;
}
__name(includes12, "includes");
function findArr12(record, predicate) {
  for (let i = 0; i < record.length; i++) {
    const value = record[i];
    if (predicate(value)) {
      return value;
    }
  }
  return;
}
__name(findArr12, "findArr");
var CustomTransformerRegistry12 = class {
  static {
    __name(this, "CustomTransformerRegistry");
  }
  constructor() {
    this.transfomers = {};
  }
  register(transformer) {
    this.transfomers[transformer.name] = transformer;
  }
  findApplicable(v) {
    return find12(this.transfomers, (transformer) => transformer.isApplicable(v));
  }
  findByName(name) {
    return this.transfomers[name];
  }
};
var getType13 = /* @__PURE__ */ __name((payload) => Object.prototype.toString.call(payload).slice(8, -1), "getType");
var isUndefined12 = /* @__PURE__ */ __name((payload) => typeof payload === "undefined", "isUndefined");
var isNull13 = /* @__PURE__ */ __name((payload) => payload === null, "isNull");
var isPlainObject13 = /* @__PURE__ */ __name((payload) => {
  if (typeof payload !== "object" || payload === null)
    return false;
  if (payload === Object.prototype)
    return false;
  if (Object.getPrototypeOf(payload) === null)
    return true;
  return Object.getPrototypeOf(payload) === Object.prototype;
}, "isPlainObject");
var isEmptyObject12 = /* @__PURE__ */ __name((payload) => isPlainObject13(payload) && Object.keys(payload).length === 0, "isEmptyObject");
var isArray13 = /* @__PURE__ */ __name((payload) => Array.isArray(payload), "isArray");
var isString12 = /* @__PURE__ */ __name((payload) => typeof payload === "string", "isString");
var isNumber12 = /* @__PURE__ */ __name((payload) => typeof payload === "number" && !isNaN(payload), "isNumber");
var isBoolean12 = /* @__PURE__ */ __name((payload) => typeof payload === "boolean", "isBoolean");
var isRegExp12 = /* @__PURE__ */ __name((payload) => payload instanceof RegExp, "isRegExp");
var isMap12 = /* @__PURE__ */ __name((payload) => payload instanceof Map, "isMap");
var isSet12 = /* @__PURE__ */ __name((payload) => payload instanceof Set, "isSet");
var isSymbol12 = /* @__PURE__ */ __name((payload) => getType13(payload) === "Symbol", "isSymbol");
var isDate12 = /* @__PURE__ */ __name((payload) => payload instanceof Date && !isNaN(payload.valueOf()), "isDate");
var isError12 = /* @__PURE__ */ __name((payload) => payload instanceof Error, "isError");
var isNaNValue12 = /* @__PURE__ */ __name((payload) => typeof payload === "number" && isNaN(payload), "isNaNValue");
var isPrimitive12 = /* @__PURE__ */ __name((payload) => isBoolean12(payload) || isNull13(payload) || isUndefined12(payload) || isNumber12(payload) || isString12(payload) || isSymbol12(payload), "isPrimitive");
var isBigint12 = /* @__PURE__ */ __name((payload) => typeof payload === "bigint", "isBigint");
var isInfinite12 = /* @__PURE__ */ __name((payload) => payload === Infinity || payload === -Infinity, "isInfinite");
var isTypedArray12 = /* @__PURE__ */ __name((payload) => ArrayBuffer.isView(payload) && !(payload instanceof DataView), "isTypedArray");
var isURL12 = /* @__PURE__ */ __name((payload) => payload instanceof URL, "isURL");
var escapeKey12 = /* @__PURE__ */ __name((key) => key.replace(/\\/g, "\\\\").replace(/\./g, "\\."), "escapeKey");
var stringifyPath12 = /* @__PURE__ */ __name((path) => path.map(String).map(escapeKey12).join("."), "stringifyPath");
var parsePath12 = /* @__PURE__ */ __name((string2, legacyPaths) => {
  const result = [];
  let segment = "";
  for (let i = 0; i < string2.length; i++) {
    let char = string2.charAt(i);
    if (!legacyPaths && char === "\\") {
      const escaped = string2.charAt(i + 1);
      if (escaped === "\\") {
        segment += "\\";
        i++;
        continue;
      } else if (escaped !== ".") {
        throw Error("invalid path");
      }
    }
    const isEscapedDot = char === "\\" && string2.charAt(i + 1) === ".";
    if (isEscapedDot) {
      segment += ".";
      i++;
      continue;
    }
    const isEndOfSegment = char === ".";
    if (isEndOfSegment) {
      result.push(segment);
      segment = "";
      continue;
    }
    segment += char;
  }
  const lastSegment = segment;
  result.push(lastSegment);
  return result;
}, "parsePath");
function simpleTransformation12(isApplicable, annotation, transform, untransform) {
  return {
    isApplicable,
    annotation,
    transform,
    untransform
  };
}
__name(simpleTransformation12, "simpleTransformation");
var simpleRules12 = [
  simpleTransformation12(isUndefined12, "undefined", () => null, () => {
    return;
  }),
  simpleTransformation12(isBigint12, "bigint", (v) => v.toString(), (v) => {
    if (typeof BigInt !== "undefined") {
      return BigInt(v);
    }
    console.error("Please add a BigInt polyfill.");
    return v;
  }),
  simpleTransformation12(isDate12, "Date", (v) => v.toISOString(), (v) => new Date(v)),
  simpleTransformation12(isError12, "Error", (v, superJson) => {
    const baseError = {
      name: v.name,
      message: v.message
    };
    if ("cause" in v) {
      baseError.cause = v.cause;
    }
    superJson.allowedErrorProps.forEach((prop) => {
      baseError[prop] = v[prop];
    });
    return baseError;
  }, (v, superJson) => {
    const e = new Error(v.message, { cause: v.cause });
    e.name = v.name;
    e.stack = v.stack;
    superJson.allowedErrorProps.forEach((prop) => {
      e[prop] = v[prop];
    });
    return e;
  }),
  simpleTransformation12(isRegExp12, "regexp", (v) => "" + v, (regex) => {
    const body = regex.slice(1, regex.lastIndexOf("/"));
    const flags2 = regex.slice(regex.lastIndexOf("/") + 1);
    return new RegExp(body, flags2);
  }),
  simpleTransformation12(isSet12, "set", (v) => [...v.values()], (v) => new Set(v)),
  simpleTransformation12(isMap12, "map", (v) => [...v.entries()], (v) => new Map(v)),
  simpleTransformation12((v) => isNaNValue12(v) || isInfinite12(v), "number", (v) => {
    if (isNaNValue12(v)) {
      return "NaN";
    }
    if (v > 0) {
      return "Infinity";
    } else {
      return "-Infinity";
    }
  }, Number),
  simpleTransformation12((v) => v === 0 && 1 / v === -Infinity, "number", () => {
    return "-0";
  }, Number),
  simpleTransformation12(isURL12, "URL", (v) => v.toString(), (v) => new URL(v))
];
function compositeTransformation12(isApplicable, annotation, transform, untransform) {
  return {
    isApplicable,
    annotation,
    transform,
    untransform
  };
}
__name(compositeTransformation12, "compositeTransformation");
var symbolRule12 = compositeTransformation12((s, superJson) => {
  if (isSymbol12(s)) {
    const isRegistered = !!superJson.symbolRegistry.getIdentifier(s);
    return isRegistered;
  }
  return false;
}, (s, superJson) => {
  const identifier = superJson.symbolRegistry.getIdentifier(s);
  return ["symbol", identifier];
}, (v) => v.description, (_, a, superJson) => {
  const value = superJson.symbolRegistry.getValue(a[1]);
  if (!value) {
    throw new Error("Trying to deserialize unknown symbol");
  }
  return value;
});
var constructorToName12 = [
  Int8Array,
  Uint8Array,
  Int16Array,
  Uint16Array,
  Int32Array,
  Uint32Array,
  Float32Array,
  Float64Array,
  Uint8ClampedArray
].reduce((obj, ctor) => {
  obj[ctor.name] = ctor;
  return obj;
}, {});
var typedArrayRule12 = compositeTransformation12(isTypedArray12, (v) => ["typed-array", v.constructor.name], (v) => [...v], (v, a) => {
  const ctor = constructorToName12[a[1]];
  if (!ctor) {
    throw new Error("Trying to deserialize unknown typed array");
  }
  return new ctor(v);
});
function isInstanceOfRegisteredClass12(potentialClass, superJson) {
  if (potentialClass?.constructor) {
    const isRegistered = !!superJson.classRegistry.getIdentifier(potentialClass.constructor);
    return isRegistered;
  }
  return false;
}
__name(isInstanceOfRegisteredClass12, "isInstanceOfRegisteredClass");
var classRule12 = compositeTransformation12(isInstanceOfRegisteredClass12, (clazz, superJson) => {
  const identifier = superJson.classRegistry.getIdentifier(clazz.constructor);
  return ["class", identifier];
}, (clazz, superJson) => {
  const allowedProps = superJson.classRegistry.getAllowedProps(clazz.constructor);
  if (!allowedProps) {
    return { ...clazz };
  }
  const result = {};
  allowedProps.forEach((prop) => {
    result[prop] = clazz[prop];
  });
  return result;
}, (v, a, superJson) => {
  const clazz = superJson.classRegistry.getValue(a[1]);
  if (!clazz) {
    throw new Error(`Trying to deserialize unknown class '${a[1]}' - check https://github.com/blitz-js/superjson/issues/116#issuecomment-773996564`);
  }
  return Object.assign(Object.create(clazz.prototype), v);
});
var customRule12 = compositeTransformation12((value, superJson) => {
  return !!superJson.customTransformerRegistry.findApplicable(value);
}, (value, superJson) => {
  const transformer = superJson.customTransformerRegistry.findApplicable(value);
  return ["custom", transformer.name];
}, (value, superJson) => {
  const transformer = superJson.customTransformerRegistry.findApplicable(value);
  return transformer.serialize(value);
}, (v, a, superJson) => {
  const transformer = superJson.customTransformerRegistry.findByName(a[1]);
  if (!transformer) {
    throw new Error("Trying to deserialize unknown custom value");
  }
  return transformer.deserialize(v);
});
var compositeRules12 = [classRule12, symbolRule12, customRule12, typedArrayRule12];
var transformValue12 = /* @__PURE__ */ __name((value, superJson) => {
  const applicableCompositeRule = findArr12(compositeRules12, (rule) => rule.isApplicable(value, superJson));
  if (applicableCompositeRule) {
    return {
      value: applicableCompositeRule.transform(value, superJson),
      type: applicableCompositeRule.annotation(value, superJson)
    };
  }
  const applicableSimpleRule = findArr12(simpleRules12, (rule) => rule.isApplicable(value, superJson));
  if (applicableSimpleRule) {
    return {
      value: applicableSimpleRule.transform(value, superJson),
      type: applicableSimpleRule.annotation
    };
  }
  return;
}, "transformValue");
var simpleRulesByAnnotation12 = {};
simpleRules12.forEach((rule) => {
  simpleRulesByAnnotation12[rule.annotation] = rule;
});
var untransformValue12 = /* @__PURE__ */ __name((json, type, superJson) => {
  if (isArray13(type)) {
    switch (type[0]) {
      case "symbol":
        return symbolRule12.untransform(json, type, superJson);
      case "class":
        return classRule12.untransform(json, type, superJson);
      case "custom":
        return customRule12.untransform(json, type, superJson);
      case "typed-array":
        return typedArrayRule12.untransform(json, type, superJson);
      default:
        throw new Error("Unknown transformation: " + type);
    }
  } else {
    const transformation = simpleRulesByAnnotation12[type];
    if (!transformation) {
      throw new Error("Unknown transformation: " + type);
    }
    return transformation.untransform(json, superJson);
  }
}, "untransformValue");
var getNthKey12 = /* @__PURE__ */ __name((value, n) => {
  if (n > value.size)
    throw new Error("index out of bounds");
  const keys = value.keys();
  while (n > 0) {
    keys.next();
    n--;
  }
  return keys.next().value;
}, "getNthKey");
function validatePath12(path) {
  if (includes12(path, "__proto__")) {
    throw new Error("__proto__ is not allowed as a property");
  }
  if (includes12(path, "prototype")) {
    throw new Error("prototype is not allowed as a property");
  }
  if (includes12(path, "constructor")) {
    throw new Error("constructor is not allowed as a property");
  }
}
__name(validatePath12, "validatePath");
var getDeep12 = /* @__PURE__ */ __name((object2, path) => {
  validatePath12(path);
  for (let i = 0; i < path.length; i++) {
    const key = path[i];
    if (isSet12(object2)) {
      object2 = getNthKey12(object2, +key);
    } else if (isMap12(object2)) {
      const row = +key;
      const type = +path[++i] === 0 ? "key" : "value";
      const keyOfRow = getNthKey12(object2, row);
      switch (type) {
        case "key":
          object2 = keyOfRow;
          break;
        case "value":
          object2 = object2.get(keyOfRow);
          break;
      }
    } else {
      object2 = object2[key];
    }
  }
  return object2;
}, "getDeep");
var setDeep12 = /* @__PURE__ */ __name((object2, path, mapper) => {
  validatePath12(path);
  if (path.length === 0) {
    return mapper(object2);
  }
  let parent = object2;
  for (let i = 0; i < path.length - 1; i++) {
    const key = path[i];
    if (isArray13(parent)) {
      const index = +key;
      parent = parent[index];
    } else if (isPlainObject13(parent)) {
      parent = parent[key];
    } else if (isSet12(parent)) {
      const row = +key;
      parent = getNthKey12(parent, row);
    } else if (isMap12(parent)) {
      const isEnd = i === path.length - 2;
      if (isEnd) {
        break;
      }
      const row = +key;
      const type = +path[++i] === 0 ? "key" : "value";
      const keyOfRow = getNthKey12(parent, row);
      switch (type) {
        case "key":
          parent = keyOfRow;
          break;
        case "value":
          parent = parent.get(keyOfRow);
          break;
      }
    }
  }
  const lastKey = path[path.length - 1];
  if (isArray13(parent)) {
    parent[+lastKey] = mapper(parent[+lastKey]);
  } else if (isPlainObject13(parent)) {
    parent[lastKey] = mapper(parent[lastKey]);
  }
  if (isSet12(parent)) {
    const oldValue = getNthKey12(parent, +lastKey);
    const newValue = mapper(oldValue);
    if (oldValue !== newValue) {
      parent.delete(oldValue);
      parent.add(newValue);
    }
  }
  if (isMap12(parent)) {
    const row = +path[path.length - 2];
    const keyToRow = getNthKey12(parent, row);
    const type = +lastKey === 0 ? "key" : "value";
    switch (type) {
      case "key": {
        const newKey = mapper(keyToRow);
        parent.set(newKey, parent.get(keyToRow));
        if (newKey !== keyToRow) {
          parent.delete(keyToRow);
        }
        break;
      }
      case "value": {
        parent.set(keyToRow, mapper(parent.get(keyToRow)));
        break;
      }
    }
  }
  return object2;
}, "setDeep");
var enableLegacyPaths12 = /* @__PURE__ */ __name((version4) => version4 < 1, "enableLegacyPaths");
function traverse12(tree, walker26, version4, origin = []) {
  if (!tree) {
    return;
  }
  const legacyPaths = enableLegacyPaths12(version4);
  if (!isArray13(tree)) {
    forEach12(tree, (subtree, key) => traverse12(subtree, walker26, version4, [
      ...origin,
      ...parsePath12(key, legacyPaths)
    ]));
    return;
  }
  const [nodeValue, children] = tree;
  if (children) {
    forEach12(children, (child, key) => {
      traverse12(child, walker26, version4, [
        ...origin,
        ...parsePath12(key, legacyPaths)
      ]);
    });
  }
  walker26(nodeValue, origin);
}
__name(traverse12, "traverse");
function applyValueAnnotations12(plain, annotations, version4, superJson) {
  traverse12(annotations, (type, path) => {
    plain = setDeep12(plain, path, (v) => untransformValue12(v, type, superJson));
  }, version4);
  return plain;
}
__name(applyValueAnnotations12, "applyValueAnnotations");
function applyReferentialEqualityAnnotations12(plain, annotations, version4) {
  const legacyPaths = enableLegacyPaths12(version4);
  function apply(identicalPaths, path) {
    const object2 = getDeep12(plain, parsePath12(path, legacyPaths));
    identicalPaths.map((path2) => parsePath12(path2, legacyPaths)).forEach((identicalObjectPath) => {
      plain = setDeep12(plain, identicalObjectPath, () => object2);
    });
  }
  __name(apply, "apply");
  if (isArray13(annotations)) {
    const [root, other] = annotations;
    root.forEach((identicalPath) => {
      plain = setDeep12(plain, parsePath12(identicalPath, legacyPaths), () => plain);
    });
    if (other) {
      forEach12(other, apply);
    }
  } else {
    forEach12(annotations, apply);
  }
  return plain;
}
__name(applyReferentialEqualityAnnotations12, "applyReferentialEqualityAnnotations");
var isDeep12 = /* @__PURE__ */ __name((object2, superJson) => isPlainObject13(object2) || isArray13(object2) || isMap12(object2) || isSet12(object2) || isError12(object2) || isInstanceOfRegisteredClass12(object2, superJson), "isDeep");
function addIdentity12(object2, path, identities) {
  const existingSet = identities.get(object2);
  if (existingSet) {
    existingSet.push(path);
  } else {
    identities.set(object2, [path]);
  }
}
__name(addIdentity12, "addIdentity");
function generateReferentialEqualityAnnotations12(identitites, dedupe) {
  const result = {};
  let rootEqualityPaths = void 0;
  identitites.forEach((paths) => {
    if (paths.length <= 1) {
      return;
    }
    if (!dedupe) {
      paths = paths.map((path) => path.map(String)).sort((a, b) => a.length - b.length);
    }
    const [representativePath, ...identicalPaths] = paths;
    if (representativePath.length === 0) {
      rootEqualityPaths = identicalPaths.map(stringifyPath12);
    } else {
      result[stringifyPath12(representativePath)] = identicalPaths.map(stringifyPath12);
    }
  });
  if (rootEqualityPaths) {
    if (isEmptyObject12(result)) {
      return [rootEqualityPaths];
    } else {
      return [rootEqualityPaths, result];
    }
  } else {
    return isEmptyObject12(result) ? void 0 : result;
  }
}
__name(generateReferentialEqualityAnnotations12, "generateReferentialEqualityAnnotations");
var walker12 = /* @__PURE__ */ __name((object2, identities, superJson, dedupe, path = [], objectsInThisPath = [], seenObjects = /* @__PURE__ */ new Map()) => {
  const primitive = isPrimitive12(object2);
  if (!primitive) {
    addIdentity12(object2, path, identities);
    const seen = seenObjects.get(object2);
    if (seen) {
      return dedupe ? {
        transformedValue: null
      } : seen;
    }
  }
  if (!isDeep12(object2, superJson)) {
    const transformed2 = transformValue12(object2, superJson);
    const result2 = transformed2 ? {
      transformedValue: transformed2.value,
      annotations: [transformed2.type]
    } : {
      transformedValue: object2
    };
    if (!primitive) {
      seenObjects.set(object2, result2);
    }
    return result2;
  }
  if (includes12(objectsInThisPath, object2)) {
    return {
      transformedValue: null
    };
  }
  const transformationResult = transformValue12(object2, superJson);
  const transformed = transformationResult?.value ?? object2;
  const transformedValue = isArray13(transformed) ? [] : {};
  const innerAnnotations = {};
  forEach12(transformed, (value, index) => {
    if (index === "__proto__" || index === "constructor" || index === "prototype") {
      throw new Error(`Detected property ${index}. This is a prototype pollution risk, please remove it from your object.`);
    }
    const recursiveResult = walker12(value, identities, superJson, dedupe, [...path, index], [...objectsInThisPath, object2], seenObjects);
    transformedValue[index] = recursiveResult.transformedValue;
    if (isArray13(recursiveResult.annotations)) {
      innerAnnotations[escapeKey12(index)] = recursiveResult.annotations;
    } else if (isPlainObject13(recursiveResult.annotations)) {
      forEach12(recursiveResult.annotations, (tree, key) => {
        innerAnnotations[escapeKey12(index) + "." + key] = tree;
      });
    }
  });
  const result = isEmptyObject12(innerAnnotations) ? {
    transformedValue,
    annotations: transformationResult ? [transformationResult.type] : void 0
  } : {
    transformedValue,
    annotations: transformationResult ? [transformationResult.type, innerAnnotations] : innerAnnotations
  };
  if (!primitive) {
    seenObjects.set(object2, result);
  }
  return result;
}, "walker");
function getType212(payload) {
  return Object.prototype.toString.call(payload).slice(8, -1);
}
__name(getType212, "getType2");
function isArray212(payload) {
  return getType212(payload) === "Array";
}
__name(isArray212, "isArray2");
function isPlainObject212(payload) {
  if (getType212(payload) !== "Object")
    return false;
  const prototype = Object.getPrototypeOf(payload);
  return !!prototype && prototype.constructor === Object && prototype === Object.prototype;
}
__name(isPlainObject212, "isPlainObject2");
function assignProp12(carry, key, newVal, originalObject, includeNonenumerable) {
  const propType = {}.propertyIsEnumerable.call(originalObject, key) ? "enumerable" : "nonenumerable";
  if (propType === "enumerable")
    carry[key] = newVal;
  if (includeNonenumerable && propType === "nonenumerable") {
    Object.defineProperty(carry, key, {
      value: newVal,
      enumerable: false,
      writable: true,
      configurable: true
    });
  }
}
__name(assignProp12, "assignProp");
function copy12(target, options = {}) {
  if (isArray212(target)) {
    return target.map((item) => copy12(item, options));
  }
  if (!isPlainObject212(target)) {
    return target;
  }
  const props = Object.getOwnPropertyNames(target);
  const symbols = Object.getOwnPropertySymbols(target);
  return [...props, ...symbols].reduce((carry, key) => {
    if (key === "__proto__")
      return carry;
    if (isArray212(options.props) && !options.props.includes(key)) {
      return carry;
    }
    const val = target[key];
    const newVal = copy12(val, options);
    assignProp12(carry, key, newVal, target, options.nonenumerable);
    return carry;
  }, {});
}
__name(copy12, "copy");
var SuperJSON12 = class {
  static {
    __name(this, "SuperJSON");
  }
  constructor({ dedupe = false } = {}) {
    this.classRegistry = new ClassRegistry12();
    this.symbolRegistry = new Registry12((s) => s.description ?? "");
    this.customTransformerRegistry = new CustomTransformerRegistry12();
    this.allowedErrorProps = [];
    this.dedupe = dedupe;
  }
  serialize(object2) {
    const identities = /* @__PURE__ */ new Map();
    const output = walker12(object2, identities, this, this.dedupe);
    const res = {
      json: output.transformedValue
    };
    if (output.annotations) {
      res.meta = {
        ...res.meta,
        values: output.annotations
      };
    }
    const equalityAnnotations = generateReferentialEqualityAnnotations12(identities, this.dedupe);
    if (equalityAnnotations) {
      res.meta = {
        ...res.meta,
        referentialEqualities: equalityAnnotations
      };
    }
    if (res.meta)
      res.meta.v = 1;
    return res;
  }
  deserialize(payload, options) {
    const { json, meta } = payload;
    let result = options?.inPlace ? json : copy12(json);
    if (meta?.values) {
      result = applyValueAnnotations12(result, meta.values, meta.v ?? 0, this);
    }
    if (meta?.referentialEqualities) {
      result = applyReferentialEqualityAnnotations12(result, meta.referentialEqualities, meta.v ?? 0);
    }
    return result;
  }
  stringify(object2) {
    return JSON.stringify(this.serialize(object2));
  }
  parse(string2) {
    return this.deserialize(JSON.parse(string2), { inPlace: true });
  }
  registerClass(v, options) {
    this.classRegistry.register(v, options);
  }
  registerSymbol(v, identifier) {
    this.symbolRegistry.register(v, identifier);
  }
  registerCustom(transformer, name) {
    this.customTransformerRegistry.register({
      name,
      ...transformer
    });
  }
  allowErrorProps(...props) {
    this.allowedErrorProps.push(...props);
  }
};
SuperJSON12.defaultInstance = new SuperJSON12();
SuperJSON12.serialize = SuperJSON12.defaultInstance.serialize.bind(SuperJSON12.defaultInstance);
SuperJSON12.deserialize = SuperJSON12.defaultInstance.deserialize.bind(SuperJSON12.defaultInstance);
SuperJSON12.stringify = SuperJSON12.defaultInstance.stringify.bind(SuperJSON12.defaultInstance);
SuperJSON12.parse = SuperJSON12.defaultInstance.parse.bind(SuperJSON12.defaultInstance);
SuperJSON12.registerClass = SuperJSON12.defaultInstance.registerClass.bind(SuperJSON12.defaultInstance);
SuperJSON12.registerSymbol = SuperJSON12.defaultInstance.registerSymbol.bind(SuperJSON12.defaultInstance);
SuperJSON12.registerCustom = SuperJSON12.defaultInstance.registerCustom.bind(SuperJSON12.defaultInstance);
SuperJSON12.allowErrorProps = SuperJSON12.defaultInstance.allowErrorProps.bind(SuperJSON12.defaultInstance);
var dist_default12 = SuperJSON12;
var serialize12 = SuperJSON12.serialize;
var deserialize12 = SuperJSON12.deserialize;
var stringify13 = SuperJSON12.stringify;
var parse12 = SuperJSON12.parse;
var registerClass12 = SuperJSON12.registerClass;
var registerCustom12 = SuperJSON12.registerCustom;
var registerSymbol12 = SuperJSON12.registerSymbol;
var allowErrorProps12 = SuperJSON12.allowErrorProps;
function parseData12(formData) {
  const propsArray = [];
  if (!formData)
    return propsArray;
  const batchsIDs = [];
  for (const [key, value] of Array.from(formData.entries())) {
    if (key.startsWith("FILE_"))
      propsArray.push(value);
    else if (key.startsWith("FILES_")) {
      if (batchsIDs.includes(key))
        continue;
      batchsIDs.push(key);
      propsArray.push(formData.getAll(key));
    } else {
      propsArray.push(JSON.parse(decodeURI(value)));
    }
  }
  return propsArray;
}
__name(parseData12, "parseData");
function paramsFromURL12(url) {
  const params = url.searchParams.entries().toArray().map(([_, v]) => v);
  return params.map((param) => JSON.parse(decodeURIComponent(param)));
}
__name(paramsFromURL12, "paramsFromURL");
async function WrapRequestHandler12(context2, endpoint) {
  const isServerAction = context2.request.headers.get("x-server-action") === "true";
  if (!isServerAction) {
    return new Response("Not Found", { status: 404 });
  }
  const parsedData = context2.request.method === "GET" || context2.request.method === "HEAD" ? paramsFromURL12(new URL(context2.request.url)) : parseData12(context2.request.headers.get("content-type") ? await context2.request.formData() : void 0);
  const missingProps = endpoint.length - parsedData.length;
  for (let i = 0; i < missingProps; i++) {
    parsedData.push(void 0);
  }
  parsedData.push(context2);
  const result = await endpoint(...parsedData);
  switch (typeof result) {
    case "string":
    case "number":
    case "boolean":
    case "bigint":
      return new Response(dist_default12.stringify(result), {
        headers: {
          "Content-Type": "application/json",
          dataType: "json"
        }
      });
    case "undefined":
      return new Response(null, { status: 204 });
    case "object":
      if (result instanceof Response) {
        result.headers.set("dataType", "response");
        return result;
      } else if (result instanceof Blob) {
        const res = new Response(await result.arrayBuffer());
        res.headers.set("dataType", "blob");
        res.headers.set("Content-Type", result.type);
        return res;
      } else if (result instanceof File) {
        return new Response(await result.arrayBuffer(), {
          headers: {
            "Content-Type": result.type,
            dataType: "file",
            fileData: JSON.stringify({
              name: result.name,
              lastModified: result.lastModified
            })
          }
        });
      } else {
        return new Response(dist_default12.stringify(result), {
          headers: { "Content-Type": "application/json", dataType: "json" }
        });
      }
    default:
      throw new Error(`Unsupported return type from action: ${typeof result}`);
  }
}
__name(WrapRequestHandler12, "WrapRequestHandler");
var onRequest12 = /* @__PURE__ */ __name(async (context2) => {
  const method = context2.request.method;
  const options = {
    GET: typeof GET4 === "function" ? GET4 : void 0,
    POST: typeof POST10 === "function" ? POST10 : void 0,
    PUT: typeof PUT === "function" ? PUT : void 0,
    DELETE: typeof DELETE === "function" ? DELETE : void 0,
    PATCH: typeof PATCH === "function" ? PATCH : void 0,
    HEAD: typeof HEAD === "function" ? HEAD : void 0,
    OPTIONS: typeof OPTIONS === "function" ? OPTIONS : void 0
  };
  if (!options[method]) {
    return new Response(`Method "${method}" Not Allowed`, {
      status: 405
    });
  }
  return await WrapRequestHandler12(context2, options[method]);
}, "onRequest");

// api/templates/id.js
async function GET5(params) {
  const ctx = getContext(arguments);
  const { env: env2 } = ctx;
  const session = await ctx.data.client.getUserSession("private");
  if (session instanceof Error) {
    return {
      success: false,
      error: "Unauthorized"
    };
  }
  const db = drizzle(env2.PROJECT_DB);
  const template = await db.select().from(emailTemplatesTable).where(and(eq(emailTemplatesTable.name, params.name), ownerGroupConditions({
    user_group_ids: session?.private?.group_ids ?? [],
    ownerGroupIdColumn: emailTemplatesTable.owner_group_id,
    otherEq: [eq(emailTemplatesTable.owner_id, session.user_id)],
    self_host: env2.SELF_HOSTED
  }))).limit(1).get();
  if (!template)
    return {
      success: false,
      error: "Template not found"
    };
  return {
    success: true,
    data: template
  };
}
__name(GET5, "GET");
async function PUT4(params) {
  const ctx = getContext(arguments);
  const { env: env2 } = ctx;
  try {
    const session = await ctx.data.client.getUserSession("private");
    if (session instanceof Error) {
      return {
        success: false,
        error: "Unauthorized"
      };
    }
    const db = drizzle(env2.PROJECT_DB);
    const existing = await db.select({ id: emailTemplatesTable.id }).from(emailTemplatesTable).where(and(eq(emailTemplatesTable.name, params.name), ownerGroupConditions({
      user_group_ids: session?.private?.group_ids ?? [],
      ownerGroupIdColumn: emailTemplatesTable.owner_group_id,
      otherEq: [eq(emailTemplatesTable.owner_id, session.user_id)],
      self_host: env2.SELF_HOSTED
    }))).limit(1).get();
    if (!existing) {
      return {
        success: false,
        error: "Template not found"
      };
    }
    const now = (/* @__PURE__ */ new Date()).toISOString();
    const updateData = {
      ...params.data,
      updated_at: now
    };
    await db.update(emailTemplatesTable).set(updateData).where(eq(emailTemplatesTable.id, existing.id));
    const updated = await db.select().from(emailTemplatesTable).where(eq(emailTemplatesTable.id, existing.id)).limit(1).get();
    return {
      success: true,
      data: updated
    };
  } catch (err) {
    return {
      success: false,
      error: err instanceof Error ? err.message : "Failed to update template"
    };
  }
}
__name(PUT4, "PUT");
async function DELETE9(params) {
  const ctx = getContext(arguments);
  const { env: env2 } = ctx;
  try {
    const currentUserId = (await ctx.data.client.getMetaData())?.id;
    if (!currentUserId) {
      return {
        success: false,
        error: "Unauthorized"
      };
    }
    const db = drizzle(env2.PROJECT_DB);
    const existing = await db.select().from(emailTemplatesTable).where(and(eq(emailTemplatesTable.name, params.name), eq(emailTemplatesTable.owner_id, currentUserId))).limit(1).get();
    if (!existing) {
      return {
        success: false,
        error: "Template not found"
      };
    }
    await db.delete(emailTemplatesTable).where(and(eq(emailTemplatesTable.name, params.name), eq(emailTemplatesTable.owner_id, currentUserId)));
    return {
      success: true
    };
  } catch (err) {
    return {
      success: false,
      error: err instanceof Error ? err.message : "Failed to delete template"
    };
  }
}
__name(DELETE9, "DELETE");
var DoubleIndexedKV13 = class {
  static {
    __name(this, "DoubleIndexedKV");
  }
  constructor() {
    this.keyToValue = /* @__PURE__ */ new Map();
    this.valueToKey = /* @__PURE__ */ new Map();
  }
  set(key, value) {
    this.keyToValue.set(key, value);
    this.valueToKey.set(value, key);
  }
  getByKey(key) {
    return this.keyToValue.get(key);
  }
  getByValue(value) {
    return this.valueToKey.get(value);
  }
  clear() {
    this.keyToValue.clear();
    this.valueToKey.clear();
  }
};
var Registry13 = class {
  static {
    __name(this, "Registry");
  }
  constructor(generateIdentifier) {
    this.generateIdentifier = generateIdentifier;
    this.kv = new DoubleIndexedKV13();
  }
  register(value, identifier) {
    if (this.kv.getByValue(value)) {
      return;
    }
    if (!identifier) {
      identifier = this.generateIdentifier(value);
    }
    this.kv.set(identifier, value);
  }
  clear() {
    this.kv.clear();
  }
  getIdentifier(value) {
    return this.kv.getByValue(value);
  }
  getValue(identifier) {
    return this.kv.getByKey(identifier);
  }
};
var ClassRegistry13 = class extends Registry13 {
  static {
    __name(this, "ClassRegistry");
  }
  constructor() {
    super((c) => c.name);
    this.classToAllowedProps = /* @__PURE__ */ new Map();
  }
  register(value, options) {
    if (typeof options === "object") {
      if (options.allowProps) {
        this.classToAllowedProps.set(value, options.allowProps);
      }
      super.register(value, options.identifier);
    } else {
      super.register(value, options);
    }
  }
  getAllowedProps(value) {
    return this.classToAllowedProps.get(value);
  }
};
function valuesOfObj13(record) {
  if ("values" in Object) {
    return Object.values(record);
  }
  const values = [];
  for (const key in record) {
    if (record.hasOwnProperty(key)) {
      values.push(record[key]);
    }
  }
  return values;
}
__name(valuesOfObj13, "valuesOfObj");
function find13(record, predicate) {
  const values = valuesOfObj13(record);
  if ("find" in values) {
    return values.find(predicate);
  }
  const valuesNotNever = values;
  for (let i = 0; i < valuesNotNever.length; i++) {
    const value = valuesNotNever[i];
    if (predicate(value)) {
      return value;
    }
  }
  return;
}
__name(find13, "find");
function forEach13(record, run) {
  Object.entries(record).forEach(([key, value]) => run(value, key));
}
__name(forEach13, "forEach");
function includes13(arr, value) {
  return arr.indexOf(value) !== -1;
}
__name(includes13, "includes");
function findArr13(record, predicate) {
  for (let i = 0; i < record.length; i++) {
    const value = record[i];
    if (predicate(value)) {
      return value;
    }
  }
  return;
}
__name(findArr13, "findArr");
var CustomTransformerRegistry13 = class {
  static {
    __name(this, "CustomTransformerRegistry");
  }
  constructor() {
    this.transfomers = {};
  }
  register(transformer) {
    this.transfomers[transformer.name] = transformer;
  }
  findApplicable(v) {
    return find13(this.transfomers, (transformer) => transformer.isApplicable(v));
  }
  findByName(name) {
    return this.transfomers[name];
  }
};
var getType14 = /* @__PURE__ */ __name((payload) => Object.prototype.toString.call(payload).slice(8, -1), "getType");
var isUndefined13 = /* @__PURE__ */ __name((payload) => typeof payload === "undefined", "isUndefined");
var isNull14 = /* @__PURE__ */ __name((payload) => payload === null, "isNull");
var isPlainObject14 = /* @__PURE__ */ __name((payload) => {
  if (typeof payload !== "object" || payload === null)
    return false;
  if (payload === Object.prototype)
    return false;
  if (Object.getPrototypeOf(payload) === null)
    return true;
  return Object.getPrototypeOf(payload) === Object.prototype;
}, "isPlainObject");
var isEmptyObject13 = /* @__PURE__ */ __name((payload) => isPlainObject14(payload) && Object.keys(payload).length === 0, "isEmptyObject");
var isArray14 = /* @__PURE__ */ __name((payload) => Array.isArray(payload), "isArray");
var isString13 = /* @__PURE__ */ __name((payload) => typeof payload === "string", "isString");
var isNumber13 = /* @__PURE__ */ __name((payload) => typeof payload === "number" && !isNaN(payload), "isNumber");
var isBoolean13 = /* @__PURE__ */ __name((payload) => typeof payload === "boolean", "isBoolean");
var isRegExp13 = /* @__PURE__ */ __name((payload) => payload instanceof RegExp, "isRegExp");
var isMap13 = /* @__PURE__ */ __name((payload) => payload instanceof Map, "isMap");
var isSet13 = /* @__PURE__ */ __name((payload) => payload instanceof Set, "isSet");
var isSymbol13 = /* @__PURE__ */ __name((payload) => getType14(payload) === "Symbol", "isSymbol");
var isDate13 = /* @__PURE__ */ __name((payload) => payload instanceof Date && !isNaN(payload.valueOf()), "isDate");
var isError13 = /* @__PURE__ */ __name((payload) => payload instanceof Error, "isError");
var isNaNValue13 = /* @__PURE__ */ __name((payload) => typeof payload === "number" && isNaN(payload), "isNaNValue");
var isPrimitive13 = /* @__PURE__ */ __name((payload) => isBoolean13(payload) || isNull14(payload) || isUndefined13(payload) || isNumber13(payload) || isString13(payload) || isSymbol13(payload), "isPrimitive");
var isBigint13 = /* @__PURE__ */ __name((payload) => typeof payload === "bigint", "isBigint");
var isInfinite13 = /* @__PURE__ */ __name((payload) => payload === Infinity || payload === -Infinity, "isInfinite");
var isTypedArray13 = /* @__PURE__ */ __name((payload) => ArrayBuffer.isView(payload) && !(payload instanceof DataView), "isTypedArray");
var isURL13 = /* @__PURE__ */ __name((payload) => payload instanceof URL, "isURL");
var escapeKey13 = /* @__PURE__ */ __name((key) => key.replace(/\\/g, "\\\\").replace(/\./g, "\\."), "escapeKey");
var stringifyPath13 = /* @__PURE__ */ __name((path) => path.map(String).map(escapeKey13).join("."), "stringifyPath");
var parsePath13 = /* @__PURE__ */ __name((string2, legacyPaths) => {
  const result = [];
  let segment = "";
  for (let i = 0; i < string2.length; i++) {
    let char = string2.charAt(i);
    if (!legacyPaths && char === "\\") {
      const escaped = string2.charAt(i + 1);
      if (escaped === "\\") {
        segment += "\\";
        i++;
        continue;
      } else if (escaped !== ".") {
        throw Error("invalid path");
      }
    }
    const isEscapedDot = char === "\\" && string2.charAt(i + 1) === ".";
    if (isEscapedDot) {
      segment += ".";
      i++;
      continue;
    }
    const isEndOfSegment = char === ".";
    if (isEndOfSegment) {
      result.push(segment);
      segment = "";
      continue;
    }
    segment += char;
  }
  const lastSegment = segment;
  result.push(lastSegment);
  return result;
}, "parsePath");
function simpleTransformation13(isApplicable, annotation, transform, untransform) {
  return {
    isApplicable,
    annotation,
    transform,
    untransform
  };
}
__name(simpleTransformation13, "simpleTransformation");
var simpleRules13 = [
  simpleTransformation13(isUndefined13, "undefined", () => null, () => {
    return;
  }),
  simpleTransformation13(isBigint13, "bigint", (v) => v.toString(), (v) => {
    if (typeof BigInt !== "undefined") {
      return BigInt(v);
    }
    console.error("Please add a BigInt polyfill.");
    return v;
  }),
  simpleTransformation13(isDate13, "Date", (v) => v.toISOString(), (v) => new Date(v)),
  simpleTransformation13(isError13, "Error", (v, superJson) => {
    const baseError = {
      name: v.name,
      message: v.message
    };
    if ("cause" in v) {
      baseError.cause = v.cause;
    }
    superJson.allowedErrorProps.forEach((prop) => {
      baseError[prop] = v[prop];
    });
    return baseError;
  }, (v, superJson) => {
    const e = new Error(v.message, { cause: v.cause });
    e.name = v.name;
    e.stack = v.stack;
    superJson.allowedErrorProps.forEach((prop) => {
      e[prop] = v[prop];
    });
    return e;
  }),
  simpleTransformation13(isRegExp13, "regexp", (v) => "" + v, (regex) => {
    const body = regex.slice(1, regex.lastIndexOf("/"));
    const flags2 = regex.slice(regex.lastIndexOf("/") + 1);
    return new RegExp(body, flags2);
  }),
  simpleTransformation13(isSet13, "set", (v) => [...v.values()], (v) => new Set(v)),
  simpleTransformation13(isMap13, "map", (v) => [...v.entries()], (v) => new Map(v)),
  simpleTransformation13((v) => isNaNValue13(v) || isInfinite13(v), "number", (v) => {
    if (isNaNValue13(v)) {
      return "NaN";
    }
    if (v > 0) {
      return "Infinity";
    } else {
      return "-Infinity";
    }
  }, Number),
  simpleTransformation13((v) => v === 0 && 1 / v === -Infinity, "number", () => {
    return "-0";
  }, Number),
  simpleTransformation13(isURL13, "URL", (v) => v.toString(), (v) => new URL(v))
];
function compositeTransformation13(isApplicable, annotation, transform, untransform) {
  return {
    isApplicable,
    annotation,
    transform,
    untransform
  };
}
__name(compositeTransformation13, "compositeTransformation");
var symbolRule13 = compositeTransformation13((s, superJson) => {
  if (isSymbol13(s)) {
    const isRegistered = !!superJson.symbolRegistry.getIdentifier(s);
    return isRegistered;
  }
  return false;
}, (s, superJson) => {
  const identifier = superJson.symbolRegistry.getIdentifier(s);
  return ["symbol", identifier];
}, (v) => v.description, (_, a, superJson) => {
  const value = superJson.symbolRegistry.getValue(a[1]);
  if (!value) {
    throw new Error("Trying to deserialize unknown symbol");
  }
  return value;
});
var constructorToName13 = [
  Int8Array,
  Uint8Array,
  Int16Array,
  Uint16Array,
  Int32Array,
  Uint32Array,
  Float32Array,
  Float64Array,
  Uint8ClampedArray
].reduce((obj, ctor) => {
  obj[ctor.name] = ctor;
  return obj;
}, {});
var typedArrayRule13 = compositeTransformation13(isTypedArray13, (v) => ["typed-array", v.constructor.name], (v) => [...v], (v, a) => {
  const ctor = constructorToName13[a[1]];
  if (!ctor) {
    throw new Error("Trying to deserialize unknown typed array");
  }
  return new ctor(v);
});
function isInstanceOfRegisteredClass13(potentialClass, superJson) {
  if (potentialClass?.constructor) {
    const isRegistered = !!superJson.classRegistry.getIdentifier(potentialClass.constructor);
    return isRegistered;
  }
  return false;
}
__name(isInstanceOfRegisteredClass13, "isInstanceOfRegisteredClass");
var classRule13 = compositeTransformation13(isInstanceOfRegisteredClass13, (clazz, superJson) => {
  const identifier = superJson.classRegistry.getIdentifier(clazz.constructor);
  return ["class", identifier];
}, (clazz, superJson) => {
  const allowedProps = superJson.classRegistry.getAllowedProps(clazz.constructor);
  if (!allowedProps) {
    return { ...clazz };
  }
  const result = {};
  allowedProps.forEach((prop) => {
    result[prop] = clazz[prop];
  });
  return result;
}, (v, a, superJson) => {
  const clazz = superJson.classRegistry.getValue(a[1]);
  if (!clazz) {
    throw new Error(`Trying to deserialize unknown class '${a[1]}' - check https://github.com/blitz-js/superjson/issues/116#issuecomment-773996564`);
  }
  return Object.assign(Object.create(clazz.prototype), v);
});
var customRule13 = compositeTransformation13((value, superJson) => {
  return !!superJson.customTransformerRegistry.findApplicable(value);
}, (value, superJson) => {
  const transformer = superJson.customTransformerRegistry.findApplicable(value);
  return ["custom", transformer.name];
}, (value, superJson) => {
  const transformer = superJson.customTransformerRegistry.findApplicable(value);
  return transformer.serialize(value);
}, (v, a, superJson) => {
  const transformer = superJson.customTransformerRegistry.findByName(a[1]);
  if (!transformer) {
    throw new Error("Trying to deserialize unknown custom value");
  }
  return transformer.deserialize(v);
});
var compositeRules13 = [classRule13, symbolRule13, customRule13, typedArrayRule13];
var transformValue13 = /* @__PURE__ */ __name((value, superJson) => {
  const applicableCompositeRule = findArr13(compositeRules13, (rule) => rule.isApplicable(value, superJson));
  if (applicableCompositeRule) {
    return {
      value: applicableCompositeRule.transform(value, superJson),
      type: applicableCompositeRule.annotation(value, superJson)
    };
  }
  const applicableSimpleRule = findArr13(simpleRules13, (rule) => rule.isApplicable(value, superJson));
  if (applicableSimpleRule) {
    return {
      value: applicableSimpleRule.transform(value, superJson),
      type: applicableSimpleRule.annotation
    };
  }
  return;
}, "transformValue");
var simpleRulesByAnnotation13 = {};
simpleRules13.forEach((rule) => {
  simpleRulesByAnnotation13[rule.annotation] = rule;
});
var untransformValue13 = /* @__PURE__ */ __name((json, type, superJson) => {
  if (isArray14(type)) {
    switch (type[0]) {
      case "symbol":
        return symbolRule13.untransform(json, type, superJson);
      case "class":
        return classRule13.untransform(json, type, superJson);
      case "custom":
        return customRule13.untransform(json, type, superJson);
      case "typed-array":
        return typedArrayRule13.untransform(json, type, superJson);
      default:
        throw new Error("Unknown transformation: " + type);
    }
  } else {
    const transformation = simpleRulesByAnnotation13[type];
    if (!transformation) {
      throw new Error("Unknown transformation: " + type);
    }
    return transformation.untransform(json, superJson);
  }
}, "untransformValue");
var getNthKey13 = /* @__PURE__ */ __name((value, n) => {
  if (n > value.size)
    throw new Error("index out of bounds");
  const keys = value.keys();
  while (n > 0) {
    keys.next();
    n--;
  }
  return keys.next().value;
}, "getNthKey");
function validatePath13(path) {
  if (includes13(path, "__proto__")) {
    throw new Error("__proto__ is not allowed as a property");
  }
  if (includes13(path, "prototype")) {
    throw new Error("prototype is not allowed as a property");
  }
  if (includes13(path, "constructor")) {
    throw new Error("constructor is not allowed as a property");
  }
}
__name(validatePath13, "validatePath");
var getDeep13 = /* @__PURE__ */ __name((object2, path) => {
  validatePath13(path);
  for (let i = 0; i < path.length; i++) {
    const key = path[i];
    if (isSet13(object2)) {
      object2 = getNthKey13(object2, +key);
    } else if (isMap13(object2)) {
      const row = +key;
      const type = +path[++i] === 0 ? "key" : "value";
      const keyOfRow = getNthKey13(object2, row);
      switch (type) {
        case "key":
          object2 = keyOfRow;
          break;
        case "value":
          object2 = object2.get(keyOfRow);
          break;
      }
    } else {
      object2 = object2[key];
    }
  }
  return object2;
}, "getDeep");
var setDeep13 = /* @__PURE__ */ __name((object2, path, mapper) => {
  validatePath13(path);
  if (path.length === 0) {
    return mapper(object2);
  }
  let parent = object2;
  for (let i = 0; i < path.length - 1; i++) {
    const key = path[i];
    if (isArray14(parent)) {
      const index = +key;
      parent = parent[index];
    } else if (isPlainObject14(parent)) {
      parent = parent[key];
    } else if (isSet13(parent)) {
      const row = +key;
      parent = getNthKey13(parent, row);
    } else if (isMap13(parent)) {
      const isEnd = i === path.length - 2;
      if (isEnd) {
        break;
      }
      const row = +key;
      const type = +path[++i] === 0 ? "key" : "value";
      const keyOfRow = getNthKey13(parent, row);
      switch (type) {
        case "key":
          parent = keyOfRow;
          break;
        case "value":
          parent = parent.get(keyOfRow);
          break;
      }
    }
  }
  const lastKey = path[path.length - 1];
  if (isArray14(parent)) {
    parent[+lastKey] = mapper(parent[+lastKey]);
  } else if (isPlainObject14(parent)) {
    parent[lastKey] = mapper(parent[lastKey]);
  }
  if (isSet13(parent)) {
    const oldValue = getNthKey13(parent, +lastKey);
    const newValue = mapper(oldValue);
    if (oldValue !== newValue) {
      parent.delete(oldValue);
      parent.add(newValue);
    }
  }
  if (isMap13(parent)) {
    const row = +path[path.length - 2];
    const keyToRow = getNthKey13(parent, row);
    const type = +lastKey === 0 ? "key" : "value";
    switch (type) {
      case "key": {
        const newKey = mapper(keyToRow);
        parent.set(newKey, parent.get(keyToRow));
        if (newKey !== keyToRow) {
          parent.delete(keyToRow);
        }
        break;
      }
      case "value": {
        parent.set(keyToRow, mapper(parent.get(keyToRow)));
        break;
      }
    }
  }
  return object2;
}, "setDeep");
var enableLegacyPaths13 = /* @__PURE__ */ __name((version4) => version4 < 1, "enableLegacyPaths");
function traverse13(tree, walker26, version4, origin = []) {
  if (!tree) {
    return;
  }
  const legacyPaths = enableLegacyPaths13(version4);
  if (!isArray14(tree)) {
    forEach13(tree, (subtree, key) => traverse13(subtree, walker26, version4, [
      ...origin,
      ...parsePath13(key, legacyPaths)
    ]));
    return;
  }
  const [nodeValue, children] = tree;
  if (children) {
    forEach13(children, (child, key) => {
      traverse13(child, walker26, version4, [
        ...origin,
        ...parsePath13(key, legacyPaths)
      ]);
    });
  }
  walker26(nodeValue, origin);
}
__name(traverse13, "traverse");
function applyValueAnnotations13(plain, annotations, version4, superJson) {
  traverse13(annotations, (type, path) => {
    plain = setDeep13(plain, path, (v) => untransformValue13(v, type, superJson));
  }, version4);
  return plain;
}
__name(applyValueAnnotations13, "applyValueAnnotations");
function applyReferentialEqualityAnnotations13(plain, annotations, version4) {
  const legacyPaths = enableLegacyPaths13(version4);
  function apply(identicalPaths, path) {
    const object2 = getDeep13(plain, parsePath13(path, legacyPaths));
    identicalPaths.map((path2) => parsePath13(path2, legacyPaths)).forEach((identicalObjectPath) => {
      plain = setDeep13(plain, identicalObjectPath, () => object2);
    });
  }
  __name(apply, "apply");
  if (isArray14(annotations)) {
    const [root, other] = annotations;
    root.forEach((identicalPath) => {
      plain = setDeep13(plain, parsePath13(identicalPath, legacyPaths), () => plain);
    });
    if (other) {
      forEach13(other, apply);
    }
  } else {
    forEach13(annotations, apply);
  }
  return plain;
}
__name(applyReferentialEqualityAnnotations13, "applyReferentialEqualityAnnotations");
var isDeep13 = /* @__PURE__ */ __name((object2, superJson) => isPlainObject14(object2) || isArray14(object2) || isMap13(object2) || isSet13(object2) || isError13(object2) || isInstanceOfRegisteredClass13(object2, superJson), "isDeep");
function addIdentity13(object2, path, identities) {
  const existingSet = identities.get(object2);
  if (existingSet) {
    existingSet.push(path);
  } else {
    identities.set(object2, [path]);
  }
}
__name(addIdentity13, "addIdentity");
function generateReferentialEqualityAnnotations13(identitites, dedupe) {
  const result = {};
  let rootEqualityPaths = void 0;
  identitites.forEach((paths) => {
    if (paths.length <= 1) {
      return;
    }
    if (!dedupe) {
      paths = paths.map((path) => path.map(String)).sort((a, b) => a.length - b.length);
    }
    const [representativePath, ...identicalPaths] = paths;
    if (representativePath.length === 0) {
      rootEqualityPaths = identicalPaths.map(stringifyPath13);
    } else {
      result[stringifyPath13(representativePath)] = identicalPaths.map(stringifyPath13);
    }
  });
  if (rootEqualityPaths) {
    if (isEmptyObject13(result)) {
      return [rootEqualityPaths];
    } else {
      return [rootEqualityPaths, result];
    }
  } else {
    return isEmptyObject13(result) ? void 0 : result;
  }
}
__name(generateReferentialEqualityAnnotations13, "generateReferentialEqualityAnnotations");
var walker13 = /* @__PURE__ */ __name((object2, identities, superJson, dedupe, path = [], objectsInThisPath = [], seenObjects = /* @__PURE__ */ new Map()) => {
  const primitive = isPrimitive13(object2);
  if (!primitive) {
    addIdentity13(object2, path, identities);
    const seen = seenObjects.get(object2);
    if (seen) {
      return dedupe ? {
        transformedValue: null
      } : seen;
    }
  }
  if (!isDeep13(object2, superJson)) {
    const transformed2 = transformValue13(object2, superJson);
    const result2 = transformed2 ? {
      transformedValue: transformed2.value,
      annotations: [transformed2.type]
    } : {
      transformedValue: object2
    };
    if (!primitive) {
      seenObjects.set(object2, result2);
    }
    return result2;
  }
  if (includes13(objectsInThisPath, object2)) {
    return {
      transformedValue: null
    };
  }
  const transformationResult = transformValue13(object2, superJson);
  const transformed = transformationResult?.value ?? object2;
  const transformedValue = isArray14(transformed) ? [] : {};
  const innerAnnotations = {};
  forEach13(transformed, (value, index) => {
    if (index === "__proto__" || index === "constructor" || index === "prototype") {
      throw new Error(`Detected property ${index}. This is a prototype pollution risk, please remove it from your object.`);
    }
    const recursiveResult = walker13(value, identities, superJson, dedupe, [...path, index], [...objectsInThisPath, object2], seenObjects);
    transformedValue[index] = recursiveResult.transformedValue;
    if (isArray14(recursiveResult.annotations)) {
      innerAnnotations[escapeKey13(index)] = recursiveResult.annotations;
    } else if (isPlainObject14(recursiveResult.annotations)) {
      forEach13(recursiveResult.annotations, (tree, key) => {
        innerAnnotations[escapeKey13(index) + "." + key] = tree;
      });
    }
  });
  const result = isEmptyObject13(innerAnnotations) ? {
    transformedValue,
    annotations: transformationResult ? [transformationResult.type] : void 0
  } : {
    transformedValue,
    annotations: transformationResult ? [transformationResult.type, innerAnnotations] : innerAnnotations
  };
  if (!primitive) {
    seenObjects.set(object2, result);
  }
  return result;
}, "walker");
function getType213(payload) {
  return Object.prototype.toString.call(payload).slice(8, -1);
}
__name(getType213, "getType2");
function isArray213(payload) {
  return getType213(payload) === "Array";
}
__name(isArray213, "isArray2");
function isPlainObject213(payload) {
  if (getType213(payload) !== "Object")
    return false;
  const prototype = Object.getPrototypeOf(payload);
  return !!prototype && prototype.constructor === Object && prototype === Object.prototype;
}
__name(isPlainObject213, "isPlainObject2");
function assignProp13(carry, key, newVal, originalObject, includeNonenumerable) {
  const propType = {}.propertyIsEnumerable.call(originalObject, key) ? "enumerable" : "nonenumerable";
  if (propType === "enumerable")
    carry[key] = newVal;
  if (includeNonenumerable && propType === "nonenumerable") {
    Object.defineProperty(carry, key, {
      value: newVal,
      enumerable: false,
      writable: true,
      configurable: true
    });
  }
}
__name(assignProp13, "assignProp");
function copy13(target, options = {}) {
  if (isArray213(target)) {
    return target.map((item) => copy13(item, options));
  }
  if (!isPlainObject213(target)) {
    return target;
  }
  const props = Object.getOwnPropertyNames(target);
  const symbols = Object.getOwnPropertySymbols(target);
  return [...props, ...symbols].reduce((carry, key) => {
    if (key === "__proto__")
      return carry;
    if (isArray213(options.props) && !options.props.includes(key)) {
      return carry;
    }
    const val = target[key];
    const newVal = copy13(val, options);
    assignProp13(carry, key, newVal, target, options.nonenumerable);
    return carry;
  }, {});
}
__name(copy13, "copy");
var SuperJSON13 = class {
  static {
    __name(this, "SuperJSON");
  }
  constructor({ dedupe = false } = {}) {
    this.classRegistry = new ClassRegistry13();
    this.symbolRegistry = new Registry13((s) => s.description ?? "");
    this.customTransformerRegistry = new CustomTransformerRegistry13();
    this.allowedErrorProps = [];
    this.dedupe = dedupe;
  }
  serialize(object2) {
    const identities = /* @__PURE__ */ new Map();
    const output = walker13(object2, identities, this, this.dedupe);
    const res = {
      json: output.transformedValue
    };
    if (output.annotations) {
      res.meta = {
        ...res.meta,
        values: output.annotations
      };
    }
    const equalityAnnotations = generateReferentialEqualityAnnotations13(identities, this.dedupe);
    if (equalityAnnotations) {
      res.meta = {
        ...res.meta,
        referentialEqualities: equalityAnnotations
      };
    }
    if (res.meta)
      res.meta.v = 1;
    return res;
  }
  deserialize(payload, options) {
    const { json, meta } = payload;
    let result = options?.inPlace ? json : copy13(json);
    if (meta?.values) {
      result = applyValueAnnotations13(result, meta.values, meta.v ?? 0, this);
    }
    if (meta?.referentialEqualities) {
      result = applyReferentialEqualityAnnotations13(result, meta.referentialEqualities, meta.v ?? 0);
    }
    return result;
  }
  stringify(object2) {
    return JSON.stringify(this.serialize(object2));
  }
  parse(string2) {
    return this.deserialize(JSON.parse(string2), { inPlace: true });
  }
  registerClass(v, options) {
    this.classRegistry.register(v, options);
  }
  registerSymbol(v, identifier) {
    this.symbolRegistry.register(v, identifier);
  }
  registerCustom(transformer, name) {
    this.customTransformerRegistry.register({
      name,
      ...transformer
    });
  }
  allowErrorProps(...props) {
    this.allowedErrorProps.push(...props);
  }
};
SuperJSON13.defaultInstance = new SuperJSON13();
SuperJSON13.serialize = SuperJSON13.defaultInstance.serialize.bind(SuperJSON13.defaultInstance);
SuperJSON13.deserialize = SuperJSON13.defaultInstance.deserialize.bind(SuperJSON13.defaultInstance);
SuperJSON13.stringify = SuperJSON13.defaultInstance.stringify.bind(SuperJSON13.defaultInstance);
SuperJSON13.parse = SuperJSON13.defaultInstance.parse.bind(SuperJSON13.defaultInstance);
SuperJSON13.registerClass = SuperJSON13.defaultInstance.registerClass.bind(SuperJSON13.defaultInstance);
SuperJSON13.registerSymbol = SuperJSON13.defaultInstance.registerSymbol.bind(SuperJSON13.defaultInstance);
SuperJSON13.registerCustom = SuperJSON13.defaultInstance.registerCustom.bind(SuperJSON13.defaultInstance);
SuperJSON13.allowErrorProps = SuperJSON13.defaultInstance.allowErrorProps.bind(SuperJSON13.defaultInstance);
var dist_default13 = SuperJSON13;
var serialize13 = SuperJSON13.serialize;
var deserialize13 = SuperJSON13.deserialize;
var stringify14 = SuperJSON13.stringify;
var parse13 = SuperJSON13.parse;
var registerClass13 = SuperJSON13.registerClass;
var registerCustom13 = SuperJSON13.registerCustom;
var registerSymbol13 = SuperJSON13.registerSymbol;
var allowErrorProps13 = SuperJSON13.allowErrorProps;
function parseData13(formData) {
  const propsArray = [];
  if (!formData)
    return propsArray;
  const batchsIDs = [];
  for (const [key, value] of Array.from(formData.entries())) {
    if (key.startsWith("FILE_"))
      propsArray.push(value);
    else if (key.startsWith("FILES_")) {
      if (batchsIDs.includes(key))
        continue;
      batchsIDs.push(key);
      propsArray.push(formData.getAll(key));
    } else {
      propsArray.push(JSON.parse(decodeURI(value)));
    }
  }
  return propsArray;
}
__name(parseData13, "parseData");
function paramsFromURL13(url) {
  const params = url.searchParams.entries().toArray().map(([_, v]) => v);
  return params.map((param) => JSON.parse(decodeURIComponent(param)));
}
__name(paramsFromURL13, "paramsFromURL");
async function WrapRequestHandler13(context2, endpoint) {
  const isServerAction = context2.request.headers.get("x-server-action") === "true";
  if (!isServerAction) {
    return new Response("Not Found", { status: 404 });
  }
  const parsedData = context2.request.method === "GET" || context2.request.method === "HEAD" ? paramsFromURL13(new URL(context2.request.url)) : parseData13(context2.request.headers.get("content-type") ? await context2.request.formData() : void 0);
  const missingProps = endpoint.length - parsedData.length;
  for (let i = 0; i < missingProps; i++) {
    parsedData.push(void 0);
  }
  parsedData.push(context2);
  const result = await endpoint(...parsedData);
  switch (typeof result) {
    case "string":
    case "number":
    case "boolean":
    case "bigint":
      return new Response(dist_default13.stringify(result), {
        headers: {
          "Content-Type": "application/json",
          dataType: "json"
        }
      });
    case "undefined":
      return new Response(null, { status: 204 });
    case "object":
      if (result instanceof Response) {
        result.headers.set("dataType", "response");
        return result;
      } else if (result instanceof Blob) {
        const res = new Response(await result.arrayBuffer());
        res.headers.set("dataType", "blob");
        res.headers.set("Content-Type", result.type);
        return res;
      } else if (result instanceof File) {
        return new Response(await result.arrayBuffer(), {
          headers: {
            "Content-Type": result.type,
            dataType: "file",
            fileData: JSON.stringify({
              name: result.name,
              lastModified: result.lastModified
            })
          }
        });
      } else {
        return new Response(dist_default13.stringify(result), {
          headers: { "Content-Type": "application/json", dataType: "json" }
        });
      }
    default:
      throw new Error(`Unsupported return type from action: ${typeof result}`);
  }
}
__name(WrapRequestHandler13, "WrapRequestHandler");
var onRequest13 = /* @__PURE__ */ __name(async (context2) => {
  const method = context2.request.method;
  const options = {
    GET: typeof GET5 === "function" ? GET5 : void 0,
    POST: typeof POST === "function" ? POST : void 0,
    PUT: typeof PUT4 === "function" ? PUT4 : void 0,
    DELETE: typeof DELETE9 === "function" ? DELETE9 : void 0,
    PATCH: typeof PATCH === "function" ? PATCH : void 0,
    HEAD: typeof HEAD === "function" ? HEAD : void 0,
    OPTIONS: typeof OPTIONS === "function" ? OPTIONS : void 0
  };
  if (!options[method]) {
    return new Response(`Method "${method}" Not Allowed`, {
      status: 405
    });
  }
  return await WrapRequestHandler13(context2, options[method]);
}, "onRequest");

// api/themes/id.js
async function GET6(params) {
  const ctx = getContext(arguments);
  const { env: env2 } = ctx;
  const { id } = params;
  if (!id) {
    return {
      success: false,
      error: "Theme ID is required"
    };
  }
  const session = await ctx.data.client.getUserSession("private");
  if (session instanceof Error) {
    return {
      success: false,
      error: "Unauthorized"
    };
  }
  const db = drizzle(env2.PROJECT_DB);
  const themes = (await db.select().from(uiStyleTable).where(and(eq(uiStyleTable.id, id), ownerGroupConditions({
    user_group_ids: session?.private?.group_ids ?? [],
    ownerGroupIdColumn: uiStyleTable.owner_group_id,
    otherEq: [eq(uiStyleTable.owner_id, session.user_id)],
    self_host: env2.SELF_HOSTED
  }))).limit(1)).at(0);
  if (!themes) {
    return {
      success: false,
      error: "Theme not found"
    };
  }
  return {
    success: true,
    data: {
      id: themes.id,
      name: themes.name,
      themeData: themes.themeData
    }
  };
}
__name(GET6, "GET");
async function PUT5(params) {
  const ctx = getContext(arguments);
  const { env: env2 } = ctx;
  const { id, data } = params;
  if (!id) {
    return {
      success: false,
      error: "Theme ID is required"
    };
  }
  const db = drizzle(env2.PROJECT_DB);
  const currentUserId = (await ctx.data.client.getMetaData())?.id;
  if (!currentUserId) {
    return {
      success: false,
      error: "Unauthorized"
    };
  }
  const existing = await db.select().from(uiStyleTable).where(and(eq(uiStyleTable.id, id), eq(uiStyleTable.owner_id, currentUserId))).limit(1);
  if (existing.length === 0) {
    return {
      success: false,
      error: "Theme not found"
    };
  }
  const currentTheme = existing.at(0)?.themeData || {};
  const updatedTheme = {
    ...currentTheme,
    ...data
  };
  await db.update(uiStyleTable).set({
    themeData: updatedTheme
  }).where(eq(uiStyleTable.id, id));
  return {
    success: true,
    data: {
      id,
      name: existing.at(0)?.name || "",
      themeData: updatedTheme
    }
  };
}
__name(PUT5, "PUT");
async function DELETE10(params) {
  const ctx = getContext(arguments);
  const { env: env2 } = ctx;
  const { id } = params;
  if (!id) {
    return {
      success: false,
      error: "Theme ID is required"
    };
  }
  const db = drizzle(env2.PROJECT_DB);
  const currentUserId = (await ctx.data.client.getMetaData())?.id;
  if (!currentUserId) {
    return {
      success: false,
      error: "Unauthorized"
    };
  }
  const existing = await db.select().from(uiStyleTable).where(and(eq(uiStyleTable.id, id), eq(uiStyleTable.owner_id, currentUserId))).limit(1);
  if (existing.length === 0) {
    return {
      success: false,
      error: "Theme not found"
    };
  }
  await db.delete(uiStyleTable).where(and(eq(uiStyleTable.id, id), eq(uiStyleTable.owner_id, currentUserId)));
  return {
    success: true
  };
}
__name(DELETE10, "DELETE");
var DoubleIndexedKV14 = class {
  static {
    __name(this, "DoubleIndexedKV");
  }
  constructor() {
    this.keyToValue = /* @__PURE__ */ new Map();
    this.valueToKey = /* @__PURE__ */ new Map();
  }
  set(key, value) {
    this.keyToValue.set(key, value);
    this.valueToKey.set(value, key);
  }
  getByKey(key) {
    return this.keyToValue.get(key);
  }
  getByValue(value) {
    return this.valueToKey.get(value);
  }
  clear() {
    this.keyToValue.clear();
    this.valueToKey.clear();
  }
};
var Registry14 = class {
  static {
    __name(this, "Registry");
  }
  constructor(generateIdentifier) {
    this.generateIdentifier = generateIdentifier;
    this.kv = new DoubleIndexedKV14();
  }
  register(value, identifier) {
    if (this.kv.getByValue(value)) {
      return;
    }
    if (!identifier) {
      identifier = this.generateIdentifier(value);
    }
    this.kv.set(identifier, value);
  }
  clear() {
    this.kv.clear();
  }
  getIdentifier(value) {
    return this.kv.getByValue(value);
  }
  getValue(identifier) {
    return this.kv.getByKey(identifier);
  }
};
var ClassRegistry14 = class extends Registry14 {
  static {
    __name(this, "ClassRegistry");
  }
  constructor() {
    super((c) => c.name);
    this.classToAllowedProps = /* @__PURE__ */ new Map();
  }
  register(value, options) {
    if (typeof options === "object") {
      if (options.allowProps) {
        this.classToAllowedProps.set(value, options.allowProps);
      }
      super.register(value, options.identifier);
    } else {
      super.register(value, options);
    }
  }
  getAllowedProps(value) {
    return this.classToAllowedProps.get(value);
  }
};
function valuesOfObj14(record) {
  if ("values" in Object) {
    return Object.values(record);
  }
  const values = [];
  for (const key in record) {
    if (record.hasOwnProperty(key)) {
      values.push(record[key]);
    }
  }
  return values;
}
__name(valuesOfObj14, "valuesOfObj");
function find14(record, predicate) {
  const values = valuesOfObj14(record);
  if ("find" in values) {
    return values.find(predicate);
  }
  const valuesNotNever = values;
  for (let i = 0; i < valuesNotNever.length; i++) {
    const value = valuesNotNever[i];
    if (predicate(value)) {
      return value;
    }
  }
  return;
}
__name(find14, "find");
function forEach14(record, run) {
  Object.entries(record).forEach(([key, value]) => run(value, key));
}
__name(forEach14, "forEach");
function includes14(arr, value) {
  return arr.indexOf(value) !== -1;
}
__name(includes14, "includes");
function findArr14(record, predicate) {
  for (let i = 0; i < record.length; i++) {
    const value = record[i];
    if (predicate(value)) {
      return value;
    }
  }
  return;
}
__name(findArr14, "findArr");
var CustomTransformerRegistry14 = class {
  static {
    __name(this, "CustomTransformerRegistry");
  }
  constructor() {
    this.transfomers = {};
  }
  register(transformer) {
    this.transfomers[transformer.name] = transformer;
  }
  findApplicable(v) {
    return find14(this.transfomers, (transformer) => transformer.isApplicable(v));
  }
  findByName(name) {
    return this.transfomers[name];
  }
};
var getType15 = /* @__PURE__ */ __name((payload) => Object.prototype.toString.call(payload).slice(8, -1), "getType");
var isUndefined14 = /* @__PURE__ */ __name((payload) => typeof payload === "undefined", "isUndefined");
var isNull15 = /* @__PURE__ */ __name((payload) => payload === null, "isNull");
var isPlainObject15 = /* @__PURE__ */ __name((payload) => {
  if (typeof payload !== "object" || payload === null)
    return false;
  if (payload === Object.prototype)
    return false;
  if (Object.getPrototypeOf(payload) === null)
    return true;
  return Object.getPrototypeOf(payload) === Object.prototype;
}, "isPlainObject");
var isEmptyObject14 = /* @__PURE__ */ __name((payload) => isPlainObject15(payload) && Object.keys(payload).length === 0, "isEmptyObject");
var isArray15 = /* @__PURE__ */ __name((payload) => Array.isArray(payload), "isArray");
var isString14 = /* @__PURE__ */ __name((payload) => typeof payload === "string", "isString");
var isNumber14 = /* @__PURE__ */ __name((payload) => typeof payload === "number" && !isNaN(payload), "isNumber");
var isBoolean14 = /* @__PURE__ */ __name((payload) => typeof payload === "boolean", "isBoolean");
var isRegExp14 = /* @__PURE__ */ __name((payload) => payload instanceof RegExp, "isRegExp");
var isMap14 = /* @__PURE__ */ __name((payload) => payload instanceof Map, "isMap");
var isSet14 = /* @__PURE__ */ __name((payload) => payload instanceof Set, "isSet");
var isSymbol14 = /* @__PURE__ */ __name((payload) => getType15(payload) === "Symbol", "isSymbol");
var isDate14 = /* @__PURE__ */ __name((payload) => payload instanceof Date && !isNaN(payload.valueOf()), "isDate");
var isError14 = /* @__PURE__ */ __name((payload) => payload instanceof Error, "isError");
var isNaNValue14 = /* @__PURE__ */ __name((payload) => typeof payload === "number" && isNaN(payload), "isNaNValue");
var isPrimitive14 = /* @__PURE__ */ __name((payload) => isBoolean14(payload) || isNull15(payload) || isUndefined14(payload) || isNumber14(payload) || isString14(payload) || isSymbol14(payload), "isPrimitive");
var isBigint14 = /* @__PURE__ */ __name((payload) => typeof payload === "bigint", "isBigint");
var isInfinite14 = /* @__PURE__ */ __name((payload) => payload === Infinity || payload === -Infinity, "isInfinite");
var isTypedArray14 = /* @__PURE__ */ __name((payload) => ArrayBuffer.isView(payload) && !(payload instanceof DataView), "isTypedArray");
var isURL14 = /* @__PURE__ */ __name((payload) => payload instanceof URL, "isURL");
var escapeKey14 = /* @__PURE__ */ __name((key) => key.replace(/\\/g, "\\\\").replace(/\./g, "\\."), "escapeKey");
var stringifyPath14 = /* @__PURE__ */ __name((path) => path.map(String).map(escapeKey14).join("."), "stringifyPath");
var parsePath14 = /* @__PURE__ */ __name((string2, legacyPaths) => {
  const result = [];
  let segment = "";
  for (let i = 0; i < string2.length; i++) {
    let char = string2.charAt(i);
    if (!legacyPaths && char === "\\") {
      const escaped = string2.charAt(i + 1);
      if (escaped === "\\") {
        segment += "\\";
        i++;
        continue;
      } else if (escaped !== ".") {
        throw Error("invalid path");
      }
    }
    const isEscapedDot = char === "\\" && string2.charAt(i + 1) === ".";
    if (isEscapedDot) {
      segment += ".";
      i++;
      continue;
    }
    const isEndOfSegment = char === ".";
    if (isEndOfSegment) {
      result.push(segment);
      segment = "";
      continue;
    }
    segment += char;
  }
  const lastSegment = segment;
  result.push(lastSegment);
  return result;
}, "parsePath");
function simpleTransformation14(isApplicable, annotation, transform, untransform) {
  return {
    isApplicable,
    annotation,
    transform,
    untransform
  };
}
__name(simpleTransformation14, "simpleTransformation");
var simpleRules14 = [
  simpleTransformation14(isUndefined14, "undefined", () => null, () => {
    return;
  }),
  simpleTransformation14(isBigint14, "bigint", (v) => v.toString(), (v) => {
    if (typeof BigInt !== "undefined") {
      return BigInt(v);
    }
    console.error("Please add a BigInt polyfill.");
    return v;
  }),
  simpleTransformation14(isDate14, "Date", (v) => v.toISOString(), (v) => new Date(v)),
  simpleTransformation14(isError14, "Error", (v, superJson) => {
    const baseError = {
      name: v.name,
      message: v.message
    };
    if ("cause" in v) {
      baseError.cause = v.cause;
    }
    superJson.allowedErrorProps.forEach((prop) => {
      baseError[prop] = v[prop];
    });
    return baseError;
  }, (v, superJson) => {
    const e = new Error(v.message, { cause: v.cause });
    e.name = v.name;
    e.stack = v.stack;
    superJson.allowedErrorProps.forEach((prop) => {
      e[prop] = v[prop];
    });
    return e;
  }),
  simpleTransformation14(isRegExp14, "regexp", (v) => "" + v, (regex) => {
    const body = regex.slice(1, regex.lastIndexOf("/"));
    const flags2 = regex.slice(regex.lastIndexOf("/") + 1);
    return new RegExp(body, flags2);
  }),
  simpleTransformation14(isSet14, "set", (v) => [...v.values()], (v) => new Set(v)),
  simpleTransformation14(isMap14, "map", (v) => [...v.entries()], (v) => new Map(v)),
  simpleTransformation14((v) => isNaNValue14(v) || isInfinite14(v), "number", (v) => {
    if (isNaNValue14(v)) {
      return "NaN";
    }
    if (v > 0) {
      return "Infinity";
    } else {
      return "-Infinity";
    }
  }, Number),
  simpleTransformation14((v) => v === 0 && 1 / v === -Infinity, "number", () => {
    return "-0";
  }, Number),
  simpleTransformation14(isURL14, "URL", (v) => v.toString(), (v) => new URL(v))
];
function compositeTransformation14(isApplicable, annotation, transform, untransform) {
  return {
    isApplicable,
    annotation,
    transform,
    untransform
  };
}
__name(compositeTransformation14, "compositeTransformation");
var symbolRule14 = compositeTransformation14((s, superJson) => {
  if (isSymbol14(s)) {
    const isRegistered = !!superJson.symbolRegistry.getIdentifier(s);
    return isRegistered;
  }
  return false;
}, (s, superJson) => {
  const identifier = superJson.symbolRegistry.getIdentifier(s);
  return ["symbol", identifier];
}, (v) => v.description, (_, a, superJson) => {
  const value = superJson.symbolRegistry.getValue(a[1]);
  if (!value) {
    throw new Error("Trying to deserialize unknown symbol");
  }
  return value;
});
var constructorToName14 = [
  Int8Array,
  Uint8Array,
  Int16Array,
  Uint16Array,
  Int32Array,
  Uint32Array,
  Float32Array,
  Float64Array,
  Uint8ClampedArray
].reduce((obj, ctor) => {
  obj[ctor.name] = ctor;
  return obj;
}, {});
var typedArrayRule14 = compositeTransformation14(isTypedArray14, (v) => ["typed-array", v.constructor.name], (v) => [...v], (v, a) => {
  const ctor = constructorToName14[a[1]];
  if (!ctor) {
    throw new Error("Trying to deserialize unknown typed array");
  }
  return new ctor(v);
});
function isInstanceOfRegisteredClass14(potentialClass, superJson) {
  if (potentialClass?.constructor) {
    const isRegistered = !!superJson.classRegistry.getIdentifier(potentialClass.constructor);
    return isRegistered;
  }
  return false;
}
__name(isInstanceOfRegisteredClass14, "isInstanceOfRegisteredClass");
var classRule14 = compositeTransformation14(isInstanceOfRegisteredClass14, (clazz, superJson) => {
  const identifier = superJson.classRegistry.getIdentifier(clazz.constructor);
  return ["class", identifier];
}, (clazz, superJson) => {
  const allowedProps = superJson.classRegistry.getAllowedProps(clazz.constructor);
  if (!allowedProps) {
    return { ...clazz };
  }
  const result = {};
  allowedProps.forEach((prop) => {
    result[prop] = clazz[prop];
  });
  return result;
}, (v, a, superJson) => {
  const clazz = superJson.classRegistry.getValue(a[1]);
  if (!clazz) {
    throw new Error(`Trying to deserialize unknown class '${a[1]}' - check https://github.com/blitz-js/superjson/issues/116#issuecomment-773996564`);
  }
  return Object.assign(Object.create(clazz.prototype), v);
});
var customRule14 = compositeTransformation14((value, superJson) => {
  return !!superJson.customTransformerRegistry.findApplicable(value);
}, (value, superJson) => {
  const transformer = superJson.customTransformerRegistry.findApplicable(value);
  return ["custom", transformer.name];
}, (value, superJson) => {
  const transformer = superJson.customTransformerRegistry.findApplicable(value);
  return transformer.serialize(value);
}, (v, a, superJson) => {
  const transformer = superJson.customTransformerRegistry.findByName(a[1]);
  if (!transformer) {
    throw new Error("Trying to deserialize unknown custom value");
  }
  return transformer.deserialize(v);
});
var compositeRules14 = [classRule14, symbolRule14, customRule14, typedArrayRule14];
var transformValue14 = /* @__PURE__ */ __name((value, superJson) => {
  const applicableCompositeRule = findArr14(compositeRules14, (rule) => rule.isApplicable(value, superJson));
  if (applicableCompositeRule) {
    return {
      value: applicableCompositeRule.transform(value, superJson),
      type: applicableCompositeRule.annotation(value, superJson)
    };
  }
  const applicableSimpleRule = findArr14(simpleRules14, (rule) => rule.isApplicable(value, superJson));
  if (applicableSimpleRule) {
    return {
      value: applicableSimpleRule.transform(value, superJson),
      type: applicableSimpleRule.annotation
    };
  }
  return;
}, "transformValue");
var simpleRulesByAnnotation14 = {};
simpleRules14.forEach((rule) => {
  simpleRulesByAnnotation14[rule.annotation] = rule;
});
var untransformValue14 = /* @__PURE__ */ __name((json, type, superJson) => {
  if (isArray15(type)) {
    switch (type[0]) {
      case "symbol":
        return symbolRule14.untransform(json, type, superJson);
      case "class":
        return classRule14.untransform(json, type, superJson);
      case "custom":
        return customRule14.untransform(json, type, superJson);
      case "typed-array":
        return typedArrayRule14.untransform(json, type, superJson);
      default:
        throw new Error("Unknown transformation: " + type);
    }
  } else {
    const transformation = simpleRulesByAnnotation14[type];
    if (!transformation) {
      throw new Error("Unknown transformation: " + type);
    }
    return transformation.untransform(json, superJson);
  }
}, "untransformValue");
var getNthKey14 = /* @__PURE__ */ __name((value, n) => {
  if (n > value.size)
    throw new Error("index out of bounds");
  const keys = value.keys();
  while (n > 0) {
    keys.next();
    n--;
  }
  return keys.next().value;
}, "getNthKey");
function validatePath14(path) {
  if (includes14(path, "__proto__")) {
    throw new Error("__proto__ is not allowed as a property");
  }
  if (includes14(path, "prototype")) {
    throw new Error("prototype is not allowed as a property");
  }
  if (includes14(path, "constructor")) {
    throw new Error("constructor is not allowed as a property");
  }
}
__name(validatePath14, "validatePath");
var getDeep14 = /* @__PURE__ */ __name((object2, path) => {
  validatePath14(path);
  for (let i = 0; i < path.length; i++) {
    const key = path[i];
    if (isSet14(object2)) {
      object2 = getNthKey14(object2, +key);
    } else if (isMap14(object2)) {
      const row = +key;
      const type = +path[++i] === 0 ? "key" : "value";
      const keyOfRow = getNthKey14(object2, row);
      switch (type) {
        case "key":
          object2 = keyOfRow;
          break;
        case "value":
          object2 = object2.get(keyOfRow);
          break;
      }
    } else {
      object2 = object2[key];
    }
  }
  return object2;
}, "getDeep");
var setDeep14 = /* @__PURE__ */ __name((object2, path, mapper) => {
  validatePath14(path);
  if (path.length === 0) {
    return mapper(object2);
  }
  let parent = object2;
  for (let i = 0; i < path.length - 1; i++) {
    const key = path[i];
    if (isArray15(parent)) {
      const index = +key;
      parent = parent[index];
    } else if (isPlainObject15(parent)) {
      parent = parent[key];
    } else if (isSet14(parent)) {
      const row = +key;
      parent = getNthKey14(parent, row);
    } else if (isMap14(parent)) {
      const isEnd = i === path.length - 2;
      if (isEnd) {
        break;
      }
      const row = +key;
      const type = +path[++i] === 0 ? "key" : "value";
      const keyOfRow = getNthKey14(parent, row);
      switch (type) {
        case "key":
          parent = keyOfRow;
          break;
        case "value":
          parent = parent.get(keyOfRow);
          break;
      }
    }
  }
  const lastKey = path[path.length - 1];
  if (isArray15(parent)) {
    parent[+lastKey] = mapper(parent[+lastKey]);
  } else if (isPlainObject15(parent)) {
    parent[lastKey] = mapper(parent[lastKey]);
  }
  if (isSet14(parent)) {
    const oldValue = getNthKey14(parent, +lastKey);
    const newValue = mapper(oldValue);
    if (oldValue !== newValue) {
      parent.delete(oldValue);
      parent.add(newValue);
    }
  }
  if (isMap14(parent)) {
    const row = +path[path.length - 2];
    const keyToRow = getNthKey14(parent, row);
    const type = +lastKey === 0 ? "key" : "value";
    switch (type) {
      case "key": {
        const newKey = mapper(keyToRow);
        parent.set(newKey, parent.get(keyToRow));
        if (newKey !== keyToRow) {
          parent.delete(keyToRow);
        }
        break;
      }
      case "value": {
        parent.set(keyToRow, mapper(parent.get(keyToRow)));
        break;
      }
    }
  }
  return object2;
}, "setDeep");
var enableLegacyPaths14 = /* @__PURE__ */ __name((version4) => version4 < 1, "enableLegacyPaths");
function traverse14(tree, walker26, version4, origin = []) {
  if (!tree) {
    return;
  }
  const legacyPaths = enableLegacyPaths14(version4);
  if (!isArray15(tree)) {
    forEach14(tree, (subtree, key) => traverse14(subtree, walker26, version4, [
      ...origin,
      ...parsePath14(key, legacyPaths)
    ]));
    return;
  }
  const [nodeValue, children] = tree;
  if (children) {
    forEach14(children, (child, key) => {
      traverse14(child, walker26, version4, [
        ...origin,
        ...parsePath14(key, legacyPaths)
      ]);
    });
  }
  walker26(nodeValue, origin);
}
__name(traverse14, "traverse");
function applyValueAnnotations14(plain, annotations, version4, superJson) {
  traverse14(annotations, (type, path) => {
    plain = setDeep14(plain, path, (v) => untransformValue14(v, type, superJson));
  }, version4);
  return plain;
}
__name(applyValueAnnotations14, "applyValueAnnotations");
function applyReferentialEqualityAnnotations14(plain, annotations, version4) {
  const legacyPaths = enableLegacyPaths14(version4);
  function apply(identicalPaths, path) {
    const object2 = getDeep14(plain, parsePath14(path, legacyPaths));
    identicalPaths.map((path2) => parsePath14(path2, legacyPaths)).forEach((identicalObjectPath) => {
      plain = setDeep14(plain, identicalObjectPath, () => object2);
    });
  }
  __name(apply, "apply");
  if (isArray15(annotations)) {
    const [root, other] = annotations;
    root.forEach((identicalPath) => {
      plain = setDeep14(plain, parsePath14(identicalPath, legacyPaths), () => plain);
    });
    if (other) {
      forEach14(other, apply);
    }
  } else {
    forEach14(annotations, apply);
  }
  return plain;
}
__name(applyReferentialEqualityAnnotations14, "applyReferentialEqualityAnnotations");
var isDeep14 = /* @__PURE__ */ __name((object2, superJson) => isPlainObject15(object2) || isArray15(object2) || isMap14(object2) || isSet14(object2) || isError14(object2) || isInstanceOfRegisteredClass14(object2, superJson), "isDeep");
function addIdentity14(object2, path, identities) {
  const existingSet = identities.get(object2);
  if (existingSet) {
    existingSet.push(path);
  } else {
    identities.set(object2, [path]);
  }
}
__name(addIdentity14, "addIdentity");
function generateReferentialEqualityAnnotations14(identitites, dedupe) {
  const result = {};
  let rootEqualityPaths = void 0;
  identitites.forEach((paths) => {
    if (paths.length <= 1) {
      return;
    }
    if (!dedupe) {
      paths = paths.map((path) => path.map(String)).sort((a, b) => a.length - b.length);
    }
    const [representativePath, ...identicalPaths] = paths;
    if (representativePath.length === 0) {
      rootEqualityPaths = identicalPaths.map(stringifyPath14);
    } else {
      result[stringifyPath14(representativePath)] = identicalPaths.map(stringifyPath14);
    }
  });
  if (rootEqualityPaths) {
    if (isEmptyObject14(result)) {
      return [rootEqualityPaths];
    } else {
      return [rootEqualityPaths, result];
    }
  } else {
    return isEmptyObject14(result) ? void 0 : result;
  }
}
__name(generateReferentialEqualityAnnotations14, "generateReferentialEqualityAnnotations");
var walker14 = /* @__PURE__ */ __name((object2, identities, superJson, dedupe, path = [], objectsInThisPath = [], seenObjects = /* @__PURE__ */ new Map()) => {
  const primitive = isPrimitive14(object2);
  if (!primitive) {
    addIdentity14(object2, path, identities);
    const seen = seenObjects.get(object2);
    if (seen) {
      return dedupe ? {
        transformedValue: null
      } : seen;
    }
  }
  if (!isDeep14(object2, superJson)) {
    const transformed2 = transformValue14(object2, superJson);
    const result2 = transformed2 ? {
      transformedValue: transformed2.value,
      annotations: [transformed2.type]
    } : {
      transformedValue: object2
    };
    if (!primitive) {
      seenObjects.set(object2, result2);
    }
    return result2;
  }
  if (includes14(objectsInThisPath, object2)) {
    return {
      transformedValue: null
    };
  }
  const transformationResult = transformValue14(object2, superJson);
  const transformed = transformationResult?.value ?? object2;
  const transformedValue = isArray15(transformed) ? [] : {};
  const innerAnnotations = {};
  forEach14(transformed, (value, index) => {
    if (index === "__proto__" || index === "constructor" || index === "prototype") {
      throw new Error(`Detected property ${index}. This is a prototype pollution risk, please remove it from your object.`);
    }
    const recursiveResult = walker14(value, identities, superJson, dedupe, [...path, index], [...objectsInThisPath, object2], seenObjects);
    transformedValue[index] = recursiveResult.transformedValue;
    if (isArray15(recursiveResult.annotations)) {
      innerAnnotations[escapeKey14(index)] = recursiveResult.annotations;
    } else if (isPlainObject15(recursiveResult.annotations)) {
      forEach14(recursiveResult.annotations, (tree, key) => {
        innerAnnotations[escapeKey14(index) + "." + key] = tree;
      });
    }
  });
  const result = isEmptyObject14(innerAnnotations) ? {
    transformedValue,
    annotations: transformationResult ? [transformationResult.type] : void 0
  } : {
    transformedValue,
    annotations: transformationResult ? [transformationResult.type, innerAnnotations] : innerAnnotations
  };
  if (!primitive) {
    seenObjects.set(object2, result);
  }
  return result;
}, "walker");
function getType214(payload) {
  return Object.prototype.toString.call(payload).slice(8, -1);
}
__name(getType214, "getType2");
function isArray214(payload) {
  return getType214(payload) === "Array";
}
__name(isArray214, "isArray2");
function isPlainObject214(payload) {
  if (getType214(payload) !== "Object")
    return false;
  const prototype = Object.getPrototypeOf(payload);
  return !!prototype && prototype.constructor === Object && prototype === Object.prototype;
}
__name(isPlainObject214, "isPlainObject2");
function assignProp14(carry, key, newVal, originalObject, includeNonenumerable) {
  const propType = {}.propertyIsEnumerable.call(originalObject, key) ? "enumerable" : "nonenumerable";
  if (propType === "enumerable")
    carry[key] = newVal;
  if (includeNonenumerable && propType === "nonenumerable") {
    Object.defineProperty(carry, key, {
      value: newVal,
      enumerable: false,
      writable: true,
      configurable: true
    });
  }
}
__name(assignProp14, "assignProp");
function copy14(target, options = {}) {
  if (isArray214(target)) {
    return target.map((item) => copy14(item, options));
  }
  if (!isPlainObject214(target)) {
    return target;
  }
  const props = Object.getOwnPropertyNames(target);
  const symbols = Object.getOwnPropertySymbols(target);
  return [...props, ...symbols].reduce((carry, key) => {
    if (key === "__proto__")
      return carry;
    if (isArray214(options.props) && !options.props.includes(key)) {
      return carry;
    }
    const val = target[key];
    const newVal = copy14(val, options);
    assignProp14(carry, key, newVal, target, options.nonenumerable);
    return carry;
  }, {});
}
__name(copy14, "copy");
var SuperJSON14 = class {
  static {
    __name(this, "SuperJSON");
  }
  constructor({ dedupe = false } = {}) {
    this.classRegistry = new ClassRegistry14();
    this.symbolRegistry = new Registry14((s) => s.description ?? "");
    this.customTransformerRegistry = new CustomTransformerRegistry14();
    this.allowedErrorProps = [];
    this.dedupe = dedupe;
  }
  serialize(object2) {
    const identities = /* @__PURE__ */ new Map();
    const output = walker14(object2, identities, this, this.dedupe);
    const res = {
      json: output.transformedValue
    };
    if (output.annotations) {
      res.meta = {
        ...res.meta,
        values: output.annotations
      };
    }
    const equalityAnnotations = generateReferentialEqualityAnnotations14(identities, this.dedupe);
    if (equalityAnnotations) {
      res.meta = {
        ...res.meta,
        referentialEqualities: equalityAnnotations
      };
    }
    if (res.meta)
      res.meta.v = 1;
    return res;
  }
  deserialize(payload, options) {
    const { json, meta } = payload;
    let result = options?.inPlace ? json : copy14(json);
    if (meta?.values) {
      result = applyValueAnnotations14(result, meta.values, meta.v ?? 0, this);
    }
    if (meta?.referentialEqualities) {
      result = applyReferentialEqualityAnnotations14(result, meta.referentialEqualities, meta.v ?? 0);
    }
    return result;
  }
  stringify(object2) {
    return JSON.stringify(this.serialize(object2));
  }
  parse(string2) {
    return this.deserialize(JSON.parse(string2), { inPlace: true });
  }
  registerClass(v, options) {
    this.classRegistry.register(v, options);
  }
  registerSymbol(v, identifier) {
    this.symbolRegistry.register(v, identifier);
  }
  registerCustom(transformer, name) {
    this.customTransformerRegistry.register({
      name,
      ...transformer
    });
  }
  allowErrorProps(...props) {
    this.allowedErrorProps.push(...props);
  }
};
SuperJSON14.defaultInstance = new SuperJSON14();
SuperJSON14.serialize = SuperJSON14.defaultInstance.serialize.bind(SuperJSON14.defaultInstance);
SuperJSON14.deserialize = SuperJSON14.defaultInstance.deserialize.bind(SuperJSON14.defaultInstance);
SuperJSON14.stringify = SuperJSON14.defaultInstance.stringify.bind(SuperJSON14.defaultInstance);
SuperJSON14.parse = SuperJSON14.defaultInstance.parse.bind(SuperJSON14.defaultInstance);
SuperJSON14.registerClass = SuperJSON14.defaultInstance.registerClass.bind(SuperJSON14.defaultInstance);
SuperJSON14.registerSymbol = SuperJSON14.defaultInstance.registerSymbol.bind(SuperJSON14.defaultInstance);
SuperJSON14.registerCustom = SuperJSON14.defaultInstance.registerCustom.bind(SuperJSON14.defaultInstance);
SuperJSON14.allowErrorProps = SuperJSON14.defaultInstance.allowErrorProps.bind(SuperJSON14.defaultInstance);
var dist_default14 = SuperJSON14;
var serialize14 = SuperJSON14.serialize;
var deserialize14 = SuperJSON14.deserialize;
var stringify15 = SuperJSON14.stringify;
var parse14 = SuperJSON14.parse;
var registerClass14 = SuperJSON14.registerClass;
var registerCustom14 = SuperJSON14.registerCustom;
var registerSymbol14 = SuperJSON14.registerSymbol;
var allowErrorProps14 = SuperJSON14.allowErrorProps;
function parseData14(formData) {
  const propsArray = [];
  if (!formData)
    return propsArray;
  const batchsIDs = [];
  for (const [key, value] of Array.from(formData.entries())) {
    if (key.startsWith("FILE_"))
      propsArray.push(value);
    else if (key.startsWith("FILES_")) {
      if (batchsIDs.includes(key))
        continue;
      batchsIDs.push(key);
      propsArray.push(formData.getAll(key));
    } else {
      propsArray.push(JSON.parse(decodeURI(value)));
    }
  }
  return propsArray;
}
__name(parseData14, "parseData");
function paramsFromURL14(url) {
  const params = url.searchParams.entries().toArray().map(([_, v]) => v);
  return params.map((param) => JSON.parse(decodeURIComponent(param)));
}
__name(paramsFromURL14, "paramsFromURL");
async function WrapRequestHandler14(context2, endpoint) {
  const isServerAction = context2.request.headers.get("x-server-action") === "true";
  if (!isServerAction) {
    return new Response("Not Found", { status: 404 });
  }
  const parsedData = context2.request.method === "GET" || context2.request.method === "HEAD" ? paramsFromURL14(new URL(context2.request.url)) : parseData14(context2.request.headers.get("content-type") ? await context2.request.formData() : void 0);
  const missingProps = endpoint.length - parsedData.length;
  for (let i = 0; i < missingProps; i++) {
    parsedData.push(void 0);
  }
  parsedData.push(context2);
  const result = await endpoint(...parsedData);
  switch (typeof result) {
    case "string":
    case "number":
    case "boolean":
    case "bigint":
      return new Response(dist_default14.stringify(result), {
        headers: {
          "Content-Type": "application/json",
          dataType: "json"
        }
      });
    case "undefined":
      return new Response(null, { status: 204 });
    case "object":
      if (result instanceof Response) {
        result.headers.set("dataType", "response");
        return result;
      } else if (result instanceof Blob) {
        const res = new Response(await result.arrayBuffer());
        res.headers.set("dataType", "blob");
        res.headers.set("Content-Type", result.type);
        return res;
      } else if (result instanceof File) {
        return new Response(await result.arrayBuffer(), {
          headers: {
            "Content-Type": result.type,
            dataType: "file",
            fileData: JSON.stringify({
              name: result.name,
              lastModified: result.lastModified
            })
          }
        });
      } else {
        return new Response(dist_default14.stringify(result), {
          headers: { "Content-Type": "application/json", dataType: "json" }
        });
      }
    default:
      throw new Error(`Unsupported return type from action: ${typeof result}`);
  }
}
__name(WrapRequestHandler14, "WrapRequestHandler");
var onRequest14 = /* @__PURE__ */ __name(async (context2) => {
  const method = context2.request.method;
  const options = {
    GET: typeof GET6 === "function" ? GET6 : void 0,
    POST: typeof POST === "function" ? POST : void 0,
    PUT: typeof PUT5 === "function" ? PUT5 : void 0,
    DELETE: typeof DELETE10 === "function" ? DELETE10 : void 0,
    PATCH: typeof PATCH === "function" ? PATCH : void 0,
    HEAD: typeof HEAD === "function" ? HEAD : void 0,
    OPTIONS: typeof OPTIONS === "function" ? OPTIONS : void 0
  };
  if (!options[method]) {
    return new Response(`Method "${method}" Not Allowed`, {
      status: 405
    });
  }
  return await WrapRequestHandler14(context2, options[method]);
}, "onRequest");

// api/users/info.js
async function GET7({ user_ids }) {
  const ctx = getContext(arguments);
  const users = await ctx.data.client.getManyUserById(user_ids);
  if (users instanceof Error) {
    return {
      success: false,
      error: users.message
    };
  }
  return {
    success: true,
    data: users.data.users.map((user) => {
      const { session_private, ...authorized } = user;
      return authorized;
    })
  };
}
__name(GET7, "GET");
var DoubleIndexedKV15 = class {
  static {
    __name(this, "DoubleIndexedKV");
  }
  constructor() {
    this.keyToValue = /* @__PURE__ */ new Map();
    this.valueToKey = /* @__PURE__ */ new Map();
  }
  set(key, value) {
    this.keyToValue.set(key, value);
    this.valueToKey.set(value, key);
  }
  getByKey(key) {
    return this.keyToValue.get(key);
  }
  getByValue(value) {
    return this.valueToKey.get(value);
  }
  clear() {
    this.keyToValue.clear();
    this.valueToKey.clear();
  }
};
var Registry15 = class {
  static {
    __name(this, "Registry");
  }
  constructor(generateIdentifier) {
    this.generateIdentifier = generateIdentifier;
    this.kv = new DoubleIndexedKV15();
  }
  register(value, identifier) {
    if (this.kv.getByValue(value)) {
      return;
    }
    if (!identifier) {
      identifier = this.generateIdentifier(value);
    }
    this.kv.set(identifier, value);
  }
  clear() {
    this.kv.clear();
  }
  getIdentifier(value) {
    return this.kv.getByValue(value);
  }
  getValue(identifier) {
    return this.kv.getByKey(identifier);
  }
};
var ClassRegistry15 = class extends Registry15 {
  static {
    __name(this, "ClassRegistry");
  }
  constructor() {
    super((c) => c.name);
    this.classToAllowedProps = /* @__PURE__ */ new Map();
  }
  register(value, options) {
    if (typeof options === "object") {
      if (options.allowProps) {
        this.classToAllowedProps.set(value, options.allowProps);
      }
      super.register(value, options.identifier);
    } else {
      super.register(value, options);
    }
  }
  getAllowedProps(value) {
    return this.classToAllowedProps.get(value);
  }
};
function valuesOfObj15(record) {
  if ("values" in Object) {
    return Object.values(record);
  }
  const values = [];
  for (const key in record) {
    if (record.hasOwnProperty(key)) {
      values.push(record[key]);
    }
  }
  return values;
}
__name(valuesOfObj15, "valuesOfObj");
function find15(record, predicate) {
  const values = valuesOfObj15(record);
  if ("find" in values) {
    return values.find(predicate);
  }
  const valuesNotNever = values;
  for (let i = 0; i < valuesNotNever.length; i++) {
    const value = valuesNotNever[i];
    if (predicate(value)) {
      return value;
    }
  }
  return;
}
__name(find15, "find");
function forEach15(record, run) {
  Object.entries(record).forEach(([key, value]) => run(value, key));
}
__name(forEach15, "forEach");
function includes15(arr, value) {
  return arr.indexOf(value) !== -1;
}
__name(includes15, "includes");
function findArr15(record, predicate) {
  for (let i = 0; i < record.length; i++) {
    const value = record[i];
    if (predicate(value)) {
      return value;
    }
  }
  return;
}
__name(findArr15, "findArr");
var CustomTransformerRegistry15 = class {
  static {
    __name(this, "CustomTransformerRegistry");
  }
  constructor() {
    this.transfomers = {};
  }
  register(transformer) {
    this.transfomers[transformer.name] = transformer;
  }
  findApplicable(v) {
    return find15(this.transfomers, (transformer) => transformer.isApplicable(v));
  }
  findByName(name) {
    return this.transfomers[name];
  }
};
var getType16 = /* @__PURE__ */ __name((payload) => Object.prototype.toString.call(payload).slice(8, -1), "getType");
var isUndefined15 = /* @__PURE__ */ __name((payload) => typeof payload === "undefined", "isUndefined");
var isNull16 = /* @__PURE__ */ __name((payload) => payload === null, "isNull");
var isPlainObject16 = /* @__PURE__ */ __name((payload) => {
  if (typeof payload !== "object" || payload === null)
    return false;
  if (payload === Object.prototype)
    return false;
  if (Object.getPrototypeOf(payload) === null)
    return true;
  return Object.getPrototypeOf(payload) === Object.prototype;
}, "isPlainObject");
var isEmptyObject15 = /* @__PURE__ */ __name((payload) => isPlainObject16(payload) && Object.keys(payload).length === 0, "isEmptyObject");
var isArray16 = /* @__PURE__ */ __name((payload) => Array.isArray(payload), "isArray");
var isString15 = /* @__PURE__ */ __name((payload) => typeof payload === "string", "isString");
var isNumber15 = /* @__PURE__ */ __name((payload) => typeof payload === "number" && !isNaN(payload), "isNumber");
var isBoolean15 = /* @__PURE__ */ __name((payload) => typeof payload === "boolean", "isBoolean");
var isRegExp15 = /* @__PURE__ */ __name((payload) => payload instanceof RegExp, "isRegExp");
var isMap15 = /* @__PURE__ */ __name((payload) => payload instanceof Map, "isMap");
var isSet15 = /* @__PURE__ */ __name((payload) => payload instanceof Set, "isSet");
var isSymbol15 = /* @__PURE__ */ __name((payload) => getType16(payload) === "Symbol", "isSymbol");
var isDate15 = /* @__PURE__ */ __name((payload) => payload instanceof Date && !isNaN(payload.valueOf()), "isDate");
var isError15 = /* @__PURE__ */ __name((payload) => payload instanceof Error, "isError");
var isNaNValue15 = /* @__PURE__ */ __name((payload) => typeof payload === "number" && isNaN(payload), "isNaNValue");
var isPrimitive15 = /* @__PURE__ */ __name((payload) => isBoolean15(payload) || isNull16(payload) || isUndefined15(payload) || isNumber15(payload) || isString15(payload) || isSymbol15(payload), "isPrimitive");
var isBigint15 = /* @__PURE__ */ __name((payload) => typeof payload === "bigint", "isBigint");
var isInfinite15 = /* @__PURE__ */ __name((payload) => payload === Infinity || payload === -Infinity, "isInfinite");
var isTypedArray15 = /* @__PURE__ */ __name((payload) => ArrayBuffer.isView(payload) && !(payload instanceof DataView), "isTypedArray");
var isURL15 = /* @__PURE__ */ __name((payload) => payload instanceof URL, "isURL");
var escapeKey15 = /* @__PURE__ */ __name((key) => key.replace(/\\/g, "\\\\").replace(/\./g, "\\."), "escapeKey");
var stringifyPath15 = /* @__PURE__ */ __name((path) => path.map(String).map(escapeKey15).join("."), "stringifyPath");
var parsePath15 = /* @__PURE__ */ __name((string2, legacyPaths) => {
  const result = [];
  let segment = "";
  for (let i = 0; i < string2.length; i++) {
    let char = string2.charAt(i);
    if (!legacyPaths && char === "\\") {
      const escaped = string2.charAt(i + 1);
      if (escaped === "\\") {
        segment += "\\";
        i++;
        continue;
      } else if (escaped !== ".") {
        throw Error("invalid path");
      }
    }
    const isEscapedDot = char === "\\" && string2.charAt(i + 1) === ".";
    if (isEscapedDot) {
      segment += ".";
      i++;
      continue;
    }
    const isEndOfSegment = char === ".";
    if (isEndOfSegment) {
      result.push(segment);
      segment = "";
      continue;
    }
    segment += char;
  }
  const lastSegment = segment;
  result.push(lastSegment);
  return result;
}, "parsePath");
function simpleTransformation15(isApplicable, annotation, transform, untransform) {
  return {
    isApplicable,
    annotation,
    transform,
    untransform
  };
}
__name(simpleTransformation15, "simpleTransformation");
var simpleRules15 = [
  simpleTransformation15(isUndefined15, "undefined", () => null, () => {
    return;
  }),
  simpleTransformation15(isBigint15, "bigint", (v) => v.toString(), (v) => {
    if (typeof BigInt !== "undefined") {
      return BigInt(v);
    }
    console.error("Please add a BigInt polyfill.");
    return v;
  }),
  simpleTransformation15(isDate15, "Date", (v) => v.toISOString(), (v) => new Date(v)),
  simpleTransformation15(isError15, "Error", (v, superJson) => {
    const baseError = {
      name: v.name,
      message: v.message
    };
    if ("cause" in v) {
      baseError.cause = v.cause;
    }
    superJson.allowedErrorProps.forEach((prop) => {
      baseError[prop] = v[prop];
    });
    return baseError;
  }, (v, superJson) => {
    const e = new Error(v.message, { cause: v.cause });
    e.name = v.name;
    e.stack = v.stack;
    superJson.allowedErrorProps.forEach((prop) => {
      e[prop] = v[prop];
    });
    return e;
  }),
  simpleTransformation15(isRegExp15, "regexp", (v) => "" + v, (regex) => {
    const body = regex.slice(1, regex.lastIndexOf("/"));
    const flags2 = regex.slice(regex.lastIndexOf("/") + 1);
    return new RegExp(body, flags2);
  }),
  simpleTransformation15(isSet15, "set", (v) => [...v.values()], (v) => new Set(v)),
  simpleTransformation15(isMap15, "map", (v) => [...v.entries()], (v) => new Map(v)),
  simpleTransformation15((v) => isNaNValue15(v) || isInfinite15(v), "number", (v) => {
    if (isNaNValue15(v)) {
      return "NaN";
    }
    if (v > 0) {
      return "Infinity";
    } else {
      return "-Infinity";
    }
  }, Number),
  simpleTransformation15((v) => v === 0 && 1 / v === -Infinity, "number", () => {
    return "-0";
  }, Number),
  simpleTransformation15(isURL15, "URL", (v) => v.toString(), (v) => new URL(v))
];
function compositeTransformation15(isApplicable, annotation, transform, untransform) {
  return {
    isApplicable,
    annotation,
    transform,
    untransform
  };
}
__name(compositeTransformation15, "compositeTransformation");
var symbolRule15 = compositeTransformation15((s, superJson) => {
  if (isSymbol15(s)) {
    const isRegistered = !!superJson.symbolRegistry.getIdentifier(s);
    return isRegistered;
  }
  return false;
}, (s, superJson) => {
  const identifier = superJson.symbolRegistry.getIdentifier(s);
  return ["symbol", identifier];
}, (v) => v.description, (_, a, superJson) => {
  const value = superJson.symbolRegistry.getValue(a[1]);
  if (!value) {
    throw new Error("Trying to deserialize unknown symbol");
  }
  return value;
});
var constructorToName15 = [
  Int8Array,
  Uint8Array,
  Int16Array,
  Uint16Array,
  Int32Array,
  Uint32Array,
  Float32Array,
  Float64Array,
  Uint8ClampedArray
].reduce((obj, ctor) => {
  obj[ctor.name] = ctor;
  return obj;
}, {});
var typedArrayRule15 = compositeTransformation15(isTypedArray15, (v) => ["typed-array", v.constructor.name], (v) => [...v], (v, a) => {
  const ctor = constructorToName15[a[1]];
  if (!ctor) {
    throw new Error("Trying to deserialize unknown typed array");
  }
  return new ctor(v);
});
function isInstanceOfRegisteredClass15(potentialClass, superJson) {
  if (potentialClass?.constructor) {
    const isRegistered = !!superJson.classRegistry.getIdentifier(potentialClass.constructor);
    return isRegistered;
  }
  return false;
}
__name(isInstanceOfRegisteredClass15, "isInstanceOfRegisteredClass");
var classRule15 = compositeTransformation15(isInstanceOfRegisteredClass15, (clazz, superJson) => {
  const identifier = superJson.classRegistry.getIdentifier(clazz.constructor);
  return ["class", identifier];
}, (clazz, superJson) => {
  const allowedProps = superJson.classRegistry.getAllowedProps(clazz.constructor);
  if (!allowedProps) {
    return { ...clazz };
  }
  const result = {};
  allowedProps.forEach((prop) => {
    result[prop] = clazz[prop];
  });
  return result;
}, (v, a, superJson) => {
  const clazz = superJson.classRegistry.getValue(a[1]);
  if (!clazz) {
    throw new Error(`Trying to deserialize unknown class '${a[1]}' - check https://github.com/blitz-js/superjson/issues/116#issuecomment-773996564`);
  }
  return Object.assign(Object.create(clazz.prototype), v);
});
var customRule15 = compositeTransformation15((value, superJson) => {
  return !!superJson.customTransformerRegistry.findApplicable(value);
}, (value, superJson) => {
  const transformer = superJson.customTransformerRegistry.findApplicable(value);
  return ["custom", transformer.name];
}, (value, superJson) => {
  const transformer = superJson.customTransformerRegistry.findApplicable(value);
  return transformer.serialize(value);
}, (v, a, superJson) => {
  const transformer = superJson.customTransformerRegistry.findByName(a[1]);
  if (!transformer) {
    throw new Error("Trying to deserialize unknown custom value");
  }
  return transformer.deserialize(v);
});
var compositeRules15 = [classRule15, symbolRule15, customRule15, typedArrayRule15];
var transformValue15 = /* @__PURE__ */ __name((value, superJson) => {
  const applicableCompositeRule = findArr15(compositeRules15, (rule) => rule.isApplicable(value, superJson));
  if (applicableCompositeRule) {
    return {
      value: applicableCompositeRule.transform(value, superJson),
      type: applicableCompositeRule.annotation(value, superJson)
    };
  }
  const applicableSimpleRule = findArr15(simpleRules15, (rule) => rule.isApplicable(value, superJson));
  if (applicableSimpleRule) {
    return {
      value: applicableSimpleRule.transform(value, superJson),
      type: applicableSimpleRule.annotation
    };
  }
  return;
}, "transformValue");
var simpleRulesByAnnotation15 = {};
simpleRules15.forEach((rule) => {
  simpleRulesByAnnotation15[rule.annotation] = rule;
});
var untransformValue15 = /* @__PURE__ */ __name((json, type, superJson) => {
  if (isArray16(type)) {
    switch (type[0]) {
      case "symbol":
        return symbolRule15.untransform(json, type, superJson);
      case "class":
        return classRule15.untransform(json, type, superJson);
      case "custom":
        return customRule15.untransform(json, type, superJson);
      case "typed-array":
        return typedArrayRule15.untransform(json, type, superJson);
      default:
        throw new Error("Unknown transformation: " + type);
    }
  } else {
    const transformation = simpleRulesByAnnotation15[type];
    if (!transformation) {
      throw new Error("Unknown transformation: " + type);
    }
    return transformation.untransform(json, superJson);
  }
}, "untransformValue");
var getNthKey15 = /* @__PURE__ */ __name((value, n) => {
  if (n > value.size)
    throw new Error("index out of bounds");
  const keys = value.keys();
  while (n > 0) {
    keys.next();
    n--;
  }
  return keys.next().value;
}, "getNthKey");
function validatePath15(path) {
  if (includes15(path, "__proto__")) {
    throw new Error("__proto__ is not allowed as a property");
  }
  if (includes15(path, "prototype")) {
    throw new Error("prototype is not allowed as a property");
  }
  if (includes15(path, "constructor")) {
    throw new Error("constructor is not allowed as a property");
  }
}
__name(validatePath15, "validatePath");
var getDeep15 = /* @__PURE__ */ __name((object2, path) => {
  validatePath15(path);
  for (let i = 0; i < path.length; i++) {
    const key = path[i];
    if (isSet15(object2)) {
      object2 = getNthKey15(object2, +key);
    } else if (isMap15(object2)) {
      const row = +key;
      const type = +path[++i] === 0 ? "key" : "value";
      const keyOfRow = getNthKey15(object2, row);
      switch (type) {
        case "key":
          object2 = keyOfRow;
          break;
        case "value":
          object2 = object2.get(keyOfRow);
          break;
      }
    } else {
      object2 = object2[key];
    }
  }
  return object2;
}, "getDeep");
var setDeep15 = /* @__PURE__ */ __name((object2, path, mapper) => {
  validatePath15(path);
  if (path.length === 0) {
    return mapper(object2);
  }
  let parent = object2;
  for (let i = 0; i < path.length - 1; i++) {
    const key = path[i];
    if (isArray16(parent)) {
      const index = +key;
      parent = parent[index];
    } else if (isPlainObject16(parent)) {
      parent = parent[key];
    } else if (isSet15(parent)) {
      const row = +key;
      parent = getNthKey15(parent, row);
    } else if (isMap15(parent)) {
      const isEnd = i === path.length - 2;
      if (isEnd) {
        break;
      }
      const row = +key;
      const type = +path[++i] === 0 ? "key" : "value";
      const keyOfRow = getNthKey15(parent, row);
      switch (type) {
        case "key":
          parent = keyOfRow;
          break;
        case "value":
          parent = parent.get(keyOfRow);
          break;
      }
    }
  }
  const lastKey = path[path.length - 1];
  if (isArray16(parent)) {
    parent[+lastKey] = mapper(parent[+lastKey]);
  } else if (isPlainObject16(parent)) {
    parent[lastKey] = mapper(parent[lastKey]);
  }
  if (isSet15(parent)) {
    const oldValue = getNthKey15(parent, +lastKey);
    const newValue = mapper(oldValue);
    if (oldValue !== newValue) {
      parent.delete(oldValue);
      parent.add(newValue);
    }
  }
  if (isMap15(parent)) {
    const row = +path[path.length - 2];
    const keyToRow = getNthKey15(parent, row);
    const type = +lastKey === 0 ? "key" : "value";
    switch (type) {
      case "key": {
        const newKey = mapper(keyToRow);
        parent.set(newKey, parent.get(keyToRow));
        if (newKey !== keyToRow) {
          parent.delete(keyToRow);
        }
        break;
      }
      case "value": {
        parent.set(keyToRow, mapper(parent.get(keyToRow)));
        break;
      }
    }
  }
  return object2;
}, "setDeep");
var enableLegacyPaths15 = /* @__PURE__ */ __name((version4) => version4 < 1, "enableLegacyPaths");
function traverse15(tree, walker26, version4, origin = []) {
  if (!tree) {
    return;
  }
  const legacyPaths = enableLegacyPaths15(version4);
  if (!isArray16(tree)) {
    forEach15(tree, (subtree, key) => traverse15(subtree, walker26, version4, [
      ...origin,
      ...parsePath15(key, legacyPaths)
    ]));
    return;
  }
  const [nodeValue, children] = tree;
  if (children) {
    forEach15(children, (child, key) => {
      traverse15(child, walker26, version4, [
        ...origin,
        ...parsePath15(key, legacyPaths)
      ]);
    });
  }
  walker26(nodeValue, origin);
}
__name(traverse15, "traverse");
function applyValueAnnotations15(plain, annotations, version4, superJson) {
  traverse15(annotations, (type, path) => {
    plain = setDeep15(plain, path, (v) => untransformValue15(v, type, superJson));
  }, version4);
  return plain;
}
__name(applyValueAnnotations15, "applyValueAnnotations");
function applyReferentialEqualityAnnotations15(plain, annotations, version4) {
  const legacyPaths = enableLegacyPaths15(version4);
  function apply(identicalPaths, path) {
    const object2 = getDeep15(plain, parsePath15(path, legacyPaths));
    identicalPaths.map((path2) => parsePath15(path2, legacyPaths)).forEach((identicalObjectPath) => {
      plain = setDeep15(plain, identicalObjectPath, () => object2);
    });
  }
  __name(apply, "apply");
  if (isArray16(annotations)) {
    const [root, other] = annotations;
    root.forEach((identicalPath) => {
      plain = setDeep15(plain, parsePath15(identicalPath, legacyPaths), () => plain);
    });
    if (other) {
      forEach15(other, apply);
    }
  } else {
    forEach15(annotations, apply);
  }
  return plain;
}
__name(applyReferentialEqualityAnnotations15, "applyReferentialEqualityAnnotations");
var isDeep15 = /* @__PURE__ */ __name((object2, superJson) => isPlainObject16(object2) || isArray16(object2) || isMap15(object2) || isSet15(object2) || isError15(object2) || isInstanceOfRegisteredClass15(object2, superJson), "isDeep");
function addIdentity15(object2, path, identities) {
  const existingSet = identities.get(object2);
  if (existingSet) {
    existingSet.push(path);
  } else {
    identities.set(object2, [path]);
  }
}
__name(addIdentity15, "addIdentity");
function generateReferentialEqualityAnnotations15(identitites, dedupe) {
  const result = {};
  let rootEqualityPaths = void 0;
  identitites.forEach((paths) => {
    if (paths.length <= 1) {
      return;
    }
    if (!dedupe) {
      paths = paths.map((path) => path.map(String)).sort((a, b) => a.length - b.length);
    }
    const [representativePath, ...identicalPaths] = paths;
    if (representativePath.length === 0) {
      rootEqualityPaths = identicalPaths.map(stringifyPath15);
    } else {
      result[stringifyPath15(representativePath)] = identicalPaths.map(stringifyPath15);
    }
  });
  if (rootEqualityPaths) {
    if (isEmptyObject15(result)) {
      return [rootEqualityPaths];
    } else {
      return [rootEqualityPaths, result];
    }
  } else {
    return isEmptyObject15(result) ? void 0 : result;
  }
}
__name(generateReferentialEqualityAnnotations15, "generateReferentialEqualityAnnotations");
var walker15 = /* @__PURE__ */ __name((object2, identities, superJson, dedupe, path = [], objectsInThisPath = [], seenObjects = /* @__PURE__ */ new Map()) => {
  const primitive = isPrimitive15(object2);
  if (!primitive) {
    addIdentity15(object2, path, identities);
    const seen = seenObjects.get(object2);
    if (seen) {
      return dedupe ? {
        transformedValue: null
      } : seen;
    }
  }
  if (!isDeep15(object2, superJson)) {
    const transformed2 = transformValue15(object2, superJson);
    const result2 = transformed2 ? {
      transformedValue: transformed2.value,
      annotations: [transformed2.type]
    } : {
      transformedValue: object2
    };
    if (!primitive) {
      seenObjects.set(object2, result2);
    }
    return result2;
  }
  if (includes15(objectsInThisPath, object2)) {
    return {
      transformedValue: null
    };
  }
  const transformationResult = transformValue15(object2, superJson);
  const transformed = transformationResult?.value ?? object2;
  const transformedValue = isArray16(transformed) ? [] : {};
  const innerAnnotations = {};
  forEach15(transformed, (value, index) => {
    if (index === "__proto__" || index === "constructor" || index === "prototype") {
      throw new Error(`Detected property ${index}. This is a prototype pollution risk, please remove it from your object.`);
    }
    const recursiveResult = walker15(value, identities, superJson, dedupe, [...path, index], [...objectsInThisPath, object2], seenObjects);
    transformedValue[index] = recursiveResult.transformedValue;
    if (isArray16(recursiveResult.annotations)) {
      innerAnnotations[escapeKey15(index)] = recursiveResult.annotations;
    } else if (isPlainObject16(recursiveResult.annotations)) {
      forEach15(recursiveResult.annotations, (tree, key) => {
        innerAnnotations[escapeKey15(index) + "." + key] = tree;
      });
    }
  });
  const result = isEmptyObject15(innerAnnotations) ? {
    transformedValue,
    annotations: transformationResult ? [transformationResult.type] : void 0
  } : {
    transformedValue,
    annotations: transformationResult ? [transformationResult.type, innerAnnotations] : innerAnnotations
  };
  if (!primitive) {
    seenObjects.set(object2, result);
  }
  return result;
}, "walker");
function getType215(payload) {
  return Object.prototype.toString.call(payload).slice(8, -1);
}
__name(getType215, "getType2");
function isArray215(payload) {
  return getType215(payload) === "Array";
}
__name(isArray215, "isArray2");
function isPlainObject215(payload) {
  if (getType215(payload) !== "Object")
    return false;
  const prototype = Object.getPrototypeOf(payload);
  return !!prototype && prototype.constructor === Object && prototype === Object.prototype;
}
__name(isPlainObject215, "isPlainObject2");
function assignProp15(carry, key, newVal, originalObject, includeNonenumerable) {
  const propType = {}.propertyIsEnumerable.call(originalObject, key) ? "enumerable" : "nonenumerable";
  if (propType === "enumerable")
    carry[key] = newVal;
  if (includeNonenumerable && propType === "nonenumerable") {
    Object.defineProperty(carry, key, {
      value: newVal,
      enumerable: false,
      writable: true,
      configurable: true
    });
  }
}
__name(assignProp15, "assignProp");
function copy15(target, options = {}) {
  if (isArray215(target)) {
    return target.map((item) => copy15(item, options));
  }
  if (!isPlainObject215(target)) {
    return target;
  }
  const props = Object.getOwnPropertyNames(target);
  const symbols = Object.getOwnPropertySymbols(target);
  return [...props, ...symbols].reduce((carry, key) => {
    if (key === "__proto__")
      return carry;
    if (isArray215(options.props) && !options.props.includes(key)) {
      return carry;
    }
    const val = target[key];
    const newVal = copy15(val, options);
    assignProp15(carry, key, newVal, target, options.nonenumerable);
    return carry;
  }, {});
}
__name(copy15, "copy");
var SuperJSON15 = class {
  static {
    __name(this, "SuperJSON");
  }
  constructor({ dedupe = false } = {}) {
    this.classRegistry = new ClassRegistry15();
    this.symbolRegistry = new Registry15((s) => s.description ?? "");
    this.customTransformerRegistry = new CustomTransformerRegistry15();
    this.allowedErrorProps = [];
    this.dedupe = dedupe;
  }
  serialize(object2) {
    const identities = /* @__PURE__ */ new Map();
    const output = walker15(object2, identities, this, this.dedupe);
    const res = {
      json: output.transformedValue
    };
    if (output.annotations) {
      res.meta = {
        ...res.meta,
        values: output.annotations
      };
    }
    const equalityAnnotations = generateReferentialEqualityAnnotations15(identities, this.dedupe);
    if (equalityAnnotations) {
      res.meta = {
        ...res.meta,
        referentialEqualities: equalityAnnotations
      };
    }
    if (res.meta)
      res.meta.v = 1;
    return res;
  }
  deserialize(payload, options) {
    const { json, meta } = payload;
    let result = options?.inPlace ? json : copy15(json);
    if (meta?.values) {
      result = applyValueAnnotations15(result, meta.values, meta.v ?? 0, this);
    }
    if (meta?.referentialEqualities) {
      result = applyReferentialEqualityAnnotations15(result, meta.referentialEqualities, meta.v ?? 0);
    }
    return result;
  }
  stringify(object2) {
    return JSON.stringify(this.serialize(object2));
  }
  parse(string2) {
    return this.deserialize(JSON.parse(string2), { inPlace: true });
  }
  registerClass(v, options) {
    this.classRegistry.register(v, options);
  }
  registerSymbol(v, identifier) {
    this.symbolRegistry.register(v, identifier);
  }
  registerCustom(transformer, name) {
    this.customTransformerRegistry.register({
      name,
      ...transformer
    });
  }
  allowErrorProps(...props) {
    this.allowedErrorProps.push(...props);
  }
};
SuperJSON15.defaultInstance = new SuperJSON15();
SuperJSON15.serialize = SuperJSON15.defaultInstance.serialize.bind(SuperJSON15.defaultInstance);
SuperJSON15.deserialize = SuperJSON15.defaultInstance.deserialize.bind(SuperJSON15.defaultInstance);
SuperJSON15.stringify = SuperJSON15.defaultInstance.stringify.bind(SuperJSON15.defaultInstance);
SuperJSON15.parse = SuperJSON15.defaultInstance.parse.bind(SuperJSON15.defaultInstance);
SuperJSON15.registerClass = SuperJSON15.defaultInstance.registerClass.bind(SuperJSON15.defaultInstance);
SuperJSON15.registerSymbol = SuperJSON15.defaultInstance.registerSymbol.bind(SuperJSON15.defaultInstance);
SuperJSON15.registerCustom = SuperJSON15.defaultInstance.registerCustom.bind(SuperJSON15.defaultInstance);
SuperJSON15.allowErrorProps = SuperJSON15.defaultInstance.allowErrorProps.bind(SuperJSON15.defaultInstance);
var dist_default15 = SuperJSON15;
var serialize15 = SuperJSON15.serialize;
var deserialize15 = SuperJSON15.deserialize;
var stringify16 = SuperJSON15.stringify;
var parse15 = SuperJSON15.parse;
var registerClass15 = SuperJSON15.registerClass;
var registerCustom15 = SuperJSON15.registerCustom;
var registerSymbol15 = SuperJSON15.registerSymbol;
var allowErrorProps15 = SuperJSON15.allowErrorProps;
function parseData15(formData) {
  const propsArray = [];
  if (!formData)
    return propsArray;
  const batchsIDs = [];
  for (const [key, value] of Array.from(formData.entries())) {
    if (key.startsWith("FILE_"))
      propsArray.push(value);
    else if (key.startsWith("FILES_")) {
      if (batchsIDs.includes(key))
        continue;
      batchsIDs.push(key);
      propsArray.push(formData.getAll(key));
    } else {
      propsArray.push(JSON.parse(decodeURI(value)));
    }
  }
  return propsArray;
}
__name(parseData15, "parseData");
function paramsFromURL15(url) {
  const params = url.searchParams.entries().toArray().map(([_, v]) => v);
  return params.map((param) => JSON.parse(decodeURIComponent(param)));
}
__name(paramsFromURL15, "paramsFromURL");
async function WrapRequestHandler15(context2, endpoint) {
  const isServerAction = context2.request.headers.get("x-server-action") === "true";
  if (!isServerAction) {
    return new Response("Not Found", { status: 404 });
  }
  const parsedData = context2.request.method === "GET" || context2.request.method === "HEAD" ? paramsFromURL15(new URL(context2.request.url)) : parseData15(context2.request.headers.get("content-type") ? await context2.request.formData() : void 0);
  const missingProps = endpoint.length - parsedData.length;
  for (let i = 0; i < missingProps; i++) {
    parsedData.push(void 0);
  }
  parsedData.push(context2);
  const result = await endpoint(...parsedData);
  switch (typeof result) {
    case "string":
    case "number":
    case "boolean":
    case "bigint":
      return new Response(dist_default15.stringify(result), {
        headers: {
          "Content-Type": "application/json",
          dataType: "json"
        }
      });
    case "undefined":
      return new Response(null, { status: 204 });
    case "object":
      if (result instanceof Response) {
        result.headers.set("dataType", "response");
        return result;
      } else if (result instanceof Blob) {
        const res = new Response(await result.arrayBuffer());
        res.headers.set("dataType", "blob");
        res.headers.set("Content-Type", result.type);
        return res;
      } else if (result instanceof File) {
        return new Response(await result.arrayBuffer(), {
          headers: {
            "Content-Type": result.type,
            dataType: "file",
            fileData: JSON.stringify({
              name: result.name,
              lastModified: result.lastModified
            })
          }
        });
      } else {
        return new Response(dist_default15.stringify(result), {
          headers: { "Content-Type": "application/json", dataType: "json" }
        });
      }
    default:
      throw new Error(`Unsupported return type from action: ${typeof result}`);
  }
}
__name(WrapRequestHandler15, "WrapRequestHandler");
var onRequest15 = /* @__PURE__ */ __name(async (context2) => {
  const method = context2.request.method;
  const options = {
    GET: typeof GET7 === "function" ? GET7 : void 0,
    POST: typeof POST === "function" ? POST : void 0,
    PUT: typeof PUT === "function" ? PUT : void 0,
    DELETE: typeof DELETE === "function" ? DELETE : void 0,
    PATCH: typeof PATCH === "function" ? PATCH : void 0,
    HEAD: typeof HEAD === "function" ? HEAD : void 0,
    OPTIONS: typeof OPTIONS === "function" ? OPTIONS : void 0
  };
  if (!options[method]) {
    return new Response(`Method "${method}" Not Allowed`, {
      status: 405
    });
  }
  return await WrapRequestHandler15(context2, options[method]);
}, "onRequest");

// api/dashboard/index.js
var USAGE_COLOR_PALETTE = [
  "#3B82F6",
  "#10B981",
  "#F59E0B",
  "#EF4444",
  "#8B5CF6",
  "#EC4899",
  "#06B6D4",
  "#84CC16",
  "#F97316",
  "#6366F1",
  "#14B8A6",
  "#A855F7"
];
async function GET8() {
  const { env: env2, data } = getContext(arguments);
  const db = drizzle(env2.PROJECT_DB);
  const session = await data.client.getUserSession("private");
  if (session instanceof Error) {
    return {
      success: false,
      error: "Unauthorized"
    };
  }
  try {
    const projects = await db.select().from(projectTable).where(ownerGroupConditions({
      user_group_ids: session.private?.group_ids ?? [],
      ownerGroupIdColumn: projectTable.owner_group_id,
      otherEq: [eq(projectTable.owner_id, session.user_id)],
      self_host: env2.SELF_HOSTED
    }));
    const parsedProjects = projects.map(parseDBProject);
    const projectStatsMap = /* @__PURE__ */ new Map();
    for (const p of parsedProjects) {
      projectStatsMap.set(p.clientID, {
        clientID: p.clientID,
        userCount: 0,
        webhookCount: 0,
        lastError: null,
        errorsCount24h: 0
      });
    }
    for (const p of parsedProjects) {
      if (!isClientIdValid(p.clientID))
        continue;
      try {
        const usersTable = OTFusersTable(p.clientID);
        const countRow = await db.select({ count: sql`count(*)` }).from(usersTable).get();
        const stats = projectStatsMap.get(p.clientID);
        if (stats) {
          stats.userCount = countRow?.count ?? 0;
        }
      } catch (_) {
      }
    }
    const webhooks = await db.select().from(WebHookTable).where(or(...parsedProjects.map((p) => eq(WebHookTable.clientID, p.clientID))));
    const webhookCountByProject = /* @__PURE__ */ new Map();
    for (const wh of webhooks) {
      const c = webhookCountByProject.get(wh.clientID) ?? 0;
      webhookCountByProject.set(wh.clientID, c + 1);
    }
    for (const [clientID, count3] of webhookCountByProject) {
      const stats = projectStatsMap.get(clientID);
      if (stats)
        stats.webhookCount = count3;
    }
    const recentLogsRows = await db.select().from(LogsTable).where(or(...parsedProjects.map((p) => eq(LogsTable.clientID, p.clientID)))).orderBy(desc(LogsTable.timestamp)).limit(15);
    const recentLogs = recentLogsRows.map((row) => ({
      id: row.id,
      clientID: row.clientID,
      type: row.type,
      message: row.message,
      timestamp: row.timestamp
    }));
    const oneDayAgo = new Date(Date.now() - 24 * 60 * 60 * 1e3).toISOString();
    const errors24hRows = await db.select().from(LogsTable).where(and(eq(LogsTable.type, "error"), gt(LogsTable.timestamp, oneDayAgo), or(...parsedProjects.map((p) => eq(LogsTable.clientID, p.clientID)))));
    const totalErrors24h = errors24hRows.length;
    const errorsByProject = /* @__PURE__ */ new Map();
    for (const row of errors24hRows) {
      const arr = errorsByProject.get(row.clientID) ?? [];
      arr.push(row);
      errorsByProject.set(row.clientID, arr);
    }
    for (const [clientID, errs] of errorsByProject) {
      const stats = projectStatsMap.get(clientID);
      if (stats) {
        stats.errorsCount24h = errs.length;
        const sorted = [...errs].sort((a, b) => new Date(b.timestamp).getTime() - new Date(a.timestamp).getTime());
        stats.lastError = {
          message: sorted.at(0)?.message ?? "Unknown error",
          timestamp: sorted.at(0)?.timestamp ?? "Unknown timestamp"
        };
      }
    }
    const allErrorLogs = await db.select().from(LogsTable).where(and(eq(LogsTable.type, "error"), or(...parsedProjects.map((p) => eq(LogsTable.clientID, p.clientID))))).orderBy(desc(LogsTable.timestamp));
    for (const row of allErrorLogs) {
      const stats = projectStatsMap.get(row.clientID);
      if (stats && !stats.lastError) {
        stats.lastError = {
          message: row.message ?? "Unknown error",
          timestamp: row.timestamp ?? "Unknown timestamp"
        };
      }
    }
    const alerts = [];
    for (const stats of projectStatsMap.values()) {
      if (stats.errorsCount24h > 0) {
        alerts.push({
          type: "errors",
          projectId: stats.clientID,
          message: `Project ${stats.clientID} has ${stats.errorsCount24h} error(s) in the last 24 hours`,
          count: stats.errorsCount24h
        });
      }
    }
    for (const log3 of recentLogsRows) {
      const msg = (log3.message || "").toLowerCase();
      if (msg.includes("webhook") && msg.includes("fail")) {
        const existing = alerts.find((a) => a.type === "webhook_failure" && a.projectId === log3.clientID);
        if (!existing) {
          alerts.push({
            type: "webhook_failure",
            projectId: log3.clientID,
            message: `Webhook delivery failed for project ${log3.clientID}`
          });
        }
      }
    }
    const totalUsers = [...projectStatsMap.values()].reduce((sum, s) => sum + s.userCount, 0);
    const totalWebhooks = webhooks.length;
    const kpis = {
      totalProjects: parsedProjects.length,
      totalUsers,
      totalErrors24h,
      totalWebhooks
    };
    const logs24hRows = await db.select({ clientID: LogsTable.clientID, timestamp: LogsTable.timestamp }).from(LogsTable).where(and(gt(LogsTable.timestamp, oneDayAgo), or(...parsedProjects.map((p) => eq(LogsTable.clientID, p.clientID)))));
    const now = /* @__PURE__ */ new Date();
    const buckets = [];
    for (let i = 23; i >= 0; i--) {
      const d = new Date(now.getTime() - i * 60 * 60 * 1e3);
      buckets.push(`${d.getFullYear()}-${String(d.getMonth() + 1).padStart(2, "0")}-${String(d.getDate()).padStart(2, "0")}T${String(d.getHours()).padStart(2, "0")}:00`);
    }
    const bucketToIndex = new Map(buckets.map((b, i) => [b, i]));
    const countByProjectAndBucket = /* @__PURE__ */ new Map();
    for (const p of parsedProjects) {
      countByProjectAndBucket.set(p.clientID, Array(buckets.length).fill(0));
    }
    for (const row of logs24hRows) {
      const d = new Date(row.timestamp);
      const bucketKey = `${d.getFullYear()}-${String(d.getMonth() + 1).padStart(2, "0")}-${String(d.getDate()).padStart(2, "0")}T${String(d.getHours()).padStart(2, "0")}:00`;
      const idx = bucketToIndex.get(bucketKey);
      if (idx !== void 0) {
        const arr = countByProjectAndBucket.get(row.clientID);
        if (arr)
          arr[idx]++;
        else {
          const newArr = Array(buckets.length).fill(0);
          newArr[idx] = 1;
          countByProjectAndBucket.set(row.clientID, newArr);
        }
      }
    }
    const usageSeries = parsedProjects.map((p, i) => ({
      name: p.name,
      color: USAGE_COLOR_PALETTE[i % USAGE_COLOR_PALETTE.length],
      counts: countByProjectAndBucket.get(p.clientID) ?? Array(buckets.length).fill(0)
    }));
    const usageOverTime = { buckets, series: usageSeries };
    return {
      success: true,
      data: {
        kpis,
        projectStats: [...projectStatsMap.values()],
        recentLogs,
        alerts,
        usageOverTime
      }
    };
  } catch (error3) {
    console.error("Dashboard API error:", error3);
    return {
      success: false,
      error: error3 instanceof Error ? error3.message : "Failed to load dashboard"
    };
  }
}
__name(GET8, "GET");
var DoubleIndexedKV16 = class {
  static {
    __name(this, "DoubleIndexedKV");
  }
  constructor() {
    this.keyToValue = /* @__PURE__ */ new Map();
    this.valueToKey = /* @__PURE__ */ new Map();
  }
  set(key, value) {
    this.keyToValue.set(key, value);
    this.valueToKey.set(value, key);
  }
  getByKey(key) {
    return this.keyToValue.get(key);
  }
  getByValue(value) {
    return this.valueToKey.get(value);
  }
  clear() {
    this.keyToValue.clear();
    this.valueToKey.clear();
  }
};
var Registry16 = class {
  static {
    __name(this, "Registry");
  }
  constructor(generateIdentifier) {
    this.generateIdentifier = generateIdentifier;
    this.kv = new DoubleIndexedKV16();
  }
  register(value, identifier) {
    if (this.kv.getByValue(value)) {
      return;
    }
    if (!identifier) {
      identifier = this.generateIdentifier(value);
    }
    this.kv.set(identifier, value);
  }
  clear() {
    this.kv.clear();
  }
  getIdentifier(value) {
    return this.kv.getByValue(value);
  }
  getValue(identifier) {
    return this.kv.getByKey(identifier);
  }
};
var ClassRegistry16 = class extends Registry16 {
  static {
    __name(this, "ClassRegistry");
  }
  constructor() {
    super((c) => c.name);
    this.classToAllowedProps = /* @__PURE__ */ new Map();
  }
  register(value, options) {
    if (typeof options === "object") {
      if (options.allowProps) {
        this.classToAllowedProps.set(value, options.allowProps);
      }
      super.register(value, options.identifier);
    } else {
      super.register(value, options);
    }
  }
  getAllowedProps(value) {
    return this.classToAllowedProps.get(value);
  }
};
function valuesOfObj16(record) {
  if ("values" in Object) {
    return Object.values(record);
  }
  const values = [];
  for (const key in record) {
    if (record.hasOwnProperty(key)) {
      values.push(record[key]);
    }
  }
  return values;
}
__name(valuesOfObj16, "valuesOfObj");
function find16(record, predicate) {
  const values = valuesOfObj16(record);
  if ("find" in values) {
    return values.find(predicate);
  }
  const valuesNotNever = values;
  for (let i = 0; i < valuesNotNever.length; i++) {
    const value = valuesNotNever[i];
    if (predicate(value)) {
      return value;
    }
  }
  return;
}
__name(find16, "find");
function forEach16(record, run) {
  Object.entries(record).forEach(([key, value]) => run(value, key));
}
__name(forEach16, "forEach");
function includes16(arr, value) {
  return arr.indexOf(value) !== -1;
}
__name(includes16, "includes");
function findArr16(record, predicate) {
  for (let i = 0; i < record.length; i++) {
    const value = record[i];
    if (predicate(value)) {
      return value;
    }
  }
  return;
}
__name(findArr16, "findArr");
var CustomTransformerRegistry16 = class {
  static {
    __name(this, "CustomTransformerRegistry");
  }
  constructor() {
    this.transfomers = {};
  }
  register(transformer) {
    this.transfomers[transformer.name] = transformer;
  }
  findApplicable(v) {
    return find16(this.transfomers, (transformer) => transformer.isApplicable(v));
  }
  findByName(name) {
    return this.transfomers[name];
  }
};
var getType17 = /* @__PURE__ */ __name((payload) => Object.prototype.toString.call(payload).slice(8, -1), "getType");
var isUndefined16 = /* @__PURE__ */ __name((payload) => typeof payload === "undefined", "isUndefined");
var isNull17 = /* @__PURE__ */ __name((payload) => payload === null, "isNull");
var isPlainObject17 = /* @__PURE__ */ __name((payload) => {
  if (typeof payload !== "object" || payload === null)
    return false;
  if (payload === Object.prototype)
    return false;
  if (Object.getPrototypeOf(payload) === null)
    return true;
  return Object.getPrototypeOf(payload) === Object.prototype;
}, "isPlainObject");
var isEmptyObject16 = /* @__PURE__ */ __name((payload) => isPlainObject17(payload) && Object.keys(payload).length === 0, "isEmptyObject");
var isArray17 = /* @__PURE__ */ __name((payload) => Array.isArray(payload), "isArray");
var isString16 = /* @__PURE__ */ __name((payload) => typeof payload === "string", "isString");
var isNumber16 = /* @__PURE__ */ __name((payload) => typeof payload === "number" && !isNaN(payload), "isNumber");
var isBoolean16 = /* @__PURE__ */ __name((payload) => typeof payload === "boolean", "isBoolean");
var isRegExp16 = /* @__PURE__ */ __name((payload) => payload instanceof RegExp, "isRegExp");
var isMap16 = /* @__PURE__ */ __name((payload) => payload instanceof Map, "isMap");
var isSet16 = /* @__PURE__ */ __name((payload) => payload instanceof Set, "isSet");
var isSymbol16 = /* @__PURE__ */ __name((payload) => getType17(payload) === "Symbol", "isSymbol");
var isDate16 = /* @__PURE__ */ __name((payload) => payload instanceof Date && !isNaN(payload.valueOf()), "isDate");
var isError16 = /* @__PURE__ */ __name((payload) => payload instanceof Error, "isError");
var isNaNValue16 = /* @__PURE__ */ __name((payload) => typeof payload === "number" && isNaN(payload), "isNaNValue");
var isPrimitive16 = /* @__PURE__ */ __name((payload) => isBoolean16(payload) || isNull17(payload) || isUndefined16(payload) || isNumber16(payload) || isString16(payload) || isSymbol16(payload), "isPrimitive");
var isBigint16 = /* @__PURE__ */ __name((payload) => typeof payload === "bigint", "isBigint");
var isInfinite16 = /* @__PURE__ */ __name((payload) => payload === Infinity || payload === -Infinity, "isInfinite");
var isTypedArray16 = /* @__PURE__ */ __name((payload) => ArrayBuffer.isView(payload) && !(payload instanceof DataView), "isTypedArray");
var isURL16 = /* @__PURE__ */ __name((payload) => payload instanceof URL, "isURL");
var escapeKey16 = /* @__PURE__ */ __name((key) => key.replace(/\\/g, "\\\\").replace(/\./g, "\\."), "escapeKey");
var stringifyPath16 = /* @__PURE__ */ __name((path) => path.map(String).map(escapeKey16).join("."), "stringifyPath");
var parsePath16 = /* @__PURE__ */ __name((string2, legacyPaths) => {
  const result = [];
  let segment = "";
  for (let i = 0; i < string2.length; i++) {
    let char = string2.charAt(i);
    if (!legacyPaths && char === "\\") {
      const escaped = string2.charAt(i + 1);
      if (escaped === "\\") {
        segment += "\\";
        i++;
        continue;
      } else if (escaped !== ".") {
        throw Error("invalid path");
      }
    }
    const isEscapedDot = char === "\\" && string2.charAt(i + 1) === ".";
    if (isEscapedDot) {
      segment += ".";
      i++;
      continue;
    }
    const isEndOfSegment = char === ".";
    if (isEndOfSegment) {
      result.push(segment);
      segment = "";
      continue;
    }
    segment += char;
  }
  const lastSegment = segment;
  result.push(lastSegment);
  return result;
}, "parsePath");
function simpleTransformation16(isApplicable, annotation, transform, untransform) {
  return {
    isApplicable,
    annotation,
    transform,
    untransform
  };
}
__name(simpleTransformation16, "simpleTransformation");
var simpleRules16 = [
  simpleTransformation16(isUndefined16, "undefined", () => null, () => {
    return;
  }),
  simpleTransformation16(isBigint16, "bigint", (v) => v.toString(), (v) => {
    if (typeof BigInt !== "undefined") {
      return BigInt(v);
    }
    console.error("Please add a BigInt polyfill.");
    return v;
  }),
  simpleTransformation16(isDate16, "Date", (v) => v.toISOString(), (v) => new Date(v)),
  simpleTransformation16(isError16, "Error", (v, superJson) => {
    const baseError = {
      name: v.name,
      message: v.message
    };
    if ("cause" in v) {
      baseError.cause = v.cause;
    }
    superJson.allowedErrorProps.forEach((prop) => {
      baseError[prop] = v[prop];
    });
    return baseError;
  }, (v, superJson) => {
    const e = new Error(v.message, { cause: v.cause });
    e.name = v.name;
    e.stack = v.stack;
    superJson.allowedErrorProps.forEach((prop) => {
      e[prop] = v[prop];
    });
    return e;
  }),
  simpleTransformation16(isRegExp16, "regexp", (v) => "" + v, (regex) => {
    const body = regex.slice(1, regex.lastIndexOf("/"));
    const flags2 = regex.slice(regex.lastIndexOf("/") + 1);
    return new RegExp(body, flags2);
  }),
  simpleTransformation16(isSet16, "set", (v) => [...v.values()], (v) => new Set(v)),
  simpleTransformation16(isMap16, "map", (v) => [...v.entries()], (v) => new Map(v)),
  simpleTransformation16((v) => isNaNValue16(v) || isInfinite16(v), "number", (v) => {
    if (isNaNValue16(v)) {
      return "NaN";
    }
    if (v > 0) {
      return "Infinity";
    } else {
      return "-Infinity";
    }
  }, Number),
  simpleTransformation16((v) => v === 0 && 1 / v === -Infinity, "number", () => {
    return "-0";
  }, Number),
  simpleTransformation16(isURL16, "URL", (v) => v.toString(), (v) => new URL(v))
];
function compositeTransformation16(isApplicable, annotation, transform, untransform) {
  return {
    isApplicable,
    annotation,
    transform,
    untransform
  };
}
__name(compositeTransformation16, "compositeTransformation");
var symbolRule16 = compositeTransformation16((s, superJson) => {
  if (isSymbol16(s)) {
    const isRegistered = !!superJson.symbolRegistry.getIdentifier(s);
    return isRegistered;
  }
  return false;
}, (s, superJson) => {
  const identifier = superJson.symbolRegistry.getIdentifier(s);
  return ["symbol", identifier];
}, (v) => v.description, (_, a, superJson) => {
  const value = superJson.symbolRegistry.getValue(a[1]);
  if (!value) {
    throw new Error("Trying to deserialize unknown symbol");
  }
  return value;
});
var constructorToName16 = [
  Int8Array,
  Uint8Array,
  Int16Array,
  Uint16Array,
  Int32Array,
  Uint32Array,
  Float32Array,
  Float64Array,
  Uint8ClampedArray
].reduce((obj, ctor) => {
  obj[ctor.name] = ctor;
  return obj;
}, {});
var typedArrayRule16 = compositeTransformation16(isTypedArray16, (v) => ["typed-array", v.constructor.name], (v) => [...v], (v, a) => {
  const ctor = constructorToName16[a[1]];
  if (!ctor) {
    throw new Error("Trying to deserialize unknown typed array");
  }
  return new ctor(v);
});
function isInstanceOfRegisteredClass16(potentialClass, superJson) {
  if (potentialClass?.constructor) {
    const isRegistered = !!superJson.classRegistry.getIdentifier(potentialClass.constructor);
    return isRegistered;
  }
  return false;
}
__name(isInstanceOfRegisteredClass16, "isInstanceOfRegisteredClass");
var classRule16 = compositeTransformation16(isInstanceOfRegisteredClass16, (clazz, superJson) => {
  const identifier = superJson.classRegistry.getIdentifier(clazz.constructor);
  return ["class", identifier];
}, (clazz, superJson) => {
  const allowedProps = superJson.classRegistry.getAllowedProps(clazz.constructor);
  if (!allowedProps) {
    return { ...clazz };
  }
  const result = {};
  allowedProps.forEach((prop) => {
    result[prop] = clazz[prop];
  });
  return result;
}, (v, a, superJson) => {
  const clazz = superJson.classRegistry.getValue(a[1]);
  if (!clazz) {
    throw new Error(`Trying to deserialize unknown class '${a[1]}' - check https://github.com/blitz-js/superjson/issues/116#issuecomment-773996564`);
  }
  return Object.assign(Object.create(clazz.prototype), v);
});
var customRule16 = compositeTransformation16((value, superJson) => {
  return !!superJson.customTransformerRegistry.findApplicable(value);
}, (value, superJson) => {
  const transformer = superJson.customTransformerRegistry.findApplicable(value);
  return ["custom", transformer.name];
}, (value, superJson) => {
  const transformer = superJson.customTransformerRegistry.findApplicable(value);
  return transformer.serialize(value);
}, (v, a, superJson) => {
  const transformer = superJson.customTransformerRegistry.findByName(a[1]);
  if (!transformer) {
    throw new Error("Trying to deserialize unknown custom value");
  }
  return transformer.deserialize(v);
});
var compositeRules16 = [classRule16, symbolRule16, customRule16, typedArrayRule16];
var transformValue16 = /* @__PURE__ */ __name((value, superJson) => {
  const applicableCompositeRule = findArr16(compositeRules16, (rule) => rule.isApplicable(value, superJson));
  if (applicableCompositeRule) {
    return {
      value: applicableCompositeRule.transform(value, superJson),
      type: applicableCompositeRule.annotation(value, superJson)
    };
  }
  const applicableSimpleRule = findArr16(simpleRules16, (rule) => rule.isApplicable(value, superJson));
  if (applicableSimpleRule) {
    return {
      value: applicableSimpleRule.transform(value, superJson),
      type: applicableSimpleRule.annotation
    };
  }
  return;
}, "transformValue");
var simpleRulesByAnnotation16 = {};
simpleRules16.forEach((rule) => {
  simpleRulesByAnnotation16[rule.annotation] = rule;
});
var untransformValue16 = /* @__PURE__ */ __name((json, type, superJson) => {
  if (isArray17(type)) {
    switch (type[0]) {
      case "symbol":
        return symbolRule16.untransform(json, type, superJson);
      case "class":
        return classRule16.untransform(json, type, superJson);
      case "custom":
        return customRule16.untransform(json, type, superJson);
      case "typed-array":
        return typedArrayRule16.untransform(json, type, superJson);
      default:
        throw new Error("Unknown transformation: " + type);
    }
  } else {
    const transformation = simpleRulesByAnnotation16[type];
    if (!transformation) {
      throw new Error("Unknown transformation: " + type);
    }
    return transformation.untransform(json, superJson);
  }
}, "untransformValue");
var getNthKey16 = /* @__PURE__ */ __name((value, n) => {
  if (n > value.size)
    throw new Error("index out of bounds");
  const keys = value.keys();
  while (n > 0) {
    keys.next();
    n--;
  }
  return keys.next().value;
}, "getNthKey");
function validatePath16(path) {
  if (includes16(path, "__proto__")) {
    throw new Error("__proto__ is not allowed as a property");
  }
  if (includes16(path, "prototype")) {
    throw new Error("prototype is not allowed as a property");
  }
  if (includes16(path, "constructor")) {
    throw new Error("constructor is not allowed as a property");
  }
}
__name(validatePath16, "validatePath");
var getDeep16 = /* @__PURE__ */ __name((object2, path) => {
  validatePath16(path);
  for (let i = 0; i < path.length; i++) {
    const key = path[i];
    if (isSet16(object2)) {
      object2 = getNthKey16(object2, +key);
    } else if (isMap16(object2)) {
      const row = +key;
      const type = +path[++i] === 0 ? "key" : "value";
      const keyOfRow = getNthKey16(object2, row);
      switch (type) {
        case "key":
          object2 = keyOfRow;
          break;
        case "value":
          object2 = object2.get(keyOfRow);
          break;
      }
    } else {
      object2 = object2[key];
    }
  }
  return object2;
}, "getDeep");
var setDeep16 = /* @__PURE__ */ __name((object2, path, mapper) => {
  validatePath16(path);
  if (path.length === 0) {
    return mapper(object2);
  }
  let parent = object2;
  for (let i = 0; i < path.length - 1; i++) {
    const key = path[i];
    if (isArray17(parent)) {
      const index = +key;
      parent = parent[index];
    } else if (isPlainObject17(parent)) {
      parent = parent[key];
    } else if (isSet16(parent)) {
      const row = +key;
      parent = getNthKey16(parent, row);
    } else if (isMap16(parent)) {
      const isEnd = i === path.length - 2;
      if (isEnd) {
        break;
      }
      const row = +key;
      const type = +path[++i] === 0 ? "key" : "value";
      const keyOfRow = getNthKey16(parent, row);
      switch (type) {
        case "key":
          parent = keyOfRow;
          break;
        case "value":
          parent = parent.get(keyOfRow);
          break;
      }
    }
  }
  const lastKey = path[path.length - 1];
  if (isArray17(parent)) {
    parent[+lastKey] = mapper(parent[+lastKey]);
  } else if (isPlainObject17(parent)) {
    parent[lastKey] = mapper(parent[lastKey]);
  }
  if (isSet16(parent)) {
    const oldValue = getNthKey16(parent, +lastKey);
    const newValue = mapper(oldValue);
    if (oldValue !== newValue) {
      parent.delete(oldValue);
      parent.add(newValue);
    }
  }
  if (isMap16(parent)) {
    const row = +path[path.length - 2];
    const keyToRow = getNthKey16(parent, row);
    const type = +lastKey === 0 ? "key" : "value";
    switch (type) {
      case "key": {
        const newKey = mapper(keyToRow);
        parent.set(newKey, parent.get(keyToRow));
        if (newKey !== keyToRow) {
          parent.delete(keyToRow);
        }
        break;
      }
      case "value": {
        parent.set(keyToRow, mapper(parent.get(keyToRow)));
        break;
      }
    }
  }
  return object2;
}, "setDeep");
var enableLegacyPaths16 = /* @__PURE__ */ __name((version4) => version4 < 1, "enableLegacyPaths");
function traverse16(tree, walker26, version4, origin = []) {
  if (!tree) {
    return;
  }
  const legacyPaths = enableLegacyPaths16(version4);
  if (!isArray17(tree)) {
    forEach16(tree, (subtree, key) => traverse16(subtree, walker26, version4, [
      ...origin,
      ...parsePath16(key, legacyPaths)
    ]));
    return;
  }
  const [nodeValue, children] = tree;
  if (children) {
    forEach16(children, (child, key) => {
      traverse16(child, walker26, version4, [
        ...origin,
        ...parsePath16(key, legacyPaths)
      ]);
    });
  }
  walker26(nodeValue, origin);
}
__name(traverse16, "traverse");
function applyValueAnnotations16(plain, annotations, version4, superJson) {
  traverse16(annotations, (type, path) => {
    plain = setDeep16(plain, path, (v) => untransformValue16(v, type, superJson));
  }, version4);
  return plain;
}
__name(applyValueAnnotations16, "applyValueAnnotations");
function applyReferentialEqualityAnnotations16(plain, annotations, version4) {
  const legacyPaths = enableLegacyPaths16(version4);
  function apply(identicalPaths, path) {
    const object2 = getDeep16(plain, parsePath16(path, legacyPaths));
    identicalPaths.map((path2) => parsePath16(path2, legacyPaths)).forEach((identicalObjectPath) => {
      plain = setDeep16(plain, identicalObjectPath, () => object2);
    });
  }
  __name(apply, "apply");
  if (isArray17(annotations)) {
    const [root, other] = annotations;
    root.forEach((identicalPath) => {
      plain = setDeep16(plain, parsePath16(identicalPath, legacyPaths), () => plain);
    });
    if (other) {
      forEach16(other, apply);
    }
  } else {
    forEach16(annotations, apply);
  }
  return plain;
}
__name(applyReferentialEqualityAnnotations16, "applyReferentialEqualityAnnotations");
var isDeep16 = /* @__PURE__ */ __name((object2, superJson) => isPlainObject17(object2) || isArray17(object2) || isMap16(object2) || isSet16(object2) || isError16(object2) || isInstanceOfRegisteredClass16(object2, superJson), "isDeep");
function addIdentity16(object2, path, identities) {
  const existingSet = identities.get(object2);
  if (existingSet) {
    existingSet.push(path);
  } else {
    identities.set(object2, [path]);
  }
}
__name(addIdentity16, "addIdentity");
function generateReferentialEqualityAnnotations16(identitites, dedupe) {
  const result = {};
  let rootEqualityPaths = void 0;
  identitites.forEach((paths) => {
    if (paths.length <= 1) {
      return;
    }
    if (!dedupe) {
      paths = paths.map((path) => path.map(String)).sort((a, b) => a.length - b.length);
    }
    const [representativePath, ...identicalPaths] = paths;
    if (representativePath.length === 0) {
      rootEqualityPaths = identicalPaths.map(stringifyPath16);
    } else {
      result[stringifyPath16(representativePath)] = identicalPaths.map(stringifyPath16);
    }
  });
  if (rootEqualityPaths) {
    if (isEmptyObject16(result)) {
      return [rootEqualityPaths];
    } else {
      return [rootEqualityPaths, result];
    }
  } else {
    return isEmptyObject16(result) ? void 0 : result;
  }
}
__name(generateReferentialEqualityAnnotations16, "generateReferentialEqualityAnnotations");
var walker16 = /* @__PURE__ */ __name((object2, identities, superJson, dedupe, path = [], objectsInThisPath = [], seenObjects = /* @__PURE__ */ new Map()) => {
  const primitive = isPrimitive16(object2);
  if (!primitive) {
    addIdentity16(object2, path, identities);
    const seen = seenObjects.get(object2);
    if (seen) {
      return dedupe ? {
        transformedValue: null
      } : seen;
    }
  }
  if (!isDeep16(object2, superJson)) {
    const transformed2 = transformValue16(object2, superJson);
    const result2 = transformed2 ? {
      transformedValue: transformed2.value,
      annotations: [transformed2.type]
    } : {
      transformedValue: object2
    };
    if (!primitive) {
      seenObjects.set(object2, result2);
    }
    return result2;
  }
  if (includes16(objectsInThisPath, object2)) {
    return {
      transformedValue: null
    };
  }
  const transformationResult = transformValue16(object2, superJson);
  const transformed = transformationResult?.value ?? object2;
  const transformedValue = isArray17(transformed) ? [] : {};
  const innerAnnotations = {};
  forEach16(transformed, (value, index) => {
    if (index === "__proto__" || index === "constructor" || index === "prototype") {
      throw new Error(`Detected property ${index}. This is a prototype pollution risk, please remove it from your object.`);
    }
    const recursiveResult = walker16(value, identities, superJson, dedupe, [...path, index], [...objectsInThisPath, object2], seenObjects);
    transformedValue[index] = recursiveResult.transformedValue;
    if (isArray17(recursiveResult.annotations)) {
      innerAnnotations[escapeKey16(index)] = recursiveResult.annotations;
    } else if (isPlainObject17(recursiveResult.annotations)) {
      forEach16(recursiveResult.annotations, (tree, key) => {
        innerAnnotations[escapeKey16(index) + "." + key] = tree;
      });
    }
  });
  const result = isEmptyObject16(innerAnnotations) ? {
    transformedValue,
    annotations: transformationResult ? [transformationResult.type] : void 0
  } : {
    transformedValue,
    annotations: transformationResult ? [transformationResult.type, innerAnnotations] : innerAnnotations
  };
  if (!primitive) {
    seenObjects.set(object2, result);
  }
  return result;
}, "walker");
function getType216(payload) {
  return Object.prototype.toString.call(payload).slice(8, -1);
}
__name(getType216, "getType2");
function isArray216(payload) {
  return getType216(payload) === "Array";
}
__name(isArray216, "isArray2");
function isPlainObject216(payload) {
  if (getType216(payload) !== "Object")
    return false;
  const prototype = Object.getPrototypeOf(payload);
  return !!prototype && prototype.constructor === Object && prototype === Object.prototype;
}
__name(isPlainObject216, "isPlainObject2");
function assignProp16(carry, key, newVal, originalObject, includeNonenumerable) {
  const propType = {}.propertyIsEnumerable.call(originalObject, key) ? "enumerable" : "nonenumerable";
  if (propType === "enumerable")
    carry[key] = newVal;
  if (includeNonenumerable && propType === "nonenumerable") {
    Object.defineProperty(carry, key, {
      value: newVal,
      enumerable: false,
      writable: true,
      configurable: true
    });
  }
}
__name(assignProp16, "assignProp");
function copy16(target, options = {}) {
  if (isArray216(target)) {
    return target.map((item) => copy16(item, options));
  }
  if (!isPlainObject216(target)) {
    return target;
  }
  const props = Object.getOwnPropertyNames(target);
  const symbols = Object.getOwnPropertySymbols(target);
  return [...props, ...symbols].reduce((carry, key) => {
    if (key === "__proto__")
      return carry;
    if (isArray216(options.props) && !options.props.includes(key)) {
      return carry;
    }
    const val = target[key];
    const newVal = copy16(val, options);
    assignProp16(carry, key, newVal, target, options.nonenumerable);
    return carry;
  }, {});
}
__name(copy16, "copy");
var SuperJSON16 = class {
  static {
    __name(this, "SuperJSON");
  }
  constructor({ dedupe = false } = {}) {
    this.classRegistry = new ClassRegistry16();
    this.symbolRegistry = new Registry16((s) => s.description ?? "");
    this.customTransformerRegistry = new CustomTransformerRegistry16();
    this.allowedErrorProps = [];
    this.dedupe = dedupe;
  }
  serialize(object2) {
    const identities = /* @__PURE__ */ new Map();
    const output = walker16(object2, identities, this, this.dedupe);
    const res = {
      json: output.transformedValue
    };
    if (output.annotations) {
      res.meta = {
        ...res.meta,
        values: output.annotations
      };
    }
    const equalityAnnotations = generateReferentialEqualityAnnotations16(identities, this.dedupe);
    if (equalityAnnotations) {
      res.meta = {
        ...res.meta,
        referentialEqualities: equalityAnnotations
      };
    }
    if (res.meta)
      res.meta.v = 1;
    return res;
  }
  deserialize(payload, options) {
    const { json, meta } = payload;
    let result = options?.inPlace ? json : copy16(json);
    if (meta?.values) {
      result = applyValueAnnotations16(result, meta.values, meta.v ?? 0, this);
    }
    if (meta?.referentialEqualities) {
      result = applyReferentialEqualityAnnotations16(result, meta.referentialEqualities, meta.v ?? 0);
    }
    return result;
  }
  stringify(object2) {
    return JSON.stringify(this.serialize(object2));
  }
  parse(string2) {
    return this.deserialize(JSON.parse(string2), { inPlace: true });
  }
  registerClass(v, options) {
    this.classRegistry.register(v, options);
  }
  registerSymbol(v, identifier) {
    this.symbolRegistry.register(v, identifier);
  }
  registerCustom(transformer, name) {
    this.customTransformerRegistry.register({
      name,
      ...transformer
    });
  }
  allowErrorProps(...props) {
    this.allowedErrorProps.push(...props);
  }
};
SuperJSON16.defaultInstance = new SuperJSON16();
SuperJSON16.serialize = SuperJSON16.defaultInstance.serialize.bind(SuperJSON16.defaultInstance);
SuperJSON16.deserialize = SuperJSON16.defaultInstance.deserialize.bind(SuperJSON16.defaultInstance);
SuperJSON16.stringify = SuperJSON16.defaultInstance.stringify.bind(SuperJSON16.defaultInstance);
SuperJSON16.parse = SuperJSON16.defaultInstance.parse.bind(SuperJSON16.defaultInstance);
SuperJSON16.registerClass = SuperJSON16.defaultInstance.registerClass.bind(SuperJSON16.defaultInstance);
SuperJSON16.registerSymbol = SuperJSON16.defaultInstance.registerSymbol.bind(SuperJSON16.defaultInstance);
SuperJSON16.registerCustom = SuperJSON16.defaultInstance.registerCustom.bind(SuperJSON16.defaultInstance);
SuperJSON16.allowErrorProps = SuperJSON16.defaultInstance.allowErrorProps.bind(SuperJSON16.defaultInstance);
var dist_default16 = SuperJSON16;
var serialize16 = SuperJSON16.serialize;
var deserialize16 = SuperJSON16.deserialize;
var stringify17 = SuperJSON16.stringify;
var parse16 = SuperJSON16.parse;
var registerClass16 = SuperJSON16.registerClass;
var registerCustom16 = SuperJSON16.registerCustom;
var registerSymbol16 = SuperJSON16.registerSymbol;
var allowErrorProps16 = SuperJSON16.allowErrorProps;
function parseData16(formData) {
  const propsArray = [];
  if (!formData)
    return propsArray;
  const batchsIDs = [];
  for (const [key, value] of Array.from(formData.entries())) {
    if (key.startsWith("FILE_"))
      propsArray.push(value);
    else if (key.startsWith("FILES_")) {
      if (batchsIDs.includes(key))
        continue;
      batchsIDs.push(key);
      propsArray.push(formData.getAll(key));
    } else {
      propsArray.push(JSON.parse(decodeURI(value)));
    }
  }
  return propsArray;
}
__name(parseData16, "parseData");
function paramsFromURL16(url) {
  const params = url.searchParams.entries().toArray().map(([_, v]) => v);
  return params.map((param) => JSON.parse(decodeURIComponent(param)));
}
__name(paramsFromURL16, "paramsFromURL");
async function WrapRequestHandler16(context2, endpoint) {
  const isServerAction = context2.request.headers.get("x-server-action") === "true";
  if (!isServerAction) {
    return new Response("Not Found", { status: 404 });
  }
  const parsedData = context2.request.method === "GET" || context2.request.method === "HEAD" ? paramsFromURL16(new URL(context2.request.url)) : parseData16(context2.request.headers.get("content-type") ? await context2.request.formData() : void 0);
  const missingProps = endpoint.length - parsedData.length;
  for (let i = 0; i < missingProps; i++) {
    parsedData.push(void 0);
  }
  parsedData.push(context2);
  const result = await endpoint(...parsedData);
  switch (typeof result) {
    case "string":
    case "number":
    case "boolean":
    case "bigint":
      return new Response(dist_default16.stringify(result), {
        headers: {
          "Content-Type": "application/json",
          dataType: "json"
        }
      });
    case "undefined":
      return new Response(null, { status: 204 });
    case "object":
      if (result instanceof Response) {
        result.headers.set("dataType", "response");
        return result;
      } else if (result instanceof Blob) {
        const res = new Response(await result.arrayBuffer());
        res.headers.set("dataType", "blob");
        res.headers.set("Content-Type", result.type);
        return res;
      } else if (result instanceof File) {
        return new Response(await result.arrayBuffer(), {
          headers: {
            "Content-Type": result.type,
            dataType: "file",
            fileData: JSON.stringify({
              name: result.name,
              lastModified: result.lastModified
            })
          }
        });
      } else {
        return new Response(dist_default16.stringify(result), {
          headers: { "Content-Type": "application/json", dataType: "json" }
        });
      }
    default:
      throw new Error(`Unsupported return type from action: ${typeof result}`);
  }
}
__name(WrapRequestHandler16, "WrapRequestHandler");
var onRequest16 = /* @__PURE__ */ __name(async (context2) => {
  const method = context2.request.method;
  const options = {
    GET: typeof GET8 === "function" ? GET8 : void 0,
    POST: typeof POST === "function" ? POST : void 0,
    PUT: typeof PUT === "function" ? PUT : void 0,
    DELETE: typeof DELETE === "function" ? DELETE : void 0,
    PATCH: typeof PATCH === "function" ? PATCH : void 0,
    HEAD: typeof HEAD === "function" ? HEAD : void 0,
    OPTIONS: typeof OPTIONS === "function" ? OPTIONS : void 0
  };
  if (!options[method]) {
    return new Response(`Method "${method}" Not Allowed`, {
      status: 405
    });
  }
  return await WrapRequestHandler16(context2, options[method]);
}, "onRequest");

// api/invitelink/index.js
async function POST11(params) {
  const ctx = getContext(arguments);
  const project = await drizzle(ctx.env.PROJECT_DB).select({
    originURL: projectTable.originURL
  }).from(projectTable).where(eq(projectTable.clientID, params.clientID)).get();
  if (!project)
    return {
      success: false,
      error: "Project not found"
    };
  else if (!project.originURL) {
    return {
      success: false,
      error: "Project origin URL is not set"
    };
  }
  if (!Number.isFinite(params.expireInMin) || params.expireInMin < 1 || params.expireInMin > 10080) {
    return {
      success: false,
      error: "expireInMin must be between 1 and 10080 (1 week)"
    };
  }
  let originUrl;
  try {
    const authorizedOrigins = project.originURL.split(",").map((u) => u.trim());
    if (!authorizedOrigins.includes(params.originURL)) {
      return {
        success: false,
        error: "Provided origin URL is not in the list of authorized origins for this project"
      };
    }
    originUrl = new URL(params.originURL);
  } catch {
    return {
      success: false,
      error: "Project origin URL is invalid"
    };
  }
  const id = crypto.randomUUID();
  const url = originUrl;
  url.searchParams.set("invite_id", id);
  url.searchParams.set("client_id", params.clientID);
  if (params.copyID) {
    url.searchParams.set("copy_id", params.copyID);
  }
  const newLink = await drizzle(ctx.env.PROJECT_DB).insert(WebUiInviteLinkTable).values({
    id,
    clientID: params.clientID,
    link: url.toString(),
    expiresAt: new Date(Date.now() + params.expireInMin * 60 * 1e3).toISOString(),
    created_at: (/* @__PURE__ */ new Date()).toISOString()
  }).returning({ link: WebUiInviteLinkTable.link }).get();
  if (!newLink) {
    return {
      success: false,
      error: "Failed to create invite link"
    };
  }
  return {
    success: true,
    data: { link: newLink.link }
  };
}
__name(POST11, "POST");
var DoubleIndexedKV17 = class {
  static {
    __name(this, "DoubleIndexedKV");
  }
  constructor() {
    this.keyToValue = /* @__PURE__ */ new Map();
    this.valueToKey = /* @__PURE__ */ new Map();
  }
  set(key, value) {
    this.keyToValue.set(key, value);
    this.valueToKey.set(value, key);
  }
  getByKey(key) {
    return this.keyToValue.get(key);
  }
  getByValue(value) {
    return this.valueToKey.get(value);
  }
  clear() {
    this.keyToValue.clear();
    this.valueToKey.clear();
  }
};
var Registry17 = class {
  static {
    __name(this, "Registry");
  }
  constructor(generateIdentifier) {
    this.generateIdentifier = generateIdentifier;
    this.kv = new DoubleIndexedKV17();
  }
  register(value, identifier) {
    if (this.kv.getByValue(value)) {
      return;
    }
    if (!identifier) {
      identifier = this.generateIdentifier(value);
    }
    this.kv.set(identifier, value);
  }
  clear() {
    this.kv.clear();
  }
  getIdentifier(value) {
    return this.kv.getByValue(value);
  }
  getValue(identifier) {
    return this.kv.getByKey(identifier);
  }
};
var ClassRegistry17 = class extends Registry17 {
  static {
    __name(this, "ClassRegistry");
  }
  constructor() {
    super((c) => c.name);
    this.classToAllowedProps = /* @__PURE__ */ new Map();
  }
  register(value, options) {
    if (typeof options === "object") {
      if (options.allowProps) {
        this.classToAllowedProps.set(value, options.allowProps);
      }
      super.register(value, options.identifier);
    } else {
      super.register(value, options);
    }
  }
  getAllowedProps(value) {
    return this.classToAllowedProps.get(value);
  }
};
function valuesOfObj17(record) {
  if ("values" in Object) {
    return Object.values(record);
  }
  const values = [];
  for (const key in record) {
    if (record.hasOwnProperty(key)) {
      values.push(record[key]);
    }
  }
  return values;
}
__name(valuesOfObj17, "valuesOfObj");
function find17(record, predicate) {
  const values = valuesOfObj17(record);
  if ("find" in values) {
    return values.find(predicate);
  }
  const valuesNotNever = values;
  for (let i = 0; i < valuesNotNever.length; i++) {
    const value = valuesNotNever[i];
    if (predicate(value)) {
      return value;
    }
  }
  return;
}
__name(find17, "find");
function forEach17(record, run) {
  Object.entries(record).forEach(([key, value]) => run(value, key));
}
__name(forEach17, "forEach");
function includes17(arr, value) {
  return arr.indexOf(value) !== -1;
}
__name(includes17, "includes");
function findArr17(record, predicate) {
  for (let i = 0; i < record.length; i++) {
    const value = record[i];
    if (predicate(value)) {
      return value;
    }
  }
  return;
}
__name(findArr17, "findArr");
var CustomTransformerRegistry17 = class {
  static {
    __name(this, "CustomTransformerRegistry");
  }
  constructor() {
    this.transfomers = {};
  }
  register(transformer) {
    this.transfomers[transformer.name] = transformer;
  }
  findApplicable(v) {
    return find17(this.transfomers, (transformer) => transformer.isApplicable(v));
  }
  findByName(name) {
    return this.transfomers[name];
  }
};
var getType18 = /* @__PURE__ */ __name((payload) => Object.prototype.toString.call(payload).slice(8, -1), "getType");
var isUndefined17 = /* @__PURE__ */ __name((payload) => typeof payload === "undefined", "isUndefined");
var isNull18 = /* @__PURE__ */ __name((payload) => payload === null, "isNull");
var isPlainObject18 = /* @__PURE__ */ __name((payload) => {
  if (typeof payload !== "object" || payload === null)
    return false;
  if (payload === Object.prototype)
    return false;
  if (Object.getPrototypeOf(payload) === null)
    return true;
  return Object.getPrototypeOf(payload) === Object.prototype;
}, "isPlainObject");
var isEmptyObject17 = /* @__PURE__ */ __name((payload) => isPlainObject18(payload) && Object.keys(payload).length === 0, "isEmptyObject");
var isArray18 = /* @__PURE__ */ __name((payload) => Array.isArray(payload), "isArray");
var isString17 = /* @__PURE__ */ __name((payload) => typeof payload === "string", "isString");
var isNumber17 = /* @__PURE__ */ __name((payload) => typeof payload === "number" && !isNaN(payload), "isNumber");
var isBoolean17 = /* @__PURE__ */ __name((payload) => typeof payload === "boolean", "isBoolean");
var isRegExp17 = /* @__PURE__ */ __name((payload) => payload instanceof RegExp, "isRegExp");
var isMap17 = /* @__PURE__ */ __name((payload) => payload instanceof Map, "isMap");
var isSet17 = /* @__PURE__ */ __name((payload) => payload instanceof Set, "isSet");
var isSymbol17 = /* @__PURE__ */ __name((payload) => getType18(payload) === "Symbol", "isSymbol");
var isDate17 = /* @__PURE__ */ __name((payload) => payload instanceof Date && !isNaN(payload.valueOf()), "isDate");
var isError17 = /* @__PURE__ */ __name((payload) => payload instanceof Error, "isError");
var isNaNValue17 = /* @__PURE__ */ __name((payload) => typeof payload === "number" && isNaN(payload), "isNaNValue");
var isPrimitive17 = /* @__PURE__ */ __name((payload) => isBoolean17(payload) || isNull18(payload) || isUndefined17(payload) || isNumber17(payload) || isString17(payload) || isSymbol17(payload), "isPrimitive");
var isBigint17 = /* @__PURE__ */ __name((payload) => typeof payload === "bigint", "isBigint");
var isInfinite17 = /* @__PURE__ */ __name((payload) => payload === Infinity || payload === -Infinity, "isInfinite");
var isTypedArray17 = /* @__PURE__ */ __name((payload) => ArrayBuffer.isView(payload) && !(payload instanceof DataView), "isTypedArray");
var isURL17 = /* @__PURE__ */ __name((payload) => payload instanceof URL, "isURL");
var escapeKey17 = /* @__PURE__ */ __name((key) => key.replace(/\\/g, "\\\\").replace(/\./g, "\\."), "escapeKey");
var stringifyPath17 = /* @__PURE__ */ __name((path) => path.map(String).map(escapeKey17).join("."), "stringifyPath");
var parsePath17 = /* @__PURE__ */ __name((string2, legacyPaths) => {
  const result = [];
  let segment = "";
  for (let i = 0; i < string2.length; i++) {
    let char = string2.charAt(i);
    if (!legacyPaths && char === "\\") {
      const escaped = string2.charAt(i + 1);
      if (escaped === "\\") {
        segment += "\\";
        i++;
        continue;
      } else if (escaped !== ".") {
        throw Error("invalid path");
      }
    }
    const isEscapedDot = char === "\\" && string2.charAt(i + 1) === ".";
    if (isEscapedDot) {
      segment += ".";
      i++;
      continue;
    }
    const isEndOfSegment = char === ".";
    if (isEndOfSegment) {
      result.push(segment);
      segment = "";
      continue;
    }
    segment += char;
  }
  const lastSegment = segment;
  result.push(lastSegment);
  return result;
}, "parsePath");
function simpleTransformation17(isApplicable, annotation, transform, untransform) {
  return {
    isApplicable,
    annotation,
    transform,
    untransform
  };
}
__name(simpleTransformation17, "simpleTransformation");
var simpleRules17 = [
  simpleTransformation17(isUndefined17, "undefined", () => null, () => {
    return;
  }),
  simpleTransformation17(isBigint17, "bigint", (v) => v.toString(), (v) => {
    if (typeof BigInt !== "undefined") {
      return BigInt(v);
    }
    console.error("Please add a BigInt polyfill.");
    return v;
  }),
  simpleTransformation17(isDate17, "Date", (v) => v.toISOString(), (v) => new Date(v)),
  simpleTransformation17(isError17, "Error", (v, superJson) => {
    const baseError = {
      name: v.name,
      message: v.message
    };
    if ("cause" in v) {
      baseError.cause = v.cause;
    }
    superJson.allowedErrorProps.forEach((prop) => {
      baseError[prop] = v[prop];
    });
    return baseError;
  }, (v, superJson) => {
    const e = new Error(v.message, { cause: v.cause });
    e.name = v.name;
    e.stack = v.stack;
    superJson.allowedErrorProps.forEach((prop) => {
      e[prop] = v[prop];
    });
    return e;
  }),
  simpleTransformation17(isRegExp17, "regexp", (v) => "" + v, (regex) => {
    const body = regex.slice(1, regex.lastIndexOf("/"));
    const flags2 = regex.slice(regex.lastIndexOf("/") + 1);
    return new RegExp(body, flags2);
  }),
  simpleTransformation17(isSet17, "set", (v) => [...v.values()], (v) => new Set(v)),
  simpleTransformation17(isMap17, "map", (v) => [...v.entries()], (v) => new Map(v)),
  simpleTransformation17((v) => isNaNValue17(v) || isInfinite17(v), "number", (v) => {
    if (isNaNValue17(v)) {
      return "NaN";
    }
    if (v > 0) {
      return "Infinity";
    } else {
      return "-Infinity";
    }
  }, Number),
  simpleTransformation17((v) => v === 0 && 1 / v === -Infinity, "number", () => {
    return "-0";
  }, Number),
  simpleTransformation17(isURL17, "URL", (v) => v.toString(), (v) => new URL(v))
];
function compositeTransformation17(isApplicable, annotation, transform, untransform) {
  return {
    isApplicable,
    annotation,
    transform,
    untransform
  };
}
__name(compositeTransformation17, "compositeTransformation");
var symbolRule17 = compositeTransformation17((s, superJson) => {
  if (isSymbol17(s)) {
    const isRegistered = !!superJson.symbolRegistry.getIdentifier(s);
    return isRegistered;
  }
  return false;
}, (s, superJson) => {
  const identifier = superJson.symbolRegistry.getIdentifier(s);
  return ["symbol", identifier];
}, (v) => v.description, (_, a, superJson) => {
  const value = superJson.symbolRegistry.getValue(a[1]);
  if (!value) {
    throw new Error("Trying to deserialize unknown symbol");
  }
  return value;
});
var constructorToName17 = [
  Int8Array,
  Uint8Array,
  Int16Array,
  Uint16Array,
  Int32Array,
  Uint32Array,
  Float32Array,
  Float64Array,
  Uint8ClampedArray
].reduce((obj, ctor) => {
  obj[ctor.name] = ctor;
  return obj;
}, {});
var typedArrayRule17 = compositeTransformation17(isTypedArray17, (v) => ["typed-array", v.constructor.name], (v) => [...v], (v, a) => {
  const ctor = constructorToName17[a[1]];
  if (!ctor) {
    throw new Error("Trying to deserialize unknown typed array");
  }
  return new ctor(v);
});
function isInstanceOfRegisteredClass17(potentialClass, superJson) {
  if (potentialClass?.constructor) {
    const isRegistered = !!superJson.classRegistry.getIdentifier(potentialClass.constructor);
    return isRegistered;
  }
  return false;
}
__name(isInstanceOfRegisteredClass17, "isInstanceOfRegisteredClass");
var classRule17 = compositeTransformation17(isInstanceOfRegisteredClass17, (clazz, superJson) => {
  const identifier = superJson.classRegistry.getIdentifier(clazz.constructor);
  return ["class", identifier];
}, (clazz, superJson) => {
  const allowedProps = superJson.classRegistry.getAllowedProps(clazz.constructor);
  if (!allowedProps) {
    return { ...clazz };
  }
  const result = {};
  allowedProps.forEach((prop) => {
    result[prop] = clazz[prop];
  });
  return result;
}, (v, a, superJson) => {
  const clazz = superJson.classRegistry.getValue(a[1]);
  if (!clazz) {
    throw new Error(`Trying to deserialize unknown class '${a[1]}' - check https://github.com/blitz-js/superjson/issues/116#issuecomment-773996564`);
  }
  return Object.assign(Object.create(clazz.prototype), v);
});
var customRule17 = compositeTransformation17((value, superJson) => {
  return !!superJson.customTransformerRegistry.findApplicable(value);
}, (value, superJson) => {
  const transformer = superJson.customTransformerRegistry.findApplicable(value);
  return ["custom", transformer.name];
}, (value, superJson) => {
  const transformer = superJson.customTransformerRegistry.findApplicable(value);
  return transformer.serialize(value);
}, (v, a, superJson) => {
  const transformer = superJson.customTransformerRegistry.findByName(a[1]);
  if (!transformer) {
    throw new Error("Trying to deserialize unknown custom value");
  }
  return transformer.deserialize(v);
});
var compositeRules17 = [classRule17, symbolRule17, customRule17, typedArrayRule17];
var transformValue17 = /* @__PURE__ */ __name((value, superJson) => {
  const applicableCompositeRule = findArr17(compositeRules17, (rule) => rule.isApplicable(value, superJson));
  if (applicableCompositeRule) {
    return {
      value: applicableCompositeRule.transform(value, superJson),
      type: applicableCompositeRule.annotation(value, superJson)
    };
  }
  const applicableSimpleRule = findArr17(simpleRules17, (rule) => rule.isApplicable(value, superJson));
  if (applicableSimpleRule) {
    return {
      value: applicableSimpleRule.transform(value, superJson),
      type: applicableSimpleRule.annotation
    };
  }
  return;
}, "transformValue");
var simpleRulesByAnnotation17 = {};
simpleRules17.forEach((rule) => {
  simpleRulesByAnnotation17[rule.annotation] = rule;
});
var untransformValue17 = /* @__PURE__ */ __name((json, type, superJson) => {
  if (isArray18(type)) {
    switch (type[0]) {
      case "symbol":
        return symbolRule17.untransform(json, type, superJson);
      case "class":
        return classRule17.untransform(json, type, superJson);
      case "custom":
        return customRule17.untransform(json, type, superJson);
      case "typed-array":
        return typedArrayRule17.untransform(json, type, superJson);
      default:
        throw new Error("Unknown transformation: " + type);
    }
  } else {
    const transformation = simpleRulesByAnnotation17[type];
    if (!transformation) {
      throw new Error("Unknown transformation: " + type);
    }
    return transformation.untransform(json, superJson);
  }
}, "untransformValue");
var getNthKey17 = /* @__PURE__ */ __name((value, n) => {
  if (n > value.size)
    throw new Error("index out of bounds");
  const keys = value.keys();
  while (n > 0) {
    keys.next();
    n--;
  }
  return keys.next().value;
}, "getNthKey");
function validatePath17(path) {
  if (includes17(path, "__proto__")) {
    throw new Error("__proto__ is not allowed as a property");
  }
  if (includes17(path, "prototype")) {
    throw new Error("prototype is not allowed as a property");
  }
  if (includes17(path, "constructor")) {
    throw new Error("constructor is not allowed as a property");
  }
}
__name(validatePath17, "validatePath");
var getDeep17 = /* @__PURE__ */ __name((object2, path) => {
  validatePath17(path);
  for (let i = 0; i < path.length; i++) {
    const key = path[i];
    if (isSet17(object2)) {
      object2 = getNthKey17(object2, +key);
    } else if (isMap17(object2)) {
      const row = +key;
      const type = +path[++i] === 0 ? "key" : "value";
      const keyOfRow = getNthKey17(object2, row);
      switch (type) {
        case "key":
          object2 = keyOfRow;
          break;
        case "value":
          object2 = object2.get(keyOfRow);
          break;
      }
    } else {
      object2 = object2[key];
    }
  }
  return object2;
}, "getDeep");
var setDeep17 = /* @__PURE__ */ __name((object2, path, mapper) => {
  validatePath17(path);
  if (path.length === 0) {
    return mapper(object2);
  }
  let parent = object2;
  for (let i = 0; i < path.length - 1; i++) {
    const key = path[i];
    if (isArray18(parent)) {
      const index = +key;
      parent = parent[index];
    } else if (isPlainObject18(parent)) {
      parent = parent[key];
    } else if (isSet17(parent)) {
      const row = +key;
      parent = getNthKey17(parent, row);
    } else if (isMap17(parent)) {
      const isEnd = i === path.length - 2;
      if (isEnd) {
        break;
      }
      const row = +key;
      const type = +path[++i] === 0 ? "key" : "value";
      const keyOfRow = getNthKey17(parent, row);
      switch (type) {
        case "key":
          parent = keyOfRow;
          break;
        case "value":
          parent = parent.get(keyOfRow);
          break;
      }
    }
  }
  const lastKey = path[path.length - 1];
  if (isArray18(parent)) {
    parent[+lastKey] = mapper(parent[+lastKey]);
  } else if (isPlainObject18(parent)) {
    parent[lastKey] = mapper(parent[lastKey]);
  }
  if (isSet17(parent)) {
    const oldValue = getNthKey17(parent, +lastKey);
    const newValue = mapper(oldValue);
    if (oldValue !== newValue) {
      parent.delete(oldValue);
      parent.add(newValue);
    }
  }
  if (isMap17(parent)) {
    const row = +path[path.length - 2];
    const keyToRow = getNthKey17(parent, row);
    const type = +lastKey === 0 ? "key" : "value";
    switch (type) {
      case "key": {
        const newKey = mapper(keyToRow);
        parent.set(newKey, parent.get(keyToRow));
        if (newKey !== keyToRow) {
          parent.delete(keyToRow);
        }
        break;
      }
      case "value": {
        parent.set(keyToRow, mapper(parent.get(keyToRow)));
        break;
      }
    }
  }
  return object2;
}, "setDeep");
var enableLegacyPaths17 = /* @__PURE__ */ __name((version4) => version4 < 1, "enableLegacyPaths");
function traverse17(tree, walker26, version4, origin = []) {
  if (!tree) {
    return;
  }
  const legacyPaths = enableLegacyPaths17(version4);
  if (!isArray18(tree)) {
    forEach17(tree, (subtree, key) => traverse17(subtree, walker26, version4, [
      ...origin,
      ...parsePath17(key, legacyPaths)
    ]));
    return;
  }
  const [nodeValue, children] = tree;
  if (children) {
    forEach17(children, (child, key) => {
      traverse17(child, walker26, version4, [
        ...origin,
        ...parsePath17(key, legacyPaths)
      ]);
    });
  }
  walker26(nodeValue, origin);
}
__name(traverse17, "traverse");
function applyValueAnnotations17(plain, annotations, version4, superJson) {
  traverse17(annotations, (type, path) => {
    plain = setDeep17(plain, path, (v) => untransformValue17(v, type, superJson));
  }, version4);
  return plain;
}
__name(applyValueAnnotations17, "applyValueAnnotations");
function applyReferentialEqualityAnnotations17(plain, annotations, version4) {
  const legacyPaths = enableLegacyPaths17(version4);
  function apply(identicalPaths, path) {
    const object2 = getDeep17(plain, parsePath17(path, legacyPaths));
    identicalPaths.map((path2) => parsePath17(path2, legacyPaths)).forEach((identicalObjectPath) => {
      plain = setDeep17(plain, identicalObjectPath, () => object2);
    });
  }
  __name(apply, "apply");
  if (isArray18(annotations)) {
    const [root, other] = annotations;
    root.forEach((identicalPath) => {
      plain = setDeep17(plain, parsePath17(identicalPath, legacyPaths), () => plain);
    });
    if (other) {
      forEach17(other, apply);
    }
  } else {
    forEach17(annotations, apply);
  }
  return plain;
}
__name(applyReferentialEqualityAnnotations17, "applyReferentialEqualityAnnotations");
var isDeep17 = /* @__PURE__ */ __name((object2, superJson) => isPlainObject18(object2) || isArray18(object2) || isMap17(object2) || isSet17(object2) || isError17(object2) || isInstanceOfRegisteredClass17(object2, superJson), "isDeep");
function addIdentity17(object2, path, identities) {
  const existingSet = identities.get(object2);
  if (existingSet) {
    existingSet.push(path);
  } else {
    identities.set(object2, [path]);
  }
}
__name(addIdentity17, "addIdentity");
function generateReferentialEqualityAnnotations17(identitites, dedupe) {
  const result = {};
  let rootEqualityPaths = void 0;
  identitites.forEach((paths) => {
    if (paths.length <= 1) {
      return;
    }
    if (!dedupe) {
      paths = paths.map((path) => path.map(String)).sort((a, b) => a.length - b.length);
    }
    const [representativePath, ...identicalPaths] = paths;
    if (representativePath.length === 0) {
      rootEqualityPaths = identicalPaths.map(stringifyPath17);
    } else {
      result[stringifyPath17(representativePath)] = identicalPaths.map(stringifyPath17);
    }
  });
  if (rootEqualityPaths) {
    if (isEmptyObject17(result)) {
      return [rootEqualityPaths];
    } else {
      return [rootEqualityPaths, result];
    }
  } else {
    return isEmptyObject17(result) ? void 0 : result;
  }
}
__name(generateReferentialEqualityAnnotations17, "generateReferentialEqualityAnnotations");
var walker17 = /* @__PURE__ */ __name((object2, identities, superJson, dedupe, path = [], objectsInThisPath = [], seenObjects = /* @__PURE__ */ new Map()) => {
  const primitive = isPrimitive17(object2);
  if (!primitive) {
    addIdentity17(object2, path, identities);
    const seen = seenObjects.get(object2);
    if (seen) {
      return dedupe ? {
        transformedValue: null
      } : seen;
    }
  }
  if (!isDeep17(object2, superJson)) {
    const transformed2 = transformValue17(object2, superJson);
    const result2 = transformed2 ? {
      transformedValue: transformed2.value,
      annotations: [transformed2.type]
    } : {
      transformedValue: object2
    };
    if (!primitive) {
      seenObjects.set(object2, result2);
    }
    return result2;
  }
  if (includes17(objectsInThisPath, object2)) {
    return {
      transformedValue: null
    };
  }
  const transformationResult = transformValue17(object2, superJson);
  const transformed = transformationResult?.value ?? object2;
  const transformedValue = isArray18(transformed) ? [] : {};
  const innerAnnotations = {};
  forEach17(transformed, (value, index) => {
    if (index === "__proto__" || index === "constructor" || index === "prototype") {
      throw new Error(`Detected property ${index}. This is a prototype pollution risk, please remove it from your object.`);
    }
    const recursiveResult = walker17(value, identities, superJson, dedupe, [...path, index], [...objectsInThisPath, object2], seenObjects);
    transformedValue[index] = recursiveResult.transformedValue;
    if (isArray18(recursiveResult.annotations)) {
      innerAnnotations[escapeKey17(index)] = recursiveResult.annotations;
    } else if (isPlainObject18(recursiveResult.annotations)) {
      forEach17(recursiveResult.annotations, (tree, key) => {
        innerAnnotations[escapeKey17(index) + "." + key] = tree;
      });
    }
  });
  const result = isEmptyObject17(innerAnnotations) ? {
    transformedValue,
    annotations: transformationResult ? [transformationResult.type] : void 0
  } : {
    transformedValue,
    annotations: transformationResult ? [transformationResult.type, innerAnnotations] : innerAnnotations
  };
  if (!primitive) {
    seenObjects.set(object2, result);
  }
  return result;
}, "walker");
function getType217(payload) {
  return Object.prototype.toString.call(payload).slice(8, -1);
}
__name(getType217, "getType2");
function isArray217(payload) {
  return getType217(payload) === "Array";
}
__name(isArray217, "isArray2");
function isPlainObject217(payload) {
  if (getType217(payload) !== "Object")
    return false;
  const prototype = Object.getPrototypeOf(payload);
  return !!prototype && prototype.constructor === Object && prototype === Object.prototype;
}
__name(isPlainObject217, "isPlainObject2");
function assignProp17(carry, key, newVal, originalObject, includeNonenumerable) {
  const propType = {}.propertyIsEnumerable.call(originalObject, key) ? "enumerable" : "nonenumerable";
  if (propType === "enumerable")
    carry[key] = newVal;
  if (includeNonenumerable && propType === "nonenumerable") {
    Object.defineProperty(carry, key, {
      value: newVal,
      enumerable: false,
      writable: true,
      configurable: true
    });
  }
}
__name(assignProp17, "assignProp");
function copy17(target, options = {}) {
  if (isArray217(target)) {
    return target.map((item) => copy17(item, options));
  }
  if (!isPlainObject217(target)) {
    return target;
  }
  const props = Object.getOwnPropertyNames(target);
  const symbols = Object.getOwnPropertySymbols(target);
  return [...props, ...symbols].reduce((carry, key) => {
    if (key === "__proto__")
      return carry;
    if (isArray217(options.props) && !options.props.includes(key)) {
      return carry;
    }
    const val = target[key];
    const newVal = copy17(val, options);
    assignProp17(carry, key, newVal, target, options.nonenumerable);
    return carry;
  }, {});
}
__name(copy17, "copy");
var SuperJSON17 = class {
  static {
    __name(this, "SuperJSON");
  }
  constructor({ dedupe = false } = {}) {
    this.classRegistry = new ClassRegistry17();
    this.symbolRegistry = new Registry17((s) => s.description ?? "");
    this.customTransformerRegistry = new CustomTransformerRegistry17();
    this.allowedErrorProps = [];
    this.dedupe = dedupe;
  }
  serialize(object2) {
    const identities = /* @__PURE__ */ new Map();
    const output = walker17(object2, identities, this, this.dedupe);
    const res = {
      json: output.transformedValue
    };
    if (output.annotations) {
      res.meta = {
        ...res.meta,
        values: output.annotations
      };
    }
    const equalityAnnotations = generateReferentialEqualityAnnotations17(identities, this.dedupe);
    if (equalityAnnotations) {
      res.meta = {
        ...res.meta,
        referentialEqualities: equalityAnnotations
      };
    }
    if (res.meta)
      res.meta.v = 1;
    return res;
  }
  deserialize(payload, options) {
    const { json, meta } = payload;
    let result = options?.inPlace ? json : copy17(json);
    if (meta?.values) {
      result = applyValueAnnotations17(result, meta.values, meta.v ?? 0, this);
    }
    if (meta?.referentialEqualities) {
      result = applyReferentialEqualityAnnotations17(result, meta.referentialEqualities, meta.v ?? 0);
    }
    return result;
  }
  stringify(object2) {
    return JSON.stringify(this.serialize(object2));
  }
  parse(string2) {
    return this.deserialize(JSON.parse(string2), { inPlace: true });
  }
  registerClass(v, options) {
    this.classRegistry.register(v, options);
  }
  registerSymbol(v, identifier) {
    this.symbolRegistry.register(v, identifier);
  }
  registerCustom(transformer, name) {
    this.customTransformerRegistry.register({
      name,
      ...transformer
    });
  }
  allowErrorProps(...props) {
    this.allowedErrorProps.push(...props);
  }
};
SuperJSON17.defaultInstance = new SuperJSON17();
SuperJSON17.serialize = SuperJSON17.defaultInstance.serialize.bind(SuperJSON17.defaultInstance);
SuperJSON17.deserialize = SuperJSON17.defaultInstance.deserialize.bind(SuperJSON17.defaultInstance);
SuperJSON17.stringify = SuperJSON17.defaultInstance.stringify.bind(SuperJSON17.defaultInstance);
SuperJSON17.parse = SuperJSON17.defaultInstance.parse.bind(SuperJSON17.defaultInstance);
SuperJSON17.registerClass = SuperJSON17.defaultInstance.registerClass.bind(SuperJSON17.defaultInstance);
SuperJSON17.registerSymbol = SuperJSON17.defaultInstance.registerSymbol.bind(SuperJSON17.defaultInstance);
SuperJSON17.registerCustom = SuperJSON17.defaultInstance.registerCustom.bind(SuperJSON17.defaultInstance);
SuperJSON17.allowErrorProps = SuperJSON17.defaultInstance.allowErrorProps.bind(SuperJSON17.defaultInstance);
var dist_default17 = SuperJSON17;
var serialize17 = SuperJSON17.serialize;
var deserialize17 = SuperJSON17.deserialize;
var stringify18 = SuperJSON17.stringify;
var parse17 = SuperJSON17.parse;
var registerClass17 = SuperJSON17.registerClass;
var registerCustom17 = SuperJSON17.registerCustom;
var registerSymbol17 = SuperJSON17.registerSymbol;
var allowErrorProps17 = SuperJSON17.allowErrorProps;
function parseData17(formData) {
  const propsArray = [];
  if (!formData)
    return propsArray;
  const batchsIDs = [];
  for (const [key, value] of Array.from(formData.entries())) {
    if (key.startsWith("FILE_"))
      propsArray.push(value);
    else if (key.startsWith("FILES_")) {
      if (batchsIDs.includes(key))
        continue;
      batchsIDs.push(key);
      propsArray.push(formData.getAll(key));
    } else {
      propsArray.push(JSON.parse(decodeURI(value)));
    }
  }
  return propsArray;
}
__name(parseData17, "parseData");
function paramsFromURL17(url) {
  const params = url.searchParams.entries().toArray().map(([_, v]) => v);
  return params.map((param) => JSON.parse(decodeURIComponent(param)));
}
__name(paramsFromURL17, "paramsFromURL");
async function WrapRequestHandler17(context2, endpoint) {
  const isServerAction = context2.request.headers.get("x-server-action") === "true";
  if (!isServerAction) {
    return new Response("Not Found", { status: 404 });
  }
  const parsedData = context2.request.method === "GET" || context2.request.method === "HEAD" ? paramsFromURL17(new URL(context2.request.url)) : parseData17(context2.request.headers.get("content-type") ? await context2.request.formData() : void 0);
  const missingProps = endpoint.length - parsedData.length;
  for (let i = 0; i < missingProps; i++) {
    parsedData.push(void 0);
  }
  parsedData.push(context2);
  const result = await endpoint(...parsedData);
  switch (typeof result) {
    case "string":
    case "number":
    case "boolean":
    case "bigint":
      return new Response(dist_default17.stringify(result), {
        headers: {
          "Content-Type": "application/json",
          dataType: "json"
        }
      });
    case "undefined":
      return new Response(null, { status: 204 });
    case "object":
      if (result instanceof Response) {
        result.headers.set("dataType", "response");
        return result;
      } else if (result instanceof Blob) {
        const res = new Response(await result.arrayBuffer());
        res.headers.set("dataType", "blob");
        res.headers.set("Content-Type", result.type);
        return res;
      } else if (result instanceof File) {
        return new Response(await result.arrayBuffer(), {
          headers: {
            "Content-Type": result.type,
            dataType: "file",
            fileData: JSON.stringify({
              name: result.name,
              lastModified: result.lastModified
            })
          }
        });
      } else {
        return new Response(dist_default17.stringify(result), {
          headers: { "Content-Type": "application/json", dataType: "json" }
        });
      }
    default:
      throw new Error(`Unsupported return type from action: ${typeof result}`);
  }
}
__name(WrapRequestHandler17, "WrapRequestHandler");
var onRequest17 = /* @__PURE__ */ __name(async (context2) => {
  const method = context2.request.method;
  const options = {
    GET: typeof GET === "function" ? GET : void 0,
    POST: typeof POST11 === "function" ? POST11 : void 0,
    PUT: typeof PUT === "function" ? PUT : void 0,
    DELETE: typeof DELETE === "function" ? DELETE : void 0,
    PATCH: typeof PATCH === "function" ? PATCH : void 0,
    HEAD: typeof HEAD === "function" ? HEAD : void 0,
    OPTIONS: typeof OPTIONS === "function" ? OPTIONS : void 0
  };
  if (!options[method]) {
    return new Response(`Method "${method}" Not Allowed`, {
      status: 405
    });
  }
  return await WrapRequestHandler17(context2, options[method]);
}, "onRequest");

// api/invites/index.js
async function GET9({
  type,
  status,
  owner_group_id
}) {
  const ctx = getContext(arguments);
  const db = drizzle(ctx.env.PROJECT_DB);
  const userInfo = await ctx.data.client.getMetaData();
  if (!userInfo) {
    return {
      success: false,
      error: "Unauthorized"
    };
  }
  if (type === "received") {
    const invites2 = await db.select().from(inviteTable).where(status ? and(eq(inviteTable.user_id, userInfo.id), eq(inviteTable.status, status), owner_group_id ? eq(inviteTable.owner_group_id, owner_group_id) : void 0) : owner_group_id ? and(eq(inviteTable.user_id, userInfo.id), eq(inviteTable.owner_group_id, owner_group_id)) : eq(inviteTable.user_id, userInfo.id)).all();
    return {
      success: true,
      data: invites2
    };
  } else if (type === "sent") {
    const invites2 = await db.select().from(inviteTable).where(status ? and(eq(inviteTable.from_user_id, userInfo.id), eq(inviteTable.status, status), owner_group_id ? eq(inviteTable.owner_group_id, owner_group_id) : void 0) : owner_group_id ? and(eq(inviteTable.from_user_id, userInfo.id), eq(inviteTable.owner_group_id, owner_group_id)) : eq(inviteTable.from_user_id, userInfo.id)).all();
    return {
      success: true,
      data: invites2
    };
  } else if (type === "all") {
    const invites2 = await db.select().from(inviteTable).where(status ? and(or(eq(inviteTable.user_id, userInfo.id), eq(inviteTable.from_user_id, userInfo.id)), eq(inviteTable.status, status), owner_group_id ? eq(inviteTable.owner_group_id, owner_group_id) : void 0) : owner_group_id ? and(or(eq(inviteTable.user_id, userInfo.id), eq(inviteTable.from_user_id, userInfo.id)), eq(inviteTable.owner_group_id, owner_group_id)) : or(eq(inviteTable.user_id, userInfo.id), eq(inviteTable.from_user_id, userInfo.id))).all();
    return {
      success: true,
      data: invites2
    };
  }
}
__name(GET9, "GET");
async function DELETE11({ code }) {
  const ctx = getContext(arguments);
  const userInfo = await ctx.data.client.getMetaData();
  if (!userInfo) {
    return { success: false, error: "Unauthorized" };
  }
  const res = await invites(ctx.env.PROJECT_DB).declineInvite(code, userInfo.id);
  return {
    success: res.success,
    error: res.error
  };
}
__name(DELETE11, "DELETE");
var DoubleIndexedKV18 = class {
  static {
    __name(this, "DoubleIndexedKV");
  }
  constructor() {
    this.keyToValue = /* @__PURE__ */ new Map();
    this.valueToKey = /* @__PURE__ */ new Map();
  }
  set(key, value) {
    this.keyToValue.set(key, value);
    this.valueToKey.set(value, key);
  }
  getByKey(key) {
    return this.keyToValue.get(key);
  }
  getByValue(value) {
    return this.valueToKey.get(value);
  }
  clear() {
    this.keyToValue.clear();
    this.valueToKey.clear();
  }
};
var Registry18 = class {
  static {
    __name(this, "Registry");
  }
  constructor(generateIdentifier) {
    this.generateIdentifier = generateIdentifier;
    this.kv = new DoubleIndexedKV18();
  }
  register(value, identifier) {
    if (this.kv.getByValue(value)) {
      return;
    }
    if (!identifier) {
      identifier = this.generateIdentifier(value);
    }
    this.kv.set(identifier, value);
  }
  clear() {
    this.kv.clear();
  }
  getIdentifier(value) {
    return this.kv.getByValue(value);
  }
  getValue(identifier) {
    return this.kv.getByKey(identifier);
  }
};
var ClassRegistry18 = class extends Registry18 {
  static {
    __name(this, "ClassRegistry");
  }
  constructor() {
    super((c) => c.name);
    this.classToAllowedProps = /* @__PURE__ */ new Map();
  }
  register(value, options) {
    if (typeof options === "object") {
      if (options.allowProps) {
        this.classToAllowedProps.set(value, options.allowProps);
      }
      super.register(value, options.identifier);
    } else {
      super.register(value, options);
    }
  }
  getAllowedProps(value) {
    return this.classToAllowedProps.get(value);
  }
};
function valuesOfObj18(record) {
  if ("values" in Object) {
    return Object.values(record);
  }
  const values = [];
  for (const key in record) {
    if (record.hasOwnProperty(key)) {
      values.push(record[key]);
    }
  }
  return values;
}
__name(valuesOfObj18, "valuesOfObj");
function find18(record, predicate) {
  const values = valuesOfObj18(record);
  if ("find" in values) {
    return values.find(predicate);
  }
  const valuesNotNever = values;
  for (let i = 0; i < valuesNotNever.length; i++) {
    const value = valuesNotNever[i];
    if (predicate(value)) {
      return value;
    }
  }
  return;
}
__name(find18, "find");
function forEach18(record, run) {
  Object.entries(record).forEach(([key, value]) => run(value, key));
}
__name(forEach18, "forEach");
function includes18(arr, value) {
  return arr.indexOf(value) !== -1;
}
__name(includes18, "includes");
function findArr18(record, predicate) {
  for (let i = 0; i < record.length; i++) {
    const value = record[i];
    if (predicate(value)) {
      return value;
    }
  }
  return;
}
__name(findArr18, "findArr");
var CustomTransformerRegistry18 = class {
  static {
    __name(this, "CustomTransformerRegistry");
  }
  constructor() {
    this.transfomers = {};
  }
  register(transformer) {
    this.transfomers[transformer.name] = transformer;
  }
  findApplicable(v) {
    return find18(this.transfomers, (transformer) => transformer.isApplicable(v));
  }
  findByName(name) {
    return this.transfomers[name];
  }
};
var getType19 = /* @__PURE__ */ __name((payload) => Object.prototype.toString.call(payload).slice(8, -1), "getType");
var isUndefined18 = /* @__PURE__ */ __name((payload) => typeof payload === "undefined", "isUndefined");
var isNull19 = /* @__PURE__ */ __name((payload) => payload === null, "isNull");
var isPlainObject19 = /* @__PURE__ */ __name((payload) => {
  if (typeof payload !== "object" || payload === null)
    return false;
  if (payload === Object.prototype)
    return false;
  if (Object.getPrototypeOf(payload) === null)
    return true;
  return Object.getPrototypeOf(payload) === Object.prototype;
}, "isPlainObject");
var isEmptyObject18 = /* @__PURE__ */ __name((payload) => isPlainObject19(payload) && Object.keys(payload).length === 0, "isEmptyObject");
var isArray19 = /* @__PURE__ */ __name((payload) => Array.isArray(payload), "isArray");
var isString18 = /* @__PURE__ */ __name((payload) => typeof payload === "string", "isString");
var isNumber18 = /* @__PURE__ */ __name((payload) => typeof payload === "number" && !isNaN(payload), "isNumber");
var isBoolean18 = /* @__PURE__ */ __name((payload) => typeof payload === "boolean", "isBoolean");
var isRegExp18 = /* @__PURE__ */ __name((payload) => payload instanceof RegExp, "isRegExp");
var isMap18 = /* @__PURE__ */ __name((payload) => payload instanceof Map, "isMap");
var isSet18 = /* @__PURE__ */ __name((payload) => payload instanceof Set, "isSet");
var isSymbol18 = /* @__PURE__ */ __name((payload) => getType19(payload) === "Symbol", "isSymbol");
var isDate18 = /* @__PURE__ */ __name((payload) => payload instanceof Date && !isNaN(payload.valueOf()), "isDate");
var isError18 = /* @__PURE__ */ __name((payload) => payload instanceof Error, "isError");
var isNaNValue18 = /* @__PURE__ */ __name((payload) => typeof payload === "number" && isNaN(payload), "isNaNValue");
var isPrimitive18 = /* @__PURE__ */ __name((payload) => isBoolean18(payload) || isNull19(payload) || isUndefined18(payload) || isNumber18(payload) || isString18(payload) || isSymbol18(payload), "isPrimitive");
var isBigint18 = /* @__PURE__ */ __name((payload) => typeof payload === "bigint", "isBigint");
var isInfinite18 = /* @__PURE__ */ __name((payload) => payload === Infinity || payload === -Infinity, "isInfinite");
var isTypedArray18 = /* @__PURE__ */ __name((payload) => ArrayBuffer.isView(payload) && !(payload instanceof DataView), "isTypedArray");
var isURL18 = /* @__PURE__ */ __name((payload) => payload instanceof URL, "isURL");
var escapeKey18 = /* @__PURE__ */ __name((key) => key.replace(/\\/g, "\\\\").replace(/\./g, "\\."), "escapeKey");
var stringifyPath18 = /* @__PURE__ */ __name((path) => path.map(String).map(escapeKey18).join("."), "stringifyPath");
var parsePath18 = /* @__PURE__ */ __name((string2, legacyPaths) => {
  const result = [];
  let segment = "";
  for (let i = 0; i < string2.length; i++) {
    let char = string2.charAt(i);
    if (!legacyPaths && char === "\\") {
      const escaped = string2.charAt(i + 1);
      if (escaped === "\\") {
        segment += "\\";
        i++;
        continue;
      } else if (escaped !== ".") {
        throw Error("invalid path");
      }
    }
    const isEscapedDot = char === "\\" && string2.charAt(i + 1) === ".";
    if (isEscapedDot) {
      segment += ".";
      i++;
      continue;
    }
    const isEndOfSegment = char === ".";
    if (isEndOfSegment) {
      result.push(segment);
      segment = "";
      continue;
    }
    segment += char;
  }
  const lastSegment = segment;
  result.push(lastSegment);
  return result;
}, "parsePath");
function simpleTransformation18(isApplicable, annotation, transform, untransform) {
  return {
    isApplicable,
    annotation,
    transform,
    untransform
  };
}
__name(simpleTransformation18, "simpleTransformation");
var simpleRules18 = [
  simpleTransformation18(isUndefined18, "undefined", () => null, () => {
    return;
  }),
  simpleTransformation18(isBigint18, "bigint", (v) => v.toString(), (v) => {
    if (typeof BigInt !== "undefined") {
      return BigInt(v);
    }
    console.error("Please add a BigInt polyfill.");
    return v;
  }),
  simpleTransformation18(isDate18, "Date", (v) => v.toISOString(), (v) => new Date(v)),
  simpleTransformation18(isError18, "Error", (v, superJson) => {
    const baseError = {
      name: v.name,
      message: v.message
    };
    if ("cause" in v) {
      baseError.cause = v.cause;
    }
    superJson.allowedErrorProps.forEach((prop) => {
      baseError[prop] = v[prop];
    });
    return baseError;
  }, (v, superJson) => {
    const e = new Error(v.message, { cause: v.cause });
    e.name = v.name;
    e.stack = v.stack;
    superJson.allowedErrorProps.forEach((prop) => {
      e[prop] = v[prop];
    });
    return e;
  }),
  simpleTransformation18(isRegExp18, "regexp", (v) => "" + v, (regex) => {
    const body = regex.slice(1, regex.lastIndexOf("/"));
    const flags2 = regex.slice(regex.lastIndexOf("/") + 1);
    return new RegExp(body, flags2);
  }),
  simpleTransformation18(isSet18, "set", (v) => [...v.values()], (v) => new Set(v)),
  simpleTransformation18(isMap18, "map", (v) => [...v.entries()], (v) => new Map(v)),
  simpleTransformation18((v) => isNaNValue18(v) || isInfinite18(v), "number", (v) => {
    if (isNaNValue18(v)) {
      return "NaN";
    }
    if (v > 0) {
      return "Infinity";
    } else {
      return "-Infinity";
    }
  }, Number),
  simpleTransformation18((v) => v === 0 && 1 / v === -Infinity, "number", () => {
    return "-0";
  }, Number),
  simpleTransformation18(isURL18, "URL", (v) => v.toString(), (v) => new URL(v))
];
function compositeTransformation18(isApplicable, annotation, transform, untransform) {
  return {
    isApplicable,
    annotation,
    transform,
    untransform
  };
}
__name(compositeTransformation18, "compositeTransformation");
var symbolRule18 = compositeTransformation18((s, superJson) => {
  if (isSymbol18(s)) {
    const isRegistered = !!superJson.symbolRegistry.getIdentifier(s);
    return isRegistered;
  }
  return false;
}, (s, superJson) => {
  const identifier = superJson.symbolRegistry.getIdentifier(s);
  return ["symbol", identifier];
}, (v) => v.description, (_, a, superJson) => {
  const value = superJson.symbolRegistry.getValue(a[1]);
  if (!value) {
    throw new Error("Trying to deserialize unknown symbol");
  }
  return value;
});
var constructorToName18 = [
  Int8Array,
  Uint8Array,
  Int16Array,
  Uint16Array,
  Int32Array,
  Uint32Array,
  Float32Array,
  Float64Array,
  Uint8ClampedArray
].reduce((obj, ctor) => {
  obj[ctor.name] = ctor;
  return obj;
}, {});
var typedArrayRule18 = compositeTransformation18(isTypedArray18, (v) => ["typed-array", v.constructor.name], (v) => [...v], (v, a) => {
  const ctor = constructorToName18[a[1]];
  if (!ctor) {
    throw new Error("Trying to deserialize unknown typed array");
  }
  return new ctor(v);
});
function isInstanceOfRegisteredClass18(potentialClass, superJson) {
  if (potentialClass?.constructor) {
    const isRegistered = !!superJson.classRegistry.getIdentifier(potentialClass.constructor);
    return isRegistered;
  }
  return false;
}
__name(isInstanceOfRegisteredClass18, "isInstanceOfRegisteredClass");
var classRule18 = compositeTransformation18(isInstanceOfRegisteredClass18, (clazz, superJson) => {
  const identifier = superJson.classRegistry.getIdentifier(clazz.constructor);
  return ["class", identifier];
}, (clazz, superJson) => {
  const allowedProps = superJson.classRegistry.getAllowedProps(clazz.constructor);
  if (!allowedProps) {
    return { ...clazz };
  }
  const result = {};
  allowedProps.forEach((prop) => {
    result[prop] = clazz[prop];
  });
  return result;
}, (v, a, superJson) => {
  const clazz = superJson.classRegistry.getValue(a[1]);
  if (!clazz) {
    throw new Error(`Trying to deserialize unknown class '${a[1]}' - check https://github.com/blitz-js/superjson/issues/116#issuecomment-773996564`);
  }
  return Object.assign(Object.create(clazz.prototype), v);
});
var customRule18 = compositeTransformation18((value, superJson) => {
  return !!superJson.customTransformerRegistry.findApplicable(value);
}, (value, superJson) => {
  const transformer = superJson.customTransformerRegistry.findApplicable(value);
  return ["custom", transformer.name];
}, (value, superJson) => {
  const transformer = superJson.customTransformerRegistry.findApplicable(value);
  return transformer.serialize(value);
}, (v, a, superJson) => {
  const transformer = superJson.customTransformerRegistry.findByName(a[1]);
  if (!transformer) {
    throw new Error("Trying to deserialize unknown custom value");
  }
  return transformer.deserialize(v);
});
var compositeRules18 = [classRule18, symbolRule18, customRule18, typedArrayRule18];
var transformValue18 = /* @__PURE__ */ __name((value, superJson) => {
  const applicableCompositeRule = findArr18(compositeRules18, (rule) => rule.isApplicable(value, superJson));
  if (applicableCompositeRule) {
    return {
      value: applicableCompositeRule.transform(value, superJson),
      type: applicableCompositeRule.annotation(value, superJson)
    };
  }
  const applicableSimpleRule = findArr18(simpleRules18, (rule) => rule.isApplicable(value, superJson));
  if (applicableSimpleRule) {
    return {
      value: applicableSimpleRule.transform(value, superJson),
      type: applicableSimpleRule.annotation
    };
  }
  return;
}, "transformValue");
var simpleRulesByAnnotation18 = {};
simpleRules18.forEach((rule) => {
  simpleRulesByAnnotation18[rule.annotation] = rule;
});
var untransformValue18 = /* @__PURE__ */ __name((json, type, superJson) => {
  if (isArray19(type)) {
    switch (type[0]) {
      case "symbol":
        return symbolRule18.untransform(json, type, superJson);
      case "class":
        return classRule18.untransform(json, type, superJson);
      case "custom":
        return customRule18.untransform(json, type, superJson);
      case "typed-array":
        return typedArrayRule18.untransform(json, type, superJson);
      default:
        throw new Error("Unknown transformation: " + type);
    }
  } else {
    const transformation = simpleRulesByAnnotation18[type];
    if (!transformation) {
      throw new Error("Unknown transformation: " + type);
    }
    return transformation.untransform(json, superJson);
  }
}, "untransformValue");
var getNthKey18 = /* @__PURE__ */ __name((value, n) => {
  if (n > value.size)
    throw new Error("index out of bounds");
  const keys = value.keys();
  while (n > 0) {
    keys.next();
    n--;
  }
  return keys.next().value;
}, "getNthKey");
function validatePath18(path) {
  if (includes18(path, "__proto__")) {
    throw new Error("__proto__ is not allowed as a property");
  }
  if (includes18(path, "prototype")) {
    throw new Error("prototype is not allowed as a property");
  }
  if (includes18(path, "constructor")) {
    throw new Error("constructor is not allowed as a property");
  }
}
__name(validatePath18, "validatePath");
var getDeep18 = /* @__PURE__ */ __name((object2, path) => {
  validatePath18(path);
  for (let i = 0; i < path.length; i++) {
    const key = path[i];
    if (isSet18(object2)) {
      object2 = getNthKey18(object2, +key);
    } else if (isMap18(object2)) {
      const row = +key;
      const type = +path[++i] === 0 ? "key" : "value";
      const keyOfRow = getNthKey18(object2, row);
      switch (type) {
        case "key":
          object2 = keyOfRow;
          break;
        case "value":
          object2 = object2.get(keyOfRow);
          break;
      }
    } else {
      object2 = object2[key];
    }
  }
  return object2;
}, "getDeep");
var setDeep18 = /* @__PURE__ */ __name((object2, path, mapper) => {
  validatePath18(path);
  if (path.length === 0) {
    return mapper(object2);
  }
  let parent = object2;
  for (let i = 0; i < path.length - 1; i++) {
    const key = path[i];
    if (isArray19(parent)) {
      const index = +key;
      parent = parent[index];
    } else if (isPlainObject19(parent)) {
      parent = parent[key];
    } else if (isSet18(parent)) {
      const row = +key;
      parent = getNthKey18(parent, row);
    } else if (isMap18(parent)) {
      const isEnd = i === path.length - 2;
      if (isEnd) {
        break;
      }
      const row = +key;
      const type = +path[++i] === 0 ? "key" : "value";
      const keyOfRow = getNthKey18(parent, row);
      switch (type) {
        case "key":
          parent = keyOfRow;
          break;
        case "value":
          parent = parent.get(keyOfRow);
          break;
      }
    }
  }
  const lastKey = path[path.length - 1];
  if (isArray19(parent)) {
    parent[+lastKey] = mapper(parent[+lastKey]);
  } else if (isPlainObject19(parent)) {
    parent[lastKey] = mapper(parent[lastKey]);
  }
  if (isSet18(parent)) {
    const oldValue = getNthKey18(parent, +lastKey);
    const newValue = mapper(oldValue);
    if (oldValue !== newValue) {
      parent.delete(oldValue);
      parent.add(newValue);
    }
  }
  if (isMap18(parent)) {
    const row = +path[path.length - 2];
    const keyToRow = getNthKey18(parent, row);
    const type = +lastKey === 0 ? "key" : "value";
    switch (type) {
      case "key": {
        const newKey = mapper(keyToRow);
        parent.set(newKey, parent.get(keyToRow));
        if (newKey !== keyToRow) {
          parent.delete(keyToRow);
        }
        break;
      }
      case "value": {
        parent.set(keyToRow, mapper(parent.get(keyToRow)));
        break;
      }
    }
  }
  return object2;
}, "setDeep");
var enableLegacyPaths18 = /* @__PURE__ */ __name((version4) => version4 < 1, "enableLegacyPaths");
function traverse18(tree, walker26, version4, origin = []) {
  if (!tree) {
    return;
  }
  const legacyPaths = enableLegacyPaths18(version4);
  if (!isArray19(tree)) {
    forEach18(tree, (subtree, key) => traverse18(subtree, walker26, version4, [
      ...origin,
      ...parsePath18(key, legacyPaths)
    ]));
    return;
  }
  const [nodeValue, children] = tree;
  if (children) {
    forEach18(children, (child, key) => {
      traverse18(child, walker26, version4, [
        ...origin,
        ...parsePath18(key, legacyPaths)
      ]);
    });
  }
  walker26(nodeValue, origin);
}
__name(traverse18, "traverse");
function applyValueAnnotations18(plain, annotations, version4, superJson) {
  traverse18(annotations, (type, path) => {
    plain = setDeep18(plain, path, (v) => untransformValue18(v, type, superJson));
  }, version4);
  return plain;
}
__name(applyValueAnnotations18, "applyValueAnnotations");
function applyReferentialEqualityAnnotations18(plain, annotations, version4) {
  const legacyPaths = enableLegacyPaths18(version4);
  function apply(identicalPaths, path) {
    const object2 = getDeep18(plain, parsePath18(path, legacyPaths));
    identicalPaths.map((path2) => parsePath18(path2, legacyPaths)).forEach((identicalObjectPath) => {
      plain = setDeep18(plain, identicalObjectPath, () => object2);
    });
  }
  __name(apply, "apply");
  if (isArray19(annotations)) {
    const [root, other] = annotations;
    root.forEach((identicalPath) => {
      plain = setDeep18(plain, parsePath18(identicalPath, legacyPaths), () => plain);
    });
    if (other) {
      forEach18(other, apply);
    }
  } else {
    forEach18(annotations, apply);
  }
  return plain;
}
__name(applyReferentialEqualityAnnotations18, "applyReferentialEqualityAnnotations");
var isDeep18 = /* @__PURE__ */ __name((object2, superJson) => isPlainObject19(object2) || isArray19(object2) || isMap18(object2) || isSet18(object2) || isError18(object2) || isInstanceOfRegisteredClass18(object2, superJson), "isDeep");
function addIdentity18(object2, path, identities) {
  const existingSet = identities.get(object2);
  if (existingSet) {
    existingSet.push(path);
  } else {
    identities.set(object2, [path]);
  }
}
__name(addIdentity18, "addIdentity");
function generateReferentialEqualityAnnotations18(identitites, dedupe) {
  const result = {};
  let rootEqualityPaths = void 0;
  identitites.forEach((paths) => {
    if (paths.length <= 1) {
      return;
    }
    if (!dedupe) {
      paths = paths.map((path) => path.map(String)).sort((a, b) => a.length - b.length);
    }
    const [representativePath, ...identicalPaths] = paths;
    if (representativePath.length === 0) {
      rootEqualityPaths = identicalPaths.map(stringifyPath18);
    } else {
      result[stringifyPath18(representativePath)] = identicalPaths.map(stringifyPath18);
    }
  });
  if (rootEqualityPaths) {
    if (isEmptyObject18(result)) {
      return [rootEqualityPaths];
    } else {
      return [rootEqualityPaths, result];
    }
  } else {
    return isEmptyObject18(result) ? void 0 : result;
  }
}
__name(generateReferentialEqualityAnnotations18, "generateReferentialEqualityAnnotations");
var walker18 = /* @__PURE__ */ __name((object2, identities, superJson, dedupe, path = [], objectsInThisPath = [], seenObjects = /* @__PURE__ */ new Map()) => {
  const primitive = isPrimitive18(object2);
  if (!primitive) {
    addIdentity18(object2, path, identities);
    const seen = seenObjects.get(object2);
    if (seen) {
      return dedupe ? {
        transformedValue: null
      } : seen;
    }
  }
  if (!isDeep18(object2, superJson)) {
    const transformed2 = transformValue18(object2, superJson);
    const result2 = transformed2 ? {
      transformedValue: transformed2.value,
      annotations: [transformed2.type]
    } : {
      transformedValue: object2
    };
    if (!primitive) {
      seenObjects.set(object2, result2);
    }
    return result2;
  }
  if (includes18(objectsInThisPath, object2)) {
    return {
      transformedValue: null
    };
  }
  const transformationResult = transformValue18(object2, superJson);
  const transformed = transformationResult?.value ?? object2;
  const transformedValue = isArray19(transformed) ? [] : {};
  const innerAnnotations = {};
  forEach18(transformed, (value, index) => {
    if (index === "__proto__" || index === "constructor" || index === "prototype") {
      throw new Error(`Detected property ${index}. This is a prototype pollution risk, please remove it from your object.`);
    }
    const recursiveResult = walker18(value, identities, superJson, dedupe, [...path, index], [...objectsInThisPath, object2], seenObjects);
    transformedValue[index] = recursiveResult.transformedValue;
    if (isArray19(recursiveResult.annotations)) {
      innerAnnotations[escapeKey18(index)] = recursiveResult.annotations;
    } else if (isPlainObject19(recursiveResult.annotations)) {
      forEach18(recursiveResult.annotations, (tree, key) => {
        innerAnnotations[escapeKey18(index) + "." + key] = tree;
      });
    }
  });
  const result = isEmptyObject18(innerAnnotations) ? {
    transformedValue,
    annotations: transformationResult ? [transformationResult.type] : void 0
  } : {
    transformedValue,
    annotations: transformationResult ? [transformationResult.type, innerAnnotations] : innerAnnotations
  };
  if (!primitive) {
    seenObjects.set(object2, result);
  }
  return result;
}, "walker");
function getType218(payload) {
  return Object.prototype.toString.call(payload).slice(8, -1);
}
__name(getType218, "getType2");
function isArray218(payload) {
  return getType218(payload) === "Array";
}
__name(isArray218, "isArray2");
function isPlainObject218(payload) {
  if (getType218(payload) !== "Object")
    return false;
  const prototype = Object.getPrototypeOf(payload);
  return !!prototype && prototype.constructor === Object && prototype === Object.prototype;
}
__name(isPlainObject218, "isPlainObject2");
function assignProp18(carry, key, newVal, originalObject, includeNonenumerable) {
  const propType = {}.propertyIsEnumerable.call(originalObject, key) ? "enumerable" : "nonenumerable";
  if (propType === "enumerable")
    carry[key] = newVal;
  if (includeNonenumerable && propType === "nonenumerable") {
    Object.defineProperty(carry, key, {
      value: newVal,
      enumerable: false,
      writable: true,
      configurable: true
    });
  }
}
__name(assignProp18, "assignProp");
function copy18(target, options = {}) {
  if (isArray218(target)) {
    return target.map((item) => copy18(item, options));
  }
  if (!isPlainObject218(target)) {
    return target;
  }
  const props = Object.getOwnPropertyNames(target);
  const symbols = Object.getOwnPropertySymbols(target);
  return [...props, ...symbols].reduce((carry, key) => {
    if (key === "__proto__")
      return carry;
    if (isArray218(options.props) && !options.props.includes(key)) {
      return carry;
    }
    const val = target[key];
    const newVal = copy18(val, options);
    assignProp18(carry, key, newVal, target, options.nonenumerable);
    return carry;
  }, {});
}
__name(copy18, "copy");
var SuperJSON18 = class {
  static {
    __name(this, "SuperJSON");
  }
  constructor({ dedupe = false } = {}) {
    this.classRegistry = new ClassRegistry18();
    this.symbolRegistry = new Registry18((s) => s.description ?? "");
    this.customTransformerRegistry = new CustomTransformerRegistry18();
    this.allowedErrorProps = [];
    this.dedupe = dedupe;
  }
  serialize(object2) {
    const identities = /* @__PURE__ */ new Map();
    const output = walker18(object2, identities, this, this.dedupe);
    const res = {
      json: output.transformedValue
    };
    if (output.annotations) {
      res.meta = {
        ...res.meta,
        values: output.annotations
      };
    }
    const equalityAnnotations = generateReferentialEqualityAnnotations18(identities, this.dedupe);
    if (equalityAnnotations) {
      res.meta = {
        ...res.meta,
        referentialEqualities: equalityAnnotations
      };
    }
    if (res.meta)
      res.meta.v = 1;
    return res;
  }
  deserialize(payload, options) {
    const { json, meta } = payload;
    let result = options?.inPlace ? json : copy18(json);
    if (meta?.values) {
      result = applyValueAnnotations18(result, meta.values, meta.v ?? 0, this);
    }
    if (meta?.referentialEqualities) {
      result = applyReferentialEqualityAnnotations18(result, meta.referentialEqualities, meta.v ?? 0);
    }
    return result;
  }
  stringify(object2) {
    return JSON.stringify(this.serialize(object2));
  }
  parse(string2) {
    return this.deserialize(JSON.parse(string2), { inPlace: true });
  }
  registerClass(v, options) {
    this.classRegistry.register(v, options);
  }
  registerSymbol(v, identifier) {
    this.symbolRegistry.register(v, identifier);
  }
  registerCustom(transformer, name) {
    this.customTransformerRegistry.register({
      name,
      ...transformer
    });
  }
  allowErrorProps(...props) {
    this.allowedErrorProps.push(...props);
  }
};
SuperJSON18.defaultInstance = new SuperJSON18();
SuperJSON18.serialize = SuperJSON18.defaultInstance.serialize.bind(SuperJSON18.defaultInstance);
SuperJSON18.deserialize = SuperJSON18.defaultInstance.deserialize.bind(SuperJSON18.defaultInstance);
SuperJSON18.stringify = SuperJSON18.defaultInstance.stringify.bind(SuperJSON18.defaultInstance);
SuperJSON18.parse = SuperJSON18.defaultInstance.parse.bind(SuperJSON18.defaultInstance);
SuperJSON18.registerClass = SuperJSON18.defaultInstance.registerClass.bind(SuperJSON18.defaultInstance);
SuperJSON18.registerSymbol = SuperJSON18.defaultInstance.registerSymbol.bind(SuperJSON18.defaultInstance);
SuperJSON18.registerCustom = SuperJSON18.defaultInstance.registerCustom.bind(SuperJSON18.defaultInstance);
SuperJSON18.allowErrorProps = SuperJSON18.defaultInstance.allowErrorProps.bind(SuperJSON18.defaultInstance);
var dist_default18 = SuperJSON18;
var serialize18 = SuperJSON18.serialize;
var deserialize18 = SuperJSON18.deserialize;
var stringify19 = SuperJSON18.stringify;
var parse18 = SuperJSON18.parse;
var registerClass18 = SuperJSON18.registerClass;
var registerCustom18 = SuperJSON18.registerCustom;
var registerSymbol18 = SuperJSON18.registerSymbol;
var allowErrorProps18 = SuperJSON18.allowErrorProps;
function parseData18(formData) {
  const propsArray = [];
  if (!formData)
    return propsArray;
  const batchsIDs = [];
  for (const [key, value] of Array.from(formData.entries())) {
    if (key.startsWith("FILE_"))
      propsArray.push(value);
    else if (key.startsWith("FILES_")) {
      if (batchsIDs.includes(key))
        continue;
      batchsIDs.push(key);
      propsArray.push(formData.getAll(key));
    } else {
      propsArray.push(JSON.parse(decodeURI(value)));
    }
  }
  return propsArray;
}
__name(parseData18, "parseData");
function paramsFromURL18(url) {
  const params = url.searchParams.entries().toArray().map(([_, v]) => v);
  return params.map((param) => JSON.parse(decodeURIComponent(param)));
}
__name(paramsFromURL18, "paramsFromURL");
async function WrapRequestHandler18(context2, endpoint) {
  const isServerAction = context2.request.headers.get("x-server-action") === "true";
  if (!isServerAction) {
    return new Response("Not Found", { status: 404 });
  }
  const parsedData = context2.request.method === "GET" || context2.request.method === "HEAD" ? paramsFromURL18(new URL(context2.request.url)) : parseData18(context2.request.headers.get("content-type") ? await context2.request.formData() : void 0);
  const missingProps = endpoint.length - parsedData.length;
  for (let i = 0; i < missingProps; i++) {
    parsedData.push(void 0);
  }
  parsedData.push(context2);
  const result = await endpoint(...parsedData);
  switch (typeof result) {
    case "string":
    case "number":
    case "boolean":
    case "bigint":
      return new Response(dist_default18.stringify(result), {
        headers: {
          "Content-Type": "application/json",
          dataType: "json"
        }
      });
    case "undefined":
      return new Response(null, { status: 204 });
    case "object":
      if (result instanceof Response) {
        result.headers.set("dataType", "response");
        return result;
      } else if (result instanceof Blob) {
        const res = new Response(await result.arrayBuffer());
        res.headers.set("dataType", "blob");
        res.headers.set("Content-Type", result.type);
        return res;
      } else if (result instanceof File) {
        return new Response(await result.arrayBuffer(), {
          headers: {
            "Content-Type": result.type,
            dataType: "file",
            fileData: JSON.stringify({
              name: result.name,
              lastModified: result.lastModified
            })
          }
        });
      } else {
        return new Response(dist_default18.stringify(result), {
          headers: { "Content-Type": "application/json", dataType: "json" }
        });
      }
    default:
      throw new Error(`Unsupported return type from action: ${typeof result}`);
  }
}
__name(WrapRequestHandler18, "WrapRequestHandler");
var onRequest18 = /* @__PURE__ */ __name(async (context2) => {
  const method = context2.request.method;
  const options = {
    GET: typeof GET9 === "function" ? GET9 : void 0,
    POST: typeof POST === "function" ? POST : void 0,
    PUT: typeof PUT === "function" ? PUT : void 0,
    DELETE: typeof DELETE11 === "function" ? DELETE11 : void 0,
    PATCH: typeof PATCH === "function" ? PATCH : void 0,
    HEAD: typeof HEAD === "function" ? HEAD : void 0,
    OPTIONS: typeof OPTIONS === "function" ? OPTIONS : void 0
  };
  if (!options[method]) {
    return new Response(`Method "${method}" Not Allowed`, {
      status: 405
    });
  }
  return await WrapRequestHandler18(context2, options[method]);
}, "onRequest");

// api/logs/index.js
var DEFAULT_LIMIT = 50;
var MAX_LIMIT = 200;
async function GET10(params) {
  const ctx = getContext(arguments);
  const { env: env2 } = ctx;
  const { clientID } = params;
  if (!clientID || typeof clientID !== "string") {
    return {
      success: false,
      error: "Invalid or missing clientID",
      data: []
    };
  }
  const session = await ctx.data.client.getUserSession("private");
  if (session instanceof Error) {
    return { success: false, error: "Unauthorized", data: [] };
  }
  const projectIsOwnedByUser = env2.SELF_HOSTED === "true" ? true : await drizzle(env2.PROJECT_DB).select({ id: projectTable.clientID }).from(projectTable).where(and(eq(projectTable.clientID, clientID), ownerGroupConditions({
    user_group_ids: session?.private?.group_ids ?? [],
    ownerGroupIdColumn: projectTable.owner_group_id,
    otherEq: [eq(projectTable.owner_id, session.user_id)],
    self_host: env2.SELF_HOSTED
  }))).get().then((e) => Boolean(e));
  if (!projectIsOwnedByUser) {
    return { success: false, error: "Unauthorized", data: [] };
  }
  const page = Math.max(1, Number(params.page) || 1);
  const limit2 = Math.min(MAX_LIMIT, Math.max(1, Number(params.limit) || DEFAULT_LIMIT));
  const offset = (page - 1) * limit2;
  const db = drizzle(env2.PROJECT_DB);
  const logs = await db.select().from(LogsTable).where(eq(LogsTable.clientID, clientID)).orderBy(desc(LogsTable.timestamp)).limit(limit2).offset(offset);
  return {
    success: true,
    data: logs,
    pagination: { page, limit: limit2 }
  };
}
__name(GET10, "GET");
var DoubleIndexedKV19 = class {
  static {
    __name(this, "DoubleIndexedKV");
  }
  constructor() {
    this.keyToValue = /* @__PURE__ */ new Map();
    this.valueToKey = /* @__PURE__ */ new Map();
  }
  set(key, value) {
    this.keyToValue.set(key, value);
    this.valueToKey.set(value, key);
  }
  getByKey(key) {
    return this.keyToValue.get(key);
  }
  getByValue(value) {
    return this.valueToKey.get(value);
  }
  clear() {
    this.keyToValue.clear();
    this.valueToKey.clear();
  }
};
var Registry19 = class {
  static {
    __name(this, "Registry");
  }
  constructor(generateIdentifier) {
    this.generateIdentifier = generateIdentifier;
    this.kv = new DoubleIndexedKV19();
  }
  register(value, identifier) {
    if (this.kv.getByValue(value)) {
      return;
    }
    if (!identifier) {
      identifier = this.generateIdentifier(value);
    }
    this.kv.set(identifier, value);
  }
  clear() {
    this.kv.clear();
  }
  getIdentifier(value) {
    return this.kv.getByValue(value);
  }
  getValue(identifier) {
    return this.kv.getByKey(identifier);
  }
};
var ClassRegistry19 = class extends Registry19 {
  static {
    __name(this, "ClassRegistry");
  }
  constructor() {
    super((c) => c.name);
    this.classToAllowedProps = /* @__PURE__ */ new Map();
  }
  register(value, options) {
    if (typeof options === "object") {
      if (options.allowProps) {
        this.classToAllowedProps.set(value, options.allowProps);
      }
      super.register(value, options.identifier);
    } else {
      super.register(value, options);
    }
  }
  getAllowedProps(value) {
    return this.classToAllowedProps.get(value);
  }
};
function valuesOfObj19(record) {
  if ("values" in Object) {
    return Object.values(record);
  }
  const values = [];
  for (const key in record) {
    if (record.hasOwnProperty(key)) {
      values.push(record[key]);
    }
  }
  return values;
}
__name(valuesOfObj19, "valuesOfObj");
function find19(record, predicate) {
  const values = valuesOfObj19(record);
  if ("find" in values) {
    return values.find(predicate);
  }
  const valuesNotNever = values;
  for (let i = 0; i < valuesNotNever.length; i++) {
    const value = valuesNotNever[i];
    if (predicate(value)) {
      return value;
    }
  }
  return;
}
__name(find19, "find");
function forEach19(record, run) {
  Object.entries(record).forEach(([key, value]) => run(value, key));
}
__name(forEach19, "forEach");
function includes19(arr, value) {
  return arr.indexOf(value) !== -1;
}
__name(includes19, "includes");
function findArr19(record, predicate) {
  for (let i = 0; i < record.length; i++) {
    const value = record[i];
    if (predicate(value)) {
      return value;
    }
  }
  return;
}
__name(findArr19, "findArr");
var CustomTransformerRegistry19 = class {
  static {
    __name(this, "CustomTransformerRegistry");
  }
  constructor() {
    this.transfomers = {};
  }
  register(transformer) {
    this.transfomers[transformer.name] = transformer;
  }
  findApplicable(v) {
    return find19(this.transfomers, (transformer) => transformer.isApplicable(v));
  }
  findByName(name) {
    return this.transfomers[name];
  }
};
var getType20 = /* @__PURE__ */ __name((payload) => Object.prototype.toString.call(payload).slice(8, -1), "getType");
var isUndefined19 = /* @__PURE__ */ __name((payload) => typeof payload === "undefined", "isUndefined");
var isNull20 = /* @__PURE__ */ __name((payload) => payload === null, "isNull");
var isPlainObject20 = /* @__PURE__ */ __name((payload) => {
  if (typeof payload !== "object" || payload === null)
    return false;
  if (payload === Object.prototype)
    return false;
  if (Object.getPrototypeOf(payload) === null)
    return true;
  return Object.getPrototypeOf(payload) === Object.prototype;
}, "isPlainObject");
var isEmptyObject19 = /* @__PURE__ */ __name((payload) => isPlainObject20(payload) && Object.keys(payload).length === 0, "isEmptyObject");
var isArray20 = /* @__PURE__ */ __name((payload) => Array.isArray(payload), "isArray");
var isString19 = /* @__PURE__ */ __name((payload) => typeof payload === "string", "isString");
var isNumber19 = /* @__PURE__ */ __name((payload) => typeof payload === "number" && !isNaN(payload), "isNumber");
var isBoolean19 = /* @__PURE__ */ __name((payload) => typeof payload === "boolean", "isBoolean");
var isRegExp19 = /* @__PURE__ */ __name((payload) => payload instanceof RegExp, "isRegExp");
var isMap19 = /* @__PURE__ */ __name((payload) => payload instanceof Map, "isMap");
var isSet19 = /* @__PURE__ */ __name((payload) => payload instanceof Set, "isSet");
var isSymbol19 = /* @__PURE__ */ __name((payload) => getType20(payload) === "Symbol", "isSymbol");
var isDate19 = /* @__PURE__ */ __name((payload) => payload instanceof Date && !isNaN(payload.valueOf()), "isDate");
var isError19 = /* @__PURE__ */ __name((payload) => payload instanceof Error, "isError");
var isNaNValue19 = /* @__PURE__ */ __name((payload) => typeof payload === "number" && isNaN(payload), "isNaNValue");
var isPrimitive19 = /* @__PURE__ */ __name((payload) => isBoolean19(payload) || isNull20(payload) || isUndefined19(payload) || isNumber19(payload) || isString19(payload) || isSymbol19(payload), "isPrimitive");
var isBigint19 = /* @__PURE__ */ __name((payload) => typeof payload === "bigint", "isBigint");
var isInfinite19 = /* @__PURE__ */ __name((payload) => payload === Infinity || payload === -Infinity, "isInfinite");
var isTypedArray19 = /* @__PURE__ */ __name((payload) => ArrayBuffer.isView(payload) && !(payload instanceof DataView), "isTypedArray");
var isURL19 = /* @__PURE__ */ __name((payload) => payload instanceof URL, "isURL");
var escapeKey19 = /* @__PURE__ */ __name((key) => key.replace(/\\/g, "\\\\").replace(/\./g, "\\."), "escapeKey");
var stringifyPath19 = /* @__PURE__ */ __name((path) => path.map(String).map(escapeKey19).join("."), "stringifyPath");
var parsePath19 = /* @__PURE__ */ __name((string2, legacyPaths) => {
  const result = [];
  let segment = "";
  for (let i = 0; i < string2.length; i++) {
    let char = string2.charAt(i);
    if (!legacyPaths && char === "\\") {
      const escaped = string2.charAt(i + 1);
      if (escaped === "\\") {
        segment += "\\";
        i++;
        continue;
      } else if (escaped !== ".") {
        throw Error("invalid path");
      }
    }
    const isEscapedDot = char === "\\" && string2.charAt(i + 1) === ".";
    if (isEscapedDot) {
      segment += ".";
      i++;
      continue;
    }
    const isEndOfSegment = char === ".";
    if (isEndOfSegment) {
      result.push(segment);
      segment = "";
      continue;
    }
    segment += char;
  }
  const lastSegment = segment;
  result.push(lastSegment);
  return result;
}, "parsePath");
function simpleTransformation19(isApplicable, annotation, transform, untransform) {
  return {
    isApplicable,
    annotation,
    transform,
    untransform
  };
}
__name(simpleTransformation19, "simpleTransformation");
var simpleRules19 = [
  simpleTransformation19(isUndefined19, "undefined", () => null, () => {
    return;
  }),
  simpleTransformation19(isBigint19, "bigint", (v) => v.toString(), (v) => {
    if (typeof BigInt !== "undefined") {
      return BigInt(v);
    }
    console.error("Please add a BigInt polyfill.");
    return v;
  }),
  simpleTransformation19(isDate19, "Date", (v) => v.toISOString(), (v) => new Date(v)),
  simpleTransformation19(isError19, "Error", (v, superJson) => {
    const baseError = {
      name: v.name,
      message: v.message
    };
    if ("cause" in v) {
      baseError.cause = v.cause;
    }
    superJson.allowedErrorProps.forEach((prop) => {
      baseError[prop] = v[prop];
    });
    return baseError;
  }, (v, superJson) => {
    const e = new Error(v.message, { cause: v.cause });
    e.name = v.name;
    e.stack = v.stack;
    superJson.allowedErrorProps.forEach((prop) => {
      e[prop] = v[prop];
    });
    return e;
  }),
  simpleTransformation19(isRegExp19, "regexp", (v) => "" + v, (regex) => {
    const body = regex.slice(1, regex.lastIndexOf("/"));
    const flags2 = regex.slice(regex.lastIndexOf("/") + 1);
    return new RegExp(body, flags2);
  }),
  simpleTransformation19(isSet19, "set", (v) => [...v.values()], (v) => new Set(v)),
  simpleTransformation19(isMap19, "map", (v) => [...v.entries()], (v) => new Map(v)),
  simpleTransformation19((v) => isNaNValue19(v) || isInfinite19(v), "number", (v) => {
    if (isNaNValue19(v)) {
      return "NaN";
    }
    if (v > 0) {
      return "Infinity";
    } else {
      return "-Infinity";
    }
  }, Number),
  simpleTransformation19((v) => v === 0 && 1 / v === -Infinity, "number", () => {
    return "-0";
  }, Number),
  simpleTransformation19(isURL19, "URL", (v) => v.toString(), (v) => new URL(v))
];
function compositeTransformation19(isApplicable, annotation, transform, untransform) {
  return {
    isApplicable,
    annotation,
    transform,
    untransform
  };
}
__name(compositeTransformation19, "compositeTransformation");
var symbolRule19 = compositeTransformation19((s, superJson) => {
  if (isSymbol19(s)) {
    const isRegistered = !!superJson.symbolRegistry.getIdentifier(s);
    return isRegistered;
  }
  return false;
}, (s, superJson) => {
  const identifier = superJson.symbolRegistry.getIdentifier(s);
  return ["symbol", identifier];
}, (v) => v.description, (_, a, superJson) => {
  const value = superJson.symbolRegistry.getValue(a[1]);
  if (!value) {
    throw new Error("Trying to deserialize unknown symbol");
  }
  return value;
});
var constructorToName19 = [
  Int8Array,
  Uint8Array,
  Int16Array,
  Uint16Array,
  Int32Array,
  Uint32Array,
  Float32Array,
  Float64Array,
  Uint8ClampedArray
].reduce((obj, ctor) => {
  obj[ctor.name] = ctor;
  return obj;
}, {});
var typedArrayRule19 = compositeTransformation19(isTypedArray19, (v) => ["typed-array", v.constructor.name], (v) => [...v], (v, a) => {
  const ctor = constructorToName19[a[1]];
  if (!ctor) {
    throw new Error("Trying to deserialize unknown typed array");
  }
  return new ctor(v);
});
function isInstanceOfRegisteredClass19(potentialClass, superJson) {
  if (potentialClass?.constructor) {
    const isRegistered = !!superJson.classRegistry.getIdentifier(potentialClass.constructor);
    return isRegistered;
  }
  return false;
}
__name(isInstanceOfRegisteredClass19, "isInstanceOfRegisteredClass");
var classRule19 = compositeTransformation19(isInstanceOfRegisteredClass19, (clazz, superJson) => {
  const identifier = superJson.classRegistry.getIdentifier(clazz.constructor);
  return ["class", identifier];
}, (clazz, superJson) => {
  const allowedProps = superJson.classRegistry.getAllowedProps(clazz.constructor);
  if (!allowedProps) {
    return { ...clazz };
  }
  const result = {};
  allowedProps.forEach((prop) => {
    result[prop] = clazz[prop];
  });
  return result;
}, (v, a, superJson) => {
  const clazz = superJson.classRegistry.getValue(a[1]);
  if (!clazz) {
    throw new Error(`Trying to deserialize unknown class '${a[1]}' - check https://github.com/blitz-js/superjson/issues/116#issuecomment-773996564`);
  }
  return Object.assign(Object.create(clazz.prototype), v);
});
var customRule19 = compositeTransformation19((value, superJson) => {
  return !!superJson.customTransformerRegistry.findApplicable(value);
}, (value, superJson) => {
  const transformer = superJson.customTransformerRegistry.findApplicable(value);
  return ["custom", transformer.name];
}, (value, superJson) => {
  const transformer = superJson.customTransformerRegistry.findApplicable(value);
  return transformer.serialize(value);
}, (v, a, superJson) => {
  const transformer = superJson.customTransformerRegistry.findByName(a[1]);
  if (!transformer) {
    throw new Error("Trying to deserialize unknown custom value");
  }
  return transformer.deserialize(v);
});
var compositeRules19 = [classRule19, symbolRule19, customRule19, typedArrayRule19];
var transformValue19 = /* @__PURE__ */ __name((value, superJson) => {
  const applicableCompositeRule = findArr19(compositeRules19, (rule) => rule.isApplicable(value, superJson));
  if (applicableCompositeRule) {
    return {
      value: applicableCompositeRule.transform(value, superJson),
      type: applicableCompositeRule.annotation(value, superJson)
    };
  }
  const applicableSimpleRule = findArr19(simpleRules19, (rule) => rule.isApplicable(value, superJson));
  if (applicableSimpleRule) {
    return {
      value: applicableSimpleRule.transform(value, superJson),
      type: applicableSimpleRule.annotation
    };
  }
  return;
}, "transformValue");
var simpleRulesByAnnotation19 = {};
simpleRules19.forEach((rule) => {
  simpleRulesByAnnotation19[rule.annotation] = rule;
});
var untransformValue19 = /* @__PURE__ */ __name((json, type, superJson) => {
  if (isArray20(type)) {
    switch (type[0]) {
      case "symbol":
        return symbolRule19.untransform(json, type, superJson);
      case "class":
        return classRule19.untransform(json, type, superJson);
      case "custom":
        return customRule19.untransform(json, type, superJson);
      case "typed-array":
        return typedArrayRule19.untransform(json, type, superJson);
      default:
        throw new Error("Unknown transformation: " + type);
    }
  } else {
    const transformation = simpleRulesByAnnotation19[type];
    if (!transformation) {
      throw new Error("Unknown transformation: " + type);
    }
    return transformation.untransform(json, superJson);
  }
}, "untransformValue");
var getNthKey19 = /* @__PURE__ */ __name((value, n) => {
  if (n > value.size)
    throw new Error("index out of bounds");
  const keys = value.keys();
  while (n > 0) {
    keys.next();
    n--;
  }
  return keys.next().value;
}, "getNthKey");
function validatePath19(path) {
  if (includes19(path, "__proto__")) {
    throw new Error("__proto__ is not allowed as a property");
  }
  if (includes19(path, "prototype")) {
    throw new Error("prototype is not allowed as a property");
  }
  if (includes19(path, "constructor")) {
    throw new Error("constructor is not allowed as a property");
  }
}
__name(validatePath19, "validatePath");
var getDeep19 = /* @__PURE__ */ __name((object2, path) => {
  validatePath19(path);
  for (let i = 0; i < path.length; i++) {
    const key = path[i];
    if (isSet19(object2)) {
      object2 = getNthKey19(object2, +key);
    } else if (isMap19(object2)) {
      const row = +key;
      const type = +path[++i] === 0 ? "key" : "value";
      const keyOfRow = getNthKey19(object2, row);
      switch (type) {
        case "key":
          object2 = keyOfRow;
          break;
        case "value":
          object2 = object2.get(keyOfRow);
          break;
      }
    } else {
      object2 = object2[key];
    }
  }
  return object2;
}, "getDeep");
var setDeep19 = /* @__PURE__ */ __name((object2, path, mapper) => {
  validatePath19(path);
  if (path.length === 0) {
    return mapper(object2);
  }
  let parent = object2;
  for (let i = 0; i < path.length - 1; i++) {
    const key = path[i];
    if (isArray20(parent)) {
      const index = +key;
      parent = parent[index];
    } else if (isPlainObject20(parent)) {
      parent = parent[key];
    } else if (isSet19(parent)) {
      const row = +key;
      parent = getNthKey19(parent, row);
    } else if (isMap19(parent)) {
      const isEnd = i === path.length - 2;
      if (isEnd) {
        break;
      }
      const row = +key;
      const type = +path[++i] === 0 ? "key" : "value";
      const keyOfRow = getNthKey19(parent, row);
      switch (type) {
        case "key":
          parent = keyOfRow;
          break;
        case "value":
          parent = parent.get(keyOfRow);
          break;
      }
    }
  }
  const lastKey = path[path.length - 1];
  if (isArray20(parent)) {
    parent[+lastKey] = mapper(parent[+lastKey]);
  } else if (isPlainObject20(parent)) {
    parent[lastKey] = mapper(parent[lastKey]);
  }
  if (isSet19(parent)) {
    const oldValue = getNthKey19(parent, +lastKey);
    const newValue = mapper(oldValue);
    if (oldValue !== newValue) {
      parent.delete(oldValue);
      parent.add(newValue);
    }
  }
  if (isMap19(parent)) {
    const row = +path[path.length - 2];
    const keyToRow = getNthKey19(parent, row);
    const type = +lastKey === 0 ? "key" : "value";
    switch (type) {
      case "key": {
        const newKey = mapper(keyToRow);
        parent.set(newKey, parent.get(keyToRow));
        if (newKey !== keyToRow) {
          parent.delete(keyToRow);
        }
        break;
      }
      case "value": {
        parent.set(keyToRow, mapper(parent.get(keyToRow)));
        break;
      }
    }
  }
  return object2;
}, "setDeep");
var enableLegacyPaths19 = /* @__PURE__ */ __name((version4) => version4 < 1, "enableLegacyPaths");
function traverse19(tree, walker26, version4, origin = []) {
  if (!tree) {
    return;
  }
  const legacyPaths = enableLegacyPaths19(version4);
  if (!isArray20(tree)) {
    forEach19(tree, (subtree, key) => traverse19(subtree, walker26, version4, [
      ...origin,
      ...parsePath19(key, legacyPaths)
    ]));
    return;
  }
  const [nodeValue, children] = tree;
  if (children) {
    forEach19(children, (child, key) => {
      traverse19(child, walker26, version4, [
        ...origin,
        ...parsePath19(key, legacyPaths)
      ]);
    });
  }
  walker26(nodeValue, origin);
}
__name(traverse19, "traverse");
function applyValueAnnotations19(plain, annotations, version4, superJson) {
  traverse19(annotations, (type, path) => {
    plain = setDeep19(plain, path, (v) => untransformValue19(v, type, superJson));
  }, version4);
  return plain;
}
__name(applyValueAnnotations19, "applyValueAnnotations");
function applyReferentialEqualityAnnotations19(plain, annotations, version4) {
  const legacyPaths = enableLegacyPaths19(version4);
  function apply(identicalPaths, path) {
    const object2 = getDeep19(plain, parsePath19(path, legacyPaths));
    identicalPaths.map((path2) => parsePath19(path2, legacyPaths)).forEach((identicalObjectPath) => {
      plain = setDeep19(plain, identicalObjectPath, () => object2);
    });
  }
  __name(apply, "apply");
  if (isArray20(annotations)) {
    const [root, other] = annotations;
    root.forEach((identicalPath) => {
      plain = setDeep19(plain, parsePath19(identicalPath, legacyPaths), () => plain);
    });
    if (other) {
      forEach19(other, apply);
    }
  } else {
    forEach19(annotations, apply);
  }
  return plain;
}
__name(applyReferentialEqualityAnnotations19, "applyReferentialEqualityAnnotations");
var isDeep19 = /* @__PURE__ */ __name((object2, superJson) => isPlainObject20(object2) || isArray20(object2) || isMap19(object2) || isSet19(object2) || isError19(object2) || isInstanceOfRegisteredClass19(object2, superJson), "isDeep");
function addIdentity19(object2, path, identities) {
  const existingSet = identities.get(object2);
  if (existingSet) {
    existingSet.push(path);
  } else {
    identities.set(object2, [path]);
  }
}
__name(addIdentity19, "addIdentity");
function generateReferentialEqualityAnnotations19(identitites, dedupe) {
  const result = {};
  let rootEqualityPaths = void 0;
  identitites.forEach((paths) => {
    if (paths.length <= 1) {
      return;
    }
    if (!dedupe) {
      paths = paths.map((path) => path.map(String)).sort((a, b) => a.length - b.length);
    }
    const [representativePath, ...identicalPaths] = paths;
    if (representativePath.length === 0) {
      rootEqualityPaths = identicalPaths.map(stringifyPath19);
    } else {
      result[stringifyPath19(representativePath)] = identicalPaths.map(stringifyPath19);
    }
  });
  if (rootEqualityPaths) {
    if (isEmptyObject19(result)) {
      return [rootEqualityPaths];
    } else {
      return [rootEqualityPaths, result];
    }
  } else {
    return isEmptyObject19(result) ? void 0 : result;
  }
}
__name(generateReferentialEqualityAnnotations19, "generateReferentialEqualityAnnotations");
var walker19 = /* @__PURE__ */ __name((object2, identities, superJson, dedupe, path = [], objectsInThisPath = [], seenObjects = /* @__PURE__ */ new Map()) => {
  const primitive = isPrimitive19(object2);
  if (!primitive) {
    addIdentity19(object2, path, identities);
    const seen = seenObjects.get(object2);
    if (seen) {
      return dedupe ? {
        transformedValue: null
      } : seen;
    }
  }
  if (!isDeep19(object2, superJson)) {
    const transformed2 = transformValue19(object2, superJson);
    const result2 = transformed2 ? {
      transformedValue: transformed2.value,
      annotations: [transformed2.type]
    } : {
      transformedValue: object2
    };
    if (!primitive) {
      seenObjects.set(object2, result2);
    }
    return result2;
  }
  if (includes19(objectsInThisPath, object2)) {
    return {
      transformedValue: null
    };
  }
  const transformationResult = transformValue19(object2, superJson);
  const transformed = transformationResult?.value ?? object2;
  const transformedValue = isArray20(transformed) ? [] : {};
  const innerAnnotations = {};
  forEach19(transformed, (value, index) => {
    if (index === "__proto__" || index === "constructor" || index === "prototype") {
      throw new Error(`Detected property ${index}. This is a prototype pollution risk, please remove it from your object.`);
    }
    const recursiveResult = walker19(value, identities, superJson, dedupe, [...path, index], [...objectsInThisPath, object2], seenObjects);
    transformedValue[index] = recursiveResult.transformedValue;
    if (isArray20(recursiveResult.annotations)) {
      innerAnnotations[escapeKey19(index)] = recursiveResult.annotations;
    } else if (isPlainObject20(recursiveResult.annotations)) {
      forEach19(recursiveResult.annotations, (tree, key) => {
        innerAnnotations[escapeKey19(index) + "." + key] = tree;
      });
    }
  });
  const result = isEmptyObject19(innerAnnotations) ? {
    transformedValue,
    annotations: transformationResult ? [transformationResult.type] : void 0
  } : {
    transformedValue,
    annotations: transformationResult ? [transformationResult.type, innerAnnotations] : innerAnnotations
  };
  if (!primitive) {
    seenObjects.set(object2, result);
  }
  return result;
}, "walker");
function getType219(payload) {
  return Object.prototype.toString.call(payload).slice(8, -1);
}
__name(getType219, "getType2");
function isArray219(payload) {
  return getType219(payload) === "Array";
}
__name(isArray219, "isArray2");
function isPlainObject219(payload) {
  if (getType219(payload) !== "Object")
    return false;
  const prototype = Object.getPrototypeOf(payload);
  return !!prototype && prototype.constructor === Object && prototype === Object.prototype;
}
__name(isPlainObject219, "isPlainObject2");
function assignProp19(carry, key, newVal, originalObject, includeNonenumerable) {
  const propType = {}.propertyIsEnumerable.call(originalObject, key) ? "enumerable" : "nonenumerable";
  if (propType === "enumerable")
    carry[key] = newVal;
  if (includeNonenumerable && propType === "nonenumerable") {
    Object.defineProperty(carry, key, {
      value: newVal,
      enumerable: false,
      writable: true,
      configurable: true
    });
  }
}
__name(assignProp19, "assignProp");
function copy19(target, options = {}) {
  if (isArray219(target)) {
    return target.map((item) => copy19(item, options));
  }
  if (!isPlainObject219(target)) {
    return target;
  }
  const props = Object.getOwnPropertyNames(target);
  const symbols = Object.getOwnPropertySymbols(target);
  return [...props, ...symbols].reduce((carry, key) => {
    if (key === "__proto__")
      return carry;
    if (isArray219(options.props) && !options.props.includes(key)) {
      return carry;
    }
    const val = target[key];
    const newVal = copy19(val, options);
    assignProp19(carry, key, newVal, target, options.nonenumerable);
    return carry;
  }, {});
}
__name(copy19, "copy");
var SuperJSON19 = class {
  static {
    __name(this, "SuperJSON");
  }
  constructor({ dedupe = false } = {}) {
    this.classRegistry = new ClassRegistry19();
    this.symbolRegistry = new Registry19((s) => s.description ?? "");
    this.customTransformerRegistry = new CustomTransformerRegistry19();
    this.allowedErrorProps = [];
    this.dedupe = dedupe;
  }
  serialize(object2) {
    const identities = /* @__PURE__ */ new Map();
    const output = walker19(object2, identities, this, this.dedupe);
    const res = {
      json: output.transformedValue
    };
    if (output.annotations) {
      res.meta = {
        ...res.meta,
        values: output.annotations
      };
    }
    const equalityAnnotations = generateReferentialEqualityAnnotations19(identities, this.dedupe);
    if (equalityAnnotations) {
      res.meta = {
        ...res.meta,
        referentialEqualities: equalityAnnotations
      };
    }
    if (res.meta)
      res.meta.v = 1;
    return res;
  }
  deserialize(payload, options) {
    const { json, meta } = payload;
    let result = options?.inPlace ? json : copy19(json);
    if (meta?.values) {
      result = applyValueAnnotations19(result, meta.values, meta.v ?? 0, this);
    }
    if (meta?.referentialEqualities) {
      result = applyReferentialEqualityAnnotations19(result, meta.referentialEqualities, meta.v ?? 0);
    }
    return result;
  }
  stringify(object2) {
    return JSON.stringify(this.serialize(object2));
  }
  parse(string2) {
    return this.deserialize(JSON.parse(string2), { inPlace: true });
  }
  registerClass(v, options) {
    this.classRegistry.register(v, options);
  }
  registerSymbol(v, identifier) {
    this.symbolRegistry.register(v, identifier);
  }
  registerCustom(transformer, name) {
    this.customTransformerRegistry.register({
      name,
      ...transformer
    });
  }
  allowErrorProps(...props) {
    this.allowedErrorProps.push(...props);
  }
};
SuperJSON19.defaultInstance = new SuperJSON19();
SuperJSON19.serialize = SuperJSON19.defaultInstance.serialize.bind(SuperJSON19.defaultInstance);
SuperJSON19.deserialize = SuperJSON19.defaultInstance.deserialize.bind(SuperJSON19.defaultInstance);
SuperJSON19.stringify = SuperJSON19.defaultInstance.stringify.bind(SuperJSON19.defaultInstance);
SuperJSON19.parse = SuperJSON19.defaultInstance.parse.bind(SuperJSON19.defaultInstance);
SuperJSON19.registerClass = SuperJSON19.defaultInstance.registerClass.bind(SuperJSON19.defaultInstance);
SuperJSON19.registerSymbol = SuperJSON19.defaultInstance.registerSymbol.bind(SuperJSON19.defaultInstance);
SuperJSON19.registerCustom = SuperJSON19.defaultInstance.registerCustom.bind(SuperJSON19.defaultInstance);
SuperJSON19.allowErrorProps = SuperJSON19.defaultInstance.allowErrorProps.bind(SuperJSON19.defaultInstance);
var dist_default19 = SuperJSON19;
var serialize19 = SuperJSON19.serialize;
var deserialize19 = SuperJSON19.deserialize;
var stringify20 = SuperJSON19.stringify;
var parse19 = SuperJSON19.parse;
var registerClass19 = SuperJSON19.registerClass;
var registerCustom19 = SuperJSON19.registerCustom;
var registerSymbol19 = SuperJSON19.registerSymbol;
var allowErrorProps19 = SuperJSON19.allowErrorProps;
function parseData19(formData) {
  const propsArray = [];
  if (!formData)
    return propsArray;
  const batchsIDs = [];
  for (const [key, value] of Array.from(formData.entries())) {
    if (key.startsWith("FILE_"))
      propsArray.push(value);
    else if (key.startsWith("FILES_")) {
      if (batchsIDs.includes(key))
        continue;
      batchsIDs.push(key);
      propsArray.push(formData.getAll(key));
    } else {
      propsArray.push(JSON.parse(decodeURI(value)));
    }
  }
  return propsArray;
}
__name(parseData19, "parseData");
function paramsFromURL19(url) {
  const params = url.searchParams.entries().toArray().map(([_, v]) => v);
  return params.map((param) => JSON.parse(decodeURIComponent(param)));
}
__name(paramsFromURL19, "paramsFromURL");
async function WrapRequestHandler19(context2, endpoint) {
  const isServerAction = context2.request.headers.get("x-server-action") === "true";
  if (!isServerAction) {
    return new Response("Not Found", { status: 404 });
  }
  const parsedData = context2.request.method === "GET" || context2.request.method === "HEAD" ? paramsFromURL19(new URL(context2.request.url)) : parseData19(context2.request.headers.get("content-type") ? await context2.request.formData() : void 0);
  const missingProps = endpoint.length - parsedData.length;
  for (let i = 0; i < missingProps; i++) {
    parsedData.push(void 0);
  }
  parsedData.push(context2);
  const result = await endpoint(...parsedData);
  switch (typeof result) {
    case "string":
    case "number":
    case "boolean":
    case "bigint":
      return new Response(dist_default19.stringify(result), {
        headers: {
          "Content-Type": "application/json",
          dataType: "json"
        }
      });
    case "undefined":
      return new Response(null, { status: 204 });
    case "object":
      if (result instanceof Response) {
        result.headers.set("dataType", "response");
        return result;
      } else if (result instanceof Blob) {
        const res = new Response(await result.arrayBuffer());
        res.headers.set("dataType", "blob");
        res.headers.set("Content-Type", result.type);
        return res;
      } else if (result instanceof File) {
        return new Response(await result.arrayBuffer(), {
          headers: {
            "Content-Type": result.type,
            dataType: "file",
            fileData: JSON.stringify({
              name: result.name,
              lastModified: result.lastModified
            })
          }
        });
      } else {
        return new Response(dist_default19.stringify(result), {
          headers: { "Content-Type": "application/json", dataType: "json" }
        });
      }
    default:
      throw new Error(`Unsupported return type from action: ${typeof result}`);
  }
}
__name(WrapRequestHandler19, "WrapRequestHandler");
var onRequest19 = /* @__PURE__ */ __name(async (context2) => {
  const method = context2.request.method;
  const options = {
    GET: typeof GET10 === "function" ? GET10 : void 0,
    POST: typeof POST === "function" ? POST : void 0,
    PUT: typeof PUT === "function" ? PUT : void 0,
    DELETE: typeof DELETE === "function" ? DELETE : void 0,
    PATCH: typeof PATCH === "function" ? PATCH : void 0,
    HEAD: typeof HEAD === "function" ? HEAD : void 0,
    OPTIONS: typeof OPTIONS === "function" ? OPTIONS : void 0
  };
  if (!options[method]) {
    return new Response(`Method "${method}" Not Allowed`, {
      status: 405
    });
  }
  return await WrapRequestHandler19(context2, options[method]);
}, "onRequest");

// api/projects/index.js
async function GET11() {
  const { env: env2, data } = getContext(arguments);
  const session = await data.client.getUserSession("private");
  if (session instanceof Error) {
    return {
      success: false,
      error: "Unauthorized",
      data: []
    };
  }
  const db = drizzle(env2.PROJECT_DB);
  const projects = await db.select().from(projectTable).where(ownerGroupConditions({
    user_group_ids: session.private?.group_ids ?? [],
    ownerGroupIdColumn: projectTable.owner_group_id,
    otherEq: [eq(projectTable.owner_id, session.user_id)],
    self_host: env2.SELF_HOSTED
  }));
  return {
    success: true,
    data: projects.map(parseDBProject)
  };
}
__name(GET11, "GET");
async function POST12(params) {
  const ctx = getContext(arguments);
  const { env: env2, data } = ctx;
  const currentUserId = (await data.client.getMetaData())?.id;
  if (!currentUserId) {
    return {
      success: false,
      error: "Unauthorized"
    };
  }
  try {
    const { name, providers_data = [] } = params;
    const clientID = [
      "_",
      name.toLowerCase().replace(/[^a-z0-9_]/g, "_"),
      crypto.randomUUID().split("-")[0]
    ].join("_");
    if (!name || typeof name !== "string") {
      return {
        success: false,
        error: "missing project name"
      };
    }
    if (!isClientIdValid(clientID)) {
      return {
        success: false,
        error: "Invalid clientID format only alphanumeric and underscores, 3-30 characters, must start with a letter or underscore"
      };
    }
    const db = drizzle(env2.PROJECT_DB);
    const existing = await db.select().from(projectTable).where(eq(projectTable.clientID, clientID)).limit(1);
    if (existing.length > 0) {
      return {
        success: false,
        error: "Project with this clientID already exists"
      };
    }
    const cfClient = createClient(env2);
    const cfDomaineCreate = await createCustomDomainForProject(env2, cfClient);
    if (!cfDomaineCreate || !cfDomaineCreate.id || !cfDomaineCreate.hostname) {
      return {
        success: false,
        error: `Failed to create Cloudflare custom domain for project. Cloudflare info: ${JSON.stringify(cfDomaineCreate)}`
      };
    }
    const newProject = {
      name,
      clientID,
      owner_id: currentUserId,
      owner_group_id: crypto.randomUUID(),
      created_at: (/* @__PURE__ */ new Date()).toISOString(),
      active: true,
      providers_data,
      codeMode: "email",
      originURL: "",
      authEndpointURL: cfDomaineCreate.hostname,
      cloudflareDomaineID: cfDomaineCreate.id,
      registerOnInvite: false,
      secret: [
        crypto.randomUUID(),
        crypto.randomUUID(),
        crypto.randomUUID()
      ].join("-"),
      theme_id: null,
      emailTemplateId: null,
      projectData: {}
    };
    const [insertedProject] = await db.insert(projectTable).values(newProject).returning();
    if (!insertedProject) {
      await deleteCustomDomainForProject(env2, cfClient, cfDomaineCreate.id).catch(() => {
      });
      return {
        success: false,
        error: "Failed to create project"
      };
    }
    return await createUserTable(clientID, env2.PROJECT_DB).then(() => ({ success: true, data: parseDBProject(insertedProject) })).catch(async (err) => {
      console.error(`Failed to create user table for project ${clientID}: ${err}`);
      await db.delete(projectTable).where(eq(projectTable.clientID, clientID)).catch(() => {
      });
      await deleteCustomDomainForProject(env2, cfClient, cfDomaineCreate.id).catch(() => {
      });
      return {
        success: false,
        error: "Failed to create user table for project"
      };
    });
  } catch (error3) {
    await insertLog({
      type: "error",
      clientID: env2.PUBLIC_CLIENT_ID,
      message: error3 instanceof Error ? error3.message : String(error3),
      database: env2.PROJECT_DB,
      endpoint: "/api/projects",
      context: {
        ...error3 instanceof CloudflareClientError ? { cloudflareErrorData: error3.data } : {},
        stack: error3 instanceof Error ? error3.stack : void 0
      }
    });
    console.error("Error in POST /api/projects:", error3);
    return {
      success: false,
      error: "Invalid request body: " + (error3 instanceof Error ? error3.message : String(error3))
    };
  }
}
__name(POST12, "POST");
var DoubleIndexedKV20 = class {
  static {
    __name(this, "DoubleIndexedKV");
  }
  constructor() {
    this.keyToValue = /* @__PURE__ */ new Map();
    this.valueToKey = /* @__PURE__ */ new Map();
  }
  set(key, value) {
    this.keyToValue.set(key, value);
    this.valueToKey.set(value, key);
  }
  getByKey(key) {
    return this.keyToValue.get(key);
  }
  getByValue(value) {
    return this.valueToKey.get(value);
  }
  clear() {
    this.keyToValue.clear();
    this.valueToKey.clear();
  }
};
var Registry20 = class {
  static {
    __name(this, "Registry");
  }
  constructor(generateIdentifier) {
    this.generateIdentifier = generateIdentifier;
    this.kv = new DoubleIndexedKV20();
  }
  register(value, identifier) {
    if (this.kv.getByValue(value)) {
      return;
    }
    if (!identifier) {
      identifier = this.generateIdentifier(value);
    }
    this.kv.set(identifier, value);
  }
  clear() {
    this.kv.clear();
  }
  getIdentifier(value) {
    return this.kv.getByValue(value);
  }
  getValue(identifier) {
    return this.kv.getByKey(identifier);
  }
};
var ClassRegistry20 = class extends Registry20 {
  static {
    __name(this, "ClassRegistry");
  }
  constructor() {
    super((c) => c.name);
    this.classToAllowedProps = /* @__PURE__ */ new Map();
  }
  register(value, options) {
    if (typeof options === "object") {
      if (options.allowProps) {
        this.classToAllowedProps.set(value, options.allowProps);
      }
      super.register(value, options.identifier);
    } else {
      super.register(value, options);
    }
  }
  getAllowedProps(value) {
    return this.classToAllowedProps.get(value);
  }
};
function valuesOfObj20(record) {
  if ("values" in Object) {
    return Object.values(record);
  }
  const values = [];
  for (const key in record) {
    if (record.hasOwnProperty(key)) {
      values.push(record[key]);
    }
  }
  return values;
}
__name(valuesOfObj20, "valuesOfObj");
function find20(record, predicate) {
  const values = valuesOfObj20(record);
  if ("find" in values) {
    return values.find(predicate);
  }
  const valuesNotNever = values;
  for (let i = 0; i < valuesNotNever.length; i++) {
    const value = valuesNotNever[i];
    if (predicate(value)) {
      return value;
    }
  }
  return;
}
__name(find20, "find");
function forEach20(record, run) {
  Object.entries(record).forEach(([key, value]) => run(value, key));
}
__name(forEach20, "forEach");
function includes20(arr, value) {
  return arr.indexOf(value) !== -1;
}
__name(includes20, "includes");
function findArr20(record, predicate) {
  for (let i = 0; i < record.length; i++) {
    const value = record[i];
    if (predicate(value)) {
      return value;
    }
  }
  return;
}
__name(findArr20, "findArr");
var CustomTransformerRegistry20 = class {
  static {
    __name(this, "CustomTransformerRegistry");
  }
  constructor() {
    this.transfomers = {};
  }
  register(transformer) {
    this.transfomers[transformer.name] = transformer;
  }
  findApplicable(v) {
    return find20(this.transfomers, (transformer) => transformer.isApplicable(v));
  }
  findByName(name) {
    return this.transfomers[name];
  }
};
var getType21 = /* @__PURE__ */ __name((payload) => Object.prototype.toString.call(payload).slice(8, -1), "getType");
var isUndefined20 = /* @__PURE__ */ __name((payload) => typeof payload === "undefined", "isUndefined");
var isNull21 = /* @__PURE__ */ __name((payload) => payload === null, "isNull");
var isPlainObject21 = /* @__PURE__ */ __name((payload) => {
  if (typeof payload !== "object" || payload === null)
    return false;
  if (payload === Object.prototype)
    return false;
  if (Object.getPrototypeOf(payload) === null)
    return true;
  return Object.getPrototypeOf(payload) === Object.prototype;
}, "isPlainObject");
var isEmptyObject20 = /* @__PURE__ */ __name((payload) => isPlainObject21(payload) && Object.keys(payload).length === 0, "isEmptyObject");
var isArray21 = /* @__PURE__ */ __name((payload) => Array.isArray(payload), "isArray");
var isString20 = /* @__PURE__ */ __name((payload) => typeof payload === "string", "isString");
var isNumber20 = /* @__PURE__ */ __name((payload) => typeof payload === "number" && !isNaN(payload), "isNumber");
var isBoolean20 = /* @__PURE__ */ __name((payload) => typeof payload === "boolean", "isBoolean");
var isRegExp20 = /* @__PURE__ */ __name((payload) => payload instanceof RegExp, "isRegExp");
var isMap20 = /* @__PURE__ */ __name((payload) => payload instanceof Map, "isMap");
var isSet20 = /* @__PURE__ */ __name((payload) => payload instanceof Set, "isSet");
var isSymbol20 = /* @__PURE__ */ __name((payload) => getType21(payload) === "Symbol", "isSymbol");
var isDate20 = /* @__PURE__ */ __name((payload) => payload instanceof Date && !isNaN(payload.valueOf()), "isDate");
var isError20 = /* @__PURE__ */ __name((payload) => payload instanceof Error, "isError");
var isNaNValue20 = /* @__PURE__ */ __name((payload) => typeof payload === "number" && isNaN(payload), "isNaNValue");
var isPrimitive20 = /* @__PURE__ */ __name((payload) => isBoolean20(payload) || isNull21(payload) || isUndefined20(payload) || isNumber20(payload) || isString20(payload) || isSymbol20(payload), "isPrimitive");
var isBigint20 = /* @__PURE__ */ __name((payload) => typeof payload === "bigint", "isBigint");
var isInfinite20 = /* @__PURE__ */ __name((payload) => payload === Infinity || payload === -Infinity, "isInfinite");
var isTypedArray20 = /* @__PURE__ */ __name((payload) => ArrayBuffer.isView(payload) && !(payload instanceof DataView), "isTypedArray");
var isURL20 = /* @__PURE__ */ __name((payload) => payload instanceof URL, "isURL");
var escapeKey20 = /* @__PURE__ */ __name((key) => key.replace(/\\/g, "\\\\").replace(/\./g, "\\."), "escapeKey");
var stringifyPath20 = /* @__PURE__ */ __name((path) => path.map(String).map(escapeKey20).join("."), "stringifyPath");
var parsePath20 = /* @__PURE__ */ __name((string2, legacyPaths) => {
  const result = [];
  let segment = "";
  for (let i = 0; i < string2.length; i++) {
    let char = string2.charAt(i);
    if (!legacyPaths && char === "\\") {
      const escaped = string2.charAt(i + 1);
      if (escaped === "\\") {
        segment += "\\";
        i++;
        continue;
      } else if (escaped !== ".") {
        throw Error("invalid path");
      }
    }
    const isEscapedDot = char === "\\" && string2.charAt(i + 1) === ".";
    if (isEscapedDot) {
      segment += ".";
      i++;
      continue;
    }
    const isEndOfSegment = char === ".";
    if (isEndOfSegment) {
      result.push(segment);
      segment = "";
      continue;
    }
    segment += char;
  }
  const lastSegment = segment;
  result.push(lastSegment);
  return result;
}, "parsePath");
function simpleTransformation20(isApplicable, annotation, transform, untransform) {
  return {
    isApplicable,
    annotation,
    transform,
    untransform
  };
}
__name(simpleTransformation20, "simpleTransformation");
var simpleRules20 = [
  simpleTransformation20(isUndefined20, "undefined", () => null, () => {
    return;
  }),
  simpleTransformation20(isBigint20, "bigint", (v) => v.toString(), (v) => {
    if (typeof BigInt !== "undefined") {
      return BigInt(v);
    }
    console.error("Please add a BigInt polyfill.");
    return v;
  }),
  simpleTransformation20(isDate20, "Date", (v) => v.toISOString(), (v) => new Date(v)),
  simpleTransformation20(isError20, "Error", (v, superJson) => {
    const baseError = {
      name: v.name,
      message: v.message
    };
    if ("cause" in v) {
      baseError.cause = v.cause;
    }
    superJson.allowedErrorProps.forEach((prop) => {
      baseError[prop] = v[prop];
    });
    return baseError;
  }, (v, superJson) => {
    const e = new Error(v.message, { cause: v.cause });
    e.name = v.name;
    e.stack = v.stack;
    superJson.allowedErrorProps.forEach((prop) => {
      e[prop] = v[prop];
    });
    return e;
  }),
  simpleTransformation20(isRegExp20, "regexp", (v) => "" + v, (regex) => {
    const body = regex.slice(1, regex.lastIndexOf("/"));
    const flags2 = regex.slice(regex.lastIndexOf("/") + 1);
    return new RegExp(body, flags2);
  }),
  simpleTransformation20(isSet20, "set", (v) => [...v.values()], (v) => new Set(v)),
  simpleTransformation20(isMap20, "map", (v) => [...v.entries()], (v) => new Map(v)),
  simpleTransformation20((v) => isNaNValue20(v) || isInfinite20(v), "number", (v) => {
    if (isNaNValue20(v)) {
      return "NaN";
    }
    if (v > 0) {
      return "Infinity";
    } else {
      return "-Infinity";
    }
  }, Number),
  simpleTransformation20((v) => v === 0 && 1 / v === -Infinity, "number", () => {
    return "-0";
  }, Number),
  simpleTransformation20(isURL20, "URL", (v) => v.toString(), (v) => new URL(v))
];
function compositeTransformation20(isApplicable, annotation, transform, untransform) {
  return {
    isApplicable,
    annotation,
    transform,
    untransform
  };
}
__name(compositeTransformation20, "compositeTransformation");
var symbolRule20 = compositeTransformation20((s, superJson) => {
  if (isSymbol20(s)) {
    const isRegistered = !!superJson.symbolRegistry.getIdentifier(s);
    return isRegistered;
  }
  return false;
}, (s, superJson) => {
  const identifier = superJson.symbolRegistry.getIdentifier(s);
  return ["symbol", identifier];
}, (v) => v.description, (_, a, superJson) => {
  const value = superJson.symbolRegistry.getValue(a[1]);
  if (!value) {
    throw new Error("Trying to deserialize unknown symbol");
  }
  return value;
});
var constructorToName20 = [
  Int8Array,
  Uint8Array,
  Int16Array,
  Uint16Array,
  Int32Array,
  Uint32Array,
  Float32Array,
  Float64Array,
  Uint8ClampedArray
].reduce((obj, ctor) => {
  obj[ctor.name] = ctor;
  return obj;
}, {});
var typedArrayRule20 = compositeTransformation20(isTypedArray20, (v) => ["typed-array", v.constructor.name], (v) => [...v], (v, a) => {
  const ctor = constructorToName20[a[1]];
  if (!ctor) {
    throw new Error("Trying to deserialize unknown typed array");
  }
  return new ctor(v);
});
function isInstanceOfRegisteredClass20(potentialClass, superJson) {
  if (potentialClass?.constructor) {
    const isRegistered = !!superJson.classRegistry.getIdentifier(potentialClass.constructor);
    return isRegistered;
  }
  return false;
}
__name(isInstanceOfRegisteredClass20, "isInstanceOfRegisteredClass");
var classRule20 = compositeTransformation20(isInstanceOfRegisteredClass20, (clazz, superJson) => {
  const identifier = superJson.classRegistry.getIdentifier(clazz.constructor);
  return ["class", identifier];
}, (clazz, superJson) => {
  const allowedProps = superJson.classRegistry.getAllowedProps(clazz.constructor);
  if (!allowedProps) {
    return { ...clazz };
  }
  const result = {};
  allowedProps.forEach((prop) => {
    result[prop] = clazz[prop];
  });
  return result;
}, (v, a, superJson) => {
  const clazz = superJson.classRegistry.getValue(a[1]);
  if (!clazz) {
    throw new Error(`Trying to deserialize unknown class '${a[1]}' - check https://github.com/blitz-js/superjson/issues/116#issuecomment-773996564`);
  }
  return Object.assign(Object.create(clazz.prototype), v);
});
var customRule20 = compositeTransformation20((value, superJson) => {
  return !!superJson.customTransformerRegistry.findApplicable(value);
}, (value, superJson) => {
  const transformer = superJson.customTransformerRegistry.findApplicable(value);
  return ["custom", transformer.name];
}, (value, superJson) => {
  const transformer = superJson.customTransformerRegistry.findApplicable(value);
  return transformer.serialize(value);
}, (v, a, superJson) => {
  const transformer = superJson.customTransformerRegistry.findByName(a[1]);
  if (!transformer) {
    throw new Error("Trying to deserialize unknown custom value");
  }
  return transformer.deserialize(v);
});
var compositeRules20 = [classRule20, symbolRule20, customRule20, typedArrayRule20];
var transformValue20 = /* @__PURE__ */ __name((value, superJson) => {
  const applicableCompositeRule = findArr20(compositeRules20, (rule) => rule.isApplicable(value, superJson));
  if (applicableCompositeRule) {
    return {
      value: applicableCompositeRule.transform(value, superJson),
      type: applicableCompositeRule.annotation(value, superJson)
    };
  }
  const applicableSimpleRule = findArr20(simpleRules20, (rule) => rule.isApplicable(value, superJson));
  if (applicableSimpleRule) {
    return {
      value: applicableSimpleRule.transform(value, superJson),
      type: applicableSimpleRule.annotation
    };
  }
  return;
}, "transformValue");
var simpleRulesByAnnotation20 = {};
simpleRules20.forEach((rule) => {
  simpleRulesByAnnotation20[rule.annotation] = rule;
});
var untransformValue20 = /* @__PURE__ */ __name((json, type, superJson) => {
  if (isArray21(type)) {
    switch (type[0]) {
      case "symbol":
        return symbolRule20.untransform(json, type, superJson);
      case "class":
        return classRule20.untransform(json, type, superJson);
      case "custom":
        return customRule20.untransform(json, type, superJson);
      case "typed-array":
        return typedArrayRule20.untransform(json, type, superJson);
      default:
        throw new Error("Unknown transformation: " + type);
    }
  } else {
    const transformation = simpleRulesByAnnotation20[type];
    if (!transformation) {
      throw new Error("Unknown transformation: " + type);
    }
    return transformation.untransform(json, superJson);
  }
}, "untransformValue");
var getNthKey20 = /* @__PURE__ */ __name((value, n) => {
  if (n > value.size)
    throw new Error("index out of bounds");
  const keys = value.keys();
  while (n > 0) {
    keys.next();
    n--;
  }
  return keys.next().value;
}, "getNthKey");
function validatePath20(path) {
  if (includes20(path, "__proto__")) {
    throw new Error("__proto__ is not allowed as a property");
  }
  if (includes20(path, "prototype")) {
    throw new Error("prototype is not allowed as a property");
  }
  if (includes20(path, "constructor")) {
    throw new Error("constructor is not allowed as a property");
  }
}
__name(validatePath20, "validatePath");
var getDeep20 = /* @__PURE__ */ __name((object2, path) => {
  validatePath20(path);
  for (let i = 0; i < path.length; i++) {
    const key = path[i];
    if (isSet20(object2)) {
      object2 = getNthKey20(object2, +key);
    } else if (isMap20(object2)) {
      const row = +key;
      const type = +path[++i] === 0 ? "key" : "value";
      const keyOfRow = getNthKey20(object2, row);
      switch (type) {
        case "key":
          object2 = keyOfRow;
          break;
        case "value":
          object2 = object2.get(keyOfRow);
          break;
      }
    } else {
      object2 = object2[key];
    }
  }
  return object2;
}, "getDeep");
var setDeep20 = /* @__PURE__ */ __name((object2, path, mapper) => {
  validatePath20(path);
  if (path.length === 0) {
    return mapper(object2);
  }
  let parent = object2;
  for (let i = 0; i < path.length - 1; i++) {
    const key = path[i];
    if (isArray21(parent)) {
      const index = +key;
      parent = parent[index];
    } else if (isPlainObject21(parent)) {
      parent = parent[key];
    } else if (isSet20(parent)) {
      const row = +key;
      parent = getNthKey20(parent, row);
    } else if (isMap20(parent)) {
      const isEnd = i === path.length - 2;
      if (isEnd) {
        break;
      }
      const row = +key;
      const type = +path[++i] === 0 ? "key" : "value";
      const keyOfRow = getNthKey20(parent, row);
      switch (type) {
        case "key":
          parent = keyOfRow;
          break;
        case "value":
          parent = parent.get(keyOfRow);
          break;
      }
    }
  }
  const lastKey = path[path.length - 1];
  if (isArray21(parent)) {
    parent[+lastKey] = mapper(parent[+lastKey]);
  } else if (isPlainObject21(parent)) {
    parent[lastKey] = mapper(parent[lastKey]);
  }
  if (isSet20(parent)) {
    const oldValue = getNthKey20(parent, +lastKey);
    const newValue = mapper(oldValue);
    if (oldValue !== newValue) {
      parent.delete(oldValue);
      parent.add(newValue);
    }
  }
  if (isMap20(parent)) {
    const row = +path[path.length - 2];
    const keyToRow = getNthKey20(parent, row);
    const type = +lastKey === 0 ? "key" : "value";
    switch (type) {
      case "key": {
        const newKey = mapper(keyToRow);
        parent.set(newKey, parent.get(keyToRow));
        if (newKey !== keyToRow) {
          parent.delete(keyToRow);
        }
        break;
      }
      case "value": {
        parent.set(keyToRow, mapper(parent.get(keyToRow)));
        break;
      }
    }
  }
  return object2;
}, "setDeep");
var enableLegacyPaths20 = /* @__PURE__ */ __name((version4) => version4 < 1, "enableLegacyPaths");
function traverse20(tree, walker26, version4, origin = []) {
  if (!tree) {
    return;
  }
  const legacyPaths = enableLegacyPaths20(version4);
  if (!isArray21(tree)) {
    forEach20(tree, (subtree, key) => traverse20(subtree, walker26, version4, [
      ...origin,
      ...parsePath20(key, legacyPaths)
    ]));
    return;
  }
  const [nodeValue, children] = tree;
  if (children) {
    forEach20(children, (child, key) => {
      traverse20(child, walker26, version4, [
        ...origin,
        ...parsePath20(key, legacyPaths)
      ]);
    });
  }
  walker26(nodeValue, origin);
}
__name(traverse20, "traverse");
function applyValueAnnotations20(plain, annotations, version4, superJson) {
  traverse20(annotations, (type, path) => {
    plain = setDeep20(plain, path, (v) => untransformValue20(v, type, superJson));
  }, version4);
  return plain;
}
__name(applyValueAnnotations20, "applyValueAnnotations");
function applyReferentialEqualityAnnotations20(plain, annotations, version4) {
  const legacyPaths = enableLegacyPaths20(version4);
  function apply(identicalPaths, path) {
    const object2 = getDeep20(plain, parsePath20(path, legacyPaths));
    identicalPaths.map((path2) => parsePath20(path2, legacyPaths)).forEach((identicalObjectPath) => {
      plain = setDeep20(plain, identicalObjectPath, () => object2);
    });
  }
  __name(apply, "apply");
  if (isArray21(annotations)) {
    const [root, other] = annotations;
    root.forEach((identicalPath) => {
      plain = setDeep20(plain, parsePath20(identicalPath, legacyPaths), () => plain);
    });
    if (other) {
      forEach20(other, apply);
    }
  } else {
    forEach20(annotations, apply);
  }
  return plain;
}
__name(applyReferentialEqualityAnnotations20, "applyReferentialEqualityAnnotations");
var isDeep20 = /* @__PURE__ */ __name((object2, superJson) => isPlainObject21(object2) || isArray21(object2) || isMap20(object2) || isSet20(object2) || isError20(object2) || isInstanceOfRegisteredClass20(object2, superJson), "isDeep");
function addIdentity20(object2, path, identities) {
  const existingSet = identities.get(object2);
  if (existingSet) {
    existingSet.push(path);
  } else {
    identities.set(object2, [path]);
  }
}
__name(addIdentity20, "addIdentity");
function generateReferentialEqualityAnnotations20(identitites, dedupe) {
  const result = {};
  let rootEqualityPaths = void 0;
  identitites.forEach((paths) => {
    if (paths.length <= 1) {
      return;
    }
    if (!dedupe) {
      paths = paths.map((path) => path.map(String)).sort((a, b) => a.length - b.length);
    }
    const [representativePath, ...identicalPaths] = paths;
    if (representativePath.length === 0) {
      rootEqualityPaths = identicalPaths.map(stringifyPath20);
    } else {
      result[stringifyPath20(representativePath)] = identicalPaths.map(stringifyPath20);
    }
  });
  if (rootEqualityPaths) {
    if (isEmptyObject20(result)) {
      return [rootEqualityPaths];
    } else {
      return [rootEqualityPaths, result];
    }
  } else {
    return isEmptyObject20(result) ? void 0 : result;
  }
}
__name(generateReferentialEqualityAnnotations20, "generateReferentialEqualityAnnotations");
var walker20 = /* @__PURE__ */ __name((object2, identities, superJson, dedupe, path = [], objectsInThisPath = [], seenObjects = /* @__PURE__ */ new Map()) => {
  const primitive = isPrimitive20(object2);
  if (!primitive) {
    addIdentity20(object2, path, identities);
    const seen = seenObjects.get(object2);
    if (seen) {
      return dedupe ? {
        transformedValue: null
      } : seen;
    }
  }
  if (!isDeep20(object2, superJson)) {
    const transformed2 = transformValue20(object2, superJson);
    const result2 = transformed2 ? {
      transformedValue: transformed2.value,
      annotations: [transformed2.type]
    } : {
      transformedValue: object2
    };
    if (!primitive) {
      seenObjects.set(object2, result2);
    }
    return result2;
  }
  if (includes20(objectsInThisPath, object2)) {
    return {
      transformedValue: null
    };
  }
  const transformationResult = transformValue20(object2, superJson);
  const transformed = transformationResult?.value ?? object2;
  const transformedValue = isArray21(transformed) ? [] : {};
  const innerAnnotations = {};
  forEach20(transformed, (value, index) => {
    if (index === "__proto__" || index === "constructor" || index === "prototype") {
      throw new Error(`Detected property ${index}. This is a prototype pollution risk, please remove it from your object.`);
    }
    const recursiveResult = walker20(value, identities, superJson, dedupe, [...path, index], [...objectsInThisPath, object2], seenObjects);
    transformedValue[index] = recursiveResult.transformedValue;
    if (isArray21(recursiveResult.annotations)) {
      innerAnnotations[escapeKey20(index)] = recursiveResult.annotations;
    } else if (isPlainObject21(recursiveResult.annotations)) {
      forEach20(recursiveResult.annotations, (tree, key) => {
        innerAnnotations[escapeKey20(index) + "." + key] = tree;
      });
    }
  });
  const result = isEmptyObject20(innerAnnotations) ? {
    transformedValue,
    annotations: transformationResult ? [transformationResult.type] : void 0
  } : {
    transformedValue,
    annotations: transformationResult ? [transformationResult.type, innerAnnotations] : innerAnnotations
  };
  if (!primitive) {
    seenObjects.set(object2, result);
  }
  return result;
}, "walker");
function getType220(payload) {
  return Object.prototype.toString.call(payload).slice(8, -1);
}
__name(getType220, "getType2");
function isArray220(payload) {
  return getType220(payload) === "Array";
}
__name(isArray220, "isArray2");
function isPlainObject220(payload) {
  if (getType220(payload) !== "Object")
    return false;
  const prototype = Object.getPrototypeOf(payload);
  return !!prototype && prototype.constructor === Object && prototype === Object.prototype;
}
__name(isPlainObject220, "isPlainObject2");
function assignProp20(carry, key, newVal, originalObject, includeNonenumerable) {
  const propType = {}.propertyIsEnumerable.call(originalObject, key) ? "enumerable" : "nonenumerable";
  if (propType === "enumerable")
    carry[key] = newVal;
  if (includeNonenumerable && propType === "nonenumerable") {
    Object.defineProperty(carry, key, {
      value: newVal,
      enumerable: false,
      writable: true,
      configurable: true
    });
  }
}
__name(assignProp20, "assignProp");
function copy20(target, options = {}) {
  if (isArray220(target)) {
    return target.map((item) => copy20(item, options));
  }
  if (!isPlainObject220(target)) {
    return target;
  }
  const props = Object.getOwnPropertyNames(target);
  const symbols = Object.getOwnPropertySymbols(target);
  return [...props, ...symbols].reduce((carry, key) => {
    if (key === "__proto__")
      return carry;
    if (isArray220(options.props) && !options.props.includes(key)) {
      return carry;
    }
    const val = target[key];
    const newVal = copy20(val, options);
    assignProp20(carry, key, newVal, target, options.nonenumerable);
    return carry;
  }, {});
}
__name(copy20, "copy");
var SuperJSON20 = class {
  static {
    __name(this, "SuperJSON");
  }
  constructor({ dedupe = false } = {}) {
    this.classRegistry = new ClassRegistry20();
    this.symbolRegistry = new Registry20((s) => s.description ?? "");
    this.customTransformerRegistry = new CustomTransformerRegistry20();
    this.allowedErrorProps = [];
    this.dedupe = dedupe;
  }
  serialize(object2) {
    const identities = /* @__PURE__ */ new Map();
    const output = walker20(object2, identities, this, this.dedupe);
    const res = {
      json: output.transformedValue
    };
    if (output.annotations) {
      res.meta = {
        ...res.meta,
        values: output.annotations
      };
    }
    const equalityAnnotations = generateReferentialEqualityAnnotations20(identities, this.dedupe);
    if (equalityAnnotations) {
      res.meta = {
        ...res.meta,
        referentialEqualities: equalityAnnotations
      };
    }
    if (res.meta)
      res.meta.v = 1;
    return res;
  }
  deserialize(payload, options) {
    const { json, meta } = payload;
    let result = options?.inPlace ? json : copy20(json);
    if (meta?.values) {
      result = applyValueAnnotations20(result, meta.values, meta.v ?? 0, this);
    }
    if (meta?.referentialEqualities) {
      result = applyReferentialEqualityAnnotations20(result, meta.referentialEqualities, meta.v ?? 0);
    }
    return result;
  }
  stringify(object2) {
    return JSON.stringify(this.serialize(object2));
  }
  parse(string2) {
    return this.deserialize(JSON.parse(string2), { inPlace: true });
  }
  registerClass(v, options) {
    this.classRegistry.register(v, options);
  }
  registerSymbol(v, identifier) {
    this.symbolRegistry.register(v, identifier);
  }
  registerCustom(transformer, name) {
    this.customTransformerRegistry.register({
      name,
      ...transformer
    });
  }
  allowErrorProps(...props) {
    this.allowedErrorProps.push(...props);
  }
};
SuperJSON20.defaultInstance = new SuperJSON20();
SuperJSON20.serialize = SuperJSON20.defaultInstance.serialize.bind(SuperJSON20.defaultInstance);
SuperJSON20.deserialize = SuperJSON20.defaultInstance.deserialize.bind(SuperJSON20.defaultInstance);
SuperJSON20.stringify = SuperJSON20.defaultInstance.stringify.bind(SuperJSON20.defaultInstance);
SuperJSON20.parse = SuperJSON20.defaultInstance.parse.bind(SuperJSON20.defaultInstance);
SuperJSON20.registerClass = SuperJSON20.defaultInstance.registerClass.bind(SuperJSON20.defaultInstance);
SuperJSON20.registerSymbol = SuperJSON20.defaultInstance.registerSymbol.bind(SuperJSON20.defaultInstance);
SuperJSON20.registerCustom = SuperJSON20.defaultInstance.registerCustom.bind(SuperJSON20.defaultInstance);
SuperJSON20.allowErrorProps = SuperJSON20.defaultInstance.allowErrorProps.bind(SuperJSON20.defaultInstance);
var dist_default20 = SuperJSON20;
var serialize20 = SuperJSON20.serialize;
var deserialize20 = SuperJSON20.deserialize;
var stringify21 = SuperJSON20.stringify;
var parse20 = SuperJSON20.parse;
var registerClass20 = SuperJSON20.registerClass;
var registerCustom20 = SuperJSON20.registerCustom;
var registerSymbol20 = SuperJSON20.registerSymbol;
var allowErrorProps20 = SuperJSON20.allowErrorProps;
function parseData20(formData) {
  const propsArray = [];
  if (!formData)
    return propsArray;
  const batchsIDs = [];
  for (const [key, value] of Array.from(formData.entries())) {
    if (key.startsWith("FILE_"))
      propsArray.push(value);
    else if (key.startsWith("FILES_")) {
      if (batchsIDs.includes(key))
        continue;
      batchsIDs.push(key);
      propsArray.push(formData.getAll(key));
    } else {
      propsArray.push(JSON.parse(decodeURI(value)));
    }
  }
  return propsArray;
}
__name(parseData20, "parseData");
function paramsFromURL20(url) {
  const params = url.searchParams.entries().toArray().map(([_, v]) => v);
  return params.map((param) => JSON.parse(decodeURIComponent(param)));
}
__name(paramsFromURL20, "paramsFromURL");
async function WrapRequestHandler20(context2, endpoint) {
  const isServerAction = context2.request.headers.get("x-server-action") === "true";
  if (!isServerAction) {
    return new Response("Not Found", { status: 404 });
  }
  const parsedData = context2.request.method === "GET" || context2.request.method === "HEAD" ? paramsFromURL20(new URL(context2.request.url)) : parseData20(context2.request.headers.get("content-type") ? await context2.request.formData() : void 0);
  const missingProps = endpoint.length - parsedData.length;
  for (let i = 0; i < missingProps; i++) {
    parsedData.push(void 0);
  }
  parsedData.push(context2);
  const result = await endpoint(...parsedData);
  switch (typeof result) {
    case "string":
    case "number":
    case "boolean":
    case "bigint":
      return new Response(dist_default20.stringify(result), {
        headers: {
          "Content-Type": "application/json",
          dataType: "json"
        }
      });
    case "undefined":
      return new Response(null, { status: 204 });
    case "object":
      if (result instanceof Response) {
        result.headers.set("dataType", "response");
        return result;
      } else if (result instanceof Blob) {
        const res = new Response(await result.arrayBuffer());
        res.headers.set("dataType", "blob");
        res.headers.set("Content-Type", result.type);
        return res;
      } else if (result instanceof File) {
        return new Response(await result.arrayBuffer(), {
          headers: {
            "Content-Type": result.type,
            dataType: "file",
            fileData: JSON.stringify({
              name: result.name,
              lastModified: result.lastModified
            })
          }
        });
      } else {
        return new Response(dist_default20.stringify(result), {
          headers: { "Content-Type": "application/json", dataType: "json" }
        });
      }
    default:
      throw new Error(`Unsupported return type from action: ${typeof result}`);
  }
}
__name(WrapRequestHandler20, "WrapRequestHandler");
var onRequest20 = /* @__PURE__ */ __name(async (context2) => {
  const method = context2.request.method;
  const options = {
    GET: typeof GET11 === "function" ? GET11 : void 0,
    POST: typeof POST12 === "function" ? POST12 : void 0,
    PUT: typeof PUT === "function" ? PUT : void 0,
    DELETE: typeof DELETE === "function" ? DELETE : void 0,
    PATCH: typeof PATCH === "function" ? PATCH : void 0,
    HEAD: typeof HEAD === "function" ? HEAD : void 0,
    OPTIONS: typeof OPTIONS === "function" ? OPTIONS : void 0
  };
  if (!options[method]) {
    return new Response(`Method "${method}" Not Allowed`, {
      status: 405
    });
  }
  return await WrapRequestHandler20(context2, options[method]);
}, "onRequest");

// api/providers/index.js
async function GET12() {
  return {
    success: true,
    data: PROVIDER_REGISTRY
  };
}
__name(GET12, "GET");
var DoubleIndexedKV21 = class {
  static {
    __name(this, "DoubleIndexedKV");
  }
  constructor() {
    this.keyToValue = /* @__PURE__ */ new Map();
    this.valueToKey = /* @__PURE__ */ new Map();
  }
  set(key, value) {
    this.keyToValue.set(key, value);
    this.valueToKey.set(value, key);
  }
  getByKey(key) {
    return this.keyToValue.get(key);
  }
  getByValue(value) {
    return this.valueToKey.get(value);
  }
  clear() {
    this.keyToValue.clear();
    this.valueToKey.clear();
  }
};
var Registry21 = class {
  static {
    __name(this, "Registry");
  }
  constructor(generateIdentifier) {
    this.generateIdentifier = generateIdentifier;
    this.kv = new DoubleIndexedKV21();
  }
  register(value, identifier) {
    if (this.kv.getByValue(value)) {
      return;
    }
    if (!identifier) {
      identifier = this.generateIdentifier(value);
    }
    this.kv.set(identifier, value);
  }
  clear() {
    this.kv.clear();
  }
  getIdentifier(value) {
    return this.kv.getByValue(value);
  }
  getValue(identifier) {
    return this.kv.getByKey(identifier);
  }
};
var ClassRegistry21 = class extends Registry21 {
  static {
    __name(this, "ClassRegistry");
  }
  constructor() {
    super((c) => c.name);
    this.classToAllowedProps = /* @__PURE__ */ new Map();
  }
  register(value, options) {
    if (typeof options === "object") {
      if (options.allowProps) {
        this.classToAllowedProps.set(value, options.allowProps);
      }
      super.register(value, options.identifier);
    } else {
      super.register(value, options);
    }
  }
  getAllowedProps(value) {
    return this.classToAllowedProps.get(value);
  }
};
function valuesOfObj21(record) {
  if ("values" in Object) {
    return Object.values(record);
  }
  const values = [];
  for (const key in record) {
    if (record.hasOwnProperty(key)) {
      values.push(record[key]);
    }
  }
  return values;
}
__name(valuesOfObj21, "valuesOfObj");
function find21(record, predicate) {
  const values = valuesOfObj21(record);
  if ("find" in values) {
    return values.find(predicate);
  }
  const valuesNotNever = values;
  for (let i = 0; i < valuesNotNever.length; i++) {
    const value = valuesNotNever[i];
    if (predicate(value)) {
      return value;
    }
  }
  return;
}
__name(find21, "find");
function forEach21(record, run) {
  Object.entries(record).forEach(([key, value]) => run(value, key));
}
__name(forEach21, "forEach");
function includes21(arr, value) {
  return arr.indexOf(value) !== -1;
}
__name(includes21, "includes");
function findArr21(record, predicate) {
  for (let i = 0; i < record.length; i++) {
    const value = record[i];
    if (predicate(value)) {
      return value;
    }
  }
  return;
}
__name(findArr21, "findArr");
var CustomTransformerRegistry21 = class {
  static {
    __name(this, "CustomTransformerRegistry");
  }
  constructor() {
    this.transfomers = {};
  }
  register(transformer) {
    this.transfomers[transformer.name] = transformer;
  }
  findApplicable(v) {
    return find21(this.transfomers, (transformer) => transformer.isApplicable(v));
  }
  findByName(name) {
    return this.transfomers[name];
  }
};
var getType30 = /* @__PURE__ */ __name((payload) => Object.prototype.toString.call(payload).slice(8, -1), "getType");
var isUndefined21 = /* @__PURE__ */ __name((payload) => typeof payload === "undefined", "isUndefined");
var isNull22 = /* @__PURE__ */ __name((payload) => payload === null, "isNull");
var isPlainObject30 = /* @__PURE__ */ __name((payload) => {
  if (typeof payload !== "object" || payload === null)
    return false;
  if (payload === Object.prototype)
    return false;
  if (Object.getPrototypeOf(payload) === null)
    return true;
  return Object.getPrototypeOf(payload) === Object.prototype;
}, "isPlainObject");
var isEmptyObject21 = /* @__PURE__ */ __name((payload) => isPlainObject30(payload) && Object.keys(payload).length === 0, "isEmptyObject");
var isArray30 = /* @__PURE__ */ __name((payload) => Array.isArray(payload), "isArray");
var isString21 = /* @__PURE__ */ __name((payload) => typeof payload === "string", "isString");
var isNumber21 = /* @__PURE__ */ __name((payload) => typeof payload === "number" && !isNaN(payload), "isNumber");
var isBoolean21 = /* @__PURE__ */ __name((payload) => typeof payload === "boolean", "isBoolean");
var isRegExp21 = /* @__PURE__ */ __name((payload) => payload instanceof RegExp, "isRegExp");
var isMap21 = /* @__PURE__ */ __name((payload) => payload instanceof Map, "isMap");
var isSet21 = /* @__PURE__ */ __name((payload) => payload instanceof Set, "isSet");
var isSymbol21 = /* @__PURE__ */ __name((payload) => getType30(payload) === "Symbol", "isSymbol");
var isDate21 = /* @__PURE__ */ __name((payload) => payload instanceof Date && !isNaN(payload.valueOf()), "isDate");
var isError21 = /* @__PURE__ */ __name((payload) => payload instanceof Error, "isError");
var isNaNValue21 = /* @__PURE__ */ __name((payload) => typeof payload === "number" && isNaN(payload), "isNaNValue");
var isPrimitive21 = /* @__PURE__ */ __name((payload) => isBoolean21(payload) || isNull22(payload) || isUndefined21(payload) || isNumber21(payload) || isString21(payload) || isSymbol21(payload), "isPrimitive");
var isBigint21 = /* @__PURE__ */ __name((payload) => typeof payload === "bigint", "isBigint");
var isInfinite21 = /* @__PURE__ */ __name((payload) => payload === Infinity || payload === -Infinity, "isInfinite");
var isTypedArray21 = /* @__PURE__ */ __name((payload) => ArrayBuffer.isView(payload) && !(payload instanceof DataView), "isTypedArray");
var isURL21 = /* @__PURE__ */ __name((payload) => payload instanceof URL, "isURL");
var escapeKey21 = /* @__PURE__ */ __name((key) => key.replace(/\\/g, "\\\\").replace(/\./g, "\\."), "escapeKey");
var stringifyPath21 = /* @__PURE__ */ __name((path) => path.map(String).map(escapeKey21).join("."), "stringifyPath");
var parsePath21 = /* @__PURE__ */ __name((string2, legacyPaths) => {
  const result = [];
  let segment = "";
  for (let i = 0; i < string2.length; i++) {
    let char = string2.charAt(i);
    if (!legacyPaths && char === "\\") {
      const escaped = string2.charAt(i + 1);
      if (escaped === "\\") {
        segment += "\\";
        i++;
        continue;
      } else if (escaped !== ".") {
        throw Error("invalid path");
      }
    }
    const isEscapedDot = char === "\\" && string2.charAt(i + 1) === ".";
    if (isEscapedDot) {
      segment += ".";
      i++;
      continue;
    }
    const isEndOfSegment = char === ".";
    if (isEndOfSegment) {
      result.push(segment);
      segment = "";
      continue;
    }
    segment += char;
  }
  const lastSegment = segment;
  result.push(lastSegment);
  return result;
}, "parsePath");
function simpleTransformation21(isApplicable, annotation, transform, untransform) {
  return {
    isApplicable,
    annotation,
    transform,
    untransform
  };
}
__name(simpleTransformation21, "simpleTransformation");
var simpleRules21 = [
  simpleTransformation21(isUndefined21, "undefined", () => null, () => {
    return;
  }),
  simpleTransformation21(isBigint21, "bigint", (v) => v.toString(), (v) => {
    if (typeof BigInt !== "undefined") {
      return BigInt(v);
    }
    console.error("Please add a BigInt polyfill.");
    return v;
  }),
  simpleTransformation21(isDate21, "Date", (v) => v.toISOString(), (v) => new Date(v)),
  simpleTransformation21(isError21, "Error", (v, superJson) => {
    const baseError = {
      name: v.name,
      message: v.message
    };
    if ("cause" in v) {
      baseError.cause = v.cause;
    }
    superJson.allowedErrorProps.forEach((prop) => {
      baseError[prop] = v[prop];
    });
    return baseError;
  }, (v, superJson) => {
    const e = new Error(v.message, { cause: v.cause });
    e.name = v.name;
    e.stack = v.stack;
    superJson.allowedErrorProps.forEach((prop) => {
      e[prop] = v[prop];
    });
    return e;
  }),
  simpleTransformation21(isRegExp21, "regexp", (v) => "" + v, (regex) => {
    const body = regex.slice(1, regex.lastIndexOf("/"));
    const flags2 = regex.slice(regex.lastIndexOf("/") + 1);
    return new RegExp(body, flags2);
  }),
  simpleTransformation21(isSet21, "set", (v) => [...v.values()], (v) => new Set(v)),
  simpleTransformation21(isMap21, "map", (v) => [...v.entries()], (v) => new Map(v)),
  simpleTransformation21((v) => isNaNValue21(v) || isInfinite21(v), "number", (v) => {
    if (isNaNValue21(v)) {
      return "NaN";
    }
    if (v > 0) {
      return "Infinity";
    } else {
      return "-Infinity";
    }
  }, Number),
  simpleTransformation21((v) => v === 0 && 1 / v === -Infinity, "number", () => {
    return "-0";
  }, Number),
  simpleTransformation21(isURL21, "URL", (v) => v.toString(), (v) => new URL(v))
];
function compositeTransformation21(isApplicable, annotation, transform, untransform) {
  return {
    isApplicable,
    annotation,
    transform,
    untransform
  };
}
__name(compositeTransformation21, "compositeTransformation");
var symbolRule21 = compositeTransformation21((s, superJson) => {
  if (isSymbol21(s)) {
    const isRegistered = !!superJson.symbolRegistry.getIdentifier(s);
    return isRegistered;
  }
  return false;
}, (s, superJson) => {
  const identifier = superJson.symbolRegistry.getIdentifier(s);
  return ["symbol", identifier];
}, (v) => v.description, (_, a, superJson) => {
  const value = superJson.symbolRegistry.getValue(a[1]);
  if (!value) {
    throw new Error("Trying to deserialize unknown symbol");
  }
  return value;
});
var constructorToName21 = [
  Int8Array,
  Uint8Array,
  Int16Array,
  Uint16Array,
  Int32Array,
  Uint32Array,
  Float32Array,
  Float64Array,
  Uint8ClampedArray
].reduce((obj, ctor) => {
  obj[ctor.name] = ctor;
  return obj;
}, {});
var typedArrayRule21 = compositeTransformation21(isTypedArray21, (v) => ["typed-array", v.constructor.name], (v) => [...v], (v, a) => {
  const ctor = constructorToName21[a[1]];
  if (!ctor) {
    throw new Error("Trying to deserialize unknown typed array");
  }
  return new ctor(v);
});
function isInstanceOfRegisteredClass21(potentialClass, superJson) {
  if (potentialClass?.constructor) {
    const isRegistered = !!superJson.classRegistry.getIdentifier(potentialClass.constructor);
    return isRegistered;
  }
  return false;
}
__name(isInstanceOfRegisteredClass21, "isInstanceOfRegisteredClass");
var classRule21 = compositeTransformation21(isInstanceOfRegisteredClass21, (clazz, superJson) => {
  const identifier = superJson.classRegistry.getIdentifier(clazz.constructor);
  return ["class", identifier];
}, (clazz, superJson) => {
  const allowedProps = superJson.classRegistry.getAllowedProps(clazz.constructor);
  if (!allowedProps) {
    return { ...clazz };
  }
  const result = {};
  allowedProps.forEach((prop) => {
    result[prop] = clazz[prop];
  });
  return result;
}, (v, a, superJson) => {
  const clazz = superJson.classRegistry.getValue(a[1]);
  if (!clazz) {
    throw new Error(`Trying to deserialize unknown class '${a[1]}' - check https://github.com/blitz-js/superjson/issues/116#issuecomment-773996564`);
  }
  return Object.assign(Object.create(clazz.prototype), v);
});
var customRule21 = compositeTransformation21((value, superJson) => {
  return !!superJson.customTransformerRegistry.findApplicable(value);
}, (value, superJson) => {
  const transformer = superJson.customTransformerRegistry.findApplicable(value);
  return ["custom", transformer.name];
}, (value, superJson) => {
  const transformer = superJson.customTransformerRegistry.findApplicable(value);
  return transformer.serialize(value);
}, (v, a, superJson) => {
  const transformer = superJson.customTransformerRegistry.findByName(a[1]);
  if (!transformer) {
    throw new Error("Trying to deserialize unknown custom value");
  }
  return transformer.deserialize(v);
});
var compositeRules21 = [classRule21, symbolRule21, customRule21, typedArrayRule21];
var transformValue21 = /* @__PURE__ */ __name((value, superJson) => {
  const applicableCompositeRule = findArr21(compositeRules21, (rule) => rule.isApplicable(value, superJson));
  if (applicableCompositeRule) {
    return {
      value: applicableCompositeRule.transform(value, superJson),
      type: applicableCompositeRule.annotation(value, superJson)
    };
  }
  const applicableSimpleRule = findArr21(simpleRules21, (rule) => rule.isApplicable(value, superJson));
  if (applicableSimpleRule) {
    return {
      value: applicableSimpleRule.transform(value, superJson),
      type: applicableSimpleRule.annotation
    };
  }
  return;
}, "transformValue");
var simpleRulesByAnnotation21 = {};
simpleRules21.forEach((rule) => {
  simpleRulesByAnnotation21[rule.annotation] = rule;
});
var untransformValue21 = /* @__PURE__ */ __name((json, type, superJson) => {
  if (isArray30(type)) {
    switch (type[0]) {
      case "symbol":
        return symbolRule21.untransform(json, type, superJson);
      case "class":
        return classRule21.untransform(json, type, superJson);
      case "custom":
        return customRule21.untransform(json, type, superJson);
      case "typed-array":
        return typedArrayRule21.untransform(json, type, superJson);
      default:
        throw new Error("Unknown transformation: " + type);
    }
  } else {
    const transformation = simpleRulesByAnnotation21[type];
    if (!transformation) {
      throw new Error("Unknown transformation: " + type);
    }
    return transformation.untransform(json, superJson);
  }
}, "untransformValue");
var getNthKey21 = /* @__PURE__ */ __name((value, n) => {
  if (n > value.size)
    throw new Error("index out of bounds");
  const keys = value.keys();
  while (n > 0) {
    keys.next();
    n--;
  }
  return keys.next().value;
}, "getNthKey");
function validatePath21(path) {
  if (includes21(path, "__proto__")) {
    throw new Error("__proto__ is not allowed as a property");
  }
  if (includes21(path, "prototype")) {
    throw new Error("prototype is not allowed as a property");
  }
  if (includes21(path, "constructor")) {
    throw new Error("constructor is not allowed as a property");
  }
}
__name(validatePath21, "validatePath");
var getDeep21 = /* @__PURE__ */ __name((object2, path) => {
  validatePath21(path);
  for (let i = 0; i < path.length; i++) {
    const key = path[i];
    if (isSet21(object2)) {
      object2 = getNthKey21(object2, +key);
    } else if (isMap21(object2)) {
      const row = +key;
      const type = +path[++i] === 0 ? "key" : "value";
      const keyOfRow = getNthKey21(object2, row);
      switch (type) {
        case "key":
          object2 = keyOfRow;
          break;
        case "value":
          object2 = object2.get(keyOfRow);
          break;
      }
    } else {
      object2 = object2[key];
    }
  }
  return object2;
}, "getDeep");
var setDeep21 = /* @__PURE__ */ __name((object2, path, mapper) => {
  validatePath21(path);
  if (path.length === 0) {
    return mapper(object2);
  }
  let parent = object2;
  for (let i = 0; i < path.length - 1; i++) {
    const key = path[i];
    if (isArray30(parent)) {
      const index = +key;
      parent = parent[index];
    } else if (isPlainObject30(parent)) {
      parent = parent[key];
    } else if (isSet21(parent)) {
      const row = +key;
      parent = getNthKey21(parent, row);
    } else if (isMap21(parent)) {
      const isEnd = i === path.length - 2;
      if (isEnd) {
        break;
      }
      const row = +key;
      const type = +path[++i] === 0 ? "key" : "value";
      const keyOfRow = getNthKey21(parent, row);
      switch (type) {
        case "key":
          parent = keyOfRow;
          break;
        case "value":
          parent = parent.get(keyOfRow);
          break;
      }
    }
  }
  const lastKey = path[path.length - 1];
  if (isArray30(parent)) {
    parent[+lastKey] = mapper(parent[+lastKey]);
  } else if (isPlainObject30(parent)) {
    parent[lastKey] = mapper(parent[lastKey]);
  }
  if (isSet21(parent)) {
    const oldValue = getNthKey21(parent, +lastKey);
    const newValue = mapper(oldValue);
    if (oldValue !== newValue) {
      parent.delete(oldValue);
      parent.add(newValue);
    }
  }
  if (isMap21(parent)) {
    const row = +path[path.length - 2];
    const keyToRow = getNthKey21(parent, row);
    const type = +lastKey === 0 ? "key" : "value";
    switch (type) {
      case "key": {
        const newKey = mapper(keyToRow);
        parent.set(newKey, parent.get(keyToRow));
        if (newKey !== keyToRow) {
          parent.delete(keyToRow);
        }
        break;
      }
      case "value": {
        parent.set(keyToRow, mapper(parent.get(keyToRow)));
        break;
      }
    }
  }
  return object2;
}, "setDeep");
var enableLegacyPaths21 = /* @__PURE__ */ __name((version4) => version4 < 1, "enableLegacyPaths");
function traverse21(tree, walker26, version4, origin = []) {
  if (!tree) {
    return;
  }
  const legacyPaths = enableLegacyPaths21(version4);
  if (!isArray30(tree)) {
    forEach21(tree, (subtree, key) => traverse21(subtree, walker26, version4, [
      ...origin,
      ...parsePath21(key, legacyPaths)
    ]));
    return;
  }
  const [nodeValue, children] = tree;
  if (children) {
    forEach21(children, (child, key) => {
      traverse21(child, walker26, version4, [
        ...origin,
        ...parsePath21(key, legacyPaths)
      ]);
    });
  }
  walker26(nodeValue, origin);
}
__name(traverse21, "traverse");
function applyValueAnnotations21(plain, annotations, version4, superJson) {
  traverse21(annotations, (type, path) => {
    plain = setDeep21(plain, path, (v) => untransformValue21(v, type, superJson));
  }, version4);
  return plain;
}
__name(applyValueAnnotations21, "applyValueAnnotations");
function applyReferentialEqualityAnnotations21(plain, annotations, version4) {
  const legacyPaths = enableLegacyPaths21(version4);
  function apply(identicalPaths, path) {
    const object2 = getDeep21(plain, parsePath21(path, legacyPaths));
    identicalPaths.map((path2) => parsePath21(path2, legacyPaths)).forEach((identicalObjectPath) => {
      plain = setDeep21(plain, identicalObjectPath, () => object2);
    });
  }
  __name(apply, "apply");
  if (isArray30(annotations)) {
    const [root, other] = annotations;
    root.forEach((identicalPath) => {
      plain = setDeep21(plain, parsePath21(identicalPath, legacyPaths), () => plain);
    });
    if (other) {
      forEach21(other, apply);
    }
  } else {
    forEach21(annotations, apply);
  }
  return plain;
}
__name(applyReferentialEqualityAnnotations21, "applyReferentialEqualityAnnotations");
var isDeep21 = /* @__PURE__ */ __name((object2, superJson) => isPlainObject30(object2) || isArray30(object2) || isMap21(object2) || isSet21(object2) || isError21(object2) || isInstanceOfRegisteredClass21(object2, superJson), "isDeep");
function addIdentity21(object2, path, identities) {
  const existingSet = identities.get(object2);
  if (existingSet) {
    existingSet.push(path);
  } else {
    identities.set(object2, [path]);
  }
}
__name(addIdentity21, "addIdentity");
function generateReferentialEqualityAnnotations21(identitites, dedupe) {
  const result = {};
  let rootEqualityPaths = void 0;
  identitites.forEach((paths) => {
    if (paths.length <= 1) {
      return;
    }
    if (!dedupe) {
      paths = paths.map((path) => path.map(String)).sort((a, b) => a.length - b.length);
    }
    const [representativePath, ...identicalPaths] = paths;
    if (representativePath.length === 0) {
      rootEqualityPaths = identicalPaths.map(stringifyPath21);
    } else {
      result[stringifyPath21(representativePath)] = identicalPaths.map(stringifyPath21);
    }
  });
  if (rootEqualityPaths) {
    if (isEmptyObject21(result)) {
      return [rootEqualityPaths];
    } else {
      return [rootEqualityPaths, result];
    }
  } else {
    return isEmptyObject21(result) ? void 0 : result;
  }
}
__name(generateReferentialEqualityAnnotations21, "generateReferentialEqualityAnnotations");
var walker21 = /* @__PURE__ */ __name((object2, identities, superJson, dedupe, path = [], objectsInThisPath = [], seenObjects = /* @__PURE__ */ new Map()) => {
  const primitive = isPrimitive21(object2);
  if (!primitive) {
    addIdentity21(object2, path, identities);
    const seen = seenObjects.get(object2);
    if (seen) {
      return dedupe ? {
        transformedValue: null
      } : seen;
    }
  }
  if (!isDeep21(object2, superJson)) {
    const transformed2 = transformValue21(object2, superJson);
    const result2 = transformed2 ? {
      transformedValue: transformed2.value,
      annotations: [transformed2.type]
    } : {
      transformedValue: object2
    };
    if (!primitive) {
      seenObjects.set(object2, result2);
    }
    return result2;
  }
  if (includes21(objectsInThisPath, object2)) {
    return {
      transformedValue: null
    };
  }
  const transformationResult = transformValue21(object2, superJson);
  const transformed = transformationResult?.value ?? object2;
  const transformedValue = isArray30(transformed) ? [] : {};
  const innerAnnotations = {};
  forEach21(transformed, (value, index) => {
    if (index === "__proto__" || index === "constructor" || index === "prototype") {
      throw new Error(`Detected property ${index}. This is a prototype pollution risk, please remove it from your object.`);
    }
    const recursiveResult = walker21(value, identities, superJson, dedupe, [...path, index], [...objectsInThisPath, object2], seenObjects);
    transformedValue[index] = recursiveResult.transformedValue;
    if (isArray30(recursiveResult.annotations)) {
      innerAnnotations[escapeKey21(index)] = recursiveResult.annotations;
    } else if (isPlainObject30(recursiveResult.annotations)) {
      forEach21(recursiveResult.annotations, (tree, key) => {
        innerAnnotations[escapeKey21(index) + "." + key] = tree;
      });
    }
  });
  const result = isEmptyObject21(innerAnnotations) ? {
    transformedValue,
    annotations: transformationResult ? [transformationResult.type] : void 0
  } : {
    transformedValue,
    annotations: transformationResult ? [transformationResult.type, innerAnnotations] : innerAnnotations
  };
  if (!primitive) {
    seenObjects.set(object2, result);
  }
  return result;
}, "walker");
function getType221(payload) {
  return Object.prototype.toString.call(payload).slice(8, -1);
}
__name(getType221, "getType2");
function isArray221(payload) {
  return getType221(payload) === "Array";
}
__name(isArray221, "isArray2");
function isPlainObject221(payload) {
  if (getType221(payload) !== "Object")
    return false;
  const prototype = Object.getPrototypeOf(payload);
  return !!prototype && prototype.constructor === Object && prototype === Object.prototype;
}
__name(isPlainObject221, "isPlainObject2");
function assignProp21(carry, key, newVal, originalObject, includeNonenumerable) {
  const propType = {}.propertyIsEnumerable.call(originalObject, key) ? "enumerable" : "nonenumerable";
  if (propType === "enumerable")
    carry[key] = newVal;
  if (includeNonenumerable && propType === "nonenumerable") {
    Object.defineProperty(carry, key, {
      value: newVal,
      enumerable: false,
      writable: true,
      configurable: true
    });
  }
}
__name(assignProp21, "assignProp");
function copy21(target, options = {}) {
  if (isArray221(target)) {
    return target.map((item) => copy21(item, options));
  }
  if (!isPlainObject221(target)) {
    return target;
  }
  const props = Object.getOwnPropertyNames(target);
  const symbols = Object.getOwnPropertySymbols(target);
  return [...props, ...symbols].reduce((carry, key) => {
    if (key === "__proto__")
      return carry;
    if (isArray221(options.props) && !options.props.includes(key)) {
      return carry;
    }
    const val = target[key];
    const newVal = copy21(val, options);
    assignProp21(carry, key, newVal, target, options.nonenumerable);
    return carry;
  }, {});
}
__name(copy21, "copy");
var SuperJSON21 = class {
  static {
    __name(this, "SuperJSON");
  }
  constructor({ dedupe = false } = {}) {
    this.classRegistry = new ClassRegistry21();
    this.symbolRegistry = new Registry21((s) => s.description ?? "");
    this.customTransformerRegistry = new CustomTransformerRegistry21();
    this.allowedErrorProps = [];
    this.dedupe = dedupe;
  }
  serialize(object2) {
    const identities = /* @__PURE__ */ new Map();
    const output = walker21(object2, identities, this, this.dedupe);
    const res = {
      json: output.transformedValue
    };
    if (output.annotations) {
      res.meta = {
        ...res.meta,
        values: output.annotations
      };
    }
    const equalityAnnotations = generateReferentialEqualityAnnotations21(identities, this.dedupe);
    if (equalityAnnotations) {
      res.meta = {
        ...res.meta,
        referentialEqualities: equalityAnnotations
      };
    }
    if (res.meta)
      res.meta.v = 1;
    return res;
  }
  deserialize(payload, options) {
    const { json, meta } = payload;
    let result = options?.inPlace ? json : copy21(json);
    if (meta?.values) {
      result = applyValueAnnotations21(result, meta.values, meta.v ?? 0, this);
    }
    if (meta?.referentialEqualities) {
      result = applyReferentialEqualityAnnotations21(result, meta.referentialEqualities, meta.v ?? 0);
    }
    return result;
  }
  stringify(object2) {
    return JSON.stringify(this.serialize(object2));
  }
  parse(string2) {
    return this.deserialize(JSON.parse(string2), { inPlace: true });
  }
  registerClass(v, options) {
    this.classRegistry.register(v, options);
  }
  registerSymbol(v, identifier) {
    this.symbolRegistry.register(v, identifier);
  }
  registerCustom(transformer, name) {
    this.customTransformerRegistry.register({
      name,
      ...transformer
    });
  }
  allowErrorProps(...props) {
    this.allowedErrorProps.push(...props);
  }
};
SuperJSON21.defaultInstance = new SuperJSON21();
SuperJSON21.serialize = SuperJSON21.defaultInstance.serialize.bind(SuperJSON21.defaultInstance);
SuperJSON21.deserialize = SuperJSON21.defaultInstance.deserialize.bind(SuperJSON21.defaultInstance);
SuperJSON21.stringify = SuperJSON21.defaultInstance.stringify.bind(SuperJSON21.defaultInstance);
SuperJSON21.parse = SuperJSON21.defaultInstance.parse.bind(SuperJSON21.defaultInstance);
SuperJSON21.registerClass = SuperJSON21.defaultInstance.registerClass.bind(SuperJSON21.defaultInstance);
SuperJSON21.registerSymbol = SuperJSON21.defaultInstance.registerSymbol.bind(SuperJSON21.defaultInstance);
SuperJSON21.registerCustom = SuperJSON21.defaultInstance.registerCustom.bind(SuperJSON21.defaultInstance);
SuperJSON21.allowErrorProps = SuperJSON21.defaultInstance.allowErrorProps.bind(SuperJSON21.defaultInstance);
var dist_default21 = SuperJSON21;
var serialize21 = SuperJSON21.serialize;
var deserialize21 = SuperJSON21.deserialize;
var stringify22 = SuperJSON21.stringify;
var parse21 = SuperJSON21.parse;
var registerClass21 = SuperJSON21.registerClass;
var registerCustom21 = SuperJSON21.registerCustom;
var registerSymbol21 = SuperJSON21.registerSymbol;
var allowErrorProps21 = SuperJSON21.allowErrorProps;
function parseData21(formData) {
  const propsArray = [];
  if (!formData)
    return propsArray;
  const batchsIDs = [];
  for (const [key, value] of Array.from(formData.entries())) {
    if (key.startsWith("FILE_"))
      propsArray.push(value);
    else if (key.startsWith("FILES_")) {
      if (batchsIDs.includes(key))
        continue;
      batchsIDs.push(key);
      propsArray.push(formData.getAll(key));
    } else {
      propsArray.push(JSON.parse(decodeURI(value)));
    }
  }
  return propsArray;
}
__name(parseData21, "parseData");
function paramsFromURL21(url) {
  const params = url.searchParams.entries().toArray().map(([_, v]) => v);
  return params.map((param) => JSON.parse(decodeURIComponent(param)));
}
__name(paramsFromURL21, "paramsFromURL");
async function WrapRequestHandler21(context2, endpoint) {
  const isServerAction = context2.request.headers.get("x-server-action") === "true";
  if (!isServerAction) {
    return new Response("Not Found", { status: 404 });
  }
  const parsedData = context2.request.method === "GET" || context2.request.method === "HEAD" ? paramsFromURL21(new URL(context2.request.url)) : parseData21(context2.request.headers.get("content-type") ? await context2.request.formData() : void 0);
  const missingProps = endpoint.length - parsedData.length;
  for (let i = 0; i < missingProps; i++) {
    parsedData.push(void 0);
  }
  parsedData.push(context2);
  const result = await endpoint(...parsedData);
  switch (typeof result) {
    case "string":
    case "number":
    case "boolean":
    case "bigint":
      return new Response(dist_default21.stringify(result), {
        headers: {
          "Content-Type": "application/json",
          dataType: "json"
        }
      });
    case "undefined":
      return new Response(null, { status: 204 });
    case "object":
      if (result instanceof Response) {
        result.headers.set("dataType", "response");
        return result;
      } else if (result instanceof Blob) {
        const res = new Response(await result.arrayBuffer());
        res.headers.set("dataType", "blob");
        res.headers.set("Content-Type", result.type);
        return res;
      } else if (result instanceof File) {
        return new Response(await result.arrayBuffer(), {
          headers: {
            "Content-Type": result.type,
            dataType: "file",
            fileData: JSON.stringify({
              name: result.name,
              lastModified: result.lastModified
            })
          }
        });
      } else {
        return new Response(dist_default21.stringify(result), {
          headers: { "Content-Type": "application/json", dataType: "json" }
        });
      }
    default:
      throw new Error(`Unsupported return type from action: ${typeof result}`);
  }
}
__name(WrapRequestHandler21, "WrapRequestHandler");
var onRequest21 = /* @__PURE__ */ __name(async (context2) => {
  const method = context2.request.method;
  const options = {
    GET: typeof GET12 === "function" ? GET12 : void 0,
    POST: typeof POST === "function" ? POST : void 0,
    PUT: typeof PUT === "function" ? PUT : void 0,
    DELETE: typeof DELETE === "function" ? DELETE : void 0,
    PATCH: typeof PATCH === "function" ? PATCH : void 0,
    HEAD: typeof HEAD === "function" ? HEAD : void 0,
    OPTIONS: typeof OPTIONS === "function" ? OPTIONS : void 0
  };
  if (!options[method]) {
    return new Response(`Method "${method}" Not Allowed`, {
      status: 405
    });
  }
  return await WrapRequestHandler21(context2, options[method]);
}, "onRequest");

// api/templates/index.js
async function GET13() {
  const ctx = getContext(arguments);
  const { env: env2 } = ctx;
  const currentUserId = (await ctx.data.client.getMetaData())?.id;
  if (!currentUserId) {
    return {
      success: false,
      error: "Unauthorized",
      data: []
    };
  }
  const db = drizzle(env2.PROJECT_DB);
  const templates = await db.select().from(emailTemplatesTable).where(eq(emailTemplatesTable.owner_id, currentUserId));
  return {
    success: true,
    data: templates
  };
}
__name(GET13, "GET");
async function POST13(params) {
  const ctx = getContext(arguments);
  const { env: env2 } = ctx;
  try {
    const currentUserId = (await ctx.data.client.getMetaData())?.id;
    if (!currentUserId) {
      return {
        success: false,
        error: "Unauthorized"
      };
    }
    const { name, body, subject: subject2 } = params;
    if (!name || typeof name !== "string" || name.trim().length === 0) {
      return {
        success: false,
        error: "Invalid or missing template name"
      };
    }
    if (!subject2 || typeof subject2 !== "string") {
      return {
        success: false,
        error: "Invalid or missing subject"
      };
    }
    if (!body || typeof body !== "string") {
      return {
        success: false,
        error: "Invalid or missing body"
      };
    }
    const db = drizzle(env2.PROJECT_DB);
    const now = (/* @__PURE__ */ new Date()).toISOString();
    const newTemplate = {
      name: name.trim(),
      body,
      subject: subject2,
      owner_id: currentUserId,
      owner_group_id: crypto.randomUUID(),
      created_at: now,
      updated_at: now
    };
    return {
      success: true,
      data: (await db.insert(emailTemplatesTable).values(newTemplate).returning()).at(0)
    };
  } catch (err) {
    return {
      success: false,
      error: err instanceof Error ? err.message : "Failed to create template"
    };
  }
}
__name(POST13, "POST");
var DoubleIndexedKV22 = class {
  static {
    __name(this, "DoubleIndexedKV");
  }
  constructor() {
    this.keyToValue = /* @__PURE__ */ new Map();
    this.valueToKey = /* @__PURE__ */ new Map();
  }
  set(key, value) {
    this.keyToValue.set(key, value);
    this.valueToKey.set(value, key);
  }
  getByKey(key) {
    return this.keyToValue.get(key);
  }
  getByValue(value) {
    return this.valueToKey.get(value);
  }
  clear() {
    this.keyToValue.clear();
    this.valueToKey.clear();
  }
};
var Registry22 = class {
  static {
    __name(this, "Registry");
  }
  constructor(generateIdentifier) {
    this.generateIdentifier = generateIdentifier;
    this.kv = new DoubleIndexedKV22();
  }
  register(value, identifier) {
    if (this.kv.getByValue(value)) {
      return;
    }
    if (!identifier) {
      identifier = this.generateIdentifier(value);
    }
    this.kv.set(identifier, value);
  }
  clear() {
    this.kv.clear();
  }
  getIdentifier(value) {
    return this.kv.getByValue(value);
  }
  getValue(identifier) {
    return this.kv.getByKey(identifier);
  }
};
var ClassRegistry22 = class extends Registry22 {
  static {
    __name(this, "ClassRegistry");
  }
  constructor() {
    super((c) => c.name);
    this.classToAllowedProps = /* @__PURE__ */ new Map();
  }
  register(value, options) {
    if (typeof options === "object") {
      if (options.allowProps) {
        this.classToAllowedProps.set(value, options.allowProps);
      }
      super.register(value, options.identifier);
    } else {
      super.register(value, options);
    }
  }
  getAllowedProps(value) {
    return this.classToAllowedProps.get(value);
  }
};
function valuesOfObj22(record) {
  if ("values" in Object) {
    return Object.values(record);
  }
  const values = [];
  for (const key in record) {
    if (record.hasOwnProperty(key)) {
      values.push(record[key]);
    }
  }
  return values;
}
__name(valuesOfObj22, "valuesOfObj");
function find22(record, predicate) {
  const values = valuesOfObj22(record);
  if ("find" in values) {
    return values.find(predicate);
  }
  const valuesNotNever = values;
  for (let i = 0; i < valuesNotNever.length; i++) {
    const value = valuesNotNever[i];
    if (predicate(value)) {
      return value;
    }
  }
  return;
}
__name(find22, "find");
function forEach22(record, run) {
  Object.entries(record).forEach(([key, value]) => run(value, key));
}
__name(forEach22, "forEach");
function includes22(arr, value) {
  return arr.indexOf(value) !== -1;
}
__name(includes22, "includes");
function findArr22(record, predicate) {
  for (let i = 0; i < record.length; i++) {
    const value = record[i];
    if (predicate(value)) {
      return value;
    }
  }
  return;
}
__name(findArr22, "findArr");
var CustomTransformerRegistry22 = class {
  static {
    __name(this, "CustomTransformerRegistry");
  }
  constructor() {
    this.transfomers = {};
  }
  register(transformer) {
    this.transfomers[transformer.name] = transformer;
  }
  findApplicable(v) {
    return find22(this.transfomers, (transformer) => transformer.isApplicable(v));
  }
  findByName(name) {
    return this.transfomers[name];
  }
};
var getType31 = /* @__PURE__ */ __name((payload) => Object.prototype.toString.call(payload).slice(8, -1), "getType");
var isUndefined22 = /* @__PURE__ */ __name((payload) => typeof payload === "undefined", "isUndefined");
var isNull23 = /* @__PURE__ */ __name((payload) => payload === null, "isNull");
var isPlainObject31 = /* @__PURE__ */ __name((payload) => {
  if (typeof payload !== "object" || payload === null)
    return false;
  if (payload === Object.prototype)
    return false;
  if (Object.getPrototypeOf(payload) === null)
    return true;
  return Object.getPrototypeOf(payload) === Object.prototype;
}, "isPlainObject");
var isEmptyObject22 = /* @__PURE__ */ __name((payload) => isPlainObject31(payload) && Object.keys(payload).length === 0, "isEmptyObject");
var isArray31 = /* @__PURE__ */ __name((payload) => Array.isArray(payload), "isArray");
var isString22 = /* @__PURE__ */ __name((payload) => typeof payload === "string", "isString");
var isNumber22 = /* @__PURE__ */ __name((payload) => typeof payload === "number" && !isNaN(payload), "isNumber");
var isBoolean22 = /* @__PURE__ */ __name((payload) => typeof payload === "boolean", "isBoolean");
var isRegExp22 = /* @__PURE__ */ __name((payload) => payload instanceof RegExp, "isRegExp");
var isMap22 = /* @__PURE__ */ __name((payload) => payload instanceof Map, "isMap");
var isSet22 = /* @__PURE__ */ __name((payload) => payload instanceof Set, "isSet");
var isSymbol22 = /* @__PURE__ */ __name((payload) => getType31(payload) === "Symbol", "isSymbol");
var isDate22 = /* @__PURE__ */ __name((payload) => payload instanceof Date && !isNaN(payload.valueOf()), "isDate");
var isError22 = /* @__PURE__ */ __name((payload) => payload instanceof Error, "isError");
var isNaNValue22 = /* @__PURE__ */ __name((payload) => typeof payload === "number" && isNaN(payload), "isNaNValue");
var isPrimitive22 = /* @__PURE__ */ __name((payload) => isBoolean22(payload) || isNull23(payload) || isUndefined22(payload) || isNumber22(payload) || isString22(payload) || isSymbol22(payload), "isPrimitive");
var isBigint22 = /* @__PURE__ */ __name((payload) => typeof payload === "bigint", "isBigint");
var isInfinite22 = /* @__PURE__ */ __name((payload) => payload === Infinity || payload === -Infinity, "isInfinite");
var isTypedArray22 = /* @__PURE__ */ __name((payload) => ArrayBuffer.isView(payload) && !(payload instanceof DataView), "isTypedArray");
var isURL22 = /* @__PURE__ */ __name((payload) => payload instanceof URL, "isURL");
var escapeKey22 = /* @__PURE__ */ __name((key) => key.replace(/\\/g, "\\\\").replace(/\./g, "\\."), "escapeKey");
var stringifyPath22 = /* @__PURE__ */ __name((path) => path.map(String).map(escapeKey22).join("."), "stringifyPath");
var parsePath22 = /* @__PURE__ */ __name((string2, legacyPaths) => {
  const result = [];
  let segment = "";
  for (let i = 0; i < string2.length; i++) {
    let char = string2.charAt(i);
    if (!legacyPaths && char === "\\") {
      const escaped = string2.charAt(i + 1);
      if (escaped === "\\") {
        segment += "\\";
        i++;
        continue;
      } else if (escaped !== ".") {
        throw Error("invalid path");
      }
    }
    const isEscapedDot = char === "\\" && string2.charAt(i + 1) === ".";
    if (isEscapedDot) {
      segment += ".";
      i++;
      continue;
    }
    const isEndOfSegment = char === ".";
    if (isEndOfSegment) {
      result.push(segment);
      segment = "";
      continue;
    }
    segment += char;
  }
  const lastSegment = segment;
  result.push(lastSegment);
  return result;
}, "parsePath");
function simpleTransformation22(isApplicable, annotation, transform, untransform) {
  return {
    isApplicable,
    annotation,
    transform,
    untransform
  };
}
__name(simpleTransformation22, "simpleTransformation");
var simpleRules22 = [
  simpleTransformation22(isUndefined22, "undefined", () => null, () => {
    return;
  }),
  simpleTransformation22(isBigint22, "bigint", (v) => v.toString(), (v) => {
    if (typeof BigInt !== "undefined") {
      return BigInt(v);
    }
    console.error("Please add a BigInt polyfill.");
    return v;
  }),
  simpleTransformation22(isDate22, "Date", (v) => v.toISOString(), (v) => new Date(v)),
  simpleTransformation22(isError22, "Error", (v, superJson) => {
    const baseError = {
      name: v.name,
      message: v.message
    };
    if ("cause" in v) {
      baseError.cause = v.cause;
    }
    superJson.allowedErrorProps.forEach((prop) => {
      baseError[prop] = v[prop];
    });
    return baseError;
  }, (v, superJson) => {
    const e = new Error(v.message, { cause: v.cause });
    e.name = v.name;
    e.stack = v.stack;
    superJson.allowedErrorProps.forEach((prop) => {
      e[prop] = v[prop];
    });
    return e;
  }),
  simpleTransformation22(isRegExp22, "regexp", (v) => "" + v, (regex) => {
    const body = regex.slice(1, regex.lastIndexOf("/"));
    const flags2 = regex.slice(regex.lastIndexOf("/") + 1);
    return new RegExp(body, flags2);
  }),
  simpleTransformation22(isSet22, "set", (v) => [...v.values()], (v) => new Set(v)),
  simpleTransformation22(isMap22, "map", (v) => [...v.entries()], (v) => new Map(v)),
  simpleTransformation22((v) => isNaNValue22(v) || isInfinite22(v), "number", (v) => {
    if (isNaNValue22(v)) {
      return "NaN";
    }
    if (v > 0) {
      return "Infinity";
    } else {
      return "-Infinity";
    }
  }, Number),
  simpleTransformation22((v) => v === 0 && 1 / v === -Infinity, "number", () => {
    return "-0";
  }, Number),
  simpleTransformation22(isURL22, "URL", (v) => v.toString(), (v) => new URL(v))
];
function compositeTransformation22(isApplicable, annotation, transform, untransform) {
  return {
    isApplicable,
    annotation,
    transform,
    untransform
  };
}
__name(compositeTransformation22, "compositeTransformation");
var symbolRule22 = compositeTransformation22((s, superJson) => {
  if (isSymbol22(s)) {
    const isRegistered = !!superJson.symbolRegistry.getIdentifier(s);
    return isRegistered;
  }
  return false;
}, (s, superJson) => {
  const identifier = superJson.symbolRegistry.getIdentifier(s);
  return ["symbol", identifier];
}, (v) => v.description, (_, a, superJson) => {
  const value = superJson.symbolRegistry.getValue(a[1]);
  if (!value) {
    throw new Error("Trying to deserialize unknown symbol");
  }
  return value;
});
var constructorToName22 = [
  Int8Array,
  Uint8Array,
  Int16Array,
  Uint16Array,
  Int32Array,
  Uint32Array,
  Float32Array,
  Float64Array,
  Uint8ClampedArray
].reduce((obj, ctor) => {
  obj[ctor.name] = ctor;
  return obj;
}, {});
var typedArrayRule22 = compositeTransformation22(isTypedArray22, (v) => ["typed-array", v.constructor.name], (v) => [...v], (v, a) => {
  const ctor = constructorToName22[a[1]];
  if (!ctor) {
    throw new Error("Trying to deserialize unknown typed array");
  }
  return new ctor(v);
});
function isInstanceOfRegisteredClass22(potentialClass, superJson) {
  if (potentialClass?.constructor) {
    const isRegistered = !!superJson.classRegistry.getIdentifier(potentialClass.constructor);
    return isRegistered;
  }
  return false;
}
__name(isInstanceOfRegisteredClass22, "isInstanceOfRegisteredClass");
var classRule22 = compositeTransformation22(isInstanceOfRegisteredClass22, (clazz, superJson) => {
  const identifier = superJson.classRegistry.getIdentifier(clazz.constructor);
  return ["class", identifier];
}, (clazz, superJson) => {
  const allowedProps = superJson.classRegistry.getAllowedProps(clazz.constructor);
  if (!allowedProps) {
    return { ...clazz };
  }
  const result = {};
  allowedProps.forEach((prop) => {
    result[prop] = clazz[prop];
  });
  return result;
}, (v, a, superJson) => {
  const clazz = superJson.classRegistry.getValue(a[1]);
  if (!clazz) {
    throw new Error(`Trying to deserialize unknown class '${a[1]}' - check https://github.com/blitz-js/superjson/issues/116#issuecomment-773996564`);
  }
  return Object.assign(Object.create(clazz.prototype), v);
});
var customRule22 = compositeTransformation22((value, superJson) => {
  return !!superJson.customTransformerRegistry.findApplicable(value);
}, (value, superJson) => {
  const transformer = superJson.customTransformerRegistry.findApplicable(value);
  return ["custom", transformer.name];
}, (value, superJson) => {
  const transformer = superJson.customTransformerRegistry.findApplicable(value);
  return transformer.serialize(value);
}, (v, a, superJson) => {
  const transformer = superJson.customTransformerRegistry.findByName(a[1]);
  if (!transformer) {
    throw new Error("Trying to deserialize unknown custom value");
  }
  return transformer.deserialize(v);
});
var compositeRules22 = [classRule22, symbolRule22, customRule22, typedArrayRule22];
var transformValue22 = /* @__PURE__ */ __name((value, superJson) => {
  const applicableCompositeRule = findArr22(compositeRules22, (rule) => rule.isApplicable(value, superJson));
  if (applicableCompositeRule) {
    return {
      value: applicableCompositeRule.transform(value, superJson),
      type: applicableCompositeRule.annotation(value, superJson)
    };
  }
  const applicableSimpleRule = findArr22(simpleRules22, (rule) => rule.isApplicable(value, superJson));
  if (applicableSimpleRule) {
    return {
      value: applicableSimpleRule.transform(value, superJson),
      type: applicableSimpleRule.annotation
    };
  }
  return;
}, "transformValue");
var simpleRulesByAnnotation22 = {};
simpleRules22.forEach((rule) => {
  simpleRulesByAnnotation22[rule.annotation] = rule;
});
var untransformValue22 = /* @__PURE__ */ __name((json, type, superJson) => {
  if (isArray31(type)) {
    switch (type[0]) {
      case "symbol":
        return symbolRule22.untransform(json, type, superJson);
      case "class":
        return classRule22.untransform(json, type, superJson);
      case "custom":
        return customRule22.untransform(json, type, superJson);
      case "typed-array":
        return typedArrayRule22.untransform(json, type, superJson);
      default:
        throw new Error("Unknown transformation: " + type);
    }
  } else {
    const transformation = simpleRulesByAnnotation22[type];
    if (!transformation) {
      throw new Error("Unknown transformation: " + type);
    }
    return transformation.untransform(json, superJson);
  }
}, "untransformValue");
var getNthKey22 = /* @__PURE__ */ __name((value, n) => {
  if (n > value.size)
    throw new Error("index out of bounds");
  const keys = value.keys();
  while (n > 0) {
    keys.next();
    n--;
  }
  return keys.next().value;
}, "getNthKey");
function validatePath22(path) {
  if (includes22(path, "__proto__")) {
    throw new Error("__proto__ is not allowed as a property");
  }
  if (includes22(path, "prototype")) {
    throw new Error("prototype is not allowed as a property");
  }
  if (includes22(path, "constructor")) {
    throw new Error("constructor is not allowed as a property");
  }
}
__name(validatePath22, "validatePath");
var getDeep22 = /* @__PURE__ */ __name((object2, path) => {
  validatePath22(path);
  for (let i = 0; i < path.length; i++) {
    const key = path[i];
    if (isSet22(object2)) {
      object2 = getNthKey22(object2, +key);
    } else if (isMap22(object2)) {
      const row = +key;
      const type = +path[++i] === 0 ? "key" : "value";
      const keyOfRow = getNthKey22(object2, row);
      switch (type) {
        case "key":
          object2 = keyOfRow;
          break;
        case "value":
          object2 = object2.get(keyOfRow);
          break;
      }
    } else {
      object2 = object2[key];
    }
  }
  return object2;
}, "getDeep");
var setDeep22 = /* @__PURE__ */ __name((object2, path, mapper) => {
  validatePath22(path);
  if (path.length === 0) {
    return mapper(object2);
  }
  let parent = object2;
  for (let i = 0; i < path.length - 1; i++) {
    const key = path[i];
    if (isArray31(parent)) {
      const index = +key;
      parent = parent[index];
    } else if (isPlainObject31(parent)) {
      parent = parent[key];
    } else if (isSet22(parent)) {
      const row = +key;
      parent = getNthKey22(parent, row);
    } else if (isMap22(parent)) {
      const isEnd = i === path.length - 2;
      if (isEnd) {
        break;
      }
      const row = +key;
      const type = +path[++i] === 0 ? "key" : "value";
      const keyOfRow = getNthKey22(parent, row);
      switch (type) {
        case "key":
          parent = keyOfRow;
          break;
        case "value":
          parent = parent.get(keyOfRow);
          break;
      }
    }
  }
  const lastKey = path[path.length - 1];
  if (isArray31(parent)) {
    parent[+lastKey] = mapper(parent[+lastKey]);
  } else if (isPlainObject31(parent)) {
    parent[lastKey] = mapper(parent[lastKey]);
  }
  if (isSet22(parent)) {
    const oldValue = getNthKey22(parent, +lastKey);
    const newValue = mapper(oldValue);
    if (oldValue !== newValue) {
      parent.delete(oldValue);
      parent.add(newValue);
    }
  }
  if (isMap22(parent)) {
    const row = +path[path.length - 2];
    const keyToRow = getNthKey22(parent, row);
    const type = +lastKey === 0 ? "key" : "value";
    switch (type) {
      case "key": {
        const newKey = mapper(keyToRow);
        parent.set(newKey, parent.get(keyToRow));
        if (newKey !== keyToRow) {
          parent.delete(keyToRow);
        }
        break;
      }
      case "value": {
        parent.set(keyToRow, mapper(parent.get(keyToRow)));
        break;
      }
    }
  }
  return object2;
}, "setDeep");
var enableLegacyPaths22 = /* @__PURE__ */ __name((version4) => version4 < 1, "enableLegacyPaths");
function traverse22(tree, walker26, version4, origin = []) {
  if (!tree) {
    return;
  }
  const legacyPaths = enableLegacyPaths22(version4);
  if (!isArray31(tree)) {
    forEach22(tree, (subtree, key) => traverse22(subtree, walker26, version4, [
      ...origin,
      ...parsePath22(key, legacyPaths)
    ]));
    return;
  }
  const [nodeValue, children] = tree;
  if (children) {
    forEach22(children, (child, key) => {
      traverse22(child, walker26, version4, [
        ...origin,
        ...parsePath22(key, legacyPaths)
      ]);
    });
  }
  walker26(nodeValue, origin);
}
__name(traverse22, "traverse");
function applyValueAnnotations22(plain, annotations, version4, superJson) {
  traverse22(annotations, (type, path) => {
    plain = setDeep22(plain, path, (v) => untransformValue22(v, type, superJson));
  }, version4);
  return plain;
}
__name(applyValueAnnotations22, "applyValueAnnotations");
function applyReferentialEqualityAnnotations22(plain, annotations, version4) {
  const legacyPaths = enableLegacyPaths22(version4);
  function apply(identicalPaths, path) {
    const object2 = getDeep22(plain, parsePath22(path, legacyPaths));
    identicalPaths.map((path2) => parsePath22(path2, legacyPaths)).forEach((identicalObjectPath) => {
      plain = setDeep22(plain, identicalObjectPath, () => object2);
    });
  }
  __name(apply, "apply");
  if (isArray31(annotations)) {
    const [root, other] = annotations;
    root.forEach((identicalPath) => {
      plain = setDeep22(plain, parsePath22(identicalPath, legacyPaths), () => plain);
    });
    if (other) {
      forEach22(other, apply);
    }
  } else {
    forEach22(annotations, apply);
  }
  return plain;
}
__name(applyReferentialEqualityAnnotations22, "applyReferentialEqualityAnnotations");
var isDeep22 = /* @__PURE__ */ __name((object2, superJson) => isPlainObject31(object2) || isArray31(object2) || isMap22(object2) || isSet22(object2) || isError22(object2) || isInstanceOfRegisteredClass22(object2, superJson), "isDeep");
function addIdentity22(object2, path, identities) {
  const existingSet = identities.get(object2);
  if (existingSet) {
    existingSet.push(path);
  } else {
    identities.set(object2, [path]);
  }
}
__name(addIdentity22, "addIdentity");
function generateReferentialEqualityAnnotations22(identitites, dedupe) {
  const result = {};
  let rootEqualityPaths = void 0;
  identitites.forEach((paths) => {
    if (paths.length <= 1) {
      return;
    }
    if (!dedupe) {
      paths = paths.map((path) => path.map(String)).sort((a, b) => a.length - b.length);
    }
    const [representativePath, ...identicalPaths] = paths;
    if (representativePath.length === 0) {
      rootEqualityPaths = identicalPaths.map(stringifyPath22);
    } else {
      result[stringifyPath22(representativePath)] = identicalPaths.map(stringifyPath22);
    }
  });
  if (rootEqualityPaths) {
    if (isEmptyObject22(result)) {
      return [rootEqualityPaths];
    } else {
      return [rootEqualityPaths, result];
    }
  } else {
    return isEmptyObject22(result) ? void 0 : result;
  }
}
__name(generateReferentialEqualityAnnotations22, "generateReferentialEqualityAnnotations");
var walker22 = /* @__PURE__ */ __name((object2, identities, superJson, dedupe, path = [], objectsInThisPath = [], seenObjects = /* @__PURE__ */ new Map()) => {
  const primitive = isPrimitive22(object2);
  if (!primitive) {
    addIdentity22(object2, path, identities);
    const seen = seenObjects.get(object2);
    if (seen) {
      return dedupe ? {
        transformedValue: null
      } : seen;
    }
  }
  if (!isDeep22(object2, superJson)) {
    const transformed2 = transformValue22(object2, superJson);
    const result2 = transformed2 ? {
      transformedValue: transformed2.value,
      annotations: [transformed2.type]
    } : {
      transformedValue: object2
    };
    if (!primitive) {
      seenObjects.set(object2, result2);
    }
    return result2;
  }
  if (includes22(objectsInThisPath, object2)) {
    return {
      transformedValue: null
    };
  }
  const transformationResult = transformValue22(object2, superJson);
  const transformed = transformationResult?.value ?? object2;
  const transformedValue = isArray31(transformed) ? [] : {};
  const innerAnnotations = {};
  forEach22(transformed, (value, index) => {
    if (index === "__proto__" || index === "constructor" || index === "prototype") {
      throw new Error(`Detected property ${index}. This is a prototype pollution risk, please remove it from your object.`);
    }
    const recursiveResult = walker22(value, identities, superJson, dedupe, [...path, index], [...objectsInThisPath, object2], seenObjects);
    transformedValue[index] = recursiveResult.transformedValue;
    if (isArray31(recursiveResult.annotations)) {
      innerAnnotations[escapeKey22(index)] = recursiveResult.annotations;
    } else if (isPlainObject31(recursiveResult.annotations)) {
      forEach22(recursiveResult.annotations, (tree, key) => {
        innerAnnotations[escapeKey22(index) + "." + key] = tree;
      });
    }
  });
  const result = isEmptyObject22(innerAnnotations) ? {
    transformedValue,
    annotations: transformationResult ? [transformationResult.type] : void 0
  } : {
    transformedValue,
    annotations: transformationResult ? [transformationResult.type, innerAnnotations] : innerAnnotations
  };
  if (!primitive) {
    seenObjects.set(object2, result);
  }
  return result;
}, "walker");
function getType222(payload) {
  return Object.prototype.toString.call(payload).slice(8, -1);
}
__name(getType222, "getType2");
function isArray222(payload) {
  return getType222(payload) === "Array";
}
__name(isArray222, "isArray2");
function isPlainObject222(payload) {
  if (getType222(payload) !== "Object")
    return false;
  const prototype = Object.getPrototypeOf(payload);
  return !!prototype && prototype.constructor === Object && prototype === Object.prototype;
}
__name(isPlainObject222, "isPlainObject2");
function assignProp22(carry, key, newVal, originalObject, includeNonenumerable) {
  const propType = {}.propertyIsEnumerable.call(originalObject, key) ? "enumerable" : "nonenumerable";
  if (propType === "enumerable")
    carry[key] = newVal;
  if (includeNonenumerable && propType === "nonenumerable") {
    Object.defineProperty(carry, key, {
      value: newVal,
      enumerable: false,
      writable: true,
      configurable: true
    });
  }
}
__name(assignProp22, "assignProp");
function copy22(target, options = {}) {
  if (isArray222(target)) {
    return target.map((item) => copy22(item, options));
  }
  if (!isPlainObject222(target)) {
    return target;
  }
  const props = Object.getOwnPropertyNames(target);
  const symbols = Object.getOwnPropertySymbols(target);
  return [...props, ...symbols].reduce((carry, key) => {
    if (key === "__proto__")
      return carry;
    if (isArray222(options.props) && !options.props.includes(key)) {
      return carry;
    }
    const val = target[key];
    const newVal = copy22(val, options);
    assignProp22(carry, key, newVal, target, options.nonenumerable);
    return carry;
  }, {});
}
__name(copy22, "copy");
var SuperJSON22 = class {
  static {
    __name(this, "SuperJSON");
  }
  constructor({ dedupe = false } = {}) {
    this.classRegistry = new ClassRegistry22();
    this.symbolRegistry = new Registry22((s) => s.description ?? "");
    this.customTransformerRegistry = new CustomTransformerRegistry22();
    this.allowedErrorProps = [];
    this.dedupe = dedupe;
  }
  serialize(object2) {
    const identities = /* @__PURE__ */ new Map();
    const output = walker22(object2, identities, this, this.dedupe);
    const res = {
      json: output.transformedValue
    };
    if (output.annotations) {
      res.meta = {
        ...res.meta,
        values: output.annotations
      };
    }
    const equalityAnnotations = generateReferentialEqualityAnnotations22(identities, this.dedupe);
    if (equalityAnnotations) {
      res.meta = {
        ...res.meta,
        referentialEqualities: equalityAnnotations
      };
    }
    if (res.meta)
      res.meta.v = 1;
    return res;
  }
  deserialize(payload, options) {
    const { json, meta } = payload;
    let result = options?.inPlace ? json : copy22(json);
    if (meta?.values) {
      result = applyValueAnnotations22(result, meta.values, meta.v ?? 0, this);
    }
    if (meta?.referentialEqualities) {
      result = applyReferentialEqualityAnnotations22(result, meta.referentialEqualities, meta.v ?? 0);
    }
    return result;
  }
  stringify(object2) {
    return JSON.stringify(this.serialize(object2));
  }
  parse(string2) {
    return this.deserialize(JSON.parse(string2), { inPlace: true });
  }
  registerClass(v, options) {
    this.classRegistry.register(v, options);
  }
  registerSymbol(v, identifier) {
    this.symbolRegistry.register(v, identifier);
  }
  registerCustom(transformer, name) {
    this.customTransformerRegistry.register({
      name,
      ...transformer
    });
  }
  allowErrorProps(...props) {
    this.allowedErrorProps.push(...props);
  }
};
SuperJSON22.defaultInstance = new SuperJSON22();
SuperJSON22.serialize = SuperJSON22.defaultInstance.serialize.bind(SuperJSON22.defaultInstance);
SuperJSON22.deserialize = SuperJSON22.defaultInstance.deserialize.bind(SuperJSON22.defaultInstance);
SuperJSON22.stringify = SuperJSON22.defaultInstance.stringify.bind(SuperJSON22.defaultInstance);
SuperJSON22.parse = SuperJSON22.defaultInstance.parse.bind(SuperJSON22.defaultInstance);
SuperJSON22.registerClass = SuperJSON22.defaultInstance.registerClass.bind(SuperJSON22.defaultInstance);
SuperJSON22.registerSymbol = SuperJSON22.defaultInstance.registerSymbol.bind(SuperJSON22.defaultInstance);
SuperJSON22.registerCustom = SuperJSON22.defaultInstance.registerCustom.bind(SuperJSON22.defaultInstance);
SuperJSON22.allowErrorProps = SuperJSON22.defaultInstance.allowErrorProps.bind(SuperJSON22.defaultInstance);
var dist_default22 = SuperJSON22;
var serialize22 = SuperJSON22.serialize;
var deserialize22 = SuperJSON22.deserialize;
var stringify23 = SuperJSON22.stringify;
var parse22 = SuperJSON22.parse;
var registerClass22 = SuperJSON22.registerClass;
var registerCustom22 = SuperJSON22.registerCustom;
var registerSymbol22 = SuperJSON22.registerSymbol;
var allowErrorProps22 = SuperJSON22.allowErrorProps;
function parseData22(formData) {
  const propsArray = [];
  if (!formData)
    return propsArray;
  const batchsIDs = [];
  for (const [key, value] of Array.from(formData.entries())) {
    if (key.startsWith("FILE_"))
      propsArray.push(value);
    else if (key.startsWith("FILES_")) {
      if (batchsIDs.includes(key))
        continue;
      batchsIDs.push(key);
      propsArray.push(formData.getAll(key));
    } else {
      propsArray.push(JSON.parse(decodeURI(value)));
    }
  }
  return propsArray;
}
__name(parseData22, "parseData");
function paramsFromURL22(url) {
  const params = url.searchParams.entries().toArray().map(([_, v]) => v);
  return params.map((param) => JSON.parse(decodeURIComponent(param)));
}
__name(paramsFromURL22, "paramsFromURL");
async function WrapRequestHandler22(context2, endpoint) {
  const isServerAction = context2.request.headers.get("x-server-action") === "true";
  if (!isServerAction) {
    return new Response("Not Found", { status: 404 });
  }
  const parsedData = context2.request.method === "GET" || context2.request.method === "HEAD" ? paramsFromURL22(new URL(context2.request.url)) : parseData22(context2.request.headers.get("content-type") ? await context2.request.formData() : void 0);
  const missingProps = endpoint.length - parsedData.length;
  for (let i = 0; i < missingProps; i++) {
    parsedData.push(void 0);
  }
  parsedData.push(context2);
  const result = await endpoint(...parsedData);
  switch (typeof result) {
    case "string":
    case "number":
    case "boolean":
    case "bigint":
      return new Response(dist_default22.stringify(result), {
        headers: {
          "Content-Type": "application/json",
          dataType: "json"
        }
      });
    case "undefined":
      return new Response(null, { status: 204 });
    case "object":
      if (result instanceof Response) {
        result.headers.set("dataType", "response");
        return result;
      } else if (result instanceof Blob) {
        const res = new Response(await result.arrayBuffer());
        res.headers.set("dataType", "blob");
        res.headers.set("Content-Type", result.type);
        return res;
      } else if (result instanceof File) {
        return new Response(await result.arrayBuffer(), {
          headers: {
            "Content-Type": result.type,
            dataType: "file",
            fileData: JSON.stringify({
              name: result.name,
              lastModified: result.lastModified
            })
          }
        });
      } else {
        return new Response(dist_default22.stringify(result), {
          headers: { "Content-Type": "application/json", dataType: "json" }
        });
      }
    default:
      throw new Error(`Unsupported return type from action: ${typeof result}`);
  }
}
__name(WrapRequestHandler22, "WrapRequestHandler");
var onRequest22 = /* @__PURE__ */ __name(async (context2) => {
  const method = context2.request.method;
  const options = {
    GET: typeof GET13 === "function" ? GET13 : void 0,
    POST: typeof POST13 === "function" ? POST13 : void 0,
    PUT: typeof PUT === "function" ? PUT : void 0,
    DELETE: typeof DELETE === "function" ? DELETE : void 0,
    PATCH: typeof PATCH === "function" ? PATCH : void 0,
    HEAD: typeof HEAD === "function" ? HEAD : void 0,
    OPTIONS: typeof OPTIONS === "function" ? OPTIONS : void 0
  };
  if (!options[method]) {
    return new Response(`Method "${method}" Not Allowed`, {
      status: 405
    });
  }
  return await WrapRequestHandler22(context2, options[method]);
}, "onRequest");

// api/themes/index.js
async function GET14() {
  const ctx = getContext(arguments);
  const { env: env2 } = ctx;
  const session = await ctx.data.client.getUserSession("private");
  if (session instanceof Error) {
    return {
      success: false,
      error: "Unauthorized",
      data: []
    };
  }
  const db = drizzle(env2.PROJECT_DB);
  const themes = await db.select({
    id: uiStyleTable.id,
    name: uiStyleTable.name,
    themeData: uiStyleTable.themeData
  }).from(uiStyleTable).where(ownerGroupConditions({
    user_group_ids: session?.private?.group_ids ?? [],
    ownerGroupIdColumn: uiStyleTable.owner_group_id,
    self_host: env2.SELF_HOSTED,
    otherEq: [eq(uiStyleTable.owner_id, session?.user_id)]
  }));
  return {
    success: true,
    data: themes
  };
}
__name(GET14, "GET");
async function POST14(params) {
  const ctx = getContext(arguments);
  const { env: env2 } = ctx;
  try {
    const { name, themeData } = params;
    if (!name || typeof name !== "string" || name.trim().length === 0) {
      return {
        success: false,
        error: "Invalid or missing theme name"
      };
    }
    if (!themeData || typeof themeData !== "object") {
      return {
        success: false,
        error: "Invalid or missing theme data"
      };
    }
    if (!themeData.primary) {
      return {
        success: false,
        error: "Primary color is required"
      };
    }
    const db = drizzle(env2.PROJECT_DB);
    const session = await ctx.data.client.getUserSession("private");
    if (session instanceof Error) {
      return {
        success: false,
        error: "Unauthorized"
      };
    }
    const existing = await db.select().from(uiStyleTable).where(and(eq(uiStyleTable.name, name.trim()), ownerGroupConditions({
      user_group_ids: session?.private?.group_ids ?? [],
      ownerGroupIdColumn: uiStyleTable.owner_group_id,
      self_host: env2.SELF_HOSTED,
      otherEq: [eq(uiStyleTable.owner_id, session.user_id)]
    }))).limit(1);
    if (existing.length > 0) {
      return {
        success: false,
        error: "A theme with this name already exists"
      };
    }
    return {
      success: true,
      data: (await db.insert(uiStyleTable).values({
        name: name.trim(),
        themeData,
        owner_id: session.user_id,
        owner_group_id: crypto.randomUUID()
      }).returning({
        id: uiStyleTable.id,
        name: uiStyleTable.name,
        themeData: uiStyleTable.themeData
      })).at(0)
    };
  } catch (err) {
    console.error("Error creating theme:", err);
    return {
      success: false,
      error: err instanceof Error ? err.message : "Failed to create theme"
    };
  }
}
__name(POST14, "POST");
var DoubleIndexedKV23 = class {
  static {
    __name(this, "DoubleIndexedKV");
  }
  constructor() {
    this.keyToValue = /* @__PURE__ */ new Map();
    this.valueToKey = /* @__PURE__ */ new Map();
  }
  set(key, value) {
    this.keyToValue.set(key, value);
    this.valueToKey.set(value, key);
  }
  getByKey(key) {
    return this.keyToValue.get(key);
  }
  getByValue(value) {
    return this.valueToKey.get(value);
  }
  clear() {
    this.keyToValue.clear();
    this.valueToKey.clear();
  }
};
var Registry23 = class {
  static {
    __name(this, "Registry");
  }
  constructor(generateIdentifier) {
    this.generateIdentifier = generateIdentifier;
    this.kv = new DoubleIndexedKV23();
  }
  register(value, identifier) {
    if (this.kv.getByValue(value)) {
      return;
    }
    if (!identifier) {
      identifier = this.generateIdentifier(value);
    }
    this.kv.set(identifier, value);
  }
  clear() {
    this.kv.clear();
  }
  getIdentifier(value) {
    return this.kv.getByValue(value);
  }
  getValue(identifier) {
    return this.kv.getByKey(identifier);
  }
};
var ClassRegistry23 = class extends Registry23 {
  static {
    __name(this, "ClassRegistry");
  }
  constructor() {
    super((c) => c.name);
    this.classToAllowedProps = /* @__PURE__ */ new Map();
  }
  register(value, options) {
    if (typeof options === "object") {
      if (options.allowProps) {
        this.classToAllowedProps.set(value, options.allowProps);
      }
      super.register(value, options.identifier);
    } else {
      super.register(value, options);
    }
  }
  getAllowedProps(value) {
    return this.classToAllowedProps.get(value);
  }
};
function valuesOfObj23(record) {
  if ("values" in Object) {
    return Object.values(record);
  }
  const values = [];
  for (const key in record) {
    if (record.hasOwnProperty(key)) {
      values.push(record[key]);
    }
  }
  return values;
}
__name(valuesOfObj23, "valuesOfObj");
function find23(record, predicate) {
  const values = valuesOfObj23(record);
  if ("find" in values) {
    return values.find(predicate);
  }
  const valuesNotNever = values;
  for (let i = 0; i < valuesNotNever.length; i++) {
    const value = valuesNotNever[i];
    if (predicate(value)) {
      return value;
    }
  }
  return;
}
__name(find23, "find");
function forEach23(record, run) {
  Object.entries(record).forEach(([key, value]) => run(value, key));
}
__name(forEach23, "forEach");
function includes23(arr, value) {
  return arr.indexOf(value) !== -1;
}
__name(includes23, "includes");
function findArr23(record, predicate) {
  for (let i = 0; i < record.length; i++) {
    const value = record[i];
    if (predicate(value)) {
      return value;
    }
  }
  return;
}
__name(findArr23, "findArr");
var CustomTransformerRegistry23 = class {
  static {
    __name(this, "CustomTransformerRegistry");
  }
  constructor() {
    this.transfomers = {};
  }
  register(transformer) {
    this.transfomers[transformer.name] = transformer;
  }
  findApplicable(v) {
    return find23(this.transfomers, (transformer) => transformer.isApplicable(v));
  }
  findByName(name) {
    return this.transfomers[name];
  }
};
var getType32 = /* @__PURE__ */ __name((payload) => Object.prototype.toString.call(payload).slice(8, -1), "getType");
var isUndefined23 = /* @__PURE__ */ __name((payload) => typeof payload === "undefined", "isUndefined");
var isNull24 = /* @__PURE__ */ __name((payload) => payload === null, "isNull");
var isPlainObject32 = /* @__PURE__ */ __name((payload) => {
  if (typeof payload !== "object" || payload === null)
    return false;
  if (payload === Object.prototype)
    return false;
  if (Object.getPrototypeOf(payload) === null)
    return true;
  return Object.getPrototypeOf(payload) === Object.prototype;
}, "isPlainObject");
var isEmptyObject23 = /* @__PURE__ */ __name((payload) => isPlainObject32(payload) && Object.keys(payload).length === 0, "isEmptyObject");
var isArray32 = /* @__PURE__ */ __name((payload) => Array.isArray(payload), "isArray");
var isString23 = /* @__PURE__ */ __name((payload) => typeof payload === "string", "isString");
var isNumber23 = /* @__PURE__ */ __name((payload) => typeof payload === "number" && !isNaN(payload), "isNumber");
var isBoolean23 = /* @__PURE__ */ __name((payload) => typeof payload === "boolean", "isBoolean");
var isRegExp23 = /* @__PURE__ */ __name((payload) => payload instanceof RegExp, "isRegExp");
var isMap23 = /* @__PURE__ */ __name((payload) => payload instanceof Map, "isMap");
var isSet23 = /* @__PURE__ */ __name((payload) => payload instanceof Set, "isSet");
var isSymbol23 = /* @__PURE__ */ __name((payload) => getType32(payload) === "Symbol", "isSymbol");
var isDate23 = /* @__PURE__ */ __name((payload) => payload instanceof Date && !isNaN(payload.valueOf()), "isDate");
var isError23 = /* @__PURE__ */ __name((payload) => payload instanceof Error, "isError");
var isNaNValue23 = /* @__PURE__ */ __name((payload) => typeof payload === "number" && isNaN(payload), "isNaNValue");
var isPrimitive23 = /* @__PURE__ */ __name((payload) => isBoolean23(payload) || isNull24(payload) || isUndefined23(payload) || isNumber23(payload) || isString23(payload) || isSymbol23(payload), "isPrimitive");
var isBigint23 = /* @__PURE__ */ __name((payload) => typeof payload === "bigint", "isBigint");
var isInfinite23 = /* @__PURE__ */ __name((payload) => payload === Infinity || payload === -Infinity, "isInfinite");
var isTypedArray23 = /* @__PURE__ */ __name((payload) => ArrayBuffer.isView(payload) && !(payload instanceof DataView), "isTypedArray");
var isURL23 = /* @__PURE__ */ __name((payload) => payload instanceof URL, "isURL");
var escapeKey23 = /* @__PURE__ */ __name((key) => key.replace(/\\/g, "\\\\").replace(/\./g, "\\."), "escapeKey");
var stringifyPath23 = /* @__PURE__ */ __name((path) => path.map(String).map(escapeKey23).join("."), "stringifyPath");
var parsePath23 = /* @__PURE__ */ __name((string2, legacyPaths) => {
  const result = [];
  let segment = "";
  for (let i = 0; i < string2.length; i++) {
    let char = string2.charAt(i);
    if (!legacyPaths && char === "\\") {
      const escaped = string2.charAt(i + 1);
      if (escaped === "\\") {
        segment += "\\";
        i++;
        continue;
      } else if (escaped !== ".") {
        throw Error("invalid path");
      }
    }
    const isEscapedDot = char === "\\" && string2.charAt(i + 1) === ".";
    if (isEscapedDot) {
      segment += ".";
      i++;
      continue;
    }
    const isEndOfSegment = char === ".";
    if (isEndOfSegment) {
      result.push(segment);
      segment = "";
      continue;
    }
    segment += char;
  }
  const lastSegment = segment;
  result.push(lastSegment);
  return result;
}, "parsePath");
function simpleTransformation23(isApplicable, annotation, transform, untransform) {
  return {
    isApplicable,
    annotation,
    transform,
    untransform
  };
}
__name(simpleTransformation23, "simpleTransformation");
var simpleRules23 = [
  simpleTransformation23(isUndefined23, "undefined", () => null, () => {
    return;
  }),
  simpleTransformation23(isBigint23, "bigint", (v) => v.toString(), (v) => {
    if (typeof BigInt !== "undefined") {
      return BigInt(v);
    }
    console.error("Please add a BigInt polyfill.");
    return v;
  }),
  simpleTransformation23(isDate23, "Date", (v) => v.toISOString(), (v) => new Date(v)),
  simpleTransformation23(isError23, "Error", (v, superJson) => {
    const baseError = {
      name: v.name,
      message: v.message
    };
    if ("cause" in v) {
      baseError.cause = v.cause;
    }
    superJson.allowedErrorProps.forEach((prop) => {
      baseError[prop] = v[prop];
    });
    return baseError;
  }, (v, superJson) => {
    const e = new Error(v.message, { cause: v.cause });
    e.name = v.name;
    e.stack = v.stack;
    superJson.allowedErrorProps.forEach((prop) => {
      e[prop] = v[prop];
    });
    return e;
  }),
  simpleTransformation23(isRegExp23, "regexp", (v) => "" + v, (regex) => {
    const body = regex.slice(1, regex.lastIndexOf("/"));
    const flags2 = regex.slice(regex.lastIndexOf("/") + 1);
    return new RegExp(body, flags2);
  }),
  simpleTransformation23(isSet23, "set", (v) => [...v.values()], (v) => new Set(v)),
  simpleTransformation23(isMap23, "map", (v) => [...v.entries()], (v) => new Map(v)),
  simpleTransformation23((v) => isNaNValue23(v) || isInfinite23(v), "number", (v) => {
    if (isNaNValue23(v)) {
      return "NaN";
    }
    if (v > 0) {
      return "Infinity";
    } else {
      return "-Infinity";
    }
  }, Number),
  simpleTransformation23((v) => v === 0 && 1 / v === -Infinity, "number", () => {
    return "-0";
  }, Number),
  simpleTransformation23(isURL23, "URL", (v) => v.toString(), (v) => new URL(v))
];
function compositeTransformation23(isApplicable, annotation, transform, untransform) {
  return {
    isApplicable,
    annotation,
    transform,
    untransform
  };
}
__name(compositeTransformation23, "compositeTransformation");
var symbolRule23 = compositeTransformation23((s, superJson) => {
  if (isSymbol23(s)) {
    const isRegistered = !!superJson.symbolRegistry.getIdentifier(s);
    return isRegistered;
  }
  return false;
}, (s, superJson) => {
  const identifier = superJson.symbolRegistry.getIdentifier(s);
  return ["symbol", identifier];
}, (v) => v.description, (_, a, superJson) => {
  const value = superJson.symbolRegistry.getValue(a[1]);
  if (!value) {
    throw new Error("Trying to deserialize unknown symbol");
  }
  return value;
});
var constructorToName23 = [
  Int8Array,
  Uint8Array,
  Int16Array,
  Uint16Array,
  Int32Array,
  Uint32Array,
  Float32Array,
  Float64Array,
  Uint8ClampedArray
].reduce((obj, ctor) => {
  obj[ctor.name] = ctor;
  return obj;
}, {});
var typedArrayRule23 = compositeTransformation23(isTypedArray23, (v) => ["typed-array", v.constructor.name], (v) => [...v], (v, a) => {
  const ctor = constructorToName23[a[1]];
  if (!ctor) {
    throw new Error("Trying to deserialize unknown typed array");
  }
  return new ctor(v);
});
function isInstanceOfRegisteredClass23(potentialClass, superJson) {
  if (potentialClass?.constructor) {
    const isRegistered = !!superJson.classRegistry.getIdentifier(potentialClass.constructor);
    return isRegistered;
  }
  return false;
}
__name(isInstanceOfRegisteredClass23, "isInstanceOfRegisteredClass");
var classRule23 = compositeTransformation23(isInstanceOfRegisteredClass23, (clazz, superJson) => {
  const identifier = superJson.classRegistry.getIdentifier(clazz.constructor);
  return ["class", identifier];
}, (clazz, superJson) => {
  const allowedProps = superJson.classRegistry.getAllowedProps(clazz.constructor);
  if (!allowedProps) {
    return { ...clazz };
  }
  const result = {};
  allowedProps.forEach((prop) => {
    result[prop] = clazz[prop];
  });
  return result;
}, (v, a, superJson) => {
  const clazz = superJson.classRegistry.getValue(a[1]);
  if (!clazz) {
    throw new Error(`Trying to deserialize unknown class '${a[1]}' - check https://github.com/blitz-js/superjson/issues/116#issuecomment-773996564`);
  }
  return Object.assign(Object.create(clazz.prototype), v);
});
var customRule23 = compositeTransformation23((value, superJson) => {
  return !!superJson.customTransformerRegistry.findApplicable(value);
}, (value, superJson) => {
  const transformer = superJson.customTransformerRegistry.findApplicable(value);
  return ["custom", transformer.name];
}, (value, superJson) => {
  const transformer = superJson.customTransformerRegistry.findApplicable(value);
  return transformer.serialize(value);
}, (v, a, superJson) => {
  const transformer = superJson.customTransformerRegistry.findByName(a[1]);
  if (!transformer) {
    throw new Error("Trying to deserialize unknown custom value");
  }
  return transformer.deserialize(v);
});
var compositeRules23 = [classRule23, symbolRule23, customRule23, typedArrayRule23];
var transformValue23 = /* @__PURE__ */ __name((value, superJson) => {
  const applicableCompositeRule = findArr23(compositeRules23, (rule) => rule.isApplicable(value, superJson));
  if (applicableCompositeRule) {
    return {
      value: applicableCompositeRule.transform(value, superJson),
      type: applicableCompositeRule.annotation(value, superJson)
    };
  }
  const applicableSimpleRule = findArr23(simpleRules23, (rule) => rule.isApplicable(value, superJson));
  if (applicableSimpleRule) {
    return {
      value: applicableSimpleRule.transform(value, superJson),
      type: applicableSimpleRule.annotation
    };
  }
  return;
}, "transformValue");
var simpleRulesByAnnotation23 = {};
simpleRules23.forEach((rule) => {
  simpleRulesByAnnotation23[rule.annotation] = rule;
});
var untransformValue23 = /* @__PURE__ */ __name((json, type, superJson) => {
  if (isArray32(type)) {
    switch (type[0]) {
      case "symbol":
        return symbolRule23.untransform(json, type, superJson);
      case "class":
        return classRule23.untransform(json, type, superJson);
      case "custom":
        return customRule23.untransform(json, type, superJson);
      case "typed-array":
        return typedArrayRule23.untransform(json, type, superJson);
      default:
        throw new Error("Unknown transformation: " + type);
    }
  } else {
    const transformation = simpleRulesByAnnotation23[type];
    if (!transformation) {
      throw new Error("Unknown transformation: " + type);
    }
    return transformation.untransform(json, superJson);
  }
}, "untransformValue");
var getNthKey23 = /* @__PURE__ */ __name((value, n) => {
  if (n > value.size)
    throw new Error("index out of bounds");
  const keys = value.keys();
  while (n > 0) {
    keys.next();
    n--;
  }
  return keys.next().value;
}, "getNthKey");
function validatePath23(path) {
  if (includes23(path, "__proto__")) {
    throw new Error("__proto__ is not allowed as a property");
  }
  if (includes23(path, "prototype")) {
    throw new Error("prototype is not allowed as a property");
  }
  if (includes23(path, "constructor")) {
    throw new Error("constructor is not allowed as a property");
  }
}
__name(validatePath23, "validatePath");
var getDeep23 = /* @__PURE__ */ __name((object2, path) => {
  validatePath23(path);
  for (let i = 0; i < path.length; i++) {
    const key = path[i];
    if (isSet23(object2)) {
      object2 = getNthKey23(object2, +key);
    } else if (isMap23(object2)) {
      const row = +key;
      const type = +path[++i] === 0 ? "key" : "value";
      const keyOfRow = getNthKey23(object2, row);
      switch (type) {
        case "key":
          object2 = keyOfRow;
          break;
        case "value":
          object2 = object2.get(keyOfRow);
          break;
      }
    } else {
      object2 = object2[key];
    }
  }
  return object2;
}, "getDeep");
var setDeep23 = /* @__PURE__ */ __name((object2, path, mapper) => {
  validatePath23(path);
  if (path.length === 0) {
    return mapper(object2);
  }
  let parent = object2;
  for (let i = 0; i < path.length - 1; i++) {
    const key = path[i];
    if (isArray32(parent)) {
      const index = +key;
      parent = parent[index];
    } else if (isPlainObject32(parent)) {
      parent = parent[key];
    } else if (isSet23(parent)) {
      const row = +key;
      parent = getNthKey23(parent, row);
    } else if (isMap23(parent)) {
      const isEnd = i === path.length - 2;
      if (isEnd) {
        break;
      }
      const row = +key;
      const type = +path[++i] === 0 ? "key" : "value";
      const keyOfRow = getNthKey23(parent, row);
      switch (type) {
        case "key":
          parent = keyOfRow;
          break;
        case "value":
          parent = parent.get(keyOfRow);
          break;
      }
    }
  }
  const lastKey = path[path.length - 1];
  if (isArray32(parent)) {
    parent[+lastKey] = mapper(parent[+lastKey]);
  } else if (isPlainObject32(parent)) {
    parent[lastKey] = mapper(parent[lastKey]);
  }
  if (isSet23(parent)) {
    const oldValue = getNthKey23(parent, +lastKey);
    const newValue = mapper(oldValue);
    if (oldValue !== newValue) {
      parent.delete(oldValue);
      parent.add(newValue);
    }
  }
  if (isMap23(parent)) {
    const row = +path[path.length - 2];
    const keyToRow = getNthKey23(parent, row);
    const type = +lastKey === 0 ? "key" : "value";
    switch (type) {
      case "key": {
        const newKey = mapper(keyToRow);
        parent.set(newKey, parent.get(keyToRow));
        if (newKey !== keyToRow) {
          parent.delete(keyToRow);
        }
        break;
      }
      case "value": {
        parent.set(keyToRow, mapper(parent.get(keyToRow)));
        break;
      }
    }
  }
  return object2;
}, "setDeep");
var enableLegacyPaths23 = /* @__PURE__ */ __name((version4) => version4 < 1, "enableLegacyPaths");
function traverse23(tree, walker26, version4, origin = []) {
  if (!tree) {
    return;
  }
  const legacyPaths = enableLegacyPaths23(version4);
  if (!isArray32(tree)) {
    forEach23(tree, (subtree, key) => traverse23(subtree, walker26, version4, [
      ...origin,
      ...parsePath23(key, legacyPaths)
    ]));
    return;
  }
  const [nodeValue, children] = tree;
  if (children) {
    forEach23(children, (child, key) => {
      traverse23(child, walker26, version4, [
        ...origin,
        ...parsePath23(key, legacyPaths)
      ]);
    });
  }
  walker26(nodeValue, origin);
}
__name(traverse23, "traverse");
function applyValueAnnotations23(plain, annotations, version4, superJson) {
  traverse23(annotations, (type, path) => {
    plain = setDeep23(plain, path, (v) => untransformValue23(v, type, superJson));
  }, version4);
  return plain;
}
__name(applyValueAnnotations23, "applyValueAnnotations");
function applyReferentialEqualityAnnotations23(plain, annotations, version4) {
  const legacyPaths = enableLegacyPaths23(version4);
  function apply(identicalPaths, path) {
    const object2 = getDeep23(plain, parsePath23(path, legacyPaths));
    identicalPaths.map((path2) => parsePath23(path2, legacyPaths)).forEach((identicalObjectPath) => {
      plain = setDeep23(plain, identicalObjectPath, () => object2);
    });
  }
  __name(apply, "apply");
  if (isArray32(annotations)) {
    const [root, other] = annotations;
    root.forEach((identicalPath) => {
      plain = setDeep23(plain, parsePath23(identicalPath, legacyPaths), () => plain);
    });
    if (other) {
      forEach23(other, apply);
    }
  } else {
    forEach23(annotations, apply);
  }
  return plain;
}
__name(applyReferentialEqualityAnnotations23, "applyReferentialEqualityAnnotations");
var isDeep23 = /* @__PURE__ */ __name((object2, superJson) => isPlainObject32(object2) || isArray32(object2) || isMap23(object2) || isSet23(object2) || isError23(object2) || isInstanceOfRegisteredClass23(object2, superJson), "isDeep");
function addIdentity23(object2, path, identities) {
  const existingSet = identities.get(object2);
  if (existingSet) {
    existingSet.push(path);
  } else {
    identities.set(object2, [path]);
  }
}
__name(addIdentity23, "addIdentity");
function generateReferentialEqualityAnnotations23(identitites, dedupe) {
  const result = {};
  let rootEqualityPaths = void 0;
  identitites.forEach((paths) => {
    if (paths.length <= 1) {
      return;
    }
    if (!dedupe) {
      paths = paths.map((path) => path.map(String)).sort((a, b) => a.length - b.length);
    }
    const [representativePath, ...identicalPaths] = paths;
    if (representativePath.length === 0) {
      rootEqualityPaths = identicalPaths.map(stringifyPath23);
    } else {
      result[stringifyPath23(representativePath)] = identicalPaths.map(stringifyPath23);
    }
  });
  if (rootEqualityPaths) {
    if (isEmptyObject23(result)) {
      return [rootEqualityPaths];
    } else {
      return [rootEqualityPaths, result];
    }
  } else {
    return isEmptyObject23(result) ? void 0 : result;
  }
}
__name(generateReferentialEqualityAnnotations23, "generateReferentialEqualityAnnotations");
var walker23 = /* @__PURE__ */ __name((object2, identities, superJson, dedupe, path = [], objectsInThisPath = [], seenObjects = /* @__PURE__ */ new Map()) => {
  const primitive = isPrimitive23(object2);
  if (!primitive) {
    addIdentity23(object2, path, identities);
    const seen = seenObjects.get(object2);
    if (seen) {
      return dedupe ? {
        transformedValue: null
      } : seen;
    }
  }
  if (!isDeep23(object2, superJson)) {
    const transformed2 = transformValue23(object2, superJson);
    const result2 = transformed2 ? {
      transformedValue: transformed2.value,
      annotations: [transformed2.type]
    } : {
      transformedValue: object2
    };
    if (!primitive) {
      seenObjects.set(object2, result2);
    }
    return result2;
  }
  if (includes23(objectsInThisPath, object2)) {
    return {
      transformedValue: null
    };
  }
  const transformationResult = transformValue23(object2, superJson);
  const transformed = transformationResult?.value ?? object2;
  const transformedValue = isArray32(transformed) ? [] : {};
  const innerAnnotations = {};
  forEach23(transformed, (value, index) => {
    if (index === "__proto__" || index === "constructor" || index === "prototype") {
      throw new Error(`Detected property ${index}. This is a prototype pollution risk, please remove it from your object.`);
    }
    const recursiveResult = walker23(value, identities, superJson, dedupe, [...path, index], [...objectsInThisPath, object2], seenObjects);
    transformedValue[index] = recursiveResult.transformedValue;
    if (isArray32(recursiveResult.annotations)) {
      innerAnnotations[escapeKey23(index)] = recursiveResult.annotations;
    } else if (isPlainObject32(recursiveResult.annotations)) {
      forEach23(recursiveResult.annotations, (tree, key) => {
        innerAnnotations[escapeKey23(index) + "." + key] = tree;
      });
    }
  });
  const result = isEmptyObject23(innerAnnotations) ? {
    transformedValue,
    annotations: transformationResult ? [transformationResult.type] : void 0
  } : {
    transformedValue,
    annotations: transformationResult ? [transformationResult.type, innerAnnotations] : innerAnnotations
  };
  if (!primitive) {
    seenObjects.set(object2, result);
  }
  return result;
}, "walker");
function getType223(payload) {
  return Object.prototype.toString.call(payload).slice(8, -1);
}
__name(getType223, "getType2");
function isArray223(payload) {
  return getType223(payload) === "Array";
}
__name(isArray223, "isArray2");
function isPlainObject223(payload) {
  if (getType223(payload) !== "Object")
    return false;
  const prototype = Object.getPrototypeOf(payload);
  return !!prototype && prototype.constructor === Object && prototype === Object.prototype;
}
__name(isPlainObject223, "isPlainObject2");
function assignProp23(carry, key, newVal, originalObject, includeNonenumerable) {
  const propType = {}.propertyIsEnumerable.call(originalObject, key) ? "enumerable" : "nonenumerable";
  if (propType === "enumerable")
    carry[key] = newVal;
  if (includeNonenumerable && propType === "nonenumerable") {
    Object.defineProperty(carry, key, {
      value: newVal,
      enumerable: false,
      writable: true,
      configurable: true
    });
  }
}
__name(assignProp23, "assignProp");
function copy23(target, options = {}) {
  if (isArray223(target)) {
    return target.map((item) => copy23(item, options));
  }
  if (!isPlainObject223(target)) {
    return target;
  }
  const props = Object.getOwnPropertyNames(target);
  const symbols = Object.getOwnPropertySymbols(target);
  return [...props, ...symbols].reduce((carry, key) => {
    if (key === "__proto__")
      return carry;
    if (isArray223(options.props) && !options.props.includes(key)) {
      return carry;
    }
    const val = target[key];
    const newVal = copy23(val, options);
    assignProp23(carry, key, newVal, target, options.nonenumerable);
    return carry;
  }, {});
}
__name(copy23, "copy");
var SuperJSON23 = class {
  static {
    __name(this, "SuperJSON");
  }
  constructor({ dedupe = false } = {}) {
    this.classRegistry = new ClassRegistry23();
    this.symbolRegistry = new Registry23((s) => s.description ?? "");
    this.customTransformerRegistry = new CustomTransformerRegistry23();
    this.allowedErrorProps = [];
    this.dedupe = dedupe;
  }
  serialize(object2) {
    const identities = /* @__PURE__ */ new Map();
    const output = walker23(object2, identities, this, this.dedupe);
    const res = {
      json: output.transformedValue
    };
    if (output.annotations) {
      res.meta = {
        ...res.meta,
        values: output.annotations
      };
    }
    const equalityAnnotations = generateReferentialEqualityAnnotations23(identities, this.dedupe);
    if (equalityAnnotations) {
      res.meta = {
        ...res.meta,
        referentialEqualities: equalityAnnotations
      };
    }
    if (res.meta)
      res.meta.v = 1;
    return res;
  }
  deserialize(payload, options) {
    const { json, meta } = payload;
    let result = options?.inPlace ? json : copy23(json);
    if (meta?.values) {
      result = applyValueAnnotations23(result, meta.values, meta.v ?? 0, this);
    }
    if (meta?.referentialEqualities) {
      result = applyReferentialEqualityAnnotations23(result, meta.referentialEqualities, meta.v ?? 0);
    }
    return result;
  }
  stringify(object2) {
    return JSON.stringify(this.serialize(object2));
  }
  parse(string2) {
    return this.deserialize(JSON.parse(string2), { inPlace: true });
  }
  registerClass(v, options) {
    this.classRegistry.register(v, options);
  }
  registerSymbol(v, identifier) {
    this.symbolRegistry.register(v, identifier);
  }
  registerCustom(transformer, name) {
    this.customTransformerRegistry.register({
      name,
      ...transformer
    });
  }
  allowErrorProps(...props) {
    this.allowedErrorProps.push(...props);
  }
};
SuperJSON23.defaultInstance = new SuperJSON23();
SuperJSON23.serialize = SuperJSON23.defaultInstance.serialize.bind(SuperJSON23.defaultInstance);
SuperJSON23.deserialize = SuperJSON23.defaultInstance.deserialize.bind(SuperJSON23.defaultInstance);
SuperJSON23.stringify = SuperJSON23.defaultInstance.stringify.bind(SuperJSON23.defaultInstance);
SuperJSON23.parse = SuperJSON23.defaultInstance.parse.bind(SuperJSON23.defaultInstance);
SuperJSON23.registerClass = SuperJSON23.defaultInstance.registerClass.bind(SuperJSON23.defaultInstance);
SuperJSON23.registerSymbol = SuperJSON23.defaultInstance.registerSymbol.bind(SuperJSON23.defaultInstance);
SuperJSON23.registerCustom = SuperJSON23.defaultInstance.registerCustom.bind(SuperJSON23.defaultInstance);
SuperJSON23.allowErrorProps = SuperJSON23.defaultInstance.allowErrorProps.bind(SuperJSON23.defaultInstance);
var dist_default23 = SuperJSON23;
var serialize23 = SuperJSON23.serialize;
var deserialize23 = SuperJSON23.deserialize;
var stringify24 = SuperJSON23.stringify;
var parse23 = SuperJSON23.parse;
var registerClass23 = SuperJSON23.registerClass;
var registerCustom23 = SuperJSON23.registerCustom;
var registerSymbol23 = SuperJSON23.registerSymbol;
var allowErrorProps23 = SuperJSON23.allowErrorProps;
function parseData23(formData) {
  const propsArray = [];
  if (!formData)
    return propsArray;
  const batchsIDs = [];
  for (const [key, value] of Array.from(formData.entries())) {
    if (key.startsWith("FILE_"))
      propsArray.push(value);
    else if (key.startsWith("FILES_")) {
      if (batchsIDs.includes(key))
        continue;
      batchsIDs.push(key);
      propsArray.push(formData.getAll(key));
    } else {
      propsArray.push(JSON.parse(decodeURI(value)));
    }
  }
  return propsArray;
}
__name(parseData23, "parseData");
function paramsFromURL23(url) {
  const params = url.searchParams.entries().toArray().map(([_, v]) => v);
  return params.map((param) => JSON.parse(decodeURIComponent(param)));
}
__name(paramsFromURL23, "paramsFromURL");
async function WrapRequestHandler23(context2, endpoint) {
  const isServerAction = context2.request.headers.get("x-server-action") === "true";
  if (!isServerAction) {
    return new Response("Not Found", { status: 404 });
  }
  const parsedData = context2.request.method === "GET" || context2.request.method === "HEAD" ? paramsFromURL23(new URL(context2.request.url)) : parseData23(context2.request.headers.get("content-type") ? await context2.request.formData() : void 0);
  const missingProps = endpoint.length - parsedData.length;
  for (let i = 0; i < missingProps; i++) {
    parsedData.push(void 0);
  }
  parsedData.push(context2);
  const result = await endpoint(...parsedData);
  switch (typeof result) {
    case "string":
    case "number":
    case "boolean":
    case "bigint":
      return new Response(dist_default23.stringify(result), {
        headers: {
          "Content-Type": "application/json",
          dataType: "json"
        }
      });
    case "undefined":
      return new Response(null, { status: 204 });
    case "object":
      if (result instanceof Response) {
        result.headers.set("dataType", "response");
        return result;
      } else if (result instanceof Blob) {
        const res = new Response(await result.arrayBuffer());
        res.headers.set("dataType", "blob");
        res.headers.set("Content-Type", result.type);
        return res;
      } else if (result instanceof File) {
        return new Response(await result.arrayBuffer(), {
          headers: {
            "Content-Type": result.type,
            dataType: "file",
            fileData: JSON.stringify({
              name: result.name,
              lastModified: result.lastModified
            })
          }
        });
      } else {
        return new Response(dist_default23.stringify(result), {
          headers: { "Content-Type": "application/json", dataType: "json" }
        });
      }
    default:
      throw new Error(`Unsupported return type from action: ${typeof result}`);
  }
}
__name(WrapRequestHandler23, "WrapRequestHandler");
var onRequest23 = /* @__PURE__ */ __name(async (context2) => {
  const method = context2.request.method;
  const options = {
    GET: typeof GET14 === "function" ? GET14 : void 0,
    POST: typeof POST14 === "function" ? POST14 : void 0,
    PUT: typeof PUT === "function" ? PUT : void 0,
    DELETE: typeof DELETE === "function" ? DELETE : void 0,
    PATCH: typeof PATCH === "function" ? PATCH : void 0,
    HEAD: typeof HEAD === "function" ? HEAD : void 0,
    OPTIONS: typeof OPTIONS === "function" ? OPTIONS : void 0
  };
  if (!options[method]) {
    return new Response(`Method "${method}" Not Allowed`, {
      status: 405
    });
  }
  return await WrapRequestHandler23(context2, options[method]);
}, "onRequest");

// api/users/index.js
var entityKind2 = /* @__PURE__ */ Symbol.for("drizzle:entityKind");
function is2(value, type) {
  if (!value || typeof value !== "object") {
    return false;
  }
  if (value instanceof type) {
    return true;
  }
  if (!Object.prototype.hasOwnProperty.call(type, entityKind2)) {
    throw new Error(`Class "${type.name ?? "<unknown>"}" doesn't look like a Drizzle entity. If this is incorrect and the class is provided by Drizzle, please report this as a bug.`);
  }
  let cls = Object.getPrototypeOf(value).constructor;
  if (cls) {
    while (cls) {
      if (entityKind2 in cls && cls[entityKind2] === type[entityKind2]) {
        return true;
      }
      cls = Object.getPrototypeOf(cls);
    }
  }
  return false;
}
__name(is2, "is");
var Column2 = class {
  static {
    __name(this, "Column");
  }
  constructor(table3, config2) {
    this.table = table3;
    this.config = config2;
    this.name = config2.name;
    this.keyAsName = config2.keyAsName;
    this.notNull = config2.notNull;
    this.default = config2.default;
    this.defaultFn = config2.defaultFn;
    this.onUpdateFn = config2.onUpdateFn;
    this.hasDefault = config2.hasDefault;
    this.primary = config2.primaryKey;
    this.isUnique = config2.isUnique;
    this.uniqueName = config2.uniqueName;
    this.uniqueType = config2.uniqueType;
    this.dataType = config2.dataType;
    this.columnType = config2.columnType;
    this.generated = config2.generated;
    this.generatedIdentity = config2.generatedIdentity;
  }
  static [entityKind2] = "Column";
  name;
  keyAsName;
  primary;
  notNull;
  default;
  defaultFn;
  onUpdateFn;
  hasDefault;
  isUnique;
  uniqueName;
  uniqueType;
  dataType;
  columnType;
  enumValues = void 0;
  generated = void 0;
  generatedIdentity = void 0;
  config;
  mapFromDriverValue(value) {
    return value;
  }
  mapToDriverValue(value) {
    return value;
  }
  shouldDisableInsert() {
    return this.config.generated !== void 0 && this.config.generated.type !== "byDefault";
  }
};
var ColumnBuilder2 = class {
  static {
    __name(this, "ColumnBuilder");
  }
  static [entityKind2] = "ColumnBuilder";
  config;
  constructor(name, dataType, columnType) {
    this.config = {
      name,
      keyAsName: name === "",
      notNull: false,
      default: void 0,
      hasDefault: false,
      primaryKey: false,
      isUnique: false,
      uniqueName: void 0,
      uniqueType: void 0,
      dataType,
      columnType,
      generated: void 0
    };
  }
  $type() {
    return this;
  }
  notNull() {
    this.config.notNull = true;
    return this;
  }
  default(value) {
    this.config.default = value;
    this.config.hasDefault = true;
    return this;
  }
  $defaultFn(fn) {
    this.config.defaultFn = fn;
    this.config.hasDefault = true;
    return this;
  }
  $default = this.$defaultFn;
  $onUpdateFn(fn) {
    this.config.onUpdateFn = fn;
    this.config.hasDefault = true;
    return this;
  }
  $onUpdate = this.$onUpdateFn;
  primaryKey() {
    this.config.primaryKey = true;
    this.config.notNull = true;
    return this;
  }
  setName(name) {
    if (this.config.name !== "")
      return;
    this.config.name = name;
  }
};
var TableName2 = /* @__PURE__ */ Symbol.for("drizzle:Name");
function iife2(fn, ...args) {
  return fn(...args);
}
__name(iife2, "iife");
function uniqueKeyName3(table3, columns) {
  return `${table3[TableName2]}_${columns.join("_")}_unique`;
}
__name(uniqueKeyName3, "uniqueKeyName");
var PgColumn2 = class extends Column2 {
  static {
    __name(this, "PgColumn");
  }
  constructor(table3, config2) {
    if (!config2.uniqueName) {
      config2.uniqueName = uniqueKeyName3(table3, [config2.name]);
    }
    super(table3, config2);
    this.table = table3;
  }
  static [entityKind2] = "PgColumn";
};
var ExtraConfigColumn2 = class extends PgColumn2 {
  static {
    __name(this, "ExtraConfigColumn");
  }
  static [entityKind2] = "ExtraConfigColumn";
  getSQLType() {
    return this.getSQLType();
  }
  indexConfig = {
    order: this.config.order ?? "asc",
    nulls: this.config.nulls ?? "last",
    opClass: this.config.opClass
  };
  defaultConfig = {
    order: "asc",
    nulls: "last",
    opClass: void 0
  };
  asc() {
    this.indexConfig.order = "asc";
    return this;
  }
  desc() {
    this.indexConfig.order = "desc";
    return this;
  }
  nullsFirst() {
    this.indexConfig.nulls = "first";
    return this;
  }
  nullsLast() {
    this.indexConfig.nulls = "last";
    return this;
  }
  op(opClass) {
    this.indexConfig.opClass = opClass;
    return this;
  }
};
var PgEnumObjectColumn2 = class extends PgColumn2 {
  static {
    __name(this, "PgEnumObjectColumn");
  }
  static [entityKind2] = "PgEnumObjectColumn";
  enum;
  enumValues = this.config.enum.enumValues;
  constructor(table3, config2) {
    super(table3, config2);
    this.enum = config2.enum;
  }
  getSQLType() {
    return this.enum.enumName;
  }
};
var isPgEnumSym2 = /* @__PURE__ */ Symbol.for("drizzle:isPgEnum");
function isPgEnum2(obj) {
  return !!obj && typeof obj === "function" && isPgEnumSym2 in obj && obj[isPgEnumSym2] === true;
}
__name(isPgEnum2, "isPgEnum");
var PgEnumColumn2 = class extends PgColumn2 {
  static {
    __name(this, "PgEnumColumn");
  }
  static [entityKind2] = "PgEnumColumn";
  enum = this.config.enum;
  enumValues = this.config.enum.enumValues;
  constructor(table3, config2) {
    super(table3, config2);
    this.enum = config2.enum;
  }
  getSQLType() {
    return this.enum.enumName;
  }
};
var Subquery2 = class {
  static {
    __name(this, "Subquery");
  }
  static [entityKind2] = "Subquery";
  constructor(sql3, fields, alias, isWith = false, usedTables = []) {
    this._ = {
      brand: "Subquery",
      sql: sql3,
      selectedFields: fields,
      alias,
      isWith,
      usedTables
    };
  }
};
var WithSubquery2 = class extends Subquery2 {
  static {
    __name(this, "WithSubquery");
  }
  static [entityKind2] = "WithSubquery";
};
var version3 = "0.45.1";
var otel2;
var rawTracer2;
var tracer2 = {
  startActiveSpan(name, fn) {
    if (!otel2) {
      return fn();
    }
    if (!rawTracer2) {
      rawTracer2 = otel2.trace.getTracer("drizzle-orm", version3);
    }
    return iife2((otel22, rawTracer22) => rawTracer22.startActiveSpan(name, (span) => {
      try {
        return fn(span);
      } catch (e) {
        span.setStatus({
          code: otel22.SpanStatusCode.ERROR,
          message: e instanceof Error ? e.message : "Unknown error"
        });
        throw e;
      } finally {
        span.end();
      }
    }), otel2, rawTracer2);
  }
};
var ViewBaseConfig2 = /* @__PURE__ */ Symbol.for("drizzle:ViewBaseConfig");
var Schema3 = /* @__PURE__ */ Symbol.for("drizzle:Schema");
var Columns2 = /* @__PURE__ */ Symbol.for("drizzle:Columns");
var ExtraConfigColumns2 = /* @__PURE__ */ Symbol.for("drizzle:ExtraConfigColumns");
var OriginalName2 = /* @__PURE__ */ Symbol.for("drizzle:OriginalName");
var BaseName2 = /* @__PURE__ */ Symbol.for("drizzle:BaseName");
var IsAlias2 = /* @__PURE__ */ Symbol.for("drizzle:IsAlias");
var ExtraConfigBuilder2 = /* @__PURE__ */ Symbol.for("drizzle:ExtraConfigBuilder");
var IsDrizzleTable2 = /* @__PURE__ */ Symbol.for("drizzle:IsDrizzleTable");
var Table2 = class {
  static {
    __name(this, "Table");
  }
  static [entityKind2] = "Table";
  static Symbol = {
    Name: TableName2,
    Schema: Schema3,
    OriginalName: OriginalName2,
    Columns: Columns2,
    ExtraConfigColumns: ExtraConfigColumns2,
    BaseName: BaseName2,
    IsAlias: IsAlias2,
    ExtraConfigBuilder: ExtraConfigBuilder2
  };
  [TableName2];
  [OriginalName2];
  [Schema3];
  [Columns2];
  [ExtraConfigColumns2];
  [BaseName2];
  [IsAlias2] = false;
  [IsDrizzleTable2] = true;
  [ExtraConfigBuilder2] = void 0;
  constructor(name, schema, baseName) {
    this[TableName2] = this[OriginalName2] = name;
    this[Schema3] = schema;
    this[BaseName2] = baseName;
  }
};
function getTableName2(table3) {
  return table3[TableName2];
}
__name(getTableName2, "getTableName");
function getTableUniqueName2(table3) {
  return `${table3[Schema3] ?? "public"}.${table3[TableName2]}`;
}
__name(getTableUniqueName2, "getTableUniqueName");
function isSQLWrapper2(value) {
  return value !== null && value !== void 0 && typeof value.getSQL === "function";
}
__name(isSQLWrapper2, "isSQLWrapper");
function mergeQueries2(queries) {
  const result = { sql: "", params: [] };
  for (const query of queries) {
    result.sql += query.sql;
    result.params.push(...query.params);
    if (query.typings?.length) {
      if (!result.typings) {
        result.typings = [];
      }
      result.typings.push(...query.typings);
    }
  }
  return result;
}
__name(mergeQueries2, "mergeQueries");
var StringChunk2 = class {
  static {
    __name(this, "StringChunk");
  }
  static [entityKind2] = "StringChunk";
  value;
  constructor(value) {
    this.value = Array.isArray(value) ? value : [value];
  }
  getSQL() {
    return new SQL2([this]);
  }
};
var SQL2 = class _SQL {
  static {
    __name(this, "SQL");
  }
  constructor(queryChunks) {
    this.queryChunks = queryChunks;
    for (const chunk of queryChunks) {
      if (is2(chunk, Table2)) {
        const schemaName = chunk[Table2.Symbol.Schema];
        this.usedTables.push(schemaName === void 0 ? chunk[Table2.Symbol.Name] : schemaName + "." + chunk[Table2.Symbol.Name]);
      }
    }
  }
  static [entityKind2] = "SQL";
  decoder = noopDecoder2;
  shouldInlineParams = false;
  usedTables = [];
  append(query) {
    this.queryChunks.push(...query.queryChunks);
    return this;
  }
  toQuery(config2) {
    return tracer2.startActiveSpan("drizzle.buildSQL", (span) => {
      const query = this.buildQueryFromSourceParams(this.queryChunks, config2);
      span?.setAttributes({
        "drizzle.query.text": query.sql,
        "drizzle.query.params": JSON.stringify(query.params)
      });
      return query;
    });
  }
  buildQueryFromSourceParams(chunks, _config) {
    const config2 = Object.assign({}, _config, {
      inlineParams: _config.inlineParams || this.shouldInlineParams,
      paramStartIndex: _config.paramStartIndex || { value: 0 }
    });
    const {
      casing,
      escapeName,
      escapeParam,
      prepareTyping,
      inlineParams,
      paramStartIndex
    } = config2;
    return mergeQueries2(chunks.map((chunk) => {
      if (is2(chunk, StringChunk2)) {
        return { sql: chunk.value.join(""), params: [] };
      }
      if (is2(chunk, Name2)) {
        return { sql: escapeName(chunk.value), params: [] };
      }
      if (chunk === void 0) {
        return { sql: "", params: [] };
      }
      if (Array.isArray(chunk)) {
        const result = [new StringChunk2("(")];
        for (const [i, p] of chunk.entries()) {
          result.push(p);
          if (i < chunk.length - 1) {
            result.push(new StringChunk2(", "));
          }
        }
        result.push(new StringChunk2(")"));
        return this.buildQueryFromSourceParams(result, config2);
      }
      if (is2(chunk, _SQL)) {
        return this.buildQueryFromSourceParams(chunk.queryChunks, {
          ...config2,
          inlineParams: inlineParams || chunk.shouldInlineParams
        });
      }
      if (is2(chunk, Table2)) {
        const schemaName = chunk[Table2.Symbol.Schema];
        const tableName = chunk[Table2.Symbol.Name];
        return {
          sql: schemaName === void 0 || chunk[IsAlias2] ? escapeName(tableName) : escapeName(schemaName) + "." + escapeName(tableName),
          params: []
        };
      }
      if (is2(chunk, Column2)) {
        const columnName = casing.getColumnCasing(chunk);
        if (_config.invokeSource === "indexes") {
          return { sql: escapeName(columnName), params: [] };
        }
        const schemaName = chunk.table[Table2.Symbol.Schema];
        return {
          sql: chunk.table[IsAlias2] || schemaName === void 0 ? escapeName(chunk.table[Table2.Symbol.Name]) + "." + escapeName(columnName) : escapeName(schemaName) + "." + escapeName(chunk.table[Table2.Symbol.Name]) + "." + escapeName(columnName),
          params: []
        };
      }
      if (is2(chunk, View2)) {
        const schemaName = chunk[ViewBaseConfig2].schema;
        const viewName = chunk[ViewBaseConfig2].name;
        return {
          sql: schemaName === void 0 || chunk[ViewBaseConfig2].isAlias ? escapeName(viewName) : escapeName(schemaName) + "." + escapeName(viewName),
          params: []
        };
      }
      if (is2(chunk, Param2)) {
        if (is2(chunk.value, Placeholder2)) {
          return { sql: escapeParam(paramStartIndex.value++, chunk), params: [chunk], typings: ["none"] };
        }
        const mappedValue = chunk.value === null ? null : chunk.encoder.mapToDriverValue(chunk.value);
        if (is2(mappedValue, _SQL)) {
          return this.buildQueryFromSourceParams([mappedValue], config2);
        }
        if (inlineParams) {
          return { sql: this.mapInlineParam(mappedValue, config2), params: [] };
        }
        let typings = ["none"];
        if (prepareTyping) {
          typings = [prepareTyping(chunk.encoder)];
        }
        return { sql: escapeParam(paramStartIndex.value++, mappedValue), params: [mappedValue], typings };
      }
      if (is2(chunk, Placeholder2)) {
        return { sql: escapeParam(paramStartIndex.value++, chunk), params: [chunk], typings: ["none"] };
      }
      if (is2(chunk, _SQL.Aliased) && chunk.fieldAlias !== void 0) {
        return { sql: escapeName(chunk.fieldAlias), params: [] };
      }
      if (is2(chunk, Subquery2)) {
        if (chunk._.isWith) {
          return { sql: escapeName(chunk._.alias), params: [] };
        }
        return this.buildQueryFromSourceParams([
          new StringChunk2("("),
          chunk._.sql,
          new StringChunk2(") "),
          new Name2(chunk._.alias)
        ], config2);
      }
      if (isPgEnum2(chunk)) {
        if (chunk.schema) {
          return { sql: escapeName(chunk.schema) + "." + escapeName(chunk.enumName), params: [] };
        }
        return { sql: escapeName(chunk.enumName), params: [] };
      }
      if (isSQLWrapper2(chunk)) {
        if (chunk.shouldOmitSQLParens?.()) {
          return this.buildQueryFromSourceParams([chunk.getSQL()], config2);
        }
        return this.buildQueryFromSourceParams([
          new StringChunk2("("),
          chunk.getSQL(),
          new StringChunk2(")")
        ], config2);
      }
      if (inlineParams) {
        return { sql: this.mapInlineParam(chunk, config2), params: [] };
      }
      return { sql: escapeParam(paramStartIndex.value++, chunk), params: [chunk], typings: ["none"] };
    }));
  }
  mapInlineParam(chunk, { escapeString: escapeString2 }) {
    if (chunk === null) {
      return "null";
    }
    if (typeof chunk === "number" || typeof chunk === "boolean") {
      return chunk.toString();
    }
    if (typeof chunk === "string") {
      return escapeString2(chunk);
    }
    if (typeof chunk === "object") {
      const mappedValueAsString = chunk.toString();
      if (mappedValueAsString === "[object Object]") {
        return escapeString2(JSON.stringify(chunk));
      }
      return escapeString2(mappedValueAsString);
    }
    throw new Error("Unexpected param value: " + chunk);
  }
  getSQL() {
    return this;
  }
  as(alias) {
    if (alias === void 0) {
      return this;
    }
    return new _SQL.Aliased(this, alias);
  }
  mapWith(decoder2) {
    this.decoder = typeof decoder2 === "function" ? { mapFromDriverValue: decoder2 } : decoder2;
    return this;
  }
  inlineParams() {
    this.shouldInlineParams = true;
    return this;
  }
  if(condition) {
    return condition ? this : void 0;
  }
};
var Name2 = class {
  static {
    __name(this, "Name");
  }
  constructor(value) {
    this.value = value;
  }
  static [entityKind2] = "Name";
  brand;
  getSQL() {
    return new SQL2([this]);
  }
};
function isDriverValueEncoder2(value) {
  return typeof value === "object" && value !== null && "mapToDriverValue" in value && typeof value.mapToDriverValue === "function";
}
__name(isDriverValueEncoder2, "isDriverValueEncoder");
var noopDecoder2 = {
  mapFromDriverValue: /* @__PURE__ */ __name((value) => value, "mapFromDriverValue")
};
var noopEncoder2 = {
  mapToDriverValue: /* @__PURE__ */ __name((value) => value, "mapToDriverValue")
};
var noopMapper2 = {
  ...noopDecoder2,
  ...noopEncoder2
};
var Param2 = class {
  static {
    __name(this, "Param");
  }
  constructor(value, encoder2 = noopEncoder2) {
    this.value = value;
    this.encoder = encoder2;
  }
  static [entityKind2] = "Param";
  brand;
  getSQL() {
    return new SQL2([this]);
  }
};
function sql2(strings, ...params) {
  const queryChunks = [];
  if (params.length > 0 || strings.length > 0 && strings[0] !== "") {
    queryChunks.push(new StringChunk2(strings[0]));
  }
  for (const [paramIndex, param2] of params.entries()) {
    queryChunks.push(param2, new StringChunk2(strings[paramIndex + 1]));
  }
  return new SQL2(queryChunks);
}
__name(sql2, "sql");
((sql22) => {
  function empty() {
    return new SQL2([]);
  }
  __name(empty, "empty");
  sql22.empty = empty;
  function fromList(list) {
    return new SQL2(list);
  }
  __name(fromList, "fromList");
  sql22.fromList = fromList;
  function raw(str) {
    return new SQL2([new StringChunk2(str)]);
  }
  __name(raw, "raw");
  sql22.raw = raw;
  function join(chunks, separator) {
    const result = [];
    for (const [i, chunk] of chunks.entries()) {
      if (i > 0 && separator !== void 0) {
        result.push(separator);
      }
      result.push(chunk);
    }
    return new SQL2(result);
  }
  __name(join, "join");
  sql22.join = join;
  function identifier(value) {
    return new Name2(value);
  }
  __name(identifier, "identifier");
  sql22.identifier = identifier;
  function placeholder2(name2) {
    return new Placeholder2(name2);
  }
  __name(placeholder2, "placeholder2");
  sql22.placeholder = placeholder2;
  function param2(value, encoder2) {
    return new Param2(value, encoder2);
  }
  __name(param2, "param2");
  sql22.param = param2;
})(sql2 || (sql2 = {}));
((SQL22) => {
  class Aliased {
    static {
      __name(this, "Aliased");
    }
    constructor(sql22, fieldAlias) {
      this.sql = sql22;
      this.fieldAlias = fieldAlias;
    }
    static [entityKind2] = "SQL.Aliased";
    isSelectionField = false;
    getSQL() {
      return this.sql;
    }
    clone() {
      return new Aliased(this.sql, this.fieldAlias);
    }
  }
  SQL22.Aliased = Aliased;
})(SQL2 || (SQL2 = {}));
var Placeholder2 = class {
  static {
    __name(this, "Placeholder");
  }
  constructor(name2) {
    this.name = name2;
  }
  static [entityKind2] = "Placeholder";
  getSQL() {
    return new SQL2([this]);
  }
};
function fillPlaceholders2(params, values) {
  return params.map((p) => {
    if (is2(p, Placeholder2)) {
      if (!(p.name in values)) {
        throw new Error(`No value for placeholder "${p.name}" was provided`);
      }
      return values[p.name];
    }
    if (is2(p, Param2) && is2(p.value, Placeholder2)) {
      if (!(p.value.name in values)) {
        throw new Error(`No value for placeholder "${p.value.name}" was provided`);
      }
      return p.encoder.mapToDriverValue(values[p.value.name]);
    }
    return p;
  });
}
__name(fillPlaceholders2, "fillPlaceholders");
var IsDrizzleView2 = /* @__PURE__ */ Symbol.for("drizzle:IsDrizzleView");
var View2 = class {
  static {
    __name(this, "View");
  }
  static [entityKind2] = "View";
  [ViewBaseConfig2];
  [IsDrizzleView2] = true;
  constructor({ name: name2, schema, selectedFields, query }) {
    this[ViewBaseConfig2] = {
      name: name2,
      originalName: name2,
      schema,
      selectedFields,
      query,
      isExisting: !query,
      isAlias: false
    };
  }
  getSQL() {
    return new SQL2([this]);
  }
};
Column2.prototype.getSQL = function() {
  return new SQL2([this]);
};
Table2.prototype.getSQL = function() {
  return new SQL2([this]);
};
Subquery2.prototype.getSQL = function() {
  return new SQL2([this]);
};
var ColumnAliasProxyHandler2 = class {
  static {
    __name(this, "ColumnAliasProxyHandler");
  }
  constructor(table3) {
    this.table = table3;
  }
  static [entityKind2] = "ColumnAliasProxyHandler";
  get(columnObj, prop) {
    if (prop === "table") {
      return this.table;
    }
    return columnObj[prop];
  }
};
var TableAliasProxyHandler2 = class {
  static {
    __name(this, "TableAliasProxyHandler");
  }
  constructor(alias, replaceOriginalName) {
    this.alias = alias;
    this.replaceOriginalName = replaceOriginalName;
  }
  static [entityKind2] = "TableAliasProxyHandler";
  get(target, prop) {
    if (prop === Table2.Symbol.IsAlias) {
      return true;
    }
    if (prop === Table2.Symbol.Name) {
      return this.alias;
    }
    if (this.replaceOriginalName && prop === Table2.Symbol.OriginalName) {
      return this.alias;
    }
    if (prop === ViewBaseConfig2) {
      return {
        ...target[ViewBaseConfig2],
        name: this.alias,
        isAlias: true
      };
    }
    if (prop === Table2.Symbol.Columns) {
      const columns = target[Table2.Symbol.Columns];
      if (!columns) {
        return columns;
      }
      const proxiedColumns = {};
      Object.keys(columns).map((key) => {
        proxiedColumns[key] = new Proxy(columns[key], new ColumnAliasProxyHandler2(new Proxy(target, this)));
      });
      return proxiedColumns;
    }
    const value = target[prop];
    if (is2(value, Column2)) {
      return new Proxy(value, new ColumnAliasProxyHandler2(new Proxy(target, this)));
    }
    return value;
  }
};
function aliasedTable2(table3, tableAlias) {
  return new Proxy(table3, new TableAliasProxyHandler2(tableAlias, false));
}
__name(aliasedTable2, "aliasedTable");
function aliasedTableColumn2(column, tableAlias) {
  return new Proxy(column, new ColumnAliasProxyHandler2(new Proxy(column.table, new TableAliasProxyHandler2(tableAlias, false))));
}
__name(aliasedTableColumn2, "aliasedTableColumn");
function mapColumnsInAliasedSQLToAlias2(query, alias) {
  return new SQL2.Aliased(mapColumnsInSQLToAlias2(query.sql, alias), query.fieldAlias);
}
__name(mapColumnsInAliasedSQLToAlias2, "mapColumnsInAliasedSQLToAlias");
function mapColumnsInSQLToAlias2(query, alias) {
  return sql2.join(query.queryChunks.map((c) => {
    if (is2(c, Column2)) {
      return aliasedTableColumn2(c, alias);
    }
    if (is2(c, SQL2)) {
      return mapColumnsInSQLToAlias2(c, alias);
    }
    if (is2(c, SQL2.Aliased)) {
      return mapColumnsInAliasedSQLToAlias2(c, alias);
    }
    return c;
  }));
}
__name(mapColumnsInSQLToAlias2, "mapColumnsInSQLToAlias");
var DrizzleError2 = class extends Error {
  static {
    __name(this, "DrizzleError");
  }
  static [entityKind2] = "DrizzleError";
  constructor({ message: message2, cause }) {
    super(message2);
    this.name = "DrizzleError";
    this.cause = cause;
  }
};
var DrizzleQueryError2 = class _DrizzleQueryError extends Error {
  static {
    __name(this, "DrizzleQueryError");
  }
  constructor(query, params, cause) {
    super(`Failed query: ${query}
params: ${params}`);
    this.query = query;
    this.params = params;
    this.cause = cause;
    Error.captureStackTrace(this, _DrizzleQueryError);
    if (cause)
      this.cause = cause;
  }
};
var TransactionRollbackError2 = class extends DrizzleError2 {
  static {
    __name(this, "TransactionRollbackError");
  }
  static [entityKind2] = "TransactionRollbackError";
  constructor() {
    super({ message: "Rollback" });
  }
};
var ConsoleLogWriter2 = class {
  static {
    __name(this, "ConsoleLogWriter");
  }
  static [entityKind2] = "ConsoleLogWriter";
  write(message2) {
    console.log(message2);
  }
};
var DefaultLogger2 = class {
  static {
    __name(this, "DefaultLogger");
  }
  static [entityKind2] = "DefaultLogger";
  writer;
  constructor(config2) {
    this.writer = config2?.writer ?? new ConsoleLogWriter2();
  }
  logQuery(query, params) {
    const stringifiedParams = params.map((p) => {
      try {
        return JSON.stringify(p);
      } catch {
        return String(p);
      }
    });
    const paramsStr = stringifiedParams.length ? ` -- params: [${stringifiedParams.join(", ")}]` : "";
    this.writer.write(`Query: ${query}${paramsStr}`);
  }
};
var NoopLogger2 = class {
  static {
    __name(this, "NoopLogger");
  }
  static [entityKind2] = "NoopLogger";
  logQuery() {
  }
};
var QueryPromise2 = class {
  static {
    __name(this, "QueryPromise");
  }
  static [entityKind2] = "QueryPromise";
  [Symbol.toStringTag] = "QueryPromise";
  catch(onRejected) {
    return this.then(void 0, onRejected);
  }
  finally(onFinally) {
    return this.then((value) => {
      onFinally?.();
      return value;
    }, (reason) => {
      onFinally?.();
      throw reason;
    });
  }
  then(onFulfilled, onRejected) {
    return this.execute().then(onFulfilled, onRejected);
  }
};
function mapResultRow2(columns, row, joinsNotNullableMap) {
  const nullifyMap = {};
  const result = columns.reduce((result2, { path, field }, columnIndex) => {
    let decoder2;
    if (is2(field, Column2)) {
      decoder2 = field;
    } else if (is2(field, SQL2)) {
      decoder2 = field.decoder;
    } else if (is2(field, Subquery2)) {
      decoder2 = field._.sql.decoder;
    } else {
      decoder2 = field.sql.decoder;
    }
    let node = result2;
    for (const [pathChunkIndex, pathChunk] of path.entries()) {
      if (pathChunkIndex < path.length - 1) {
        if (!(pathChunk in node)) {
          node[pathChunk] = {};
        }
        node = node[pathChunk];
      } else {
        const rawValue = row[columnIndex];
        const value = node[pathChunk] = rawValue === null ? null : decoder2.mapFromDriverValue(rawValue);
        if (joinsNotNullableMap && is2(field, Column2) && path.length === 2) {
          const objectName = path[0];
          if (!(objectName in nullifyMap)) {
            nullifyMap[objectName] = value === null ? getTableName2(field.table) : false;
          } else if (typeof nullifyMap[objectName] === "string" && nullifyMap[objectName] !== getTableName2(field.table)) {
            nullifyMap[objectName] = false;
          }
        }
      }
    }
    return result2;
  }, {});
  if (joinsNotNullableMap && Object.keys(nullifyMap).length > 0) {
    for (const [objectName, tableName] of Object.entries(nullifyMap)) {
      if (typeof tableName === "string" && !joinsNotNullableMap[tableName]) {
        result[objectName] = null;
      }
    }
  }
  return result;
}
__name(mapResultRow2, "mapResultRow");
function orderSelectedFields2(fields, pathPrefix) {
  return Object.entries(fields).reduce((result, [name, field]) => {
    if (typeof name !== "string") {
      return result;
    }
    const newPath = pathPrefix ? [...pathPrefix, name] : [name];
    if (is2(field, Column2) || is2(field, SQL2) || is2(field, SQL2.Aliased) || is2(field, Subquery2)) {
      result.push({ path: newPath, field });
    } else if (is2(field, Table2)) {
      result.push(...orderSelectedFields2(field[Table2.Symbol.Columns], newPath));
    } else {
      result.push(...orderSelectedFields2(field, newPath));
    }
    return result;
  }, []);
}
__name(orderSelectedFields2, "orderSelectedFields");
function haveSameKeys2(left, right) {
  const leftKeys = Object.keys(left);
  const rightKeys = Object.keys(right);
  if (leftKeys.length !== rightKeys.length) {
    return false;
  }
  for (const [index, key] of leftKeys.entries()) {
    if (key !== rightKeys[index]) {
      return false;
    }
  }
  return true;
}
__name(haveSameKeys2, "haveSameKeys");
function mapUpdateSet2(table3, values) {
  const entries = Object.entries(values).filter(([, value]) => value !== void 0).map(([key, value]) => {
    if (is2(value, SQL2) || is2(value, Column2)) {
      return [key, value];
    } else {
      return [key, new Param2(value, table3[Table2.Symbol.Columns][key])];
    }
  });
  if (entries.length === 0) {
    throw new Error("No values to set");
  }
  return Object.fromEntries(entries);
}
__name(mapUpdateSet2, "mapUpdateSet");
function applyMixins2(baseClass, extendedClasses) {
  for (const extendedClass of extendedClasses) {
    for (const name of Object.getOwnPropertyNames(extendedClass.prototype)) {
      if (name === "constructor")
        continue;
      Object.defineProperty(baseClass.prototype, name, Object.getOwnPropertyDescriptor(extendedClass.prototype, name) || /* @__PURE__ */ Object.create(null));
    }
  }
}
__name(applyMixins2, "applyMixins");
function getTableColumns2(table3) {
  return table3[Table2.Symbol.Columns];
}
__name(getTableColumns2, "getTableColumns");
function getTableLikeName2(table3) {
  return is2(table3, Subquery2) ? table3._.alias : is2(table3, View2) ? table3[ViewBaseConfig2].name : is2(table3, SQL2) ? void 0 : table3[Table2.Symbol.IsAlias] ? table3[Table2.Symbol.Name] : table3[Table2.Symbol.BaseName];
}
__name(getTableLikeName2, "getTableLikeName");
function getColumnNameAndConfig2(a, b) {
  return {
    name: typeof a === "string" && a.length > 0 ? a : "",
    config: typeof a === "object" ? a : b
  };
}
__name(getColumnNameAndConfig2, "getColumnNameAndConfig");
var textDecoder2 = typeof TextDecoder === "undefined" ? null : new TextDecoder();
var InlineForeignKeys3 = /* @__PURE__ */ Symbol.for("drizzle:PgInlineForeignKeys");
var EnableRLS2 = /* @__PURE__ */ Symbol.for("drizzle:EnableRLS");
var PgTable2 = class extends Table2 {
  static {
    __name(this, "PgTable");
  }
  static [entityKind2] = "PgTable";
  static Symbol = Object.assign({}, Table2.Symbol, {
    InlineForeignKeys: InlineForeignKeys3,
    EnableRLS: EnableRLS2
  });
  [InlineForeignKeys3] = [];
  [EnableRLS2] = false;
  [Table2.Symbol.ExtraConfigBuilder] = void 0;
  [Table2.Symbol.ExtraConfigColumns] = {};
};
var PrimaryKeyBuilder2 = class {
  static {
    __name(this, "PrimaryKeyBuilder");
  }
  static [entityKind2] = "PgPrimaryKeyBuilder";
  columns;
  name;
  constructor(columns, name) {
    this.columns = columns;
    this.name = name;
  }
  build(table3) {
    return new PrimaryKey2(table3, this.columns, this.name);
  }
};
var PrimaryKey2 = class {
  static {
    __name(this, "PrimaryKey");
  }
  constructor(table3, columns, name) {
    this.table = table3;
    this.columns = columns;
    this.name = name;
  }
  static [entityKind2] = "PgPrimaryKey";
  columns;
  name;
  getName() {
    return this.name ?? `${this.table[PgTable2.Symbol.Name]}_${this.columns.map((column) => column.name).join("_")}_pk`;
  }
};
function bindIfParam2(value, column) {
  if (isDriverValueEncoder2(column) && !isSQLWrapper2(value) && !is2(value, Param2) && !is2(value, Placeholder2) && !is2(value, Column2) && !is2(value, Table2) && !is2(value, View2)) {
    return new Param2(value, column);
  }
  return value;
}
__name(bindIfParam2, "bindIfParam");
var eq2 = /* @__PURE__ */ __name((left, right) => {
  return sql2`${left} = ${bindIfParam2(right, left)}`;
}, "eq");
var ne2 = /* @__PURE__ */ __name((left, right) => {
  return sql2`${left} <> ${bindIfParam2(right, left)}`;
}, "ne");
function and2(...unfilteredConditions) {
  const conditions = unfilteredConditions.filter((c) => c !== void 0);
  if (conditions.length === 0) {
    return;
  }
  if (conditions.length === 1) {
    return new SQL2(conditions);
  }
  return new SQL2([
    new StringChunk2("("),
    sql2.join(conditions, new StringChunk2(" and ")),
    new StringChunk2(")")
  ]);
}
__name(and2, "and");
function or2(...unfilteredConditions) {
  const conditions = unfilteredConditions.filter((c) => c !== void 0);
  if (conditions.length === 0) {
    return;
  }
  if (conditions.length === 1) {
    return new SQL2(conditions);
  }
  return new SQL2([
    new StringChunk2("("),
    sql2.join(conditions, new StringChunk2(" or ")),
    new StringChunk2(")")
  ]);
}
__name(or2, "or");
function not2(condition) {
  return sql2`not ${condition}`;
}
__name(not2, "not");
var gt2 = /* @__PURE__ */ __name((left, right) => {
  return sql2`${left} > ${bindIfParam2(right, left)}`;
}, "gt");
var gte2 = /* @__PURE__ */ __name((left, right) => {
  return sql2`${left} >= ${bindIfParam2(right, left)}`;
}, "gte");
var lt2 = /* @__PURE__ */ __name((left, right) => {
  return sql2`${left} < ${bindIfParam2(right, left)}`;
}, "lt");
var lte2 = /* @__PURE__ */ __name((left, right) => {
  return sql2`${left} <= ${bindIfParam2(right, left)}`;
}, "lte");
function inArray2(column, values) {
  if (Array.isArray(values)) {
    if (values.length === 0) {
      return sql2`false`;
    }
    return sql2`${column} in ${values.map((v) => bindIfParam2(v, column))}`;
  }
  return sql2`${column} in ${bindIfParam2(values, column)}`;
}
__name(inArray2, "inArray");
function notInArray2(column, values) {
  if (Array.isArray(values)) {
    if (values.length === 0) {
      return sql2`true`;
    }
    return sql2`${column} not in ${values.map((v) => bindIfParam2(v, column))}`;
  }
  return sql2`${column} not in ${bindIfParam2(values, column)}`;
}
__name(notInArray2, "notInArray");
function isNull25(value) {
  return sql2`${value} is null`;
}
__name(isNull25, "isNull");
function isNotNull2(value) {
  return sql2`${value} is not null`;
}
__name(isNotNull2, "isNotNull");
function exists2(subquery) {
  return sql2`exists ${subquery}`;
}
__name(exists2, "exists");
function notExists2(subquery) {
  return sql2`not exists ${subquery}`;
}
__name(notExists2, "notExists");
function between2(column, min, max) {
  return sql2`${column} between ${bindIfParam2(min, column)} and ${bindIfParam2(max, column)}`;
}
__name(between2, "between");
function notBetween2(column, min, max) {
  return sql2`${column} not between ${bindIfParam2(min, column)} and ${bindIfParam2(max, column)}`;
}
__name(notBetween2, "notBetween");
function like2(column, value) {
  return sql2`${column} like ${value}`;
}
__name(like2, "like");
function notLike2(column, value) {
  return sql2`${column} not like ${value}`;
}
__name(notLike2, "notLike");
function ilike2(column, value) {
  return sql2`${column} ilike ${value}`;
}
__name(ilike2, "ilike");
function notIlike2(column, value) {
  return sql2`${column} not ilike ${value}`;
}
__name(notIlike2, "notIlike");
function asc2(column) {
  return sql2`${column} asc`;
}
__name(asc2, "asc");
function desc2(column) {
  return sql2`${column} desc`;
}
__name(desc2, "desc");
var Relation2 = class {
  static {
    __name(this, "Relation");
  }
  constructor(sourceTable, referencedTable, relationName) {
    this.sourceTable = sourceTable;
    this.referencedTable = referencedTable;
    this.relationName = relationName;
    this.referencedTableName = referencedTable[Table2.Symbol.Name];
  }
  static [entityKind2] = "Relation";
  referencedTableName;
  fieldName;
};
var Relations2 = class {
  static {
    __name(this, "Relations");
  }
  constructor(table3, config2) {
    this.table = table3;
    this.config = config2;
  }
  static [entityKind2] = "Relations";
};
var One2 = class _One extends Relation2 {
  static {
    __name(this, "One");
  }
  constructor(sourceTable, referencedTable, config2, isNullable) {
    super(sourceTable, referencedTable, config2?.relationName);
    this.config = config2;
    this.isNullable = isNullable;
  }
  static [entityKind2] = "One";
  withFieldName(fieldName) {
    const relation = new _One(this.sourceTable, this.referencedTable, this.config, this.isNullable);
    relation.fieldName = fieldName;
    return relation;
  }
};
var Many2 = class _Many extends Relation2 {
  static {
    __name(this, "Many");
  }
  constructor(sourceTable, referencedTable, config2) {
    super(sourceTable, referencedTable, config2?.relationName);
    this.config = config2;
  }
  static [entityKind2] = "Many";
  withFieldName(fieldName) {
    const relation = new _Many(this.sourceTable, this.referencedTable, this.config);
    relation.fieldName = fieldName;
    return relation;
  }
};
function getOperators2() {
  return {
    and: and2,
    between: between2,
    eq: eq2,
    exists: exists2,
    gt: gt2,
    gte: gte2,
    ilike: ilike2,
    inArray: inArray2,
    isNull: isNull25,
    isNotNull: isNotNull2,
    like: like2,
    lt: lt2,
    lte: lte2,
    ne: ne2,
    not: not2,
    notBetween: notBetween2,
    notExists: notExists2,
    notLike: notLike2,
    notIlike: notIlike2,
    notInArray: notInArray2,
    or: or2,
    sql: sql2
  };
}
__name(getOperators2, "getOperators");
function getOrderByOperators2() {
  return {
    sql: sql2,
    asc: asc2,
    desc: desc2
  };
}
__name(getOrderByOperators2, "getOrderByOperators");
function extractTablesRelationalConfig2(schema, configHelpers) {
  if (Object.keys(schema).length === 1 && "default" in schema && !is2(schema["default"], Table2)) {
    schema = schema["default"];
  }
  const tableNamesMap = {};
  const relationsBuffer = {};
  const tablesConfig = {};
  for (const [key, value] of Object.entries(schema)) {
    if (is2(value, Table2)) {
      const dbName = getTableUniqueName2(value);
      const bufferedRelations = relationsBuffer[dbName];
      tableNamesMap[dbName] = key;
      tablesConfig[key] = {
        tsName: key,
        dbName: value[Table2.Symbol.Name],
        schema: value[Table2.Symbol.Schema],
        columns: value[Table2.Symbol.Columns],
        relations: bufferedRelations?.relations ?? {},
        primaryKey: bufferedRelations?.primaryKey ?? []
      };
      for (const column of Object.values(value[Table2.Symbol.Columns])) {
        if (column.primary) {
          tablesConfig[key].primaryKey.push(column);
        }
      }
      const extraConfig = value[Table2.Symbol.ExtraConfigBuilder]?.(value[Table2.Symbol.ExtraConfigColumns]);
      if (extraConfig) {
        for (const configEntry of Object.values(extraConfig)) {
          if (is2(configEntry, PrimaryKeyBuilder2)) {
            tablesConfig[key].primaryKey.push(...configEntry.columns);
          }
        }
      }
    } else if (is2(value, Relations2)) {
      const dbName = getTableUniqueName2(value.table);
      const tableName = tableNamesMap[dbName];
      const relations2 = value.config(configHelpers(value.table));
      let primaryKey;
      for (const [relationName, relation] of Object.entries(relations2)) {
        if (tableName) {
          const tableConfig = tablesConfig[tableName];
          tableConfig.relations[relationName] = relation;
          if (primaryKey) {
            tableConfig.primaryKey.push(...primaryKey);
          }
        } else {
          if (!(dbName in relationsBuffer)) {
            relationsBuffer[dbName] = {
              relations: {},
              primaryKey
            };
          }
          relationsBuffer[dbName].relations[relationName] = relation;
        }
      }
    }
  }
  return { tables: tablesConfig, tableNamesMap };
}
__name(extractTablesRelationalConfig2, "extractTablesRelationalConfig");
function createOne2(sourceTable) {
  return /* @__PURE__ */ __name(function one(table3, config2) {
    return new One2(sourceTable, table3, config2, config2?.fields.reduce((res, f) => res && f.notNull, true) ?? false);
  }, "one");
}
__name(createOne2, "createOne");
function createMany2(sourceTable) {
  return /* @__PURE__ */ __name(function many(referencedTable, config2) {
    return new Many2(sourceTable, referencedTable, config2);
  }, "many");
}
__name(createMany2, "createMany");
function normalizeRelation2(schema, tableNamesMap, relation) {
  if (is2(relation, One2) && relation.config) {
    return {
      fields: relation.config.fields,
      references: relation.config.references
    };
  }
  const referencedTableTsName = tableNamesMap[getTableUniqueName2(relation.referencedTable)];
  if (!referencedTableTsName) {
    throw new Error(`Table "${relation.referencedTable[Table2.Symbol.Name]}" not found in schema`);
  }
  const referencedTableConfig = schema[referencedTableTsName];
  if (!referencedTableConfig) {
    throw new Error(`Table "${referencedTableTsName}" not found in schema`);
  }
  const sourceTable = relation.sourceTable;
  const sourceTableTsName = tableNamesMap[getTableUniqueName2(sourceTable)];
  if (!sourceTableTsName) {
    throw new Error(`Table "${sourceTable[Table2.Symbol.Name]}" not found in schema`);
  }
  const reverseRelations = [];
  for (const referencedTableRelation of Object.values(referencedTableConfig.relations)) {
    if (relation.relationName && relation !== referencedTableRelation && referencedTableRelation.relationName === relation.relationName || !relation.relationName && referencedTableRelation.referencedTable === relation.sourceTable) {
      reverseRelations.push(referencedTableRelation);
    }
  }
  if (reverseRelations.length > 1) {
    throw relation.relationName ? new Error(`There are multiple relations with name "${relation.relationName}" in table "${referencedTableTsName}"`) : new Error(`There are multiple relations between "${referencedTableTsName}" and "${relation.sourceTable[Table2.Symbol.Name]}". Please specify relation name`);
  }
  if (reverseRelations[0] && is2(reverseRelations[0], One2) && reverseRelations[0].config) {
    return {
      fields: reverseRelations[0].config.references,
      references: reverseRelations[0].config.fields
    };
  }
  throw new Error(`There is not enough information to infer relation "${sourceTableTsName}.${relation.fieldName}"`);
}
__name(normalizeRelation2, "normalizeRelation");
function createTableRelationsHelpers2(sourceTable) {
  return {
    one: createOne2(sourceTable),
    many: createMany2(sourceTable)
  };
}
__name(createTableRelationsHelpers2, "createTableRelationsHelpers");
function mapRelationalRow2(tablesConfig, tableConfig, row, buildQueryResultSelection, mapColumnValue = (value) => value) {
  const result = {};
  for (const [
    selectionItemIndex,
    selectionItem
  ] of buildQueryResultSelection.entries()) {
    if (selectionItem.isJson) {
      const relation = tableConfig.relations[selectionItem.tsKey];
      const rawSubRows = row[selectionItemIndex];
      const subRows = typeof rawSubRows === "string" ? JSON.parse(rawSubRows) : rawSubRows;
      result[selectionItem.tsKey] = is2(relation, One2) ? subRows && mapRelationalRow2(tablesConfig, tablesConfig[selectionItem.relationTableTsKey], subRows, selectionItem.selection, mapColumnValue) : subRows.map((subRow) => mapRelationalRow2(tablesConfig, tablesConfig[selectionItem.relationTableTsKey], subRow, selectionItem.selection, mapColumnValue));
    } else {
      const value = mapColumnValue(row[selectionItemIndex]);
      const field = selectionItem.field;
      let decoder2;
      if (is2(field, Column2)) {
        decoder2 = field;
      } else if (is2(field, SQL2)) {
        decoder2 = field.decoder;
      } else {
        decoder2 = field.sql.decoder;
      }
      result[selectionItem.tsKey] = value === null ? null : decoder2.mapFromDriverValue(value);
    }
  }
  return result;
}
__name(mapRelationalRow2, "mapRelationalRow");
function ownerGroupConditions2({
  user_group_ids,
  ownerGroupIdColumn,
  otherEq = [],
  self_host
}) {
  const isSelfHosted = self_host === "true" || self_host === true;
  return isSelfHosted ? void 0 : or2(...otherEq, ...user_group_ids.map((groupId) => eq2(ownerGroupIdColumn, groupId)));
}
__name(ownerGroupConditions2, "ownerGroupConditions");
function onSelfHosted2(selfHosted, selfHostCallback, otherWiseCallback) {
  const isSelfHosted = selfHosted === "true" || selfHosted === true;
  if (isSelfHosted) {
    return typeof selfHostCallback === "function" ? selfHostCallback() : selfHostCallback;
  } else
    return typeof otherWiseCallback === "function" ? otherWiseCallback() : otherWiseCallback;
}
__name(onSelfHosted2, "onSelfHosted");
function getContext2(args) {
  const len = args.length;
  return args[len - 1];
}
__name(getContext2, "getContext");
var ForeignKeyBuilder2 = class {
  static {
    __name(this, "ForeignKeyBuilder");
  }
  static [entityKind2] = "SQLiteForeignKeyBuilder";
  reference;
  _onUpdate;
  _onDelete;
  constructor(config2, actions) {
    this.reference = () => {
      const { name, columns, foreignColumns } = config2();
      return { name, columns, foreignTable: foreignColumns[0].table, foreignColumns };
    };
    if (actions) {
      this._onUpdate = actions.onUpdate;
      this._onDelete = actions.onDelete;
    }
  }
  onUpdate(action) {
    this._onUpdate = action;
    return this;
  }
  onDelete(action) {
    this._onDelete = action;
    return this;
  }
  build(table3) {
    return new ForeignKey2(table3, this);
  }
};
var ForeignKey2 = class {
  static {
    __name(this, "ForeignKey");
  }
  constructor(table3, builder) {
    this.table = table3;
    this.reference = builder.reference;
    this.onUpdate = builder._onUpdate;
    this.onDelete = builder._onDelete;
  }
  static [entityKind2] = "SQLiteForeignKey";
  reference;
  onUpdate;
  onDelete;
  getName() {
    const { name, columns, foreignColumns } = this.reference();
    const columnNames = columns.map((column) => column.name);
    const foreignColumnNames = foreignColumns.map((column) => column.name);
    const chunks = [
      this.table[TableName2],
      ...columnNames,
      foreignColumns[0].table[TableName2],
      ...foreignColumnNames
    ];
    return name ?? `${chunks.join("_")}_fk`;
  }
};
function uniqueKeyName22(table3, columns) {
  return `${table3[TableName2]}_${columns.join("_")}_unique`;
}
__name(uniqueKeyName22, "uniqueKeyName2");
var SQLiteColumnBuilder2 = class extends ColumnBuilder2 {
  static {
    __name(this, "SQLiteColumnBuilder");
  }
  static [entityKind2] = "SQLiteColumnBuilder";
  foreignKeyConfigs = [];
  references(ref2, actions = {}) {
    this.foreignKeyConfigs.push({ ref: ref2, actions });
    return this;
  }
  unique(name) {
    this.config.isUnique = true;
    this.config.uniqueName = name;
    return this;
  }
  generatedAlwaysAs(as, config2) {
    this.config.generated = {
      as,
      type: "always",
      mode: config2?.mode ?? "virtual"
    };
    return this;
  }
  buildForeignKeys(column, table3) {
    return this.foreignKeyConfigs.map(({ ref: ref2, actions }) => {
      return ((ref22, actions2) => {
        const builder = new ForeignKeyBuilder2(() => {
          const foreignColumn = ref22();
          return { columns: [column], foreignColumns: [foreignColumn] };
        });
        if (actions2.onUpdate) {
          builder.onUpdate(actions2.onUpdate);
        }
        if (actions2.onDelete) {
          builder.onDelete(actions2.onDelete);
        }
        return builder.build(table3);
      })(ref2, actions);
    });
  }
};
var SQLiteColumn2 = class extends Column2 {
  static {
    __name(this, "SQLiteColumn");
  }
  constructor(table3, config2) {
    if (!config2.uniqueName) {
      config2.uniqueName = uniqueKeyName22(table3, [config2.name]);
    }
    super(table3, config2);
    this.table = table3;
  }
  static [entityKind2] = "SQLiteColumn";
};
var SQLiteBigIntBuilder2 = class extends SQLiteColumnBuilder2 {
  static {
    __name(this, "SQLiteBigIntBuilder");
  }
  static [entityKind2] = "SQLiteBigIntBuilder";
  constructor(name) {
    super(name, "bigint", "SQLiteBigInt");
  }
  build(table3) {
    return new SQLiteBigInt2(table3, this.config);
  }
};
var SQLiteBigInt2 = class extends SQLiteColumn2 {
  static {
    __name(this, "SQLiteBigInt");
  }
  static [entityKind2] = "SQLiteBigInt";
  getSQLType() {
    return "blob";
  }
  mapFromDriverValue(value) {
    if (typeof Buffer !== "undefined" && Buffer.from) {
      const buf = Buffer.isBuffer(value) ? value : value instanceof ArrayBuffer ? Buffer.from(value) : value.buffer ? Buffer.from(value.buffer, value.byteOffset, value.byteLength) : Buffer.from(value);
      return BigInt(buf.toString("utf8"));
    }
    return BigInt(textDecoder2.decode(value));
  }
  mapToDriverValue(value) {
    return Buffer.from(value.toString());
  }
};
var SQLiteBlobJsonBuilder2 = class extends SQLiteColumnBuilder2 {
  static {
    __name(this, "SQLiteBlobJsonBuilder");
  }
  static [entityKind2] = "SQLiteBlobJsonBuilder";
  constructor(name) {
    super(name, "json", "SQLiteBlobJson");
  }
  build(table3) {
    return new SQLiteBlobJson2(table3, this.config);
  }
};
var SQLiteBlobJson2 = class extends SQLiteColumn2 {
  static {
    __name(this, "SQLiteBlobJson");
  }
  static [entityKind2] = "SQLiteBlobJson";
  getSQLType() {
    return "blob";
  }
  mapFromDriverValue(value) {
    if (typeof Buffer !== "undefined" && Buffer.from) {
      const buf = Buffer.isBuffer(value) ? value : value instanceof ArrayBuffer ? Buffer.from(value) : value.buffer ? Buffer.from(value.buffer, value.byteOffset, value.byteLength) : Buffer.from(value);
      return JSON.parse(buf.toString("utf8"));
    }
    return JSON.parse(textDecoder2.decode(value));
  }
  mapToDriverValue(value) {
    return Buffer.from(JSON.stringify(value));
  }
};
var SQLiteBlobBufferBuilder2 = class extends SQLiteColumnBuilder2 {
  static {
    __name(this, "SQLiteBlobBufferBuilder");
  }
  static [entityKind2] = "SQLiteBlobBufferBuilder";
  constructor(name) {
    super(name, "buffer", "SQLiteBlobBuffer");
  }
  build(table3) {
    return new SQLiteBlobBuffer2(table3, this.config);
  }
};
var SQLiteBlobBuffer2 = class extends SQLiteColumn2 {
  static {
    __name(this, "SQLiteBlobBuffer");
  }
  static [entityKind2] = "SQLiteBlobBuffer";
  mapFromDriverValue(value) {
    if (Buffer.isBuffer(value)) {
      return value;
    }
    return Buffer.from(value);
  }
  getSQLType() {
    return "blob";
  }
};
function blob2(a, b) {
  const { name, config: config2 } = getColumnNameAndConfig2(a, b);
  if (config2?.mode === "json") {
    return new SQLiteBlobJsonBuilder2(name);
  }
  if (config2?.mode === "bigint") {
    return new SQLiteBigIntBuilder2(name);
  }
  return new SQLiteBlobBufferBuilder2(name);
}
__name(blob2, "blob");
var SQLiteCustomColumnBuilder2 = class extends SQLiteColumnBuilder2 {
  static {
    __name(this, "SQLiteCustomColumnBuilder");
  }
  static [entityKind2] = "SQLiteCustomColumnBuilder";
  constructor(name, fieldConfig, customTypeParams) {
    super(name, "custom", "SQLiteCustomColumn");
    this.config.fieldConfig = fieldConfig;
    this.config.customTypeParams = customTypeParams;
  }
  build(table3) {
    return new SQLiteCustomColumn2(table3, this.config);
  }
};
var SQLiteCustomColumn2 = class extends SQLiteColumn2 {
  static {
    __name(this, "SQLiteCustomColumn");
  }
  static [entityKind2] = "SQLiteCustomColumn";
  sqlName;
  mapTo;
  mapFrom;
  constructor(table3, config2) {
    super(table3, config2);
    this.sqlName = config2.customTypeParams.dataType(config2.fieldConfig);
    this.mapTo = config2.customTypeParams.toDriver;
    this.mapFrom = config2.customTypeParams.fromDriver;
  }
  getSQLType() {
    return this.sqlName;
  }
  mapFromDriverValue(value) {
    return typeof this.mapFrom === "function" ? this.mapFrom(value) : value;
  }
  mapToDriverValue(value) {
    return typeof this.mapTo === "function" ? this.mapTo(value) : value;
  }
};
function customType2(customTypeParams) {
  return (a, b) => {
    const { name, config: config2 } = getColumnNameAndConfig2(a, b);
    return new SQLiteCustomColumnBuilder2(name, config2, customTypeParams);
  };
}
__name(customType2, "customType");
var SQLiteBaseIntegerBuilder2 = class extends SQLiteColumnBuilder2 {
  static {
    __name(this, "SQLiteBaseIntegerBuilder");
  }
  static [entityKind2] = "SQLiteBaseIntegerBuilder";
  constructor(name, dataType, columnType) {
    super(name, dataType, columnType);
    this.config.autoIncrement = false;
  }
  primaryKey(config2) {
    if (config2?.autoIncrement) {
      this.config.autoIncrement = true;
    }
    this.config.hasDefault = true;
    return super.primaryKey();
  }
};
var SQLiteBaseInteger2 = class extends SQLiteColumn2 {
  static {
    __name(this, "SQLiteBaseInteger");
  }
  static [entityKind2] = "SQLiteBaseInteger";
  autoIncrement = this.config.autoIncrement;
  getSQLType() {
    return "integer";
  }
};
var SQLiteIntegerBuilder2 = class extends SQLiteBaseIntegerBuilder2 {
  static {
    __name(this, "SQLiteIntegerBuilder");
  }
  static [entityKind2] = "SQLiteIntegerBuilder";
  constructor(name) {
    super(name, "number", "SQLiteInteger");
  }
  build(table3) {
    return new SQLiteInteger2(table3, this.config);
  }
};
var SQLiteInteger2 = class extends SQLiteBaseInteger2 {
  static {
    __name(this, "SQLiteInteger");
  }
  static [entityKind2] = "SQLiteInteger";
};
var SQLiteTimestampBuilder2 = class extends SQLiteBaseIntegerBuilder2 {
  static {
    __name(this, "SQLiteTimestampBuilder");
  }
  static [entityKind2] = "SQLiteTimestampBuilder";
  constructor(name, mode) {
    super(name, "date", "SQLiteTimestamp");
    this.config.mode = mode;
  }
  defaultNow() {
    return this.default(sql2`(cast((julianday('now') - 2440587.5)*86400000 as integer))`);
  }
  build(table3) {
    return new SQLiteTimestamp2(table3, this.config);
  }
};
var SQLiteTimestamp2 = class extends SQLiteBaseInteger2 {
  static {
    __name(this, "SQLiteTimestamp");
  }
  static [entityKind2] = "SQLiteTimestamp";
  mode = this.config.mode;
  mapFromDriverValue(value) {
    if (this.config.mode === "timestamp") {
      return new Date(value * 1e3);
    }
    return new Date(value);
  }
  mapToDriverValue(value) {
    const unix = value.getTime();
    if (this.config.mode === "timestamp") {
      return Math.floor(unix / 1e3);
    }
    return unix;
  }
};
var SQLiteBooleanBuilder2 = class extends SQLiteBaseIntegerBuilder2 {
  static {
    __name(this, "SQLiteBooleanBuilder");
  }
  static [entityKind2] = "SQLiteBooleanBuilder";
  constructor(name, mode) {
    super(name, "boolean", "SQLiteBoolean");
    this.config.mode = mode;
  }
  build(table3) {
    return new SQLiteBoolean2(table3, this.config);
  }
};
var SQLiteBoolean2 = class extends SQLiteBaseInteger2 {
  static {
    __name(this, "SQLiteBoolean");
  }
  static [entityKind2] = "SQLiteBoolean";
  mode = this.config.mode;
  mapFromDriverValue(value) {
    return Number(value) === 1;
  }
  mapToDriverValue(value) {
    return value ? 1 : 0;
  }
};
function integer2(a, b) {
  const { name, config: config2 } = getColumnNameAndConfig2(a, b);
  if (config2?.mode === "timestamp" || config2?.mode === "timestamp_ms") {
    return new SQLiteTimestampBuilder2(name, config2.mode);
  }
  if (config2?.mode === "boolean") {
    return new SQLiteBooleanBuilder2(name, config2.mode);
  }
  return new SQLiteIntegerBuilder2(name);
}
__name(integer2, "integer");
var SQLiteNumericBuilder2 = class extends SQLiteColumnBuilder2 {
  static {
    __name(this, "SQLiteNumericBuilder");
  }
  static [entityKind2] = "SQLiteNumericBuilder";
  constructor(name) {
    super(name, "string", "SQLiteNumeric");
  }
  build(table3) {
    return new SQLiteNumeric2(table3, this.config);
  }
};
var SQLiteNumeric2 = class extends SQLiteColumn2 {
  static {
    __name(this, "SQLiteNumeric");
  }
  static [entityKind2] = "SQLiteNumeric";
  mapFromDriverValue(value) {
    if (typeof value === "string")
      return value;
    return String(value);
  }
  getSQLType() {
    return "numeric";
  }
};
var SQLiteNumericNumberBuilder2 = class extends SQLiteColumnBuilder2 {
  static {
    __name(this, "SQLiteNumericNumberBuilder");
  }
  static [entityKind2] = "SQLiteNumericNumberBuilder";
  constructor(name) {
    super(name, "number", "SQLiteNumericNumber");
  }
  build(table3) {
    return new SQLiteNumericNumber2(table3, this.config);
  }
};
var SQLiteNumericNumber2 = class extends SQLiteColumn2 {
  static {
    __name(this, "SQLiteNumericNumber");
  }
  static [entityKind2] = "SQLiteNumericNumber";
  mapFromDriverValue(value) {
    if (typeof value === "number")
      return value;
    return Number(value);
  }
  mapToDriverValue = String;
  getSQLType() {
    return "numeric";
  }
};
var SQLiteNumericBigIntBuilder2 = class extends SQLiteColumnBuilder2 {
  static {
    __name(this, "SQLiteNumericBigIntBuilder");
  }
  static [entityKind2] = "SQLiteNumericBigIntBuilder";
  constructor(name) {
    super(name, "bigint", "SQLiteNumericBigInt");
  }
  build(table3) {
    return new SQLiteNumericBigInt2(table3, this.config);
  }
};
var SQLiteNumericBigInt2 = class extends SQLiteColumn2 {
  static {
    __name(this, "SQLiteNumericBigInt");
  }
  static [entityKind2] = "SQLiteNumericBigInt";
  mapFromDriverValue = BigInt;
  mapToDriverValue = String;
  getSQLType() {
    return "numeric";
  }
};
function numeric2(a, b) {
  const { name, config: config2 } = getColumnNameAndConfig2(a, b);
  const mode = config2?.mode;
  return mode === "number" ? new SQLiteNumericNumberBuilder2(name) : mode === "bigint" ? new SQLiteNumericBigIntBuilder2(name) : new SQLiteNumericBuilder2(name);
}
__name(numeric2, "numeric");
var SQLiteRealBuilder2 = class extends SQLiteColumnBuilder2 {
  static {
    __name(this, "SQLiteRealBuilder");
  }
  static [entityKind2] = "SQLiteRealBuilder";
  constructor(name) {
    super(name, "number", "SQLiteReal");
  }
  build(table3) {
    return new SQLiteReal2(table3, this.config);
  }
};
var SQLiteReal2 = class extends SQLiteColumn2 {
  static {
    __name(this, "SQLiteReal");
  }
  static [entityKind2] = "SQLiteReal";
  getSQLType() {
    return "real";
  }
};
function real2(name) {
  return new SQLiteRealBuilder2(name ?? "");
}
__name(real2, "real");
var SQLiteTextBuilder2 = class extends SQLiteColumnBuilder2 {
  static {
    __name(this, "SQLiteTextBuilder");
  }
  static [entityKind2] = "SQLiteTextBuilder";
  constructor(name, config2) {
    super(name, "string", "SQLiteText");
    this.config.enumValues = config2.enum;
    this.config.length = config2.length;
  }
  build(table3) {
    return new SQLiteText2(table3, this.config);
  }
};
var SQLiteText2 = class extends SQLiteColumn2 {
  static {
    __name(this, "SQLiteText");
  }
  static [entityKind2] = "SQLiteText";
  enumValues = this.config.enumValues;
  length = this.config.length;
  constructor(table3, config2) {
    super(table3, config2);
  }
  getSQLType() {
    return `text${this.config.length ? `(${this.config.length})` : ""}`;
  }
};
var SQLiteTextJsonBuilder2 = class extends SQLiteColumnBuilder2 {
  static {
    __name(this, "SQLiteTextJsonBuilder");
  }
  static [entityKind2] = "SQLiteTextJsonBuilder";
  constructor(name) {
    super(name, "json", "SQLiteTextJson");
  }
  build(table3) {
    return new SQLiteTextJson2(table3, this.config);
  }
};
var SQLiteTextJson2 = class extends SQLiteColumn2 {
  static {
    __name(this, "SQLiteTextJson");
  }
  static [entityKind2] = "SQLiteTextJson";
  getSQLType() {
    return "text";
  }
  mapFromDriverValue(value) {
    return JSON.parse(value);
  }
  mapToDriverValue(value) {
    return JSON.stringify(value);
  }
};
function text2(a, b = {}) {
  const { name, config: config2 } = getColumnNameAndConfig2(a, b);
  if (config2.mode === "json") {
    return new SQLiteTextJsonBuilder2(name);
  }
  return new SQLiteTextBuilder2(name, config2);
}
__name(text2, "text");
var SelectionProxyHandler2 = class _SelectionProxyHandler {
  static {
    __name(this, "SelectionProxyHandler");
  }
  static [entityKind2] = "SelectionProxyHandler";
  config;
  constructor(config2) {
    this.config = { ...config2 };
  }
  get(subquery, prop) {
    if (prop === "_") {
      return {
        ...subquery["_"],
        selectedFields: new Proxy(subquery._.selectedFields, this)
      };
    }
    if (prop === ViewBaseConfig2) {
      return {
        ...subquery[ViewBaseConfig2],
        selectedFields: new Proxy(subquery[ViewBaseConfig2].selectedFields, this)
      };
    }
    if (typeof prop === "symbol") {
      return subquery[prop];
    }
    const columns = is2(subquery, Subquery2) ? subquery._.selectedFields : is2(subquery, View2) ? subquery[ViewBaseConfig2].selectedFields : subquery;
    const value = columns[prop];
    if (is2(value, SQL2.Aliased)) {
      if (this.config.sqlAliasedBehavior === "sql" && !value.isSelectionField) {
        return value.sql;
      }
      const newValue = value.clone();
      newValue.isSelectionField = true;
      return newValue;
    }
    if (is2(value, SQL2)) {
      if (this.config.sqlBehavior === "sql") {
        return value;
      }
      throw new Error(`You tried to reference "${prop}" field from a subquery, which is a raw SQL field, but it doesn't have an alias declared. Please add an alias to the field using ".as('alias')" method.`);
    }
    if (is2(value, Column2)) {
      if (this.config.alias) {
        return new Proxy(value, new ColumnAliasProxyHandler2(new Proxy(value.table, new TableAliasProxyHandler2(this.config.alias, this.config.replaceOriginalName ?? false))));
      }
      return value;
    }
    if (typeof value !== "object" || value === null) {
      return value;
    }
    return new Proxy(value, new _SelectionProxyHandler(this.config));
  }
};
function getSQLiteColumnBuilders2() {
  return {
    blob: blob2,
    customType: customType2,
    integer: integer2,
    numeric: numeric2,
    real: real2,
    text: text2
  };
}
__name(getSQLiteColumnBuilders2, "getSQLiteColumnBuilders");
var InlineForeignKeys22 = /* @__PURE__ */ Symbol.for("drizzle:SQLiteInlineForeignKeys");
var SQLiteTable2 = class extends Table2 {
  static {
    __name(this, "SQLiteTable");
  }
  static [entityKind2] = "SQLiteTable";
  static Symbol = Object.assign({}, Table2.Symbol, {
    InlineForeignKeys: InlineForeignKeys22
  });
  [Table2.Symbol.Columns];
  [InlineForeignKeys22] = [];
  [Table2.Symbol.ExtraConfigBuilder] = void 0;
};
function sqliteTableBase2(name, columns, extraConfig, schema, baseName = name) {
  const rawTable = new SQLiteTable2(name, schema, baseName);
  const parsedColumns = typeof columns === "function" ? columns(getSQLiteColumnBuilders2()) : columns;
  const builtColumns = Object.fromEntries(Object.entries(parsedColumns).map(([name2, colBuilderBase]) => {
    const colBuilder = colBuilderBase;
    colBuilder.setName(name2);
    const column = colBuilder.build(rawTable);
    rawTable[InlineForeignKeys22].push(...colBuilder.buildForeignKeys(column, rawTable));
    return [name2, column];
  }));
  const table3 = Object.assign(rawTable, builtColumns);
  table3[Table2.Symbol.Columns] = builtColumns;
  table3[Table2.Symbol.ExtraConfigColumns] = builtColumns;
  if (extraConfig) {
    table3[SQLiteTable2.Symbol.ExtraConfigBuilder] = extraConfig;
  }
  return table3;
}
__name(sqliteTableBase2, "sqliteTableBase");
var sqliteTable2 = /* @__PURE__ */ __name((name, columns, extraConfig) => {
  return sqliteTableBase2(name, columns, extraConfig);
}, "sqliteTable");
function extractUsedTable2(table3) {
  if (is2(table3, SQLiteTable2)) {
    return [`${table3[Table2.Symbol.BaseName]}`];
  }
  if (is2(table3, Subquery2)) {
    return table3._.usedTables ?? [];
  }
  if (is2(table3, SQL2)) {
    return table3.usedTables ?? [];
  }
  return [];
}
__name(extractUsedTable2, "extractUsedTable");
var SQLiteDeleteBase2 = class extends QueryPromise2 {
  static {
    __name(this, "SQLiteDeleteBase");
  }
  constructor(table3, session, dialect, withList) {
    super();
    this.table = table3;
    this.session = session;
    this.dialect = dialect;
    this.config = { table: table3, withList };
  }
  static [entityKind2] = "SQLiteDelete";
  config;
  where(where) {
    this.config.where = where;
    return this;
  }
  orderBy(...columns) {
    if (typeof columns[0] === "function") {
      const orderBy = columns[0](new Proxy(this.config.table[Table2.Symbol.Columns], new SelectionProxyHandler2({ sqlAliasedBehavior: "alias", sqlBehavior: "sql" })));
      const orderByArray = Array.isArray(orderBy) ? orderBy : [orderBy];
      this.config.orderBy = orderByArray;
    } else {
      const orderByArray = columns;
      this.config.orderBy = orderByArray;
    }
    return this;
  }
  limit(limit2) {
    this.config.limit = limit2;
    return this;
  }
  returning(fields = this.table[SQLiteTable2.Symbol.Columns]) {
    this.config.returning = orderSelectedFields2(fields);
    return this;
  }
  getSQL() {
    return this.dialect.buildDeleteQuery(this.config);
  }
  toSQL() {
    const { typings: _typings, ...rest } = this.dialect.sqlToQuery(this.getSQL());
    return rest;
  }
  _prepare(isOneTimeQuery = true) {
    return this.session[isOneTimeQuery ? "prepareOneTimeQuery" : "prepareQuery"](this.dialect.sqlToQuery(this.getSQL()), this.config.returning, this.config.returning ? "all" : "run", true, void 0, {
      type: "delete",
      tables: extractUsedTable2(this.config.table)
    });
  }
  prepare() {
    return this._prepare(false);
  }
  run = /* @__PURE__ */ __name((placeholderValues) => {
    return this._prepare().run(placeholderValues);
  }, "run");
  all = /* @__PURE__ */ __name((placeholderValues) => {
    return this._prepare().all(placeholderValues);
  }, "all");
  get = /* @__PURE__ */ __name((placeholderValues) => {
    return this._prepare().get(placeholderValues);
  }, "get");
  values = /* @__PURE__ */ __name((placeholderValues) => {
    return this._prepare().values(placeholderValues);
  }, "values");
  async execute(placeholderValues) {
    return this._prepare().execute(placeholderValues);
  }
  $dynamic() {
    return this;
  }
};
function toSnakeCase2(input) {
  const words = input.replace(/['\u2019]/g, "").match(/[\da-z]+|[A-Z]+(?![a-z])|[A-Z][\da-z]+/g) ?? [];
  return words.map((word) => word.toLowerCase()).join("_");
}
__name(toSnakeCase2, "toSnakeCase");
function toCamelCase2(input) {
  const words = input.replace(/['\u2019]/g, "").match(/[\da-z]+|[A-Z]+(?![a-z])|[A-Z][\da-z]+/g) ?? [];
  return words.reduce((acc, word, i) => {
    const formattedWord = i === 0 ? word.toLowerCase() : `${word[0].toUpperCase()}${word.slice(1)}`;
    return acc + formattedWord;
  }, "");
}
__name(toCamelCase2, "toCamelCase");
function noopCase2(input) {
  return input;
}
__name(noopCase2, "noopCase");
var CasingCache2 = class {
  static {
    __name(this, "CasingCache");
  }
  static [entityKind2] = "CasingCache";
  cache = {};
  cachedTables = {};
  convert;
  constructor(casing) {
    this.convert = casing === "snake_case" ? toSnakeCase2 : casing === "camelCase" ? toCamelCase2 : noopCase2;
  }
  getColumnCasing(column) {
    if (!column.keyAsName)
      return column.name;
    const schema = column.table[Table2.Symbol.Schema] ?? "public";
    const tableName = column.table[Table2.Symbol.OriginalName];
    const key = `${schema}.${tableName}.${column.name}`;
    if (!this.cache[key]) {
      this.cacheTable(column.table);
    }
    return this.cache[key];
  }
  cacheTable(table3) {
    const schema = table3[Table2.Symbol.Schema] ?? "public";
    const tableName = table3[Table2.Symbol.OriginalName];
    const tableKey = `${schema}.${tableName}`;
    if (!this.cachedTables[tableKey]) {
      for (const column of Object.values(table3[Table2.Symbol.Columns])) {
        const columnKey = `${tableKey}.${column.name}`;
        this.cache[columnKey] = this.convert(column.name);
      }
      this.cachedTables[tableKey] = true;
    }
  }
  clearCache() {
    this.cache = {};
    this.cachedTables = {};
  }
};
var SQLiteViewBase2 = class extends View2 {
  static {
    __name(this, "SQLiteViewBase");
  }
  static [entityKind2] = "SQLiteViewBase";
};
var SQLiteDialect2 = class {
  static {
    __name(this, "SQLiteDialect");
  }
  static [entityKind2] = "SQLiteDialect";
  casing;
  constructor(config2) {
    this.casing = new CasingCache2(config2?.casing);
  }
  escapeName(name) {
    return `"${name}"`;
  }
  escapeParam(_num) {
    return "?";
  }
  escapeString(str) {
    return `'${str.replace(/'/g, "''")}'`;
  }
  buildWithCTE(queries) {
    if (!queries?.length)
      return;
    const withSqlChunks = [sql2`with `];
    for (const [i, w] of queries.entries()) {
      withSqlChunks.push(sql2`${sql2.identifier(w._.alias)} as (${w._.sql})`);
      if (i < queries.length - 1) {
        withSqlChunks.push(sql2`, `);
      }
    }
    withSqlChunks.push(sql2` `);
    return sql2.join(withSqlChunks);
  }
  buildDeleteQuery({ table: table3, where, returning, withList, limit: limit2, orderBy }) {
    const withSql = this.buildWithCTE(withList);
    const returningSql = returning ? sql2` returning ${this.buildSelection(returning, { isSingleTable: true })}` : void 0;
    const whereSql = where ? sql2` where ${where}` : void 0;
    const orderBySql = this.buildOrderBy(orderBy);
    const limitSql = this.buildLimit(limit2);
    return sql2`${withSql}delete from ${table3}${whereSql}${returningSql}${orderBySql}${limitSql}`;
  }
  buildUpdateSet(table3, set) {
    const tableColumns = table3[Table2.Symbol.Columns];
    const columnNames = Object.keys(tableColumns).filter((colName) => set[colName] !== void 0 || tableColumns[colName]?.onUpdateFn !== void 0);
    const setSize = columnNames.length;
    return sql2.join(columnNames.flatMap((colName, i) => {
      const col = tableColumns[colName];
      const onUpdateFnResult = col.onUpdateFn?.();
      const value = set[colName] ?? (is2(onUpdateFnResult, SQL2) ? onUpdateFnResult : sql2.param(onUpdateFnResult, col));
      const res = sql2`${sql2.identifier(this.casing.getColumnCasing(col))} = ${value}`;
      if (i < setSize - 1) {
        return [res, sql2.raw(", ")];
      }
      return [res];
    }));
  }
  buildUpdateQuery({ table: table3, set, where, returning, withList, joins, from, limit: limit2, orderBy }) {
    const withSql = this.buildWithCTE(withList);
    const setSql = this.buildUpdateSet(table3, set);
    const fromSql = from && sql2.join([sql2.raw(" from "), this.buildFromTable(from)]);
    const joinsSql = this.buildJoins(joins);
    const returningSql = returning ? sql2` returning ${this.buildSelection(returning, { isSingleTable: true })}` : void 0;
    const whereSql = where ? sql2` where ${where}` : void 0;
    const orderBySql = this.buildOrderBy(orderBy);
    const limitSql = this.buildLimit(limit2);
    return sql2`${withSql}update ${table3} set ${setSql}${fromSql}${joinsSql}${whereSql}${returningSql}${orderBySql}${limitSql}`;
  }
  buildSelection(fields, { isSingleTable = false } = {}) {
    const columnsLen = fields.length;
    const chunks = fields.flatMap(({ field }, i) => {
      const chunk = [];
      if (is2(field, SQL2.Aliased) && field.isSelectionField) {
        chunk.push(sql2.identifier(field.fieldAlias));
      } else if (is2(field, SQL2.Aliased) || is2(field, SQL2)) {
        const query = is2(field, SQL2.Aliased) ? field.sql : field;
        if (isSingleTable) {
          chunk.push(new SQL2(query.queryChunks.map((c) => {
            if (is2(c, Column2)) {
              return sql2.identifier(this.casing.getColumnCasing(c));
            }
            return c;
          })));
        } else {
          chunk.push(query);
        }
        if (is2(field, SQL2.Aliased)) {
          chunk.push(sql2` as ${sql2.identifier(field.fieldAlias)}`);
        }
      } else if (is2(field, Column2)) {
        const tableName = field.table[Table2.Symbol.Name];
        if (field.columnType === "SQLiteNumericBigInt") {
          if (isSingleTable) {
            chunk.push(sql2`cast(${sql2.identifier(this.casing.getColumnCasing(field))} as text)`);
          } else {
            chunk.push(sql2`cast(${sql2.identifier(tableName)}.${sql2.identifier(this.casing.getColumnCasing(field))} as text)`);
          }
        } else {
          if (isSingleTable) {
            chunk.push(sql2.identifier(this.casing.getColumnCasing(field)));
          } else {
            chunk.push(sql2`${sql2.identifier(tableName)}.${sql2.identifier(this.casing.getColumnCasing(field))}`);
          }
        }
      } else if (is2(field, Subquery2)) {
        const entries = Object.entries(field._.selectedFields);
        if (entries.length === 1) {
          const entry = entries[0][1];
          const fieldDecoder = is2(entry, SQL2) ? entry.decoder : is2(entry, Column2) ? { mapFromDriverValue: /* @__PURE__ */ __name((v) => entry.mapFromDriverValue(v), "mapFromDriverValue") } : entry.sql.decoder;
          if (fieldDecoder)
            field._.sql.decoder = fieldDecoder;
        }
        chunk.push(field);
      }
      if (i < columnsLen - 1) {
        chunk.push(sql2`, `);
      }
      return chunk;
    });
    return sql2.join(chunks);
  }
  buildJoins(joins) {
    if (!joins || joins.length === 0) {
      return;
    }
    const joinsArray = [];
    if (joins) {
      for (const [index, joinMeta] of joins.entries()) {
        if (index === 0) {
          joinsArray.push(sql2` `);
        }
        const table3 = joinMeta.table;
        const onSql = joinMeta.on ? sql2` on ${joinMeta.on}` : void 0;
        if (is2(table3, SQLiteTable2)) {
          const tableName = table3[SQLiteTable2.Symbol.Name];
          const tableSchema = table3[SQLiteTable2.Symbol.Schema];
          const origTableName = table3[SQLiteTable2.Symbol.OriginalName];
          const alias = tableName === origTableName ? void 0 : joinMeta.alias;
          joinsArray.push(sql2`${sql2.raw(joinMeta.joinType)} join ${tableSchema ? sql2`${sql2.identifier(tableSchema)}.` : void 0}${sql2.identifier(origTableName)}${alias && sql2` ${sql2.identifier(alias)}`}${onSql}`);
        } else {
          joinsArray.push(sql2`${sql2.raw(joinMeta.joinType)} join ${table3}${onSql}`);
        }
        if (index < joins.length - 1) {
          joinsArray.push(sql2` `);
        }
      }
    }
    return sql2.join(joinsArray);
  }
  buildLimit(limit2) {
    return typeof limit2 === "object" || typeof limit2 === "number" && limit2 >= 0 ? sql2` limit ${limit2}` : void 0;
  }
  buildOrderBy(orderBy) {
    const orderByList = [];
    if (orderBy) {
      for (const [index, orderByValue] of orderBy.entries()) {
        orderByList.push(orderByValue);
        if (index < orderBy.length - 1) {
          orderByList.push(sql2`, `);
        }
      }
    }
    return orderByList.length > 0 ? sql2` order by ${sql2.join(orderByList)}` : void 0;
  }
  buildFromTable(table3) {
    if (is2(table3, Table2) && table3[Table2.Symbol.IsAlias]) {
      return sql2`${sql2`${sql2.identifier(table3[Table2.Symbol.Schema] ?? "")}.`.if(table3[Table2.Symbol.Schema])}${sql2.identifier(table3[Table2.Symbol.OriginalName])} ${sql2.identifier(table3[Table2.Symbol.Name])}`;
    }
    return table3;
  }
  buildSelectQuery({
    withList,
    fields,
    fieldsFlat,
    where,
    having,
    table: table3,
    joins,
    orderBy,
    groupBy,
    limit: limit2,
    offset,
    distinct,
    setOperators
  }) {
    const fieldsList = fieldsFlat ?? orderSelectedFields2(fields);
    for (const f of fieldsList) {
      if (is2(f.field, Column2) && getTableName2(f.field.table) !== (is2(table3, Subquery2) ? table3._.alias : is2(table3, SQLiteViewBase2) ? table3[ViewBaseConfig2].name : is2(table3, SQL2) ? void 0 : getTableName2(table3)) && !((table22) => joins?.some(({ alias }) => alias === (table22[Table2.Symbol.IsAlias] ? getTableName2(table22) : table22[Table2.Symbol.BaseName])))(f.field.table)) {
        const tableName = getTableName2(f.field.table);
        throw new Error(`Your "${f.path.join("->")}" field references a column "${tableName}"."${f.field.name}", but the table "${tableName}" is not part of the query! Did you forget to join it?`);
      }
    }
    const isSingleTable = !joins || joins.length === 0;
    const withSql = this.buildWithCTE(withList);
    const distinctSql = distinct ? sql2` distinct` : void 0;
    const selection = this.buildSelection(fieldsList, { isSingleTable });
    const tableSql = this.buildFromTable(table3);
    const joinsSql = this.buildJoins(joins);
    const whereSql = where ? sql2` where ${where}` : void 0;
    const havingSql = having ? sql2` having ${having}` : void 0;
    const groupByList = [];
    if (groupBy) {
      for (const [index, groupByValue] of groupBy.entries()) {
        groupByList.push(groupByValue);
        if (index < groupBy.length - 1) {
          groupByList.push(sql2`, `);
        }
      }
    }
    const groupBySql = groupByList.length > 0 ? sql2` group by ${sql2.join(groupByList)}` : void 0;
    const orderBySql = this.buildOrderBy(orderBy);
    const limitSql = this.buildLimit(limit2);
    const offsetSql = offset ? sql2` offset ${offset}` : void 0;
    const finalQuery = sql2`${withSql}select${distinctSql} ${selection} from ${tableSql}${joinsSql}${whereSql}${groupBySql}${havingSql}${orderBySql}${limitSql}${offsetSql}`;
    if (setOperators.length > 0) {
      return this.buildSetOperations(finalQuery, setOperators);
    }
    return finalQuery;
  }
  buildSetOperations(leftSelect, setOperators) {
    const [setOperator, ...rest] = setOperators;
    if (!setOperator) {
      throw new Error("Cannot pass undefined values to any set operator");
    }
    if (rest.length === 0) {
      return this.buildSetOperationQuery({ leftSelect, setOperator });
    }
    return this.buildSetOperations(this.buildSetOperationQuery({ leftSelect, setOperator }), rest);
  }
  buildSetOperationQuery({
    leftSelect,
    setOperator: { type, isAll, rightSelect, limit: limit2, orderBy, offset }
  }) {
    const leftChunk = sql2`${leftSelect.getSQL()} `;
    const rightChunk = sql2`${rightSelect.getSQL()}`;
    let orderBySql;
    if (orderBy && orderBy.length > 0) {
      const orderByValues = [];
      for (const singleOrderBy of orderBy) {
        if (is2(singleOrderBy, SQLiteColumn2)) {
          orderByValues.push(sql2.identifier(singleOrderBy.name));
        } else if (is2(singleOrderBy, SQL2)) {
          for (let i = 0; i < singleOrderBy.queryChunks.length; i++) {
            const chunk = singleOrderBy.queryChunks[i];
            if (is2(chunk, SQLiteColumn2)) {
              singleOrderBy.queryChunks[i] = sql2.identifier(this.casing.getColumnCasing(chunk));
            }
          }
          orderByValues.push(sql2`${singleOrderBy}`);
        } else {
          orderByValues.push(sql2`${singleOrderBy}`);
        }
      }
      orderBySql = sql2` order by ${sql2.join(orderByValues, sql2`, `)}`;
    }
    const limitSql = typeof limit2 === "object" || typeof limit2 === "number" && limit2 >= 0 ? sql2` limit ${limit2}` : void 0;
    const operatorChunk = sql2.raw(`${type} ${isAll ? "all " : ""}`);
    const offsetSql = offset ? sql2` offset ${offset}` : void 0;
    return sql2`${leftChunk}${operatorChunk}${rightChunk}${orderBySql}${limitSql}${offsetSql}`;
  }
  buildInsertQuery({ table: table3, values: valuesOrSelect, onConflict, returning, withList, select }) {
    const valuesSqlList = [];
    const columns = table3[Table2.Symbol.Columns];
    const colEntries = Object.entries(columns).filter(([_, col]) => !col.shouldDisableInsert());
    const insertOrder = colEntries.map(([, column]) => sql2.identifier(this.casing.getColumnCasing(column)));
    if (select) {
      const select2 = valuesOrSelect;
      if (is2(select2, SQL2)) {
        valuesSqlList.push(select2);
      } else {
        valuesSqlList.push(select2.getSQL());
      }
    } else {
      const values = valuesOrSelect;
      valuesSqlList.push(sql2.raw("values "));
      for (const [valueIndex, value] of values.entries()) {
        const valueList = [];
        for (const [fieldName, col] of colEntries) {
          const colValue = value[fieldName];
          if (colValue === void 0 || is2(colValue, Param2) && colValue.value === void 0) {
            let defaultValue;
            if (col.default !== null && col.default !== void 0) {
              defaultValue = is2(col.default, SQL2) ? col.default : sql2.param(col.default, col);
            } else if (col.defaultFn !== void 0) {
              const defaultFnResult = col.defaultFn();
              defaultValue = is2(defaultFnResult, SQL2) ? defaultFnResult : sql2.param(defaultFnResult, col);
            } else if (!col.default && col.onUpdateFn !== void 0) {
              const onUpdateFnResult = col.onUpdateFn();
              defaultValue = is2(onUpdateFnResult, SQL2) ? onUpdateFnResult : sql2.param(onUpdateFnResult, col);
            } else {
              defaultValue = sql2`null`;
            }
            valueList.push(defaultValue);
          } else {
            valueList.push(colValue);
          }
        }
        valuesSqlList.push(valueList);
        if (valueIndex < values.length - 1) {
          valuesSqlList.push(sql2`, `);
        }
      }
    }
    const withSql = this.buildWithCTE(withList);
    const valuesSql = sql2.join(valuesSqlList);
    const returningSql = returning ? sql2` returning ${this.buildSelection(returning, { isSingleTable: true })}` : void 0;
    const onConflictSql = onConflict?.length ? sql2.join(onConflict) : void 0;
    return sql2`${withSql}insert into ${table3} ${insertOrder} ${valuesSql}${onConflictSql}${returningSql}`;
  }
  sqlToQuery(sql22, invokeSource) {
    return sql22.toQuery({
      casing: this.casing,
      escapeName: this.escapeName,
      escapeParam: this.escapeParam,
      escapeString: this.escapeString,
      invokeSource
    });
  }
  buildRelationalQuery({
    fullSchema,
    schema,
    tableNamesMap,
    table: table3,
    tableConfig,
    queryConfig: config2,
    tableAlias,
    nestedQueryRelation,
    joinOn
  }) {
    let selection = [];
    let limit2, offset, orderBy = [], where;
    const joins = [];
    if (config2 === true) {
      const selectionEntries = Object.entries(tableConfig.columns);
      selection = selectionEntries.map(([key, value]) => ({
        dbKey: value.name,
        tsKey: key,
        field: aliasedTableColumn2(value, tableAlias),
        relationTableTsKey: void 0,
        isJson: false,
        selection: []
      }));
    } else {
      const aliasedColumns = Object.fromEntries(Object.entries(tableConfig.columns).map(([key, value]) => [key, aliasedTableColumn2(value, tableAlias)]));
      if (config2.where) {
        const whereSql = typeof config2.where === "function" ? config2.where(aliasedColumns, getOperators2()) : config2.where;
        where = whereSql && mapColumnsInSQLToAlias2(whereSql, tableAlias);
      }
      const fieldsSelection = [];
      let selectedColumns = [];
      if (config2.columns) {
        let isIncludeMode = false;
        for (const [field, value] of Object.entries(config2.columns)) {
          if (value === void 0) {
            continue;
          }
          if (field in tableConfig.columns) {
            if (!isIncludeMode && value === true) {
              isIncludeMode = true;
            }
            selectedColumns.push(field);
          }
        }
        if (selectedColumns.length > 0) {
          selectedColumns = isIncludeMode ? selectedColumns.filter((c) => config2.columns?.[c] === true) : Object.keys(tableConfig.columns).filter((key) => !selectedColumns.includes(key));
        }
      } else {
        selectedColumns = Object.keys(tableConfig.columns);
      }
      for (const field of selectedColumns) {
        const column = tableConfig.columns[field];
        fieldsSelection.push({ tsKey: field, value: column });
      }
      let selectedRelations = [];
      if (config2.with) {
        selectedRelations = Object.entries(config2.with).filter((entry) => !!entry[1]).map(([tsKey, queryConfig]) => ({ tsKey, queryConfig, relation: tableConfig.relations[tsKey] }));
      }
      let extras;
      if (config2.extras) {
        extras = typeof config2.extras === "function" ? config2.extras(aliasedColumns, { sql: sql2 }) : config2.extras;
        for (const [tsKey, value] of Object.entries(extras)) {
          fieldsSelection.push({
            tsKey,
            value: mapColumnsInAliasedSQLToAlias2(value, tableAlias)
          });
        }
      }
      for (const { tsKey, value } of fieldsSelection) {
        selection.push({
          dbKey: is2(value, SQL2.Aliased) ? value.fieldAlias : tableConfig.columns[tsKey].name,
          tsKey,
          field: is2(value, Column2) ? aliasedTableColumn2(value, tableAlias) : value,
          relationTableTsKey: void 0,
          isJson: false,
          selection: []
        });
      }
      let orderByOrig = typeof config2.orderBy === "function" ? config2.orderBy(aliasedColumns, getOrderByOperators2()) : config2.orderBy ?? [];
      if (!Array.isArray(orderByOrig)) {
        orderByOrig = [orderByOrig];
      }
      orderBy = orderByOrig.map((orderByValue) => {
        if (is2(orderByValue, Column2)) {
          return aliasedTableColumn2(orderByValue, tableAlias);
        }
        return mapColumnsInSQLToAlias2(orderByValue, tableAlias);
      });
      limit2 = config2.limit;
      offset = config2.offset;
      for (const {
        tsKey: selectedRelationTsKey,
        queryConfig: selectedRelationConfigValue,
        relation
      } of selectedRelations) {
        const normalizedRelation = normalizeRelation2(schema, tableNamesMap, relation);
        const relationTableName = getTableUniqueName2(relation.referencedTable);
        const relationTableTsName = tableNamesMap[relationTableName];
        const relationTableAlias = `${tableAlias}_${selectedRelationTsKey}`;
        const joinOn2 = and2(...normalizedRelation.fields.map((field2, i) => eq2(aliasedTableColumn2(normalizedRelation.references[i], relationTableAlias), aliasedTableColumn2(field2, tableAlias))));
        const builtRelation = this.buildRelationalQuery({
          fullSchema,
          schema,
          tableNamesMap,
          table: fullSchema[relationTableTsName],
          tableConfig: schema[relationTableTsName],
          queryConfig: is2(relation, One2) ? selectedRelationConfigValue === true ? { limit: 1 } : { ...selectedRelationConfigValue, limit: 1 } : selectedRelationConfigValue,
          tableAlias: relationTableAlias,
          joinOn: joinOn2,
          nestedQueryRelation: relation
        });
        const field = sql2`(${builtRelation.sql})`.as(selectedRelationTsKey);
        selection.push({
          dbKey: selectedRelationTsKey,
          tsKey: selectedRelationTsKey,
          field,
          relationTableTsKey: relationTableTsName,
          isJson: true,
          selection: builtRelation.selection
        });
      }
    }
    if (selection.length === 0) {
      throw new DrizzleError2({
        message: `No fields selected for table "${tableConfig.tsName}" ("${tableAlias}"). You need to have at least one item in "columns", "with" or "extras". If you need to select all columns, omit the "columns" key or set it to undefined.`
      });
    }
    let result;
    where = and2(joinOn, where);
    if (nestedQueryRelation) {
      let field = sql2`json_array(${sql2.join(selection.map(({ field: field2 }) => is2(field2, SQLiteColumn2) ? sql2.identifier(this.casing.getColumnCasing(field2)) : is2(field2, SQL2.Aliased) ? field2.sql : field2), sql2`, `)})`;
      if (is2(nestedQueryRelation, Many2)) {
        field = sql2`coalesce(json_group_array(${field}), json_array())`;
      }
      const nestedSelection = [{
        dbKey: "data",
        tsKey: "data",
        field: field.as("data"),
        isJson: true,
        relationTableTsKey: tableConfig.tsName,
        selection
      }];
      const needsSubquery = limit2 !== void 0 || offset !== void 0 || orderBy.length > 0;
      if (needsSubquery) {
        result = this.buildSelectQuery({
          table: aliasedTable2(table3, tableAlias),
          fields: {},
          fieldsFlat: [
            {
              path: [],
              field: sql2.raw("*")
            }
          ],
          where,
          limit: limit2,
          offset,
          orderBy,
          setOperators: []
        });
        where = void 0;
        limit2 = void 0;
        offset = void 0;
        orderBy = void 0;
      } else {
        result = aliasedTable2(table3, tableAlias);
      }
      result = this.buildSelectQuery({
        table: is2(result, SQLiteTable2) ? result : new Subquery2(result, {}, tableAlias),
        fields: {},
        fieldsFlat: nestedSelection.map(({ field: field2 }) => ({
          path: [],
          field: is2(field2, Column2) ? aliasedTableColumn2(field2, tableAlias) : field2
        })),
        joins,
        where,
        limit: limit2,
        offset,
        orderBy,
        setOperators: []
      });
    } else {
      result = this.buildSelectQuery({
        table: aliasedTable2(table3, tableAlias),
        fields: {},
        fieldsFlat: selection.map(({ field }) => ({
          path: [],
          field: is2(field, Column2) ? aliasedTableColumn2(field, tableAlias) : field
        })),
        joins,
        where,
        limit: limit2,
        offset,
        orderBy,
        setOperators: []
      });
    }
    return {
      tableTsKey: tableConfig.tsName,
      sql: result,
      selection
    };
  }
};
var SQLiteSyncDialect2 = class extends SQLiteDialect2 {
  static {
    __name(this, "SQLiteSyncDialect");
  }
  static [entityKind2] = "SQLiteSyncDialect";
  migrate(migrations, session, config2) {
    const migrationsTable = config2 === void 0 ? "__drizzle_migrations" : typeof config2 === "string" ? "__drizzle_migrations" : config2.migrationsTable ?? "__drizzle_migrations";
    const migrationTableCreate = sql2`
			CREATE TABLE IF NOT EXISTS ${sql2.identifier(migrationsTable)} (
				id SERIAL PRIMARY KEY,
				hash text NOT NULL,
				created_at numeric
			)
		`;
    session.run(migrationTableCreate);
    const dbMigrations = session.values(sql2`SELECT id, hash, created_at FROM ${sql2.identifier(migrationsTable)} ORDER BY created_at DESC LIMIT 1`);
    const lastDbMigration = dbMigrations[0] ?? void 0;
    session.run(sql2`BEGIN`);
    try {
      for (const migration of migrations) {
        if (!lastDbMigration || Number(lastDbMigration[2]) < migration.folderMillis) {
          for (const stmt of migration.sql) {
            session.run(sql2.raw(stmt));
          }
          session.run(sql2`INSERT INTO ${sql2.identifier(migrationsTable)} ("hash", "created_at") VALUES(${migration.hash}, ${migration.folderMillis})`);
        }
      }
      session.run(sql2`COMMIT`);
    } catch (e) {
      session.run(sql2`ROLLBACK`);
      throw e;
    }
  }
};
var SQLiteAsyncDialect2 = class extends SQLiteDialect2 {
  static {
    __name(this, "SQLiteAsyncDialect");
  }
  static [entityKind2] = "SQLiteAsyncDialect";
  async migrate(migrations, session, config2) {
    const migrationsTable = config2 === void 0 ? "__drizzle_migrations" : typeof config2 === "string" ? "__drizzle_migrations" : config2.migrationsTable ?? "__drizzle_migrations";
    const migrationTableCreate = sql2`
			CREATE TABLE IF NOT EXISTS ${sql2.identifier(migrationsTable)} (
				id SERIAL PRIMARY KEY,
				hash text NOT NULL,
				created_at numeric
			)
		`;
    await session.run(migrationTableCreate);
    const dbMigrations = await session.values(sql2`SELECT id, hash, created_at FROM ${sql2.identifier(migrationsTable)} ORDER BY created_at DESC LIMIT 1`);
    const lastDbMigration = dbMigrations[0] ?? void 0;
    await session.transaction(async (tx) => {
      for (const migration of migrations) {
        if (!lastDbMigration || Number(lastDbMigration[2]) < migration.folderMillis) {
          for (const stmt of migration.sql) {
            await tx.run(sql2.raw(stmt));
          }
          await tx.run(sql2`INSERT INTO ${sql2.identifier(migrationsTable)} ("hash", "created_at") VALUES(${migration.hash}, ${migration.folderMillis})`);
        }
      }
    });
  }
};
var TypedQueryBuilder2 = class {
  static {
    __name(this, "TypedQueryBuilder");
  }
  static [entityKind2] = "TypedQueryBuilder";
  getSelectedFields() {
    return this._.selectedFields;
  }
};
var SQLiteSelectBuilder2 = class {
  static {
    __name(this, "SQLiteSelectBuilder");
  }
  static [entityKind2] = "SQLiteSelectBuilder";
  fields;
  session;
  dialect;
  withList;
  distinct;
  constructor(config2) {
    this.fields = config2.fields;
    this.session = config2.session;
    this.dialect = config2.dialect;
    this.withList = config2.withList;
    this.distinct = config2.distinct;
  }
  from(source) {
    const isPartialSelect = !!this.fields;
    let fields;
    if (this.fields) {
      fields = this.fields;
    } else if (is2(source, Subquery2)) {
      fields = Object.fromEntries(Object.keys(source._.selectedFields).map((key) => [key, source[key]]));
    } else if (is2(source, SQLiteViewBase2)) {
      fields = source[ViewBaseConfig2].selectedFields;
    } else if (is2(source, SQL2)) {
      fields = {};
    } else {
      fields = getTableColumns2(source);
    }
    return new SQLiteSelectBase2({
      table: source,
      fields,
      isPartialSelect,
      session: this.session,
      dialect: this.dialect,
      withList: this.withList,
      distinct: this.distinct
    });
  }
};
var SQLiteSelectQueryBuilderBase2 = class extends TypedQueryBuilder2 {
  static {
    __name(this, "SQLiteSelectQueryBuilderBase");
  }
  static [entityKind2] = "SQLiteSelectQueryBuilder";
  _;
  config;
  joinsNotNullableMap;
  tableName;
  isPartialSelect;
  session;
  dialect;
  cacheConfig = void 0;
  usedTables = /* @__PURE__ */ new Set();
  constructor({ table: table3, fields, isPartialSelect, session, dialect, withList, distinct }) {
    super();
    this.config = {
      withList,
      table: table3,
      fields: { ...fields },
      distinct,
      setOperators: []
    };
    this.isPartialSelect = isPartialSelect;
    this.session = session;
    this.dialect = dialect;
    this._ = {
      selectedFields: fields,
      config: this.config
    };
    this.tableName = getTableLikeName2(table3);
    this.joinsNotNullableMap = typeof this.tableName === "string" ? { [this.tableName]: true } : {};
    for (const item of extractUsedTable2(table3))
      this.usedTables.add(item);
  }
  getUsedTables() {
    return [...this.usedTables];
  }
  createJoin(joinType) {
    return (table3, on2) => {
      const baseTableName = this.tableName;
      const tableName = getTableLikeName2(table3);
      for (const item of extractUsedTable2(table3))
        this.usedTables.add(item);
      if (typeof tableName === "string" && this.config.joins?.some((join) => join.alias === tableName)) {
        throw new Error(`Alias "${tableName}" is already used in this query`);
      }
      if (!this.isPartialSelect) {
        if (Object.keys(this.joinsNotNullableMap).length === 1 && typeof baseTableName === "string") {
          this.config.fields = {
            [baseTableName]: this.config.fields
          };
        }
        if (typeof tableName === "string" && !is2(table3, SQL2)) {
          const selection = is2(table3, Subquery2) ? table3._.selectedFields : is2(table3, View2) ? table3[ViewBaseConfig2].selectedFields : table3[Table2.Symbol.Columns];
          this.config.fields[tableName] = selection;
        }
      }
      if (typeof on2 === "function") {
        on2 = on2(new Proxy(this.config.fields, new SelectionProxyHandler2({ sqlAliasedBehavior: "sql", sqlBehavior: "sql" })));
      }
      if (!this.config.joins) {
        this.config.joins = [];
      }
      this.config.joins.push({ on: on2, table: table3, joinType, alias: tableName });
      if (typeof tableName === "string") {
        switch (joinType) {
          case "left": {
            this.joinsNotNullableMap[tableName] = false;
            break;
          }
          case "right": {
            this.joinsNotNullableMap = Object.fromEntries(Object.entries(this.joinsNotNullableMap).map(([key]) => [key, false]));
            this.joinsNotNullableMap[tableName] = true;
            break;
          }
          case "cross":
          case "inner": {
            this.joinsNotNullableMap[tableName] = true;
            break;
          }
          case "full": {
            this.joinsNotNullableMap = Object.fromEntries(Object.entries(this.joinsNotNullableMap).map(([key]) => [key, false]));
            this.joinsNotNullableMap[tableName] = false;
            break;
          }
        }
      }
      return this;
    };
  }
  leftJoin = this.createJoin("left");
  rightJoin = this.createJoin("right");
  innerJoin = this.createJoin("inner");
  fullJoin = this.createJoin("full");
  crossJoin = this.createJoin("cross");
  createSetOperator(type, isAll) {
    return (rightSelection) => {
      const rightSelect = typeof rightSelection === "function" ? rightSelection(getSQLiteSetOperators2()) : rightSelection;
      if (!haveSameKeys2(this.getSelectedFields(), rightSelect.getSelectedFields())) {
        throw new Error("Set operator error (union / intersect / except): selected fields are not the same or are in a different order");
      }
      this.config.setOperators.push({ type, isAll, rightSelect });
      return this;
    };
  }
  union = this.createSetOperator("union", false);
  unionAll = this.createSetOperator("union", true);
  intersect = this.createSetOperator("intersect", false);
  except = this.createSetOperator("except", false);
  addSetOperators(setOperators) {
    this.config.setOperators.push(...setOperators);
    return this;
  }
  where(where) {
    if (typeof where === "function") {
      where = where(new Proxy(this.config.fields, new SelectionProxyHandler2({ sqlAliasedBehavior: "sql", sqlBehavior: "sql" })));
    }
    this.config.where = where;
    return this;
  }
  having(having) {
    if (typeof having === "function") {
      having = having(new Proxy(this.config.fields, new SelectionProxyHandler2({ sqlAliasedBehavior: "sql", sqlBehavior: "sql" })));
    }
    this.config.having = having;
    return this;
  }
  groupBy(...columns) {
    if (typeof columns[0] === "function") {
      const groupBy = columns[0](new Proxy(this.config.fields, new SelectionProxyHandler2({ sqlAliasedBehavior: "alias", sqlBehavior: "sql" })));
      this.config.groupBy = Array.isArray(groupBy) ? groupBy : [groupBy];
    } else {
      this.config.groupBy = columns;
    }
    return this;
  }
  orderBy(...columns) {
    if (typeof columns[0] === "function") {
      const orderBy = columns[0](new Proxy(this.config.fields, new SelectionProxyHandler2({ sqlAliasedBehavior: "alias", sqlBehavior: "sql" })));
      const orderByArray = Array.isArray(orderBy) ? orderBy : [orderBy];
      if (this.config.setOperators.length > 0) {
        this.config.setOperators.at(-1).orderBy = orderByArray;
      } else {
        this.config.orderBy = orderByArray;
      }
    } else {
      const orderByArray = columns;
      if (this.config.setOperators.length > 0) {
        this.config.setOperators.at(-1).orderBy = orderByArray;
      } else {
        this.config.orderBy = orderByArray;
      }
    }
    return this;
  }
  limit(limit2) {
    if (this.config.setOperators.length > 0) {
      this.config.setOperators.at(-1).limit = limit2;
    } else {
      this.config.limit = limit2;
    }
    return this;
  }
  offset(offset) {
    if (this.config.setOperators.length > 0) {
      this.config.setOperators.at(-1).offset = offset;
    } else {
      this.config.offset = offset;
    }
    return this;
  }
  getSQL() {
    return this.dialect.buildSelectQuery(this.config);
  }
  toSQL() {
    const { typings: _typings, ...rest } = this.dialect.sqlToQuery(this.getSQL());
    return rest;
  }
  as(alias) {
    const usedTables = [];
    usedTables.push(...extractUsedTable2(this.config.table));
    if (this.config.joins) {
      for (const it of this.config.joins)
        usedTables.push(...extractUsedTable2(it.table));
    }
    return new Proxy(new Subquery2(this.getSQL(), this.config.fields, alias, false, [...new Set(usedTables)]), new SelectionProxyHandler2({ alias, sqlAliasedBehavior: "alias", sqlBehavior: "error" }));
  }
  getSelectedFields() {
    return new Proxy(this.config.fields, new SelectionProxyHandler2({ alias: this.tableName, sqlAliasedBehavior: "alias", sqlBehavior: "error" }));
  }
  $dynamic() {
    return this;
  }
};
var SQLiteSelectBase2 = class extends SQLiteSelectQueryBuilderBase2 {
  static {
    __name(this, "SQLiteSelectBase");
  }
  static [entityKind2] = "SQLiteSelect";
  _prepare(isOneTimeQuery = true) {
    if (!this.session) {
      throw new Error("Cannot execute a query on a query builder. Please use a database instance instead.");
    }
    const fieldsList = orderSelectedFields2(this.config.fields);
    const query = this.session[isOneTimeQuery ? "prepareOneTimeQuery" : "prepareQuery"](this.dialect.sqlToQuery(this.getSQL()), fieldsList, "all", true, void 0, {
      type: "select",
      tables: [...this.usedTables]
    }, this.cacheConfig);
    query.joinsNotNullableMap = this.joinsNotNullableMap;
    return query;
  }
  $withCache(config2) {
    this.cacheConfig = config2 === void 0 ? { config: {}, enable: true, autoInvalidate: true } : config2 === false ? { enable: false } : { enable: true, autoInvalidate: true, ...config2 };
    return this;
  }
  prepare() {
    return this._prepare(false);
  }
  run = /* @__PURE__ */ __name((placeholderValues) => {
    return this._prepare().run(placeholderValues);
  }, "run");
  all = /* @__PURE__ */ __name((placeholderValues) => {
    return this._prepare().all(placeholderValues);
  }, "all");
  get = /* @__PURE__ */ __name((placeholderValues) => {
    return this._prepare().get(placeholderValues);
  }, "get");
  values = /* @__PURE__ */ __name((placeholderValues) => {
    return this._prepare().values(placeholderValues);
  }, "values");
  async execute() {
    return this.all();
  }
};
applyMixins2(SQLiteSelectBase2, [QueryPromise2]);
function createSetOperator2(type, isAll) {
  return (leftSelect, rightSelect, ...restSelects) => {
    const setOperators = [rightSelect, ...restSelects].map((select) => ({
      type,
      isAll,
      rightSelect: select
    }));
    for (const setOperator of setOperators) {
      if (!haveSameKeys2(leftSelect.getSelectedFields(), setOperator.rightSelect.getSelectedFields())) {
        throw new Error("Set operator error (union / intersect / except): selected fields are not the same or are in a different order");
      }
    }
    return leftSelect.addSetOperators(setOperators);
  };
}
__name(createSetOperator2, "createSetOperator");
var getSQLiteSetOperators2 = /* @__PURE__ */ __name(() => ({
  union: union2,
  unionAll: unionAll2,
  intersect: intersect2,
  except: except2
}), "getSQLiteSetOperators");
var union2 = createSetOperator2("union", false);
var unionAll2 = createSetOperator2("union", true);
var intersect2 = createSetOperator2("intersect", false);
var except2 = createSetOperator2("except", false);
var QueryBuilder2 = class {
  static {
    __name(this, "QueryBuilder");
  }
  static [entityKind2] = "SQLiteQueryBuilder";
  dialect;
  dialectConfig;
  constructor(dialect) {
    this.dialect = is2(dialect, SQLiteDialect2) ? dialect : void 0;
    this.dialectConfig = is2(dialect, SQLiteDialect2) ? void 0 : dialect;
  }
  $with = /* @__PURE__ */ __name((alias, selection) => {
    const queryBuilder = this;
    const as = /* @__PURE__ */ __name((qb) => {
      if (typeof qb === "function") {
        qb = qb(queryBuilder);
      }
      return new Proxy(new WithSubquery2(qb.getSQL(), selection ?? ("getSelectedFields" in qb ? qb.getSelectedFields() ?? {} : {}), alias, true), new SelectionProxyHandler2({ alias, sqlAliasedBehavior: "alias", sqlBehavior: "error" }));
    }, "as");
    return { as };
  }, "$with");
  with(...queries) {
    const self = this;
    function select(fields) {
      return new SQLiteSelectBuilder2({
        fields: fields ?? void 0,
        session: void 0,
        dialect: self.getDialect(),
        withList: queries
      });
    }
    __name(select, "select");
    function selectDistinct(fields) {
      return new SQLiteSelectBuilder2({
        fields: fields ?? void 0,
        session: void 0,
        dialect: self.getDialect(),
        withList: queries,
        distinct: true
      });
    }
    __name(selectDistinct, "selectDistinct");
    return { select, selectDistinct };
  }
  select(fields) {
    return new SQLiteSelectBuilder2({ fields: fields ?? void 0, session: void 0, dialect: this.getDialect() });
  }
  selectDistinct(fields) {
    return new SQLiteSelectBuilder2({
      fields: fields ?? void 0,
      session: void 0,
      dialect: this.getDialect(),
      distinct: true
    });
  }
  getDialect() {
    if (!this.dialect) {
      this.dialect = new SQLiteSyncDialect2(this.dialectConfig);
    }
    return this.dialect;
  }
};
var SQLiteInsertBuilder2 = class {
  static {
    __name(this, "SQLiteInsertBuilder");
  }
  constructor(table3, session, dialect, withList) {
    this.table = table3;
    this.session = session;
    this.dialect = dialect;
    this.withList = withList;
  }
  static [entityKind2] = "SQLiteInsertBuilder";
  values(values) {
    values = Array.isArray(values) ? values : [values];
    if (values.length === 0) {
      throw new Error("values() must be called with at least one value");
    }
    const mappedValues = values.map((entry) => {
      const result = {};
      const cols = this.table[Table2.Symbol.Columns];
      for (const colKey of Object.keys(entry)) {
        const colValue = entry[colKey];
        result[colKey] = is2(colValue, SQL2) ? colValue : new Param2(colValue, cols[colKey]);
      }
      return result;
    });
    return new SQLiteInsertBase2(this.table, mappedValues, this.session, this.dialect, this.withList);
  }
  select(selectQuery) {
    const select = typeof selectQuery === "function" ? selectQuery(new QueryBuilder2()) : selectQuery;
    if (!is2(select, SQL2) && !haveSameKeys2(this.table[Columns2], select._.selectedFields)) {
      throw new Error("Insert select error: selected fields are not the same or are in a different order compared to the table definition");
    }
    return new SQLiteInsertBase2(this.table, select, this.session, this.dialect, this.withList, true);
  }
};
var SQLiteInsertBase2 = class extends QueryPromise2 {
  static {
    __name(this, "SQLiteInsertBase");
  }
  constructor(table3, values, session, dialect, withList, select) {
    super();
    this.session = session;
    this.dialect = dialect;
    this.config = { table: table3, values, withList, select };
  }
  static [entityKind2] = "SQLiteInsert";
  config;
  returning(fields = this.config.table[SQLiteTable2.Symbol.Columns]) {
    this.config.returning = orderSelectedFields2(fields);
    return this;
  }
  onConflictDoNothing(config2 = {}) {
    if (!this.config.onConflict)
      this.config.onConflict = [];
    if (config2.target === void 0) {
      this.config.onConflict.push(sql2` on conflict do nothing`);
    } else {
      const targetSql = Array.isArray(config2.target) ? sql2`${config2.target}` : sql2`${[config2.target]}`;
      const whereSql = config2.where ? sql2` where ${config2.where}` : sql2``;
      this.config.onConflict.push(sql2` on conflict ${targetSql} do nothing${whereSql}`);
    }
    return this;
  }
  onConflictDoUpdate(config2) {
    if (config2.where && (config2.targetWhere || config2.setWhere)) {
      throw new Error('You cannot use both "where" and "targetWhere"/"setWhere" at the same time - "where" is deprecated, use "targetWhere" or "setWhere" instead.');
    }
    if (!this.config.onConflict)
      this.config.onConflict = [];
    const whereSql = config2.where ? sql2` where ${config2.where}` : void 0;
    const targetWhereSql = config2.targetWhere ? sql2` where ${config2.targetWhere}` : void 0;
    const setWhereSql = config2.setWhere ? sql2` where ${config2.setWhere}` : void 0;
    const targetSql = Array.isArray(config2.target) ? sql2`${config2.target}` : sql2`${[config2.target]}`;
    const setSql = this.dialect.buildUpdateSet(this.config.table, mapUpdateSet2(this.config.table, config2.set));
    this.config.onConflict.push(sql2` on conflict ${targetSql}${targetWhereSql} do update set ${setSql}${whereSql}${setWhereSql}`);
    return this;
  }
  getSQL() {
    return this.dialect.buildInsertQuery(this.config);
  }
  toSQL() {
    const { typings: _typings, ...rest } = this.dialect.sqlToQuery(this.getSQL());
    return rest;
  }
  _prepare(isOneTimeQuery = true) {
    return this.session[isOneTimeQuery ? "prepareOneTimeQuery" : "prepareQuery"](this.dialect.sqlToQuery(this.getSQL()), this.config.returning, this.config.returning ? "all" : "run", true, void 0, {
      type: "insert",
      tables: extractUsedTable2(this.config.table)
    });
  }
  prepare() {
    return this._prepare(false);
  }
  run = /* @__PURE__ */ __name((placeholderValues) => {
    return this._prepare().run(placeholderValues);
  }, "run");
  all = /* @__PURE__ */ __name((placeholderValues) => {
    return this._prepare().all(placeholderValues);
  }, "all");
  get = /* @__PURE__ */ __name((placeholderValues) => {
    return this._prepare().get(placeholderValues);
  }, "get");
  values = /* @__PURE__ */ __name((placeholderValues) => {
    return this._prepare().values(placeholderValues);
  }, "values");
  async execute() {
    return this.config.returning ? this.all() : this.run();
  }
  $dynamic() {
    return this;
  }
};
var SQLiteUpdateBuilder2 = class {
  static {
    __name(this, "SQLiteUpdateBuilder");
  }
  constructor(table3, session, dialect, withList) {
    this.table = table3;
    this.session = session;
    this.dialect = dialect;
    this.withList = withList;
  }
  static [entityKind2] = "SQLiteUpdateBuilder";
  set(values) {
    return new SQLiteUpdateBase2(this.table, mapUpdateSet2(this.table, values), this.session, this.dialect, this.withList);
  }
};
var SQLiteUpdateBase2 = class extends QueryPromise2 {
  static {
    __name(this, "SQLiteUpdateBase");
  }
  constructor(table3, set, session, dialect, withList) {
    super();
    this.session = session;
    this.dialect = dialect;
    this.config = { set, table: table3, withList, joins: [] };
  }
  static [entityKind2] = "SQLiteUpdate";
  config;
  from(source) {
    this.config.from = source;
    return this;
  }
  createJoin(joinType) {
    return (table3, on2) => {
      const tableName = getTableLikeName2(table3);
      if (typeof tableName === "string" && this.config.joins.some((join) => join.alias === tableName)) {
        throw new Error(`Alias "${tableName}" is already used in this query`);
      }
      if (typeof on2 === "function") {
        const from = this.config.from ? is2(table3, SQLiteTable2) ? table3[Table2.Symbol.Columns] : is2(table3, Subquery2) ? table3._.selectedFields : is2(table3, SQLiteViewBase2) ? table3[ViewBaseConfig2].selectedFields : void 0 : void 0;
        on2 = on2(new Proxy(this.config.table[Table2.Symbol.Columns], new SelectionProxyHandler2({ sqlAliasedBehavior: "sql", sqlBehavior: "sql" })), from && new Proxy(from, new SelectionProxyHandler2({ sqlAliasedBehavior: "sql", sqlBehavior: "sql" })));
      }
      this.config.joins.push({ on: on2, table: table3, joinType, alias: tableName });
      return this;
    };
  }
  leftJoin = this.createJoin("left");
  rightJoin = this.createJoin("right");
  innerJoin = this.createJoin("inner");
  fullJoin = this.createJoin("full");
  where(where) {
    this.config.where = where;
    return this;
  }
  orderBy(...columns) {
    if (typeof columns[0] === "function") {
      const orderBy = columns[0](new Proxy(this.config.table[Table2.Symbol.Columns], new SelectionProxyHandler2({ sqlAliasedBehavior: "alias", sqlBehavior: "sql" })));
      const orderByArray = Array.isArray(orderBy) ? orderBy : [orderBy];
      this.config.orderBy = orderByArray;
    } else {
      const orderByArray = columns;
      this.config.orderBy = orderByArray;
    }
    return this;
  }
  limit(limit2) {
    this.config.limit = limit2;
    return this;
  }
  returning(fields = this.config.table[SQLiteTable2.Symbol.Columns]) {
    this.config.returning = orderSelectedFields2(fields);
    return this;
  }
  getSQL() {
    return this.dialect.buildUpdateQuery(this.config);
  }
  toSQL() {
    const { typings: _typings, ...rest } = this.dialect.sqlToQuery(this.getSQL());
    return rest;
  }
  _prepare(isOneTimeQuery = true) {
    return this.session[isOneTimeQuery ? "prepareOneTimeQuery" : "prepareQuery"](this.dialect.sqlToQuery(this.getSQL()), this.config.returning, this.config.returning ? "all" : "run", true, void 0, {
      type: "insert",
      tables: extractUsedTable2(this.config.table)
    });
  }
  prepare() {
    return this._prepare(false);
  }
  run = /* @__PURE__ */ __name((placeholderValues) => {
    return this._prepare().run(placeholderValues);
  }, "run");
  all = /* @__PURE__ */ __name((placeholderValues) => {
    return this._prepare().all(placeholderValues);
  }, "all");
  get = /* @__PURE__ */ __name((placeholderValues) => {
    return this._prepare().get(placeholderValues);
  }, "get");
  values = /* @__PURE__ */ __name((placeholderValues) => {
    return this._prepare().values(placeholderValues);
  }, "values");
  async execute() {
    return this.config.returning ? this.all() : this.run();
  }
  $dynamic() {
    return this;
  }
};
var SQLiteCountBuilder2 = class _SQLiteCountBuilder extends SQL2 {
  static {
    __name(this, "SQLiteCountBuilder");
  }
  constructor(params) {
    super(_SQLiteCountBuilder.buildEmbeddedCount(params.source, params.filters).queryChunks);
    this.params = params;
    this.session = params.session;
    this.sql = _SQLiteCountBuilder.buildCount(params.source, params.filters);
  }
  sql;
  static [entityKind2] = "SQLiteCountBuilderAsync";
  [Symbol.toStringTag] = "SQLiteCountBuilderAsync";
  session;
  static buildEmbeddedCount(source, filters) {
    return sql2`(select count(*) from ${source}${sql2.raw(" where ").if(filters)}${filters})`;
  }
  static buildCount(source, filters) {
    return sql2`select count(*) from ${source}${sql2.raw(" where ").if(filters)}${filters}`;
  }
  then(onfulfilled, onrejected) {
    return Promise.resolve(this.session.count(this.sql)).then(onfulfilled, onrejected);
  }
  catch(onRejected) {
    return this.then(void 0, onRejected);
  }
  finally(onFinally) {
    return this.then((value) => {
      onFinally?.();
      return value;
    }, (reason) => {
      onFinally?.();
      throw reason;
    });
  }
};
var RelationalQueryBuilder2 = class {
  static {
    __name(this, "RelationalQueryBuilder");
  }
  constructor(mode, fullSchema, schema, tableNamesMap, table3, tableConfig, dialect, session) {
    this.mode = mode;
    this.fullSchema = fullSchema;
    this.schema = schema;
    this.tableNamesMap = tableNamesMap;
    this.table = table3;
    this.tableConfig = tableConfig;
    this.dialect = dialect;
    this.session = session;
  }
  static [entityKind2] = "SQLiteAsyncRelationalQueryBuilder";
  findMany(config2) {
    return this.mode === "sync" ? new SQLiteSyncRelationalQuery2(this.fullSchema, this.schema, this.tableNamesMap, this.table, this.tableConfig, this.dialect, this.session, config2 ? config2 : {}, "many") : new SQLiteRelationalQuery2(this.fullSchema, this.schema, this.tableNamesMap, this.table, this.tableConfig, this.dialect, this.session, config2 ? config2 : {}, "many");
  }
  findFirst(config2) {
    return this.mode === "sync" ? new SQLiteSyncRelationalQuery2(this.fullSchema, this.schema, this.tableNamesMap, this.table, this.tableConfig, this.dialect, this.session, config2 ? { ...config2, limit: 1 } : { limit: 1 }, "first") : new SQLiteRelationalQuery2(this.fullSchema, this.schema, this.tableNamesMap, this.table, this.tableConfig, this.dialect, this.session, config2 ? { ...config2, limit: 1 } : { limit: 1 }, "first");
  }
};
var SQLiteRelationalQuery2 = class extends QueryPromise2 {
  static {
    __name(this, "SQLiteRelationalQuery");
  }
  constructor(fullSchema, schema, tableNamesMap, table3, tableConfig, dialect, session, config2, mode) {
    super();
    this.fullSchema = fullSchema;
    this.schema = schema;
    this.tableNamesMap = tableNamesMap;
    this.table = table3;
    this.tableConfig = tableConfig;
    this.dialect = dialect;
    this.session = session;
    this.config = config2;
    this.mode = mode;
  }
  static [entityKind2] = "SQLiteAsyncRelationalQuery";
  mode;
  getSQL() {
    return this.dialect.buildRelationalQuery({
      fullSchema: this.fullSchema,
      schema: this.schema,
      tableNamesMap: this.tableNamesMap,
      table: this.table,
      tableConfig: this.tableConfig,
      queryConfig: this.config,
      tableAlias: this.tableConfig.tsName
    }).sql;
  }
  _prepare(isOneTimeQuery = false) {
    const { query, builtQuery } = this._toSQL();
    return this.session[isOneTimeQuery ? "prepareOneTimeQuery" : "prepareQuery"](builtQuery, void 0, this.mode === "first" ? "get" : "all", true, (rawRows, mapColumnValue) => {
      const rows = rawRows.map((row) => mapRelationalRow2(this.schema, this.tableConfig, row, query.selection, mapColumnValue));
      if (this.mode === "first") {
        return rows[0];
      }
      return rows;
    });
  }
  prepare() {
    return this._prepare(false);
  }
  _toSQL() {
    const query = this.dialect.buildRelationalQuery({
      fullSchema: this.fullSchema,
      schema: this.schema,
      tableNamesMap: this.tableNamesMap,
      table: this.table,
      tableConfig: this.tableConfig,
      queryConfig: this.config,
      tableAlias: this.tableConfig.tsName
    });
    const builtQuery = this.dialect.sqlToQuery(query.sql);
    return { query, builtQuery };
  }
  toSQL() {
    return this._toSQL().builtQuery;
  }
  executeRaw() {
    if (this.mode === "first") {
      return this._prepare(false).get();
    }
    return this._prepare(false).all();
  }
  async execute() {
    return this.executeRaw();
  }
};
var SQLiteSyncRelationalQuery2 = class extends SQLiteRelationalQuery2 {
  static {
    __name(this, "SQLiteSyncRelationalQuery");
  }
  static [entityKind2] = "SQLiteSyncRelationalQuery";
  sync() {
    return this.executeRaw();
  }
};
var SQLiteRaw2 = class extends QueryPromise2 {
  static {
    __name(this, "SQLiteRaw");
  }
  constructor(execute, getSQL, action, dialect, mapBatchResult) {
    super();
    this.execute = execute;
    this.getSQL = getSQL;
    this.dialect = dialect;
    this.mapBatchResult = mapBatchResult;
    this.config = { action };
  }
  static [entityKind2] = "SQLiteRaw";
  config;
  getQuery() {
    return { ...this.dialect.sqlToQuery(this.getSQL()), method: this.config.action };
  }
  mapResult(result, isFromBatch) {
    return isFromBatch ? this.mapBatchResult(result) : result;
  }
  _prepare() {
    return this;
  }
  isResponseInArrayMode() {
    return false;
  }
};
var BaseSQLiteDatabase2 = class {
  static {
    __name(this, "BaseSQLiteDatabase");
  }
  constructor(resultKind, dialect, session, schema) {
    this.resultKind = resultKind;
    this.dialect = dialect;
    this.session = session;
    this._ = schema ? {
      schema: schema.schema,
      fullSchema: schema.fullSchema,
      tableNamesMap: schema.tableNamesMap
    } : {
      schema: void 0,
      fullSchema: {},
      tableNamesMap: {}
    };
    this.query = {};
    const query = this.query;
    if (this._.schema) {
      for (const [tableName, columns] of Object.entries(this._.schema)) {
        query[tableName] = new RelationalQueryBuilder2(resultKind, schema.fullSchema, this._.schema, this._.tableNamesMap, schema.fullSchema[tableName], columns, dialect, session);
      }
    }
    this.$cache = { invalidate: /* @__PURE__ */ __name(async (_params) => {
    }, "invalidate") };
  }
  static [entityKind2] = "BaseSQLiteDatabase";
  query;
  $with = /* @__PURE__ */ __name((alias, selection) => {
    const self = this;
    const as = /* @__PURE__ */ __name((qb) => {
      if (typeof qb === "function") {
        qb = qb(new QueryBuilder2(self.dialect));
      }
      return new Proxy(new WithSubquery2(qb.getSQL(), selection ?? ("getSelectedFields" in qb ? qb.getSelectedFields() ?? {} : {}), alias, true), new SelectionProxyHandler2({ alias, sqlAliasedBehavior: "alias", sqlBehavior: "error" }));
    }, "as");
    return { as };
  }, "$with");
  $count(source, filters) {
    return new SQLiteCountBuilder2({ source, filters, session: this.session });
  }
  with(...queries) {
    const self = this;
    function select(fields) {
      return new SQLiteSelectBuilder2({
        fields: fields ?? void 0,
        session: self.session,
        dialect: self.dialect,
        withList: queries
      });
    }
    __name(select, "select");
    function selectDistinct(fields) {
      return new SQLiteSelectBuilder2({
        fields: fields ?? void 0,
        session: self.session,
        dialect: self.dialect,
        withList: queries,
        distinct: true
      });
    }
    __name(selectDistinct, "selectDistinct");
    function update(table3) {
      return new SQLiteUpdateBuilder2(table3, self.session, self.dialect, queries);
    }
    __name(update, "update");
    function insert(into) {
      return new SQLiteInsertBuilder2(into, self.session, self.dialect, queries);
    }
    __name(insert, "insert");
    function delete_(from) {
      return new SQLiteDeleteBase2(from, self.session, self.dialect, queries);
    }
    __name(delete_, "delete_");
    return { select, selectDistinct, update, insert, delete: delete_ };
  }
  select(fields) {
    return new SQLiteSelectBuilder2({ fields: fields ?? void 0, session: this.session, dialect: this.dialect });
  }
  selectDistinct(fields) {
    return new SQLiteSelectBuilder2({
      fields: fields ?? void 0,
      session: this.session,
      dialect: this.dialect,
      distinct: true
    });
  }
  update(table3) {
    return new SQLiteUpdateBuilder2(table3, this.session, this.dialect);
  }
  $cache;
  insert(into) {
    return new SQLiteInsertBuilder2(into, this.session, this.dialect);
  }
  delete(from) {
    return new SQLiteDeleteBase2(from, this.session, this.dialect);
  }
  run(query) {
    const sequel = typeof query === "string" ? sql2.raw(query) : query.getSQL();
    if (this.resultKind === "async") {
      return new SQLiteRaw2(async () => this.session.run(sequel), () => sequel, "run", this.dialect, this.session.extractRawRunValueFromBatchResult.bind(this.session));
    }
    return this.session.run(sequel);
  }
  all(query) {
    const sequel = typeof query === "string" ? sql2.raw(query) : query.getSQL();
    if (this.resultKind === "async") {
      return new SQLiteRaw2(async () => this.session.all(sequel), () => sequel, "all", this.dialect, this.session.extractRawAllValueFromBatchResult.bind(this.session));
    }
    return this.session.all(sequel);
  }
  get(query) {
    const sequel = typeof query === "string" ? sql2.raw(query) : query.getSQL();
    if (this.resultKind === "async") {
      return new SQLiteRaw2(async () => this.session.get(sequel), () => sequel, "get", this.dialect, this.session.extractRawGetValueFromBatchResult.bind(this.session));
    }
    return this.session.get(sequel);
  }
  values(query) {
    const sequel = typeof query === "string" ? sql2.raw(query) : query.getSQL();
    if (this.resultKind === "async") {
      return new SQLiteRaw2(async () => this.session.values(sequel), () => sequel, "values", this.dialect, this.session.extractRawValuesValueFromBatchResult.bind(this.session));
    }
    return this.session.values(sequel);
  }
  transaction(transaction, config2) {
    return this.session.transaction(transaction, config2);
  }
};
var Cache3 = class {
  static {
    __name(this, "Cache");
  }
  static [entityKind2] = "Cache";
};
var NoopCache2 = class extends Cache3 {
  static {
    __name(this, "NoopCache");
  }
  strategy() {
    return "all";
  }
  static [entityKind2] = "NoopCache";
  async get(_key) {
    return;
  }
  async put(_hashedQuery, _response, _tables, _config) {
  }
  async onMutate(_params) {
  }
};
async function hashQuery2(sql22, params) {
  const dataToHash = `${sql22}-${JSON.stringify(params)}`;
  const encoder2 = new TextEncoder();
  const data = encoder2.encode(dataToHash);
  const hashBuffer = await crypto.subtle.digest("SHA-256", data);
  const hashArray = [...new Uint8Array(hashBuffer)];
  const hashHex = hashArray.map((b) => b.toString(16).padStart(2, "0")).join("");
  return hashHex;
}
__name(hashQuery2, "hashQuery");
var ExecuteResultSync2 = class extends QueryPromise2 {
  static {
    __name(this, "ExecuteResultSync");
  }
  constructor(resultCb) {
    super();
    this.resultCb = resultCb;
  }
  static [entityKind2] = "ExecuteResultSync";
  async execute() {
    return this.resultCb();
  }
  sync() {
    return this.resultCb();
  }
};
var SQLitePreparedQuery2 = class {
  static {
    __name(this, "SQLitePreparedQuery");
  }
  constructor(mode, executeMethod, query, cache2, queryMetadata, cacheConfig) {
    this.mode = mode;
    this.executeMethod = executeMethod;
    this.query = query;
    this.cache = cache2;
    this.queryMetadata = queryMetadata;
    this.cacheConfig = cacheConfig;
    if (cache2 && cache2.strategy() === "all" && cacheConfig === void 0) {
      this.cacheConfig = { enable: true, autoInvalidate: true };
    }
    if (!this.cacheConfig?.enable) {
      this.cacheConfig = void 0;
    }
  }
  static [entityKind2] = "PreparedQuery";
  joinsNotNullableMap;
  async queryWithCache(queryString, params, query) {
    if (this.cache === void 0 || is2(this.cache, NoopCache2) || this.queryMetadata === void 0) {
      try {
        return await query();
      } catch (e) {
        throw new DrizzleQueryError2(queryString, params, e);
      }
    }
    if (this.cacheConfig && !this.cacheConfig.enable) {
      try {
        return await query();
      } catch (e) {
        throw new DrizzleQueryError2(queryString, params, e);
      }
    }
    if ((this.queryMetadata.type === "insert" || this.queryMetadata.type === "update" || this.queryMetadata.type === "delete") && this.queryMetadata.tables.length > 0) {
      try {
        const [res] = await Promise.all([
          query(),
          this.cache.onMutate({ tables: this.queryMetadata.tables })
        ]);
        return res;
      } catch (e) {
        throw new DrizzleQueryError2(queryString, params, e);
      }
    }
    if (!this.cacheConfig) {
      try {
        return await query();
      } catch (e) {
        throw new DrizzleQueryError2(queryString, params, e);
      }
    }
    if (this.queryMetadata.type === "select") {
      const fromCache = await this.cache.get(this.cacheConfig.tag ?? await hashQuery2(queryString, params), this.queryMetadata.tables, this.cacheConfig.tag !== void 0, this.cacheConfig.autoInvalidate);
      if (fromCache === void 0) {
        let result;
        try {
          result = await query();
        } catch (e) {
          throw new DrizzleQueryError2(queryString, params, e);
        }
        await this.cache.put(this.cacheConfig.tag ?? await hashQuery2(queryString, params), result, this.cacheConfig.autoInvalidate ? this.queryMetadata.tables : [], this.cacheConfig.tag !== void 0, this.cacheConfig.config);
        return result;
      }
      return fromCache;
    }
    try {
      return await query();
    } catch (e) {
      throw new DrizzleQueryError2(queryString, params, e);
    }
  }
  getQuery() {
    return this.query;
  }
  mapRunResult(result, _isFromBatch) {
    return result;
  }
  mapAllResult(_result, _isFromBatch) {
    throw new Error("Not implemented");
  }
  mapGetResult(_result, _isFromBatch) {
    throw new Error("Not implemented");
  }
  execute(placeholderValues) {
    if (this.mode === "async") {
      return this[this.executeMethod](placeholderValues);
    }
    return new ExecuteResultSync2(() => this[this.executeMethod](placeholderValues));
  }
  mapResult(response, isFromBatch) {
    switch (this.executeMethod) {
      case "run": {
        return this.mapRunResult(response, isFromBatch);
      }
      case "all": {
        return this.mapAllResult(response, isFromBatch);
      }
      case "get": {
        return this.mapGetResult(response, isFromBatch);
      }
    }
  }
};
var SQLiteSession2 = class {
  static {
    __name(this, "SQLiteSession");
  }
  constructor(dialect) {
    this.dialect = dialect;
  }
  static [entityKind2] = "SQLiteSession";
  prepareOneTimeQuery(query, fields, executeMethod, isResponseInArrayMode, customResultMapper, queryMetadata, cacheConfig) {
    return this.prepareQuery(query, fields, executeMethod, isResponseInArrayMode, customResultMapper, queryMetadata, cacheConfig);
  }
  run(query) {
    const staticQuery = this.dialect.sqlToQuery(query);
    try {
      return this.prepareOneTimeQuery(staticQuery, void 0, "run", false).run();
    } catch (err) {
      throw new DrizzleError2({ cause: err, message: `Failed to run the query '${staticQuery.sql}'` });
    }
  }
  extractRawRunValueFromBatchResult(result) {
    return result;
  }
  all(query) {
    return this.prepareOneTimeQuery(this.dialect.sqlToQuery(query), void 0, "run", false).all();
  }
  extractRawAllValueFromBatchResult(_result) {
    throw new Error("Not implemented");
  }
  get(query) {
    return this.prepareOneTimeQuery(this.dialect.sqlToQuery(query), void 0, "run", false).get();
  }
  extractRawGetValueFromBatchResult(_result) {
    throw new Error("Not implemented");
  }
  values(query) {
    return this.prepareOneTimeQuery(this.dialect.sqlToQuery(query), void 0, "run", false).values();
  }
  async count(sql22) {
    const result = await this.values(sql22);
    return result[0][0];
  }
  extractRawValuesValueFromBatchResult(_result) {
    throw new Error("Not implemented");
  }
};
var SQLiteTransaction2 = class extends BaseSQLiteDatabase2 {
  static {
    __name(this, "SQLiteTransaction");
  }
  constructor(resultType, dialect, session, schema, nestedIndex = 0) {
    super(resultType, dialect, session, schema);
    this.schema = schema;
    this.nestedIndex = nestedIndex;
  }
  static [entityKind2] = "SQLiteTransaction";
  rollback() {
    throw new TransactionRollbackError2();
  }
};
var SQLiteD1Session2 = class extends SQLiteSession2 {
  static {
    __name(this, "SQLiteD1Session");
  }
  constructor(client, dialect, schema, options = {}) {
    super(dialect);
    this.client = client;
    this.schema = schema;
    this.options = options;
    this.logger = options.logger ?? new NoopLogger2();
    this.cache = options.cache ?? new NoopCache2();
  }
  static [entityKind2] = "SQLiteD1Session";
  logger;
  cache;
  prepareQuery(query, fields, executeMethod, isResponseInArrayMode, customResultMapper, queryMetadata, cacheConfig) {
    const stmt = this.client.prepare(query.sql);
    return new D1PreparedQuery2(stmt, query, this.logger, this.cache, queryMetadata, cacheConfig, fields, executeMethod, isResponseInArrayMode, customResultMapper);
  }
  async batch(queries) {
    const preparedQueries = [];
    const builtQueries = [];
    for (const query of queries) {
      const preparedQuery = query._prepare();
      const builtQuery = preparedQuery.getQuery();
      preparedQueries.push(preparedQuery);
      if (builtQuery.params.length > 0) {
        builtQueries.push(preparedQuery.stmt.bind(...builtQuery.params));
      } else {
        const builtQuery2 = preparedQuery.getQuery();
        builtQueries.push(this.client.prepare(builtQuery2.sql).bind(...builtQuery2.params));
      }
    }
    const batchResults = await this.client.batch(builtQueries);
    return batchResults.map((result, i) => preparedQueries[i].mapResult(result, true));
  }
  extractRawAllValueFromBatchResult(result) {
    return result.results;
  }
  extractRawGetValueFromBatchResult(result) {
    return result.results[0];
  }
  extractRawValuesValueFromBatchResult(result) {
    return d1ToRawMapping2(result.results);
  }
  async transaction(transaction, config2) {
    const tx = new D1Transaction2("async", this.dialect, this, this.schema);
    await this.run(sql2.raw(`begin${config2?.behavior ? " " + config2.behavior : ""}`));
    try {
      const result = await transaction(tx);
      await this.run(sql2`commit`);
      return result;
    } catch (err) {
      await this.run(sql2`rollback`);
      throw err;
    }
  }
};
var D1Transaction2 = class _D1Transaction extends SQLiteTransaction2 {
  static {
    __name(this, "D1Transaction");
  }
  static [entityKind2] = "D1Transaction";
  async transaction(transaction) {
    const savepointName = `sp${this.nestedIndex}`;
    const tx = new _D1Transaction("async", this.dialect, this.session, this.schema, this.nestedIndex + 1);
    await this.session.run(sql2.raw(`savepoint ${savepointName}`));
    try {
      const result = await transaction(tx);
      await this.session.run(sql2.raw(`release savepoint ${savepointName}`));
      return result;
    } catch (err) {
      await this.session.run(sql2.raw(`rollback to savepoint ${savepointName}`));
      throw err;
    }
  }
};
function d1ToRawMapping2(results) {
  const rows = [];
  for (const row of results) {
    const entry = Object.keys(row).map((k) => row[k]);
    rows.push(entry);
  }
  return rows;
}
__name(d1ToRawMapping2, "d1ToRawMapping");
var D1PreparedQuery2 = class extends SQLitePreparedQuery2 {
  static {
    __name(this, "D1PreparedQuery");
  }
  constructor(stmt, query, logger, cache2, queryMetadata, cacheConfig, fields, executeMethod, _isResponseInArrayMode, customResultMapper) {
    super("async", executeMethod, query, cache2, queryMetadata, cacheConfig);
    this.logger = logger;
    this._isResponseInArrayMode = _isResponseInArrayMode;
    this.customResultMapper = customResultMapper;
    this.fields = fields;
    this.stmt = stmt;
  }
  static [entityKind2] = "D1PreparedQuery";
  customResultMapper;
  fields;
  stmt;
  async run(placeholderValues) {
    const params = fillPlaceholders2(this.query.params, placeholderValues ?? {});
    this.logger.logQuery(this.query.sql, params);
    return await this.queryWithCache(this.query.sql, params, async () => {
      return this.stmt.bind(...params).run();
    });
  }
  async all(placeholderValues) {
    const { fields, query, logger, stmt, customResultMapper } = this;
    if (!fields && !customResultMapper) {
      const params = fillPlaceholders2(query.params, placeholderValues ?? {});
      logger.logQuery(query.sql, params);
      return await this.queryWithCache(query.sql, params, async () => {
        return stmt.bind(...params).all().then(({ results }) => this.mapAllResult(results));
      });
    }
    const rows = await this.values(placeholderValues);
    return this.mapAllResult(rows);
  }
  mapAllResult(rows, isFromBatch) {
    if (isFromBatch) {
      rows = d1ToRawMapping2(rows.results);
    }
    if (!this.fields && !this.customResultMapper) {
      return rows;
    }
    if (this.customResultMapper) {
      return this.customResultMapper(rows);
    }
    return rows.map((row) => mapResultRow2(this.fields, row, this.joinsNotNullableMap));
  }
  async get(placeholderValues) {
    const { fields, joinsNotNullableMap, query, logger, stmt, customResultMapper } = this;
    if (!fields && !customResultMapper) {
      const params = fillPlaceholders2(query.params, placeholderValues ?? {});
      logger.logQuery(query.sql, params);
      return await this.queryWithCache(query.sql, params, async () => {
        return stmt.bind(...params).all().then(({ results }) => results[0]);
      });
    }
    const rows = await this.values(placeholderValues);
    if (!rows[0]) {
      return;
    }
    if (customResultMapper) {
      return customResultMapper(rows);
    }
    return mapResultRow2(fields, rows[0], joinsNotNullableMap);
  }
  mapGetResult(result, isFromBatch) {
    if (isFromBatch) {
      result = d1ToRawMapping2(result.results)[0];
    }
    if (!this.fields && !this.customResultMapper) {
      return result;
    }
    if (this.customResultMapper) {
      return this.customResultMapper([result]);
    }
    return mapResultRow2(this.fields, result, this.joinsNotNullableMap);
  }
  async values(placeholderValues) {
    const params = fillPlaceholders2(this.query.params, placeholderValues ?? {});
    this.logger.logQuery(this.query.sql, params);
    return await this.queryWithCache(this.query.sql, params, async () => {
      return this.stmt.bind(...params).raw();
    });
  }
  isResponseInArrayMode() {
    return this._isResponseInArrayMode;
  }
};
var DrizzleD1Database2 = class extends BaseSQLiteDatabase2 {
  static {
    __name(this, "DrizzleD1Database");
  }
  static [entityKind2] = "D1Database";
  async batch(batch) {
    return this.session.batch(batch);
  }
};
function drizzle2(client, config2 = {}) {
  const dialect = new SQLiteAsyncDialect2({ casing: config2.casing });
  let logger;
  if (config2.logger === true) {
    logger = new DefaultLogger2();
  } else if (config2.logger !== false) {
    logger = config2.logger;
  }
  let schema;
  if (config2.schema) {
    const tablesConfig = extractTablesRelationalConfig2(config2.schema, createTableRelationsHelpers2);
    schema = {
      fullSchema: config2.schema,
      schema: tablesConfig.tables,
      tableNamesMap: tablesConfig.tableNamesMap
    };
  }
  const session = new SQLiteD1Session2(client, dialect, schema, { logger, cache: config2.cache });
  const db = new DrizzleD1Database2("async", dialect, session, schema);
  db.$client = client;
  db.$cache = config2.cache;
  if (db.$cache) {
    db.$cache["invalidate"] = config2.cache?.onMutate;
  }
  return db;
}
__name(drizzle2, "drizzle");
function isClientIdValid2(name) {
  const regex = /^[a-zA-Z_][a-zA-Z0-9_]{2,250}$/;
  return regex.test(name);
}
__name(isClientIdValid2, "isClientIdValid");
function parseDBUser(user) {
  return {
    ...user,
    data: typeof user.data === "string" ? JSON.parse(user.data) : user.data,
    session_private: user.session_private ? JSON.parse(user.session_private) : null,
    session_public: user.session_public ? JSON.parse(user.session_public) : null
  };
}
__name(parseDBUser, "parseDBUser");
var OTFusersTable2 = /* @__PURE__ */ __name((clientID) => {
  const name = `${clientID}_users`;
  if (!isClientIdValid2(name))
    throw new Error(`Invalid clientID: "${clientID}"`);
  return sqliteTable2(name, {
    id: text2().primaryKey(),
    identifier: text2().unique().notNull(),
    data: text2({
      mode: "json"
    }).notNull(),
    session_private: text2(),
    session_public: text2(),
    role: text2(),
    created_at: text2().notNull()
  });
}, "OTFusersTable");
var uiStyleTable2 = sqliteTable2("openauth_webui_ui_styles", {
  id: integer2().primaryKey({ autoIncrement: true }),
  name: text2().notNull(),
  owner_id: text2().notNull(),
  owner_group_id: text2().notNull(),
  themeData: text2({
    mode: "json"
  }).notNull()
});
var projectTable2 = sqliteTable2("openauth_webui_projects", {
  clientID: text2().primaryKey(),
  name: text2().notNull().default("Project Name"),
  owner_id: text2().notNull(),
  owner_group_id: text2().notNull(),
  active: integer2({
    mode: "boolean"
  }).default(true),
  providers_data: text2({
    mode: "json"
  }).default("[]"),
  theme_id: integer2().references(() => uiStyleTable2.id),
  projectData: text2({
    mode: "json"
  }).default("{}"),
  registerOnInvite: integer2({
    mode: "boolean"
  }).default(false),
  originURL: text2(),
  secret: text2().notNull(),
  authEndpointURL: text2().notNull(),
  cloudflareDomaineID: text2().notNull(),
  created_at: text2().notNull()
});
var WebHookTable2 = sqliteTable2("openauth_webui_webhooks", {
  id: text2().primaryKey(),
  clientID: text2().notNull().references(() => projectTable2.clientID),
  event: text2().notNull(),
  url: text2().notNull(),
  method: text2().notNull(),
  headers: text2({
    mode: "json"
  }),
  created_at: text2().notNull()
});
var emailTemplatesTable2 = sqliteTable2("openauth_webui_email_templates", {
  id: integer2().primaryKey({ autoIncrement: true }),
  name: text2().notNull(),
  body: text2().notNull(),
  subject: text2().notNull(),
  owner_id: text2().notNull(),
  owner_group_id: text2().notNull(),
  created_at: text2().notNull(),
  updated_at: text2().notNull()
});
var webuiProjectTable2 = sqliteTable2("openauth_webui", {
  key: text2().primaryKey(),
  value: text2().notNull(),
  expiry: integer2()
});
var totpTable2 = sqliteTable2("openauth_totp", {
  user_id: text2().primaryKey(),
  clientID: text2().notNull().references(() => projectTable2.clientID),
  secret: text2().notNull(),
  is_verified: integer2({
    mode: "boolean"
  }).default(false),
  backup_codes: text2({
    mode: "json"
  }).notNull(),
  created_at: text2().notNull()
});
var totpTokenTable2 = sqliteTable2("openauth_totp_tokens", {
  token: text2().primaryKey(),
  user_id: text2().notNull(),
  clientID: text2().notNull().references(() => projectTable2.clientID),
  token_expires_at: text2().notNull(),
  created_at: text2().notNull()
});
var WebUiProjectUserTable2 = OTFusersTable2("openauth_webui");
var WebUiCopyTemplateTable2 = sqliteTable2("openauth_webui_copy_templates", {
  id: integer2().primaryKey({ autoIncrement: true }),
  name: text2().notNull(),
  copyData: text2({
    mode: "json"
  }).notNull(),
  owner_id: text2().notNull(),
  owner_group_id: text2().notNull(),
  created_at: text2().notNull(),
  updated_at: text2().notNull()
});
var WebUiInviteLinkTable2 = sqliteTable2("openauth_webui_invite_links", {
  id: text2().primaryKey(),
  clientID: text2().notNull().references(() => projectTable2.clientID),
  link: text2().notNull(),
  expiresAt: text2().notNull(),
  created_at: text2().notNull()
});
var LogsTable2 = sqliteTable2("openauth_webui_logs", {
  id: text2().primaryKey(),
  clientID: text2().notNull().references(() => projectTable2.clientID),
  type: text2().notNull(),
  message: text2().notNull(),
  context: text2({
    mode: "json"
  }),
  timestamp: text2().notNull()
});
var webauthnChallengesTable2 = sqliteTable2("webauthn_challenges", {
  id: text2().primaryKey(),
  clientID: text2().notNull(),
  challenge: text2().notNull(),
  expires_at: text2().notNull(),
  created_at: text2().notNull()
});
var webauthnCredentialsTable2 = sqliteTable2("webauthn_credentials", {
  credential_id: text2().primaryKey(),
  user_id: text2().notNull(),
  clientID: text2().notNull(),
  public_key: text2().notNull(),
  counter: integer2().notNull(),
  device_type: text2(),
  backed_up: integer2({
    mode: "boolean"
  }).default(false),
  transports: text2({
    mode: "json"
  }),
  created_at: text2().notNull()
});
var webAuthnTokenAccessTable2 = sqliteTable2("webauthn_token_access", {
  token: text2().primaryKey(),
  user_id: text2().notNull(),
  clientID: text2().notNull(),
  expires_at: text2().notNull(),
  created_at: text2().notNull()
});
var inviteTable2 = sqliteTable2("openauth_webui_ui_invites", {
  id: integer2().primaryKey({ autoIncrement: true }),
  label: text2().notNull(),
  from_user_id: text2().notNull(),
  from_user_name: text2().notNull(),
  user_id: text2().notNull(),
  code: text2().notNull(),
  type: text2().notNull(),
  owner_group_id: text2().notNull(),
  expires_at: text2().notNull(),
  created_at: text2().notNull(),
  status: text2().default("pending")
});
async function GET15(params) {
  const ctx = getContext2(arguments);
  const { env: env2 } = ctx;
  const clientID = params.clientID?.trim();
  if (!clientID || !isClientIdValid2(clientID)) {
    return { success: false, error: "Invalid or missing clientID" };
  }
  const session = await ctx.data.client.getUserSession("private").then((s) => s instanceof Error ? null : s);
  if (!session) {
    return { success: false, error: "Unauthorized" };
  }
  const projectIsOwnedByUser = await onSelfHosted2(env2.SELF_HOSTED, Promise.resolve(true), () => drizzle2(env2.PROJECT_DB).select({ id: projectTable2.clientID }).from(projectTable2).where(and2(eq2(projectTable2.clientID, clientID), ownerGroupConditions2({
    user_group_ids: session?.private?.group_ids ?? [],
    ownerGroupIdColumn: projectTable2.owner_group_id,
    otherEq: [eq2(projectTable2.owner_id, session.user_id)],
    self_host: env2.SELF_HOSTED
  }))).get().then((e) => Boolean(e)));
  if (!projectIsOwnedByUser) {
    return { success: false, error: "Unauthorized" };
  }
  const page = Math.max(1, Number(params.page) || 1);
  const pageSize = Math.min(100, Math.max(1, Number(params.pageSize) || 20));
  const search = params.search?.trim() || "";
  try {
    const db = drizzle2(env2.PROJECT_DB);
    const usersTable = OTFusersTable2(clientID);
    const filters = search ? like2(usersTable.identifier, `%${search}%`) : void 0;
    const countQuery = filters ? db.select({ count: sql2`count(*)` }).from(usersTable).where(filters) : db.select({ count: sql2`count(*)` }).from(usersTable);
    const totalRow = await countQuery.get();
    const total = totalRow?.count ?? 0;
    let dataQuery = db.select({
      id: usersTable.id,
      identifier: usersTable.identifier,
      data: usersTable.data,
      session_public: usersTable.session_public,
      created_at: usersTable.created_at
    }).from(usersTable);
    if (filters) {
      dataQuery = dataQuery.where(filters);
    }
    const rows = await dataQuery.orderBy(desc2(usersTable.created_at)).limit(pageSize).offset((page - 1) * pageSize);
    const users = rows.map(parseDBUser);
    return {
      success: true,
      data: {
        users,
        total,
        page,
        pageSize
      }
    };
  } catch (error3) {
    console.error("Failed to load users:", error3);
    return {
      success: false,
      error: error3 instanceof Error ? error3.message : "Failed to load users"
    };
  }
}
__name(GET15, "GET");
async function DELETE12(params) {
  const ctx = getContext2(arguments);
  const { env: env2 } = ctx;
  const session = await ctx.data.client.getUserSession("private").then((s) => s instanceof Error ? null : s);
  if (!session) {
    return { success: false, error: "Unauthorized" };
  }
  const clientID = params.clientID.trim();
  const userID = params.userID.trim();
  const db = drizzle2(env2.PROJECT_DB);
  if (!await db.select({ id: projectTable2.clientID }).from(projectTable2).where(and2(eq2(projectTable2.clientID, clientID), ownerGroupConditions2({
    user_group_ids: session?.private?.group_ids ?? [],
    ownerGroupIdColumn: projectTable2.owner_group_id,
    otherEq: [eq2(projectTable2.owner_id, session.user_id)],
    self_host: env2.SELF_HOSTED
  }))).get().then((e) => Boolean(e))) {
    return { success: false, error: "Unauthorized" };
  }
  if (!clientID) {
    return { success: false, error: "Invalid or missing clientID" };
  } else if (!userID) {
    return { success: false, error: "Invalid or missing userID" };
  }
  try {
    const usersTable = OTFusersTable2(clientID);
    const existing = await db.select({ id: usersTable.id }).from(usersTable).where(eq2(usersTable.id, userID)).get();
    console.log("Existing user check:", { existing, clientID, userID });
    if (!existing) {
      return { success: false, error: "User not found" };
    }
    const res = await ctx.data.client.deleteUserById(userID);
    if (res.error) {
      return {
        success: false,
        error: res.error
      };
    }
    await Promise.allSettled([
      db.delete(totpTable2).where(and2(eq2(totpTable2.user_id, userID), eq2(totpTable2.clientID, clientID))),
      db.delete(webauthnCredentialsTable2).where(and2(eq2(webauthnCredentialsTable2.user_id, userID), eq2(webauthnCredentialsTable2.clientID, clientID)))
    ]);
    return { success: true };
  } catch (error3) {
    console.error("Failed to delete user:", error3);
    return {
      success: false,
      error: error3 instanceof Error ? error3.message : "Failed to delete user"
    };
  }
}
__name(DELETE12, "DELETE");
var DoubleIndexedKV24 = class {
  static {
    __name(this, "DoubleIndexedKV");
  }
  constructor() {
    this.keyToValue = /* @__PURE__ */ new Map();
    this.valueToKey = /* @__PURE__ */ new Map();
  }
  set(key, value) {
    this.keyToValue.set(key, value);
    this.valueToKey.set(value, key);
  }
  getByKey(key) {
    return this.keyToValue.get(key);
  }
  getByValue(value) {
    return this.valueToKey.get(value);
  }
  clear() {
    this.keyToValue.clear();
    this.valueToKey.clear();
  }
};
var Registry24 = class {
  static {
    __name(this, "Registry");
  }
  constructor(generateIdentifier) {
    this.generateIdentifier = generateIdentifier;
    this.kv = new DoubleIndexedKV24();
  }
  register(value, identifier) {
    if (this.kv.getByValue(value)) {
      return;
    }
    if (!identifier) {
      identifier = this.generateIdentifier(value);
    }
    this.kv.set(identifier, value);
  }
  clear() {
    this.kv.clear();
  }
  getIdentifier(value) {
    return this.kv.getByValue(value);
  }
  getValue(identifier) {
    return this.kv.getByKey(identifier);
  }
};
var ClassRegistry24 = class extends Registry24 {
  static {
    __name(this, "ClassRegistry");
  }
  constructor() {
    super((c) => c.name);
    this.classToAllowedProps = /* @__PURE__ */ new Map();
  }
  register(value, options) {
    if (typeof options === "object") {
      if (options.allowProps) {
        this.classToAllowedProps.set(value, options.allowProps);
      }
      super.register(value, options.identifier);
    } else {
      super.register(value, options);
    }
  }
  getAllowedProps(value) {
    return this.classToAllowedProps.get(value);
  }
};
function valuesOfObj24(record) {
  if ("values" in Object) {
    return Object.values(record);
  }
  const values = [];
  for (const key in record) {
    if (record.hasOwnProperty(key)) {
      values.push(record[key]);
    }
  }
  return values;
}
__name(valuesOfObj24, "valuesOfObj");
function find24(record, predicate) {
  const values = valuesOfObj24(record);
  if ("find" in values) {
    return values.find(predicate);
  }
  const valuesNotNever = values;
  for (let i = 0; i < valuesNotNever.length; i++) {
    const value = valuesNotNever[i];
    if (predicate(value)) {
      return value;
    }
  }
  return;
}
__name(find24, "find");
function forEach24(record, run) {
  Object.entries(record).forEach(([key, value]) => run(value, key));
}
__name(forEach24, "forEach");
function includes24(arr, value) {
  return arr.indexOf(value) !== -1;
}
__name(includes24, "includes");
function findArr24(record, predicate) {
  for (let i = 0; i < record.length; i++) {
    const value = record[i];
    if (predicate(value)) {
      return value;
    }
  }
  return;
}
__name(findArr24, "findArr");
var CustomTransformerRegistry24 = class {
  static {
    __name(this, "CustomTransformerRegistry");
  }
  constructor() {
    this.transfomers = {};
  }
  register(transformer) {
    this.transfomers[transformer.name] = transformer;
  }
  findApplicable(v) {
    return find24(this.transfomers, (transformer) => transformer.isApplicable(v));
  }
  findByName(name) {
    return this.transfomers[name];
  }
};
var getType33 = /* @__PURE__ */ __name((payload) => Object.prototype.toString.call(payload).slice(8, -1), "getType");
var isUndefined24 = /* @__PURE__ */ __name((payload) => typeof payload === "undefined", "isUndefined");
var isNull26 = /* @__PURE__ */ __name((payload) => payload === null, "isNull2");
var isPlainObject33 = /* @__PURE__ */ __name((payload) => {
  if (typeof payload !== "object" || payload === null)
    return false;
  if (payload === Object.prototype)
    return false;
  if (Object.getPrototypeOf(payload) === null)
    return true;
  return Object.getPrototypeOf(payload) === Object.prototype;
}, "isPlainObject");
var isEmptyObject24 = /* @__PURE__ */ __name((payload) => isPlainObject33(payload) && Object.keys(payload).length === 0, "isEmptyObject");
var isArray33 = /* @__PURE__ */ __name((payload) => Array.isArray(payload), "isArray");
var isString24 = /* @__PURE__ */ __name((payload) => typeof payload === "string", "isString");
var isNumber24 = /* @__PURE__ */ __name((payload) => typeof payload === "number" && !isNaN(payload), "isNumber");
var isBoolean24 = /* @__PURE__ */ __name((payload) => typeof payload === "boolean", "isBoolean");
var isRegExp24 = /* @__PURE__ */ __name((payload) => payload instanceof RegExp, "isRegExp");
var isMap24 = /* @__PURE__ */ __name((payload) => payload instanceof Map, "isMap");
var isSet24 = /* @__PURE__ */ __name((payload) => payload instanceof Set, "isSet");
var isSymbol24 = /* @__PURE__ */ __name((payload) => getType33(payload) === "Symbol", "isSymbol");
var isDate24 = /* @__PURE__ */ __name((payload) => payload instanceof Date && !isNaN(payload.valueOf()), "isDate");
var isError24 = /* @__PURE__ */ __name((payload) => payload instanceof Error, "isError");
var isNaNValue24 = /* @__PURE__ */ __name((payload) => typeof payload === "number" && isNaN(payload), "isNaNValue");
var isPrimitive24 = /* @__PURE__ */ __name((payload) => isBoolean24(payload) || isNull26(payload) || isUndefined24(payload) || isNumber24(payload) || isString24(payload) || isSymbol24(payload), "isPrimitive");
var isBigint24 = /* @__PURE__ */ __name((payload) => typeof payload === "bigint", "isBigint");
var isInfinite24 = /* @__PURE__ */ __name((payload) => payload === Infinity || payload === -Infinity, "isInfinite");
var isTypedArray24 = /* @__PURE__ */ __name((payload) => ArrayBuffer.isView(payload) && !(payload instanceof DataView), "isTypedArray");
var isURL24 = /* @__PURE__ */ __name((payload) => payload instanceof URL, "isURL");
var escapeKey24 = /* @__PURE__ */ __name((key) => key.replace(/\\/g, "\\\\").replace(/\./g, "\\."), "escapeKey");
var stringifyPath24 = /* @__PURE__ */ __name((path) => path.map(String).map(escapeKey24).join("."), "stringifyPath");
var parsePath24 = /* @__PURE__ */ __name((string2, legacyPaths) => {
  const result = [];
  let segment = "";
  for (let i = 0; i < string2.length; i++) {
    let char = string2.charAt(i);
    if (!legacyPaths && char === "\\") {
      const escaped = string2.charAt(i + 1);
      if (escaped === "\\") {
        segment += "\\";
        i++;
        continue;
      } else if (escaped !== ".") {
        throw Error("invalid path");
      }
    }
    const isEscapedDot = char === "\\" && string2.charAt(i + 1) === ".";
    if (isEscapedDot) {
      segment += ".";
      i++;
      continue;
    }
    const isEndOfSegment = char === ".";
    if (isEndOfSegment) {
      result.push(segment);
      segment = "";
      continue;
    }
    segment += char;
  }
  const lastSegment = segment;
  result.push(lastSegment);
  return result;
}, "parsePath");
function simpleTransformation24(isApplicable, annotation, transform, untransform) {
  return {
    isApplicable,
    annotation,
    transform,
    untransform
  };
}
__name(simpleTransformation24, "simpleTransformation");
var simpleRules24 = [
  simpleTransformation24(isUndefined24, "undefined", () => null, () => {
    return;
  }),
  simpleTransformation24(isBigint24, "bigint", (v) => v.toString(), (v) => {
    if (typeof BigInt !== "undefined") {
      return BigInt(v);
    }
    console.error("Please add a BigInt polyfill.");
    return v;
  }),
  simpleTransformation24(isDate24, "Date", (v) => v.toISOString(), (v) => new Date(v)),
  simpleTransformation24(isError24, "Error", (v, superJson) => {
    const baseError = {
      name: v.name,
      message: v.message
    };
    if ("cause" in v) {
      baseError.cause = v.cause;
    }
    superJson.allowedErrorProps.forEach((prop) => {
      baseError[prop] = v[prop];
    });
    return baseError;
  }, (v, superJson) => {
    const e = new Error(v.message, { cause: v.cause });
    e.name = v.name;
    e.stack = v.stack;
    superJson.allowedErrorProps.forEach((prop) => {
      e[prop] = v[prop];
    });
    return e;
  }),
  simpleTransformation24(isRegExp24, "regexp", (v) => "" + v, (regex) => {
    const body = regex.slice(1, regex.lastIndexOf("/"));
    const flags2 = regex.slice(regex.lastIndexOf("/") + 1);
    return new RegExp(body, flags2);
  }),
  simpleTransformation24(isSet24, "set", (v) => [...v.values()], (v) => new Set(v)),
  simpleTransformation24(isMap24, "map", (v) => [...v.entries()], (v) => new Map(v)),
  simpleTransformation24((v) => isNaNValue24(v) || isInfinite24(v), "number", (v) => {
    if (isNaNValue24(v)) {
      return "NaN";
    }
    if (v > 0) {
      return "Infinity";
    } else {
      return "-Infinity";
    }
  }, Number),
  simpleTransformation24((v) => v === 0 && 1 / v === -Infinity, "number", () => {
    return "-0";
  }, Number),
  simpleTransformation24(isURL24, "URL", (v) => v.toString(), (v) => new URL(v))
];
function compositeTransformation24(isApplicable, annotation, transform, untransform) {
  return {
    isApplicable,
    annotation,
    transform,
    untransform
  };
}
__name(compositeTransformation24, "compositeTransformation");
var symbolRule24 = compositeTransformation24((s, superJson) => {
  if (isSymbol24(s)) {
    const isRegistered = !!superJson.symbolRegistry.getIdentifier(s);
    return isRegistered;
  }
  return false;
}, (s, superJson) => {
  const identifier = superJson.symbolRegistry.getIdentifier(s);
  return ["symbol", identifier];
}, (v) => v.description, (_, a, superJson) => {
  const value = superJson.symbolRegistry.getValue(a[1]);
  if (!value) {
    throw new Error("Trying to deserialize unknown symbol");
  }
  return value;
});
var constructorToName24 = [
  Int8Array,
  Uint8Array,
  Int16Array,
  Uint16Array,
  Int32Array,
  Uint32Array,
  Float32Array,
  Float64Array,
  Uint8ClampedArray
].reduce((obj, ctor) => {
  obj[ctor.name] = ctor;
  return obj;
}, {});
var typedArrayRule24 = compositeTransformation24(isTypedArray24, (v) => ["typed-array", v.constructor.name], (v) => [...v], (v, a) => {
  const ctor = constructorToName24[a[1]];
  if (!ctor) {
    throw new Error("Trying to deserialize unknown typed array");
  }
  return new ctor(v);
});
function isInstanceOfRegisteredClass24(potentialClass, superJson) {
  if (potentialClass?.constructor) {
    const isRegistered = !!superJson.classRegistry.getIdentifier(potentialClass.constructor);
    return isRegistered;
  }
  return false;
}
__name(isInstanceOfRegisteredClass24, "isInstanceOfRegisteredClass");
var classRule24 = compositeTransformation24(isInstanceOfRegisteredClass24, (clazz, superJson) => {
  const identifier = superJson.classRegistry.getIdentifier(clazz.constructor);
  return ["class", identifier];
}, (clazz, superJson) => {
  const allowedProps = superJson.classRegistry.getAllowedProps(clazz.constructor);
  if (!allowedProps) {
    return { ...clazz };
  }
  const result = {};
  allowedProps.forEach((prop) => {
    result[prop] = clazz[prop];
  });
  return result;
}, (v, a, superJson) => {
  const clazz = superJson.classRegistry.getValue(a[1]);
  if (!clazz) {
    throw new Error(`Trying to deserialize unknown class '${a[1]}' - check https://github.com/blitz-js/superjson/issues/116#issuecomment-773996564`);
  }
  return Object.assign(Object.create(clazz.prototype), v);
});
var customRule24 = compositeTransformation24((value, superJson) => {
  return !!superJson.customTransformerRegistry.findApplicable(value);
}, (value, superJson) => {
  const transformer = superJson.customTransformerRegistry.findApplicable(value);
  return ["custom", transformer.name];
}, (value, superJson) => {
  const transformer = superJson.customTransformerRegistry.findApplicable(value);
  return transformer.serialize(value);
}, (v, a, superJson) => {
  const transformer = superJson.customTransformerRegistry.findByName(a[1]);
  if (!transformer) {
    throw new Error("Trying to deserialize unknown custom value");
  }
  return transformer.deserialize(v);
});
var compositeRules24 = [classRule24, symbolRule24, customRule24, typedArrayRule24];
var transformValue24 = /* @__PURE__ */ __name((value, superJson) => {
  const applicableCompositeRule = findArr24(compositeRules24, (rule) => rule.isApplicable(value, superJson));
  if (applicableCompositeRule) {
    return {
      value: applicableCompositeRule.transform(value, superJson),
      type: applicableCompositeRule.annotation(value, superJson)
    };
  }
  const applicableSimpleRule = findArr24(simpleRules24, (rule) => rule.isApplicable(value, superJson));
  if (applicableSimpleRule) {
    return {
      value: applicableSimpleRule.transform(value, superJson),
      type: applicableSimpleRule.annotation
    };
  }
  return;
}, "transformValue");
var simpleRulesByAnnotation24 = {};
simpleRules24.forEach((rule) => {
  simpleRulesByAnnotation24[rule.annotation] = rule;
});
var untransformValue24 = /* @__PURE__ */ __name((json, type, superJson) => {
  if (isArray33(type)) {
    switch (type[0]) {
      case "symbol":
        return symbolRule24.untransform(json, type, superJson);
      case "class":
        return classRule24.untransform(json, type, superJson);
      case "custom":
        return customRule24.untransform(json, type, superJson);
      case "typed-array":
        return typedArrayRule24.untransform(json, type, superJson);
      default:
        throw new Error("Unknown transformation: " + type);
    }
  } else {
    const transformation = simpleRulesByAnnotation24[type];
    if (!transformation) {
      throw new Error("Unknown transformation: " + type);
    }
    return transformation.untransform(json, superJson);
  }
}, "untransformValue");
var getNthKey24 = /* @__PURE__ */ __name((value, n) => {
  if (n > value.size)
    throw new Error("index out of bounds");
  const keys = value.keys();
  while (n > 0) {
    keys.next();
    n--;
  }
  return keys.next().value;
}, "getNthKey");
function validatePath24(path) {
  if (includes24(path, "__proto__")) {
    throw new Error("__proto__ is not allowed as a property");
  }
  if (includes24(path, "prototype")) {
    throw new Error("prototype is not allowed as a property");
  }
  if (includes24(path, "constructor")) {
    throw new Error("constructor is not allowed as a property");
  }
}
__name(validatePath24, "validatePath");
var getDeep24 = /* @__PURE__ */ __name((object2, path) => {
  validatePath24(path);
  for (let i = 0; i < path.length; i++) {
    const key = path[i];
    if (isSet24(object2)) {
      object2 = getNthKey24(object2, +key);
    } else if (isMap24(object2)) {
      const row = +key;
      const type = +path[++i] === 0 ? "key" : "value";
      const keyOfRow = getNthKey24(object2, row);
      switch (type) {
        case "key":
          object2 = keyOfRow;
          break;
        case "value":
          object2 = object2.get(keyOfRow);
          break;
      }
    } else {
      object2 = object2[key];
    }
  }
  return object2;
}, "getDeep");
var setDeep24 = /* @__PURE__ */ __name((object2, path, mapper) => {
  validatePath24(path);
  if (path.length === 0) {
    return mapper(object2);
  }
  let parent = object2;
  for (let i = 0; i < path.length - 1; i++) {
    const key = path[i];
    if (isArray33(parent)) {
      const index = +key;
      parent = parent[index];
    } else if (isPlainObject33(parent)) {
      parent = parent[key];
    } else if (isSet24(parent)) {
      const row = +key;
      parent = getNthKey24(parent, row);
    } else if (isMap24(parent)) {
      const isEnd = i === path.length - 2;
      if (isEnd) {
        break;
      }
      const row = +key;
      const type = +path[++i] === 0 ? "key" : "value";
      const keyOfRow = getNthKey24(parent, row);
      switch (type) {
        case "key":
          parent = keyOfRow;
          break;
        case "value":
          parent = parent.get(keyOfRow);
          break;
      }
    }
  }
  const lastKey = path[path.length - 1];
  if (isArray33(parent)) {
    parent[+lastKey] = mapper(parent[+lastKey]);
  } else if (isPlainObject33(parent)) {
    parent[lastKey] = mapper(parent[lastKey]);
  }
  if (isSet24(parent)) {
    const oldValue = getNthKey24(parent, +lastKey);
    const newValue = mapper(oldValue);
    if (oldValue !== newValue) {
      parent.delete(oldValue);
      parent.add(newValue);
    }
  }
  if (isMap24(parent)) {
    const row = +path[path.length - 2];
    const keyToRow = getNthKey24(parent, row);
    const type = +lastKey === 0 ? "key" : "value";
    switch (type) {
      case "key": {
        const newKey = mapper(keyToRow);
        parent.set(newKey, parent.get(keyToRow));
        if (newKey !== keyToRow) {
          parent.delete(keyToRow);
        }
        break;
      }
      case "value": {
        parent.set(keyToRow, mapper(parent.get(keyToRow)));
        break;
      }
    }
  }
  return object2;
}, "setDeep");
var enableLegacyPaths24 = /* @__PURE__ */ __name((version22) => version22 < 1, "enableLegacyPaths");
function traverse24(tree, walker26, version22, origin = []) {
  if (!tree) {
    return;
  }
  const legacyPaths = enableLegacyPaths24(version22);
  if (!isArray33(tree)) {
    forEach24(tree, (subtree, key) => traverse24(subtree, walker26, version22, [
      ...origin,
      ...parsePath24(key, legacyPaths)
    ]));
    return;
  }
  const [nodeValue, children] = tree;
  if (children) {
    forEach24(children, (child, key) => {
      traverse24(child, walker26, version22, [
        ...origin,
        ...parsePath24(key, legacyPaths)
      ]);
    });
  }
  walker26(nodeValue, origin);
}
__name(traverse24, "traverse");
function applyValueAnnotations24(plain, annotations, version22, superJson) {
  traverse24(annotations, (type, path) => {
    plain = setDeep24(plain, path, (v) => untransformValue24(v, type, superJson));
  }, version22);
  return plain;
}
__name(applyValueAnnotations24, "applyValueAnnotations");
function applyReferentialEqualityAnnotations24(plain, annotations, version22) {
  const legacyPaths = enableLegacyPaths24(version22);
  function apply(identicalPaths, path) {
    const object2 = getDeep24(plain, parsePath24(path, legacyPaths));
    identicalPaths.map((path2) => parsePath24(path2, legacyPaths)).forEach((identicalObjectPath) => {
      plain = setDeep24(plain, identicalObjectPath, () => object2);
    });
  }
  __name(apply, "apply");
  if (isArray33(annotations)) {
    const [root, other] = annotations;
    root.forEach((identicalPath) => {
      plain = setDeep24(plain, parsePath24(identicalPath, legacyPaths), () => plain);
    });
    if (other) {
      forEach24(other, apply);
    }
  } else {
    forEach24(annotations, apply);
  }
  return plain;
}
__name(applyReferentialEqualityAnnotations24, "applyReferentialEqualityAnnotations");
var isDeep24 = /* @__PURE__ */ __name((object2, superJson) => isPlainObject33(object2) || isArray33(object2) || isMap24(object2) || isSet24(object2) || isError24(object2) || isInstanceOfRegisteredClass24(object2, superJson), "isDeep");
function addIdentity24(object2, path, identities) {
  const existingSet = identities.get(object2);
  if (existingSet) {
    existingSet.push(path);
  } else {
    identities.set(object2, [path]);
  }
}
__name(addIdentity24, "addIdentity");
function generateReferentialEqualityAnnotations24(identitites, dedupe) {
  const result = {};
  let rootEqualityPaths = void 0;
  identitites.forEach((paths) => {
    if (paths.length <= 1) {
      return;
    }
    if (!dedupe) {
      paths = paths.map((path) => path.map(String)).sort((a, b) => a.length - b.length);
    }
    const [representativePath, ...identicalPaths] = paths;
    if (representativePath.length === 0) {
      rootEqualityPaths = identicalPaths.map(stringifyPath24);
    } else {
      result[stringifyPath24(representativePath)] = identicalPaths.map(stringifyPath24);
    }
  });
  if (rootEqualityPaths) {
    if (isEmptyObject24(result)) {
      return [rootEqualityPaths];
    } else {
      return [rootEqualityPaths, result];
    }
  } else {
    return isEmptyObject24(result) ? void 0 : result;
  }
}
__name(generateReferentialEqualityAnnotations24, "generateReferentialEqualityAnnotations");
var walker24 = /* @__PURE__ */ __name((object2, identities, superJson, dedupe, path = [], objectsInThisPath = [], seenObjects = /* @__PURE__ */ new Map()) => {
  const primitive = isPrimitive24(object2);
  if (!primitive) {
    addIdentity24(object2, path, identities);
    const seen = seenObjects.get(object2);
    if (seen) {
      return dedupe ? {
        transformedValue: null
      } : seen;
    }
  }
  if (!isDeep24(object2, superJson)) {
    const transformed2 = transformValue24(object2, superJson);
    const result2 = transformed2 ? {
      transformedValue: transformed2.value,
      annotations: [transformed2.type]
    } : {
      transformedValue: object2
    };
    if (!primitive) {
      seenObjects.set(object2, result2);
    }
    return result2;
  }
  if (includes24(objectsInThisPath, object2)) {
    return {
      transformedValue: null
    };
  }
  const transformationResult = transformValue24(object2, superJson);
  const transformed = transformationResult?.value ?? object2;
  const transformedValue = isArray33(transformed) ? [] : {};
  const innerAnnotations = {};
  forEach24(transformed, (value, index) => {
    if (index === "__proto__" || index === "constructor" || index === "prototype") {
      throw new Error(`Detected property ${index}. This is a prototype pollution risk, please remove it from your object.`);
    }
    const recursiveResult = walker24(value, identities, superJson, dedupe, [...path, index], [...objectsInThisPath, object2], seenObjects);
    transformedValue[index] = recursiveResult.transformedValue;
    if (isArray33(recursiveResult.annotations)) {
      innerAnnotations[escapeKey24(index)] = recursiveResult.annotations;
    } else if (isPlainObject33(recursiveResult.annotations)) {
      forEach24(recursiveResult.annotations, (tree, key) => {
        innerAnnotations[escapeKey24(index) + "." + key] = tree;
      });
    }
  });
  const result = isEmptyObject24(innerAnnotations) ? {
    transformedValue,
    annotations: transformationResult ? [transformationResult.type] : void 0
  } : {
    transformedValue,
    annotations: transformationResult ? [transformationResult.type, innerAnnotations] : innerAnnotations
  };
  if (!primitive) {
    seenObjects.set(object2, result);
  }
  return result;
}, "walker");
function getType224(payload) {
  return Object.prototype.toString.call(payload).slice(8, -1);
}
__name(getType224, "getType2");
function isArray224(payload) {
  return getType224(payload) === "Array";
}
__name(isArray224, "isArray2");
function isPlainObject224(payload) {
  if (getType224(payload) !== "Object")
    return false;
  const prototype = Object.getPrototypeOf(payload);
  return !!prototype && prototype.constructor === Object && prototype === Object.prototype;
}
__name(isPlainObject224, "isPlainObject2");
function assignProp24(carry, key, newVal, originalObject, includeNonenumerable) {
  const propType = {}.propertyIsEnumerable.call(originalObject, key) ? "enumerable" : "nonenumerable";
  if (propType === "enumerable")
    carry[key] = newVal;
  if (includeNonenumerable && propType === "nonenumerable") {
    Object.defineProperty(carry, key, {
      value: newVal,
      enumerable: false,
      writable: true,
      configurable: true
    });
  }
}
__name(assignProp24, "assignProp");
function copy24(target, options = {}) {
  if (isArray224(target)) {
    return target.map((item) => copy24(item, options));
  }
  if (!isPlainObject224(target)) {
    return target;
  }
  const props = Object.getOwnPropertyNames(target);
  const symbols = Object.getOwnPropertySymbols(target);
  return [...props, ...symbols].reduce((carry, key) => {
    if (key === "__proto__")
      return carry;
    if (isArray224(options.props) && !options.props.includes(key)) {
      return carry;
    }
    const val = target[key];
    const newVal = copy24(val, options);
    assignProp24(carry, key, newVal, target, options.nonenumerable);
    return carry;
  }, {});
}
__name(copy24, "copy");
var SuperJSON24 = class {
  static {
    __name(this, "SuperJSON");
  }
  constructor({ dedupe = false } = {}) {
    this.classRegistry = new ClassRegistry24();
    this.symbolRegistry = new Registry24((s) => s.description ?? "");
    this.customTransformerRegistry = new CustomTransformerRegistry24();
    this.allowedErrorProps = [];
    this.dedupe = dedupe;
  }
  serialize(object2) {
    const identities = /* @__PURE__ */ new Map();
    const output = walker24(object2, identities, this, this.dedupe);
    const res = {
      json: output.transformedValue
    };
    if (output.annotations) {
      res.meta = {
        ...res.meta,
        values: output.annotations
      };
    }
    const equalityAnnotations = generateReferentialEqualityAnnotations24(identities, this.dedupe);
    if (equalityAnnotations) {
      res.meta = {
        ...res.meta,
        referentialEqualities: equalityAnnotations
      };
    }
    if (res.meta)
      res.meta.v = 1;
    return res;
  }
  deserialize(payload, options) {
    const { json, meta } = payload;
    let result = options?.inPlace ? json : copy24(json);
    if (meta?.values) {
      result = applyValueAnnotations24(result, meta.values, meta.v ?? 0, this);
    }
    if (meta?.referentialEqualities) {
      result = applyReferentialEqualityAnnotations24(result, meta.referentialEqualities, meta.v ?? 0);
    }
    return result;
  }
  stringify(object2) {
    return JSON.stringify(this.serialize(object2));
  }
  parse(string2) {
    return this.deserialize(JSON.parse(string2), { inPlace: true });
  }
  registerClass(v, options) {
    this.classRegistry.register(v, options);
  }
  registerSymbol(v, identifier) {
    this.symbolRegistry.register(v, identifier);
  }
  registerCustom(transformer, name) {
    this.customTransformerRegistry.register({
      name,
      ...transformer
    });
  }
  allowErrorProps(...props) {
    this.allowedErrorProps.push(...props);
  }
};
SuperJSON24.defaultInstance = new SuperJSON24();
SuperJSON24.serialize = SuperJSON24.defaultInstance.serialize.bind(SuperJSON24.defaultInstance);
SuperJSON24.deserialize = SuperJSON24.defaultInstance.deserialize.bind(SuperJSON24.defaultInstance);
SuperJSON24.stringify = SuperJSON24.defaultInstance.stringify.bind(SuperJSON24.defaultInstance);
SuperJSON24.parse = SuperJSON24.defaultInstance.parse.bind(SuperJSON24.defaultInstance);
SuperJSON24.registerClass = SuperJSON24.defaultInstance.registerClass.bind(SuperJSON24.defaultInstance);
SuperJSON24.registerSymbol = SuperJSON24.defaultInstance.registerSymbol.bind(SuperJSON24.defaultInstance);
SuperJSON24.registerCustom = SuperJSON24.defaultInstance.registerCustom.bind(SuperJSON24.defaultInstance);
SuperJSON24.allowErrorProps = SuperJSON24.defaultInstance.allowErrorProps.bind(SuperJSON24.defaultInstance);
var dist_default24 = SuperJSON24;
var serialize24 = SuperJSON24.serialize;
var deserialize24 = SuperJSON24.deserialize;
var stringify25 = SuperJSON24.stringify;
var parse24 = SuperJSON24.parse;
var registerClass24 = SuperJSON24.registerClass;
var registerCustom24 = SuperJSON24.registerCustom;
var registerSymbol24 = SuperJSON24.registerSymbol;
var allowErrorProps24 = SuperJSON24.allowErrorProps;
function parseData24(formData) {
  const propsArray = [];
  if (!formData)
    return propsArray;
  const batchsIDs = [];
  for (const [key, value] of Array.from(formData.entries())) {
    if (key.startsWith("FILE_"))
      propsArray.push(value);
    else if (key.startsWith("FILES_")) {
      if (batchsIDs.includes(key))
        continue;
      batchsIDs.push(key);
      propsArray.push(formData.getAll(key));
    } else {
      propsArray.push(JSON.parse(decodeURI(value)));
    }
  }
  return propsArray;
}
__name(parseData24, "parseData");
function paramsFromURL24(url) {
  const params = url.searchParams.entries().toArray().map(([_, v]) => v);
  return params.map((param) => JSON.parse(decodeURIComponent(param)));
}
__name(paramsFromURL24, "paramsFromURL");
async function WrapRequestHandler24(context2, endpoint) {
  const isServerAction = context2.request.headers.get("x-server-action") === "true";
  if (!isServerAction) {
    return new Response("Not Found", { status: 404 });
  }
  const parsedData = context2.request.method === "GET" || context2.request.method === "HEAD" ? paramsFromURL24(new URL(context2.request.url)) : parseData24(context2.request.headers.get("content-type") ? await context2.request.formData() : void 0);
  const missingProps = endpoint.length - parsedData.length;
  for (let i = 0; i < missingProps; i++) {
    parsedData.push(void 0);
  }
  parsedData.push(context2);
  const result = await endpoint(...parsedData);
  switch (typeof result) {
    case "string":
    case "number":
    case "boolean":
    case "bigint":
      return new Response(dist_default24.stringify(result), {
        headers: {
          "Content-Type": "application/json",
          dataType: "json"
        }
      });
    case "undefined":
      return new Response(null, { status: 204 });
    case "object":
      if (result instanceof Response) {
        result.headers.set("dataType", "response");
        return result;
      } else if (result instanceof Blob) {
        const res = new Response(await result.arrayBuffer());
        res.headers.set("dataType", "blob");
        res.headers.set("Content-Type", result.type);
        return res;
      } else if (result instanceof File) {
        return new Response(await result.arrayBuffer(), {
          headers: {
            "Content-Type": result.type,
            dataType: "file",
            fileData: JSON.stringify({
              name: result.name,
              lastModified: result.lastModified
            })
          }
        });
      } else {
        return new Response(dist_default24.stringify(result), {
          headers: { "Content-Type": "application/json", dataType: "json" }
        });
      }
    default:
      throw new Error(`Unsupported return type from action: ${typeof result}`);
  }
}
__name(WrapRequestHandler24, "WrapRequestHandler");
var onRequest24 = /* @__PURE__ */ __name(async (context2) => {
  const method = context2.request.method;
  const options = {
    GET: typeof GET15 === "function" ? GET15 : void 0,
    POST: typeof POST === "function" ? POST : void 0,
    PUT: typeof PUT === "function" ? PUT : void 0,
    DELETE: typeof DELETE12 === "function" ? DELETE12 : void 0,
    PATCH: typeof PATCH === "function" ? PATCH : void 0,
    HEAD: typeof HEAD === "function" ? HEAD : void 0,
    OPTIONS: typeof OPTIONS === "function" ? OPTIONS : void 0
  };
  if (!options[method]) {
    return new Response(`Method "${method}" Not Allowed`, {
      status: 405
    });
  }
  return await WrapRequestHandler24(context2, options[method]);
}, "onRequest");

// api/webhooks/index.js
async function hashWithSecretKey(data, secretKey) {
  const encoder2 = new TextEncoder();
  const key = await crypto.subtle.importKey("raw", encoder2.encode(secretKey), { name: "HMAC", hash: "SHA-256" }, false, ["sign"]);
  const signature = await crypto.subtle.sign("HMAC", key, encoder2.encode(JSON.stringify(data)));
  return Array.from(new Uint8Array(signature)).map((b) => b.toString(16).padStart(2, "0")).join("");
}
__name(hashWithSecretKey, "hashWithSecretKey");
async function verifySignature({
  data,
  signatureHex,
  secretKey
}) {
  return hashWithSecretKey(data, secretKey).then((expectedSignatureHex) => expectedSignatureHex === signatureHex);
}
__name(verifySignature, "verifySignature");
var WebHookUnAuthorizedError = class extends Error {
  static {
    __name(this, "WebHookUnAuthorizedError");
  }
  constructor(message2) {
    super(message2 || "Unauthorized webhook request");
    this.name = "WebHookUnAuthorizedError";
  }
};
var WebHook = class _WebHook {
  static {
    __name(this, "WebHook");
  }
  db;
  rawDB;
  constructor({ db }) {
    this.db = drizzle(db);
    this.rawDB = db;
  }
  async register({
    event,
    config: config2,
    clientID
  }) {
    return this.db.insert(WebHookTable).values({
      id: crypto.randomUUID(),
      clientID,
      event,
      url: config2.url,
      method: config2.method,
      headers: config2.headers ? JSON.stringify(config2.headers) : void 0,
      created_at: (/* @__PURE__ */ new Date()).toISOString()
    }).returning().then((c) => this.parseWebHookConfig(c[0]));
  }
  async update({
    webHookID,
    config: config2
  }) {
    return this.db.update(WebHookTable).set({
      ...this.stringifyWebHookConfig(config2)
    }).where(and(eq(WebHookTable.id, webHookID))).returning().then((c) => this.parseWebHookConfig(c[0]));
  }
  async getWebHooks(clientID, filters) {
    return this.db.select().from(WebHookTable).where(filters?.id ? and(eq(WebHookTable.id, filters.id), eq(WebHookTable.clientID, clientID)) : filters?.event ? and(eq(WebHookTable.event, filters.event), eq(WebHookTable.clientID, clientID)) : eq(WebHookTable.clientID, clientID)).all().then((res) => res.map((r) => this.parseWebHookConfig(r)));
  }
  async deleteWebHook(webHookID, clientID) {
    return this.db.delete(WebHookTable).where(and(eq(WebHookTable.id, webHookID), eq(WebHookTable.clientID, clientID))).run();
  }
  async trigger({
    clientID,
    event,
    secret,
    data,
    log: log3 = false,
    request
  }) {
    const webhooks = await this.db.select().from(WebHookTable).where(and(eq(WebHookTable.clientID, clientID), eq(WebHookTable.event, event))).all();
    const res = await Promise.all(webhooks.map(this.parseWebHookConfig).map(async (webhook) => {
      try {
        const fullPayload = {
          id: webhook.id,
          timestamp: (/* @__PURE__ */ new Date()).toISOString(),
          clientID: webhook.clientID,
          event: webhook.event,
          data,
          meta: {
            ip: request.headers.get("cf-connecting-ip") || request.headers.get("x-real-ip") || request.headers.get("x-forwarded-for")?.split(",").at(0)?.trim() || "unknown",
            userAgent: request.headers.get("user-agent") || "unknown"
          }
        };
        const url = new URL(webhook.url);
        if (webhook.method === "GET") {
          url.searchParams.set("payload", JSON.stringify(fullPayload));
        }
        return await fetch(url.toString(), {
          method: webhook.method,
          signal: AbortSignal.timeout(5e3),
          headers: {
            "Content-Type": "application/json",
            "x-secret": await hashWithSecretKey(JSON.stringify(fullPayload), secret),
            ...webhook.headers
          },
          body: webhook.method === "GET" ? void 0 : JSON.stringify(fullPayload)
        }).then((res2) => {
          if (!res2.ok) {
            throw new Error(`Failed to trigger webhook ${webhook.id}: ${res2.statusText}`);
          }
          return {
            success: true,
            id: webhook.id
          };
        });
      } catch (error3) {
        console.error(`Failed to trigger webhook ${webhook.id}:`, error3);
        return {
          success: false,
          error: error3 instanceof WebHookUnAuthorizedError ? error3 : new Error(String(error3)),
          id: webhook.id
        };
      }
    }));
    const failedEvent = res.filter((r) => !r.success).map((r) => ({
      id: r.id,
      error: r.error instanceof Error ? r.error.message : String(r.error)
    }));
    if (log3 && failedEvent.length > 0) {
      await insertLog({
        type: "warning",
        clientID,
        message: `Triggered webhooks for event ${event}. Some requests have failed.`,
        database: this.rawDB,
        context: {
          event,
          payload: data,
          results: failedEvent
        }
      });
    }
    return res;
  }
  static async getWebHookPayloadFromRequest(event, request, appSecret) {
    const payload = await _WebHook.getPayLoadFromRequest(request);
    await _WebHook.ensureAuthenticity({
      request,
      secret: appSecret,
      data: payload
    });
    const parsedPayload = JSON.parse(payload);
    _WebHook.ensureTimeStamp(parsedPayload);
    return parsedPayload;
  }
  static getPayLoadFromRequest(request) {
    if (request.method === "GET") {
      const url = new URL(request.url);
      const payload = url.searchParams.get("payload");
      if (!payload) {
        throw new Error("Missing payload in webhook request");
      }
      return Promise.resolve(payload);
    }
    return request.text();
  }
  parseWebHookConfig(raw) {
    return {
      id: raw.id,
      clientID: raw.clientID,
      url: raw.url,
      method: raw.method,
      created_at: raw.created_at,
      event: raw.event,
      headers: raw.headers ? JSON.parse(raw.headers) : void 0
    };
  }
  stringifyWebHookConfig(config2) {
    return {
      ...config2,
      headers: config2.headers ? JSON.stringify(config2.headers) : void 0
    };
  }
  static async ensureAuthenticity({
    request,
    secret,
    data
  }) {
    const reqSignature = request.headers.get("x-secret");
    if (!reqSignature || !await verifySignature({
      data,
      signatureHex: reqSignature,
      secretKey: secret
    })) {
      throw new WebHookUnAuthorizedError("Unauthorized webhook request");
    }
  }
  static ensureTimeStamp(payload) {
    const payloadTime = new Date(payload.timestamp).getTime();
    const now = Date.now();
    const diff = Math.abs(now - payloadTime);
    if (diff > 5 * 60 * 1e3) {
      throw new WebHookUnAuthorizedError("Webhook request timestamp is too old");
    }
  }
  static create(config2) {
    return new _WebHook(config2);
  }
};
async function GET16({
  clientID,
  filters
}) {
  const ctx = getContext(arguments);
  const wh = new WebHook({ db: ctx.env.PROJECT_DB });
  const session = await ctx.data.client.getUserSession("private");
  if (session instanceof Error) {
    return {
      success: false,
      error: "Unauthorized"
    };
  }
  const projectIsOwnedByUser = onSelfHosted(ctx.env.SELF_HOSTED, Promise.resolve(true), () => drizzle(ctx.env.PROJECT_DB).select({ id: projectTable.clientID }).from(projectTable).where(and(eq(projectTable.clientID, clientID), ownerGroupConditions({
    user_group_ids: session?.private?.group_ids ?? [],
    ownerGroupIdColumn: projectTable.owner_group_id,
    otherEq: [eq(projectTable.owner_id, session.user_id)],
    self_host: ctx.env.SELF_HOSTED
  }))).then((result) => Boolean(result.at(0))));
  if (!projectIsOwnedByUser) {
    return {
      success: false,
      error: "Unauthorized"
    };
  }
  try {
    return {
      success: true,
      data: await wh.getWebHooks(clientID, filters)
    };
  } catch (error3) {
    return {
      success: false,
      error: "Failed to fetch webhooks",
      details: error3 instanceof Error ? error3.message : String(error3)
    };
  }
}
__name(GET16, "GET");
async function POST15({
  clientID,
  event,
  config: config2
}) {
  const ctx = getContext(arguments);
  try {
    return {
      success: true,
      data: await new WebHook({ db: ctx.env.PROJECT_DB }).register({
        clientID,
        event,
        config: config2
      })
    };
  } catch (error3) {
    return {
      error: "Failed to register webhook",
      details: error3 instanceof Error ? error3.message : String(error3),
      success: false
    };
  }
}
__name(POST15, "POST");
async function DELETE13({
  webHookID,
  clientID
}) {
  const ctx = getContext(arguments);
  try {
    await new WebHook({ db: ctx.env.PROJECT_DB }).deleteWebHook(webHookID, clientID);
    return {
      success: true,
      data: null
    };
  } catch (error3) {
    return {
      error: "Failed to delete webhook",
      details: error3 instanceof Error ? error3.message : String(error3),
      success: false
    };
  }
}
__name(DELETE13, "DELETE");
async function PUT6({
  webHookID,
  config: config2
}) {
  const ctx = getContext(arguments);
  const wh = new WebHook({ db: ctx.env.PROJECT_DB });
  try {
    return {
      success: true,
      data: await wh.update({ webHookID, config: config2 })
    };
  } catch (error3) {
    return {
      error: "Failed to update webhook",
      details: error3 instanceof Error ? error3.message : String(error3),
      success: false
    };
  }
}
__name(PUT6, "PUT");
var DoubleIndexedKV25 = class {
  static {
    __name(this, "DoubleIndexedKV");
  }
  constructor() {
    this.keyToValue = /* @__PURE__ */ new Map();
    this.valueToKey = /* @__PURE__ */ new Map();
  }
  set(key, value) {
    this.keyToValue.set(key, value);
    this.valueToKey.set(value, key);
  }
  getByKey(key) {
    return this.keyToValue.get(key);
  }
  getByValue(value) {
    return this.valueToKey.get(value);
  }
  clear() {
    this.keyToValue.clear();
    this.valueToKey.clear();
  }
};
var Registry25 = class {
  static {
    __name(this, "Registry");
  }
  constructor(generateIdentifier) {
    this.generateIdentifier = generateIdentifier;
    this.kv = new DoubleIndexedKV25();
  }
  register(value, identifier) {
    if (this.kv.getByValue(value)) {
      return;
    }
    if (!identifier) {
      identifier = this.generateIdentifier(value);
    }
    this.kv.set(identifier, value);
  }
  clear() {
    this.kv.clear();
  }
  getIdentifier(value) {
    return this.kv.getByValue(value);
  }
  getValue(identifier) {
    return this.kv.getByKey(identifier);
  }
};
var ClassRegistry25 = class extends Registry25 {
  static {
    __name(this, "ClassRegistry");
  }
  constructor() {
    super((c) => c.name);
    this.classToAllowedProps = /* @__PURE__ */ new Map();
  }
  register(value, options) {
    if (typeof options === "object") {
      if (options.allowProps) {
        this.classToAllowedProps.set(value, options.allowProps);
      }
      super.register(value, options.identifier);
    } else {
      super.register(value, options);
    }
  }
  getAllowedProps(value) {
    return this.classToAllowedProps.get(value);
  }
};
function valuesOfObj25(record) {
  if ("values" in Object) {
    return Object.values(record);
  }
  const values = [];
  for (const key in record) {
    if (record.hasOwnProperty(key)) {
      values.push(record[key]);
    }
  }
  return values;
}
__name(valuesOfObj25, "valuesOfObj");
function find25(record, predicate) {
  const values = valuesOfObj25(record);
  if ("find" in values) {
    return values.find(predicate);
  }
  const valuesNotNever = values;
  for (let i = 0; i < valuesNotNever.length; i++) {
    const value = valuesNotNever[i];
    if (predicate(value)) {
      return value;
    }
  }
  return;
}
__name(find25, "find");
function forEach25(record, run) {
  Object.entries(record).forEach(([key, value]) => run(value, key));
}
__name(forEach25, "forEach");
function includes25(arr, value) {
  return arr.indexOf(value) !== -1;
}
__name(includes25, "includes");
function findArr25(record, predicate) {
  for (let i = 0; i < record.length; i++) {
    const value = record[i];
    if (predicate(value)) {
      return value;
    }
  }
  return;
}
__name(findArr25, "findArr");
var CustomTransformerRegistry25 = class {
  static {
    __name(this, "CustomTransformerRegistry");
  }
  constructor() {
    this.transfomers = {};
  }
  register(transformer) {
    this.transfomers[transformer.name] = transformer;
  }
  findApplicable(v) {
    return find25(this.transfomers, (transformer) => transformer.isApplicable(v));
  }
  findByName(name) {
    return this.transfomers[name];
  }
};
var getType34 = /* @__PURE__ */ __name((payload) => Object.prototype.toString.call(payload).slice(8, -1), "getType");
var isUndefined25 = /* @__PURE__ */ __name((payload) => typeof payload === "undefined", "isUndefined");
var isNull27 = /* @__PURE__ */ __name((payload) => payload === null, "isNull");
var isPlainObject34 = /* @__PURE__ */ __name((payload) => {
  if (typeof payload !== "object" || payload === null)
    return false;
  if (payload === Object.prototype)
    return false;
  if (Object.getPrototypeOf(payload) === null)
    return true;
  return Object.getPrototypeOf(payload) === Object.prototype;
}, "isPlainObject");
var isEmptyObject25 = /* @__PURE__ */ __name((payload) => isPlainObject34(payload) && Object.keys(payload).length === 0, "isEmptyObject");
var isArray34 = /* @__PURE__ */ __name((payload) => Array.isArray(payload), "isArray");
var isString25 = /* @__PURE__ */ __name((payload) => typeof payload === "string", "isString");
var isNumber25 = /* @__PURE__ */ __name((payload) => typeof payload === "number" && !isNaN(payload), "isNumber");
var isBoolean25 = /* @__PURE__ */ __name((payload) => typeof payload === "boolean", "isBoolean");
var isRegExp25 = /* @__PURE__ */ __name((payload) => payload instanceof RegExp, "isRegExp");
var isMap25 = /* @__PURE__ */ __name((payload) => payload instanceof Map, "isMap");
var isSet25 = /* @__PURE__ */ __name((payload) => payload instanceof Set, "isSet");
var isSymbol25 = /* @__PURE__ */ __name((payload) => getType34(payload) === "Symbol", "isSymbol");
var isDate25 = /* @__PURE__ */ __name((payload) => payload instanceof Date && !isNaN(payload.valueOf()), "isDate");
var isError25 = /* @__PURE__ */ __name((payload) => payload instanceof Error, "isError");
var isNaNValue25 = /* @__PURE__ */ __name((payload) => typeof payload === "number" && isNaN(payload), "isNaNValue");
var isPrimitive25 = /* @__PURE__ */ __name((payload) => isBoolean25(payload) || isNull27(payload) || isUndefined25(payload) || isNumber25(payload) || isString25(payload) || isSymbol25(payload), "isPrimitive");
var isBigint25 = /* @__PURE__ */ __name((payload) => typeof payload === "bigint", "isBigint");
var isInfinite25 = /* @__PURE__ */ __name((payload) => payload === Infinity || payload === -Infinity, "isInfinite");
var isTypedArray25 = /* @__PURE__ */ __name((payload) => ArrayBuffer.isView(payload) && !(payload instanceof DataView), "isTypedArray");
var isURL25 = /* @__PURE__ */ __name((payload) => payload instanceof URL, "isURL");
var escapeKey25 = /* @__PURE__ */ __name((key) => key.replace(/\\/g, "\\\\").replace(/\./g, "\\."), "escapeKey");
var stringifyPath25 = /* @__PURE__ */ __name((path) => path.map(String).map(escapeKey25).join("."), "stringifyPath");
var parsePath25 = /* @__PURE__ */ __name((string2, legacyPaths) => {
  const result = [];
  let segment = "";
  for (let i = 0; i < string2.length; i++) {
    let char = string2.charAt(i);
    if (!legacyPaths && char === "\\") {
      const escaped = string2.charAt(i + 1);
      if (escaped === "\\") {
        segment += "\\";
        i++;
        continue;
      } else if (escaped !== ".") {
        throw Error("invalid path");
      }
    }
    const isEscapedDot = char === "\\" && string2.charAt(i + 1) === ".";
    if (isEscapedDot) {
      segment += ".";
      i++;
      continue;
    }
    const isEndOfSegment = char === ".";
    if (isEndOfSegment) {
      result.push(segment);
      segment = "";
      continue;
    }
    segment += char;
  }
  const lastSegment = segment;
  result.push(lastSegment);
  return result;
}, "parsePath");
function simpleTransformation25(isApplicable, annotation, transform, untransform) {
  return {
    isApplicable,
    annotation,
    transform,
    untransform
  };
}
__name(simpleTransformation25, "simpleTransformation");
var simpleRules25 = [
  simpleTransformation25(isUndefined25, "undefined", () => null, () => {
    return;
  }),
  simpleTransformation25(isBigint25, "bigint", (v) => v.toString(), (v) => {
    if (typeof BigInt !== "undefined") {
      return BigInt(v);
    }
    console.error("Please add a BigInt polyfill.");
    return v;
  }),
  simpleTransformation25(isDate25, "Date", (v) => v.toISOString(), (v) => new Date(v)),
  simpleTransformation25(isError25, "Error", (v, superJson) => {
    const baseError = {
      name: v.name,
      message: v.message
    };
    if ("cause" in v) {
      baseError.cause = v.cause;
    }
    superJson.allowedErrorProps.forEach((prop) => {
      baseError[prop] = v[prop];
    });
    return baseError;
  }, (v, superJson) => {
    const e = new Error(v.message, { cause: v.cause });
    e.name = v.name;
    e.stack = v.stack;
    superJson.allowedErrorProps.forEach((prop) => {
      e[prop] = v[prop];
    });
    return e;
  }),
  simpleTransformation25(isRegExp25, "regexp", (v) => "" + v, (regex) => {
    const body = regex.slice(1, regex.lastIndexOf("/"));
    const flags2 = regex.slice(regex.lastIndexOf("/") + 1);
    return new RegExp(body, flags2);
  }),
  simpleTransformation25(isSet25, "set", (v) => [...v.values()], (v) => new Set(v)),
  simpleTransformation25(isMap25, "map", (v) => [...v.entries()], (v) => new Map(v)),
  simpleTransformation25((v) => isNaNValue25(v) || isInfinite25(v), "number", (v) => {
    if (isNaNValue25(v)) {
      return "NaN";
    }
    if (v > 0) {
      return "Infinity";
    } else {
      return "-Infinity";
    }
  }, Number),
  simpleTransformation25((v) => v === 0 && 1 / v === -Infinity, "number", () => {
    return "-0";
  }, Number),
  simpleTransformation25(isURL25, "URL", (v) => v.toString(), (v) => new URL(v))
];
function compositeTransformation25(isApplicable, annotation, transform, untransform) {
  return {
    isApplicable,
    annotation,
    transform,
    untransform
  };
}
__name(compositeTransformation25, "compositeTransformation");
var symbolRule25 = compositeTransformation25((s, superJson) => {
  if (isSymbol25(s)) {
    const isRegistered = !!superJson.symbolRegistry.getIdentifier(s);
    return isRegistered;
  }
  return false;
}, (s, superJson) => {
  const identifier = superJson.symbolRegistry.getIdentifier(s);
  return ["symbol", identifier];
}, (v) => v.description, (_, a, superJson) => {
  const value = superJson.symbolRegistry.getValue(a[1]);
  if (!value) {
    throw new Error("Trying to deserialize unknown symbol");
  }
  return value;
});
var constructorToName25 = [
  Int8Array,
  Uint8Array,
  Int16Array,
  Uint16Array,
  Int32Array,
  Uint32Array,
  Float32Array,
  Float64Array,
  Uint8ClampedArray
].reduce((obj, ctor) => {
  obj[ctor.name] = ctor;
  return obj;
}, {});
var typedArrayRule25 = compositeTransformation25(isTypedArray25, (v) => ["typed-array", v.constructor.name], (v) => [...v], (v, a) => {
  const ctor = constructorToName25[a[1]];
  if (!ctor) {
    throw new Error("Trying to deserialize unknown typed array");
  }
  return new ctor(v);
});
function isInstanceOfRegisteredClass25(potentialClass, superJson) {
  if (potentialClass?.constructor) {
    const isRegistered = !!superJson.classRegistry.getIdentifier(potentialClass.constructor);
    return isRegistered;
  }
  return false;
}
__name(isInstanceOfRegisteredClass25, "isInstanceOfRegisteredClass");
var classRule25 = compositeTransformation25(isInstanceOfRegisteredClass25, (clazz, superJson) => {
  const identifier = superJson.classRegistry.getIdentifier(clazz.constructor);
  return ["class", identifier];
}, (clazz, superJson) => {
  const allowedProps = superJson.classRegistry.getAllowedProps(clazz.constructor);
  if (!allowedProps) {
    return { ...clazz };
  }
  const result = {};
  allowedProps.forEach((prop) => {
    result[prop] = clazz[prop];
  });
  return result;
}, (v, a, superJson) => {
  const clazz = superJson.classRegistry.getValue(a[1]);
  if (!clazz) {
    throw new Error(`Trying to deserialize unknown class '${a[1]}' - check https://github.com/blitz-js/superjson/issues/116#issuecomment-773996564`);
  }
  return Object.assign(Object.create(clazz.prototype), v);
});
var customRule25 = compositeTransformation25((value, superJson) => {
  return !!superJson.customTransformerRegistry.findApplicable(value);
}, (value, superJson) => {
  const transformer = superJson.customTransformerRegistry.findApplicable(value);
  return ["custom", transformer.name];
}, (value, superJson) => {
  const transformer = superJson.customTransformerRegistry.findApplicable(value);
  return transformer.serialize(value);
}, (v, a, superJson) => {
  const transformer = superJson.customTransformerRegistry.findByName(a[1]);
  if (!transformer) {
    throw new Error("Trying to deserialize unknown custom value");
  }
  return transformer.deserialize(v);
});
var compositeRules25 = [classRule25, symbolRule25, customRule25, typedArrayRule25];
var transformValue25 = /* @__PURE__ */ __name((value, superJson) => {
  const applicableCompositeRule = findArr25(compositeRules25, (rule) => rule.isApplicable(value, superJson));
  if (applicableCompositeRule) {
    return {
      value: applicableCompositeRule.transform(value, superJson),
      type: applicableCompositeRule.annotation(value, superJson)
    };
  }
  const applicableSimpleRule = findArr25(simpleRules25, (rule) => rule.isApplicable(value, superJson));
  if (applicableSimpleRule) {
    return {
      value: applicableSimpleRule.transform(value, superJson),
      type: applicableSimpleRule.annotation
    };
  }
  return;
}, "transformValue");
var simpleRulesByAnnotation25 = {};
simpleRules25.forEach((rule) => {
  simpleRulesByAnnotation25[rule.annotation] = rule;
});
var untransformValue25 = /* @__PURE__ */ __name((json, type, superJson) => {
  if (isArray34(type)) {
    switch (type[0]) {
      case "symbol":
        return symbolRule25.untransform(json, type, superJson);
      case "class":
        return classRule25.untransform(json, type, superJson);
      case "custom":
        return customRule25.untransform(json, type, superJson);
      case "typed-array":
        return typedArrayRule25.untransform(json, type, superJson);
      default:
        throw new Error("Unknown transformation: " + type);
    }
  } else {
    const transformation = simpleRulesByAnnotation25[type];
    if (!transformation) {
      throw new Error("Unknown transformation: " + type);
    }
    return transformation.untransform(json, superJson);
  }
}, "untransformValue");
var getNthKey25 = /* @__PURE__ */ __name((value, n) => {
  if (n > value.size)
    throw new Error("index out of bounds");
  const keys = value.keys();
  while (n > 0) {
    keys.next();
    n--;
  }
  return keys.next().value;
}, "getNthKey");
function validatePath25(path) {
  if (includes25(path, "__proto__")) {
    throw new Error("__proto__ is not allowed as a property");
  }
  if (includes25(path, "prototype")) {
    throw new Error("prototype is not allowed as a property");
  }
  if (includes25(path, "constructor")) {
    throw new Error("constructor is not allowed as a property");
  }
}
__name(validatePath25, "validatePath");
var getDeep25 = /* @__PURE__ */ __name((object2, path) => {
  validatePath25(path);
  for (let i = 0; i < path.length; i++) {
    const key = path[i];
    if (isSet25(object2)) {
      object2 = getNthKey25(object2, +key);
    } else if (isMap25(object2)) {
      const row = +key;
      const type = +path[++i] === 0 ? "key" : "value";
      const keyOfRow = getNthKey25(object2, row);
      switch (type) {
        case "key":
          object2 = keyOfRow;
          break;
        case "value":
          object2 = object2.get(keyOfRow);
          break;
      }
    } else {
      object2 = object2[key];
    }
  }
  return object2;
}, "getDeep");
var setDeep25 = /* @__PURE__ */ __name((object2, path, mapper) => {
  validatePath25(path);
  if (path.length === 0) {
    return mapper(object2);
  }
  let parent = object2;
  for (let i = 0; i < path.length - 1; i++) {
    const key = path[i];
    if (isArray34(parent)) {
      const index = +key;
      parent = parent[index];
    } else if (isPlainObject34(parent)) {
      parent = parent[key];
    } else if (isSet25(parent)) {
      const row = +key;
      parent = getNthKey25(parent, row);
    } else if (isMap25(parent)) {
      const isEnd = i === path.length - 2;
      if (isEnd) {
        break;
      }
      const row = +key;
      const type = +path[++i] === 0 ? "key" : "value";
      const keyOfRow = getNthKey25(parent, row);
      switch (type) {
        case "key":
          parent = keyOfRow;
          break;
        case "value":
          parent = parent.get(keyOfRow);
          break;
      }
    }
  }
  const lastKey = path[path.length - 1];
  if (isArray34(parent)) {
    parent[+lastKey] = mapper(parent[+lastKey]);
  } else if (isPlainObject34(parent)) {
    parent[lastKey] = mapper(parent[lastKey]);
  }
  if (isSet25(parent)) {
    const oldValue = getNthKey25(parent, +lastKey);
    const newValue = mapper(oldValue);
    if (oldValue !== newValue) {
      parent.delete(oldValue);
      parent.add(newValue);
    }
  }
  if (isMap25(parent)) {
    const row = +path[path.length - 2];
    const keyToRow = getNthKey25(parent, row);
    const type = +lastKey === 0 ? "key" : "value";
    switch (type) {
      case "key": {
        const newKey = mapper(keyToRow);
        parent.set(newKey, parent.get(keyToRow));
        if (newKey !== keyToRow) {
          parent.delete(keyToRow);
        }
        break;
      }
      case "value": {
        parent.set(keyToRow, mapper(parent.get(keyToRow)));
        break;
      }
    }
  }
  return object2;
}, "setDeep");
var enableLegacyPaths25 = /* @__PURE__ */ __name((version4) => version4 < 1, "enableLegacyPaths");
function traverse25(tree, walker26, version4, origin = []) {
  if (!tree) {
    return;
  }
  const legacyPaths = enableLegacyPaths25(version4);
  if (!isArray34(tree)) {
    forEach25(tree, (subtree, key) => traverse25(subtree, walker26, version4, [
      ...origin,
      ...parsePath25(key, legacyPaths)
    ]));
    return;
  }
  const [nodeValue, children] = tree;
  if (children) {
    forEach25(children, (child, key) => {
      traverse25(child, walker26, version4, [
        ...origin,
        ...parsePath25(key, legacyPaths)
      ]);
    });
  }
  walker26(nodeValue, origin);
}
__name(traverse25, "traverse");
function applyValueAnnotations25(plain, annotations, version4, superJson) {
  traverse25(annotations, (type, path) => {
    plain = setDeep25(plain, path, (v) => untransformValue25(v, type, superJson));
  }, version4);
  return plain;
}
__name(applyValueAnnotations25, "applyValueAnnotations");
function applyReferentialEqualityAnnotations25(plain, annotations, version4) {
  const legacyPaths = enableLegacyPaths25(version4);
  function apply(identicalPaths, path) {
    const object2 = getDeep25(plain, parsePath25(path, legacyPaths));
    identicalPaths.map((path2) => parsePath25(path2, legacyPaths)).forEach((identicalObjectPath) => {
      plain = setDeep25(plain, identicalObjectPath, () => object2);
    });
  }
  __name(apply, "apply");
  if (isArray34(annotations)) {
    const [root, other] = annotations;
    root.forEach((identicalPath) => {
      plain = setDeep25(plain, parsePath25(identicalPath, legacyPaths), () => plain);
    });
    if (other) {
      forEach25(other, apply);
    }
  } else {
    forEach25(annotations, apply);
  }
  return plain;
}
__name(applyReferentialEqualityAnnotations25, "applyReferentialEqualityAnnotations");
var isDeep25 = /* @__PURE__ */ __name((object2, superJson) => isPlainObject34(object2) || isArray34(object2) || isMap25(object2) || isSet25(object2) || isError25(object2) || isInstanceOfRegisteredClass25(object2, superJson), "isDeep");
function addIdentity25(object2, path, identities) {
  const existingSet = identities.get(object2);
  if (existingSet) {
    existingSet.push(path);
  } else {
    identities.set(object2, [path]);
  }
}
__name(addIdentity25, "addIdentity");
function generateReferentialEqualityAnnotations25(identitites, dedupe) {
  const result = {};
  let rootEqualityPaths = void 0;
  identitites.forEach((paths) => {
    if (paths.length <= 1) {
      return;
    }
    if (!dedupe) {
      paths = paths.map((path) => path.map(String)).sort((a, b) => a.length - b.length);
    }
    const [representativePath, ...identicalPaths] = paths;
    if (representativePath.length === 0) {
      rootEqualityPaths = identicalPaths.map(stringifyPath25);
    } else {
      result[stringifyPath25(representativePath)] = identicalPaths.map(stringifyPath25);
    }
  });
  if (rootEqualityPaths) {
    if (isEmptyObject25(result)) {
      return [rootEqualityPaths];
    } else {
      return [rootEqualityPaths, result];
    }
  } else {
    return isEmptyObject25(result) ? void 0 : result;
  }
}
__name(generateReferentialEqualityAnnotations25, "generateReferentialEqualityAnnotations");
var walker25 = /* @__PURE__ */ __name((object2, identities, superJson, dedupe, path = [], objectsInThisPath = [], seenObjects = /* @__PURE__ */ new Map()) => {
  const primitive = isPrimitive25(object2);
  if (!primitive) {
    addIdentity25(object2, path, identities);
    const seen = seenObjects.get(object2);
    if (seen) {
      return dedupe ? {
        transformedValue: null
      } : seen;
    }
  }
  if (!isDeep25(object2, superJson)) {
    const transformed2 = transformValue25(object2, superJson);
    const result2 = transformed2 ? {
      transformedValue: transformed2.value,
      annotations: [transformed2.type]
    } : {
      transformedValue: object2
    };
    if (!primitive) {
      seenObjects.set(object2, result2);
    }
    return result2;
  }
  if (includes25(objectsInThisPath, object2)) {
    return {
      transformedValue: null
    };
  }
  const transformationResult = transformValue25(object2, superJson);
  const transformed = transformationResult?.value ?? object2;
  const transformedValue = isArray34(transformed) ? [] : {};
  const innerAnnotations = {};
  forEach25(transformed, (value, index) => {
    if (index === "__proto__" || index === "constructor" || index === "prototype") {
      throw new Error(`Detected property ${index}. This is a prototype pollution risk, please remove it from your object.`);
    }
    const recursiveResult = walker25(value, identities, superJson, dedupe, [...path, index], [...objectsInThisPath, object2], seenObjects);
    transformedValue[index] = recursiveResult.transformedValue;
    if (isArray34(recursiveResult.annotations)) {
      innerAnnotations[escapeKey25(index)] = recursiveResult.annotations;
    } else if (isPlainObject34(recursiveResult.annotations)) {
      forEach25(recursiveResult.annotations, (tree, key) => {
        innerAnnotations[escapeKey25(index) + "." + key] = tree;
      });
    }
  });
  const result = isEmptyObject25(innerAnnotations) ? {
    transformedValue,
    annotations: transformationResult ? [transformationResult.type] : void 0
  } : {
    transformedValue,
    annotations: transformationResult ? [transformationResult.type, innerAnnotations] : innerAnnotations
  };
  if (!primitive) {
    seenObjects.set(object2, result);
  }
  return result;
}, "walker");
function getType225(payload) {
  return Object.prototype.toString.call(payload).slice(8, -1);
}
__name(getType225, "getType2");
function isArray225(payload) {
  return getType225(payload) === "Array";
}
__name(isArray225, "isArray2");
function isPlainObject225(payload) {
  if (getType225(payload) !== "Object")
    return false;
  const prototype = Object.getPrototypeOf(payload);
  return !!prototype && prototype.constructor === Object && prototype === Object.prototype;
}
__name(isPlainObject225, "isPlainObject2");
function assignProp25(carry, key, newVal, originalObject, includeNonenumerable) {
  const propType = {}.propertyIsEnumerable.call(originalObject, key) ? "enumerable" : "nonenumerable";
  if (propType === "enumerable")
    carry[key] = newVal;
  if (includeNonenumerable && propType === "nonenumerable") {
    Object.defineProperty(carry, key, {
      value: newVal,
      enumerable: false,
      writable: true,
      configurable: true
    });
  }
}
__name(assignProp25, "assignProp");
function copy25(target, options = {}) {
  if (isArray225(target)) {
    return target.map((item) => copy25(item, options));
  }
  if (!isPlainObject225(target)) {
    return target;
  }
  const props = Object.getOwnPropertyNames(target);
  const symbols = Object.getOwnPropertySymbols(target);
  return [...props, ...symbols].reduce((carry, key) => {
    if (key === "__proto__")
      return carry;
    if (isArray225(options.props) && !options.props.includes(key)) {
      return carry;
    }
    const val = target[key];
    const newVal = copy25(val, options);
    assignProp25(carry, key, newVal, target, options.nonenumerable);
    return carry;
  }, {});
}
__name(copy25, "copy");
var SuperJSON25 = class {
  static {
    __name(this, "SuperJSON");
  }
  constructor({ dedupe = false } = {}) {
    this.classRegistry = new ClassRegistry25();
    this.symbolRegistry = new Registry25((s) => s.description ?? "");
    this.customTransformerRegistry = new CustomTransformerRegistry25();
    this.allowedErrorProps = [];
    this.dedupe = dedupe;
  }
  serialize(object2) {
    const identities = /* @__PURE__ */ new Map();
    const output = walker25(object2, identities, this, this.dedupe);
    const res = {
      json: output.transformedValue
    };
    if (output.annotations) {
      res.meta = {
        ...res.meta,
        values: output.annotations
      };
    }
    const equalityAnnotations = generateReferentialEqualityAnnotations25(identities, this.dedupe);
    if (equalityAnnotations) {
      res.meta = {
        ...res.meta,
        referentialEqualities: equalityAnnotations
      };
    }
    if (res.meta)
      res.meta.v = 1;
    return res;
  }
  deserialize(payload, options) {
    const { json, meta } = payload;
    let result = options?.inPlace ? json : copy25(json);
    if (meta?.values) {
      result = applyValueAnnotations25(result, meta.values, meta.v ?? 0, this);
    }
    if (meta?.referentialEqualities) {
      result = applyReferentialEqualityAnnotations25(result, meta.referentialEqualities, meta.v ?? 0);
    }
    return result;
  }
  stringify(object2) {
    return JSON.stringify(this.serialize(object2));
  }
  parse(string2) {
    return this.deserialize(JSON.parse(string2), { inPlace: true });
  }
  registerClass(v, options) {
    this.classRegistry.register(v, options);
  }
  registerSymbol(v, identifier) {
    this.symbolRegistry.register(v, identifier);
  }
  registerCustom(transformer, name) {
    this.customTransformerRegistry.register({
      name,
      ...transformer
    });
  }
  allowErrorProps(...props) {
    this.allowedErrorProps.push(...props);
  }
};
SuperJSON25.defaultInstance = new SuperJSON25();
SuperJSON25.serialize = SuperJSON25.defaultInstance.serialize.bind(SuperJSON25.defaultInstance);
SuperJSON25.deserialize = SuperJSON25.defaultInstance.deserialize.bind(SuperJSON25.defaultInstance);
SuperJSON25.stringify = SuperJSON25.defaultInstance.stringify.bind(SuperJSON25.defaultInstance);
SuperJSON25.parse = SuperJSON25.defaultInstance.parse.bind(SuperJSON25.defaultInstance);
SuperJSON25.registerClass = SuperJSON25.defaultInstance.registerClass.bind(SuperJSON25.defaultInstance);
SuperJSON25.registerSymbol = SuperJSON25.defaultInstance.registerSymbol.bind(SuperJSON25.defaultInstance);
SuperJSON25.registerCustom = SuperJSON25.defaultInstance.registerCustom.bind(SuperJSON25.defaultInstance);
SuperJSON25.allowErrorProps = SuperJSON25.defaultInstance.allowErrorProps.bind(SuperJSON25.defaultInstance);
var dist_default25 = SuperJSON25;
var serialize25 = SuperJSON25.serialize;
var deserialize25 = SuperJSON25.deserialize;
var stringify26 = SuperJSON25.stringify;
var parse25 = SuperJSON25.parse;
var registerClass25 = SuperJSON25.registerClass;
var registerCustom25 = SuperJSON25.registerCustom;
var registerSymbol25 = SuperJSON25.registerSymbol;
var allowErrorProps25 = SuperJSON25.allowErrorProps;
function parseData25(formData) {
  const propsArray = [];
  if (!formData)
    return propsArray;
  const batchsIDs = [];
  for (const [key, value] of Array.from(formData.entries())) {
    if (key.startsWith("FILE_"))
      propsArray.push(value);
    else if (key.startsWith("FILES_")) {
      if (batchsIDs.includes(key))
        continue;
      batchsIDs.push(key);
      propsArray.push(formData.getAll(key));
    } else {
      propsArray.push(JSON.parse(decodeURI(value)));
    }
  }
  return propsArray;
}
__name(parseData25, "parseData");
function paramsFromURL25(url) {
  const params = url.searchParams.entries().toArray().map(([_, v]) => v);
  return params.map((param) => JSON.parse(decodeURIComponent(param)));
}
__name(paramsFromURL25, "paramsFromURL");
async function WrapRequestHandler25(context2, endpoint) {
  const isServerAction = context2.request.headers.get("x-server-action") === "true";
  if (!isServerAction) {
    return new Response("Not Found", { status: 404 });
  }
  const parsedData = context2.request.method === "GET" || context2.request.method === "HEAD" ? paramsFromURL25(new URL(context2.request.url)) : parseData25(context2.request.headers.get("content-type") ? await context2.request.formData() : void 0);
  const missingProps = endpoint.length - parsedData.length;
  for (let i = 0; i < missingProps; i++) {
    parsedData.push(void 0);
  }
  parsedData.push(context2);
  const result = await endpoint(...parsedData);
  switch (typeof result) {
    case "string":
    case "number":
    case "boolean":
    case "bigint":
      return new Response(dist_default25.stringify(result), {
        headers: {
          "Content-Type": "application/json",
          dataType: "json"
        }
      });
    case "undefined":
      return new Response(null, { status: 204 });
    case "object":
      if (result instanceof Response) {
        result.headers.set("dataType", "response");
        return result;
      } else if (result instanceof Blob) {
        const res = new Response(await result.arrayBuffer());
        res.headers.set("dataType", "blob");
        res.headers.set("Content-Type", result.type);
        return res;
      } else if (result instanceof File) {
        return new Response(await result.arrayBuffer(), {
          headers: {
            "Content-Type": result.type,
            dataType: "file",
            fileData: JSON.stringify({
              name: result.name,
              lastModified: result.lastModified
            })
          }
        });
      } else {
        return new Response(dist_default25.stringify(result), {
          headers: { "Content-Type": "application/json", dataType: "json" }
        });
      }
    default:
      throw new Error(`Unsupported return type from action: ${typeof result}`);
  }
}
__name(WrapRequestHandler25, "WrapRequestHandler");
var onRequest25 = /* @__PURE__ */ __name(async (context2) => {
  const method = context2.request.method;
  const options = {
    GET: typeof GET16 === "function" ? GET16 : void 0,
    POST: typeof POST15 === "function" ? POST15 : void 0,
    PUT: typeof PUT6 === "function" ? PUT6 : void 0,
    DELETE: typeof DELETE13 === "function" ? DELETE13 : void 0,
    PATCH: typeof PATCH === "function" ? PATCH : void 0,
    HEAD: typeof HEAD === "function" ? HEAD : void 0,
    OPTIONS: typeof OPTIONS === "function" ? OPTIONS : void 0
  };
  if (!options[method]) {
    return new Response(`Method "${method}" Not Allowed`, {
      status: 405
    });
  }
  return await WrapRequestHandler25(context2, options[method]);
}, "onRequest");

// api/_middleware.js
function createSubjects(types) {
  return { ...types };
}
__name(createSubjects, "createSubjects");
var store$4;
function getGlobalConfig(config$1) {
  return {
    lang: config$1?.lang ?? store$4?.lang,
    message: config$1?.message,
    abortEarly: config$1?.abortEarly ?? store$4?.abortEarly,
    abortPipeEarly: config$1?.abortPipeEarly ?? store$4?.abortPipeEarly
  };
}
__name(getGlobalConfig, "getGlobalConfig");
var store$3;
function getGlobalMessage(lang) {
  return store$3?.get(lang);
}
__name(getGlobalMessage, "getGlobalMessage");
var store$2;
function getSchemaMessage(lang) {
  return store$2?.get(lang);
}
__name(getSchemaMessage, "getSchemaMessage");
var store$1;
function getSpecificMessage(reference, lang) {
  return store$1?.get(reference)?.get(lang);
}
__name(getSpecificMessage, "getSpecificMessage");
function _stringify(input) {
  const type = typeof input;
  if (type === "string")
    return `"${input}"`;
  if (type === "number" || type === "bigint" || type === "boolean")
    return `${input}`;
  if (type === "object" || type === "function")
    return (input && Object.getPrototypeOf(input)?.constructor?.name) ?? "null";
  return type;
}
__name(_stringify, "_stringify");
function _addIssue(context2, label, dataset, config$1, other) {
  const input = other && "input" in other ? other.input : dataset.value;
  const expected = other?.expected ?? context2.expects ?? null;
  const received = other?.received ?? /* @__PURE__ */ _stringify(input);
  const issue = {
    kind: context2.kind,
    type: context2.type,
    input,
    expected,
    received,
    message: `Invalid ${label}: ${expected ? `Expected ${expected} but r` : "R"}eceived ${received}`,
    requirement: context2.requirement,
    path: other?.path,
    issues: other?.issues,
    lang: config$1.lang,
    abortEarly: config$1.abortEarly,
    abortPipeEarly: config$1.abortPipeEarly
  };
  const isSchema = context2.kind === "schema";
  const message$1 = other?.message ?? context2.message ?? /* @__PURE__ */ getSpecificMessage(context2.reference, issue.lang) ?? (isSchema ? /* @__PURE__ */ getSchemaMessage(issue.lang) : null) ?? config$1.message ?? /* @__PURE__ */ getGlobalMessage(issue.lang);
  if (message$1 !== void 0)
    issue.message = typeof message$1 === "function" ? message$1(issue) : message$1;
  if (isSchema)
    dataset.typed = false;
  if (dataset.issues)
    dataset.issues.push(issue);
  else
    dataset.issues = [issue];
}
__name(_addIssue, "_addIssue");
function _getStandardProps(context2) {
  return {
    version: 1,
    vendor: "valibot",
    validate(value$1) {
      return context2["~run"]({ value: value$1 }, /* @__PURE__ */ getGlobalConfig());
    }
  };
}
__name(_getStandardProps, "_getStandardProps");
function _isValidObjectKey(object$1, key) {
  return Object.hasOwn(object$1, key) && key !== "__proto__" && key !== "prototype" && key !== "constructor";
}
__name(_isValidObjectKey, "_isValidObjectKey");
function _joinExpects(values$1, separator) {
  const list = [...new Set(values$1)];
  if (list.length > 1)
    return `(${list.join(` ${separator} `)})`;
  return list[0] ?? "never";
}
__name(_joinExpects, "_joinExpects");
var ValiError = class extends Error {
  static {
    __name(this, "ValiError");
  }
  constructor(issues) {
    super(issues[0].message);
    this.name = "ValiError";
    this.issues = issues;
  }
};
function getFallback(schema, dataset, config$1) {
  return typeof schema.fallback === "function" ? schema.fallback(dataset, config$1) : schema.fallback;
}
__name(getFallback, "getFallback");
function getDefault(schema, dataset, config$1) {
  return typeof schema.default === "function" ? schema.default(dataset, config$1) : schema.default;
}
__name(getDefault, "getDefault");
function any() {
  return {
    kind: "schema",
    type: "any",
    reference: any,
    expects: "any",
    async: false,
    get "~standard"() {
      return /* @__PURE__ */ _getStandardProps(this);
    },
    "~run"(dataset) {
      dataset.typed = true;
      return dataset;
    }
  };
}
__name(any, "any");
function array(item, message$1) {
  return {
    kind: "schema",
    type: "array",
    reference: array,
    expects: "Array",
    async: false,
    item,
    message: message$1,
    get "~standard"() {
      return /* @__PURE__ */ _getStandardProps(this);
    },
    "~run"(dataset, config$1) {
      const input = dataset.value;
      if (Array.isArray(input)) {
        dataset.typed = true;
        dataset.value = [];
        for (let key = 0; key < input.length; key++) {
          const value$1 = input[key];
          const itemDataset = this.item["~run"]({ value: value$1 }, config$1);
          if (itemDataset.issues) {
            const pathItem = {
              type: "array",
              origin: "value",
              input,
              key,
              value: value$1
            };
            for (const issue of itemDataset.issues) {
              if (issue.path)
                issue.path.unshift(pathItem);
              else
                issue.path = [pathItem];
              dataset.issues?.push(issue);
            }
            if (!dataset.issues)
              dataset.issues = itemDataset.issues;
            if (config$1.abortEarly) {
              dataset.typed = false;
              break;
            }
          }
          if (!itemDataset.typed)
            dataset.typed = false;
          dataset.value.push(itemDataset.value);
        }
      } else
        _addIssue(this, "type", dataset, config$1);
      return dataset;
    }
  };
}
__name(array, "array");
function boolean(message$1) {
  return {
    kind: "schema",
    type: "boolean",
    reference: boolean,
    expects: "boolean",
    async: false,
    message: message$1,
    get "~standard"() {
      return /* @__PURE__ */ _getStandardProps(this);
    },
    "~run"(dataset, config$1) {
      if (typeof dataset.value === "boolean")
        dataset.typed = true;
      else
        _addIssue(this, "type", dataset, config$1);
      return dataset;
    }
  };
}
__name(boolean, "boolean");
function literal(literal_, message$1) {
  return {
    kind: "schema",
    type: "literal",
    reference: literal,
    expects: /* @__PURE__ */ _stringify(literal_),
    async: false,
    literal: literal_,
    message: message$1,
    get "~standard"() {
      return /* @__PURE__ */ _getStandardProps(this);
    },
    "~run"(dataset, config$1) {
      if (dataset.value === this.literal)
        dataset.typed = true;
      else
        _addIssue(this, "type", dataset, config$1);
      return dataset;
    }
  };
}
__name(literal, "literal");
function looseObject(entries$1, message$1) {
  return {
    kind: "schema",
    type: "loose_object",
    reference: looseObject,
    expects: "Object",
    async: false,
    entries: entries$1,
    message: message$1,
    get "~standard"() {
      return /* @__PURE__ */ _getStandardProps(this);
    },
    "~run"(dataset, config$1) {
      const input = dataset.value;
      if (input && typeof input === "object") {
        dataset.typed = true;
        dataset.value = {};
        for (const key in this.entries) {
          const valueSchema = this.entries[key];
          if (key in input || (valueSchema.type === "exact_optional" || valueSchema.type === "optional" || valueSchema.type === "nullish") && valueSchema.default !== void 0) {
            const value$1 = key in input ? input[key] : /* @__PURE__ */ getDefault(valueSchema);
            const valueDataset = valueSchema["~run"]({ value: value$1 }, config$1);
            if (valueDataset.issues) {
              const pathItem = {
                type: "object",
                origin: "value",
                input,
                key,
                value: value$1
              };
              for (const issue of valueDataset.issues) {
                if (issue.path)
                  issue.path.unshift(pathItem);
                else
                  issue.path = [pathItem];
                dataset.issues?.push(issue);
              }
              if (!dataset.issues)
                dataset.issues = valueDataset.issues;
              if (config$1.abortEarly) {
                dataset.typed = false;
                break;
              }
            }
            if (!valueDataset.typed)
              dataset.typed = false;
            dataset.value[key] = valueDataset.value;
          } else if (valueSchema.fallback !== void 0)
            dataset.value[key] = /* @__PURE__ */ getFallback(valueSchema);
          else if (valueSchema.type !== "exact_optional" && valueSchema.type !== "optional" && valueSchema.type !== "nullish") {
            _addIssue(this, "key", dataset, config$1, {
              input: void 0,
              expected: `"${key}"`,
              path: [{
                type: "object",
                origin: "key",
                input,
                key,
                value: input[key]
              }]
            });
            if (config$1.abortEarly)
              break;
          }
        }
        if (!dataset.issues || !config$1.abortEarly) {
          for (const key in input)
            if (/* @__PURE__ */ _isValidObjectKey(input, key) && !(key in this.entries))
              dataset.value[key] = input[key];
        }
      } else
        _addIssue(this, "type", dataset, config$1);
      return dataset;
    }
  };
}
__name(looseObject, "looseObject");
function nullable(wrapped, default_) {
  return {
    kind: "schema",
    type: "nullable",
    reference: nullable,
    expects: `(${wrapped.expects} | null)`,
    async: false,
    wrapped,
    default: default_,
    get "~standard"() {
      return /* @__PURE__ */ _getStandardProps(this);
    },
    "~run"(dataset, config$1) {
      if (dataset.value === null) {
        if (this.default !== void 0)
          dataset.value = /* @__PURE__ */ getDefault(this, dataset, config$1);
        if (dataset.value === null) {
          dataset.typed = true;
          return dataset;
        }
      }
      return this.wrapped["~run"](dataset, config$1);
    }
  };
}
__name(nullable, "nullable");
function number(message$1) {
  return {
    kind: "schema",
    type: "number",
    reference: number,
    expects: "number",
    async: false,
    message: message$1,
    get "~standard"() {
      return /* @__PURE__ */ _getStandardProps(this);
    },
    "~run"(dataset, config$1) {
      if (typeof dataset.value === "number" && !isNaN(dataset.value))
        dataset.typed = true;
      else
        _addIssue(this, "type", dataset, config$1);
      return dataset;
    }
  };
}
__name(number, "number");
function object(entries$1, message$1) {
  return {
    kind: "schema",
    type: "object",
    reference: object,
    expects: "Object",
    async: false,
    entries: entries$1,
    message: message$1,
    get "~standard"() {
      return /* @__PURE__ */ _getStandardProps(this);
    },
    "~run"(dataset, config$1) {
      const input = dataset.value;
      if (input && typeof input === "object") {
        dataset.typed = true;
        dataset.value = {};
        for (const key in this.entries) {
          const valueSchema = this.entries[key];
          if (key in input || (valueSchema.type === "exact_optional" || valueSchema.type === "optional" || valueSchema.type === "nullish") && valueSchema.default !== void 0) {
            const value$1 = key in input ? input[key] : /* @__PURE__ */ getDefault(valueSchema);
            const valueDataset = valueSchema["~run"]({ value: value$1 }, config$1);
            if (valueDataset.issues) {
              const pathItem = {
                type: "object",
                origin: "value",
                input,
                key,
                value: value$1
              };
              for (const issue of valueDataset.issues) {
                if (issue.path)
                  issue.path.unshift(pathItem);
                else
                  issue.path = [pathItem];
                dataset.issues?.push(issue);
              }
              if (!dataset.issues)
                dataset.issues = valueDataset.issues;
              if (config$1.abortEarly) {
                dataset.typed = false;
                break;
              }
            }
            if (!valueDataset.typed)
              dataset.typed = false;
            dataset.value[key] = valueDataset.value;
          } else if (valueSchema.fallback !== void 0)
            dataset.value[key] = /* @__PURE__ */ getFallback(valueSchema);
          else if (valueSchema.type !== "exact_optional" && valueSchema.type !== "optional" && valueSchema.type !== "nullish") {
            _addIssue(this, "key", dataset, config$1, {
              input: void 0,
              expected: `"${key}"`,
              path: [{
                type: "object",
                origin: "key",
                input,
                key,
                value: input[key]
              }]
            });
            if (config$1.abortEarly)
              break;
          }
        }
      } else
        _addIssue(this, "type", dataset, config$1);
      return dataset;
    }
  };
}
__name(object, "object");
function optional(wrapped, default_) {
  return {
    kind: "schema",
    type: "optional",
    reference: optional,
    expects: `(${wrapped.expects} | undefined)`,
    async: false,
    wrapped,
    default: default_,
    get "~standard"() {
      return /* @__PURE__ */ _getStandardProps(this);
    },
    "~run"(dataset, config$1) {
      if (dataset.value === void 0) {
        if (this.default !== void 0)
          dataset.value = /* @__PURE__ */ getDefault(this, dataset, config$1);
        if (dataset.value === void 0) {
          dataset.typed = true;
          return dataset;
        }
      }
      return this.wrapped["~run"](dataset, config$1);
    }
  };
}
__name(optional, "optional");
function string(message$1) {
  return {
    kind: "schema",
    type: "string",
    reference: string,
    expects: "string",
    async: false,
    message: message$1,
    get "~standard"() {
      return /* @__PURE__ */ _getStandardProps(this);
    },
    "~run"(dataset, config$1) {
      if (typeof dataset.value === "string")
        dataset.typed = true;
      else
        _addIssue(this, "type", dataset, config$1);
      return dataset;
    }
  };
}
__name(string, "string");
function _subIssues(datasets) {
  let issues;
  if (datasets)
    for (const dataset of datasets)
      if (issues)
        issues.push(...dataset.issues);
      else
        issues = dataset.issues;
  return issues;
}
__name(_subIssues, "_subIssues");
function union3(options, message$1) {
  return {
    kind: "schema",
    type: "union",
    reference: union3,
    expects: /* @__PURE__ */ _joinExpects(options.map((option) => option.expects), "|"),
    async: false,
    options,
    message: message$1,
    get "~standard"() {
      return /* @__PURE__ */ _getStandardProps(this);
    },
    "~run"(dataset, config$1) {
      let validDataset;
      let typedDatasets;
      let untypedDatasets;
      for (const schema of this.options) {
        const optionDataset = schema["~run"]({ value: dataset.value }, config$1);
        if (optionDataset.typed)
          if (optionDataset.issues)
            if (typedDatasets)
              typedDatasets.push(optionDataset);
            else
              typedDatasets = [optionDataset];
          else {
            validDataset = optionDataset;
            break;
          }
        else if (untypedDatasets)
          untypedDatasets.push(optionDataset);
        else
          untypedDatasets = [optionDataset];
      }
      if (validDataset)
        return validDataset;
      if (typedDatasets) {
        if (typedDatasets.length === 1)
          return typedDatasets[0];
        _addIssue(this, "type", dataset, config$1, { issues: /* @__PURE__ */ _subIssues(typedDatasets) });
        dataset.typed = true;
      } else if (untypedDatasets?.length === 1)
        return untypedDatasets[0];
      else
        _addIssue(this, "type", dataset, config$1, { issues: /* @__PURE__ */ _subIssues(untypedDatasets) });
      return dataset;
    }
  };
}
__name(union3, "union");
function parse26(schema, input, config$1) {
  const dataset = schema["~run"]({ value: input }, /* @__PURE__ */ getGlobalConfig(config$1));
  if (dataset.issues)
    throw new ValiError(dataset.issues);
  return dataset.value;
}
__name(parse26, "parse");
var UserListSchemaValidation = object({
  success: boolean(),
  data: nullable(object({
    users: array(object({
      id: string(),
      identifier: string(),
      data: looseObject({}),
      created_at: string(),
      session_public: nullable(looseObject({})),
      session_private: nullable(looseObject({}))
    })),
    total: number()
  })),
  error: nullable(string())
});
var exports_base64url = {};
__export(exports_base64url, {
  encode: /* @__PURE__ */ __name(() => encode22, "encode"),
  decode: /* @__PURE__ */ __name(() => decode, "decode")
});
var encoder = new TextEncoder();
var decoder = new TextDecoder();
var MAX_INT32 = 2 ** 32;
function concat(...buffers) {
  const size = buffers.reduce((acc, { length }) => acc + length, 0);
  const buf = new Uint8Array(size);
  let i = 0;
  for (const buffer of buffers) {
    buf.set(buffer, i);
    i += buffer.length;
  }
  return buf;
}
__name(concat, "concat");
function encode2(string2) {
  const bytes = new Uint8Array(string2.length);
  for (let i = 0; i < string2.length; i++) {
    const code = string2.charCodeAt(i);
    if (code > 127) {
      throw new TypeError("non-ASCII string encountered in encode()");
    }
    bytes[i] = code;
  }
  return bytes;
}
__name(encode2, "encode");
function encodeBase64(input) {
  if (Uint8Array.prototype.toBase64) {
    return input.toBase64();
  }
  const CHUNK_SIZE = 32768;
  const arr = [];
  for (let i = 0; i < input.length; i += CHUNK_SIZE) {
    arr.push(String.fromCharCode.apply(null, input.subarray(i, i + CHUNK_SIZE)));
  }
  return btoa(arr.join(""));
}
__name(encodeBase64, "encodeBase64");
function decodeBase64(encoded) {
  if (Uint8Array.fromBase64) {
    return Uint8Array.fromBase64(encoded);
  }
  const binary = atob(encoded);
  const bytes = new Uint8Array(binary.length);
  for (let i = 0; i < binary.length; i++) {
    bytes[i] = binary.charCodeAt(i);
  }
  return bytes;
}
__name(decodeBase64, "decodeBase64");
function decode(input) {
  if (Uint8Array.fromBase64) {
    return Uint8Array.fromBase64(typeof input === "string" ? input : decoder.decode(input), {
      alphabet: "base64url"
    });
  }
  let encoded = input;
  if (encoded instanceof Uint8Array) {
    encoded = decoder.decode(encoded);
  }
  encoded = encoded.replace(/-/g, "+").replace(/_/g, "/");
  try {
    return decodeBase64(encoded);
  } catch {
    throw new TypeError("The input to be decoded is not correctly encoded.");
  }
}
__name(decode, "decode");
function encode22(input) {
  let unencoded = input;
  if (typeof unencoded === "string") {
    unencoded = encoder.encode(unencoded);
  }
  if (Uint8Array.prototype.toBase64) {
    return unencoded.toBase64({ alphabet: "base64url", omitPadding: true });
  }
  return encodeBase64(unencoded).replace(/=/g, "").replace(/\+/g, "-").replace(/\//g, "_");
}
__name(encode22, "encode2");
var unusable = /* @__PURE__ */ __name((name, prop = "algorithm.name") => new TypeError(`CryptoKey does not support this operation, its ${prop} must be ${name}`), "unusable");
var isAlgorithm = /* @__PURE__ */ __name((algorithm, name) => algorithm.name === name, "isAlgorithm");
function getHashLength(hash) {
  return parseInt(hash.name.slice(4), 10);
}
__name(getHashLength, "getHashLength");
function checkHashLength(algorithm, expected) {
  const actual = getHashLength(algorithm.hash);
  if (actual !== expected)
    throw unusable(`SHA-${expected}`, "algorithm.hash");
}
__name(checkHashLength, "checkHashLength");
function getNamedCurve(alg) {
  switch (alg) {
    case "ES256":
      return "P-256";
    case "ES384":
      return "P-384";
    case "ES512":
      return "P-521";
    default:
      throw new Error("unreachable");
  }
}
__name(getNamedCurve, "getNamedCurve");
function checkUsage(key, usage) {
  if (usage && !key.usages.includes(usage)) {
    throw new TypeError(`CryptoKey does not support this operation, its usages must include ${usage}.`);
  }
}
__name(checkUsage, "checkUsage");
function checkSigCryptoKey(key, alg, usage) {
  switch (alg) {
    case "HS256":
    case "HS384":
    case "HS512": {
      if (!isAlgorithm(key.algorithm, "HMAC"))
        throw unusable("HMAC");
      checkHashLength(key.algorithm, parseInt(alg.slice(2), 10));
      break;
    }
    case "RS256":
    case "RS384":
    case "RS512": {
      if (!isAlgorithm(key.algorithm, "RSASSA-PKCS1-v1_5"))
        throw unusable("RSASSA-PKCS1-v1_5");
      checkHashLength(key.algorithm, parseInt(alg.slice(2), 10));
      break;
    }
    case "PS256":
    case "PS384":
    case "PS512": {
      if (!isAlgorithm(key.algorithm, "RSA-PSS"))
        throw unusable("RSA-PSS");
      checkHashLength(key.algorithm, parseInt(alg.slice(2), 10));
      break;
    }
    case "Ed25519":
    case "EdDSA": {
      if (!isAlgorithm(key.algorithm, "Ed25519"))
        throw unusable("Ed25519");
      break;
    }
    case "ML-DSA-44":
    case "ML-DSA-65":
    case "ML-DSA-87": {
      if (!isAlgorithm(key.algorithm, alg))
        throw unusable(alg);
      break;
    }
    case "ES256":
    case "ES384":
    case "ES512": {
      if (!isAlgorithm(key.algorithm, "ECDSA"))
        throw unusable("ECDSA");
      const expected = getNamedCurve(alg);
      const actual = key.algorithm.namedCurve;
      if (actual !== expected)
        throw unusable(expected, "algorithm.namedCurve");
      break;
    }
    default:
      throw new TypeError("CryptoKey does not support this operation");
  }
  checkUsage(key, usage);
}
__name(checkSigCryptoKey, "checkSigCryptoKey");
function message(msg, actual, ...types) {
  types = types.filter(Boolean);
  if (types.length > 2) {
    const last = types.pop();
    msg += `one of type ${types.join(", ")}, or ${last}.`;
  } else if (types.length === 2) {
    msg += `one of type ${types[0]} or ${types[1]}.`;
  } else {
    msg += `of type ${types[0]}.`;
  }
  if (actual == null) {
    msg += ` Received ${actual}`;
  } else if (typeof actual === "function" && actual.name) {
    msg += ` Received function ${actual.name}`;
  } else if (typeof actual === "object" && actual != null) {
    if (actual.constructor?.name) {
      msg += ` Received an instance of ${actual.constructor.name}`;
    }
  }
  return msg;
}
__name(message, "message");
var invalidKeyInput = /* @__PURE__ */ __name((actual, ...types) => message("Key must be ", actual, ...types), "invalidKeyInput");
var withAlg = /* @__PURE__ */ __name((alg, actual, ...types) => message(`Key for the ${alg} algorithm must be `, actual, ...types), "withAlg");
var exports_errors = {};
__export(exports_errors, {
  JWTInvalid: /* @__PURE__ */ __name(() => JWTInvalid, "JWTInvalid"),
  JWTExpired: /* @__PURE__ */ __name(() => JWTExpired, "JWTExpired"),
  JWTClaimValidationFailed: /* @__PURE__ */ __name(() => JWTClaimValidationFailed, "JWTClaimValidationFailed"),
  JWSSignatureVerificationFailed: /* @__PURE__ */ __name(() => JWSSignatureVerificationFailed, "JWSSignatureVerificationFailed"),
  JWSInvalid: /* @__PURE__ */ __name(() => JWSInvalid, "JWSInvalid"),
  JWKSTimeout: /* @__PURE__ */ __name(() => JWKSTimeout, "JWKSTimeout"),
  JWKSNoMatchingKey: /* @__PURE__ */ __name(() => JWKSNoMatchingKey, "JWKSNoMatchingKey"),
  JWKSMultipleMatchingKeys: /* @__PURE__ */ __name(() => JWKSMultipleMatchingKeys, "JWKSMultipleMatchingKeys"),
  JWKSInvalid: /* @__PURE__ */ __name(() => JWKSInvalid, "JWKSInvalid"),
  JWKInvalid: /* @__PURE__ */ __name(() => JWKInvalid, "JWKInvalid"),
  JWEInvalid: /* @__PURE__ */ __name(() => JWEInvalid, "JWEInvalid"),
  JWEDecryptionFailed: /* @__PURE__ */ __name(() => JWEDecryptionFailed, "JWEDecryptionFailed"),
  JOSENotSupported: /* @__PURE__ */ __name(() => JOSENotSupported, "JOSENotSupported"),
  JOSEError: /* @__PURE__ */ __name(() => JOSEError, "JOSEError"),
  JOSEAlgNotAllowed: /* @__PURE__ */ __name(() => JOSEAlgNotAllowed, "JOSEAlgNotAllowed")
});
var JOSEError = class extends Error {
  static {
    __name(this, "JOSEError");
  }
  static code = "ERR_JOSE_GENERIC";
  code = "ERR_JOSE_GENERIC";
  constructor(message2, options) {
    super(message2, options);
    this.name = this.constructor.name;
    Error.captureStackTrace?.(this, this.constructor);
  }
};
var JWTClaimValidationFailed = class extends JOSEError {
  static {
    __name(this, "JWTClaimValidationFailed");
  }
  static code = "ERR_JWT_CLAIM_VALIDATION_FAILED";
  code = "ERR_JWT_CLAIM_VALIDATION_FAILED";
  claim;
  reason;
  payload;
  constructor(message2, payload, claim = "unspecified", reason = "unspecified") {
    super(message2, { cause: { claim, reason, payload } });
    this.claim = claim;
    this.reason = reason;
    this.payload = payload;
  }
};
var JWTExpired = class extends JOSEError {
  static {
    __name(this, "JWTExpired");
  }
  static code = "ERR_JWT_EXPIRED";
  code = "ERR_JWT_EXPIRED";
  claim;
  reason;
  payload;
  constructor(message2, payload, claim = "unspecified", reason = "unspecified") {
    super(message2, { cause: { claim, reason, payload } });
    this.claim = claim;
    this.reason = reason;
    this.payload = payload;
  }
};
var JOSEAlgNotAllowed = class extends JOSEError {
  static {
    __name(this, "JOSEAlgNotAllowed");
  }
  static code = "ERR_JOSE_ALG_NOT_ALLOWED";
  code = "ERR_JOSE_ALG_NOT_ALLOWED";
};
var JOSENotSupported = class extends JOSEError {
  static {
    __name(this, "JOSENotSupported");
  }
  static code = "ERR_JOSE_NOT_SUPPORTED";
  code = "ERR_JOSE_NOT_SUPPORTED";
};
var JWEDecryptionFailed = class extends JOSEError {
  static {
    __name(this, "JWEDecryptionFailed");
  }
  static code = "ERR_JWE_DECRYPTION_FAILED";
  code = "ERR_JWE_DECRYPTION_FAILED";
  constructor(message2 = "decryption operation failed", options) {
    super(message2, options);
  }
};
var JWEInvalid = class extends JOSEError {
  static {
    __name(this, "JWEInvalid");
  }
  static code = "ERR_JWE_INVALID";
  code = "ERR_JWE_INVALID";
};
var JWSInvalid = class extends JOSEError {
  static {
    __name(this, "JWSInvalid");
  }
  static code = "ERR_JWS_INVALID";
  code = "ERR_JWS_INVALID";
};
var JWTInvalid = class extends JOSEError {
  static {
    __name(this, "JWTInvalid");
  }
  static code = "ERR_JWT_INVALID";
  code = "ERR_JWT_INVALID";
};
var JWKInvalid = class extends JOSEError {
  static {
    __name(this, "JWKInvalid");
  }
  static code = "ERR_JWK_INVALID";
  code = "ERR_JWK_INVALID";
};
var JWKSInvalid = class extends JOSEError {
  static {
    __name(this, "JWKSInvalid");
  }
  static code = "ERR_JWKS_INVALID";
  code = "ERR_JWKS_INVALID";
};
var JWKSNoMatchingKey = class extends JOSEError {
  static {
    __name(this, "JWKSNoMatchingKey");
  }
  static code = "ERR_JWKS_NO_MATCHING_KEY";
  code = "ERR_JWKS_NO_MATCHING_KEY";
  constructor(message2 = "no applicable key found in the JSON Web Key Set", options) {
    super(message2, options);
  }
};
var JWKSMultipleMatchingKeys = class extends JOSEError {
  static {
    __name(this, "JWKSMultipleMatchingKeys");
  }
  [Symbol.asyncIterator];
  static code = "ERR_JWKS_MULTIPLE_MATCHING_KEYS";
  code = "ERR_JWKS_MULTIPLE_MATCHING_KEYS";
  constructor(message2 = "multiple matching keys found in the JSON Web Key Set", options) {
    super(message2, options);
  }
};
var JWKSTimeout = class extends JOSEError {
  static {
    __name(this, "JWKSTimeout");
  }
  static code = "ERR_JWKS_TIMEOUT";
  code = "ERR_JWKS_TIMEOUT";
  constructor(message2 = "request timed out", options) {
    super(message2, options);
  }
};
var JWSSignatureVerificationFailed = class extends JOSEError {
  static {
    __name(this, "JWSSignatureVerificationFailed");
  }
  static code = "ERR_JWS_SIGNATURE_VERIFICATION_FAILED";
  code = "ERR_JWS_SIGNATURE_VERIFICATION_FAILED";
  constructor(message2 = "signature verification failed", options) {
    super(message2, options);
  }
};
var isCryptoKey = /* @__PURE__ */ __name((key) => {
  if (key?.[Symbol.toStringTag] === "CryptoKey")
    return true;
  try {
    return key instanceof CryptoKey;
  } catch {
    return false;
  }
}, "isCryptoKey");
var isKeyObject = /* @__PURE__ */ __name((key) => key?.[Symbol.toStringTag] === "KeyObject", "isKeyObject");
var isKeyLike = /* @__PURE__ */ __name((key) => isCryptoKey(key) || isKeyObject(key), "isKeyLike");
function decodeBase64url(value, label, ErrorClass) {
  try {
    return decode(value);
  } catch {
    throw new ErrorClass(`Failed to base64url decode the ${label}`);
  }
}
__name(decodeBase64url, "decodeBase64url");
var isObjectLike = /* @__PURE__ */ __name((value) => typeof value === "object" && value !== null, "isObjectLike");
function isObject(input) {
  if (!isObjectLike(input) || Object.prototype.toString.call(input) !== "[object Object]") {
    return false;
  }
  if (Object.getPrototypeOf(input) === null) {
    return true;
  }
  let proto = input;
  while (Object.getPrototypeOf(proto) !== null) {
    proto = Object.getPrototypeOf(proto);
  }
  return Object.getPrototypeOf(input) === proto;
}
__name(isObject, "isObject");
function isDisjoint(...headers) {
  const sources = headers.filter(Boolean);
  if (sources.length === 0 || sources.length === 1) {
    return true;
  }
  let acc;
  for (const header of sources) {
    const parameters = Object.keys(header);
    if (!acc || acc.size === 0) {
      acc = new Set(parameters);
      continue;
    }
    for (const parameter of parameters) {
      if (acc.has(parameter)) {
        return false;
      }
      acc.add(parameter);
    }
  }
  return true;
}
__name(isDisjoint, "isDisjoint");
var isJWK = /* @__PURE__ */ __name((key) => isObject(key) && typeof key.kty === "string", "isJWK");
var isPrivateJWK = /* @__PURE__ */ __name((key) => key.kty !== "oct" && (key.kty === "AKP" && typeof key.priv === "string" || typeof key.d === "string"), "isPrivateJWK");
var isPublicJWK = /* @__PURE__ */ __name((key) => key.kty !== "oct" && key.d === void 0 && key.priv === void 0, "isPublicJWK");
var isSecretJWK = /* @__PURE__ */ __name((key) => key.kty === "oct" && typeof key.k === "string", "isSecretJWK");
function checkKeyLength(alg, key) {
  if (alg.startsWith("RS") || alg.startsWith("PS")) {
    const { modulusLength } = key.algorithm;
    if (typeof modulusLength !== "number" || modulusLength < 2048) {
      throw new TypeError(`${alg} requires key modulusLength to be 2048 bits or larger`);
    }
  }
}
__name(checkKeyLength, "checkKeyLength");
function subtleAlgorithm(alg, algorithm) {
  const hash = `SHA-${alg.slice(-3)}`;
  switch (alg) {
    case "HS256":
    case "HS384":
    case "HS512":
      return { hash, name: "HMAC" };
    case "PS256":
    case "PS384":
    case "PS512":
      return { hash, name: "RSA-PSS", saltLength: parseInt(alg.slice(-3), 10) >> 3 };
    case "RS256":
    case "RS384":
    case "RS512":
      return { hash, name: "RSASSA-PKCS1-v1_5" };
    case "ES256":
    case "ES384":
    case "ES512":
      return { hash, name: "ECDSA", namedCurve: algorithm.namedCurve };
    case "Ed25519":
    case "EdDSA":
      return { name: "Ed25519" };
    case "ML-DSA-44":
    case "ML-DSA-65":
    case "ML-DSA-87":
      return { name: alg };
    default:
      throw new JOSENotSupported(`alg ${alg} is not supported either by JOSE or your javascript runtime`);
  }
}
__name(subtleAlgorithm, "subtleAlgorithm");
async function getSigKey(alg, key, usage) {
  if (key instanceof Uint8Array) {
    if (!alg.startsWith("HS")) {
      throw new TypeError(invalidKeyInput(key, "CryptoKey", "KeyObject", "JSON Web Key"));
    }
    return crypto.subtle.importKey("raw", key, { hash: `SHA-${alg.slice(-3)}`, name: "HMAC" }, false, [usage]);
  }
  checkSigCryptoKey(key, alg, usage);
  return key;
}
__name(getSigKey, "getSigKey");
async function verify(alg, key, signature, data) {
  const cryptoKey = await getSigKey(alg, key, "verify");
  checkKeyLength(alg, cryptoKey);
  const algorithm = subtleAlgorithm(alg, cryptoKey.algorithm);
  try {
    return await crypto.subtle.verify(algorithm, cryptoKey, signature, data);
  } catch {
    return false;
  }
}
__name(verify, "verify");
var unsupportedAlg = 'Invalid or unsupported JWK "alg" (Algorithm) Parameter value';
function subtleMapping(jwk) {
  let algorithm;
  let keyUsages;
  switch (jwk.kty) {
    case "AKP": {
      switch (jwk.alg) {
        case "ML-DSA-44":
        case "ML-DSA-65":
        case "ML-DSA-87":
          algorithm = { name: jwk.alg };
          keyUsages = jwk.priv ? ["sign"] : ["verify"];
          break;
        default:
          throw new JOSENotSupported(unsupportedAlg);
      }
      break;
    }
    case "RSA": {
      switch (jwk.alg) {
        case "PS256":
        case "PS384":
        case "PS512":
          algorithm = { name: "RSA-PSS", hash: `SHA-${jwk.alg.slice(-3)}` };
          keyUsages = jwk.d ? ["sign"] : ["verify"];
          break;
        case "RS256":
        case "RS384":
        case "RS512":
          algorithm = { name: "RSASSA-PKCS1-v1_5", hash: `SHA-${jwk.alg.slice(-3)}` };
          keyUsages = jwk.d ? ["sign"] : ["verify"];
          break;
        case "RSA-OAEP":
        case "RSA-OAEP-256":
        case "RSA-OAEP-384":
        case "RSA-OAEP-512":
          algorithm = {
            name: "RSA-OAEP",
            hash: `SHA-${parseInt(jwk.alg.slice(-3), 10) || 1}`
          };
          keyUsages = jwk.d ? ["decrypt", "unwrapKey"] : ["encrypt", "wrapKey"];
          break;
        default:
          throw new JOSENotSupported(unsupportedAlg);
      }
      break;
    }
    case "EC": {
      switch (jwk.alg) {
        case "ES256":
        case "ES384":
        case "ES512":
          algorithm = {
            name: "ECDSA",
            namedCurve: { ES256: "P-256", ES384: "P-384", ES512: "P-521" }[jwk.alg]
          };
          keyUsages = jwk.d ? ["sign"] : ["verify"];
          break;
        case "ECDH-ES":
        case "ECDH-ES+A128KW":
        case "ECDH-ES+A192KW":
        case "ECDH-ES+A256KW":
          algorithm = { name: "ECDH", namedCurve: jwk.crv };
          keyUsages = jwk.d ? ["deriveBits"] : [];
          break;
        default:
          throw new JOSENotSupported(unsupportedAlg);
      }
      break;
    }
    case "OKP": {
      switch (jwk.alg) {
        case "Ed25519":
        case "EdDSA":
          algorithm = { name: "Ed25519" };
          keyUsages = jwk.d ? ["sign"] : ["verify"];
          break;
        case "ECDH-ES":
        case "ECDH-ES+A128KW":
        case "ECDH-ES+A192KW":
        case "ECDH-ES+A256KW":
          algorithm = { name: jwk.crv };
          keyUsages = jwk.d ? ["deriveBits"] : [];
          break;
        default:
          throw new JOSENotSupported(unsupportedAlg);
      }
      break;
    }
    default:
      throw new JOSENotSupported('Invalid or unsupported JWK "kty" (Key Type) Parameter value');
  }
  return { algorithm, keyUsages };
}
__name(subtleMapping, "subtleMapping");
async function jwkToKey(jwk) {
  if (!jwk.alg) {
    throw new TypeError('"alg" argument is required when "jwk.alg" is not present');
  }
  const { algorithm, keyUsages } = subtleMapping(jwk);
  const keyData = { ...jwk };
  if (keyData.kty !== "AKP") {
    delete keyData.alg;
  }
  delete keyData.use;
  return crypto.subtle.importKey("jwk", keyData, algorithm, jwk.ext ?? (jwk.d || jwk.priv ? false : true), jwk.key_ops ?? keyUsages);
}
__name(jwkToKey, "jwkToKey");
var unusableForAlg = "given KeyObject instance cannot be used for this algorithm";
var cache;
var handleJWK = /* @__PURE__ */ __name(async (key, jwk, alg, freeze = false) => {
  cache ||= /* @__PURE__ */ new WeakMap();
  let cached = cache.get(key);
  if (cached?.[alg]) {
    return cached[alg];
  }
  const cryptoKey = await jwkToKey({ ...jwk, alg });
  if (freeze)
    Object.freeze(key);
  if (!cached) {
    cache.set(key, { [alg]: cryptoKey });
  } else {
    cached[alg] = cryptoKey;
  }
  return cryptoKey;
}, "handleJWK");
var handleKeyObject = /* @__PURE__ */ __name((keyObject, alg) => {
  cache ||= /* @__PURE__ */ new WeakMap();
  let cached = cache.get(keyObject);
  if (cached?.[alg]) {
    return cached[alg];
  }
  const isPublic = keyObject.type === "public";
  const extractable = isPublic ? true : false;
  let cryptoKey;
  if (keyObject.asymmetricKeyType === "x25519") {
    switch (alg) {
      case "ECDH-ES":
      case "ECDH-ES+A128KW":
      case "ECDH-ES+A192KW":
      case "ECDH-ES+A256KW":
        break;
      default:
        throw new TypeError(unusableForAlg);
    }
    cryptoKey = keyObject.toCryptoKey(keyObject.asymmetricKeyType, extractable, isPublic ? [] : ["deriveBits"]);
  }
  if (keyObject.asymmetricKeyType === "ed25519") {
    if (alg !== "EdDSA" && alg !== "Ed25519") {
      throw new TypeError(unusableForAlg);
    }
    cryptoKey = keyObject.toCryptoKey(keyObject.asymmetricKeyType, extractable, [
      isPublic ? "verify" : "sign"
    ]);
  }
  switch (keyObject.asymmetricKeyType) {
    case "ml-dsa-44":
    case "ml-dsa-65":
    case "ml-dsa-87": {
      if (alg !== keyObject.asymmetricKeyType.toUpperCase()) {
        throw new TypeError(unusableForAlg);
      }
      cryptoKey = keyObject.toCryptoKey(keyObject.asymmetricKeyType, extractable, [
        isPublic ? "verify" : "sign"
      ]);
    }
  }
  if (keyObject.asymmetricKeyType === "rsa") {
    let hash;
    switch (alg) {
      case "RSA-OAEP":
        hash = "SHA-1";
        break;
      case "RS256":
      case "PS256":
      case "RSA-OAEP-256":
        hash = "SHA-256";
        break;
      case "RS384":
      case "PS384":
      case "RSA-OAEP-384":
        hash = "SHA-384";
        break;
      case "RS512":
      case "PS512":
      case "RSA-OAEP-512":
        hash = "SHA-512";
        break;
      default:
        throw new TypeError(unusableForAlg);
    }
    if (alg.startsWith("RSA-OAEP")) {
      return keyObject.toCryptoKey({
        name: "RSA-OAEP",
        hash
      }, extractable, isPublic ? ["encrypt"] : ["decrypt"]);
    }
    cryptoKey = keyObject.toCryptoKey({
      name: alg.startsWith("PS") ? "RSA-PSS" : "RSASSA-PKCS1-v1_5",
      hash
    }, extractable, [isPublic ? "verify" : "sign"]);
  }
  if (keyObject.asymmetricKeyType === "ec") {
    const nist = /* @__PURE__ */ new Map([
      ["prime256v1", "P-256"],
      ["secp384r1", "P-384"],
      ["secp521r1", "P-521"]
    ]);
    const namedCurve = nist.get(keyObject.asymmetricKeyDetails?.namedCurve);
    if (!namedCurve) {
      throw new TypeError(unusableForAlg);
    }
    const expectedCurve = { ES256: "P-256", ES384: "P-384", ES512: "P-521" };
    if (expectedCurve[alg] && namedCurve === expectedCurve[alg]) {
      cryptoKey = keyObject.toCryptoKey({
        name: "ECDSA",
        namedCurve
      }, extractable, [isPublic ? "verify" : "sign"]);
    }
    if (alg.startsWith("ECDH-ES")) {
      cryptoKey = keyObject.toCryptoKey({
        name: "ECDH",
        namedCurve
      }, extractable, isPublic ? [] : ["deriveBits"]);
    }
  }
  if (!cryptoKey) {
    throw new TypeError(unusableForAlg);
  }
  if (!cached) {
    cache.set(keyObject, { [alg]: cryptoKey });
  } else {
    cached[alg] = cryptoKey;
  }
  return cryptoKey;
}, "handleKeyObject");
async function normalizeKey(key, alg) {
  if (key instanceof Uint8Array) {
    return key;
  }
  if (isCryptoKey(key)) {
    return key;
  }
  if (isKeyObject(key)) {
    if (key.type === "secret") {
      return key.export();
    }
    if ("toCryptoKey" in key && typeof key.toCryptoKey === "function") {
      try {
        return handleKeyObject(key, alg);
      } catch (err) {
        if (err instanceof TypeError) {
          throw err;
        }
      }
    }
    let jwk = key.export({ format: "jwk" });
    return handleJWK(key, jwk, alg);
  }
  if (isJWK(key)) {
    if (key.k) {
      return decode(key.k);
    }
    return handleJWK(key, key, alg, true);
  }
  throw new Error("unreachable");
}
__name(normalizeKey, "normalizeKey");
async function importJWK(jwk, alg, options) {
  if (!isObject(jwk)) {
    throw new TypeError("JWK must be an object");
  }
  let ext;
  alg ??= jwk.alg;
  ext ??= options?.extractable ?? jwk.ext;
  switch (jwk.kty) {
    case "oct":
      if (typeof jwk.k !== "string" || !jwk.k) {
        throw new TypeError('missing "k" (Key Value) Parameter value');
      }
      return decode(jwk.k);
    case "RSA":
      if ("oth" in jwk && jwk.oth !== void 0) {
        throw new JOSENotSupported('RSA JWK "oth" (Other Primes Info) Parameter value is not supported');
      }
      return jwkToKey({ ...jwk, alg, ext });
    case "AKP": {
      if (typeof jwk.alg !== "string" || !jwk.alg) {
        throw new TypeError('missing "alg" (Algorithm) Parameter value');
      }
      if (alg !== void 0 && alg !== jwk.alg) {
        throw new TypeError("JWK alg and alg option value mismatch");
      }
      return jwkToKey({ ...jwk, ext });
    }
    case "EC":
    case "OKP":
      return jwkToKey({ ...jwk, alg, ext });
    default:
      throw new JOSENotSupported('Unsupported "kty" (Key Type) Parameter value');
  }
}
__name(importJWK, "importJWK");
function validateCrit(Err, recognizedDefault, recognizedOption, protectedHeader, joseHeader) {
  if (joseHeader.crit !== void 0 && protectedHeader?.crit === void 0) {
    throw new Err('"crit" (Critical) Header Parameter MUST be integrity protected');
  }
  if (!protectedHeader || protectedHeader.crit === void 0) {
    return /* @__PURE__ */ new Set();
  }
  if (!Array.isArray(protectedHeader.crit) || protectedHeader.crit.length === 0 || protectedHeader.crit.some((input) => typeof input !== "string" || input.length === 0)) {
    throw new Err('"crit" (Critical) Header Parameter MUST be an array of non-empty strings when present');
  }
  let recognized;
  if (recognizedOption !== void 0) {
    recognized = new Map([...Object.entries(recognizedOption), ...recognizedDefault.entries()]);
  } else {
    recognized = recognizedDefault;
  }
  for (const parameter of protectedHeader.crit) {
    if (!recognized.has(parameter)) {
      throw new JOSENotSupported(`Extension Header Parameter "${parameter}" is not recognized`);
    }
    if (joseHeader[parameter] === void 0) {
      throw new Err(`Extension Header Parameter "${parameter}" is missing`);
    }
    if (recognized.get(parameter) && protectedHeader[parameter] === void 0) {
      throw new Err(`Extension Header Parameter "${parameter}" MUST be integrity protected`);
    }
  }
  return new Set(protectedHeader.crit);
}
__name(validateCrit, "validateCrit");
function validateAlgorithms(option, algorithms) {
  if (algorithms !== void 0 && (!Array.isArray(algorithms) || algorithms.some((s) => typeof s !== "string"))) {
    throw new TypeError(`"${option}" option must be an array of strings`);
  }
  if (!algorithms) {
    return;
  }
  return new Set(algorithms);
}
__name(validateAlgorithms, "validateAlgorithms");
var tag = /* @__PURE__ */ __name((key) => key?.[Symbol.toStringTag], "tag");
var jwkMatchesOp = /* @__PURE__ */ __name((alg, key, usage) => {
  if (key.use !== void 0) {
    let expected;
    switch (usage) {
      case "sign":
      case "verify":
        expected = "sig";
        break;
      case "encrypt":
      case "decrypt":
        expected = "enc";
        break;
    }
    if (key.use !== expected) {
      throw new TypeError(`Invalid key for this operation, its "use" must be "${expected}" when present`);
    }
  }
  if (key.alg !== void 0 && key.alg !== alg) {
    throw new TypeError(`Invalid key for this operation, its "alg" must be "${alg}" when present`);
  }
  if (Array.isArray(key.key_ops)) {
    let expectedKeyOp;
    switch (true) {
      case (usage === "sign" || usage === "verify"):
      case alg === "dir":
      case alg.includes("CBC-HS"):
        expectedKeyOp = usage;
        break;
      case alg.startsWith("PBES2"):
        expectedKeyOp = "deriveBits";
        break;
      case /^A\d{3}(?:GCM)?(?:KW)?$/.test(alg):
        if (!alg.includes("GCM") && alg.endsWith("KW")) {
          expectedKeyOp = usage === "encrypt" ? "wrapKey" : "unwrapKey";
        } else {
          expectedKeyOp = usage;
        }
        break;
      case (usage === "encrypt" && alg.startsWith("RSA")):
        expectedKeyOp = "wrapKey";
        break;
      case usage === "decrypt":
        expectedKeyOp = alg.startsWith("RSA") ? "unwrapKey" : "deriveBits";
        break;
    }
    if (expectedKeyOp && key.key_ops?.includes?.(expectedKeyOp) === false) {
      throw new TypeError(`Invalid key for this operation, its "key_ops" must include "${expectedKeyOp}" when present`);
    }
  }
  return true;
}, "jwkMatchesOp");
var symmetricTypeCheck = /* @__PURE__ */ __name((alg, key, usage) => {
  if (key instanceof Uint8Array)
    return;
  if (isJWK(key)) {
    if (isSecretJWK(key) && jwkMatchesOp(alg, key, usage))
      return;
    throw new TypeError(`JSON Web Key for symmetric algorithms must have JWK "kty" (Key Type) equal to "oct" and the JWK "k" (Key Value) present`);
  }
  if (!isKeyLike(key)) {
    throw new TypeError(withAlg(alg, key, "CryptoKey", "KeyObject", "JSON Web Key", "Uint8Array"));
  }
  if (key.type !== "secret") {
    throw new TypeError(`${tag(key)} instances for symmetric algorithms must be of type "secret"`);
  }
}, "symmetricTypeCheck");
var asymmetricTypeCheck = /* @__PURE__ */ __name((alg, key, usage) => {
  if (isJWK(key)) {
    switch (usage) {
      case "decrypt":
      case "sign":
        if (isPrivateJWK(key) && jwkMatchesOp(alg, key, usage))
          return;
        throw new TypeError(`JSON Web Key for this operation must be a private JWK`);
      case "encrypt":
      case "verify":
        if (isPublicJWK(key) && jwkMatchesOp(alg, key, usage))
          return;
        throw new TypeError(`JSON Web Key for this operation must be a public JWK`);
    }
  }
  if (!isKeyLike(key)) {
    throw new TypeError(withAlg(alg, key, "CryptoKey", "KeyObject", "JSON Web Key"));
  }
  if (key.type === "secret") {
    throw new TypeError(`${tag(key)} instances for asymmetric algorithms must not be of type "secret"`);
  }
  if (key.type === "public") {
    switch (usage) {
      case "sign":
        throw new TypeError(`${tag(key)} instances for asymmetric algorithm signing must be of type "private"`);
      case "decrypt":
        throw new TypeError(`${tag(key)} instances for asymmetric algorithm decryption must be of type "private"`);
    }
  }
  if (key.type === "private") {
    switch (usage) {
      case "verify":
        throw new TypeError(`${tag(key)} instances for asymmetric algorithm verifying must be of type "public"`);
      case "encrypt":
        throw new TypeError(`${tag(key)} instances for asymmetric algorithm encryption must be of type "public"`);
    }
  }
}, "asymmetricTypeCheck");
function checkKeyType(alg, key, usage) {
  switch (alg.substring(0, 2)) {
    case "A1":
    case "A2":
    case "di":
    case "HS":
    case "PB":
      symmetricTypeCheck(alg, key, usage);
      break;
    default:
      asymmetricTypeCheck(alg, key, usage);
  }
}
__name(checkKeyType, "checkKeyType");
async function flattenedVerify(jws, key, options) {
  if (!isObject(jws)) {
    throw new JWSInvalid("Flattened JWS must be an object");
  }
  if (jws.protected === void 0 && jws.header === void 0) {
    throw new JWSInvalid('Flattened JWS must have either of the "protected" or "header" members');
  }
  if (jws.protected !== void 0 && typeof jws.protected !== "string") {
    throw new JWSInvalid("JWS Protected Header incorrect type");
  }
  if (jws.payload === void 0) {
    throw new JWSInvalid("JWS Payload missing");
  }
  if (typeof jws.signature !== "string") {
    throw new JWSInvalid("JWS Signature missing or incorrect type");
  }
  if (jws.header !== void 0 && !isObject(jws.header)) {
    throw new JWSInvalid("JWS Unprotected Header incorrect type");
  }
  let parsedProt = {};
  if (jws.protected) {
    try {
      const protectedHeader = decode(jws.protected);
      parsedProt = JSON.parse(decoder.decode(protectedHeader));
    } catch {
      throw new JWSInvalid("JWS Protected Header is invalid");
    }
  }
  if (!isDisjoint(parsedProt, jws.header)) {
    throw new JWSInvalid("JWS Protected and JWS Unprotected Header Parameter names must be disjoint");
  }
  const joseHeader = {
    ...parsedProt,
    ...jws.header
  };
  const extensions = validateCrit(JWSInvalid, /* @__PURE__ */ new Map([["b64", true]]), options?.crit, parsedProt, joseHeader);
  let b64 = true;
  if (extensions.has("b64")) {
    b64 = parsedProt.b64;
    if (typeof b64 !== "boolean") {
      throw new JWSInvalid('The "b64" (base64url-encode payload) Header Parameter must be a boolean');
    }
  }
  const { alg } = joseHeader;
  if (typeof alg !== "string" || !alg) {
    throw new JWSInvalid('JWS "alg" (Algorithm) Header Parameter missing or invalid');
  }
  const algorithms = options && validateAlgorithms("algorithms", options.algorithms);
  if (algorithms && !algorithms.has(alg)) {
    throw new JOSEAlgNotAllowed('"alg" (Algorithm) Header Parameter value not allowed');
  }
  if (b64) {
    if (typeof jws.payload !== "string") {
      throw new JWSInvalid("JWS Payload must be a string");
    }
  } else if (typeof jws.payload !== "string" && !(jws.payload instanceof Uint8Array)) {
    throw new JWSInvalid("JWS Payload must be a string or an Uint8Array instance");
  }
  let resolvedKey = false;
  if (typeof key === "function") {
    key = await key(parsedProt, jws);
    resolvedKey = true;
  }
  checkKeyType(alg, key, "verify");
  const data = concat(jws.protected !== void 0 ? encode2(jws.protected) : new Uint8Array(), encode2("."), typeof jws.payload === "string" ? b64 ? encode2(jws.payload) : encoder.encode(jws.payload) : jws.payload);
  const signature = decodeBase64url(jws.signature, "signature", JWSInvalid);
  const k = await normalizeKey(key, alg);
  const verified = await verify(alg, k, signature, data);
  if (!verified) {
    throw new JWSSignatureVerificationFailed();
  }
  let payload;
  if (b64) {
    payload = decodeBase64url(jws.payload, "payload", JWSInvalid);
  } else if (typeof jws.payload === "string") {
    payload = encoder.encode(jws.payload);
  } else {
    payload = jws.payload;
  }
  const result = { payload };
  if (jws.protected !== void 0) {
    result.protectedHeader = parsedProt;
  }
  if (jws.header !== void 0) {
    result.unprotectedHeader = jws.header;
  }
  if (resolvedKey) {
    return { ...result, key: k };
  }
  return result;
}
__name(flattenedVerify, "flattenedVerify");
async function compactVerify(jws, key, options) {
  if (jws instanceof Uint8Array) {
    jws = decoder.decode(jws);
  }
  if (typeof jws !== "string") {
    throw new JWSInvalid("Compact JWS must be a string or Uint8Array");
  }
  const { 0: protectedHeader, 1: payload, 2: signature, length } = jws.split(".");
  if (length !== 3) {
    throw new JWSInvalid("Invalid Compact JWS");
  }
  const verified = await flattenedVerify({ payload, protected: protectedHeader, signature }, key, options);
  const result = { payload: verified.payload, protectedHeader: verified.protectedHeader };
  if (typeof key === "function") {
    return { ...result, key: verified.key };
  }
  return result;
}
__name(compactVerify, "compactVerify");
var epoch = /* @__PURE__ */ __name((date) => Math.floor(date.getTime() / 1e3), "epoch");
var minute = 60;
var hour = minute * 60;
var day = hour * 24;
var week = day * 7;
var year = day * 365.25;
var REGEX = /^(\+|\-)? ?(\d+|\d+\.\d+) ?(seconds?|secs?|s|minutes?|mins?|m|hours?|hrs?|h|days?|d|weeks?|w|years?|yrs?|y)(?: (ago|from now))?$/i;
function secs(str) {
  const matched = REGEX.exec(str);
  if (!matched || matched[4] && matched[1]) {
    throw new TypeError("Invalid time period format");
  }
  const value = parseFloat(matched[2]);
  const unit = matched[3].toLowerCase();
  let numericDate;
  switch (unit) {
    case "sec":
    case "secs":
    case "second":
    case "seconds":
    case "s":
      numericDate = Math.round(value);
      break;
    case "minute":
    case "minutes":
    case "min":
    case "mins":
    case "m":
      numericDate = Math.round(value * minute);
      break;
    case "hour":
    case "hours":
    case "hr":
    case "hrs":
    case "h":
      numericDate = Math.round(value * hour);
      break;
    case "day":
    case "days":
    case "d":
      numericDate = Math.round(value * day);
      break;
    case "week":
    case "weeks":
    case "w":
      numericDate = Math.round(value * week);
      break;
    default:
      numericDate = Math.round(value * year);
      break;
  }
  if (matched[1] === "-" || matched[4] === "ago") {
    return -numericDate;
  }
  return numericDate;
}
__name(secs, "secs");
var normalizeTyp = /* @__PURE__ */ __name((value) => {
  if (value.includes("/")) {
    return value.toLowerCase();
  }
  return `application/${value.toLowerCase()}`;
}, "normalizeTyp");
var checkAudiencePresence = /* @__PURE__ */ __name((audPayload, audOption) => {
  if (typeof audPayload === "string") {
    return audOption.includes(audPayload);
  }
  if (Array.isArray(audPayload)) {
    return audOption.some(Set.prototype.has.bind(new Set(audPayload)));
  }
  return false;
}, "checkAudiencePresence");
function validateClaimsSet(protectedHeader, encodedPayload, options = {}) {
  let payload;
  try {
    payload = JSON.parse(decoder.decode(encodedPayload));
  } catch {
  }
  if (!isObject(payload)) {
    throw new JWTInvalid("JWT Claims Set must be a top-level JSON object");
  }
  const { typ } = options;
  if (typ && (typeof protectedHeader.typ !== "string" || normalizeTyp(protectedHeader.typ) !== normalizeTyp(typ))) {
    throw new JWTClaimValidationFailed('unexpected "typ" JWT header value', payload, "typ", "check_failed");
  }
  const { requiredClaims = [], issuer, subject: subject2, audience, maxTokenAge } = options;
  const presenceCheck = [...requiredClaims];
  if (maxTokenAge !== void 0)
    presenceCheck.push("iat");
  if (audience !== void 0)
    presenceCheck.push("aud");
  if (subject2 !== void 0)
    presenceCheck.push("sub");
  if (issuer !== void 0)
    presenceCheck.push("iss");
  for (const claim of new Set(presenceCheck.reverse())) {
    if (!(claim in payload)) {
      throw new JWTClaimValidationFailed(`missing required "${claim}" claim`, payload, claim, "missing");
    }
  }
  if (issuer && !(Array.isArray(issuer) ? issuer : [issuer]).includes(payload.iss)) {
    throw new JWTClaimValidationFailed('unexpected "iss" claim value', payload, "iss", "check_failed");
  }
  if (subject2 && payload.sub !== subject2) {
    throw new JWTClaimValidationFailed('unexpected "sub" claim value', payload, "sub", "check_failed");
  }
  if (audience && !checkAudiencePresence(payload.aud, typeof audience === "string" ? [audience] : audience)) {
    throw new JWTClaimValidationFailed('unexpected "aud" claim value', payload, "aud", "check_failed");
  }
  let tolerance;
  switch (typeof options.clockTolerance) {
    case "string":
      tolerance = secs(options.clockTolerance);
      break;
    case "number":
      tolerance = options.clockTolerance;
      break;
    case "undefined":
      tolerance = 0;
      break;
    default:
      throw new TypeError("Invalid clockTolerance option type");
  }
  const { currentDate } = options;
  const now = epoch(currentDate || /* @__PURE__ */ new Date());
  if ((payload.iat !== void 0 || maxTokenAge) && typeof payload.iat !== "number") {
    throw new JWTClaimValidationFailed('"iat" claim must be a number', payload, "iat", "invalid");
  }
  if (payload.nbf !== void 0) {
    if (typeof payload.nbf !== "number") {
      throw new JWTClaimValidationFailed('"nbf" claim must be a number', payload, "nbf", "invalid");
    }
    if (payload.nbf > now + tolerance) {
      throw new JWTClaimValidationFailed('"nbf" claim timestamp check failed', payload, "nbf", "check_failed");
    }
  }
  if (payload.exp !== void 0) {
    if (typeof payload.exp !== "number") {
      throw new JWTClaimValidationFailed('"exp" claim must be a number', payload, "exp", "invalid");
    }
    if (payload.exp <= now - tolerance) {
      throw new JWTExpired('"exp" claim timestamp check failed', payload, "exp", "check_failed");
    }
  }
  if (maxTokenAge) {
    const age = now - payload.iat;
    const max = typeof maxTokenAge === "number" ? maxTokenAge : secs(maxTokenAge);
    if (age - tolerance > max) {
      throw new JWTExpired('"iat" claim timestamp check failed (too far in the past)', payload, "iat", "check_failed");
    }
    if (age < 0 - tolerance) {
      throw new JWTClaimValidationFailed('"iat" claim timestamp check failed (it should be in the past)', payload, "iat", "check_failed");
    }
  }
  return payload;
}
__name(validateClaimsSet, "validateClaimsSet");
async function jwtVerify(jwt, key, options) {
  const verified = await compactVerify(jwt, key, options);
  if (verified.protectedHeader.crit?.includes("b64") && verified.protectedHeader.b64 === false) {
    throw new JWTInvalid("JWTs MUST NOT use unencoded payload");
  }
  const payload = validateClaimsSet(verified.protectedHeader, verified.payload, options);
  const result = { payload, protectedHeader: verified.protectedHeader };
  if (typeof key === "function") {
    return { ...result, key: verified.key };
  }
  return result;
}
__name(jwtVerify, "jwtVerify");
function getKtyFromAlg(alg) {
  switch (typeof alg === "string" && alg.slice(0, 2)) {
    case "RS":
    case "PS":
      return "RSA";
    case "ES":
      return "EC";
    case "Ed":
      return "OKP";
    case "ML":
      return "AKP";
    default:
      throw new JOSENotSupported('Unsupported "alg" value for a JSON Web Key Set');
  }
}
__name(getKtyFromAlg, "getKtyFromAlg");
function isJWKSLike(jwks) {
  return jwks && typeof jwks === "object" && Array.isArray(jwks.keys) && jwks.keys.every(isJWKLike);
}
__name(isJWKSLike, "isJWKSLike");
function isJWKLike(key) {
  return isObject(key);
}
__name(isJWKLike, "isJWKLike");
var LocalJWKSet = class {
  static {
    __name(this, "LocalJWKSet");
  }
  #jwks;
  #cached = /* @__PURE__ */ new WeakMap();
  constructor(jwks) {
    if (!isJWKSLike(jwks)) {
      throw new JWKSInvalid("JSON Web Key Set malformed");
    }
    this.#jwks = structuredClone(jwks);
  }
  jwks() {
    return this.#jwks;
  }
  async getKey(protectedHeader, token) {
    const { alg, kid } = { ...protectedHeader, ...token?.header };
    const kty = getKtyFromAlg(alg);
    const candidates = this.#jwks.keys.filter((jwk2) => {
      let candidate = kty === jwk2.kty;
      if (candidate && typeof kid === "string") {
        candidate = kid === jwk2.kid;
      }
      if (candidate && (typeof jwk2.alg === "string" || kty === "AKP")) {
        candidate = alg === jwk2.alg;
      }
      if (candidate && typeof jwk2.use === "string") {
        candidate = jwk2.use === "sig";
      }
      if (candidate && Array.isArray(jwk2.key_ops)) {
        candidate = jwk2.key_ops.includes("verify");
      }
      if (candidate) {
        switch (alg) {
          case "ES256":
            candidate = jwk2.crv === "P-256";
            break;
          case "ES384":
            candidate = jwk2.crv === "P-384";
            break;
          case "ES512":
            candidate = jwk2.crv === "P-521";
            break;
          case "Ed25519":
          case "EdDSA":
            candidate = jwk2.crv === "Ed25519";
            break;
        }
      }
      return candidate;
    });
    const { 0: jwk, length } = candidates;
    if (length === 0) {
      throw new JWKSNoMatchingKey();
    }
    if (length !== 1) {
      const error3 = new JWKSMultipleMatchingKeys();
      const _cached = this.#cached;
      error3[Symbol.asyncIterator] = async function* () {
        for (const jwk2 of candidates) {
          try {
            yield await importWithAlgCache(_cached, jwk2, alg);
          } catch {
          }
        }
      };
      throw error3;
    }
    return importWithAlgCache(this.#cached, jwk, alg);
  }
};
async function importWithAlgCache(cache2, jwk, alg) {
  const cached = cache2.get(jwk) || cache2.set(jwk, {}).get(jwk);
  if (cached[alg] === void 0) {
    const key = await importJWK({ ...jwk, ext: true }, alg);
    if (key instanceof Uint8Array || key.type !== "public") {
      throw new JWKSInvalid("JSON Web Key Set members must be public keys");
    }
    cached[alg] = key;
  }
  return cached[alg];
}
__name(importWithAlgCache, "importWithAlgCache");
function createLocalJWKSet(jwks) {
  const set = new LocalJWKSet(jwks);
  const localJWKSet = /* @__PURE__ */ __name(async (protectedHeader, token) => set.getKey(protectedHeader, token), "localJWKSet");
  Object.defineProperties(localJWKSet, {
    jwks: {
      value: /* @__PURE__ */ __name(() => structuredClone(set.jwks()), "value"),
      enumerable: false,
      configurable: false,
      writable: false
    }
  });
  return localJWKSet;
}
__name(createLocalJWKSet, "createLocalJWKSet");
function decodeJwt(jwt) {
  if (typeof jwt !== "string")
    throw new JWTInvalid("JWTs must use Compact JWS serialization, JWT must be a string");
  const { 1: payload, length } = jwt.split(".");
  if (length === 5)
    throw new JWTInvalid("Only JWTs using Compact JWS serialization can be decoded");
  if (length !== 3)
    throw new JWTInvalid("Invalid JWT");
  if (!payload)
    throw new JWTInvalid("JWTs must contain a payload");
  let decoded;
  try {
    decoded = decode(payload);
  } catch {
    throw new JWTInvalid("Failed to base64url decode the payload");
  }
  let result;
  try {
    result = JSON.parse(decoder.decode(decoded));
  } catch {
    throw new JWTInvalid("Failed to parse the decoded payload as JSON");
  }
  if (!isObject(result))
    throw new JWTInvalid("Invalid JWT Claims Set");
  return result;
}
__name(decodeJwt, "decodeJwt");
var InvalidSubjectError = class extends Error {
  static {
    __name(this, "InvalidSubjectError");
  }
  constructor() {
    super("Invalid subject");
  }
};
var InvalidRefreshTokenError = class extends Error {
  static {
    __name(this, "InvalidRefreshTokenError");
  }
  constructor() {
    super("Invalid refresh token");
  }
};
var InvalidAccessTokenError = class extends Error {
  static {
    __name(this, "InvalidAccessTokenError");
  }
  constructor() {
    super("Invalid access token");
  }
};
var InvalidAuthorizationCodeError = class extends Error {
  static {
    __name(this, "InvalidAuthorizationCodeError");
  }
  constructor() {
    super("Invalid authorization code");
  }
};
function generateVerifier(length) {
  const buffer = new Uint8Array(length);
  crypto.getRandomValues(buffer);
  return exports_base64url.encode(buffer);
}
__name(generateVerifier, "generateVerifier");
async function generateChallenge(verifier, method) {
  if (method === "plain")
    return verifier;
  const encoder2 = new TextEncoder();
  const data = encoder2.encode(verifier);
  const hash = await crypto.subtle.digest("SHA-256", data);
  return exports_base64url.encode(new Uint8Array(hash));
}
__name(generateChallenge, "generateChallenge");
async function generatePKCE(length = 64) {
  if (length < 43 || length > 128) {
    throw new Error("Code verifier length must be between 43 and 128 characters");
  }
  const verifier = generateVerifier(length);
  const challenge = await generateChallenge(verifier, "S256");
  return {
    verifier,
    challenge,
    method: "S256"
  };
}
__name(generatePKCE, "generatePKCE");
function createClient2(input) {
  const jwksCache = /* @__PURE__ */ new Map();
  const issuerCache = /* @__PURE__ */ new Map();
  const issuer = input.issuer || process.env.OPENAUTH_ISSUER;
  if (!issuer)
    throw new Error("No issuer");
  const f = input.fetch ?? fetch;
  async function getIssuer() {
    const cached = issuerCache.get(issuer);
    if (cached)
      return cached;
    const wellKnown = await (f || fetch)(`${issuer}/.well-known/oauth-authorization-server`).then((r) => r.json());
    issuerCache.set(issuer, wellKnown);
    return wellKnown;
  }
  __name(getIssuer, "getIssuer");
  async function getJWKS() {
    const wk = await getIssuer();
    const cached = jwksCache.get(issuer);
    if (cached)
      return cached;
    const keyset = await (f || fetch)(wk.jwks_uri).then((r) => r.json());
    const result2 = createLocalJWKSet(keyset);
    jwksCache.set(issuer, result2);
    return result2;
  }
  __name(getJWKS, "getJWKS");
  const result = {
    async authorize(redirectURI, response, opts) {
      const result2 = new URL(issuer + "/authorize");
      const challenge = {
        state: crypto.randomUUID()
      };
      result2.searchParams.set("client_id", input.clientID);
      result2.searchParams.set("redirect_uri", redirectURI);
      result2.searchParams.set("response_type", response);
      result2.searchParams.set("state", challenge.state);
      if (opts?.provider)
        result2.searchParams.set("provider", opts.provider);
      if (opts?.pkce && response === "code") {
        const pkce = await generatePKCE();
        result2.searchParams.set("code_challenge_method", "S256");
        result2.searchParams.set("code_challenge", pkce.challenge);
        challenge.verifier = pkce.verifier;
      }
      return {
        challenge,
        url: result2.toString()
      };
    },
    async pkce(redirectURI, opts) {
      const result2 = new URL(issuer + "/authorize");
      if (opts?.provider)
        result2.searchParams.set("provider", opts.provider);
      result2.searchParams.set("client_id", input.clientID);
      result2.searchParams.set("redirect_uri", redirectURI);
      result2.searchParams.set("response_type", "code");
      const pkce = await generatePKCE();
      result2.searchParams.set("code_challenge_method", "S256");
      result2.searchParams.set("code_challenge", pkce.challenge);
      return [pkce.verifier, result2.toString()];
    },
    async exchange(code, redirectURI, verifier) {
      const tokens = await f(issuer + "/token", {
        method: "POST",
        headers: {
          "Content-Type": "application/x-www-form-urlencoded"
        },
        body: new URLSearchParams({
          code,
          redirect_uri: redirectURI,
          grant_type: "authorization_code",
          client_id: input.clientID,
          code_verifier: verifier || ""
        }).toString()
      });
      const json = await tokens.json();
      if (!tokens.ok) {
        return {
          err: new InvalidAuthorizationCodeError()
        };
      }
      return {
        err: false,
        tokens: {
          access: json.access_token,
          refresh: json.refresh_token,
          expiresIn: json.expires_in
        }
      };
    },
    async refresh(refresh, opts) {
      if (opts?.access) {
        const decoded = decodeJwt(opts.access);
        if (!decoded) {
          return {
            err: new InvalidAccessTokenError()
          };
        }
        if ((decoded.exp || 0) > Date.now() / 1e3 + 30) {
          return {
            err: false
          };
        }
      }
      const tokens = await f(issuer + "/token", {
        method: "POST",
        headers: {
          "Content-Type": "application/x-www-form-urlencoded"
        },
        body: new URLSearchParams({
          grant_type: "refresh_token",
          refresh_token: refresh
        }).toString()
      });
      const json = await tokens.json();
      if (!tokens.ok) {
        return {
          err: new InvalidRefreshTokenError()
        };
      }
      return {
        err: false,
        tokens: {
          access: json.access_token,
          refresh: json.refresh_token,
          expiresIn: json.expires_in
        }
      };
    },
    async verify(subjects, token, options) {
      const jwks = await getJWKS();
      const expectedAudience = options?.audience ?? input.clientID;
      try {
        const result2 = await jwtVerify(token, jwks, {
          issuer,
          audience: expectedAudience
        });
        const validated = await subjects[result2.payload.type]["~standard"].validate(result2.payload.properties);
        if (!validated.issues && result2.payload.mode === "access")
          return {
            aud: result2.payload.aud,
            subject: {
              type: result2.payload.type,
              properties: validated.value
            }
          };
        return {
          err: new InvalidSubjectError()
        };
      } catch (e) {
        if (e instanceof exports_errors.JWTExpired && options?.refresh) {
          const refreshed = await this.refresh(options.refresh);
          if (refreshed.err)
            return refreshed;
          const verified = await result.verify(subjects, refreshed.tokens.access, {
            refresh: refreshed.tokens.refresh,
            issuer,
            audience: expectedAudience,
            fetch: options?.fetch
          });
          if (verified.err)
            return verified;
          verified.tokens = refreshed.tokens;
          return verified;
        }
        return {
          err: new InvalidAccessTokenError()
        };
      }
    }
  };
  return result;
}
__name(createClient2, "createClient");
function createCookieContent(name, value, options) {
  let cookieString = `${name}=${encodeURIComponent(value)}`;
  if (options?.path) {
    cookieString += `; Path=${options.path}`;
  }
  if (options?.maxAge !== void 0) {
    cookieString += `; Max-Age=${options.maxAge}`;
  }
  if (options?.httpOnly) {
    cookieString += `; HttpOnly`;
  }
  return cookieString;
}
__name(createCookieContent, "createCookieContent");
var fetcher = /* @__PURE__ */ __name(({
  clientID,
  copyID
}) => {
  return async (input, init2) => {
    const headers = new Headers(init2?.headers || {});
    const url = new URL(input.toString());
    url.searchParams.set("client_id", clientID);
    if (copyID) {
      url.searchParams.set("copy_id", copyID);
    }
    headers.append("Cookie", createCookieContent(COOKIE_NAME, clientID, { path: "/" }));
    return await fetch(url.toString(), {
      ...init2,
      headers,
      credentials: "include"
    });
  };
}, "fetcher");
var createClient22 = /* @__PURE__ */ __name(({
  clientID,
  issuer,
  copyID
}) => createClient2({
  clientID,
  issuer,
  fetch: fetcher({ clientID, copyID })
}), "createClient2");
var CallbackError = class extends Error {
  static {
    __name(this, "CallbackError");
  }
  originalError;
  constructor(message2, originalError) {
    super(message2);
    this.originalError = originalError;
    this.name = "CallbackError";
  }
};
var RefreshError = class extends Error {
  static {
    __name(this, "RefreshError");
  }
  originalError;
  type;
  constructor(message2, originalError, type = "refresh_failed") {
    super(message2);
    this.originalError = originalError;
    this.type = type;
    this.name = "RefreshError";
  }
};
var TokenVerificationError = class extends Error {
  static {
    __name(this, "TokenVerificationError");
  }
  originalError;
  constructor(message2, originalError) {
    super(message2);
    this.originalError = originalError;
    this.name = "TokenVerificationError";
  }
};
var TotpError = class extends Error {
  static {
    __name(this, "TotpError");
  }
  type;
  originalError;
  constructor(message2, type, originalError) {
    super(message2);
    this.type = type;
    this.originalError = originalError;
    this.name = "TotpError";
  }
};
var errors_default = {
  CallbackError,
  RefreshError,
  TokenVerificationError,
  TotpError
};
var TOTPClient = class {
  static {
    __name(this, "TOTPClient");
  }
  issuerURI;
  fetch;
  onError;
  elevatedToken = null;
  constructor(issuerURI, fetch22, onError) {
    this.issuerURI = issuerURI;
    this.fetch = fetch22;
    this.onError = onError;
  }
  async getElevatedToken(code) {
    const res = await this.fetch(`${this.issuerURI}/totp/elevate`, {
      method: "POST",
      body: JSON.stringify({ code })
    });
    const data = await res.json();
    if (!res.ok) {
      this.onError(new TotpError(data.error || "Request failed", "request_failed"));
      return null;
    } else if (data.error) {
      const err = new TotpError(data.error_description || "TOTP elevation failed", data.error);
      this.onError(err);
      return err;
    } else if (!data.data) {
      const err = new TotpError("No data returned", "request_failed");
      this.onError(err);
      return err;
    }
    return {
      token: data.data.token,
      fetch: /* @__PURE__ */ __name((input, init2) => this.elevatedFetch.bind(this)({
        input,
        init: init2,
        elevatedToken: data.data.token
      }), "fetch")
    };
  }
  async verify(token) {
    const res = await this.fetch(`${this.issuerURI}/totp/validate`, {
      method: "POST",
      body: JSON.stringify({ token })
    }).then((res2) => res2.json());
    if (!res.success) {
      const err = new TotpError(res.error || "Request failed", "request_failed");
      this.onError(err);
      return err;
    }
    return true;
  }
  async verifyFromRequest(request) {
    const token = request.headers.get("x-elevated-token");
    if (!token) {
      const err = new TotpError("No elevated token found", "request_failed");
      this.onError(err);
      return err;
    }
    return this.verify(token);
  }
  async setupTotp() {
    const res = await this.fetch(`${this.issuerURI}/totp/setup`, {
      method: "POST"
    });
    const body = await res.json();
    if (!res.ok || !body.success) {
      const err = new TotpError(body.error || "Request failed", "request_failed");
      this.onError(err);
      return err;
    }
    return body.data;
  }
  async confirmSetup({
    code,
    onError
  }) {
    const res = await this.fetch(`${this.issuerURI}/totp/confirm`, {
      method: "POST",
      body: JSON.stringify({ code })
    }).then((res2) => res2.json());
    if (!res.success) {
      const err = new TotpError(res.error || "Request failed", "request_failed");
      this.onError(err);
      onError?.(err);
      return false;
    }
    return true;
  }
  elevatedFetch({
    input,
    init: init2,
    elevatedToken
  }) {
    const headers = new Headers(init2?.headers || {});
    headers.set("x-elevated-token", elevatedToken);
    return this.fetch(input, { ...init2, headers });
  }
  removeMFAById(userID) {
    return this.fetch(`${this.issuerURI}/admin/totp/${userID}`, {
      method: "DELETE"
    }).then((res) => res.json());
  }
  removeMFAWithBackupCode(backupCode) {
    return this.fetch(`${this.issuerURI}/totp/remove`, {
      method: "POST",
      body: JSON.stringify({ code: backupCode })
    }).then((res) => res.json());
  }
  removeMFAWithElevatedToken({
    elevatedToken,
    TOTPCode
  }) {
    if (elevatedToken)
      return this.elevatedFetch({
        input: `${this.issuerURI}/totp/remove`,
        init: { method: "POST" },
        elevatedToken
      }).then((res) => res.json());
    else if (TOTPCode)
      return this.getElevatedToken(TOTPCode).then((res) => {
        if (res instanceof TotpError) {
          return {
            success: false,
            error: res.type,
            error_description: res.message
          };
        } else if (!res) {
          return {
            success: false,
            error: "invalid_code",
            error_description: "Invalid TOTP code"
          };
        }
        return this.removeMFAWithElevatedToken({
          elevatedToken: res?.token
        }).then((res2) => res2);
      });
    else
      throw new Error("Either elevatedToken or TOTPCode must be provided");
  }
  resetMFAWithBackupCode(backupCode) {
    return this.fetch(`${this.issuerURI}/totp/reset`, {
      method: "POST",
      body: JSON.stringify({ code: backupCode })
    }).then((res) => res.json());
  }
  verifyTOTPCode(code) {
    return this.fetch(`${this.issuerURI}/totp/verify`, {
      method: "POST",
      body: JSON.stringify({ code })
    }).then((res) => res.json()).then((data) => {
      if (!data.success) {
        const err = new TotpError(data.error || "Request failed", "request_failed");
        this.onError(err);
        return err;
      }
      return true;
    });
  }
};
var MFAmanager = class {
  static {
    __name(this, "MFAmanager");
  }
  totpClient;
  constructor({
    issuerURI,
    fetch: fetch22,
    onError
  }) {
    this.totpClient = new TOTPClient(issuerURI, fetch22, onError);
  }
};
function bufferToBase64URLString(buffer) {
  const bytes = new Uint8Array(buffer);
  let str = "";
  for (const charCode of bytes) {
    str += String.fromCharCode(charCode);
  }
  const base64String = btoa(str);
  return base64String.replace(/\+/g, "-").replace(/\//g, "_").replace(/=/g, "");
}
__name(bufferToBase64URLString, "bufferToBase64URLString");
function base64URLStringToBuffer(base64URLString) {
  const base64 = base64URLString.replace(/-/g, "+").replace(/_/g, "/");
  const padLength = (4 - base64.length % 4) % 4;
  const padded = base64.padEnd(base64.length + padLength, "=");
  const binary = atob(padded);
  const buffer = new ArrayBuffer(binary.length);
  const bytes = new Uint8Array(buffer);
  for (let i = 0; i < binary.length; i++) {
    bytes[i] = binary.charCodeAt(i);
  }
  return buffer;
}
__name(base64URLStringToBuffer, "base64URLStringToBuffer");
function browserSupportsWebAuthn() {
  return _browserSupportsWebAuthnInternals.stubThis(globalThis?.PublicKeyCredential !== void 0 && typeof globalThis.PublicKeyCredential === "function");
}
__name(browserSupportsWebAuthn, "browserSupportsWebAuthn");
var _browserSupportsWebAuthnInternals = {
  stubThis: /* @__PURE__ */ __name((value) => value, "stubThis")
};
function toPublicKeyCredentialDescriptor(descriptor) {
  const { id } = descriptor;
  return {
    ...descriptor,
    id: base64URLStringToBuffer(id),
    transports: descriptor.transports
  };
}
__name(toPublicKeyCredentialDescriptor, "toPublicKeyCredentialDescriptor");
function isValidDomain(hostname) {
  return hostname === "localhost" || /^((xn--[a-z0-9-]+|[a-z0-9]+(-[a-z0-9]+)*)\.)+([a-z]{2,}|xn--[a-z0-9-]+)$/i.test(hostname);
}
__name(isValidDomain, "isValidDomain");
var WebAuthnError = class extends Error {
  static {
    __name(this, "WebAuthnError");
  }
  constructor({ message: message2, code, cause, name }) {
    super(message2, { cause });
    Object.defineProperty(this, "code", {
      enumerable: true,
      configurable: true,
      writable: true,
      value: void 0
    });
    this.name = name ?? cause.name;
    this.code = code;
  }
};
function identifyRegistrationError({ error: error3, options }) {
  const { publicKey } = options;
  if (!publicKey) {
    throw Error("options was missing required publicKey property");
  }
  if (error3.name === "AbortError") {
    if (options.signal instanceof AbortSignal) {
      return new WebAuthnError({
        message: "Registration ceremony was sent an abort signal",
        code: "ERROR_CEREMONY_ABORTED",
        cause: error3
      });
    }
  } else if (error3.name === "ConstraintError") {
    if (publicKey.authenticatorSelection?.requireResidentKey === true) {
      return new WebAuthnError({
        message: "Discoverable credentials were required but no available authenticator supported it",
        code: "ERROR_AUTHENTICATOR_MISSING_DISCOVERABLE_CREDENTIAL_SUPPORT",
        cause: error3
      });
    } else if (options.mediation === "conditional" && publicKey.authenticatorSelection?.userVerification === "required") {
      return new WebAuthnError({
        message: "User verification was required during automatic registration but it could not be performed",
        code: "ERROR_AUTO_REGISTER_USER_VERIFICATION_FAILURE",
        cause: error3
      });
    } else if (publicKey.authenticatorSelection?.userVerification === "required") {
      return new WebAuthnError({
        message: "User verification was required but no available authenticator supported it",
        code: "ERROR_AUTHENTICATOR_MISSING_USER_VERIFICATION_SUPPORT",
        cause: error3
      });
    }
  } else if (error3.name === "InvalidStateError") {
    return new WebAuthnError({
      message: "The authenticator was previously registered",
      code: "ERROR_AUTHENTICATOR_PREVIOUSLY_REGISTERED",
      cause: error3
    });
  } else if (error3.name === "NotAllowedError") {
    return new WebAuthnError({
      message: error3.message,
      code: "ERROR_PASSTHROUGH_SEE_CAUSE_PROPERTY",
      cause: error3
    });
  } else if (error3.name === "NotSupportedError") {
    const validPubKeyCredParams = publicKey.pubKeyCredParams.filter((param) => param.type === "public-key");
    if (validPubKeyCredParams.length === 0) {
      return new WebAuthnError({
        message: 'No entry in pubKeyCredParams was of type "public-key"',
        code: "ERROR_MALFORMED_PUBKEYCREDPARAMS",
        cause: error3
      });
    }
    return new WebAuthnError({
      message: "No available authenticator supported any of the specified pubKeyCredParams algorithms",
      code: "ERROR_AUTHENTICATOR_NO_SUPPORTED_PUBKEYCREDPARAMS_ALG",
      cause: error3
    });
  } else if (error3.name === "SecurityError") {
    const effectiveDomain = globalThis.location.hostname;
    if (!isValidDomain(effectiveDomain)) {
      return new WebAuthnError({
        message: `${globalThis.location.hostname} is an invalid domain`,
        code: "ERROR_INVALID_DOMAIN",
        cause: error3
      });
    } else if (publicKey.rp.id !== effectiveDomain) {
      return new WebAuthnError({
        message: `The RP ID "${publicKey.rp.id}" is invalid for this domain`,
        code: "ERROR_INVALID_RP_ID",
        cause: error3
      });
    }
  } else if (error3.name === "TypeError") {
    if (publicKey.user.id.byteLength < 1 || publicKey.user.id.byteLength > 64) {
      return new WebAuthnError({
        message: "User ID was not between 1 and 64 characters",
        code: "ERROR_INVALID_USER_ID_LENGTH",
        cause: error3
      });
    }
  } else if (error3.name === "UnknownError") {
    return new WebAuthnError({
      message: "The authenticator was unable to process the specified options, or could not create a new credential",
      code: "ERROR_AUTHENTICATOR_GENERAL_ERROR",
      cause: error3
    });
  }
  return error3;
}
__name(identifyRegistrationError, "identifyRegistrationError");
var BaseWebAuthnAbortService = class {
  static {
    __name(this, "BaseWebAuthnAbortService");
  }
  constructor() {
    Object.defineProperty(this, "controller", {
      enumerable: true,
      configurable: true,
      writable: true,
      value: void 0
    });
  }
  createNewAbortSignal() {
    if (this.controller) {
      const abortError = new Error("Cancelling existing WebAuthn API call for new one");
      abortError.name = "AbortError";
      this.controller.abort(abortError);
    }
    const newController = new AbortController();
    this.controller = newController;
    return newController.signal;
  }
  cancelCeremony() {
    if (this.controller) {
      const abortError = new Error("Manually cancelling existing WebAuthn API call");
      abortError.name = "AbortError";
      this.controller.abort(abortError);
      this.controller = void 0;
    }
  }
};
var WebAuthnAbortService = new BaseWebAuthnAbortService();
var attachments = ["cross-platform", "platform"];
function toAuthenticatorAttachment(attachment) {
  if (!attachment) {
    return;
  }
  if (attachments.indexOf(attachment) < 0) {
    return;
  }
  return attachment;
}
__name(toAuthenticatorAttachment, "toAuthenticatorAttachment");
async function startRegistration(options) {
  if (!options.optionsJSON && options.challenge) {
    console.warn("startRegistration() was not called correctly. It will try to continue with the provided options, but this call should be refactored to use the expected call structure instead. See https://simplewebauthn.dev/docs/packages/browser#typeerror-cannot-read-properties-of-undefined-reading-challenge for more information.");
    options = { optionsJSON: options };
  }
  const { optionsJSON, useAutoRegister = false } = options;
  if (!browserSupportsWebAuthn()) {
    throw new Error("WebAuthn is not supported in this browser");
  }
  const publicKey = {
    ...optionsJSON,
    challenge: base64URLStringToBuffer(optionsJSON.challenge),
    user: {
      ...optionsJSON.user,
      id: base64URLStringToBuffer(optionsJSON.user.id)
    },
    excludeCredentials: optionsJSON.excludeCredentials?.map(toPublicKeyCredentialDescriptor)
  };
  const createOptions = {};
  if (useAutoRegister) {
    createOptions.mediation = "conditional";
  }
  createOptions.publicKey = publicKey;
  createOptions.signal = WebAuthnAbortService.createNewAbortSignal();
  let credential;
  try {
    credential = await navigator.credentials.create(createOptions);
  } catch (err) {
    throw identifyRegistrationError({ error: err, options: createOptions });
  }
  if (!credential) {
    throw new Error("Registration was not completed");
  }
  const { id, rawId, response, type } = credential;
  let transports = void 0;
  if (typeof response.getTransports === "function") {
    transports = response.getTransports();
  }
  let responsePublicKeyAlgorithm = void 0;
  if (typeof response.getPublicKeyAlgorithm === "function") {
    try {
      responsePublicKeyAlgorithm = response.getPublicKeyAlgorithm();
    } catch (error3) {
      warnOnBrokenImplementation("getPublicKeyAlgorithm()", error3);
    }
  }
  let responsePublicKey = void 0;
  if (typeof response.getPublicKey === "function") {
    try {
      const _publicKey = response.getPublicKey();
      if (_publicKey !== null) {
        responsePublicKey = bufferToBase64URLString(_publicKey);
      }
    } catch (error3) {
      warnOnBrokenImplementation("getPublicKey()", error3);
    }
  }
  let responseAuthenticatorData;
  if (typeof response.getAuthenticatorData === "function") {
    try {
      responseAuthenticatorData = bufferToBase64URLString(response.getAuthenticatorData());
    } catch (error3) {
      warnOnBrokenImplementation("getAuthenticatorData()", error3);
    }
  }
  return {
    id,
    rawId: bufferToBase64URLString(rawId),
    response: {
      attestationObject: bufferToBase64URLString(response.attestationObject),
      clientDataJSON: bufferToBase64URLString(response.clientDataJSON),
      transports,
      publicKeyAlgorithm: responsePublicKeyAlgorithm,
      publicKey: responsePublicKey,
      authenticatorData: responseAuthenticatorData
    },
    type,
    clientExtensionResults: credential.getClientExtensionResults(),
    authenticatorAttachment: toAuthenticatorAttachment(credential.authenticatorAttachment)
  };
}
__name(startRegistration, "startRegistration");
function warnOnBrokenImplementation(methodName, cause) {
  console.warn(`The browser extension that intercepted this WebAuthn API call incorrectly implemented ${methodName}. You should report this error to them.
`, cause);
}
__name(warnOnBrokenImplementation, "warnOnBrokenImplementation");
function browserSupportsWebAuthnAutofill() {
  if (!browserSupportsWebAuthn()) {
    return _browserSupportsWebAuthnAutofillInternals.stubThis(new Promise((resolve) => resolve(false)));
  }
  const globalPublicKeyCredential = globalThis.PublicKeyCredential;
  if (globalPublicKeyCredential?.isConditionalMediationAvailable === void 0) {
    return _browserSupportsWebAuthnAutofillInternals.stubThis(new Promise((resolve) => resolve(false)));
  }
  return _browserSupportsWebAuthnAutofillInternals.stubThis(globalPublicKeyCredential.isConditionalMediationAvailable());
}
__name(browserSupportsWebAuthnAutofill, "browserSupportsWebAuthnAutofill");
var _browserSupportsWebAuthnAutofillInternals = {
  stubThis: /* @__PURE__ */ __name((value) => value, "stubThis")
};
function identifyAuthenticationError({ error: error3, options }) {
  const { publicKey } = options;
  if (!publicKey) {
    throw Error("options was missing required publicKey property");
  }
  if (error3.name === "AbortError") {
    if (options.signal instanceof AbortSignal) {
      return new WebAuthnError({
        message: "Authentication ceremony was sent an abort signal",
        code: "ERROR_CEREMONY_ABORTED",
        cause: error3
      });
    }
  } else if (error3.name === "NotAllowedError") {
    return new WebAuthnError({
      message: error3.message,
      code: "ERROR_PASSTHROUGH_SEE_CAUSE_PROPERTY",
      cause: error3
    });
  } else if (error3.name === "SecurityError") {
    const effectiveDomain = globalThis.location.hostname;
    if (!isValidDomain(effectiveDomain)) {
      return new WebAuthnError({
        message: `${globalThis.location.hostname} is an invalid domain`,
        code: "ERROR_INVALID_DOMAIN",
        cause: error3
      });
    } else if (publicKey.rpId !== effectiveDomain) {
      return new WebAuthnError({
        message: `The RP ID "${publicKey.rpId}" is invalid for this domain`,
        code: "ERROR_INVALID_RP_ID",
        cause: error3
      });
    }
  } else if (error3.name === "UnknownError") {
    return new WebAuthnError({
      message: "The authenticator was unable to process the specified options, or could not create a new assertion signature",
      code: "ERROR_AUTHENTICATOR_GENERAL_ERROR",
      cause: error3
    });
  }
  return error3;
}
__name(identifyAuthenticationError, "identifyAuthenticationError");
async function startAuthentication(options) {
  if (!options.optionsJSON && options.challenge) {
    console.warn("startAuthentication() was not called correctly. It will try to continue with the provided options, but this call should be refactored to use the expected call structure instead. See https://simplewebauthn.dev/docs/packages/browser#typeerror-cannot-read-properties-of-undefined-reading-challenge for more information.");
    options = { optionsJSON: options };
  }
  const { optionsJSON, useBrowserAutofill = false, verifyBrowserAutofillInput = true } = options;
  if (!browserSupportsWebAuthn()) {
    throw new Error("WebAuthn is not supported in this browser");
  }
  let allowCredentials;
  if (optionsJSON.allowCredentials?.length !== 0) {
    allowCredentials = optionsJSON.allowCredentials?.map(toPublicKeyCredentialDescriptor);
  }
  const publicKey = {
    ...optionsJSON,
    challenge: base64URLStringToBuffer(optionsJSON.challenge),
    allowCredentials
  };
  const getOptions = {};
  if (useBrowserAutofill) {
    if (!await browserSupportsWebAuthnAutofill()) {
      throw Error("Browser does not support WebAuthn autofill");
    }
    const eligibleInputs = document.querySelectorAll("input[autocomplete$='webauthn']");
    if (eligibleInputs.length < 1 && verifyBrowserAutofillInput) {
      throw Error('No <input> with "webauthn" as the only or last value in its `autocomplete` attribute was detected');
    }
    getOptions.mediation = "conditional";
    publicKey.allowCredentials = [];
  }
  getOptions.publicKey = publicKey;
  getOptions.signal = WebAuthnAbortService.createNewAbortSignal();
  let credential;
  try {
    credential = await navigator.credentials.get(getOptions);
  } catch (err) {
    throw identifyAuthenticationError({ error: err, options: getOptions });
  }
  if (!credential) {
    throw new Error("Authentication was not completed");
  }
  const { id, rawId, response, type } = credential;
  let userHandle = void 0;
  if (response.userHandle) {
    userHandle = bufferToBase64URLString(response.userHandle);
  }
  return {
    id,
    rawId: bufferToBase64URLString(rawId),
    response: {
      authenticatorData: bufferToBase64URLString(response.authenticatorData),
      clientDataJSON: bufferToBase64URLString(response.clientDataJSON),
      signature: bufferToBase64URLString(response.signature),
      userHandle
    },
    type,
    clientExtensionResults: credential.getClientExtensionResults(),
    authenticatorAttachment: toAuthenticatorAttachment(credential.authenticatorAttachment)
  };
}
__name(startAuthentication, "startAuthentication");
var Passkey = class {
  static {
    __name(this, "Passkey");
  }
  issuerURI;
  client;
  constructor(issuerURI, client) {
    this.issuerURI = issuerURI;
    this.client = client;
  }
  async register(options) {
    return this.registerPasskeyAppFlow(options);
  }
  async registerPasskeyAppFlow(options) {
    const { userDisplayName } = options || {};
    try {
      const startRes = await this.client.fetchWithOptions(`${this.issuerURI}/passkey/register/start`, {
        method: "POST",
        body: JSON.stringify({ userDisplayName })
      });
      const startData = await startRes.json();
      if (!startRes.ok || startData.error) {
        throw new Error(startData.error || "Impossible d'initialiser le Passkey.");
      }
      const { challengeId, options: options2 } = startData;
      let attestationResponse;
      try {
        attestationResponse = await startRegistration({
          optionsJSON: options2
        });
      } catch (domError) {
        if (domError instanceof Error && domError.name === "NotAllowedError") {
          return {
            success: false,
            error: "Vous avez annul\xE9 la cr\xE9ation du Passkey."
          };
        }
        if (domError instanceof Error && domError.name === "NotSupportedError") {
          return {
            success: false,
            error: "Cet appareil ne supporte pas les Passkeys."
          };
        }
        throw domError;
      }
      const finishRes = await this.client.fetchWithOptions(`${this.issuerURI}/passkey/register/finish`, {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          challengeId,
          response: attestationResponse
        })
      });
      const finishData = await finishRes.json();
      if (!finishRes.ok || finishData.error) {
        throw new Error(finishData.error || "La validation de la cl\xE9 a \xE9chou\xE9.");
      }
      return { success: true, message: "Passkey enregistr\xE9 avec succ\xE8s !" };
    } catch (error3) {
      return {
        success: false,
        error: error3 instanceof Error ? error3.message : "Une erreur inattendue est survenue."
      };
    }
  }
  login() {
    return this.client.login({
      provider: "passkey"
    });
  }
  async flowCallback() {
    const challengeData = await this.createFetch("/generate_challenge", {
      credentials: "include"
    }).then(async (r) => {
      if (!r.ok)
        throw new Error(await r.text());
      return await r.json();
    });
    const res = await startAuthentication({
      optionsJSON: challengeData.options
    });
    const verifyRes = await this.createFetch(`/authorize/token/${challengeData.challenge.id}`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      credentials: "include",
      body: JSON.stringify(res)
    });
    if (!verifyRes.ok) {
      const errorText = await verifyRes.text();
      throw new Error(errorText || "Authentication failed");
    }
    const token_access = await verifyRes.json();
    window.location.href = `${this.issuerURI}/passkey/callback/${token_access.token}`;
  }
  createFetch(endpoint, options) {
    const url = new URL(`${this.issuerURI}/passkey${endpoint}`);
    return this.client.fetchWithOptions(url.toString(), options);
  }
};
var UserEndpointValidation = object({
  type: union3([literal("public"), literal("private")]),
  data: optional(any()),
  client_id: string()
});
var defaultSubjectSchema = createSubjects({
  user: object({
    id: string(),
    identifier: string(),
    role: nullable(string()),
    data: any(),
    clientID: string(),
    provider: string()
  })
});
var UserEndpointResponseValidation = object({
  success: boolean(),
  data: optional(object({
    public: nullable(looseObject({})),
    private: nullable(looseObject({})),
    user_id: string(),
    user_identifier: string(),
    userInfo: looseObject({
      provider: string()
    })
  })),
  error: optional(string())
});
var OpenAuthsterClient = class {
  static {
    __name(this, "OpenAuthsterClient");
  }
  openAuthClient;
  expiresAt = null;
  isLoaded = false;
  isAuthenticated = false;
  data = {
    public: {},
    private: {}
  };
  userMeta = {
    user_id: null,
    user_identifier: null
  };
  userInfo = null;
  error = null;
  issuerURI;
  clientID;
  copyID = null;
  secret;
  token = null;
  refreshToken = null;
  redirectURI;
  refreshTimer;
  refreshPromise = null;
  initListeners = /* @__PURE__ */ new Map();
  subject = defaultSubjectSchema;
  authFlowCallbacks;
  onError;
  mfa;
  passkey;
  constructor(props) {
    this.verifyProps(props);
    this.issuerURI = props.issuerURI;
    this.clientID = props.clientID;
    this.copyID = props.copyID ?? null;
    this.openAuthClient = this.createOpenAuthClient();
    this.secret = props.secret;
    this.token = props.token ?? this.getStoredToken();
    this.refreshToken = props.refreshToken ?? this.getStoredRefreshToken();
    this.redirectURI = props.redirectURI;
    if (props.subject) {
      this.subject = props.subject;
    }
    this.authFlowCallbacks = props.authFlowCallbacks ?? {};
    this.onError = props.onError;
    this.mfa = new MFAmanager({
      issuerURI: this.issuerURI,
      fetch: this.fetch.bind(this),
      onError: this.onError ?? (() => {
      })
    });
    this.passkey = new Passkey(this.issuerURI, this);
  }
  async init() {
    return this._init().then(this.triggerUpdate.bind(this)).then(() => this);
  }
  triggerUpdate() {
    return Promise.all(this.initListeners.values().map((callback) => callback(this, this.error || void 0)));
  }
  getUserSession(type) {
    this.ensureReady();
    return this.fetchWithOptions(`${this.issuerURI}/session/${type}`, {
      method: "GET"
    }).then((res) => res.json()).then((_json) => this.parseResponseData(parse26(UserEndpointResponseValidation, _json))).catch((err) => {
      console.error(`Failed to fetch user session: ${err.message}`);
      return new Error(`Failed to fetch user session: ${err.message}`);
    });
  }
  updateUserSession(type, data) {
    this.ensureReady();
    return this.fetchWithOptions(`${this.issuerURI}/session/${type}`, {
      method: "PATCH",
      body: JSON.stringify(data),
      headers: {
        "Content-Type": "application/json"
      }
    }).then((res) => res.json()).then((_json) => this.parseResponseData(parse26(UserEndpointResponseValidation, _json))).catch((err) => new Error(`Failed to update user session: ${err.message}`));
  }
  async login(options) {
    const { autoNavigate = true, copyID, ...authorizedOptions } = options || {};
    if (typeof copyID !== "undefined") {
      this.setCopyID(copyID ?? null);
    }
    const effectiveCopyID = copyID ?? this.copyID ?? void 0;
    return this.openAuthClient.authorize(this.redirectURI, "code", authorizedOptions).then((e) => {
      this.setChallenge(e.challenge);
      const authURL = new URL(e.url);
      const currentURI = new URL(window.location.href);
      const inviteId = currentURI.searchParams.get("invite_id");
      inviteId && authURL.searchParams.set("invite_id", inviteId);
      effectiveCopyID && authURL.searchParams.set("copy_id", effectiveCopyID);
      if (autoNavigate) {
        window.location.href = authURL.toString();
      }
      return authURL.toString();
    });
  }
  logout() {
    this.token = null;
    this.refreshToken = null;
    this.expiresAt = null;
    this.refreshPromise = null;
    this.clearRefreshTimer();
    this.removeToken();
    this.removeRefreshToken();
    this.removeChallenge();
    this.removeStoredExpiresAt();
    this.isAuthenticated = false;
    this.isLoaded = true;
    this.data = {
      public: {},
      private: {}
    };
    return this.triggerUpdate();
  }
  dispose() {
    this.refreshPromise = null;
    this.clearRefreshTimer();
    this.initListeners.clear();
  }
  async callback() {
    const challenge = this.getChallenge();
    const code = this.getCode();
    if (!code)
      return;
    if (!challenge) {
      return Promise.reject(new errors_default.CallbackError("No challenge found in storage."));
    }
    this.removeRefreshToken();
    this.removeToken();
    await this.openAuthClient.exchange(code, this.redirectURI, challenge.verifier).then((tokens) => {
      if (tokens.err)
        throw tokens.err;
      this.updateTokens(tokens);
      this.isAuthenticated = true;
    }).catch((err) => {
      console.error("Error during callback exchange: ", err);
      this.triggerError(new errors_default.CallbackError(`Error during callback exchange: ${err.message}`, err));
    }).finally(() => {
      this.removeChallenge();
      location.search = location.search.replace(/([?&])code=[^&]*&?/, "$1").replace(/([?&])state=[^&]*&?/, "$1").replace(/&$/, "").replace(/^\?&/, "?");
    });
  }
  updateOptions(options) {
    if ("secret" in options) {
      this.secret = options.secret;
    }
    if ("copyID" in options) {
      this.setCopyID(options.copyID ?? null);
    }
  }
  addInitializationListener(key, callback) {
    this.initListeners.set(key, callback);
  }
  removeInitializationListener(key) {
    this.initListeners.delete(key);
  }
  getTokenFromRequest(request) {
    const authHeader = request.headers.get("Authorization");
    if (authHeader?.startsWith("Bearer ")) {
      return authHeader.substring(7);
    }
    const tokenFromCookie = request.headers.get("Cookie")?.split(";").find((cookie) => cookie.trim().startsWith("access_token="))?.replace(/^\s*access_token=/, "")?.trim();
    return tokenFromCookie || null;
  }
  setTokenToCookie() {
    if (typeof window === "undefined" || !this.token)
      throw new Error("Cannot set token to cookie: document is undefined or token is null");
    document.cookie = `access_token=${this.token}; path=/; secure; samesite=lax;`;
  }
  setTokenFromRequest(request) {
    const token = this.getTokenFromRequest(request);
    if (!token) {
      this.token = null;
      this.isAuthenticated = false;
      return Promise.resolve(this);
    }
    return this.verifyToken(token).then((res) => {
      if (res.err) {
        throw new Error("Failed to verify token from request.", {
          cause: res.err
        });
      }
      if (token) {
        this.token = token;
        this.isAuthenticated = true;
      }
      return this;
    });
  }
  async fetch(input, init2) {
    return this.fetchWithOptions(input, init2, { noClientId: true });
  }
  async fetchWithOptions(input, init2, options) {
    const isRequest = typeof input !== "string";
    const inputUrl = isRequest ? input.url : input;
    const { noClientId = false } = options || {};
    const isAbsolute = inputUrl.startsWith("http://") || inputUrl.startsWith("https://");
    const base = isAbsolute ? void 0 : typeof globalThis.window !== "undefined" ? globalThis.window.location.origin : this.issuerURI;
    const url = new URL(inputUrl, base);
    if (!noClientId) {
      url.searchParams.set("client_id", this.clientID);
    }
    const mergedHeaders = new Headers();
    if (isRequest && input.headers) {
      new Headers(input.headers).forEach((value, key) => {
        mergedHeaders.set(key, value);
      });
    }
    if (this.token) {
      if (this.expiresAt && /* @__PURE__ */ new Date() > this.expiresAt) {
        if (!await this.triggerRefresh())
          throw new Error("Token expired and refresh failed");
      }
      mergedHeaders.set("Authorization", `Bearer ${this.token}`);
    }
    if (this.secret) {
      const timestamp = Math.floor(Date.now() / 1e3).toString();
      const signature = await this.generateSignature(timestamp);
      mergedHeaders.set("X-Client-Timestamp", timestamp);
      mergedHeaders.set("X-Client-Signature", signature);
    }
    if (init2?.headers) {
      new Headers(init2.headers).forEach((value, key) => {
        mergedHeaders.set(key, value);
      });
    }
    const requestDefaults = isRequest ? {
      method: input.method,
      body: input.body,
      credentials: input.credentials,
      cache: input.cache,
      redirect: input.redirect,
      referrer: input.referrer,
      integrity: input.integrity,
      keepalive: input.keepalive,
      signal: input.signal
    } : {};
    const authInit = {
      ...requestDefaults,
      ...init2,
      headers: mergedHeaders
    };
    return fetch(url.toString(), authInit);
  }
  clearPublicSession() {
    return this.fetchWithOptions(`${this.issuerURI}/session/public`, {
      method: "DELETE"
    }).then((res) => res.json()).then((_json) => this.parseResponseData(parse26(UserEndpointResponseValidation, _json))).catch((err) => new Error(`Failed to clear public session: ${err.message}`));
  }
  clearPrivateSession() {
    return this.fetchWithOptions(`${this.issuerURI}/session/private`, {
      method: "DELETE"
    }).then((res) => res.json()).then((_json) => this.parseResponseData(parse26(UserEndpointResponseValidation, _json))).catch((err) => new Error(`Failed to clear private session: ${err.message}`));
  }
  getUserById(user_id) {
    return this.fetchWithOptions(`${this.issuerURI}/user/${user_id}`, {
      method: "GET"
    }).then((res) => res.json()).then((_json) => parse26(UserListSchemaValidation, _json)).catch((err) => new Error(`Failed to fetch user by ID: ${err.message}`));
  }
  getManyUserById(ids) {
    const url = new URL(`${this.issuerURI}/users/specific`);
    ids.forEach((id) => {
      url.searchParams.append("user_id", id);
    });
    return this.fetchWithOptions(url.toString(), {
      method: "GET"
    }).then((res) => res.json()).then((_json) => parse26(UserListSchemaValidation, _json)).catch((err) => new Error(`Failed to fetch users by IDs: ${err.message}`));
  }
  getUsers(filters) {
    const url = new URL(`${this.issuerURI}/users`);
    if (filters?.page)
      url.searchParams.set("page", filters.page.toString());
    if (filters?.limit)
      url.searchParams.set("limit", filters.limit.toString());
    return this.fetchWithOptions(url.toString(), {
      method: "GET"
    }).then((res) => res.json()).then((_json) => parse26(UserListSchemaValidation, _json)).catch((err) => new Error(`Failed to fetch users: ${err.message}`));
  }
  deleteUserById(user_id) {
    return this.fetchWithOptions(`${this.issuerURI}/user/${user_id}`, {
      method: "DELETE"
    }).then((res) => res.json()).then((json) => {
      if (!json.success) {
        throw new Error(json.error || "Failed to delete user.");
      }
      return { success: true, error: null };
    }).catch((err) => ({
      success: false,
      error: `Failed to delete user by ID: ${err.message}`
    }));
  }
  async deleteCurrentUser() {
    this.ensureReady();
    return this.fetchWithOptions(`${this.issuerURI}/manage/user`, {
      method: "DELETE"
    }).then((res) => res.json()).then(async (json) => {
      if (!json.success) {
        throw new Error(json.error || "Failed to delete current user.");
      }
      await this.logout();
      return { success: true, error: null };
    }).catch((err) => ({
      success: false,
      error: `Failed to delete current user: ${err.message}`
    }));
  }
  updateUserById(user_id, data) {
    return this.fetchWithOptions(`${this.issuerURI}/user/${user_id}`, {
      method: "PUT",
      body: JSON.stringify(data),
      headers: {
        "Content-Type": "application/json"
      }
    }).then((res) => res.json()).then((json) => {
      if (!json.success) {
        throw new Error(json.error || "Failed to update user.");
      }
      return json.data;
    }).catch((err) => new Error(`Failed to update user by ID: ${err.message}`));
  }
  getToken() {
    return this.token || this.getStoredToken();
  }
  async getMetaData() {
    const token = this.getToken();
    if (!token)
      return null;
    const result = await this.verifyToken(token);
    if (result.err || !result.subject)
      return null;
    const props = result.subject.properties;
    return {
      id: props?.id ?? null,
      identifier: props?.identifier ?? null,
      provider: props?.provider ?? null,
      data: props?.data
    };
  }
  verify(token) {
    const tokenToVerify = token || this.token;
    if (!tokenToVerify) {
      return Promise.reject(new Error("No token available for verification."));
    }
    return this.verifyToken(tokenToVerify).then((res) => {
      if (res.err) {
        console.error("Failed to verify token.", res.err);
        return false;
      }
      if (this.token) {
        this.isAuthenticated = true;
      }
      return true;
    }).catch((err) => {
      console.error(err);
    });
  }
  async triggerRefresh() {
    if (this.refreshPromise) {
      return this.refreshPromise;
    }
    this.refreshPromise = this.runRefreshFlow().finally(() => {
      this.refreshPromise = null;
    });
    return this.refreshPromise;
  }
  async runRefreshFlow() {
    const refreshToken = this.refreshToken || this.getStoredRefreshToken();
    let count3 = 0;
    if (!refreshToken)
      return Promise.resolve(false);
    const refresher = /* @__PURE__ */ __name(async () => {
      try {
        const refreshResult = await this.openAuthClient.refresh(refreshToken);
        if (refreshResult.err)
          throw refreshResult.err;
        this.updateTokens(refreshResult);
        return true;
      } catch (err) {
        console.error("Token refresh failed:", err);
        return false;
      }
    }, "refresher");
    const attemptRefresh = /* @__PURE__ */ __name(async () => {
      return refresher().then((success) => {
        if (!success && count3 < 3) {
          count3++;
          const retryDelay = 2e3 * count3;
          console.warn(`Retrying token refresh in ${retryDelay / 1e3} seconds...`);
          return new Promise((resolve) => setTimeout(resolve, retryDelay)).then(attemptRefresh);
        }
        return success;
      });
    }, "attemptRefresh");
    return attemptRefresh().then((success) => {
      if (!success) {
        if (this.authFlowCallbacks.onLoginRequired) {
          this.authFlowCallbacks.onLoginRequired(this);
        } else {
          this.logout();
        }
      }
      return success;
    });
  }
  verifyToken(token) {
    return this.openAuthClient.verify(this.subject, token);
  }
  async _init() {
    const url = new URLSearchParams(window.location.search);
    const error3 = url.get("error");
    const error_description = url.get("error_description");
    const inviteFlow = url.get("invite_id");
    const flow = url.get("flow");
    if (error3) {
      this.handleAuthError(error3, error_description);
    } else if (this.getCode()) {
      await this.callback();
    } else {
      await this.restoreSession();
    }
    if (flow === "qr") {
      await this.QRauthFlowCallback(url.get("id"));
    } else if (inviteFlow) {
      return this.login({
        autoNavigate: true
      });
    } else if (flow === "passkey") {
      return this.passkey.flowCallback();
    }
    this.isLoaded = true;
  }
  handleAuthError(error3, error_description) {
    console.error("Error from authorization callback: ", {
      error: error3,
      error_description
    });
    this.error = { error: error3, error_description };
  }
  async restoreSession() {
    this.token = this.getStoredToken();
    this.refreshToken = this.getStoredRefreshToken();
    this.expiresAt = this.getStoredExpiresAt();
    if (this.expiresAt && this.expiresAt < /* @__PURE__ */ new Date()) {
      const refreshed = await this.triggerRefresh();
      if (!refreshed) {
        return;
      }
      if (this.expiresAt) {
        this.createResetTimer(this.expiresAt.getTime() - Date.now());
      }
    } else if (this.expiresAt) {
      this.createResetTimer(this.expiresAt.getTime() - Date.now());
    }
    if (this.token) {
      this.isAuthenticated = true;
    }
  }
  async QRauthFlowCallback(id) {
    if (this.authFlowCallbacks.onQRAuthFlowStart && !await this.authFlowCallbacks.onQRAuthFlowStart(this))
      return;
    if (!this.getStoredToken())
      return this.login();
    if (!id) {
      this.error = {
        error: "missing_qr_validation_id",
        error_description: "No QR validation ID provided in URL."
      };
      return this.triggerRefresh();
    }
    const _url = new URL(`${this.issuerURI}/qr/validate`);
    _url.searchParams.set("id", id);
    return this.fetchWithOptions(_url.toString(), {
      method: "POST"
    });
  }
  ensureReady() {
    if (!this.token) {
      throw new Error("Client is not authenticated. Token is missing.");
    }
  }
  parseResponseData(data) {
    if (!data.success || !data.data)
      throw new Error(data.error || "Failed to fetch user session data.");
    if (data.data.private)
      this.data.private = data.data.private;
    if (data.data.public)
      this.data.public = data.data.public;
    if (data.data.user_id)
      this.userMeta.user_id = data.data.user_id;
    if (data.data.user_identifier)
      this.userMeta.user_identifier = data.data.user_identifier;
    if (data.data.userInfo)
      this.userInfo = data.data.userInfo;
    return data.data;
  }
  createResetTimer(expiresInMs) {
    if (!expiresInMs || expiresInMs <= 0)
      return;
    clearTimeout(this.refreshTimer);
    this.refreshTimer = setTimeout(this.triggerRefresh.bind(this), expiresInMs);
  }
  updateTokens(tokens) {
    if (tokens.tokens?.access) {
      this.token = tokens.tokens?.access;
      this.storeToken(tokens.tokens.access);
      if (tokens.tokens?.expiresIn) {
        const expTimeStamp = tokens.tokens?.expiresIn * 1e3 + Date.now();
        this.expiresAt = new Date(expTimeStamp);
        this.storeExpiresAt(expTimeStamp);
        this.createResetTimer(expTimeStamp - Date.now());
      }
    }
    if (tokens.tokens?.refresh) {
      this.refreshToken = tokens.tokens.refresh;
      this.storeRefreshToken(tokens.tokens.refresh);
    }
  }
  clearRefreshTimer() {
    if (this.refreshTimer) {
      clearTimeout(this.refreshTimer);
      this.refreshTimer = void 0;
    }
  }
  createOpenAuthClient() {
    return createClient22({
      clientID: this.clientID,
      issuer: this.issuerURI,
      copyID: this.copyID
    });
  }
  setCopyID(copyID) {
    if (this.copyID === copyID)
      return;
    this.copyID = copyID;
    this.openAuthClient = this.createOpenAuthClient();
  }
  getCode() {
    const params = new URLSearchParams(window.location.search);
    return params.get("code");
  }
  setChallenge(challenge) {
    localStorage.setItem("oa_challenge", JSON.stringify(challenge));
  }
  getChallenge() {
    const challenge = localStorage.getItem("oa_challenge");
    return challenge ? JSON.parse(challenge) : null;
  }
  removeChallenge() {
    localStorage.removeItem("oa_challenge");
  }
  getStoredToken() {
    return typeof window !== "undefined" ? localStorage.getItem("oa_token") : null;
  }
  storeToken(token) {
    if (typeof window !== "undefined") {
      localStorage.setItem("oa_token", token);
    }
  }
  removeToken() {
    if (typeof window !== "undefined") {
      localStorage.removeItem("oa_token");
    }
  }
  getStoredRefreshToken() {
    return typeof window !== "undefined" ? localStorage.getItem("oa_refresh_token") : null;
  }
  storeRefreshToken(refreshToken) {
    if (typeof window !== "undefined") {
      localStorage.setItem("oa_refresh_token", refreshToken);
    }
  }
  removeRefreshToken() {
    if (typeof window !== "undefined") {
      localStorage.removeItem("oa_refresh_token");
    }
  }
  getStoredExpiresAt() {
    const stored = typeof window !== "undefined" ? localStorage.getItem("oa_expires_at") : null;
    if (!stored)
      return null;
    const expiresAt = parseInt(stored, 10);
    return Number.isNaN(expiresAt) ? null : new Date(expiresAt);
  }
  storeExpiresAt(expiresAt) {
    if (typeof window === "undefined")
      return;
    localStorage.setItem("oa_expires_at", expiresAt.toString());
  }
  removeStoredExpiresAt() {
    if (typeof window === "undefined")
      return;
    localStorage.removeItem("oa_expires_at");
  }
  triggerError(err) {
    this.onError?.(err);
    throw err;
  }
  async generateSignature(timestamp) {
    const encoder2 = new TextEncoder();
    const keyData = encoder2.encode(this.secret);
    const messageData = encoder2.encode(`${timestamp}:${this.clientID}`);
    const key = await crypto.subtle.importKey("raw", keyData, { name: "HMAC", hash: "SHA-256" }, false, ["sign"]);
    const signature = await crypto.subtle.sign("HMAC", key, messageData);
    return Array.from(new Uint8Array(signature)).map((b) => b.toString(16).padStart(2, "0")).join("");
  }
  verifyProps(props) {
    if (!props.issuerURI.startsWith("http")) {
      throw new Error("Invalid issuer URI. Must start with http or https.");
    }
    if (props.secret && typeof window !== "undefined") {
      console.warn("Warning: Initializing client with a secret in a browser environment can lead to security risks. Make sure to only use secrets in server-side environments.");
    }
  }
};
function createOpenAuthsterClient(props) {
  return new OpenAuthsterClient(props);
}
__name(createOpenAuthsterClient, "createOpenAuthsterClient");
var subject = defaultSubjectSchema;
var createClient3 = /* @__PURE__ */ __name(({
  token,
  clientID,
  issuerURI,
  redirectURI,
  secret
} = {}) => createOpenAuthsterClient({
  clientID: clientID ?? process.env.PUBLIC_CLIENT_ID,
  issuerURI: issuerURI ?? process.env.PUBLIC_ISSUER,
  redirectURI: redirectURI ?? process.env.PUBLIC_REDIRECT_URI,
  subject,
  token,
  authFlowCallbacks: {
    onLoginRequired: /* @__PURE__ */ __name(() => {
      console.log("Login required");
    }, "onLoginRequired")
  },
  secret
}), "createClient3");
async function onRequest26(context2) {
  if (context2.env.NODE_ENV === "development") {
    const host = new URL(context2.request.url).hostname;
    if (host !== "localhost" && host !== "127.0.0.1") {
      console.error("Unauthorized request in development environment from host:", host, ". Only localhost and 127.0.0.1 are allowed.", "Set NODE_ENV to production to disable this check.");
      return new Response("Unauthorized", { status: 401 });
    }
    context2.data.client = {
      getMetaData: /* @__PURE__ */ __name(async () => ({
        id: "admin",
        identifier: "admin@example.com",
        provider: "password"
      }), "getMetaData"),
      getUserSession: /* @__PURE__ */ __name(async () => ({
        public: {},
        private: {
          group_ids: ["admin"]
        },
        user_id: "admin",
        provider: "password",
        user_identifier: "admin@example.com"
      }), "getUserSession")
    };
    return await context2.next();
  }
  const auth = await createClient3({
    clientID: context2.env.PUBLIC_CLIENT_ID,
    issuerURI: context2.env.PUBLIC_ISSUER,
    redirectURI: context2.env.PUBLIC_REDIRECT_URI,
    secret: context2.env.WEBUI_SECRET
  }).setTokenFromRequest(context2.request);
  if (!auth.isAuthenticated) {
    return new Response("Unauthorized", { status: 401 });
  }
  context2.data.client = auth;
  try {
    return await context2.next();
  } catch (err) {
    if (err instanceof Error) {
      console.error("Error in API middleware:", err);
      return new Response("Internal Server Error", { status: 500 });
    }
    return new Response("Unknown error", { status: 500 });
  }
}
__name(onRequest26, "onRequest");

// ../.wrangler/tmp/pages-CUS8nI/functionsRoutes-0.9514584509015572.mjs
var routes = [
  {
    routePath: "/api/invites/template/copy/accept",
    mountPath: "/api/invites/template/copy",
    method: "",
    middlewares: [],
    modules: [onRequest]
  },
  {
    routePath: "/api/invites/template/copy/manage",
    mountPath: "/api/invites/template/copy",
    method: "",
    middlewares: [],
    modules: [onRequest2]
  },
  {
    routePath: "/api/invites/template/email/accept",
    mountPath: "/api/invites/template/email",
    method: "",
    middlewares: [],
    modules: [onRequest3]
  },
  {
    routePath: "/api/invites/template/email/manage",
    mountPath: "/api/invites/template/email",
    method: "",
    middlewares: [],
    modules: [onRequest4]
  },
  {
    routePath: "/api/invites/template/ui/accept",
    mountPath: "/api/invites/template/ui",
    method: "",
    middlewares: [],
    modules: [onRequest5]
  },
  {
    routePath: "/api/invites/template/ui/manage",
    mountPath: "/api/invites/template/ui",
    method: "",
    middlewares: [],
    modules: [onRequest6]
  },
  {
    routePath: "/api/invites/project/accept",
    mountPath: "/api/invites/project",
    method: "",
    middlewares: [],
    modules: [onRequest7]
  },
  {
    routePath: "/api/invites/project/manage",
    mountPath: "/api/invites/project",
    method: "",
    middlewares: [],
    modules: [onRequest8]
  },
  {
    routePath: "/api/templates/copy/id",
    mountPath: "/api/templates/copy",
    method: "",
    middlewares: [],
    modules: [onRequest9]
  },
  {
    routePath: "/api/invites/revoke",
    mountPath: "/api/invites",
    method: "",
    middlewares: [],
    modules: [onRequest10]
  },
  {
    routePath: "/api/projects/manage",
    mountPath: "/api/projects",
    method: "",
    middlewares: [],
    modules: [onRequest11]
  },
  {
    routePath: "/api/templates/copy",
    mountPath: "/api/templates/copy",
    method: "",
    middlewares: [],
    modules: [onRequest12]
  },
  {
    routePath: "/api/templates/id",
    mountPath: "/api/templates",
    method: "",
    middlewares: [],
    modules: [onRequest13]
  },
  {
    routePath: "/api/themes/id",
    mountPath: "/api/themes",
    method: "",
    middlewares: [],
    modules: [onRequest14]
  },
  {
    routePath: "/api/users/info",
    mountPath: "/api/users",
    method: "",
    middlewares: [],
    modules: [onRequest15]
  },
  {
    routePath: "/api/dashboard",
    mountPath: "/api/dashboard",
    method: "",
    middlewares: [],
    modules: [onRequest16]
  },
  {
    routePath: "/api/invitelink",
    mountPath: "/api/invitelink",
    method: "",
    middlewares: [],
    modules: [onRequest17]
  },
  {
    routePath: "/api/invites",
    mountPath: "/api/invites",
    method: "",
    middlewares: [],
    modules: [onRequest18]
  },
  {
    routePath: "/api/logs",
    mountPath: "/api/logs",
    method: "",
    middlewares: [],
    modules: [onRequest19]
  },
  {
    routePath: "/api/projects",
    mountPath: "/api/projects",
    method: "",
    middlewares: [],
    modules: [onRequest20]
  },
  {
    routePath: "/api/providers",
    mountPath: "/api/providers",
    method: "",
    middlewares: [],
    modules: [onRequest21]
  },
  {
    routePath: "/api/templates",
    mountPath: "/api/templates",
    method: "",
    middlewares: [],
    modules: [onRequest22]
  },
  {
    routePath: "/api/themes",
    mountPath: "/api/themes",
    method: "",
    middlewares: [],
    modules: [onRequest23]
  },
  {
    routePath: "/api/users",
    mountPath: "/api/users",
    method: "",
    middlewares: [],
    modules: [onRequest24]
  },
  {
    routePath: "/api/webhooks",
    mountPath: "/api/webhooks",
    method: "",
    middlewares: [],
    modules: [onRequest25]
  },
  {
    routePath: "/api",
    mountPath: "/api",
    method: "",
    middlewares: [onRequest26],
    modules: []
  }
];

// ../node_modules/path-to-regexp/dist.es2015/index.js
function lexer(str) {
  var tokens = [];
  var i = 0;
  while (i < str.length) {
    var char = str[i];
    if (char === "*" || char === "+" || char === "?") {
      tokens.push({ type: "MODIFIER", index: i, value: str[i++] });
      continue;
    }
    if (char === "\\") {
      tokens.push({ type: "ESCAPED_CHAR", index: i++, value: str[i++] });
      continue;
    }
    if (char === "{") {
      tokens.push({ type: "OPEN", index: i, value: str[i++] });
      continue;
    }
    if (char === "}") {
      tokens.push({ type: "CLOSE", index: i, value: str[i++] });
      continue;
    }
    if (char === ":") {
      var name = "";
      var j = i + 1;
      while (j < str.length) {
        var code = str.charCodeAt(j);
        if (
          // `0-9`
          code >= 48 && code <= 57 || // `A-Z`
          code >= 65 && code <= 90 || // `a-z`
          code >= 97 && code <= 122 || // `_`
          code === 95
        ) {
          name += str[j++];
          continue;
        }
        break;
      }
      if (!name)
        throw new TypeError("Missing parameter name at ".concat(i));
      tokens.push({ type: "NAME", index: i, value: name });
      i = j;
      continue;
    }
    if (char === "(") {
      var count3 = 1;
      var pattern = "";
      var j = i + 1;
      if (str[j] === "?") {
        throw new TypeError('Pattern cannot start with "?" at '.concat(j));
      }
      while (j < str.length) {
        if (str[j] === "\\") {
          pattern += str[j++] + str[j++];
          continue;
        }
        if (str[j] === ")") {
          count3--;
          if (count3 === 0) {
            j++;
            break;
          }
        } else if (str[j] === "(") {
          count3++;
          if (str[j + 1] !== "?") {
            throw new TypeError("Capturing groups are not allowed at ".concat(j));
          }
        }
        pattern += str[j++];
      }
      if (count3)
        throw new TypeError("Unbalanced pattern at ".concat(i));
      if (!pattern)
        throw new TypeError("Missing pattern at ".concat(i));
      tokens.push({ type: "PATTERN", index: i, value: pattern });
      i = j;
      continue;
    }
    tokens.push({ type: "CHAR", index: i, value: str[i++] });
  }
  tokens.push({ type: "END", index: i, value: "" });
  return tokens;
}
__name(lexer, "lexer");
function parse27(str, options) {
  if (options === void 0) {
    options = {};
  }
  var tokens = lexer(str);
  var _a2 = options.prefixes, prefixes = _a2 === void 0 ? "./" : _a2, _b = options.delimiter, delimiter = _b === void 0 ? "/#?" : _b;
  var result = [];
  var key = 0;
  var i = 0;
  var path = "";
  var tryConsume = /* @__PURE__ */ __name(function(type) {
    if (i < tokens.length && tokens[i].type === type)
      return tokens[i++].value;
  }, "tryConsume");
  var mustConsume = /* @__PURE__ */ __name(function(type) {
    var value2 = tryConsume(type);
    if (value2 !== void 0)
      return value2;
    var _a3 = tokens[i], nextType = _a3.type, index = _a3.index;
    throw new TypeError("Unexpected ".concat(nextType, " at ").concat(index, ", expected ").concat(type));
  }, "mustConsume");
  var consumeText = /* @__PURE__ */ __name(function() {
    var result2 = "";
    var value2;
    while (value2 = tryConsume("CHAR") || tryConsume("ESCAPED_CHAR")) {
      result2 += value2;
    }
    return result2;
  }, "consumeText");
  var isSafe = /* @__PURE__ */ __name(function(value2) {
    for (var _i = 0, delimiter_1 = delimiter; _i < delimiter_1.length; _i++) {
      var char2 = delimiter_1[_i];
      if (value2.indexOf(char2) > -1)
        return true;
    }
    return false;
  }, "isSafe");
  var safePattern = /* @__PURE__ */ __name(function(prefix2) {
    var prev = result[result.length - 1];
    var prevText = prefix2 || (prev && typeof prev === "string" ? prev : "");
    if (prev && !prevText) {
      throw new TypeError('Must have text between two parameters, missing text after "'.concat(prev.name, '"'));
    }
    if (!prevText || isSafe(prevText))
      return "[^".concat(escapeString(delimiter), "]+?");
    return "(?:(?!".concat(escapeString(prevText), ")[^").concat(escapeString(delimiter), "])+?");
  }, "safePattern");
  while (i < tokens.length) {
    var char = tryConsume("CHAR");
    var name = tryConsume("NAME");
    var pattern = tryConsume("PATTERN");
    if (name || pattern) {
      var prefix = char || "";
      if (prefixes.indexOf(prefix) === -1) {
        path += prefix;
        prefix = "";
      }
      if (path) {
        result.push(path);
        path = "";
      }
      result.push({
        name: name || key++,
        prefix,
        suffix: "",
        pattern: pattern || safePattern(prefix),
        modifier: tryConsume("MODIFIER") || ""
      });
      continue;
    }
    var value = char || tryConsume("ESCAPED_CHAR");
    if (value) {
      path += value;
      continue;
    }
    if (path) {
      result.push(path);
      path = "";
    }
    var open = tryConsume("OPEN");
    if (open) {
      var prefix = consumeText();
      var name_1 = tryConsume("NAME") || "";
      var pattern_1 = tryConsume("PATTERN") || "";
      var suffix = consumeText();
      mustConsume("CLOSE");
      result.push({
        name: name_1 || (pattern_1 ? key++ : ""),
        pattern: name_1 && !pattern_1 ? safePattern(prefix) : pattern_1,
        prefix,
        suffix,
        modifier: tryConsume("MODIFIER") || ""
      });
      continue;
    }
    mustConsume("END");
  }
  return result;
}
__name(parse27, "parse");
function match(str, options) {
  var keys = [];
  var re = pathToRegexp(str, keys, options);
  return regexpToFunction(re, keys, options);
}
__name(match, "match");
function regexpToFunction(re, keys, options) {
  if (options === void 0) {
    options = {};
  }
  var _a2 = options.decode, decode2 = _a2 === void 0 ? function(x) {
    return x;
  } : _a2;
  return function(pathname) {
    var m = re.exec(pathname);
    if (!m)
      return false;
    var path = m[0], index = m.index;
    var params = /* @__PURE__ */ Object.create(null);
    var _loop_1 = /* @__PURE__ */ __name(function(i2) {
      if (m[i2] === void 0)
        return "continue";
      var key = keys[i2 - 1];
      if (key.modifier === "*" || key.modifier === "+") {
        params[key.name] = m[i2].split(key.prefix + key.suffix).map(function(value) {
          return decode2(value, key);
        });
      } else {
        params[key.name] = decode2(m[i2], key);
      }
    }, "_loop_1");
    for (var i = 1; i < m.length; i++) {
      _loop_1(i);
    }
    return { path, index, params };
  };
}
__name(regexpToFunction, "regexpToFunction");
function escapeString(str) {
  return str.replace(/([.+*?=^!:${}()[\]|/\\])/g, "\\$1");
}
__name(escapeString, "escapeString");
function flags(options) {
  return options && options.sensitive ? "" : "i";
}
__name(flags, "flags");
function regexpToRegexp(path, keys) {
  if (!keys)
    return path;
  var groupsRegex = /\((?:\?<(.*?)>)?(?!\?)/g;
  var index = 0;
  var execResult = groupsRegex.exec(path.source);
  while (execResult) {
    keys.push({
      // Use parenthesized substring match if available, index otherwise
      name: execResult[1] || index++,
      prefix: "",
      suffix: "",
      modifier: "",
      pattern: ""
    });
    execResult = groupsRegex.exec(path.source);
  }
  return path;
}
__name(regexpToRegexp, "regexpToRegexp");
function arrayToRegexp(paths, keys, options) {
  var parts = paths.map(function(path) {
    return pathToRegexp(path, keys, options).source;
  });
  return new RegExp("(?:".concat(parts.join("|"), ")"), flags(options));
}
__name(arrayToRegexp, "arrayToRegexp");
function stringToRegexp(path, keys, options) {
  return tokensToRegexp(parse27(path, options), keys, options);
}
__name(stringToRegexp, "stringToRegexp");
function tokensToRegexp(tokens, keys, options) {
  if (options === void 0) {
    options = {};
  }
  var _a2 = options.strict, strict = _a2 === void 0 ? false : _a2, _b = options.start, start = _b === void 0 ? true : _b, _c = options.end, end = _c === void 0 ? true : _c, _d = options.encode, encode3 = _d === void 0 ? function(x) {
    return x;
  } : _d, _e = options.delimiter, delimiter = _e === void 0 ? "/#?" : _e, _f = options.endsWith, endsWith = _f === void 0 ? "" : _f;
  var endsWithRe = "[".concat(escapeString(endsWith), "]|$");
  var delimiterRe = "[".concat(escapeString(delimiter), "]");
  var route = start ? "^" : "";
  for (var _i = 0, tokens_1 = tokens; _i < tokens_1.length; _i++) {
    var token = tokens_1[_i];
    if (typeof token === "string") {
      route += escapeString(encode3(token));
    } else {
      var prefix = escapeString(encode3(token.prefix));
      var suffix = escapeString(encode3(token.suffix));
      if (token.pattern) {
        if (keys)
          keys.push(token);
        if (prefix || suffix) {
          if (token.modifier === "+" || token.modifier === "*") {
            var mod = token.modifier === "*" ? "?" : "";
            route += "(?:".concat(prefix, "((?:").concat(token.pattern, ")(?:").concat(suffix).concat(prefix, "(?:").concat(token.pattern, "))*)").concat(suffix, ")").concat(mod);
          } else {
            route += "(?:".concat(prefix, "(").concat(token.pattern, ")").concat(suffix, ")").concat(token.modifier);
          }
        } else {
          if (token.modifier === "+" || token.modifier === "*") {
            throw new TypeError('Can not repeat "'.concat(token.name, '" without a prefix and suffix'));
          }
          route += "(".concat(token.pattern, ")").concat(token.modifier);
        }
      } else {
        route += "(?:".concat(prefix).concat(suffix, ")").concat(token.modifier);
      }
    }
  }
  if (end) {
    if (!strict)
      route += "".concat(delimiterRe, "?");
    route += !options.endsWith ? "$" : "(?=".concat(endsWithRe, ")");
  } else {
    var endToken = tokens[tokens.length - 1];
    var isEndDelimited = typeof endToken === "string" ? delimiterRe.indexOf(endToken[endToken.length - 1]) > -1 : endToken === void 0;
    if (!strict) {
      route += "(?:".concat(delimiterRe, "(?=").concat(endsWithRe, "))?");
    }
    if (!isEndDelimited) {
      route += "(?=".concat(delimiterRe, "|").concat(endsWithRe, ")");
    }
  }
  return new RegExp(route, flags(options));
}
__name(tokensToRegexp, "tokensToRegexp");
function pathToRegexp(path, keys, options) {
  if (path instanceof RegExp)
    return regexpToRegexp(path, keys);
  if (Array.isArray(path))
    return arrayToRegexp(path, keys, options);
  return stringToRegexp(path, keys, options);
}
__name(pathToRegexp, "pathToRegexp");

// ../node_modules/wrangler/templates/pages-template-worker.ts
var escapeRegex = /[.+?^${}()|[\]\\]/g;
function* executeRequest(request) {
  const requestPath = new URL(request.url).pathname;
  for (const route of [...routes].reverse()) {
    if (route.method && route.method !== request.method) {
      continue;
    }
    const routeMatcher = match(route.routePath.replace(escapeRegex, "\\$&"), {
      end: false
    });
    const mountMatcher = match(route.mountPath.replace(escapeRegex, "\\$&"), {
      end: false
    });
    const matchResult = routeMatcher(requestPath);
    const mountMatchResult = mountMatcher(requestPath);
    if (matchResult && mountMatchResult) {
      for (const handler of route.middlewares.flat()) {
        yield {
          handler,
          params: matchResult.params,
          path: mountMatchResult.path
        };
      }
    }
  }
  for (const route of routes) {
    if (route.method && route.method !== request.method) {
      continue;
    }
    const routeMatcher = match(route.routePath.replace(escapeRegex, "\\$&"), {
      end: true
    });
    const mountMatcher = match(route.mountPath.replace(escapeRegex, "\\$&"), {
      end: false
    });
    const matchResult = routeMatcher(requestPath);
    const mountMatchResult = mountMatcher(requestPath);
    if (matchResult && mountMatchResult && route.modules.length) {
      for (const handler of route.modules.flat()) {
        yield {
          handler,
          params: matchResult.params,
          path: matchResult.path
        };
      }
      break;
    }
  }
}
__name(executeRequest, "executeRequest");
var pages_template_worker_default = {
  async fetch(originalRequest, env2, workerContext) {
    let request = originalRequest;
    const handlerIterator = executeRequest(request);
    let data = {};
    let isFailOpen = false;
    const next = /* @__PURE__ */ __name(async (input, init2) => {
      if (input !== void 0) {
        let url = input;
        if (typeof input === "string") {
          url = new URL(input, request.url).toString();
        }
        request = new Request(url, init2);
      }
      const result = handlerIterator.next();
      if (result.done === false) {
        const { handler, params, path } = result.value;
        const context2 = {
          request: new Request(request.clone()),
          functionPath: path,
          next,
          params,
          get data() {
            return data;
          },
          set data(value) {
            if (typeof value !== "object" || value === null) {
              throw new Error("context.data must be an object");
            }
            data = value;
          },
          env: env2,
          waitUntil: workerContext.waitUntil.bind(workerContext),
          passThroughOnException: /* @__PURE__ */ __name(() => {
            isFailOpen = true;
          }, "passThroughOnException")
        };
        const response = await handler(context2);
        if (!(response instanceof Response)) {
          throw new Error("Your Pages function should return a Response");
        }
        return cloneResponse(response);
      } else if ("ASSETS") {
        const response = await env2["ASSETS"].fetch(request);
        return cloneResponse(response);
      } else {
        const response = await fetch(request);
        return cloneResponse(response);
      }
    }, "next");
    try {
      return await next();
    } catch (error3) {
      if (isFailOpen) {
        const response = await env2["ASSETS"].fetch(request);
        return cloneResponse(response);
      }
      throw error3;
    }
  }
};
var cloneResponse = /* @__PURE__ */ __name((response) => (
  // https://fetch.spec.whatwg.org/#null-body-status
  new Response(
    [101, 204, 205, 304].includes(response.status) ? null : response.body,
    response
  )
), "cloneResponse");

// ../node_modules/wrangler/templates/middleware/middleware-ensure-req-body-drained.ts
var drainBody = /* @__PURE__ */ __name(async (request, env2, _ctx, middlewareCtx) => {
  try {
    return await middlewareCtx.next(request, env2);
  } finally {
    try {
      if (request.body !== null && !request.bodyUsed) {
        const reader = request.body.getReader();
        while (!(await reader.read()).done) {
        }
      }
    } catch (e) {
      console.error("Failed to drain the unused request body.", e);
    }
  }
}, "drainBody");
var middleware_ensure_req_body_drained_default = drainBody;

// ../node_modules/wrangler/templates/middleware/middleware-miniflare3-json-error.ts
function reduceError(e) {
  return {
    name: e?.name,
    message: e?.message ?? String(e),
    stack: e?.stack,
    cause: e?.cause === void 0 ? void 0 : reduceError(e.cause)
  };
}
__name(reduceError, "reduceError");
var jsonError = /* @__PURE__ */ __name(async (request, env2, _ctx, middlewareCtx) => {
  try {
    return await middlewareCtx.next(request, env2);
  } catch (e) {
    const error3 = reduceError(e);
    return Response.json(error3, {
      status: 500,
      headers: { "MF-Experimental-Error-Stack": "true" }
    });
  }
}, "jsonError");
var middleware_miniflare3_json_error_default = jsonError;

// ../.wrangler/tmp/bundle-CsgmOk/middleware-insertion-facade.js
var __INTERNAL_WRANGLER_MIDDLEWARE__ = [
  middleware_ensure_req_body_drained_default,
  middleware_miniflare3_json_error_default
];
var middleware_insertion_facade_default = pages_template_worker_default;

// ../node_modules/wrangler/templates/middleware/common.ts
var __facade_middleware__ = [];
function __facade_register__(...args) {
  __facade_middleware__.push(...args.flat());
}
__name(__facade_register__, "__facade_register__");
function __facade_invokeChain__(request, env2, ctx, dispatch, middlewareChain) {
  const [head, ...tail] = middlewareChain;
  const middlewareCtx = {
    dispatch,
    next(newRequest, newEnv) {
      return __facade_invokeChain__(newRequest, newEnv, ctx, dispatch, tail);
    }
  };
  return head(request, env2, ctx, middlewareCtx);
}
__name(__facade_invokeChain__, "__facade_invokeChain__");
function __facade_invoke__(request, env2, ctx, dispatch, finalMiddleware) {
  return __facade_invokeChain__(request, env2, ctx, dispatch, [
    ...__facade_middleware__,
    finalMiddleware
  ]);
}
__name(__facade_invoke__, "__facade_invoke__");

// ../.wrangler/tmp/bundle-CsgmOk/middleware-loader.entry.ts
var __Facade_ScheduledController__ = class ___Facade_ScheduledController__ {
  constructor(scheduledTime, cron, noRetry) {
    this.scheduledTime = scheduledTime;
    this.cron = cron;
    this.#noRetry = noRetry;
  }
  static {
    __name(this, "__Facade_ScheduledController__");
  }
  #noRetry;
  noRetry() {
    if (!(this instanceof ___Facade_ScheduledController__)) {
      throw new TypeError("Illegal invocation");
    }
    this.#noRetry();
  }
};
function wrapExportedHandler(worker) {
  if (__INTERNAL_WRANGLER_MIDDLEWARE__ === void 0 || __INTERNAL_WRANGLER_MIDDLEWARE__.length === 0) {
    return worker;
  }
  for (const middleware of __INTERNAL_WRANGLER_MIDDLEWARE__) {
    __facade_register__(middleware);
  }
  const fetchDispatcher = /* @__PURE__ */ __name(function(request, env2, ctx) {
    if (worker.fetch === void 0) {
      throw new Error("Handler does not export a fetch() function.");
    }
    return worker.fetch(request, env2, ctx);
  }, "fetchDispatcher");
  return {
    ...worker,
    fetch(request, env2, ctx) {
      const dispatcher = /* @__PURE__ */ __name(function(type, init2) {
        if (type === "scheduled" && worker.scheduled !== void 0) {
          const controller = new __Facade_ScheduledController__(
            Date.now(),
            init2.cron ?? "",
            () => {
            }
          );
          return worker.scheduled(controller, env2, ctx);
        }
      }, "dispatcher");
      return __facade_invoke__(request, env2, ctx, dispatcher, fetchDispatcher);
    }
  };
}
__name(wrapExportedHandler, "wrapExportedHandler");
function wrapWorkerEntrypoint(klass) {
  if (__INTERNAL_WRANGLER_MIDDLEWARE__ === void 0 || __INTERNAL_WRANGLER_MIDDLEWARE__.length === 0) {
    return klass;
  }
  for (const middleware of __INTERNAL_WRANGLER_MIDDLEWARE__) {
    __facade_register__(middleware);
  }
  return class extends klass {
    #fetchDispatcher = /* @__PURE__ */ __name((request, env2, ctx) => {
      this.env = env2;
      this.ctx = ctx;
      if (super.fetch === void 0) {
        throw new Error("Entrypoint class does not define a fetch() function.");
      }
      return super.fetch(request);
    }, "#fetchDispatcher");
    #dispatcher = /* @__PURE__ */ __name((type, init2) => {
      if (type === "scheduled" && super.scheduled !== void 0) {
        const controller = new __Facade_ScheduledController__(
          Date.now(),
          init2.cron ?? "",
          () => {
          }
        );
        return super.scheduled(controller);
      }
    }, "#dispatcher");
    fetch(request) {
      return __facade_invoke__(
        request,
        this.env,
        this.ctx,
        this.#dispatcher,
        this.#fetchDispatcher
      );
    }
  };
}
__name(wrapWorkerEntrypoint, "wrapWorkerEntrypoint");
var WRAPPED_ENTRY;
if (typeof middleware_insertion_facade_default === "object") {
  WRAPPED_ENTRY = wrapExportedHandler(middleware_insertion_facade_default);
} else if (typeof middleware_insertion_facade_default === "function") {
  WRAPPED_ENTRY = wrapWorkerEntrypoint(middleware_insertion_facade_default);
}
var middleware_loader_entry_default = WRAPPED_ENTRY;
export {
  __INTERNAL_WRANGLER_MIDDLEWARE__,
  middleware_loader_entry_default as default
};
//# sourceMappingURL=functionsWorker-0.544773209036698.mjs.map
